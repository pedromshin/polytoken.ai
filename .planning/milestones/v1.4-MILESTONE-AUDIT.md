---
milestone: v1.4
audited: 2026-07-07T03:20:00Z
status: tech_debt
scores:
  requirements: 23/23
  phases: 3/3
  integration: 18/18
  flows: 2/2
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 26-zero-dependency-contract-fixes-backlog-polish
    items:
      - "Human visual verification deferred (light/dark: React Flow chrome, node differentiation, studio dark-mode colors, empty-state/button-weight before-after) — user live-testing is the ongoing check; scripts in 26-VERIFICATION.md"
  - phase: 27-adopted-external-design-picks
    items:
      - "Human visual verification deferred (ring sweep, 3 reveal transitions, OS reduced-motion behavior) — 27-VERIFICATION.md"
      - "Inert font-medium remains in UNUSED @nauta/ui primitives (CommandDialog wrapper; SidebarGroupLabel; SidebarMenuBadge) — zero live consumers on /chat or /studio; sweep if those primitives are ever mounted"
  - phase: 28-design-system-token-upgrades
    items:
      - "Human visual verification deferred (panel mount entrance, Studio stagger, rail legibility over live canvas after blur removal, light/dark max-drift gestalt) — 28-VERIFICATION.md"
      - "chart-1..5 registered + teal-rebased but zero live chart consumers (forward-looking by design); chart-4 light-mode 2.60:1 vs the 3:1 non-text guideline — codify a check when the first real chart lands"
      - "Dark-mode chart-2/chart-3 pairwise distinguishability marginal (7pt ΔL/14° ΔHue) — widen chart-3 hue 6-8° if a real chart reads too similar"
carried_forward:
  - "999.3 connected-env verification + measurement (live Bedrock + browser) — unchanged from v1.2/v1.3 closes"
  - "salvage/surface truncated chat tool calls (todo 2026-07-06, found live during this milestone's testing; cap raised 4096→12000 as interim fix e501e57)"
---

# Milestone v1.4 — Chat & Studio Design Uplift — Audit

**Audited:** 2026-07-07 · **Status:** `tech_debt` (no blockers; deferred items aggregated below)
**Scope:** Phases 26–28 (15 plans, 23 requirements FIX-01..11 / POLISH-01..02 / ADOPT-01..05 / TOKEN-01..05)

## Requirements Coverage — 23/23 satisfied

Three-source cross-reference (VERIFICATION.md tables × SUMMARY.md `requirements_completed`
frontmatter × REQUIREMENTS.md traceability): every REQ-ID is `[x]`, traceability `Complete`, listed
in at least one SUMMARY, and code-verified by its phase VERIFICATION. No orphans, no partials.

| Range | Phase | Verification | Notes |
|-------|-------|--------------|-------|
| FIX-01..11, POLISH-01..02 | 26 | human_needed (5/5 code-verified) | 13/13 |
| ADOPT-01..05 | 27 | human_needed (5/5 code-verified) | ADOPT-05 amended: hand-authored (transitions.dev license-blocked) |
| TOKEN-01..05 | 28 | human_needed (4/4 code-verified, after gap closure 69c3afa) | sidebar family registered post-initial-report |

`human_needed` on all three phases means: zero code gaps; only real-browser visual/OS-motion checks
remain, per this project's long-standing connected-env deferral pattern (v1.2/v1.3 precedent). The
user is actively live-testing this build — two real defects found that way were fixed same-day
(chat output-token truncation e501e57; globals.css comment self-termination 64f3cbc).

## Cross-Phase Integration — 18/18 seams wired (after audit closures)

gsd-integration-checker traced all shared-component, CSS-utility, and token-value seams across
26→27→28. Initial report: 15/18 with 1 BLOCKER + 2 warnings; all three closed during this audit:

- **BLOCKER closed (e9faa55):** FIX-02's 2-weight contract leaked through two shared primitives the
  phase-level greps never traced — `packages/ui/src/tabs.tsx` `TabsTrigger` (`font-medium` on the
  /chat Chat↔Canvas toggle + all 5 /studio tab labels; now `font-normal` base +
  `data-[state=active]:font-semibold`) and `packages/ui/src/sidebar.tsx` active-nav
  `data-[active=true]:font-medium` (now `font-semibold`). Residual `font-medium` exists only in
  UNUSED primitives (CommandDialog / SidebarGroupLabel / SidebarMenuBadge — zero live consumers).
- **Warning closed (e9faa55):** `docs/design/product-register-and-bans.md`'s ADOPT-05 section still
  claimed "hand-copied, MIT" — corrected to the verified hand-authored/license-blocked disposition.
- **Warning accepted:** chart-1..5 have no live consumer (disclosed, forward-looking; guarded by
  `token-registration.test.ts` against the unregistered-utility bug class).

## E2E Flows — 2/2 complete

- **/chat:** page → conversation rail → transcript (role chrome, hover affordances, markdown
  scrollbars, composer dock) → Chat↔Canvas toggle → React Flow canvas (token chrome, differentiated
  nodes, mount entrance, generating ring on streaming parts, persistence intact 71/71 canvas tests).
- **/studio:** tabs → catalog (zebra prop table) / sandbox (GeneratingRing + JsonPane) / code-island
  (FileTree preset browser) / history + page-ideas (stagger, JsonPane, token colors).

## Gates at audit time

- 178/178 web vitest (incl. 6 contrast + 4 registration token gates), 477 genui, api-client, and
  email-listener suites green at their respective phase closes; @nauta/web + @nauta/ui typecheck
  clean; Tailwind compile clean (no warnings); zero new npm dependencies across the milestone;
  `spec-renderer.tsx`/`GenuiPartBoundary`/`InteractiveWidgetBoundary` untouched (verified per phase
  via git range diffs); teal `--primary: 164 39% 22%` byte-identical.

## Verdict

All 23 requirements delivered and integration-verified. Remaining items are deferred
visual/OS-level verifications (listed per phase above) and forward-looking chart-token notes — no
code gaps. Recommended: proceed to `/gsd:complete-milestone v1.4`.
