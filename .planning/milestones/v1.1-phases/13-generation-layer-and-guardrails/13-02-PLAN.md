---
phase: 13-generation-layer-and-guardrails
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/genui-generation-events.ts
  - packages/db/src/schema/index.ts
  - packages/db/migrations/0021_genui_generation_events.sql
  - packages/db/migrations/meta/_journal.json
  - apps/email-listener/app/domain/ports/generation_audit_repository.py
  - apps/email-listener/app/infrastructure/persistence/supabase_generation_audit_repository.py
  - apps/email-listener/tests/infrastructure/test_supabase_generation_audit_repository.py
autonomous: true
requirements: [GEN-05]

user_setup: []

must_haves:
  truths:
    - "Every generation event (intent, model, tokens, attempts, outcome, validation, node/depth count, registry version, latency, importer) can be written to a Postgres table (GEN-05, D-19, success criterion 4)"
    - "The audit table exists in Drizzle/packages/db consistent with where Phase 14's ui_spec_templates will live (D-19)"
    - "A GenerationAuditRepository port + Supabase adapter persist one row per event and never raise into the generation path (D-19 best-effort audit)"
    - "Migration 0021 is generated AND verified to create the table (information_schema check)"
  artifacts:
    - path: "packages/db/src/schema/genui-generation-events.ts"
      provides: "Drizzle table genui_generation_events with the D-19 column set"
      contains: "genui_generation_events"
    - path: "packages/db/migrations/0021_genui_generation_events.sql"
      provides: "DDL creating the audit table"
      contains: "create table"
    - path: "apps/email-listener/app/domain/ports/generation_audit_repository.py"
      provides: "GenerationAuditRepository port (Protocol) + GenerationEvent dataclass"
      contains: "GenerationAuditRepository"
    - path: "apps/email-listener/app/infrastructure/persistence/supabase_generation_audit_repository.py"
      provides: "Supabase adapter that inserts one event row"
  key_links:
    - from: "apps/email-listener/app/infrastructure/persistence/supabase_generation_audit_repository.py"
      to: "genui_generation_events"
      via: "supabase table insert"
      pattern: "genui_generation_events"
    - from: "packages/db/src/schema/index.ts"
      to: "packages/db/src/schema/genui-generation-events.ts"
      via: "export * re-export"
      pattern: "genui-generation-events"
---

<objective>
Create the audit-log foundation for the generation pipeline: a Drizzle/Postgres table `genui_generation_events` (D-19) plus the Python port + Supabase adapter the generator uses to write one row per generation event. This is the seam Phase 14 reads to prove "cache hit → zero new generation entry" (CACHE-02).

This plan has zero file overlap with the TS contract plan (13-01) and no dependency on it, so it runs in the same wave. The migration follows the project's migrations-first deploy discipline (apply to staging+prod before the writer deploys).

Purpose: GEN-05 + success criterion 4 require every generation (intent, model, tokens, outcome) recorded; this builds the durable sink.
Output: Drizzle table + migration 0021, a `GenerationAuditRepository` port + Supabase adapter, and a fake-repo-free DB-less adapter test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-generation-layer-and-guardrails/13-CONTEXT.md
@.planning/research/SAFETY-PITFALLS.md

<interfaces>
<!-- Templates to mirror — use directly. -->

From packages/db/src/schema/knowledge-node-edges.ts (Drizzle table idiom):
- pgTable("name", { id: uuid("id").primaryKey().defaultRandom(), col: text(...).notNull().default(...), createdAt: timestamp("created_at",{withTimezone:true}).notNull().defaultNow() }, (t)=>({ idx: index("idx_...").on(t.col) }))
- export type Row = typeof Table.$inferSelect; export type Insert = typeof Table.$inferInsert;

From packages/db/src/schema/index.ts: append `export * from "./genui-generation-events";` after the last entry.

Migration tooling (MEMORY: gsd-execution-state-handlers): migrations live at packages/db/migrations/ (drizzle.config out=./migrations). Generate via the package's drizzle-kit generate script; the migration is named 0021. Apply to LOCAL Postgres and assert the table exists via information_schema before marking done.

D-19 column set (canonical): intent_hash (text), model_id (text), input_tokens (int), output_tokens (int), attempts (int), outcome (text: ok|fallback|escalated), spec_validation_passed (bool), spec_node_count (int), spec_depth (int), registry_version (text), latency_ms (int), importer_id (uuid), created_at (timestamptz default now). intent stored as a canonical hash, not raw prose (D-19, privacy).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: genui_generation_events Drizzle table + migration 0021</name>
  <read_first>
    - CREATING: packages/db/src/schema/genui-generation-events.ts, packages/db/migrations/0021_genui_generation_events.sql
    - MODIFYING: packages/db/src/schema/index.ts (append re-export), packages/db/migrations/meta/_journal.json (drizzle-kit updates)
    - ANALOG: packages/db/src/schema/knowledge-node-edges.ts (table idiom, indexes, inferred types), migrations/0019_cold_energizer.sql + 0020 (migration shape); STATE.md Phase 11 "[BLOCKING] migration 0019 (generate + apply + assert table exists via information_schema)" is the exact procedure to repeat
    - DECISION: 13-CONTEXT.md D-19 (audit table, column set, lives in packages/db Drizzle consistent with Phase 14 ui_spec_templates; intent hashed not raw), Claude's Discretion "Drizzle/packages/db recommended"
    - RESEARCH: SAFETY-PITFALLS.md §5c (observability/tracing field table), GR-12 (spec storage + audit log), GR-24 (log validation failures as security events)
  </read_first>
  <action>
    Create `genui-generation-events.ts` defining `pgTable("genui_generation_events", {...})` with the D-19 column set: id (uuid pk defaultRandom), intentHash text notNull, modelId text notNull, inputTokens integer notNull default 0, outputTokens integer notNull default 0, attempts integer notNull default 1, outcome text notNull (values ok|fallback|escalated enforced by the writer + a CHECK in the SQL), specValidationPassed boolean notNull, specNodeCount integer, specDepth integer, registryVersion text notNull, latencyMs integer, importerId uuid, createdAt timestamptz notNull defaultNow. Add indexes on createdAt and importerId. Export $inferSelect / $inferInsert types. Append the re-export to schema/index.ts (last position — dependency order). Generate migration 0021 via the packages/db drizzle-kit generate script (name: genui_generation_events); scope it to ONLY this table (mirror the 0013/0019 drift-discipline — strip any re-emitted unrelated DDL, add IF NOT EXISTS where the project uses it). Apply 0021 to the LOCAL Postgres and verify via information_schema that the table + columns exist. Migrations-first discipline: record in the SUMMARY that 0021 must be applied to staging+prod before the generator endpoint deploys.
  </action>
  <verify>
    <automated>cd packages/db && pnpm typecheck && grep -c "genui_generation_events" migrations/0021_genui_generation_events.sql</automated>
  </verify>
  <acceptance_criteria>
    - packages/db/src/schema/genui-generation-events.ts compiles (`pnpm typecheck` exit 0) and exports the table + inferred types.
    - migrations/0021_genui_generation_events.sql contains `create table` ... `genui_generation_events` with all D-19 columns and an outcome CHECK constraint (ok|fallback|escalated).
    - After applying locally: `information_schema.columns` lists every D-19 column for `genui_generation_events` (assert in the SUMMARY with the actual query output).
    - schema/index.ts ends with the new `export * from "./genui-generation-events";`.
  </acceptance_criteria>
  <done>Audit table defined in Drizzle, migration 0021 generated + applied locally + verified via information_schema, re-exported from the schema barrel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GenerationAuditRepository port + Supabase adapter</name>
  <read_first>
    - CREATING: apps/email-listener/app/domain/ports/generation_audit_repository.py, apps/email-listener/app/infrastructure/persistence/supabase_generation_audit_repository.py, apps/email-listener/tests/infrastructure/test_supabase_generation_audit_repository.py
    - ANALOG: an existing Supabase repository under apps/email-listener/app/infrastructure/persistence/ (e.g. the component or extraction repository) for the `supabase.table(...).insert(...).execute()` idiom + structlog error handling + tenancy; an existing domain port under app/domain/ports/ for the Protocol shape; the existing fake/AsyncMock repo tests for the DB-less test idiom
    - DECISION: 13-CONTEXT.md D-19 (one row per generation event, written server-side in FastAPI after the validation gate; best-effort — a logging failure must NOT fail generation), D-15 (safeParse-style boundary), CLAUDE.md (domain-pure ports, Pydantic/dataclass at boundaries, immutable, named exports, no print)
    - RESEARCH: SAFETY-PITFALLS.md §5c table; GR-12; the row is also the Phase-14 CACHE-02 seam
  </read_first>
  <behavior>
    - GenerationEvent is a frozen dataclass with the D-19 fields (intent_hash, model_id, input_tokens, output_tokens, attempts, outcome, spec_validation_passed, spec_node_count, spec_depth, registry_version, latency_ms, importer_id).
    - GenerationAuditRepository is a Protocol with `async def record(event: GenerationEvent) -> None`.
    - The Supabase adapter maps the dataclass to the genui_generation_events column names and inserts one row.
    - On an insert exception the adapter logs (structlog) and returns None — it NEVER raises (best-effort audit, D-19), so a logging failure cannot break a generation request. The test asserts a raising client is swallowed.
  </behavior>
  <action>
    Create `generation_audit_repository.py`: a `@dataclass(frozen=True) class GenerationEvent` with the D-19 fields (outcome typed as `Literal["ok","fallback","escalated"]`) and a `class GenerationAuditRepository(Protocol)` exposing `async def record(self, event: GenerationEvent) -> None`. Domain-pure: import only typing/dataclasses (lint-imports clean). Create `supabase_generation_audit_repository.py`: `class SupabaseGenerationAuditRepository` taking the Supabase `Client`, implementing `record` by inserting the column-mapped dict into `genui_generation_events`; wrap the insert in try/except that logs `generation_audit_record_failed` via structlog and returns None (best-effort, D-19). Write `test_supabase_generation_audit_repository.py` (TDD, AsyncMock client): (1) a successful record issues exactly one insert with the mapped columns; (2) a client whose insert raises is swallowed and `record` returns None without raising. Register nothing in container.py here (that wiring lands in 13-03 alongside the use case). No print; ruff/mypy/bandit clean.
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/infrastructure/test_supabase_generation_audit_repository.py -x && uv run ruff check app/domain/ports/generation_audit_repository.py app/infrastructure/persistence/supabase_generation_audit_repository.py</automated>
  </verify>
  <acceptance_criteria>
    - Test file passes: success path issues one insert into `genui_generation_events` with the D-19 mapped columns; raising client is swallowed (record returns None, no exception propagates).
    - `ruff check` + `mypy` clean on both new files; lint-imports clean (port imports no infrastructure).
    - Source assertion: `grep -c "genui_generation_events" apps/email-listener/app/infrastructure/persistence/supabase_generation_audit_repository.py` >= 1.
    - GenerationEvent dataclass is frozen (`grep -c "frozen=True" apps/email-listener/app/domain/ports/generation_audit_repository.py` >= 1).
  </acceptance_criteria>
  <done>Port + best-effort Supabase audit adapter implemented and tested DB-less; generation path can record events without risk of audit failure breaking generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator → audit table | The audit row is the security/cost forensic record; it must be complete and must not become a failure point that breaks generation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Repudiation | generation event logging | mitigate | Every generation writes one row (intent_hash, model, tokens, attempts, outcome, validation, counts, registry_version, importer) — GEN-05/D-19; success/fallback/escalated all recorded; test in 13-03 asserts a row per call. |
| T-13-09 | Information Disclosure | raw intent in audit log | mitigate | Intent stored as a canonical hash, not raw prose (D-19); column is `intent_hash`. |
| T-13-10 | Denial of Service | audit insert failure | accept (best-effort) | Adapter swallows insert exceptions and logs them (D-19); a failing audit cannot break a generation request; test asserts swallow. |
| T-13-11 | Tampering | outcome value | mitigate | SQL CHECK constraint restricts outcome to ok|fallback|escalated; writer types it as Literal. |
</threat_model>

<verification>
- `cd packages/db && pnpm typecheck` exit 0; migration 0021 present and applied locally (information_schema verified).
- `cd apps/email-listener && uv run pytest tests/infrastructure/test_supabase_generation_audit_repository.py` green; ruff/mypy/lint-imports/bandit clean on new files.
- SUMMARY records the PENDING DEPLOY: apply 0021 to staging+prod before the generator endpoint (13-03) deploys.
</verification>

<success_criteria>
- `genui_generation_events` exists (Drizzle + migration 0021, applied + verified locally) with the full D-19 column set (GEN-05, success criterion 4).
- A best-effort audit port + adapter can persist one event per generation without risking the generation path.
- Table lives in packages/db consistent with Phase 14's ui_spec_templates (D-19).
</success_criteria>

<output>
Create `.planning/phases/13-generation-layer-and-guardrails/13-02-SUMMARY.md` when done.
</output>
