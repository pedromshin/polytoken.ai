---
phase: 40-confirm-action-widgets
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - apps/email-listener/app/application/use_cases/promote_edge.py
  - apps/email-listener/tests/test_promote_edge.py
  - apps/email-listener/app/application/use_cases/confirm_action_dispatch.py
  - apps/email-listener/app/application/use_cases/__tests__/test_confirm_action_dispatch.py
  - apps/email-listener/app/application/use_cases/submit_widget_interaction.py
  - apps/email-listener/app/application/use_cases/__tests__/test_submit_widget_interaction.py
  - apps/email-listener/app/container.py
  - apps/web/src/app/chat/_components/compact-interaction-entry.tsx
  - apps/web/src/app/chat/_components/__tests__/compact-interaction-entry.test.tsx
autonomous: true
requirements: [CONF-02]

must_haves:
  truths:
    - "Submitting a confirm-action widget re-checks the referenced edge's LIVE tier against the tierSnapshot recorded at emission, and rejects with 409 stale (WidgetSubmitRejected reason=stale) if it was promoted or deactivated out-of-band — BEFORE any interaction-row mutation happens"
    - "A test proves: emit a confirm-action widget -> promote the SAME edge out-of-band (v1.5's promote_edge path) -> submit the widget -> 409 stale, no double mutation of the edge"
    - "Confirm dispatches through the v1.5 promote_edge path with promotion provenance recording {mechanism: chat_confirm_action, widget_interaction_id}, distinct from a plain human_promote via REST"
    - "Reject never deletes or mutates the edge row — the interaction row's own submitted_value is the durable audit record (audit-on-the-row convention)"
    - "Dispatch resolves the use case from an explicit 2-entry table (knowledge_edge_tier_promotion, entity_merge_confirm) keyed by the STORED declaration's kind — never by a dynamically-resolved name, and a lookup never raises a raw KeyError"
    - "A human can confirm/reject a confirm_action widget in the transcript using the UNMODIFIED proposal-card rendering machinery — the compact post-submit summary correctly shows 'Selected \"Confirm\"'/'Selected \"Reject\"' instead of falling through to the clarify-widget summary view"
  artifacts:
    - path: "apps/email-listener/app/application/use_cases/confirm_action_dispatch.py"
      provides: "ConfirmActionHandler Protocol + KnowledgeEdgeTierPromotionHandler + UnsupportedConfirmActionHandler"
      contains: "class KnowledgeEdgeTierPromotionHandler"
    - path: "apps/email-listener/app/application/use_cases/submit_widget_interaction.py"
      provides: "the CONF-02 edge-tier staleness re-check + dispatch invocation, prepare() signature UNCHANGED"
      contains: "confirm_action"
    - path: "apps/email-listener/app/application/use_cases/promote_edge.py"
      provides: "PromoteEdgeUseCase.execute() additive mechanism/extra params for non-REST promotion provenance"
      contains: "mechanism"
  key_links:
    - from: "apps/email-listener/app/application/use_cases/submit_widget_interaction.py"
      to: "apps/email-listener/app/domain/ports/knowledge_graph_repository.py"
      via: "self._knowledge_graph.find_edge_by_id(...) inside the new pre-CAS staleness check"
      pattern: "find_edge_by_id"
    - from: "apps/email-listener/app/application/use_cases/confirm_action_dispatch.py"
      to: "apps/email-listener/app/application/use_cases/promote_edge.py"
      via: "KnowledgeEdgeTierPromotionHandler wraps PromoteEdgeUseCase.execute(mechanism='chat_confirm_action', extra={'widget_interaction_id': ...})"
      pattern: "PromoteEdgeUseCase"
    - from: "apps/email-listener/app/container.py"
      to: "apps/email-listener/app/application/use_cases/submit_widget_interaction.py"
      via: "_provide_submit_widget_interaction wires knowledge_graph + confirm_action_dispatch (2-entry dict)"
      pattern: "confirm_action_dispatch"
---

<objective>
Close CONF-02: submitting a `confirm_action` widget re-checks the referenced `knowledge_node_edges` row's LIVE tier against the declaration's `tierSnapshot` before any mutation, then dispatches through an explicit 2-entry use-case table (`knowledge_edge_tier_promotion` real; `entity_merge_confirm` a registered-but-unsupported stub, per the confirmed pair-keyed blocker). `SubmitWidgetInteraction.prepare()`'s existing signature and step order (ownership -> staleness -> schema-revalidate -> CAS) stay unchanged; the new logic is additive inside the same method.

Purpose: prevent a stale confirm/reject from double-mutating an edge that was already promoted or changed elsewhere (the design-case narrative: "suggestions become chat-confirmable without ever letting the model touch the mutation parameters").
Output: the 409-stale safety property, proven by a dedicated test; the 2-entry dispatch table; the one-line web change making the existing proposal-card transcript summary render confirm_action correctly.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-confirm-action-widgets/40-CONTEXT.md
@.planning/research/v1.6-chat-knowledge/SYNTHESIS.md

# Needed: Plan 40-01 built the declaration shape (suggestionRef/tierSnapshot keys) and WidgetKind this plan reads.
@.planning/phases/40-confirm-action-widgets/40-01-SUMMARY.md
</context>

<interfaces>
<!-- Extracted from the live codebase — executor should use these directly, no exploration needed. -->

From `apps/email-listener/app/application/use_cases/submit_widget_interaction.py` (the file this plan edits — current full shape):
```
class WidgetSubmitRejected(Exception):
    def __init__(self, reason: WidgetSubmitRejectionReason, message: str = "") -> None: ...

class SubmitWidgetInteraction:
    def __init__(self, *, widget_interactions: ChatWidgetInteractionRepository, messages: ChatMessageRepository, continuation_runner: ContinuationRunner) -> None: ...

    async def prepare(self, *, conversation_id: str, interaction_id: str, result: dict[str, Any], model_id: str) -> AsyncIterator[ChatRunEvent]:
        interaction = await self._widget_interactions.get(interaction_id)
        if interaction is None or interaction.conversation_id != conversation_id:
            raise WidgetSubmitRejected("not_found", ...)
        if await self._widget_interactions.is_stale(interaction):
            raise WidgetSubmitRejected("stale", ...)
        outcome = validate_result_against_schema(result, interaction.declared_response_schema)
        if not outcome.ok:
            raise WidgetSubmitRejected("invalid", outcome.reason)
        submitted = await self._widget_interactions.try_submit(interaction_id, result)
        if not submitted:
            raise WidgetSubmitRejected("conflict", ...)
        summary = _resolve_summary(interaction, result)
        ...  # insert interaction_result message, return continuation
```
`_resolve_summary(interaction, result)` currently: `if interaction.widget_kind == "proposal_cards": ...`; `elif interaction.widget_kind == "clarify_widget": ...`; `else: raise ValueError`.

From Plan 40-01's `run_chat_turn_confirm_action.py` (already built by 40-01 — import from here, do not redefine):
```
SUGGESTION_KIND_EDGE_TIER_PROMOTION = "knowledge_edge_tier_promotion"
SUGGESTION_KIND_ENTITY_MERGE_CONFIRM = "entity_merge_confirm"
```
A `confirm_action` declaration (built in 40-01) has the shape `{"prompt": ..., "options": [{"id": "confirm", ...}, {"id": "reject", ...}], "suggestionRef": {"kind": "knowledge_edge_tier_promotion", "id": <edge_id>}, "tierSnapshot": <tier at emission>}`. The submitted `result` for a confirm_action interaction is `{"optionId": "confirm"}` or `{"optionId": "reject"}` (reused proposal_cards wire format — validated against the `optionId` enum schema 40-01 already derives).

From `apps/email-listener/app/application/use_cases/promote_edge.py` (current signature you are extending additively):
```
class PromoteEdgeUseCase:
    def __init__(self, *, knowledge: KnowledgeGraphRepository) -> None: ...
    async def execute(self, *, edge_id: str, importer_id: str) -> dict[str, object]:
        ...
        promotion: dict[str, object] = {"promoted_at": ..., "from_tier": from_tier, "mechanism": "human_promote"}
        updated = await self._knowledge.promote_edge(edge_id=edge_id, promotion=promotion)
        ...
```
Raises `EdgeNotFound` / `EdgeNotPromotable(reason: Literal["tenant_mismatch","inactive","not_promotable","conflict"], message)`.

From `apps/email-listener/app/domain/ports/knowledge_graph_repository.py`:
```
async def find_edge_by_id(self, edge_id: str) -> dict[str, object] | None   # flattens importer_id via the source_node_id join
async def promote_edge(self, *, edge_id: str, promotion: dict[str, object]) -> bool   # CAS: id + is_active=true + tier in (INFERRED,AMBIGUOUS)
```

From `apps/email-listener/app/application/use_cases/curate_entity_merge.py` (CONFIRMS the pair-keyed blocker — read, do not modify):
```
class ConfirmMergeUseCase:
    async def execute(self, entity_instance_id: str, target_id: str) -> dict[str, str]: ...
```
Takes a PAIR of ids, not one addressable id — `component_entity_candidate_links` has its own `id` PK but the entire merge-confirm flow (this class) is built around the `(entity_instance_id, target_id)` pair, not that PK. A single `suggestionRef.id` cannot address one merge candidate without inventing a surrogate key. 40-CONTEXT.md forbids inventing one this phase — this is why `entity_merge_confirm` ships as a registered-but-unsupported dispatch entry only (already tracked in ROADMAP.md's Backlog under "v1.6 deferred items" — no new backlog entry needed).

From `apps/email-listener/app/container.py` (the two factories this plan extends):
```
def _provide_submit_widget_interaction(widget_interactions: ChatWidgetInteractionRepository, messages: ChatMessageRepository, continuation_runner: RunChatTurn) -> SubmitWidgetInteraction: ...
```
`PromoteEdgeUseCase` is already `provider.provide(_provide_promote_edge_use_case, provides=PromoteEdgeUseCase)` — resolvable as a plain constructor param on any factory dishka calls.

From `apps/web/src/app/chat/_components/interactive-widget-boundary.tsx` (read-only — already handles confirm_action with ZERO changes, verify this claim): `const isClarify = part.widgetKind === "clarify_widget";` — anything else (including `"confirm_action"`) falls into the proposal_cards rendering branch already. Do NOT touch this file.

From `apps/web/src/app/chat/_components/compact-interaction-entry.tsx` (the ONE file needing a change — current line 88): `{widgetKind === "proposal_cards" ? (<ProposalSummary summary={summary} />) : (<ClarifySummary summary={summary} />)}`. A `confirm_action` widgetKind currently falls into `ClarifySummary` (wrong — expects `summary.fields`, but confirm_action's summary is `{chosenTitle}` shaped) and would render an empty "Your response" row.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PromoteEdgeUseCase provenance extension + confirm_action_dispatch handlers</name>
  <files>apps/email-listener/app/application/use_cases/promote_edge.py, apps/email-listener/tests/test_promote_edge.py, apps/email-listener/app/application/use_cases/confirm_action_dispatch.py, apps/email-listener/app/application/use_cases/__tests__/test_confirm_action_dispatch.py</files>
  <read_first>
    - apps/email-listener/app/application/use_cases/promote_edge.py (full file)
    - apps/email-listener/tests/test_promote_edge.py (full file — existing test shape to extend, AsyncMock repo double pattern)
    - apps/email-listener/app/application/use_cases/curate_entity_merge.py (confirms the pair-keyed blocker — cited above)
    - apps/email-listener/app/application/use_cases/promote_entity_on_confirm.py lines 230-233 (the "idempotent by id" precedent CONF-02 asks you to mirror — re-running upsert/promote for the same id is always safe because the underlying write is itself CAS/idempotent-guarded, not because this dispatch layer adds its own dedup)
  </read_first>
  <behavior>
    - `PromoteEdgeUseCase.execute(edge_id=..., importer_id=...)` with no new kwargs behaves BYTE-IDENTICAL to today (existing `test_promote_edge.py` tests pass unmodified) — `mechanism` defaults to `"human_promote"`, `extra` defaults to `None`/no extra keys merged.
    - `PromoteEdgeUseCase.execute(edge_id=..., importer_id=..., mechanism="chat_confirm_action", extra={"widget_interaction_id": "wi-1"})` on a promotable edge writes a `promotion` dict containing `mechanism == "chat_confirm_action"` AND `widget_interaction_id == "wi-1"`, alongside the existing `promoted_at`/`from_tier` keys.
    - `KnowledgeEdgeTierPromotionHandler.execute(action="confirm", suggestion_id=<edge>, importer_id=..., widget_interaction_id=...)` calls the wrapped `PromoteEdgeUseCase.execute` with `mechanism="chat_confirm_action"` and `extra={"widget_interaction_id": ...}`, returning `{"status": "promoted", "edge_id": ..., "tier": "EXTRACTED"}` on success.
    - `KnowledgeEdgeTierPromotionHandler.execute(action="confirm", ...)` where the wrapped use case raises `EdgeNotPromotable` (e.g. a race lost against a concurrent promotion) — the handler catches it, logs a warning, and returns `{"status": "promote_failed"}` — it NEVER re-raises (this runs after the interaction row's own CAS has already succeeded; the turn must still complete cleanly).
    - `KnowledgeEdgeTierPromotionHandler.execute(action="reject", ...)` performs NO promote_edge call at all (assert the wrapped use case's execute is never awaited) and returns `{"status": "rejected"}`.
    - `UnsupportedConfirmActionHandler.execute(action=..., suggestion_id=..., importer_id=..., widget_interaction_id=...)` (registered under `"entity_merge_confirm"`) never raises, always returns a dict with `"status": "unsupported"`.
  </behavior>
  <action>
    In `promote_edge.py`: extend `PromoteEdgeUseCase.execute` with two new keyword-only params, `mechanism: str = _MECHANISM_HUMAN_PROMOTE` and `extra: dict[str, object] | None = None`. Change the `promotion` dict construction to `{"promoted_at": ..., "from_tier": from_tier, "mechanism": mechanism, **(extra or {})}`. Do not touch any other guard/ordering logic. Extend `apps/email-listener/tests/test_promote_edge.py` with the two new-kwarg cases from `<behavior>` above (mirror the existing `AsyncMock`-based `_use_case` helper).

    Create NEW file `apps/email-listener/app/application/use_cases/confirm_action_dispatch.py` (application layer; imports only `app.application.use_cases.promote_edge` + stdlib/structlog — no infrastructure). Contents:
    - `ConfirmActionResult = dict[str, Any]` (or an equivalent typed alias)
    - `class ConfirmActionHandler(Protocol): async def execute(self, *, action: Literal["confirm", "reject"], suggestion_id: str, importer_id: str, widget_interaction_id: str) -> ConfirmActionResult: ...`
    - `class KnowledgeEdgeTierPromotionHandler:` constructor takes `*, promote_edge: PromoteEdgeUseCase`. `execute()`: if `action == "reject"`, return `{"status": "rejected"}` immediately (docstring: audit-on-the-row convention — the interaction row's own `submitted_value`, already persisted by the CAS step in `SubmitWidgetInteraction.prepare()` BEFORE this handler ever runs, IS the durable rejection record; the edge is left untouched, still a live suggestion elsewhere, never deleted, per 40-CONTEXT.md's "rejection must NOT delete" constraint). Else (`action == "confirm"`): `try: result = await self._promote_edge.execute(edge_id=suggestion_id, importer_id=importer_id, mechanism="chat_confirm_action", extra={"widget_interaction_id": widget_interaction_id}); except (EdgeNotFound, EdgeNotPromotable) as exc: log a structlog warning (edge_id, reason) and return {"status": "promote_failed"}` else `return {"status": "promoted", **result}`.
    - `class UnsupportedConfirmActionHandler:` docstring cites the pair-keyed blocker (see `<interfaces>`) and states this handler exists ONLY so the dispatch table has its full 2 entries (CONF-02) and a lookup by kind never raises a raw KeyError. `execute()` logs a structlog warning and returns `{"status": "unsupported", "reason": "entity_merge_confirm is not yet supported via chat"}` — never raises.

    Create `apps/email-listener/app/application/use_cases/__tests__/test_confirm_action_dispatch.py` implementing every case in `<behavior>` (use `AsyncMock` for the wrapped `PromoteEdgeUseCase`, mirroring `test_promote_edge.py`'s style).
  </action>
  <acceptance_criteria>
    - Source: `PromoteEdgeUseCase.execute`'s two new params are keyword-only with defaults preserving today's exact behavior (existing tests pass with zero edits)
    - Source: `KnowledgeEdgeTierPromotionHandler`'s reject branch never references `self._promote_edge` at all
    - Source: `UnsupportedConfirmActionHandler.execute` has no bare `raise` statement
    - Test command: `pytest apps/email-listener/tests/test_promote_edge.py apps/email-listener/app/application/use_cases/__tests__/test_confirm_action_dispatch.py -v` — all green
  </acceptance_criteria>
  <verify>
    <automated>cd apps/email-listener and python -m pytest tests/test_promote_edge.py app/application/use_cases/__tests__/test_confirm_action_dispatch.py -v</automated>
  </verify>
  <done>PromoteEdgeUseCase records chat-confirm provenance additively; confirm_action_dispatch.py's 2 handlers exist, are unit-tested, and never crash on the entity_merge_confirm path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CONF-02 staleness re-check + dispatch wiring in SubmitWidgetInteraction + container</name>
  <files>apps/email-listener/app/application/use_cases/submit_widget_interaction.py, apps/email-listener/app/container.py, apps/email-listener/app/application/use_cases/__tests__/test_submit_widget_interaction.py</files>
  <read_first>
    - apps/email-listener/app/application/use_cases/submit_widget_interaction.py (full file)
    - apps/email-listener/app/application/use_cases/__tests__/test_submit_widget_interaction.py (full file — the exact Fake*/[_make_use_case] pattern to extend)
    - apps/email-listener/app/container.py lines ~660-680 (`_provide_submit_widget_interaction`) and lines ~385-395 (`_provide_promote_edge_use_case`, the SupabaseKnowledgeGraphRepository-direct-instantiation pattern to mirror)
  </read_first>
  <behavior>
    - MUST-test (CONF-02's headline safety property): a pending `confirm_action` interaction exists with `declaration.tierSnapshot == "INFERRED"` and `declaration.suggestionRef == {"kind": "knowledge_edge_tier_promotion", "id": "edge-1"}`. The fake `KnowledgeGraphRepository`'s `find_edge_by_id("edge-1")` returns an edge with `tier == "EXTRACTED"` (simulating an out-of-band promotion via v1.5's REST `promote_edge` path, which flips exactly this field). Submitting `{"optionId": "confirm"}` raises `WidgetSubmitRejected` with `reason == "stale"`. Assert `try_submit` was NEVER called on the widget-interactions fake (no interaction-row mutation) AND the dispatch handler's `execute` was NEVER called (no edge double-mutation).
    - Same MUST-test shape for `is_active == False` (edge deactivated out-of-band) -> also `stale`.
    - Non-stale confirm: edge tier still matches the snapshot (`"INFERRED"`) -> submit succeeds, `try_submit` called once, the confirm_action dispatch handler's `execute(action="confirm", suggestion_id="edge-1", ...)` is called exactly once, `_resolve_summary` returns `{"chosenTitle": "Confirm"}`, continuation events are yielded.
    - Non-stale reject: same setup, submit `{"optionId": "reject"}` -> succeeds, dispatch handler's `execute(action="reject", ...)` called once, summary `{"chosenTitle": "Reject"}`.
    - Unknown/unregistered `suggestionRef.kind` in the stored declaration (defensive — should never occur given 40-01's tool schema, but the dispatch lookup must not crash): submit still succeeds (schema/CAS already passed), dispatch is skipped or resolves to a safe no-op — assert no exception propagates.
    - Regression: every existing `proposal_cards`/`clarify_widget` test in this file passes unmodified (the staleness check + dispatch are both no-ops for those `widget_kind`s).
  </behavior>
  <action>
    In `submit_widget_interaction.py`: add `from types import MappingProxyType` and, under `TYPE_CHECKING`, add `Mapping` (from `collections.abc`) and import `KnowledgeGraphRepository` (from `app.domain.ports.knowledge_graph_repository`) and `ConfirmActionHandler` (from `app.application.use_cases.confirm_action_dispatch`). Add `import structlog` + `logger = structlog.get_logger(__name__)` at module level.

    Extend `SubmitWidgetInteraction.__init__` with two new keyword params: `knowledge_graph: KnowledgeGraphRepository`, `confirm_action_dispatch: Mapping[str, ConfirmActionHandler] = MappingProxyType({})`. Store as `self._knowledge_graph`/`self._confirm_action_dispatch`.

    In `prepare()`, immediately AFTER the existing staleness check (`if await self._widget_interactions.is_stale(interaction): raise ...`) and BEFORE the schema re-validation step, insert the NEW edge-tier staleness re-check, scoped ONLY to `interaction.widget_kind == "confirm_action"`: read `suggestion_ref = interaction.declaration.get("suggestionRef", {})`, `kind = suggestion_ref.get("kind")`, `suggestion_id = suggestion_ref.get("id")`, `tier_snapshot = interaction.declaration.get("tierSnapshot")`. If `kind == "knowledge_edge_tier_promotion"`: wrap in `try/except Exception` (fail-closed -> stale) around `edge = await self._knowledge_graph.find_edge_by_id(suggestion_id)`; raise `WidgetSubmitRejected("stale", "this suggestion is no longer available")` when `edge is None or not edge.get("is_active") or edge.get("tier") != tier_snapshot`. This MUST run before `try_submit` (step 4) — no interaction-row mutation may happen on a stale confirm-action, mirroring D-12's existing turn-staleness placement.

    AFTER the existing `try_submit` CAS succeeds (i.e., right after `if not submitted: raise WidgetSubmitRejected("conflict", ...)`), add a best-effort dispatch call scoped to `interaction.widget_kind == "confirm_action"`: resolve `kind`/`suggestion_id` from `interaction.declaration["suggestionRef"]` again, `action = result.get("optionId")` (already schema-validated to be `"confirm"` or `"reject"` by this point), `handler = self._confirm_action_dispatch.get(kind)`. If a handler is registered, `try: await handler.execute(action=action, suggestion_id=suggestion_id, importer_id=edge_importer_id_or_fallback, widget_interaction_id=interaction.id) except Exception: logger.warning(...)` — NEVER let a dispatch failure raise past this point (the interaction row is already durably submitted; this is best-effort, mirrors the codebase's established synthesis-hook/instrumentation-write log-and-swallow convention). Derive `importer_id` for the dispatch call from the ALREADY-FETCHED `edge` dict from the staleness check above (`edge.get("importer_id")`) — never introduce a new caller-supplied `importer_id` parameter on `prepare()` itself (D-21-style: importer_id is derived from the loaded row, never a caller arg — this keeps `prepare()`'s signature UNCHANGED per 40-CONTEXT.md).

    Extend `_resolve_summary`: change `if interaction.widget_kind == "proposal_cards":` to `if interaction.widget_kind in ("proposal_cards", "confirm_action"):` (the body — option-id-to-title lookup — is reused verbatim; a confirm_action declaration's `options` are shaped identically to proposal_cards').

    In `container.py`'s `_provide_submit_widget_interaction`: add `client: Client` and `promote_edge_use_case: PromoteEdgeUseCase` params (the latter already DI-registered — reuse via injection, do not rebuild). Build `knowledge_repo = SupabaseKnowledgeGraphRepository(client=client)` and the 2-entry dispatch dict: `{"knowledge_edge_tier_promotion": KnowledgeEdgeTierPromotionHandler(promote_edge=promote_edge_use_case), "entity_merge_confirm": UnsupportedConfirmActionHandler()}`. Pass `knowledge_graph=knowledge_repo, confirm_action_dispatch=dispatch` into the `SubmitWidgetInteraction(...)` call. Add the needed imports (`SUGGESTION_KIND_EDGE_TIER_PROMOTION`/`SUGGESTION_KIND_ENTITY_MERGE_CONFIRM` constants if used literally for the dict keys, `KnowledgeEdgeTierPromotionHandler`, `UnsupportedConfirmActionHandler` from `confirm_action_dispatch.py`).

    Extend `test_submit_widget_interaction.py`: add a `FakeKnowledgeGraphRepository` (records `find_edge_by_id` calls, returns a pre-configured `dict | None`) and a `FakeConfirmActionHandler` (records `execute` calls, returns a pre-configured result). Update `_make_use_case`'s signature to accept `knowledge_graph`/`confirm_action_dispatch` optional params (sensible defaults so every EXISTING proposal_cards/clarify_widget test keeps working with zero edits). Implement every case in `<behavior>` above.
  </action>
  <acceptance_criteria>
    - Source: `prepare()`'s public signature (`conversation_id`, `interaction_id`, `result`, `model_id`) is byte-identical to before this task — grep confirms no new parameter added
    - Source: the edge-tier staleness check runs strictly BEFORE `try_submit` is called (source ordering, not just test-proven)
    - Source: the dispatch call is wrapped in a bare `except Exception` that only logs — never re-raises `WidgetSubmitRejected` or any other exception after the CAS has succeeded
    - Behavior: all cases in `<behavior>` pass, including BOTH out-of-band staleness variants (tier-changed AND deactivated)
    - Test command: `pytest apps/email-listener/app/application/use_cases/__tests__/test_submit_widget_interaction.py -v` — all green (new + existing)
    - Test command: `mypy apps/email-listener/app/application/use_cases/submit_widget_interaction.py apps/email-listener/app/container.py` clean
  </acceptance_criteria>
  <verify>
    <automated>cd apps/email-listener and python -m pytest app/application/use_cases/__tests__/test_submit_widget_interaction.py -v</automated>
  </verify>
  <done>Submitting a confirm_action re-checks live edge tier before any mutation (409 stale proven by a dedicated test simulating v1.5's out-of-band REST promote), dispatches through the explicit 2-entry table after the interaction CAS succeeds, and reject never mutates the edge.</done>
</task>

<task type="auto">
  <name>Task 3: compact transcript summary — render confirm_action via the existing ProposalSummary</name>
  <files>apps/web/src/app/chat/_components/compact-interaction-entry.tsx, apps/web/src/app/chat/_components/__tests__/compact-interaction-entry.test.tsx</files>
  <read_first>
    - apps/web/src/app/chat/_components/compact-interaction-entry.tsx (full file)
    - apps/web/src/app/chat/_components/__tests__/compact-interaction-entry.test.tsx (full file — existing test shape)
    - apps/web/src/app/chat/_components/interactive-widget-boundary.tsx lines 220-247 (confirms this file needs NO change — `isClarify` already excludes confirm_action)
  </read_first>
  <action>
    In `compact-interaction-entry.tsx`: change the widgetKind check from `widgetKind === "proposal_cards"` to `widgetKind === "proposal_cards" || widgetKind === "confirm_action"` — everything else about the component (BUBBLE_CLASS, ProposalSummary, ClarifySummary) stays untouched. Update the file's module docstring comment (currently states "proposal_cards: `Selected \"{chosenTitle}\"`. clarify_widget...") to mention confirm_action reuses the SAME `ProposalSummary` path (its `_resolve_summary` server-side output is `{chosenTitle: "Confirm" | "Reject"}`, identical shape to proposal_cards').

    In `compact-interaction-entry.test.tsx`: add one test asserting `<CompactInteractionEntry widgetKind="confirm_action" summary={{chosenTitle: "Confirm"}} />` renders `Selected "Confirm"` (via `ProposalSummary`, not the clarify key-value-list path).
  </action>
  <acceptance_criteria>
    - Source: `interactive-widget-boundary.tsx` has ZERO diff (confirmed by `git diff --stat` showing the file absent) — this task touches only `compact-interaction-entry.tsx` and its test
    - Behavior: a confirm_action summary renders through `ProposalSummary`, never `ClarifySummary`
    - Test command: `npm test -- compact-interaction-entry` (vitest) passes, including the new confirm_action case
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web and npx vitest run src/app/chat/_components/__tests__/compact-interaction-entry.test.tsx</automated>
  </verify>
  <done>The compact post-submit transcript entry correctly renders confirm_action the same way as proposal_cards; interactive-widget-boundary.tsx remains untouched (zero new web components, per 40-CONTEXT.md).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client -> POST /v1/chat/widget/submit | The submitted `result` (`{optionId: "confirm"|"reject"}`) is untrusted client input |
| Server (SubmitWidgetInteraction) -> knowledge_node_edges | The live re-read that gates whether a mutation is even allowed to proceed |
| Dispatch table -> use case | Which use case actually runs must never be resolvable from client-supplied data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-40-05 | Tampering | out-of-band edge mutation racing a pending confirm-action | mitigate | new edge-tier staleness re-check compares LIVE tier against the `tierSnapshot` recorded at emission, runs BEFORE any interaction-row mutation (`try_submit`), rejects 409 stale — proven by the MUST-test |
| T-40-06 | Elevation of Privilege | resolving a use case to run from a `kind` string | mitigate | explicit finite 2-entry `Mapping[str, ConfirmActionHandler]` built entirely server-side in `container.py`; the `kind` driving the lookup comes from the STORED `declaration` (server-authored at emission, Plan 40-01), never from the client's submit body; an unregistered kind resolves to `None`/no-op, never a dynamic `getattr`/`globals()` lookup |
| T-40-07 | Tampering | residual race between the staleness check and the edge's own CAS write inside `promote_edge` | accept | sub-second window; `promote_edge`'s own `id + is_active=true + tier IN (...)` CAS filter is a second, independent guard — a race loser returns `EdgeNotPromotable("conflict")`, caught and logged by `KnowledgeEdgeTierPromotionHandler`, never crashes the turn; the interaction row itself still durably records what the human clicked, which is the primary audit property CONF-02 needs |
| T-40-08 | Repudiation | no per-user attribution on who confirmed/rejected | accept | matches REQUIREMENTS.md's documented Out-of-Scope decision (single shared API key this milestone) — `promotion.widget_interaction_id` records *via what*, not *who* |
</threat_model>

<verification>
Run both new/extended pytest suites (`test_promote_edge.py`, `test_confirm_action_dispatch.py`, `test_submit_widget_interaction.py`) and the vitest suite for `compact-interaction-entry.test.tsx`. Manually trace the MUST-test's ordering in source (staleness check before CAS, dispatch after CAS) since this is the phase's headline safety property. Run `mypy` across all touched Python files.
</verification>

<success_criteria>
- The out-of-band-promote -> submit -> 409-stale-no-double-mutation test exists and passes
- `SubmitWidgetInteraction.prepare()`'s public signature is unchanged
- The 2-entry dispatch table is explicit, server-built, never resolved from client input; entity_merge_confirm never raises a raw KeyError
- Reject performs zero edge-row mutation
- Confirm records `mechanism: chat_confirm_action` + `widget_interaction_id` provenance, distinct from a plain REST human_promote
- The compact transcript entry renders confirm_action correctly with zero changes to `interactive-widget-boundary.tsx`
- All new + existing tests green (Python + the one touched web test file); mypy clean
</success_criteria>

<output>
Create `.planning/phases/40-confirm-action-widgets/40-02-SUMMARY.md` when done
</output>
