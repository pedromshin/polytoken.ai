---
phase: 41-knowledge-preview-canvas-node
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - apps/web/src/app/chat/_canvas/knowledge-preview-mini-graph.tsx
  - apps/web/src/app/chat/_canvas/__tests__/knowledge-preview-mini-graph.test.tsx
  - apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx
  - apps/web/src/app/chat/_canvas/__tests__/knowledge-preview-node.test.tsx
  - apps/web/src/app/chat/_canvas/node-types.ts
  - apps/web/src/app/chat/_canvas/add-knowledge-preview-popover.tsx
  - apps/web/src/app/chat/_canvas/__tests__/add-knowledge-preview-popover.test.tsx
  - apps/web/src/app/chat/_canvas/chat-canvas.tsx
autonomous: true
requirements: [PREV-01]

must_haves:
  truths:
    - "User can click an 'Add knowledge preview' toolbar affordance, paste a knowledge node UUID (+ optional label), and see a knowledge-preview node appear on the /chat canvas, selected and near the current viewport"
    - "The node renders a bounded (<=25 visible nodes, <=2-hop via knowledge.expandNode depth:2), NON-interactive subgraph — no pan/zoom/drag inside it, no second <ReactFlow>/<ReactFlowProvider> ever mounted (grep-verified across both new UI files)"
    - "Every mini-graph node dot AND the footer row are real Next <Link>s computed via hrefFor('knowledge', id) (the same fixed switch Phase 39's <ProvenanceLink> uses) -- never a server-supplied route string -- deep-linking to /knowledge?focus={id}"
    - "The node has a working remove button (first node-level remove affordance on this canvas) that removes it via useReactFlow().deleteElements and triggers the existing debounced persistence.scheduleSave -- survives a reload"
    - "Loading/error/empty-not-found/empty-no-connections/success are 5 distinct, correctly-branched render states inside the node body, the footer link always renders regardless of state"
    - "Zero new npm dependencies; zero edits to spec-renderer.tsx/render-node.tsx/genui-part-boundary.tsx (byte-identical, diff-verified)"
  artifacts:
    - path: "apps/web/src/app/chat/_canvas/knowledge-preview-mini-graph.tsx"
      provides: "KnowledgePreviewMiniGraph -- SVG edges + absolutely-positioned Link node dots + loading/error/empty-notfound/empty-noconn/success branching"
      exports: ["KnowledgePreviewMiniGraph"]
    - path: "apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx"
      provides: "KnowledgePreviewNode -- the 3rd React Flow custom node component (header, remove, footer, data-fetching)"
      exports: ["KnowledgePreviewNode", "resolveHeaderLabel", "resolveFooterCopy"]
    - path: "apps/web/src/app/chat/_canvas/add-knowledge-preview-popover.tsx"
      provides: "AddKnowledgePreviewPopover -- toolbar trigger + UUID-validated creation form"
      exports: ["AddKnowledgePreviewPopover"]
    - path: "apps/web/src/app/chat/_canvas/node-types.ts"
      provides: "nodeTypes[\"knowledge-preview\"] = KnowledgePreviewNode"
      contains: "\"knowledge-preview\": KnowledgePreviewNode"
  key_links:
    - from: "apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx"
      to: "packages/api-client's knowledge.expandNode procedure"
      via: "api.knowledge.expandNode.useQuery({ nodeId: data.focusNodeId, depth: 2 })"
      pattern: "knowledge\\.expandNode\\.useQuery"
    - from: "apps/web/src/app/chat/_canvas/knowledge-preview-mini-graph.tsx"
      to: "apps/web/src/app/knowledge/_components/tier-edge-style.ts"
      via: "import { tierEdgeStyle } from \"~/app/knowledge/_components/tier-edge-style\""
      pattern: "tierEdgeStyle"
    - from: "apps/web/src/app/chat/_canvas/knowledge-preview-mini-graph.tsx"
      to: "apps/web/src/components/provenance-link.tsx"
      via: "import { hrefFor } from \"~/components/provenance-link\" -- every node dot/footer href"
      pattern: "hrefFor\\(\"knowledge\""
    - from: "apps/web/src/app/chat/_canvas/chat-canvas.tsx"
      to: "apps/web/src/app/chat/_canvas/use-canvas-persistence.ts"
      via: "handleNodesChange (new) + handleAddKnowledgePreview both call persistence.scheduleSave(canvasStore) -- same debounced path handleNodeDragStop already uses"
      pattern: "scheduleSave"
    - from: "apps/web/src/app/chat/_canvas/node-types.ts"
      to: "apps/web/src/app/chat/_canvas/node-type-registry.ts (Plan 41-01)"
      via: "the \"knowledge-preview\" key matches NODE_TYPE_REGISTRY's 3rd entry id exactly"
      pattern: "\"knowledge-preview\""
---

<objective>
Build and wire the visible surface for Phase 41's `knowledge-preview` canvas node (PREV-01): the
static SVG/div mini-graph renderer, the React Flow node shell (header, remove, footer), the
"Add knowledge preview" toolbar creation affordance, and `chat-canvas.tsx`'s wiring (node-type
registration, add-node flow, remove-triggers-save). Builds directly against Plan 41-01's
`KnowledgePreviewNodeDataSchema`/registry entry and `knowledge-preview-layout.ts`'s 5 pure functions
— no further data-layer work needed.

Purpose: PREV-01's user-observable outcome — placing a bounded, non-interactive knowledge subgraph on
the `/chat` canvas that deep-links to `/knowledge?focus={id}` — only exists once this plan lands.

Output: 3 new component files + `node-types.ts`/`chat-canvas.tsx` wired, fully test-covered, zero new
npm dependencies, zero edits to the 3 locked genui renderer files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-knowledge-preview-canvas-node/41-CONTEXT.md
@.planning/phases/41-knowledge-preview-canvas-node/41-UI-SPEC.md
@.claude/skills/nauta-design-system/SKILL.md
@.planning/phases/39-tool-round-ui-citation-chips/39-02-SUMMARY.md

<interfaces>
<!-- Contracts this plan's components must match. Extracted from the codebase this session (including
Plan 41-01's new module, read fresh at execution time) — no further exploration needed. -->

From Plan 41-01's apps/web/src/app/chat/_canvas/node-data-schemas.ts (now existing):
  export const KnowledgePreviewNodeDataSchema = z.object({ focusNodeId: z.string().uuid(), label: z.string().max(80).optional() }).strict();
  export type KnowledgePreviewNodeData = z.infer<typeof KnowledgePreviewNodeDataSchema>;

From Plan 41-01's apps/web/src/app/chat/_canvas/knowledge-preview-layout.ts (now existing):
  export const MAX_PREVIEW_NODES = 25;
  export function computeHopDistances(focusId: string, edges: ReadonlyArray<{id;source;target}>): ReadonlyMap<string, number>
  export function trimPreviewGraph<TNode extends {id:string}, TEdge extends {id;source;target}>(focusId, nodes, edges, cap = MAX_PREVIEW_NODES):
    { nodes: ReadonlyArray<TNode>; edges: ReadonlyArray<TEdge>; oneHopIds: readonly string[]; twoHopIds: readonly string[]; overflowCount: number }
  export function orderTwoHopByParent(oneHopIds: readonly string[], twoHopIds: readonly string[], edges): readonly string[]
  export function layoutPreview(focusId: string, oneHop: readonly string[], twoHop: readonly string[], box = {width:280,height:140}): Record<string, {x,y}>

From apps/web/src/app/knowledge/_components/tier-edge-style.ts (UNCHANGED, already read):
  export interface TierEdgeStyle { readonly style?: CSSProperties; readonly labelStyle?: CSSProperties; }
  export function tierEdgeStyle(tier: string | undefined): TierEdgeStyle
    // INFERRED -> {style:{strokeDasharray:"5 3", stroke:"hsl(var(--muted-foreground))"}}
    // AMBIGUOUS -> {style:{stroke:"hsl(var(--muted-foreground))", opacity:0.45}, labelStyle:{opacity:0.6}}
    // EXTRACTED/undefined -> {} (caller applies fallback stroke hsl(var(--foreground)), strokeWidth 1.5, opacity 1)

From apps/web/src/components/provenance-link.tsx (Phase 39, UNCHANGED, already read):
  export type ProvenanceKind = "email" | "entity" | "knowledge";
  export function hrefFor(kind: ProvenanceKind, id: string): string
    // "knowledge" -> `/knowledge?focus=${encodeURIComponent(id)}` -- REUSE this directly for every
    // node dot / footer href in this plan's mini-graph, never hand-duplicate the route string.

From apps/web/src/components/empty-state.tsx (UNCHANGED, already read):
  export interface EmptyStateAction { readonly label: string; readonly icon?: LucideIcon; readonly onClick: () => void; readonly disabled?: boolean; }
  export interface EmptyStateProps {
    readonly icon: LucideIcon; readonly heading: string; readonly body: string;
    readonly layout?: "centered" | "inline"; readonly tone?: "muted" | "destructive";
    readonly size?: "compact" | "spacious"; readonly action?: EmptyStateAction; readonly caption?: string;
  }
  export function EmptyState(props: EmptyStateProps): React.ReactElement
    // size="compact" layout="centered" (both defaults) renders `absolute inset-0 flex ... text-center`

From packages/ui/src/skeleton.tsx (UNCHANGED, already read):
  export function Skeleton({ className, ...props }: React.HTMLAttributes<HTMLDivElement>): JSX.Element
    // renders <div className={cn("motion-safe:animate-pulse rounded-md bg-muted", className)} {...props} />

From apps/web/src/app/chat/_canvas/node-types.ts (current shape, BEFORE this plan's edit):
  export const nodeTypes: NodeTypes = { chat: ChatNode, "genui-panel": GenuiPanelNode, "unknown-node-type": UnknownNodeTypePlaceholder };
  export function resolveNodeComponent(type: string): NodeTypes[string]  // never throws

From apps/web/src/app/chat/_canvas/canvas-layout.ts (Plan 41-01 already added a "knowledge-preview"
CANVAS_NODE_DIMENSIONS entry — read fresh, unchanged shape otherwise):
  export const CANVAS_NODE_DIMENSIONS: Readonly<Record<string,{width;height}>>  // now includes "knowledge-preview": {width:320,height:240}
  export const DEFAULT_CANVAS_NODE_DIMENSIONS = { width: 320, height: 240 };
  export interface CanvasRect { readonly x; readonly y; readonly width; readonly height; }
  export function offsetCascadePosition(desired: CanvasRect, existing: ReadonlyArray<CanvasRect>): {x,y}

From apps/web/src/app/chat/_canvas/chat-canvas.tsx (current shape, BEFORE this plan's edit — the exact
sections this plan touches):
  const [nodes, setNodes, onNodesChange] = useNodesState<FlowNode>([]);
  const handleEdgesChange = useCallback((changes: EdgeChange<FlowEdge>[]) => {
    onEdgesChange(changes);
    if (changes.some((change) => change.type === "add" || change.type === "remove")) {
      persistence.scheduleSave(canvasStore);
    }
  }, [onEdgesChange, persistence, canvasStore]);
  // ^ Task 3's handleNodesChange mirrors this EXACT shape, checking only "remove" (add already
  //   handled by handleAddKnowledgePreview calling scheduleSave directly).
  const rfInstanceRef = useRef<ReactFlowInstance<FlowNode, FlowEdge> | null>(null);
  const handleInit = useCallback((instance) => { rfInstanceRef.current = instance; }, []);
  const DRAG_HANDLE_SELECTOR = ".node-drag-handle";
  // Current toolbar JSX (top-right Panel, no wrapping div today):
  //   <Panel position="top-right">
  //     <Button aria-label="Toggle minimap" className="size-11 bg-background/70 backdrop-blur-md" onClick={handleToggleMiniMap}><MapIcon .../></Button>
  //   </Panel>
  // persistence.scheduleSave(canvasStore?: CanvasStore | null): void -- debounced ~800ms, reads LATEST nodes/edges via a ref at fire time.

NOTE — `packages/api-client/src/router/chat/canvas-schema.ts`'s `nodeDataSchema` (the RUNTIME
persistence boundary every canvas node.data payload passes through, `chat.saveCanvasLayout`/
`getCanvasLayout`) is ALREADY a generic `z.record(z.string(), z.unknown())` + the existing spec/root-key
refine + the existing `hasForbiddenKeyDeep` refine — type-agnostic across every node type, unchanged
since Phase 23. It requires ZERO edits for this node type: `{focusNodeId, label}` already satisfies it
(no `spec`/`root` keys, no forbidden keys, plain string values). Do NOT edit
`packages/api-client/src/router/chat/canvas-schema.ts` or `canvas.ts` this plan — confirmed by reading
both this session; 41-UI-SPEC.md flags this file only so the executor doesn't independently guess an
edit is needed here.

From RouterOutputs["knowledge"]["expandNode"] (packages/api-client's typed procedure output, mirrors
use-canvas-persistence.ts's `RouterOutputs["chat"]["getCanvasLayout"]` import-and-index convention):
  { nodes: Array<{ id: string; type: "knowledge_node"; label: string; scope?; source?; confidence? }>;
    edges: Array<{ id: string; source: string; target: string; relationType: string; tier?: string; confidence?: number; provenanceSummary?: string }>;
    truncated: boolean }
  // api.knowledge.expandNode.useQuery({ nodeId, depth: 2 }) -- mirrors api.chat.getCanvasLayout.useQuery({conversationId}) usage pattern in use-canvas-persistence.ts. No importerId param -- server resolves it from the seed row.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KnowledgePreviewMiniGraph -- SVG edges + node dots + loading/error/empty/success branching</name>
  <files>
    apps/web/src/app/chat/_canvas/knowledge-preview-mini-graph.tsx,
    apps/web/src/app/chat/_canvas/__tests__/knowledge-preview-mini-graph.test.tsx
  </files>
  <read_first>
    .planning/phases/41-knowledge-preview-canvas-node/41-UI-SPEC.md sections "2. Mini-Graph Rendering"
    and "4. Loading / Empty / Error States" and "5. Deep-Link Interactions" IN FULL, verbatim — the
    authoritative anatomy (rendering technique, node dot sizes/classes, tooltip wrapping, hit-target
    sizing, aria-labels, copy tables). This is a presentational, PROP-DRIVEN component — it does NOT
    call any tRPC hook itself (that lives in Task 2's node.tsx); it receives already-fetched raw
    nodes/edges + loading/error flags as props.
    apps/web/src/app/chat/_canvas/knowledge-preview-layout.ts in full (Plan 41-01's output, read fresh
    — the 5 exports this component's success branch calls, in order: trimPreviewGraph ->
    orderTwoHopByParent -> layoutPreview).
    apps/web/src/app/knowledge/_components/tier-edge-style.ts in full (already read this session).
    apps/web/src/components/provenance-link.tsx's `hrefFor` export (already read).
    apps/web/src/components/empty-state.tsx in full (already read — `EmptyState`'s exact prop names).
    packages/ui/src/skeleton.tsx + packages/ui/src/tooltip.tsx (Tooltip/TooltipTrigger/TooltipContent/
    TooltipProvider named exports).
  </read_first>
  <behavior>
    - Test 1 (loading): `isLoading: true` renders `role="status"` + `aria-label="Loading knowledge
      preview"` with exactly 3 `Skeleton` (rendered as `<div>`) circles present — no `<svg>`, no
      `EmptyState` heading text anywhere in the mount.
    - Test 2 (error): `isError: true` (nodes empty, isLoading false) renders `EmptyState` with heading
      "Couldn't load this preview." and body "Try again, or open the full graph." — a "Retry" button is
      present and calls the `onRetry` prop exactly once when clicked.
    - Test 3 (empty — not found): `isLoading: false, isError: false, nodes: []` renders `EmptyState`
      heading "This preview is unavailable." body "The knowledge node may have been removed or is no
      longer accessible." — NO action button present.
    - Test 4 (empty — no connections): `nodes: [{id: focusNodeId, type:"knowledge_node", label:"Focus"}]`,
      `edges: []` renders `EmptyState` heading "No connections yet." body "This knowledge node isn't
      linked to others yet." — NO action button present.
    - Test 5 (success — node count): `nodes.length > 1` renders `role="group" aria-label="Related
      knowledge nodes"` containing exactly `nodes.length` `<a>` elements (one per node dot, focus
      included) plus exactly one `<svg>` element — no `EmptyState`, no `Skeleton` present.
    - Test 6 (success — href computation): every node dot `<a>`'s `href` attribute equals
      `hrefFor("knowledge", thatNode'sId)` (imported and asserted against directly, never a
      hand-duplicated string) and its `aria-label` equals `` `Open ${fullLabel} in Knowledge graph` ``
      using that node's OWN full (untruncated) label.
    - Test 7 (success — over-cap trim): a 30-node input (1 focus + 29 neighbours, edges forming a valid
      <=2-hop graph from the focus) renders AT MOST `MAX_PREVIEW_NODES` (25) `<a>` node dots — proves
      `trimPreviewGraph` is actually invoked, not bypassed.
    - Test 8 (success — tier encoding, MUST match `/knowledge`): an edge with `tier: "INFERRED"` renders
      its `<line>`/`<path>` element with a `stroke-dasharray` attribute/style equal to `"5 3"`; an edge
      with `tier: "AMBIGUOUS"` renders with `opacity` `0.45`; an edge with `tier: undefined` (or
      `"EXTRACTED"`) renders solid (no `stroke-dasharray`) at full opacity — all 3 assertions read the
      real SVG element's resolved `style`/attribute, not a mocked `tierEdgeStyle` call.
    - Test 9 (focus emphasis): the focus node's dot is the ONLY one whose rendered markup includes a
      `Share2`-icon child element (assert via a distinguishing marker — e.g. an `svg` child inside that
      specific `<a>` vs none inside a 1-hop/2-hop `<a>`).
  </behavior>
  <action>
    Implement `apps/web/src/app/chat/_canvas/knowledge-preview-mini-graph.tsx`:

    1. Export `KnowledgePreviewMiniGraphProps` (and reusable type aliases `PreviewSourceNode =
    RouterOutputs["knowledge"]["expandNode"]["nodes"][number]`, `PreviewSourceEdge =
    RouterOutputs["knowledge"]["expandNode"]["edges"][number]` — import `RouterOutputs` from
    `@nauta/api-client`, mirroring `use-canvas-persistence.ts`'s exact import style): `{ focusNodeId:
    string; nodes: ReadonlyArray<PreviewSourceNode>; edges: ReadonlyArray<PreviewSourceEdge>; isLoading:
    boolean; isError: boolean; onRetry: () => void }`.

    2. Branch in this fixed order (41-UI-SPEC.md §4's table): `isLoading` -> the loading skeleton (3
    `Skeleton` circles at roughly 20px/12px/12px, `rounded-full`, loosely centered inside a `relative`
    wrapper, `role="status" aria-label="Loading knowledge preview"`). Else `isError` -> `<EmptyState
    icon={AlertCircle} tone="destructive" layout="centered" size="compact" heading="Couldn't load this
    preview." body="Try again, or open the full graph." action={{label:"Retry", onClick: onRetry}} />`.
    Else `nodes.length === 0` -> `<EmptyState icon={Share2} tone="muted" layout="centered"
    size="compact" heading="This preview is unavailable." body="The knowledge node may have been
    removed or is no longer accessible." />` (no `action`). Else `nodes.length === 1` (the seed only,
    `edges.length === 0` implied) -> `<EmptyState icon={Share2} tone="muted" layout="centered"
    size="compact" heading="No connections yet." body="This knowledge node isn't linked to others
    yet." />` (no `action`). Else -> the mini-graph itself (step 3). Every branch except loading/success
    wraps in the SAME `relative` positioned parent 41-UI-SPEC.md §4 documents (EmptyState's own
    `absolute inset-0` anchors against it).

    3. Success branch: call `trimPreviewGraph(focusNodeId, nodes, edges)` -> `{nodes: kept, edges:
    keptEdges, oneHopIds, twoHopIds}`; call `orderTwoHopByParent(oneHopIds, twoHopIds, keptEdges)` ->
    `orderedTwoHop`; call `layoutPreview(focusNodeId, oneHopIds, orderedTwoHop)` -> `positions`. Build a
    `distanceOf(id)` lookup (id === focusNodeId -> 0; id in a `Set(oneHopIds)` -> 1; else -> 2) for the
    2 local components below. Render the EXACT structure from 41-UI-SPEC.md §2's anatomy: an outer
    `relative flex flex-1 items-center justify-center p-3` wrapper containing a `width:280,height:140`
    inner div (`motion-safe:animate-in motion-safe:fade-in motion-safe:duration-200` — ONE fade for the
    whole layer, no per-node stagger, DO-NOT 12), which contains ONE `<svg width={280} height={140}
    className="absolute inset-0" aria-hidden>` rendering a local `PreviewEdge` component per kept edge,
    and ONE `<div role="group" aria-label="Related knowledge nodes" className="absolute inset-0">`
    wrapped in a single local `TooltipProvider` (`delayDuration={300}`) rendering a local
    `PreviewNodeDot` component per kept node.

    4. Local `PreviewEdge({ edge, positions })`: reads `tierEdgeStyle(edge.tier)`; if either endpoint's
    position is missing from `positions` (defensive — should not happen post-trim), render nothing.
    Otherwise render an SVG `<line>` from `positions[edge.source]` to `positions[edge.target]` with
    `stroke={style.style?.stroke ?? "hsl(var(--foreground))"}` (the same fallback
    `graph-legend.tsx`'s `LegendSwatch` uses), `strokeWidth={1.5}`, `strokeDasharray={style.style
    ?.strokeDasharray}`, `opacity={style.style?.opacity ?? 1}`.

    5. Local `PreviewNodeDot({ node, position, distance })`: a `Tooltip` wrapping a real `<Link
    href={hrefFor("knowledge", node.id)}>` (`asChild` on the trigger), `aria-label={\`Open ${node.label}
    in Knowledge graph\`}`, absolutely positioned via `style={{ left: position.x, top: position.y,
    transform: "translate(-50%, -50%)" }}` so the dot's visual center lands exactly on the computed
    point. Sizing/classes per 41-UI-SPEC.md §"Mini-graph node dots" table and §2's "Node dot sizes"
    table, keyed on `distance`: `0` (focus) -> 20px visual dot, `bg-primary/15 border-2 border-primary
    shadow-[0_0_8px_hsl(164_39%_22%/0.35)]`, a `Share2` `size-3 text-primary` icon inside, a
    `text-xs font-semibold text-primary max-w-[64px] truncate` label BELOW the dot; `1` -> 12px dot,
    `bg-primary/10 border border-primary/30`, no icon, `text-xs font-normal text-foreground
    max-w-[56px] truncate` label below; `2` -> 8px dot, `bg-muted border border-border/60`, no icon, no
    inline label. EVERY dot (all 3 distances) gets a `min-h-6 min-w-6` (24x24) padded hit-target
    wrapping the visual dot (WCAG 2.5.8), and the `TooltipContent` always shows the node's FULL,
    untruncated `label`.

    6. Icon imports: `Share2`, `AlertCircle` from `lucide-react` (already installed). `Link` from
    `next/link`. Zero other new imports; zero `@xyflow/react` import anywhere in this file (DO-NOT 1 —
    grep-verified in acceptance criteria).
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_canvas/__tests__/knowledge-preview-mini-graph.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 9 behaviors have an independently named, passing test.
    - `grep -c "@xyflow/react\|ReactFlow" apps/web/src/app/chat/_canvas/knowledge-preview-mini-graph.tsx`
      is 0 (no nested React Flow, DO-NOT 1).
    - `grep -c "onWheel\|onDrag\|useState.*[Zz]oom\|onPointerMove" apps/web/src/app/chat/_canvas/knowledge-preview-mini-graph.tsx`
      is 0 (non-interactive mini-graph, DO-NOT 2).
    - `grep -n "hrefFor" apps/web/src/app/chat/_canvas/knowledge-preview-mini-graph.tsx` matches (route
      reuse, never a hand-duplicated `/knowledge?focus=` string literal — `grep -c "knowledge?focus="`
      on this file, EXCLUDING the import line, is 0).
    - `grep -n "import { tierEdgeStyle } from \"~/app/knowledge/_components/tier-edge-style\""
      apps/web/src/app/chat/_canvas/knowledge-preview-mini-graph.tsx` matches (DO-NOT 5).
    - `cd apps/web && npx tsc --noEmit -p apps/web` reports no new errors.
  </acceptance_criteria>
  <done>KnowledgePreviewMiniGraph exists, all 5 render states correctly branch, tier encoding matches /knowledge exactly via direct tierEdgeStyle reuse, every link uses hrefFor, all 9 tests pass, zero React Flow/interactivity leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KnowledgePreviewNode -- the 3rd React Flow custom node (header, remove, footer, data-fetching) + node-types.ts wiring</name>
  <files>
    apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx,
    apps/web/src/app/chat/_canvas/__tests__/knowledge-preview-node.test.tsx,
    apps/web/src/app/chat/_canvas/node-types.ts
  </files>
  <read_first>
    .planning/phases/41-knowledge-preview-canvas-node/41-UI-SPEC.md section "1. Node Shell" IN FULL,
    verbatim (the exact JSX anatomy, `headerLabel` resolution order, the remove button's exact classes/
    aria-label, the fixed `h-[240px] w-[320px]` dimensions) plus its "3. Cap + Footer" section's footer
    JSX/copy table.
    apps/web/src/app/chat/_canvas/genui-panel-node.tsx in full (already read this session — the
    `Handle`/`Position` + memo + outer-shell-className precedent this node's shell mirrors; note
    GenuiPanelNode uses `min-h-[240px] min-w-[320px]` — THIS node uses fixed `h-[240px] w-[320px]`
    instead, per 41-UI-SPEC.md's explicit rationale, since its content never grows).
    apps/web/src/app/chat/_canvas/node-types.ts in full (already read — add the 3rd map entry exactly
    like the existing 2).
    apps/web/src/app/chat/_canvas/__tests__/use-data-bindings.test.tsx's `vi.mock("~/trpc/react", ...)`
    pattern (already read this session, lines 1-124) — mirror this EXACT mocking convention for
    `api.knowledge.expandNode.useQuery` (a plain `.useQuery`, simpler than `.useQueries` — mock it as a
    single `vi.fn()` returning a controllable `{data, isLoading, isError, refetch}` per test).
    Task 1's `knowledge-preview-mini-graph.tsx` (this session's own output) for the exact
    `KnowledgePreviewMiniGraphProps` shape this node passes down.
  </read_first>
  <behavior>
    - Test 1 (`resolveHeaderLabel`): `resolveHeaderLabel("Custom", anything, "f")` returns `"Custom"`
      regardless of the other args (explicit label always wins, first in the resolution order).
    - Test 2 (`resolveHeaderLabel`): `resolveHeaderLabel(undefined, [{id:"f",label:"Acme Corp"}], "f")`
      returns `"Acme Corp"` (resolved focus node's own title once data has settled).
    - Test 3 (`resolveHeaderLabel`): `resolveHeaderLabel(undefined, undefined, "f")` (still loading, no
      custom label, no data yet) returns the fallback literal `"Knowledge preview"`.
    - Test 4 (`resolveHeaderLabel`): `resolveHeaderLabel(undefined, [], "f")` (settled, focus not found
      in the resolved node list — e.g. fail-closed empty response) ALSO returns `"Knowledge preview"`
      (defensive — `.find` returns `undefined`, never throws).
    - Test 5 (`resolveFooterCopy`): `resolveFooterCopy(0)` returns `"Open in Knowledge →"`;
      `resolveFooterCopy(3)` returns `"+3 more — Open in Knowledge →"` — the exact 41-UI-SPEC.md §3 copy.
    - Test 6 (mount, loading): with `api.knowledge.expandNode.useQuery` mocked to return `{data:
      undefined, isLoading: true, isError: false, refetch: vi.fn()}`, mounting `<KnowledgePreviewNode>`
      (via `NodeProps`-shaped test props: `id`, `data: {focusNodeId: "f"}`, `selected: false`) renders
      the header with fallback text `"Knowledge preview"` AND delegates loading down to
      `KnowledgePreviewMiniGraph` (assert `role="status"` is present in the mount — proves the loading
      flag actually threads through, not asserting mini-graph's own internals again).
    - Test 7 (remove wiring): mock `useReactFlow` from `@xyflow/react` (a PARTIAL mock — `vi.mock`'s
      factory spreads the real module via `await vi.importActual("@xyflow/react")` and overrides ONLY
      `useReactFlow`, so `Handle`/`Position`/every other real export stays intact) to return `{
      deleteElements: mockDeleteElements }`. Clicking the remove button (`aria-label="Remove knowledge
      preview"`) calls `mockDeleteElements` exactly once with `{ nodes: [{ id: <the mounted node's own
      `id` prop> }] }`.
    - Test 8 (`node-types.ts` wiring): `nodeTypes["knowledge-preview"]` is reference-equal to the
      exported `KnowledgePreviewNode` component; `resolveNodeComponent("knowledge-preview")` also
      resolves to it (not `UnknownNodeTypePlaceholder`).
  </behavior>
  <action>
    1. In `apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx`, export 2 pure helpers first
    (interface-first within this task): `resolveHeaderLabel(customLabel: string | undefined, nodes:
    ReadonlyArray<{id:string; label:string}> | undefined, focusNodeId: string): string` — returns
    `customLabel` if defined, else `nodes?.find(n => n.id === focusNodeId)?.label`, else the literal
    `"Knowledge preview"` (41-UI-SPEC.md §1's exact 3-step resolution order). `resolveFooterCopy
    (overflowCount: number): string` — returns `` `+${overflowCount} more — Open in Knowledge →` ``
    when `overflowCount > 0`, else the literal `"Open in Knowledge →"`.

    2. Export `type KnowledgePreviewNodeType = Node<KnowledgePreviewNodeData, "knowledge-preview">`
    (import `KnowledgePreviewNodeData` from `./node-data-schemas`, Plan 41-01's output).

    3. Export `KnowledgePreviewNode` as a `memo`-wrapped `NodeProps<KnowledgePreviewNodeType>`
    component, mirroring `GenuiPanelNode`'s `Handle`+outer-shell structure but using the FIXED `h-[240px]
    w-[320px]` shell + `SELECTED_RING`/elevation classes from 41-UI-SPEC.md §1's anatomy block verbatim
    (copy the exact className string). Inside: call `api.knowledge.expandNode.useQuery({ nodeId:
    data.focusNodeId, depth: 2 }, { staleTime: KNOWLEDGE_PREVIEW_STALE_TIME_MS })` (import `api` from
    `~/trpc/react`, mirroring `use-canvas-persistence.ts`'s exact import) — no `enabled` gate needed
    (`focusNodeId` is always present, validated at node-creation time by Plan 41-01's schema-shaped
    data). Declare `const KNOWLEDGE_PREVIEW_STALE_TIME_MS = 10_000;` as a module-level constant at the
    top of this file (41-CONTEXT.md: "TanStack defaults + a MODEST staleTime" — TanStack's own default
    is 0/always-stale; this matches the existing `knowledge.*` freshness tier `use-data-bindings.ts`'s
    `STALE_TIME_MS["knowledge.graph"]`/`["knowledge.byId"]` already use for the same domain, kept as an
    independent local constant since `expandNode` isn't one of `use-data-bindings.ts`'s 5 wired
    procedures). Compute `headerLabel =
    resolveHeaderLabel(data.label, query.data?.nodes, data.focusNodeId)`, `overflowCount =
    Math.max(0, (query.data?.nodes.length ?? 0) - MAX_PREVIEW_NODES)` (import `MAX_PREVIEW_NODES` from
    `./knowledge-preview-layout`, Plan 41-01's output), `footerCopy = resolveFooterCopy(overflowCount)`.

    4. Render the header exactly per 41-UI-SPEC.md §1's JSX: `Share2` icon (`size-3 text-primary`),
    truncating `headerLabel` span, and the remove `<button aria-label="Remove knowledge preview"
    onClick={(e) => { e.stopPropagation(); void deleteElements({ nodes: [{ id }] }); }}>` with an `X`
    icon — `deleteElements` from `useReactFlow()` (import `useReactFlow` from `@xyflow/react` alongside
    the existing `Handle`/`Position`/`NodeProps` imports). Render `<KnowledgePreviewMiniGraph
    focusNodeId={data.focusNodeId} nodes={query.data?.nodes ?? []} edges={query.data?.edges ?? []}
    isLoading={query.isPending} isError={query.isError} onRetry={() => void query.refetch()} />` as the
    body. Render the footer exactly per 41-UI-SPEC.md §3's JSX: a `<Link href={hrefFor("knowledge",
    data.focusNodeId)}>` (import `hrefFor` from `~/components/provenance-link`) showing `footerCopy`,
    with the exact class string from the UI-SPEC.

    5. In `node-types.ts`: import `KnowledgePreviewNode` from `./knowledge-preview-node`; add
    `"knowledge-preview": KnowledgePreviewNode` as the 3rd entry in the `nodeTypes` object literal
    (alongside `chat`/`genui-panel`, before `"unknown-node-type"` — order matches
    `NODE_TYPE_REGISTRY`'s own entry order for readability, though object-key order has no functional
    effect here). No other change to this file — `resolveNodeComponent`'s logic is generic.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_canvas/__tests__/knowledge-preview-node.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 8 behaviors have an independently named, passing test.
    - `grep -n "\"knowledge-preview\": KnowledgePreviewNode" apps/web/src/app/chat/_canvas/node-types.ts`
      matches.
    - `grep -n "api.knowledge.expandNode.useQuery" apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx`
      matches, with `depth: 2` in the same call (grep the surrounding lines).
    - `grep -c "@xyflow/react\" import.*ReactFlow\b\|<ReactFlow" apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx`
      is 0 (only `Handle`/`Position`/`NodeProps`/`Node`/`useReactFlow` types/hooks are legitimate
      imports here — never the `ReactFlow` component itself, DO-NOT 1).
    - `git diff --stat packages/genui/src/renderer/render-node.tsx packages/genui/src/renderer/spec-renderer.tsx apps/web/src/app/chat/_components/genui-part-boundary.tsx`
      is empty (locked files untouched, DO-NOT 6).
    - `cd apps/web && npx tsc --noEmit -p apps/web` reports no new errors.
  </acceptance_criteria>
  <done>KnowledgePreviewNode exists, matches the UI-SPEC shell/remove/footer anatomy exactly, is wired into node-types.ts as the 3rd entry, all 8 tests pass, no nested React Flow, locked files untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AddKnowledgePreviewPopover (creation affordance) + chat-canvas.tsx wiring (add flow + remove-triggers-save)</name>
  <files>
    apps/web/src/app/chat/_canvas/add-knowledge-preview-popover.tsx,
    apps/web/src/app/chat/_canvas/__tests__/add-knowledge-preview-popover.test.tsx,
    apps/web/src/app/chat/_canvas/chat-canvas.tsx
  </files>
  <read_first>
    .planning/phases/41-knowledge-preview-canvas-node/41-UI-SPEC.md section "6. \"Add knowledge
    preview\" Creation Affordance" IN FULL, verbatim (the exact toolbar-trigger JSX nesting order —
    `Popover` > `TooltipProvider` > `Tooltip` > `TooltipTrigger asChild` > `PopoverTrigger asChild` >
    `Button` — and the popover form JSX/copy).
    apps/web/src/app/chat/_canvas/edge-creation-picker.tsx in full (already read this session — the
    closest existing Popover-form precedent in this codebase; note it uses a controlled, ALWAYS-open
    `Popover` anchored via `PopoverAnchor` at a fixed screen point, which this task's popover does NOT
    do — this task's popover is a normal `PopoverTrigger`-driven popover, controlled `open` state only
    so it can programmatically close itself on successful add).
    apps/web/src/app/chat/_canvas/chat-canvas.tsx in full, re-read fresh (Task 1/2 of this plan did not
    touch it; confirm no drift) — specifically `handleEdgesChange` (the exact pattern
    `handleNodesChange` mirrors), the `<Panel position="top-right">` toolbar block (lines ~653-665
    at last read — re-locate by searching for `"Toggle minimap"`), `rfInstanceRef`/`handleInit`,
    `DRAG_HANDLE_SELECTOR`, `handlePaneClick`'s deselect-all pattern, and the `<ReactFlowJSX
    onNodesChange={onNodesChange} ...>` prop this task changes to `handleNodesChange`.
    apps/web/src/app/chat/_canvas/canvas-layout.ts's `CanvasRect`/`offsetCascadePosition`/
    `CANVAS_NODE_DIMENSIONS`/`DEFAULT_CANVAS_NODE_DIMENSIONS` exports (already read, Plan 41-01 added
    the `"knowledge-preview"` dimensions entry).
  </read_first>
  <behavior>
    - Test 1: the trigger `Button` renders with `aria-label="Add knowledge preview"`; clicking it makes
      the popover form fields (the node-id `Input`, the label `Input`, both buttons) present in the DOM.
    - Test 2: the "Add preview" button is `disabled` while the node-id input is empty or whitespace-only.
    - Test 3: typing a non-UUID string into the node-id input and clicking "Add preview" renders the
      inline error text `"Enter a valid knowledge node ID."`, does NOT call the `onAdd` prop, and the
      popover form is STILL present (did not close).
    - Test 4: typing a valid UUID (label left blank) and clicking "Add preview" calls `onAdd` exactly
      once with `(thatUuid, undefined)`; the popover form is no longer present afterward (closed) and
      no error text renders.
    - Test 5: typing a valid UUID AND a label, then clicking "Add preview", calls `onAdd` with
      `(thatUuid, "the typed label")`.
    - Test 6: clicking "Cancel" (while the popover is open, with or without text typed) closes the
      popover (form fields no longer present) and never calls `onAdd`.
  </behavior>
  <action>
    1. Implement `apps/web/src/app/chat/_canvas/add-knowledge-preview-popover.tsx`: export
    `AddKnowledgePreviewPopoverProps = { readonly onAdd: (focusNodeId: string, label: string |
    undefined) => void }` and `AddKnowledgePreviewPopover`. Internal state: `open` (boolean, controlled
    — `<Popover open={open} onOpenChange={setOpen}>`, needed so a successful add can close it
    programmatically, unlike an uncontrolled trigger-only popover), `nodeIdInput`, `labelInput`,
    `validationError` (string | null). Trigger JSX per 41-UI-SPEC.md §6's exact nesting: `Popover` >
    `TooltipProvider` > `Tooltip` > `TooltipTrigger asChild` wrapping `PopoverTrigger asChild` wrapping a
    `Button` (`variant="ghost" size="icon" aria-label="Add knowledge preview" className="size-11
    bg-background/70 backdrop-blur-md"`, `Share2` `size-4` icon inside) — `TooltipContent` shows "Add
    knowledge preview". `PopoverContent` (`align="end" className="w-72 space-y-3"`) renders the form per
    41-UI-SPEC.md §6's exact copy: heading "Add knowledge preview", a "Knowledge node ID" labeled
    `Input` (`placeholder="Paste a node ID…"`) with helper text "Find an ID on the /knowledge graph, or
    paste one from a chat citation chip." and the conditional error paragraph, a "Label (optional)"
    labeled `Input` (`placeholder="Custom name for this preview"`), and a `Cancel`/`Add preview` button
    row (`Add preview` `disabled={!nodeIdInput.trim()}`).

    2. `onAdd` click handler (named e.g. `handleAddClick`): `z.string().uuid().safeParse(nodeIdInput.trim())`
    — on failure, `setValidationError("Enter a valid knowledge node ID.")`, return (popover stays open,
    `onAdd` never called). On success: call `props.onAdd(parsedId, labelInput.trim() || undefined)`,
    then reset `nodeIdInput`/`labelInput`/`validationError` to their initial empty values and
    `setOpen(false)`. `Cancel` click handler: reset the same 3 fields and `setOpen(false)` — never calls
    `onAdd`.

    3. In `chat-canvas.tsx`: import `NodeChange` type from `@xyflow/react` (alongside the existing
    `EdgeChange` import) and `AddKnowledgePreviewPopover` from `./add-knowledge-preview-popover`; import
    `CANVAS_NODE_DIMENSIONS`, `DEFAULT_CANVAS_NODE_DIMENSIONS`, `offsetCascadePosition`, type
    `CanvasRect` from `./canvas-layout`. Add `handleNodesChange`, mirroring `handleEdgesChange`'s EXACT
    shape: calls `onNodesChange(changes)`, then `if (changes.some((c) => c.type === "remove"))
    persistence.scheduleSave(canvasStore)`. Change the `<ReactFlowJSX ... onNodesChange={onNodesChange}
    .../>` prop to `onNodesChange={handleNodesChange}`.

    4. Add `handleAddKnowledgePreview(focusNodeId: string, label: string | undefined): void`: computes
    a flow-space center point via `rfInstanceRef.current?.screenToFlowPosition({ x: window.innerWidth /
    2, y: window.innerHeight / 2 }) ?? { x: 0, y: 0 }`; builds `existingRects: CanvasRect[]` from the
    CURRENT `nodes` state (`nodes.map((n) => ({ x: n.position.x, y: n.position.y, ...(CANVAS_NODE_DIMENSIONS[n.type
    ?? ""] ?? DEFAULT_CANVAS_NODE_DIMENSIONS) }))`); computes `position = offsetCascadePosition({
    x: center.x, y: center.y, width: 320, height: 240 }, existingRects)`; builds a new `FlowNode` with
    `id: \`knowledge-preview:${crypto.randomUUID()}\``, `type: "knowledge-preview"`, `position`,
    `dragHandle: DRAG_HANDLE_SELECTOR`, `selected: true`, `data: { focusNodeId, ...(label ? { label } :
    {}) }`; calls `setNodes((prev) => [...prev.map((n) => (n.selected ? { ...n, selected: false } : n)),
    newNode])` (deselect existing, mirrors `handlePaneClick`'s pattern, then append the new selected
    node); calls `persistence.scheduleSave(canvasStore)`.

    5. Mount `<AddKnowledgePreviewPopover onAdd={handleAddKnowledgePreview} />` inside the existing
    `<Panel position="top-right">`: wrap it AND the pre-existing minimap-toggle `Button` in a new
    `<div className="flex items-center gap-2">`, with the popover FIRST (before the minimap button), per
    41-UI-SPEC.md §6's exact ordering.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_canvas/__tests__/add-knowledge-preview-popover.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 6 behaviors have an independently named, passing test.
    - `grep -n "z.string().uuid().safeParse\|z.string().uuid()" apps/web/src/app/chat/_canvas/add-knowledge-preview-popover.tsx`
      matches (boundary validation present, T-41-07).
    - `grep -n "handleNodesChange" apps/web/src/app/chat/_canvas/chat-canvas.tsx` shows both the
      definition AND its use as the `onNodesChange` prop passed to `ReactFlowJSX` (2+ occurrences).
    - `grep -n "AddKnowledgePreviewPopover" apps/web/src/app/chat/_canvas/chat-canvas.tsx` matches both
      the import and the JSX mount inside the top-right `Panel`.
    - `grep -n "scheduleSave" apps/web/src/app/chat/_canvas/chat-canvas.tsx` shows `handleAddKnowledgePreview`
      calling it (in addition to the pre-existing call sites — count increases by exactly 2: the new
      `handleNodesChange` remove-branch call plus `handleAddKnowledgePreview`'s call).
    - `cd apps/web && npx vitest run src/app/chat/_canvas/__tests__/chat-canvas.test.ts` — the
      PRE-EXISTING pure-function test file (`buildSpecsByProvenance`/`buildStreamingByProvenance`) still
      passes unchanged, 0 regressions.
    - `cd apps/web && npx tsc --noEmit -p apps/web` reports no new errors.
  </acceptance_criteria>
  <done>AddKnowledgePreviewPopover exists with UUID-validated creation, mounted in the toolbar; chat-canvas.tsx's node-remove path now triggers the same debounced save node-drag already uses; adding a preview creates a correctly-positioned, selected knowledge-preview node and schedules a save; all 6 popover tests + the pre-existing chat-canvas test file pass; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-pasted "knowledge node ID" (Add-preview popover) -> tRPC `expandNode` call / persisted node.data | Free-text input crossing into both a query param and a persisted canvas snapshot field |
| `knowledge.expandNode`'s response (`GraphNode.label`, tier strings) -> rendered DOM text/href/style | Server-returned, importer-scoped data rendered into the transcript-adjacent canvas surface |
| The new remove affordance -> canvas persistence | The FIRST node-level destructive action on this canvas — must actually persist, not silently no-op |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02 | Information Disclosure | The preview's empty/error states could be used to probe whether a given node id exists for another tenant | accept | Mitigated upstream and unchanged this phase: `expand.ts`'s T-32-02 (tenant-scoped joins) / T-32-03 (fail-closed empty response, never distinguishes "not found" from "not yours"). This plan's "This preview is unavailable." copy is deliberately vague per that same posture — never confirms/denies existence. |
| T-41-03 | Tampering (open-redirect-style) | Mini-graph node dots / footer link trusting a server-supplied route string | mitigate | Every href in this plan is computed via `hrefFor("knowledge", id)` (Phase 39's fixed, locally-computed switch) — `GraphNode` carries no `route` field to begin with, and this plan never introduces one; navigation targets are always locally derived from `id`, never a payload string |
| T-41-05 | Tampering (prototype pollution) | A persisted/replayed `knowledge-preview` node.data payload | accept | Already covered structurally, unchanged this plan, by `canvas-schema.ts`'s generic `nodeDataSchema` (`hasForbiddenKeyDeep` at any depth) — `focusNodeId`/`label` are flat string fields with nothing to nest-pollute |
| T-41-06 | Elevation / architectural hazard | A second `<ReactFlow>`/`ReactFlowProvider` being reached for inside either new component | mitigate | Both `knowledge-preview-mini-graph.tsx` and `knowledge-preview-node.tsx` are grep-verified to contain zero `<ReactFlow`/`ReactFlowProvider` usage — only `Handle`/`Position`/`NodeProps`/`useReactFlow` (a hook, not a mount) are legitimate `@xyflow/react` imports here |
| T-41-07 | Tampering | The Add-preview popover's free-text node-id input reaching a tRPC call or a persisted node/URL unvalidated | mitigate | `z.string().uuid().safeParse` gates `onAdd` — an invalid value never creates a node, never fires `expandNode`, never reaches a URL; inline error copy keeps the popover open instead |
| T-41-08 | Denial of Service | Unbounded/duplicate knowledge-preview node creation flooding the canvas | accept | The existing `MAX_CANVAS_NODES = 200` cap (`canvas-schema.ts`, unchanged this phase) already bounds total canvas nodes of any type; no new gate needed |

</threat_model>

<verification>
Run all 3 tasks' targeted vitest commands together, then the broader canvas-surface sweep + typecheck:
`cd apps/web && npx vitest run src/app/chat/_canvas/__tests__/knowledge-preview-mini-graph.test.tsx src/app/chat/_canvas/__tests__/knowledge-preview-node.test.tsx src/app/chat/_canvas/__tests__/add-knowledge-preview-popover.test.tsx`
`cd apps/web && npx vitest run src/app/chat/_canvas` (every pre-existing canvas-directory test, 0 regressions)
`cd apps/web && npx tsc --noEmit -p apps/web`
`git diff --stat packages/genui/src/renderer/render-node.tsx packages/genui/src/renderer/spec-renderer.tsx apps/web/src/app/chat/_components/genui-part-boundary.tsx` — confirm EMPTY.
`grep -rc "@xyflow/react\" import.*ReactFlow\b\|<ReactFlow " apps/web/src/app/chat/_canvas/knowledge-preview-mini-graph.tsx apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx` — confirm 0 in both.
</verification>

<success_criteria>
- A `knowledge-preview` node (3rd `NODE_TYPE_REGISTRY` entry, Plan 41-01) can be placed on the `/chat`
  canvas via the new toolbar "Add knowledge preview" affordance, by pasting a knowledge node UUID.
- The node renders a bounded (<=25 nodes, <=2-hop) non-interactive subgraph via `knowledge.expandNode`
  (`depth: 2`) — no second `<ReactFlow>` instance is ever mounted (grep-verified).
- Every node dot and the footer link deep-link to `/knowledge?focus={id}` via `hrefFor`, the same
  primitive Phase 39's `<ProvenanceLink>` uses.
- The node's remove button actually removes it AND persists that removal (the pre-existing gap this
  plan closes: node removal now triggers the same debounced save node-drag already used).
- Zero new npm dependencies; the 3 locked genui renderer files stay byte-identical.
- All new tests plus every pre-existing test in `apps/web/src/app/chat/_canvas` pass; typecheck clean.
</success_criteria>

<output>
Create `.planning/phases/41-knowledge-preview-canvas-node/41-02-SUMMARY.md` when done
</output>
