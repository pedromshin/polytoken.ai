---
phase: 33-live-bindings-plumbing
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - apps/web/src/app/chat/_canvas/genui-panel-node.tsx
  - apps/web/src/app/knowledge/_components/knowledge-graph.tsx
  - apps/web/src/app/chat/_canvas/__tests__/panel-data-flow.test.tsx
autonomous: true
requirements: [BIND-01, BIND-02]

must_haves:
  truths:
    - "A genui-panel node whose spec declares bindings renders live data merged into panelData, with live binding values winning on key collision"
    - "spec-renderer.tsx, render-node.tsx, and genui-part-boundary.tsx remain byte-identical to their locked state (diff-verified, zero lines changed)"
    - "A successful promotion mutation (POST /api/knowledge/edges/{id}/promote) triggers invalidation of knowledge.byId and knowledge.graph query caches, observable as a refetch of any bound panel showing that data"
    - "ALLOWED_PROCEDURES still lists exactly its original 9 entries, no new entries added"
  artifacts:
    - path: "apps/web/src/app/chat/_canvas/genui-panel-node.tsx"
      provides: "GenuiPanelNodeBody calls useDataBindings and spreads its result over panelData before GenuiPartBoundary"
    - path: "apps/web/src/app/knowledge/_components/knowledge-graph.tsx"
      provides: "handlePromote's success branch invalidates knowledge.byId and knowledge.graph via api.useUtils()"
  key_links:
    - from: "apps/web/src/app/chat/_canvas/genui-panel-node.tsx"
      to: "apps/web/src/app/chat/_canvas/use-data-bindings.ts"
      via: "import { useDataBindings } from \"./use-data-bindings\""
      pattern: "useDataBindings"
    - from: "apps/web/src/app/chat/_canvas/genui-panel-node.tsx"
      to: "apps/web/src/app/chat/_components/genui-part-boundary.tsx"
      via: "data={{ ...panelData, ...liveBindingData }} prop, GenuiPartBoundary itself untouched"
      pattern: "\\.\\.\\.panelData"
    - from: "apps/web/src/app/knowledge/_components/knowledge-graph.tsx"
      to: "~/trpc/react (api.useUtils)"
      via: "const utils = api.useUtils(); utils.knowledge.byId.invalidate(); utils.knowledge.graph.invalidate();"
      pattern: "utils\\.knowledge\\.(byId|graph)\\.invalidate"
---

<objective>
Wire the `use-data-bindings.ts` hook (built in 33-01) into `GenuiPanelNodeBody` so live binding data
merges into `panelData` before reaching the (untouched) `GenuiPartBoundary`/`SpecRenderer` chain, and
add event-driven cache invalidation to the real `/knowledge` promotion success path so bound
`knowledge.byId`/`knowledge.graph` panels refetch automatically. Includes an explicit diff-verification
step proving the 3 locked renderer files stay byte-identical (ROADMAP SC2).

Purpose: completes BIND-01 (live data reaching the render path) and BIND-02 (event-driven
invalidation half; staleTime tiers already shipped in 33-01).
Output: `genui-panel-node.tsx` wiring, `knowledge-graph.tsx` invalidation, extended
`panel-data-flow.test.tsx` coverage, and a proven-clean diff on the 3 locked files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-live-bindings-plumbing/33-CONTEXT.md
@.planning/phases/33-live-bindings-plumbing/33-01-SUMMARY.md
</context>

<interfaces>
<!-- Extracted from the codebase — executor should use these directly, no exploration needed. -->

From 33-01 (`apps/web/src/app/chat/_canvas/use-data-bindings.ts`):
```
export interface UseDataBindingsArgs { readonly specJson: string; readonly isStreaming: boolean; readonly panelData: Record<string, unknown>; }
export function useDataBindings(args: UseDataBindingsArgs): Record<string, unknown>
```

From `apps/web/src/app/chat/_canvas/genui-panel-node.tsx:62-148` (current `GenuiPanelNodeBody`):
```tsx
const { specJson, isStreaming } = useCanvasSpec(provenance);
const part = useCanvasPart(provenance);
...
const { data: panelData, dispatch } = usePanelData(panelId, incomingEdges);
const actions = usePanelActionRegistry(dispatch);
...
<GenuiPartBoundary specJson={specJson} isStreaming={isStreaming} data={panelData} actions={actions} variant="bare" />
```
This is the ONLY call site to change. `GenuiPartBoundary` itself (imported from
`../_components/genui-part-boundary`) is one of the 3 locked files — its call here gets a new `data`
VALUE, not a new prop or changed import; the component file itself is untouched.

From `apps/web/src/app/knowledge/_components/knowledge-graph.tsx:1-70` (imports) and `:503-546`
(`handlePromote`):
```tsx
"use client";
...
import { api } from "~/trpc/react";
...
const handlePromote = useCallback(async () => {
  ...
  const response = await fetch(`/api/knowledge/edges/${edgeId}/promote`, {...});
  if (!response.ok) { ...toast.error...; return; }
  const promotedEdgeId = selectedPopoverEdge.id;
  setEdges((prev) => prev.map((e) => { ... tier: "EXTRACTED" ...}));
  setSelectedEdgeId(null);
  setEdgePopoverAnchor(null);
} catch (error) { ... } finally { setPromotePending(false); }
```
`api` is already imported at line 63. Add `const utils = api.useUtils();` near the component's other
hooks (mirror `use-entity-curation.ts:47`'s `const utils = api.useUtils();` convention), then call
`utils.knowledge.byId.invalidate()` and `utils.knowledge.graph.invalidate()` inside the success branch
— right after the `setEdges` call, before `setSelectedEdgeId(null)`.

`api.useUtils()` returns the same object shape as `api.useContext()` (tRPC v11's context proxy) — each
router/procedure exposes `.invalidate(input?)`; calling with no `input` invalidates ALL cached queries
for that procedure regardless of input variation (correct here — we don't know which specific
`knowledge.byId` id or `knowledge.graph` importerId a chat panel has bound).

From `apps/web/src/app/chat/_canvas/__tests__/panel-data-flow.test.tsx` (existing integration test file
for the panel-data seam) — read this in full before editing; extend its existing render/assert pattern
(manual `createRoot`/`act`, per its imports of `react-dom/client`) rather than introducing a new test
harness.

Locked-files list for the SC2 diff-verification (do not touch, ever, in this plan):
- `packages/genui/src/renderer/spec-renderer.tsx`
- `packages/genui/src/renderer/render-node.tsx`
- `apps/web/src/app/chat/_components/genui-part-boundary.tsx`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire useDataBindings into GenuiPanelNodeBody + extend integration test</name>
  <files>
    apps/web/src/app/chat/_canvas/genui-panel-node.tsx
    apps/web/src/app/chat/_canvas/__tests__/panel-data-flow.test.tsx
  </files>
  <read_first>
    apps/web/src/app/chat/_canvas/genui-panel-node.tsx
    apps/web/src/app/chat/_canvas/use-data-bindings.ts
    apps/web/src/app/chat/_canvas/__tests__/panel-data-flow.test.tsx
    apps/web/src/app/chat/_components/genui-part-boundary.tsx
  </read_first>
  <behavior>
    - Test: extend `panel-data-flow.test.tsx` (or add a colocated new `describe` block within it) to
      mock `~/trpc/react`'s `api.useQueries` (same mock convention as 33-01's test file) and assert
      that when a rendered `GenuiPanelNodeBody`-equivalent tree resolves a binding, the merged `data`
      passed into `GenuiPartBoundary` contains the live value.
    - Test: assert collision precedence — when `panelData` already has a key that a live binding also
      resolves to, the live binding's value wins in the final merged object (`{ ...panelData,
      ...liveBindingData }` order, per CONTEXT's locked decision).
    - Test: assert that when `useDataBindings` returns `{}` (no bindings declared), the merged data
      passed to `GenuiPartBoundary` is referentially/structurally identical in content to plain
      `panelData` (no accidental key stripping or shape change) — this is the "zero renderer edits,
      unchanged behavior for specs without bindings" regression guard.
  </behavior>
  <action>
    In `genui-panel-node.tsx`'s `GenuiPanelNodeBody`, import `useDataBindings` from `./use-data-bindings`
    and call it right after `usePanelData`: `const liveBindingData = useDataBindings({ specJson,
    isStreaming, panelData });`. Replace the `data={panelData}` prop on the non-interactive-widget
    `GenuiPartBoundary` branch (the `else` arm of `isInteractiveWidget ? ... : (<GenuiPartBoundary
    ... data={panelData} .../>)`) with `data={{ ...panelData, ...liveBindingData }}` (immutable spread
    merge per CLAUDE.md, live keys last so they win). Leave the `InteractiveWidgetBoundary` branch's
    `data={panelData}` untouched — bindings apply only to genui_spec panels this phase (interactive
    widgets are a different D-08 surface, out of this phase's scope per the source audit). Update the
    file's module docstring only if it materially changes what a reader needs to know (optional, keep
    concise). Do NOT touch `genui-part-boundary.tsx`, `spec-renderer.tsx`, or `render-node.tsx`.
    Run tests until GREEN.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_canvas/__tests__/panel-data-flow.test.tsx</automated>
  </verify>
  <done>GenuiPanelNodeBody merges live binding data over panelData with correct collision precedence; extended tests pass; no changes outside the two listed files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire promotion-success invalidation + verify locked files byte-identical</name>
  <files>apps/web/src/app/knowledge/_components/knowledge-graph.tsx</files>
  <read_first>
    apps/web/src/app/knowledge/_components/knowledge-graph.tsx
    apps/web/src/app/entities/[id]/_components/use-entity-curation.ts
  </read_first>
  <behavior>
    - Test (check for an existing test file under `apps/web/src/app/knowledge/_components/__tests__/`
      first; if none exists for `knowledge-graph.tsx`'s promote flow, add a minimal new test file
      `apps/web/src/app/knowledge/_components/__tests__/knowledge-graph-invalidate.test.tsx` mocking
      `~/trpc/react`'s `api.useUtils` to return `{ knowledge: { byId: { invalidate: vi.fn() },
      graph: { invalidate: vi.fn() } } }` and a mocked `global.fetch` resolving
      `{ ok: true, json: async () => ({}) }`): assert that triggering the promote flow results in both
      `invalidate` mocks being called exactly once each, AFTER the `fetch` resolves successfully.
    - Test: assert invalidation is NOT called when the fetch response is `!ok` (error branch returns
      early before invalidation per the existing `if (!response.ok) { toast.error(...); return; }`
      guard — this is a regression guard proving the mitigation doesn't fire on failed promotions).
  </behavior>
  <action>
    Add `const utils = api.useUtils();` inside the component function (near wherever other top-level
    hooks are declared, e.g. alongside existing `useState`/`useCallback` declarations before
    `handlePromote`). Inside `handlePromote`'s success branch, immediately after the existing
    `setEdges((prev) => ...)` call and before `setSelectedEdgeId(null)`, add:
    `utils.knowledge.byId.invalidate();` then `utils.knowledge.graph.invalidate();` (both called with
    no input — invalidates all cached variants of each procedure, since a bound chat panel's specific
    id/importerId is unknown to this call site). Do not add any new imports beyond what's already
    present (`api` is already imported at line 63). Then run the byte-identical verification for the 3
    locked renderer files (see interfaces section for the exact list) via `git diff --stat` scoped to
    those 3 paths and confirm the output is EMPTY (no lines). Record this command's empty output in
    the plan SUMMARY as the SC2 proof artifact.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/knowledge/_components/__tests__/knowledge-graph-invalidate.test.tsx</automated>
  </verify>
  <done>
    handlePromote's success branch calls both invalidate methods exactly once on success and zero
    times on a non-ok response; `git diff --stat -- packages/genui/src/renderer/spec-renderer.tsx
    packages/genui/src/renderer/render-node.tsx apps/web/src/app/chat/_components/genui-part-boundary.tsx`
    (run from the repo root) produces empty output, proving byte-identical locked files (SC2).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `/knowledge` promote success → chat canvas cache | A REST-triggered cache invalidation crossing into a shared browser-side TanStack QueryClient consumed by an unrelated route's panels |
| Locked renderer files → this plan's edits | The 3 files must never be modified by any task in this plan; a diff leak here would silently break v1.3's locked-file guarantee for downstream phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-33-05 | Repudiation | Locked-file drift | mitigate | Task 2's explicit `git diff --stat` check against the 3 locked paths, asserted empty, recorded in SUMMARY — not just an implicit "don't touch" instruction |
| T-33-06 | Tampering | `handlePromote`'s invalidate calls firing on a failed promotion | mitigate | Invalidation calls placed strictly after the `if (!response.ok) return;` guard, proven by Task 2's negative test |
| T-33-07 | Information Disclosure | `utils.knowledge.*.invalidate()` with no input | accept | Invalidating without a specific input triggers a refetch using each panel's own already-scoped query args (tenant scoping enforced server-side by the existing `knowledge.byId`/`knowledge.graph` procedures, unchanged this phase) — invalidation itself carries no data, only a refetch signal |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `cd apps/web && npx vitest run src/app/chat/_canvas/__tests__/panel-data-flow.test.tsx` — green.
- `cd apps/web && npx vitest run src/app/knowledge/_components/__tests__/knowledge-graph-invalidate.test.tsx` — green.
- From repo root: `git diff --stat -- packages/genui/src/renderer/spec-renderer.tsx packages/genui/src/renderer/render-node.tsx apps/web/src/app/chat/_components/genui-part-boundary.tsx` — empty output (SC2).
- `grep -n "\"emails.list\"\|\"emails.byId\"\|\"entityTypes.list\"\|\"knowledge.list\"" packages/genui/src/generation/allowed-procedures.ts` still shows the original 9-entry list, unchanged (SC5) — no new entries added anywhere in this plan.
- `npx tsc --noEmit -p apps/web` clean.
</verification>

<success_criteria>
A genui canvas panel with `spec.bindings` now renders live data end-to-end (merged into `panelData`
before `GenuiPartBoundary`), a successful `/knowledge` promotion invalidates the two knowledge query
caches so any bound chat panel refetches automatically, and the 3 locked renderer files are proven
byte-identical via an explicit diff check. Combined with 33-01, all 5 ROADMAP Phase 33 success
criteria (SC1–SC5) and both requirements (BIND-01, BIND-02) are satisfied.
</success_criteria>

<output>
Create `.planning/phases/33-live-bindings-plumbing/33-02-SUMMARY.md` when done
</output>
