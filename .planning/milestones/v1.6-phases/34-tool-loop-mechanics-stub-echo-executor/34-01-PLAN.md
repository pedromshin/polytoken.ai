---
phase: 34-tool-loop-mechanics-stub-echo-executor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/email-listener/app/domain/ports/tool_executor.py
  - apps/email-listener/app/domain/services/chat_model_registry.py
  - apps/email-listener/app/application/use_cases/run_chat_turn_tool_loop.py
  - apps/email-listener/tests/support/__init__.py
  - apps/email-listener/tests/support/echo_tool_executor.py
  - apps/email-listener/tests/test_chat_model_registry.py
  - apps/email-listener/tests/application/test_run_chat_turn_tool_loop.py
  - apps/email-listener/tests/support/test_echo_tool_executor.py
autonomous: true
requirements: [LOOP-01]
must_haves:
  truths:
    - "A ToolExecutor domain port exists with an async execute(...) -> ToolExecutionResult contract, importable by the application layer without touching infrastructure"
    - "ChatModelCapabilities carries max_tool_rounds; only the 2 Bedrock Claude entries are set to 4, every other entry stays 0"
    - "An echo stub executor returns its arguments, caps output, and can be forced to return is_error=True"
    - "Pure loop helpers build tool_invocation / tool_invocation_result parts, a native tool_result content block, classify tool dispatch, and cap output size — all without I/O"
  artifacts:
    - path: "apps/email-listener/app/domain/ports/tool_executor.py"
      provides: "ToolExecutor Protocol + ToolExecutionResult dataclass + quarantine obligation docstring"
      contains: "class ToolExecutor"
    - path: "apps/email-listener/app/application/use_cases/run_chat_turn_tool_loop.py"
      provides: "Pure tool-loop helpers (part builders, synthetic tool_result block, dispatch classifier, output cap, visible-text constants)"
      exports: ["build_tool_invocation_part", "build_tool_invocation_result_part", "build_synthetic_tool_result_message", "classify_tool_dispatch", "cap_tool_output"]
    - path: "apps/email-listener/tests/support/echo_tool_executor.py"
      provides: "EchoToolExecutor test double implementing the ToolExecutor port"
      contains: "class EchoToolExecutor"
  key_links:
    - from: "apps/email-listener/tests/support/echo_tool_executor.py"
      to: "apps/email-listener/app/domain/ports/tool_executor.py"
      via: "implements ToolExecutor / returns ToolExecutionResult"
      pattern: "ToolExecutionResult"
    - from: "apps/email-listener/app/application/use_cases/run_chat_turn_tool_loop.py"
      to: "apps/email-listener/app/domain/ports/tool_executor.py"
      via: "type-hints ToolExecutionResult in build_synthetic_tool_result_message"
      pattern: "ToolExecutionResult"
---

<objective>
Lay down the dependency-free primitives the mid-turn tool loop is built from: the new `ToolExecutor`
domain port + `ToolExecutionResult`, the `max_tool_rounds` capability gate on the 2 Bedrock Claude
registry entries, a pure tool-loop helper module, and an echo stub executor for tests. None of this
touches `run_chat_turn.py` — it is the contract layer that Plan 34-03 wires into `_execute_turn`.

Purpose: de-risk the highest-uncertainty phase (research: "prove the loop with a stub first") by
freezing the contracts and pure logic before the streaming loop consumes them.
Output: `tool_executor.py` port, registry `max_tool_rounds` field, `run_chat_turn_tool_loop.py`
pure helpers, `EchoToolExecutor` test double, and unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-tool-loop-mechanics-stub-echo-executor/34-CONTEXT.md

<interfaces>
<!-- Existing shapes the new code must mirror. Extracted from the codebase. -->

From app/domain/ports/chat_provider.py — the delta the loop will emit (already modeled, never emitted):
```python
@dataclass(frozen=True)
class ToolResultDelta:
    tool_use_id: str
    content: str
    is_error: bool = False
```

From app/domain/services/chat_model_registry.py — the frozen dataclass to extend and the 2 Bedrock entries:
```python
@dataclass(frozen=True)
class ChatModelCapabilities:
    tools: bool
    genui: bool
    streaming: bool
    context_tokens: int
# 2 Bedrock Claude entries: id="us.anthropic.claude-sonnet-4-6" and
# id="us.anthropic.claude-haiku-4-5-20251001-v1:0" (transport="bedrock").
# All other entries are transport="openrouter"/"browser".
```

From app/application/use_cases/run_chat_turn_widgets.py — the pure-helper module pattern to mirror
(no I/O, no ports imported at module level except domain), plus the existing dispatch name set:
```python
INTERACTIVE_WIDGET_TOOL_NAMES: tuple[str, ...] = ("emit_proposal_cards", "emit_clarify_widget")
EMIT_UI_SPEC_TOOL_NAME = "emit_ui_spec"  # (from app/infrastructure/llm/chat_tools.py)
```

Import-linter contract (pyproject.toml): `app.application` MUST NOT import `app.infrastructure`
or `app.presentation`; `app.domain` imports nothing outside domain + stdlib/structlog. The new
port lives in `app.domain.ports`; the helper module lives in `app.application.use_cases` and may
import only `app.domain.*` + stdlib.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ToolExecutor domain port + ToolExecutionResult</name>
  <files>apps/email-listener/app/domain/ports/tool_executor.py</files>
  <read_first>
    - apps/email-listener/app/domain/ports/chat_provider.py (ToolResultDelta shape at :52-64; frozen-dataclass + Protocol idiom to mirror)
    - apps/email-listener/app/domain/ports/knowledge_graph_repository.py (an existing domain port's docstring/Protocol style to match)
    - apps/email-listener/app/application/use_cases/run_chat_turn.py (:88-92 — confirm run_chat_turn imports domain ports only; the new port must be importable there)
  </read_first>
  <action>
    Create `app/domain/ports/tool_executor.py`. Define a frozen dataclass
    `ToolExecutionResult` with fields `tool_use_id: str`, `content: str`, `is_error: bool = False`
    (field names match `ToolResultDelta` so the loop can map 1:1). Define a
    `ToolExecutor` Protocol with a single async method
    `async def execute(self, *, name: str, arguments: dict[str, Any]) -> ToolExecutionResult: ...`.
    The class docstring MUST state the quarantine obligation (Fork 3 ⊗ 4 conflict resolution):
    "Executors return tier-filtered / quarantined typed payloads, never raw retrieved text —
    only EXTRACTED-tier text may enter model context (enforcement lands in Phase 38 / QUAR-01;
    the obligation is stated here so a future executor built against this port alone cannot ship
    raw body." Add a module-level constant `MAX_TOOL_OUTPUT_CHARS = 2000` with a comment that
    executors cap their own `content` at this boundary (history trimming does not cover in-round
    messages). `from __future__ import annotations`; import only stdlib (`dataclasses`, `typing`).
    No infrastructure, no I/O.
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run python -c "from app.domain.ports.tool_executor import ToolExecutor, ToolExecutionResult, MAX_TOOL_OUTPUT_CHARS; assert MAX_TOOL_OUTPUT_CHARS == 2000"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/email-listener/app/domain/ports/tool_executor.py` exists and defines `ToolExecutor`, `ToolExecutionResult`, `MAX_TOOL_OUTPUT_CHARS`
    - `ToolExecutionResult` is a frozen dataclass with fields `tool_use_id: str`, `content: str`, `is_error: bool = False`
    - `ToolExecutor.execute` is async and returns `ToolExecutionResult`
    - The docstring contains the substring "never raw" (the quarantine obligation)
    - `cd apps/email-listener && uv run lint-imports` passes (module imports no infrastructure)
    - `cd apps/email-listener && uv run mypy app/domain/ports/tool_executor.py` is clean
  </acceptance_criteria>
  <done>The ToolExecutor port + ToolExecutionResult exist, importable, mypy-clean, lint-imports-clean, docstring states the never-raw obligation.</done>
</task>

<task type="auto">
  <name>Task 2: max_tool_rounds capability gate on the 2 Bedrock Claude entries</name>
  <files>apps/email-listener/app/domain/services/chat_model_registry.py, apps/email-listener/tests/test_chat_model_registry.py</files>
  <read_first>
    - apps/email-listener/app/domain/services/chat_model_registry.py (whole file — ChatModelCapabilities at :33-41; the 2 Bedrock entries at :66-85; the 5 openrouter/browser entries; chat_registry_version() at :146)
    - apps/email-listener/tests/test_chat_model_registry.py (existing assertions — version determinism/64-hex only, no golden hash; add the new test alongside)
  </read_first>
  <action>
    Add `max_tool_rounds: int = 0` as a new field on the frozen dataclass `ChatModelCapabilities`
    (default 0 = server tools disabled — the field doubles as the capability gate; NO second boolean).
    Update the `ChatModelCapabilities(...)` construction for the 2 Bedrock Claude entries ONLY —
    `id="us.anthropic.claude-sonnet-4-6"` and `id="us.anthropic.claude-haiku-4-5-20251001-v1:0"` —
    to pass `max_tool_rounds=4`. Leave every OpenRouter entry (`deepseek/deepseek-chat`,
    `qwen/qwen-2.5-72b-instruct`, `z-ai/glm-4.6`, `google/gemma-2-27b-it`) and the browser entry
    (`webllm-qwen3-4b`) at the default 0 (do not pass the kwarg, or pass 0). Do NOT touch any TS
    registry or packages/* — the Python `chat_registry_version()` hash will change, which is expected
    and harmless (it is a separate hash from the genui catalog `_REGISTRY_VERSION`; no golden value
    is asserted anywhere). In `tests/test_chat_model_registry.py` add a test
    `test_only_bedrock_claude_entries_enable_tool_rounds` asserting: both bedrock entries have
    `capabilities.max_tool_rounds == 4`, and every non-bedrock entry has
    `capabilities.max_tool_rounds == 0`.
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/test_chat_model_registry.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `ChatModelCapabilities` has field `max_tool_rounds: int = 0`
    - `get_model("us.anthropic.claude-sonnet-4-6").capabilities.max_tool_rounds == 4`
    - `get_model("us.anthropic.claude-haiku-4-5-20251001-v1:0").capabilities.max_tool_rounds == 4`
    - Every entry with `transport != "bedrock"` has `max_tool_rounds == 0` (asserted by the new test)
    - `tests/test_chat_model_registry.py` passes including the pre-existing determinism/64-hex tests
    - `cd apps/email-listener && uv run mypy app/domain/services/chat_model_registry.py` clean
  </acceptance_criteria>
  <done>The capability gate exists; exactly the 2 Bedrock Claude entries carry max_tool_rounds=4; a test proves it; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Pure tool-loop helpers + EchoToolExecutor stub</name>
  <files>apps/email-listener/app/application/use_cases/run_chat_turn_tool_loop.py, apps/email-listener/tests/support/__init__.py, apps/email-listener/tests/support/echo_tool_executor.py, apps/email-listener/tests/application/test_run_chat_turn_tool_loop.py, apps/email-listener/tests/support/test_echo_tool_executor.py</files>
  <read_first>
    - apps/email-listener/app/application/use_cases/run_chat_turn_widgets.py (whole file — the pure-helper module pattern: no I/O, docstring style, __all__, part-dict shapes; content_block_stand_in at :253)
    - apps/email-listener/app/domain/ports/tool_executor.py (ToolExecutionResult + MAX_TOOL_OUTPUT_CHARS from Task 1)
    - apps/email-listener/app/application/use_cases/run_chat_turn.py (:679-716 _finalize_pending_tool — how emit_ui_spec/widget parts are shaped; :745-766 _provider_content_blocks — how parts become Anthropic blocks; INTERACTIVE_WIDGET_TOOL_NAMES import at :64-69)
    - apps/email-listener/tests/conftest.py (shared fixture conventions; confirm a tests/support package is import-safe)
  </read_first>
  <action>
    Create `app/application/use_cases/run_chat_turn_tool_loop.py` — pure functions only (mirror
    run_chat_turn_widgets.py), importing only `app.domain.*` + stdlib. Provide:
    (1) `build_tool_invocation_part(tool_name: str, tool_use_id: str, arguments: dict[str, Any]) -> dict[str, Any]`
        returning `{"type": "tool_invocation", "toolUseId": ..., "toolName": ..., "arguments": ...}`
        (NEW part type `tool_invocation`; NOT reusing `interactive_widget`).
    (2) `build_tool_invocation_result_part(result: ToolExecutionResult, tool_name: str) -> dict[str, Any]`
        returning `{"type": "tool_invocation_result", "toolUseId": ..., "toolName": ..., "content": ..., "isError": ...}`.
    (3) `build_synthetic_tool_result_message(result: ToolExecutionResult) -> dict[str, Any]`
        returning the native Bedrock block `{"role": "user", "content": [{"type": "tool_result",
        "tool_use_id": result.tool_use_id, "content": result.content, "is_error": result.is_error}]}`
        (the Bedrock adapter accepts native tool_result blocks verbatim — prefer over string fencing).
    (4) `classify_tool_dispatch(tool_name: str, server_tool_names: Collection[str]) -> str` returning
        one of `"server"` (name in server_tool_names), `"widget"` (name in INTERACTIVE_WIDGET_TOOL_NAMES),
        `"emit_ui_spec"` (name == EMIT_UI_SPEC_TOOL_NAME), or `"unknown"`. Precedence: server first.
    (5) `cap_tool_output(text: str, limit: int = MAX_TOOL_OUTPUT_CHARS) -> str` — truncate to `limit`
        chars appending a visible " …[truncated]" marker when cut.
    (6) module constants `PARSE_FAILURE_TEXT = "I couldn't parse that lookup result, so I stopped
        before using it. Could you rephrase your request?"` and `ROUND_CAP_EXHAUSTED_TEXT = "I
        couldn't fully resolve that after several lookups. Here's what I have so far."` (exact strings;
        consumed by Plans 34-02/34-03). `EMIT_UI_SPEC_TOOL_NAME` is defined locally as the literal
        "emit_ui_spec" (NOT imported from infrastructure — the import-linter forbids it), with a
        comment that it mirrors chat_tools.EMIT_UI_SPEC_TOOL_NAME. `INTERACTIVE_WIDGET_TOOL_NAMES`
        is imported from run_chat_turn_widgets (same application layer). Add `__all__`.
    Create `tests/support/__init__.py` (empty package marker) and `tests/support/echo_tool_executor.py`
    with `class EchoToolExecutor` implementing the ToolExecutor port: `execute(name, arguments)`
    returns `ToolExecutionResult(tool_use_id=arguments.get("tool_use_id", "echo"),
    content=cap_tool_output(json.dumps(arguments)), is_error=False)`; when
    `arguments.get("__force_error__")` is truthy it returns `is_error=True` with a fixed error string;
    when `arguments.get("__sleep__")` is set it `await asyncio.sleep(<that value>)` first (so 34-03
    can drive the timeout path). Add unit tests: `tests/application/test_run_chat_turn_tool_loop.py`
    (part builders, synthetic block shape, classify precedence incl. unknown, cap truncation marker)
    and `tests/support/test_echo_tool_executor.py` (echo round-trips args, forced-error → is_error,
    cap respected).
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/application/test_run_chat_turn_tool_loop.py tests/support/test_echo_tool_executor.py -x -q && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `run_chat_turn_tool_loop.py` exports all of: `build_tool_invocation_part`, `build_tool_invocation_result_part`, `build_synthetic_tool_result_message`, `classify_tool_dispatch`, `cap_tool_output`, `PARSE_FAILURE_TEXT`, `ROUND_CAP_EXHAUSTED_TEXT`
    - `build_tool_invocation_part(...)["type"] == "tool_invocation"` and `build_tool_invocation_result_part(...)["type"] == "tool_invocation_result"`
    - `build_synthetic_tool_result_message(...)` yields `{"role":"user","content":[{"type":"tool_result", ...}]}` with `tool_use_id`/`content`/`is_error` keys
    - `classify_tool_dispatch("emit_ui_spec", set())` == "emit_ui_spec"; `classify_tool_dispatch("emit_proposal_cards", set())` == "widget"; `classify_tool_dispatch("echo", {"echo"})` == "server"; `classify_tool_dispatch("nope", set())` == "unknown"
    - `cap_tool_output("x"*5000)` length <= 2000 + marker length and ends with the truncated marker
    - `EchoToolExecutor().execute(...)` returns a `ToolExecutionResult`; `__force_error__` → `is_error=True`
    - `uv run lint-imports` passes (helper module imports no infrastructure)
    - both new test files pass
  </acceptance_criteria>
  <done>Pure loop helpers + echo stub exist, all unit tests green, lint-imports clean, new part types named tool_invocation/tool_invocation_result.</done>
</task>

</tasks>

<verification>
- `cd apps/email-listener && uv run pytest tests/test_chat_model_registry.py tests/application/test_run_chat_turn_tool_loop.py tests/support/test_echo_tool_executor.py -q` all green
- `cd apps/email-listener && uv run lint-imports` clean (no application→infrastructure, no domain external deps)
- `cd apps/email-listener && uv run mypy app/domain/ports/tool_executor.py app/domain/services/chat_model_registry.py app/application/use_cases/run_chat_turn_tool_loop.py` clean
- No file outside `apps/email-listener/**` or `.planning/**` modified; no migration created
</verification>

<success_criteria>
ToolExecutor port + ToolExecutionResult exist with the never-raw obligation; max_tool_rounds=4 on
exactly the 2 Bedrock Claude entries (0 elsewhere); pure loop helpers + echo stub exist and are
unit-tested; import-linter and mypy clean.
</success_criteria>

<output>
Create `.planning/phases/34-tool-loop-mechanics-stub-echo-executor/34-01-SUMMARY.md` when done.
</output>
