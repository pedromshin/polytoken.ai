---
phase: 34-tool-loop-mechanics-stub-echo-executor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/email-listener/app/application/use_cases/run_chat_turn.py
  - apps/email-listener/tests/application/test_run_chat_turn_tool_loop_bugfixes.py
autonomous: true
requirements: [LOOP-02]
must_haves:
  truths:
    - "Token usage accumulates across multiple UsageDelta events in one turn — summed, not overwritten (a test asserts summed totals across >=2 rounds)"
    - "A malformed/truncated tool-call argument produces a VISIBLE text part explaining the lookup failed, never a silent drop"
  artifacts:
    - path: "apps/email-listener/app/application/use_cases/run_chat_turn.py"
      provides: "UsageDelta accumulation in _apply_delta; visible-text-on-parse-failure in _finalize_pending_tool"
      contains: "state.input_tokens + delta.input_tokens"
    - path: "apps/email-listener/tests/application/test_run_chat_turn_tool_loop_bugfixes.py"
      provides: "Regression tests for both latent bugs"
      contains: "def test_"
  key_links:
    - from: "apps/email-listener/app/application/use_cases/run_chat_turn.py"
      to: "apps/email-listener/app/application/use_cases/run_chat_turn_tool_loop.py"
      via: "PARSE_FAILURE_TEXT import for the visible parse-failure part"
      pattern: "PARSE_FAILURE_TEXT"
---

<objective>
Fix the 2 concrete latent bugs research found in the existing single-round chat turn, BEFORE the
round loop (Plan 34-03) amplifies them: (1) `UsageDelta` overwrites instead of accumulating —
harmless with one delta, silently under-reports cost the moment multiple rounds each emit a
UsageDelta; (2) a tool-call whose JSON never parses is silently dropped with only a logger.warning —
the 2026-07-06 truncated-tool-call lesson. This plan fixes the terminal (existing) path for both;
Plan 34-03 adds the analogous fix on the new server-tool round path.

Purpose: LOOP-02 — "never silent" is the phase motto; make cost honest across rounds and make a
parse failure visible to the user. Resolves pending todo `2026-07-06-salvage-truncated-tool-calls`.
Output: a corrected `run_chat_turn.py` accumulation fold + visible-text parse-failure, with tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-tool-loop-mechanics-stub-echo-executor/34-CONTEXT.md

<interfaces>
<!-- The two exact bug sites in run_chat_turn.py (line numbers approximate; match by code). -->

Bug 1 — _apply_delta UsageDelta branch OVERWRITES (around :665-667):
```python
if isinstance(delta, UsageDelta):
    state = replace(state, input_tokens=delta.input_tokens, output_tokens=delta.output_tokens)
    return state, []
```

Bug 2 — _finalize_pending_tool silently drops on parse failure (widget path around :700-703,
emit_ui_spec path around :710-714):
```python
if widget_part is None:
    logger.warning("interactive_widget_tool_call_parse_failed", ...)
    return cleared, None            # <-- silent drop
...
except (json.JSONDecodeError, TypeError):
    logger.warning("emit_ui_spec_tool_call_parse_failed", ...)
    return cleared, None            # <-- silent drop
```

`_TurnState.parts` is a tuple of part dicts; a plain visible text part is `{"type":"text","text": ...}`.
`PARSE_FAILURE_TEXT` is the exact string exported by run_chat_turn_tool_loop.py (Plan 34-01) — but
note 34-02 runs in the same wave as 34-01; if that import is not yet present at execution time, define
the identical literal locally with a comment referencing run_chat_turn_tool_loop.PARSE_FAILURE_TEXT
and let a later refactor dedupe. Exact string: "I couldn't parse that lookup result, so I stopped
before using it. Could you rephrase your request?"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Accumulate UsageDelta across rounds (bug 1)</name>
  <files>apps/email-listener/app/application/use_cases/run_chat_turn.py, apps/email-listener/tests/application/test_run_chat_turn_tool_loop_bugfixes.py</files>
  <read_first>
    - apps/email-listener/app/application/use_cases/run_chat_turn.py (:626-669 _apply_delta — the UsageDelta branch; :106-124 _TurnState with input_tokens/output_tokens; :473 where state.input_tokens/output_tokens is emitted as the usage event)
    - apps/email-listener/tests/application/test_run_chat_turn_interactive_widget.py (:76-253 — the full set of in-memory fakes + _make_use_case + FakeChatProvider streaming a delta list; reuse this shape)
  </read_first>
  <behavior>
    - Test: a provider streaming two `UsageDelta(input_tokens=10, output_tokens=20)` then
      `UsageDelta(input_tokens=5, output_tokens=7)` (with a StreamEnd) results in the persisted
      assistant message / usage event reporting input_tokens=15, output_tokens=27 — summed, not the
      last delta's 5/7.
    - Test: a single UsageDelta still reports exactly its own values (no regression to the 1-round case).
  </behavior>
  <action>
    In `_apply_delta`'s `UsageDelta` branch, change the overwrite to accumulation:
    `state = replace(state, input_tokens=state.input_tokens + delta.input_tokens,
    output_tokens=state.output_tokens + delta.output_tokens)`. Update the branch docstring line to say
    "records the real captured token counts, ACCUMULATING across multiple UsageDelta events (a
    multi-round turn emits one per round)". Add `tests/application/test_run_chat_turn_tool_loop_bugfixes.py`
    with the two behavior tests above — copy the fakes/_make_use_case scaffold from
    test_run_chat_turn_interactive_widget.py (or import them if that file exposes them cleanly;
    prefer a local copy to avoid coupling). Assert against the FakeCostLedgerRepository's recorded
    UsageEvent and/or the persisted assistant message status path.
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/application/test_run_chat_turn_tool_loop_bugfixes.py -x -q -k usage</automated>
  </verify>
  <acceptance_criteria>
    - `run_chat_turn.py` `_apply_delta` UsageDelta branch contains `state.input_tokens + delta.input_tokens` and `state.output_tokens + delta.output_tokens`
    - Test streaming 2 UsageDeltas (10/20 then 5/7) asserts recorded usage == input 15 / output 27
    - Single-UsageDelta test asserts exact passthrough (no double-count regression)
    - `grep -n "input_tokens=delta.input_tokens" run_chat_turn.py` returns no match (the overwrite is gone)
  </acceptance_criteria>
  <done>UsageDelta accumulates; summed-total test green; the overwrite line no longer exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Visible text part on tool-call parse failure (bug 2, terminal path)</name>
  <files>apps/email-listener/app/application/use_cases/run_chat_turn.py, apps/email-listener/tests/application/test_run_chat_turn_tool_loop_bugfixes.py</files>
  <read_first>
    - apps/email-listener/app/application/use_cases/run_chat_turn.py (:679-716 _finalize_pending_tool — BOTH the widget-path None-return at :700-703 and the emit_ui_spec except at :710-714; :672-676 _flush_text_buffer for how a text part is appended to parts)
    - apps/email-listener/app/application/use_cases/run_chat_turn_tool_loop.py (PARSE_FAILURE_TEXT constant from Plan 34-01)
    - apps/email-listener/tests/application/test_run_chat_turn_interactive_widget.py (:261-300 — how a completed turn's persisted parts are asserted)
  </read_first>
  <behavior>
    - Test: a genui-capable model streams a `ToolCallDelta` for emit_ui_spec whose partial_json is
      truncated invalid JSON (e.g. '{"type": "SpecRoot", "chi') then StreamEnd — the persisted
      assistant message MUST contain a `{"type":"text"}` part whose text == PARSE_FAILURE_TEXT, and
      MUST NOT contain a genui_spec part. The turn still ends `completed` (not failed).
    - Test: the same for an interactive-widget tool (emit_proposal_cards) with malformed JSON — a
      visible text part appears, no interactive_widget part, turn completes.
  </behavior>
  <action>
    In `_finalize_pending_tool`, replace BOTH silent `return cleared, None` drops with a path that
    appends a visible text part. Define the visible text via `PARSE_FAILURE_TEXT` (import from
    run_chat_turn_tool_loop if available at execution time, else a local literal matching it exactly —
    see <interfaces>). For the widget path (build_interactive_widget_part returned None) and the
    emit_ui_spec `except (json.JSONDecodeError, TypeError)` path: keep the existing `logger.warning`
    (server-side detail retained) but instead of dropping, return
    `replace(cleared, parts=(*cleared.parts, {"type": "text", "text": PARSE_FAILURE_TEXT})), None`
    (append the text part; no tool_result event since there is no valid tool output). Do NOT change
    the happy paths. This makes a truncated/malformed tool call surface to the user in every existing
    terminal path — the REQUIRED "never silent" behavior of the salvage todo (full lenient JSON-prefix
    repair remains an optional future enhancement, out of scope here). Add the two behavior tests to
    `test_run_chat_turn_tool_loop_bugfixes.py`.
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/application/test_run_chat_turn_tool_loop_bugfixes.py -x -q -k parse</automated>
  </verify>
  <acceptance_criteria>
    - Both former `return cleared, None` silent-drop sites in `_finalize_pending_tool` now append a `{"type":"text","text": PARSE_FAILURE_TEXT}` part
    - Malformed emit_ui_spec JSON test: persisted parts include the text part == PARSE_FAILURE_TEXT and NO genui_spec part; turn status "completed"
    - Malformed emit_proposal_cards JSON test: persisted parts include the visible text part and NO interactive_widget part; turn "completed"
    - `logger.warning(...)` calls are retained (server-side detail), asserted absent-of-silent-drop via the persisted part, not by log inspection
    - `cd apps/email-listener && uv run mypy app/application/use_cases/run_chat_turn.py` clean; `uv run lint-imports` clean
  </acceptance_criteria>
  <done>Both terminal parse-failure paths surface a visible text part; tests green; salvage-todo REQUIRED behavior met.</done>
</task>

</tasks>

<verification>
- `cd apps/email-listener && uv run pytest tests/application/test_run_chat_turn_tool_loop_bugfixes.py tests/application/test_run_chat_turn_interactive_widget.py tests/application/test_run_chat_turn_clarify_widget.py -q` all green (no regression to existing single-round behavior)
- `cd apps/email-listener && uv run pytest tests/application/test_run_chat_turn.py -q` (existing regression suite) green
- `cd apps/email-listener && uv run mypy app/application/use_cases/run_chat_turn.py` clean; `uv run lint-imports` clean; `uv run ruff check app tests` clean
- No file outside `apps/email-listener/**` or `.planning/**` modified; no migration created
</verification>

<success_criteria>
UsageDelta accumulates across rounds (summed test green); both terminal tool-call parse-failure paths
surface a visible text part instead of a silent drop; existing single-round chat behavior unregressed.
</success_criteria>

<output>
Create `.planning/phases/34-tool-loop-mechanics-stub-echo-executor/34-02-SUMMARY.md` when done.
</output>
