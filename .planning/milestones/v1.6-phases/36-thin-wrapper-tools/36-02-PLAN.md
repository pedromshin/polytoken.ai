---
phase: 36-thin-wrapper-tools
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - apps/email-listener/app/infrastructure/tools/search_emails_executor.py
  - apps/email-listener/tests/infrastructure/tools/test_search_emails_executor.py
  - apps/email-listener/app/application/use_cases/run_chat_turn.py
  - apps/email-listener/app/container.py
  - apps/email-listener/tests/test_container.py
  - apps/email-listener/tests/application/test_run_chat_turn_real_tools_wiring.py
autonomous: true
requirements: [TOOL-02]

must_haves:
  truths:
    - "Asking the chat agent to find related emails returns grounded results via the existing find_similar_confirmed() RRF, with zero new repository methods"
    - "search_emails results NEVER contain a raw email body or raw confirmed-region text field — only subject/sender/received_at metadata plus the confirmed structured extracted_fields"
    - "The model is advertised real per-tool JSON schemas (name_or_id / query as declared string properties) for lookup_entity and search_emails — not the Phase-34 placeholder empty-object schema"
    - "container.py wires both real executors into RunChatTurn.tool_executors — the production mapping is no longer empty"
    - "Every search_emails result carries a server-built citations[] entry of {kind:'email', id, route:'/emails/{id}'}"
  artifacts:
    - path: "apps/email-listener/app/infrastructure/tools/search_emails_executor.py"
      provides: "SEARCH_EMAILS_TOOL_NAME, build_search_emails_tool(), SearchEmailsExecutor, EmailSearchResult"
      exports: ["SEARCH_EMAILS_TOOL_NAME", "build_search_emails_tool", "SearchEmailsExecutor", "EmailSearchResult"]
    - path: "apps/email-listener/app/container.py"
      provides: "_provide_run_chat_turn wires tool_executors={lookup_entity, search_emails} + server_tool_defs"
  key_links:
    - from: "app/container.py:_provide_run_chat_turn"
      to: "RunChatTurn(tool_executors={...}, server_tool_defs={...})"
      via: "LookupEntityExecutor + SearchEmailsExecutor instances, build_lookup_entity_tool()/build_search_emails_tool() dicts"
      pattern: "LOOKUP_ENTITY_TOOL_NAME:|SEARCH_EMAILS_TOOL_NAME:"
    - from: "app/application/use_cases/run_chat_turn.py:_build_tool_offer"
      to: "self._server_tool_defs"
      via: "real per-tool input_schema replaces the old {\"type\":\"object\",\"additionalProperties\":False} stub"
      pattern: "server_tool_defs"
    - from: "app/infrastructure/tools/search_emails_executor.py"
      to: "app/domain/ports/retrieval_port.py:RetrievalPort.find_similar_confirmed"
      via: "direct call, zero new repository methods"
      pattern: "find_similar_confirmed\\("
---

<objective>
Ship the SECOND real `ToolExecutor` — `search_emails`, a thin wrapper over the existing
`RetrievalPort.find_similar_confirmed()` (TOOL-02) — enforcing the Tier-2 "never raw body" rule from
day one, then wire BOTH real tools (`lookup_entity` from 36-01 + `search_emails` from this plan) into
`container.py`, closing the tool-schema-advertisement gap 34-03 deliberately left as a stub.

Purpose: TOOL-02's own requirement text is the Tier-2 quarantine rule itself ("returns the quarantine
adapter's output, never raw email body") — this must be correct from the first line of code, not
deferred to Phase 38 (which only ADDS adversarial proof + user-facing exposure for `search_knowledge`,
per synthesis P6 — it is not what makes TOOL-01/02 safe). Container wiring is deferred to this plan
(not split across both) because it is one coherent edit to one factory function that both tools share.

Output: `SearchEmailsExecutor`, both tools live in `container.py`'s production `tool_executors`
mapping, and `_build_tool_offer` advertises real argument schemas — the phase's chat surface is now
end-to-end functional (pending Phase 39's UI for tool-round visibility, out of scope here).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-thin-wrapper-tools/36-CONTEXT.md
@.planning/phases/36-thin-wrapper-tools/36-01-SUMMARY.md
@.planning/phases/34-tool-loop-mechanics-stub-echo-executor/34-03-SUMMARY.md

<concurrency_warning>
Phase 35 modifies `app/container.py` and `app/application/use_cases/run_chat_turn.py` immediately
before this plan executes. Both files will be in whatever final shape Phase 35 left them by
execution time — re-read each fresh via grep-by-symbol-name, never trust a quoted line number.
`_provide_run_chat_turn` may have gained new parameters (e.g. cost-related collaborators) — this
plan only ADDS parameters/keyword-arguments, never removes or reorders Phase 35's additions.
</concurrency_warning>

<interfaces>
<!-- From 36-01 (already landed by the time this plan executes) -->

From app/infrastructure/tools/envelope.py:
  ToolCitation(kind: str, id: str, route: str)
  build_citation(kind: Literal["entity","email"], entity_or_email_id: str) -> ToolCitation
  citation_to_dict(citation: ToolCitation) -> dict[str, str]
  truncate_field(text: str, limit: int = MAX_RESULT_FIELD_CHARS) -> str
  MAX_RESULT_FIELD_CHARS = 300

From app/infrastructure/tools/lookup_entity_executor.py:
  LOOKUP_ENTITY_TOOL_NAME = "lookup_entity"
  build_lookup_entity_tool() -> dict[str, Any]
  LookupEntityExecutor(*, entity_instances, resolution_repo, entity_types, embedder)

From app/domain/ports/tool_executor.py (extended by 36-01):
  ToolExecutor.execute(self, *, name: str, arguments: dict[str, Any], importer_id: str) -> ToolExecutionResult

<!-- Existing ports THIS plan's executor calls directly. Zero new repository methods. -->

From app/domain/ports/retrieval_port.py (unchanged):
  RetrievedExample(component_id: str, content_text: str, extracted_fields: dict[str, object], score: float)
  RetrievalPort.find_similar_confirmed(*, component_embedding: tuple[float, ...], entity_type_id: str,
    importer_id: str, key_terms: tuple[str, ...], top_n: int = 3) -> list[RetrievedExample]  # async

From app/domain/entities/component.py (unchanged):
  Component(id, email_id, importer_id, attachment_id, parent_component_id, source_type, location,
    content_text, content_markdown, content_raw, embedding, sequence_index, extraction_status,
    role, entity_type_id, entity_type_field_id)

From app/domain/ports/component_repository.py (unchanged):
  ComponentRepository.find_by_id(component_id: str) -> Component | None

From app/domain/entities/email.py (unchanged — NOTE body_html/body_text/raw_storage_key exist on this
entity and MUST NEVER be read or surfaced by this plan's executor):
  Email(id, importer_id, message_id, in_reply_to, references_ids, received_at, sender_address,
    sender_name, to_addresses, cc_addresses, subject, body_html, body_text, raw_storage_key,
    parse_status, parse_error, parsed_at, created_at)

From app/domain/ports/email_repository.py (unchanged):
  EmailRepository.find_by_id(email_id: str) -> Email | None

From app/domain/services/key_terms.py (unchanged, pure):
  extract_key_terms(text: str) -> tuple[str, ...]

From app/infrastructure/llm/chat_tools.py (unchanged — the established tool-def builder pattern this
plan's build_search_emails_tool() and build_lookup_entity_tool() both follow):
  build_emit_ui_spec_tool() -> dict[str, Any]  # {"name", "description", "input_schema"}

From app/container.py's existing DI-bound ports (all already `provider.provide(...)`-registered
elsewhere in the file — dishka auto-injects any of these as a factory-function parameter by type):
  Client (from supabase), EntityInstanceRepository, EntityTypeRepository, EmbeddingProtocol,
  RetrievalPort, ComponentRepository, EmailRepository
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SearchEmailsExecutor — thin wrapper over find_similar_confirmed() (TOOL-02)</name>
  <files>
    apps/email-listener/app/infrastructure/tools/search_emails_executor.py,
    apps/email-listener/tests/infrastructure/tools/test_search_emails_executor.py
  </files>
  <read_first>
    apps/email-listener/app/application/use_cases/autofill_fields.py — specifically
    `_retrieve_examples` (embeds text, calls `find_similar_confirmed`, cold-start-safe try/except
    returning `()` on failure) and `_example_to_dict` — the existing precedent for embedding a
    free-text string and calling this exact port; follow the same embed-then-retrieve shape.
    apps/email-listener/app/infrastructure/supabase/retrieval_repository.py — confirms
    `entity_type_id` is a REQUIRED filter at the RPC level (both `match_components_by_embedding` and
    `match_components_by_trgm` take `match_entity_type_id`), which is why this executor must loop over
    every active entity type for the importer exactly as 36-01's LookupEntityExecutor loops for its
    name-search fallback — there is no single "search across all entity types" RPC (zero new backend).
  </read_first>
  <behavior>
    SearchEmailsExecutor.execute(name="search_emails", arguments={"query": ...}, importer_id=...):
    - Test 1 (happy path, merges across entity types, dedupes by resulting email): `query` is
      non-empty → embeds it via `embedder.embed`, extracts `key_terms` via `extract_key_terms(query)`,
      calls `entity_types.list_active(importer_id)`, then calls `retrieval.find_similar_confirmed`
      ONCE PER active entity type (same `component_embedding`/`key_terms`, `top_n=5`), merges every
      returned `RetrievedExample` list, resolves each surviving example's `component_id` ->
      `components.find_by_id` -> `.email_id` -> `emails.find_by_id`, DEDUPES the final result list by
      `email_id` (keep the highest-scoring example per email, since two confirmed components can
      belong to the same email), sorts by score descending, and caps at the top 5 EMAILS.
    - Test 2 (tenant defense-in-depth): a resolved `Component` or `Email` whose `.importer_id !=
      importer_id` is SKIPPED entirely (never included in results) even though
      `find_similar_confirmed`'s own RPC is already importer-scoped — this is belt-and-suspenders
      against a future RPC regression, not the primary control.
    - Test 3 (never raw body — the Tier-2 rule): every result's serialized JSON contains none of
      `content_text`, `body_html`, `body_text`, `raw_storage_key` as KEYS or as substrings of any
      VALUE (assert both `"content_text" not in content` and the raw fixture body string used in the
      test is not a substring of `content` — construct a `RetrievedExample`/`Email` fixture whose
      `content_text`/`body_text` contains a unique marker string and assert that marker is absent from
      the executor's returned `ToolExecutionResult.content`). Each result instead carries: `email_id`,
      `subject` (truncated via `truncate_field`), `sender_name`, `sender_address`, `received_at` (ISO
      string via `.isoformat()`), `extracted_fields` (the confirmed structured dict from the matching
      `RetrievedExample`, `str`-coerced values truncated via `truncate_field` where they are strings),
      and `score`.
    - Test 4 (empty query → is_error, no repo calls): `arguments.get("query")` missing/None/blank →
      `ToolExecutionResult(is_error=True, ...)` without calling any collaborator (assert not
      called/awaited).
    - Test 5 (no active entity types / no confirmed matches → empty, non-error envelope): `
      entity_types.list_active` returns `[]`, OR every `find_similar_confirmed` call returns `[]` →
      returns `is_error=False` with `results: []`, `citations: []` (cold-start safe, mirrors D-13's
      existing convention — never an error just because nothing matched).
    - Test 6 (collaborator exception → is_error, never raises): any awaited collaborator raises →
      caught, logged via structlog, returns a friendly `is_error=True` result with no exception
      internals leaked in `content`.
    - Test 7 (citations shape): result envelope's `citations` contains exactly one
      `{"kind": "email", "id": <email_id>, "route": "/emails/<email_id>"}` per distinct `email_id` in
      `results`, built via `build_citation("email", ...)`.
  </behavior>
  <action>
    Implement apps/email-listener/app/infrastructure/tools/search_emails_executor.py:
    - `SEARCH_EMAILS_TOOL_NAME = "search_emails"`.
    - `build_search_emails_tool() -> dict[str, Any]` — same Bedrock-valid shape convention as
      `build_lookup_entity_tool()`: `name`, a `description` explaining it finds emails related to a
      free-text query and returns grounded, cited, sanitized results (explicitly state in the
      description that raw email bodies are never returned — sets correct model expectations), and
      `input_schema` = `{"type": "object", "required": ["query"], "additionalProperties": False,
      "properties": {"query": {"type": "string", "maxLength": 200, "description": "..."}}}`.
    - A frozen dataclass `EmailSearchResult` with fields `email_id: str`, `subject: str | None`,
      `sender_name: str | None`, `sender_address: str`, `received_at: str`, `extracted_fields:
      dict[str, object]`, `score: float`. NOTE: this dataclass has NO `content_text`/`body_*` field —
      the type itself makes the Tier-2 rule structurally unreachable to violate by omission, not by a
      runtime check (mirrors the synthesis's "EXTRACTED-only enforced by field omission, not a
      boolean flag" principle applied to the Tier-2 case here).
    - `class SearchEmailsExecutor` with `__init__(self, *, retrieval: RetrievalPort, entity_types:
      EntityTypeRepository, components: ComponentRepository, emails: EmailRepository, embedder:
      EmbeddingProtocol) -> None`, and `async def execute(self, *, name: str, arguments: dict[str,
      Any], importer_id: str) -> ToolExecutionResult` implementing the 7 behaviors above. Structure as
      one try/except Exception around the whole body (mirrors `_retrieve_examples`'s cold-start-safe
      posture, but here ANY exception maps to `is_error=True` rather than falling back to `()`, since
      there is no cold-start concept for an on-demand chat tool call). Extract the per-entity-type
      loop + component/email resolution + dedupe-by-email_id into a private async helper
      (`_gather_candidates`) so `execute` itself stays a thin orchestration/envelope-building method,
      matching `LookupEntityExecutor`'s two-helper structure from 36-01. Build and return
      `ToolExecutionResult(tool_use_id="", content=cap_tool_output(json.dumps(envelope,
      separators=(",", ":"))), is_error=False)` on success, same envelope shape convention as
      LookupEntityExecutor (`{"results": [...], "citations": [...]}`).
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/infrastructure/tools/test_search_emails_executor.py -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - All 7 behaviors have a corresponding, independently-named passing test.
    - `grep -n "content_text\|body_html\|body_text\|raw_storage_key" apps/email-listener/app/infrastructure/tools/search_emails_executor.py`
      returns ONLY read-time source-attribute references needed to resolve routing (e.g. reading
      `component.email_id` is fine) — no occurrence of `content_text`, `body_html`, `body_text`, or
      `raw_storage_key` as an attribute READ or as an envelope field name anywhere in the file (grep
      for these 4 identifiers must return zero matches — the executor never even touches those
      attributes on the `Component`/`Email` objects it loads, since `EmailSearchResult` structurally
      excludes them).
    - Test 3's marker-string assertion is present and passes (`uv run pytest
      tests/infrastructure/tools/test_search_emails_executor.py -k tier2 -q --no-cov` or equivalent
      `-k` filter matching the never-raw-body test name).
    - `cd apps/email-listener && uv run mypy app/infrastructure/tools/search_emails_executor.py` reports no issues.
  </acceptance_criteria>
  <done>SearchEmailsExecutor + build_search_emails_tool() exist, implement all 7 behaviors, pass their full targeted test file, and structurally never surface content_text/body_html/body_text/raw_storage_key.</done>
</task>

<task type="auto">
  <name>Task 2: Wire both real executors into container.py + fix the tool-schema advertisement gap</name>
  <files>
    apps/email-listener/app/application/use_cases/run_chat_turn.py,
    apps/email-listener/app/container.py
  </files>
  <read_first>
    Re-read (fresh) app/application/use_cases/run_chat_turn.py: grep for `_build_tool_offer` (its def)
    and for the `RunChatTurn.__init__` signature (grep `def __init__` inside `class RunChatTurn`) to
    see Phase 35's current parameter list before adding to it.
    Re-read (fresh) app/container.py: grep for `_provide_run_chat_turn` (its def) and for the
    `RunChatTurn(` construction call inside it, and for the current `tool_executors=` keyword
    argument's value (still `{}` as of 34-03, but confirm) plus the comment sitting above it.
  </read_first>
  <action>
    1. In app/application/use_cases/run_chat_turn.py's `RunChatTurn.__init__`: add a new keyword-only
    constructor parameter `server_tool_defs: Mapping[str, dict[str, Any]] = MappingProxyType({})`
    positioned alongside the existing `tool_executors` parameter (append after it; keep every
    parameter Phase 35 added, in whatever order they exist). Store it as
    `self._server_tool_defs = server_tool_defs`.

    2. In `_build_tool_offer`: change the `server_tools = tuple(...)` comprehension that currently
    builds a generic `{"name": tool_name, "description": f"Server tool: {tool_name}", "input_schema":
    {"type": "object", "additionalProperties": False}}` dict per `tool_name` in
    `self._server_tool_names`. For each `tool_name`, look it up in `self._server_tool_defs` FIRST — if
    present, use that real dict verbatim; if ABSENT (e.g. a test-only executor registered without a
    matching def, like `EchoToolExecutor` in Phase 34/35's own tests), fall back to the existing
    generic stub dict exactly as today (this keeps every existing Phase 34/35 test that constructs
    `RunChatTurn` without passing `server_tool_defs` green — additive, non-breaking default). Do not
    change any other branch of `_build_tool_offer`.

    3. In app/container.py, add these imports near the existing
    `from app.infrastructure.llm.chat_tools import (...)` block:
       `from app.infrastructure.tools.lookup_entity_executor import (LOOKUP_ENTITY_TOOL_NAME,
       LookupEntityExecutor, build_lookup_entity_tool)`
       `from app.infrastructure.tools.search_emails_executor import (SEARCH_EMAILS_TOOL_NAME,
       SearchEmailsExecutor, build_search_emails_tool)`

    4. In app/container.py's `_provide_run_chat_turn` factory function: add these NEW parameters to
    its signature (dishka auto-injects each by type — every one is already `provider.provide(...)`-
    bound elsewhere in this file, confirmed by the <interfaces> block): `client: Client,
    entity_instances: EntityInstanceRepository, entity_types: EntityTypeRepository, embedder:
    EmbeddingProtocol, retrieval: RetrievalPort, components: ComponentRepository, email_repo:
    EmailRepository`. Keep every parameter already present (Phase 35's additions included).

    5. Inside the function body, before the `return RunChatTurn(...)` statement, add: `resolution_repo
    = SupabaseEntityResolutionRepository(client=client)` (mirrors `_provide_resolve_candidates_use_case`'s
    identical existing pattern — `SupabaseEntityResolutionRepository` is already imported at the top of
    this file), then `lookup_entity_executor = LookupEntityExecutor(entity_instances=entity_instances,
    resolution_repo=resolution_repo, entity_types=entity_types, embedder=embedder)` and
    `search_emails_executor = SearchEmailsExecutor(retrieval=retrieval, entity_types=entity_types,
    components=components, emails=email_repo, embedder=embedder)`.

    6. Change the `RunChatTurn(...)` call's `tool_executors={}` keyword argument to
    `tool_executors={LOOKUP_ENTITY_TOOL_NAME: lookup_entity_executor, SEARCH_EMAILS_TOOL_NAME:
    search_emails_executor}`, and add a new keyword argument `server_tool_defs=
    {LOOKUP_ENTITY_TOOL_NAME: build_lookup_entity_tool(), SEARCH_EMAILS_TOOL_NAME:
    build_search_emails_tool()}`. Update the comment currently explaining "EMPTY in production — no
    real server tool exists until Phase 36" to instead state these are the first two real production
    tools (Phase 36, TOOL-01/TOOL-02) and both are offered to every `max_tool_rounds > 0` model (the 2
    Bedrock Claude registry entries) — no further capability gating is added by this plan (the
    existing `max_tool_rounds` gate already covers it).
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/test_container.py tests/application/test_run_chat_turn.py -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LOOKUP_ENTITY_TOOL_NAME:\|SEARCH_EMAILS_TOOL_NAME:" apps/email-listener/app/container.py`
      matches inside both the `tool_executors=` and `server_tool_defs=` dict literals (4 total
      occurrences expected).
    - `grep -n "server_tool_defs" apps/email-listener/app/application/use_cases/run_chat_turn.py`
      matches in `__init__`'s signature, the `self._server_tool_defs =` assignment, and inside
      `_build_tool_offer`.
    - `cd apps/email-listener && uv run mypy app/container.py app/application/use_cases/run_chat_turn.py`
      reports no NEW issues introduced by this task's diff (container.py may show pre-existing
      unrelated errors per 34-03-SUMMARY.md's precedent — confirm via `git stash` before/after
      comparison if any errors appear, exactly as 34-03 documented doing).
    - `cd apps/email-listener && uv run lint-imports` stays "3 kept, 0 broken".
  </acceptance_criteria>
  <done>container.py's tool_executors mapping carries both real executors; RunChatTurn accepts server_tool_defs and _build_tool_offer uses it; targeted pytest + lint-imports pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wiring integration tests — real schemas advertised + DI resolves + one real round trip</name>
  <files>
    apps/email-listener/tests/application/test_run_chat_turn_real_tools_wiring.py
  </files>
  <read_first>
    apps/email-listener/tests/application/test_run_chat_turn_tool_loop_e2e.py — reuse/adapt its fake
    collaborators (`_FakeRouter`, `FakeCostLedgerRepository`, `FakeChatMessageRepository`,
    `FakeChatRunRepository`, `FakeChatConversationRepository`, a `_MultiRoundFakeChatProvider`-style
    provider double, and `_make_use_case`'s construction pattern) rather than re-inventing them — this
    new test file is self-contained (this repo's convention: each test file duplicates its own small
    fakes rather than sharing a fixtures module) but should copy the EXISTING working shapes, not
    redesign them.
    apps/email-listener/tests/test_container.py — the existing container-resolution test pattern (how
    a live `AsyncContainer`/test client fixture is built) for the DI-resolution assertion.
  </read_first>
  <action>
    Create apps/email-listener/tests/application/test_run_chat_turn_real_tools_wiring.py with 3
    test functions:
    1. `test_build_tool_offer_advertises_real_lookup_entity_and_search_emails_schemas` — construct a
    `RunChatTurn` directly (fakes for every required collaborator, mirrors `_make_use_case`'s pattern
    from test_run_chat_turn_tool_loop_e2e.py) passing `tool_executors={LOOKUP_ENTITY_TOOL_NAME:
    <any ToolExecutor double>, SEARCH_EMAILS_TOOL_NAME: <any ToolExecutor double>}` AND
    `server_tool_defs={LOOKUP_ENTITY_TOOL_NAME: build_lookup_entity_tool(),
    SEARCH_EMAILS_TOOL_NAME: build_search_emails_tool()}`. Call `_build_tool_offer` (it is a
    private method — call it directly via `use_case._build_tool_offer(model)`, matching the
    project's existing `SLF001`-suppressed test convention for exercising private methods, confirmed
    by grepping this repo's `# noqa: SLF001` usage elsewhere if present, else add the same suppression
    comment) with a genui-capable, `max_tool_rounds=4` model (e.g. the real
    `us.anthropic.claude-sonnet-4-6` registry entry via `get_model(...)`). Assert the returned tools
    tuple contains a dict named `lookup_entity` whose `input_schema["properties"]` has a
    `name_or_id` key, and a dict named `search_emails` whose `input_schema["properties"]` has a
    `query` key — i.e. NEITHER tool's `input_schema` is the old empty-properties stub
    (`{"type": "object", "additionalProperties": False}` with no `properties` key at all).
    2. `test_container_wires_both_real_tool_executors` — resolve `RunChatTurn` from the real dishka
    container (mirrors test_container.py's existing resolution pattern) and assert
    `run_chat_turn._tool_executors` (private attribute access, same SLF001 convention) has EXACTLY
    the 2 keys `{"lookup_entity", "search_emails"}` (set equality, order-independent) and that the
    value for each key is an instance of `LookupEntityExecutor`/`SearchEmailsExecutor` respectively
    (isinstance checks).
    3. `test_lookup_entity_round_trip_produces_grounded_citations` — a single real-tool round-trip
    proof for lookup_entity (search_emails already has thorough direct-unit coverage in Task 1 of
    this plan; duplicating a full round-loop harness for both tools is out of budget for this task).
    Construct `RunChatTurn` with a REAL `LookupEntityExecutor` wired against `AsyncMock`-based fakes
    for `EntityInstanceRepository`/`EntityTypeRepository`/`EmbeddingProtocol` and a manual (non-Async)
    fake for `EntityResolutionRepository.find_candidates` returning one canned `EntityCandidate`, plus
    a fake single-round `ChatProvider` (mirrors this repo's existing fake-provider pattern) whose one
    streamed delta calls the `lookup_entity` tool with `{"name_or_id": "Acme Corp"}`. Drive one full
    `run_chat_turn.run(...)` call and assert: the persisted `tool_invocation_result` part's `content`
    parses as JSON, contains a `citations` entry with `route == "/entities/<the fixture's
    entity_instance_id>"`, and does NOT contain the substring `content_text` anywhere in the raw
    content string.
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/application/test_run_chat_turn_real_tools_wiring.py -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - All 3 test functions pass.
    - Test 1 explicitly asserts `"properties" in tool["input_schema"]` for both tools (this is the
      exact regression guard for the gap 36-CONTEXT.md flagged: "planner confirms the advertisement
      path... and closes any gap if 34 left tool-def advertisement stub-only").
    - Test 3's JSON-content substring assertion (`"content_text" not in content`) passes, proving the
      Tier-1/Tier-2 quarantine obligation holds through a REAL round trip, not just in isolated unit
      tests.
  </acceptance_criteria>
  <done>3 wiring/integration tests pass, permanently regression-guarding real schema advertisement, container DI resolution, and one real lookup_entity round trip with correct citations and no raw-body leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM (Bedrock Claude) -> tool arguments | `arguments["query"]` is model-authored, untrusted free text |
| ToolExecutor -> Supabase RPC | `find_similar_confirmed`/`find_by_id` queries, must stay importer-scoped |
| ToolExecutor result -> LLM context | The JSON envelope re-enters the model's context as a `tool_result` block |
| container.py composition root -> RunChatTurn | Wiring must not silently regress to the empty-mapping stub on a future edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Information Disclosure | SearchEmailsExecutor surfacing raw email body/region text | mitigate | `EmailSearchResult` dataclass has no `content_text`/`body_*` field — structurally unreachable by field omission (Task 1, Test 3 proves it end-to-end via a marker-string absence assertion) |
| T-36-06 | Information Disclosure | Cross-tenant email/component read via a future RPC regression | mitigate | Task 1's `_gather_candidates` re-checks `component.importer_id`/`email.importer_id` against the caller's `importer_id` even though `find_similar_confirmed`'s RPC is already importer-scoped (defense-in-depth, T-04-28 precedent) |
| T-36-07 | Tampering | A future container.py edit accidentally reverts `tool_executors` to `{}` or drops `server_tool_defs`, silently disabling both tools or reverting to the empty-schema stub | mitigate | Task 3's `test_container_wires_both_real_tool_executors` and `test_build_tool_offer_advertises_real_lookup_entity_and_search_emails_schemas` are permanent regression guards in the test suite, not one-off manual checks |
| T-36-08 | Elevation of Privilege | Both tools are offered to every `max_tool_rounds > 0` model (both Bedrock Claude entries) with no additional per-tool capability gate | accept | Both tools are read-only, Tier-1/Tier-2-safe by construction (this phase's whole point) — no write capability exists yet to escalate into; narrower per-model tool gating is not required until a write-capable or higher-risk tool exists (mirrors Fork 3's "escalate only when a write-capable tool triggers the need" staging principle) |

</threat_model>

<verification>
Run all 3 task-level pytest commands. Then the full targeted sweep:
`cd apps/email-listener && uv run pytest tests/infrastructure/tools/ tests/application/test_run_chat_turn_real_tools_wiring.py tests/application/test_run_chat_turn_tool_loop_e2e.py tests/application/test_run_chat_turn.py tests/test_container.py -q --no-cov`
`cd apps/email-listener && uv run mypy app/infrastructure/tools app/container.py app/application/use_cases/run_chat_turn.py`
`cd apps/email-listener && uv run lint-imports` (must stay "3 kept, 0 broken")
`grep -rn "CREATE \|ALTER \|\.rpc(\"match_components\|\.rpc(\"" apps/email-listener/app/infrastructure/tools/` — confirm the only `.rpc(` usage anywhere in this phase's new files is transitively via the EXISTING `find_candidates`/`find_similar_confirmed` calls (i.e. zero NEW rpc names, zero raw SQL) — the HARD constraint check.
</verification>

<success_criteria>
- `container.py`'s `tool_executors` mapping is no longer empty in production — both `lookup_entity`
  and `search_emails` are real, wired `ToolExecutor` instances.
- `_build_tool_offer` advertises real per-tool `input_schema.properties` for both tools — the
  Phase-34 placeholder stub is gone for these two tool names (still used as a fallback for any
  future executor registered without a matching `server_tool_defs` entry).
- `search_emails` never returns `content_text`/`body_html`/`body_text`/`raw_storage_key` — proven by
  both a unit-level grep-absence check and an integration-level substring-absence assertion.
- Both tools' results carry `citations[]` resolving to real `/entities/[id]`/`/emails/[id]` routes.
- Zero new SQL, migrations, or RPCs.
</success_criteria>

<output>
Create `.planning/phases/36-thin-wrapper-tools/36-02-SUMMARY.md` when done
</output>
