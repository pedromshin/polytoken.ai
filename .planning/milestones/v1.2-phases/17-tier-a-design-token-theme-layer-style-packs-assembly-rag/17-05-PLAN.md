---
phase: 17-tier-a-design-token-theme-layer-style-packs-assembly-rag
plan: 05
type: execute
wave: 3
depends_on: ["17-01", "17-04"]
files_modified:
  - apps/email-listener/scripts/genui_eval/rubric.py
  - apps/email-listener/scripts/genui_eval/style_metrics.py
  - apps/email-listener/scripts/genui_eval/judge_adapter.py
  - apps/email-listener/scripts/genui_eval/run_eval.py
  - apps/email-listener/scripts/genui_eval/report.py
  - apps/email-listener/scripts/genui_eval/compare_reports.py
  - apps/email-listener/tests/test_genui_eval_style.py
autonomous: false
requirements: [STYLE-03, STYLE-04, RAG-02]
must_haves:
  truths:
    - "A deterministic WCAG-AA contrast check on resolved token pairs is part of the a11y criterion; token-driven styling that fails contrast is a regression (a11y HARD no-regression, D-09)"
    - "Style-distinctiveness is measured deterministically (same intent under >=2 packs → divergence in emitted token aliases/structure) and reported ADDITIVELY — NOT folded into the 4-criterion weighted aggregate (D-15/D-16)"
    - "An LLM-judge 'custom-not-generic/on-brand' sub-criterion is reported alongside on-intent (escalation model, temp 0, single structured call, D-17)"
    - "The runner has a --style-pack/--all-packs mode that runs the golden set under each pack and emits per-pack + distinctiveness aggregates, reusing the Phase-16 modules (no fork, D-19)"
    - "An eval assertion verifies the emitted spec references a meaningful fraction of the retrieved components (RAG-02)"
  artifacts:
    - path: "apps/email-listener/scripts/genui_eval/style_metrics.py"
      provides: "Pure deterministic contrast check + pairwise distinctiveness score + retrieval-overlap assertion (no Bedrock)"
    - path: "apps/email-listener/scripts/genui_eval/run_eval.py"
      provides: "--style-pack/--all-packs mode emitting per-pack + distinctiveness aggregates (extends, not forks)"
  key_links:
    - from: "apps/email-listener/scripts/genui_eval/rubric.py"
      to: "apps/email-listener/scripts/genui_eval/style_metrics.py"
      via: "a11y criterion calls the deterministic contrast check (D-09)"
      pattern: "contrast|wcag|aa_contrast"
    - from: "apps/email-listener/scripts/genui_eval/run_eval.py"
      to: "apps/email-listener/scripts/genui_eval/style_metrics.py"
      via: "--all-packs computes pairwise distinctiveness across per-pack runs"
      pattern: "all_packs|distinctiveness|style_pack"
---

<objective>
Extend the Phase-16 eval harness (no fork) to MEASURE the Tier-A win: a deterministic WCAG-AA contrast
check folded into the a11y criterion (D-09 hard no-regression), an additive deterministic
style-distinctiveness signal (same intent under >=2 packs → divergence), an LLM-judge
custom-not-generic/on-brand sub-criterion, a `--style-pack`/`--all-packs` runner mode emitting per-pack +
distinctiveness aggregates, and an eval assertion that the emitted spec references a meaningful fraction of
the retrieved components (RAG-02). The pass bar (STYLE-04) is gated via the existing `compare_reports.py`
lift/regression diff — not a brittle absolute threshold.

Purpose: STYLE-04 (a measured before/after lift with no a11y regression) + STYLE-03 (contrast on token-driven
styling) + RAG-02 (retrieval-influence assertion). This is the gate the whole v1.2 milestone hangs on; it
MUST preserve baseline comparability (D-15 — the existing four numbers stay diff-able).

Output: an extended rubric (contrast), a new pure `style_metrics.py` (distinctiveness + overlap), an extended
judge, an `--all-packs` runner, and per-pack/distinctiveness reporting + comparison.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-tier-a-design-token-theme-layer-style-packs-assembly-rag/17-CONTEXT.md
@.planning/phases/17-tier-a-design-token-theme-layer-style-packs-assembly-rag/17-01-SUMMARY.md
@.planning/phases/17-tier-a-design-token-theme-layer-style-packs-assembly-rag/17-04-SUMMARY.md
@.planning/phases/16-studio-foundation-eval-harness-history-page-ideas-tabs/16-CONTEXT.md

<interfaces>
<!-- Phase-16 eval modules to extend (no fork). Use directly. -->

From scripts/genui_eval/rubric.py (the pure deterministic rubric — D-11, no Bedrock/Supabase/boto3):
  WEIGHTS = { "valid-spec":0.30, "composed":0.30, "on-intent":0.25, "a11y":0.15 }  # DO NOT change these (D-15 comparability)
  def valid_spec(spec, outcome) -> CriterionResult
  def composed_not_placeholder(spec) -> CriterionResult
  def a11y(spec) -> CriterionResult   # extend: also fail if any resolved token pair fails AA contrast (D-09)
  def aggregate(sub_scores) -> float  # renormalizes when on-intent absent — keep as is
  @dataclass(frozen=True) class CriterionResult(name, score, passed)

From scripts/genui_eval/run_eval.py:
  async def _eval_prompt(*, entry, use_case, judge, semaphore, registry_version) -> PromptReport
  - calls use_case.execute(intent=prompt, raw_content="", registry_version=...) — now accepts style_pack_id (Plan 17-04).
  - sub_scores = [valid_spec, composed, a11y] (+ on-intent if judge). aggregate() over those.
  - CLI: --out --limit --no-judge --label. Add --style-pack <id> and --all-packs.

From scripts/genui_eval/judge_adapter.py:
  class JudgeAdapter: async def score(*, intent, spec) -> JudgeResult(score, rationale, ...)  # single forced-tool-use, temp 0, escalation model.
  - Add a custom-not-generic/on-brand judge call (same discipline) — a second structured score, reported alongside on-intent.

From scripts/genui_eval/report.py:
  @dataclass(frozen=True) PromptReport(..., on_intent_score, a11y_score, ...)
  @dataclass(frozen=True) EvalReport(..., mean_*, prompt_reports: tuple[...])  # prompt_reports MUST stay a tuple (WR-05)
  build_report(...) / write_report(...) — extend additively with style fields.

From scripts/genui_eval/compare_reports.py:
  def compare(baseline, candidate) -> str  # the lift/regression gate (delta < -0.05 = regression). This is the STYLE-04 gate (D-18).

From app/application/use_cases/generate_ui_spec.py (Plan 17-04):
  GenerateUiSpecResult now carries style_pack_id + retrieved_ids + retrieved_overlap_count; execute(style_pack_id=...) supported.

NOTE: the recorded Phase-16 baseline run was DEFERRED to a connected env (reports/ has only .gitkeep). The
deterministic metrics here are fully offline-unit-testable; the actual lift-vs-baseline comparison + --all-packs
live run require Bedrock and are the connected-env step (see <verification>).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic contrast check + style-distinctiveness + retrieval-overlap metrics</name>
  <read_first>
    - apps/email-listener/scripts/genui_eval/rubric.py (the a11y criterion to extend + the purity guarantee — no Bedrock/network)
    - packages/genui/src/theme/packs.ts (Plan 17-01 — resolved token values; contrast is computed on resolved pairs)
    - apps/email-listener/app/infrastructure/llm/genui_style_packs.py (Plan 17-04 — the Python pack token values to resolve)
    - apps/email-listener/scripts/genui_eval/report.py (PromptReport/EvalReport — the additive style fields land here)
    - .planning/phases/17-tier-a-design-token-theme-layer-style-packs-assembly-rag/17-CONTEXT.md (D-09 contrast, D-15 additive not folded, D-16 deterministic distinctiveness, D-14 RAG-02 assertion)
  </read_first>
  <files>apps/email-listener/scripts/genui_eval/style_metrics.py, apps/email-listener/scripts/genui_eval/rubric.py, apps/email-listener/tests/test_genui_eval_style.py</files>
  <behavior>
    - Test: wcag_contrast_ratio(fg_hsl, bg_hsl) computes the standard WCAG relative-luminance ratio; a known AA-passing pair (>=4.5:1 for normal text) passes and a known failing pair (e.g. light-gray on white) fails (D-09).
    - Test: a11y(spec) now FAILS (score < 1.0 / passed False) when a node's resolved token style pair (text vs surface) fails AA, and passes when all pairs meet AA — additive to the existing required-props check, hardening a11y (D-09).
    - Test: distinctiveness_score(spec_a, spec_b) returns a value in [0,1] that is HIGHER when two specs (same intent, different packs) diverge in emitted token aliases / structure, and ~0 when identical (D-16 deterministic pairwise).
    - Test: retrieval_overlap_ratio(spec, retrieved_ids) returns the fraction of retrieved ids referenced by the emitted spec; an assertion helper flags "inert retrieval" when overlap is below a documented floor (RAG-02, D-14).
    - Test: style_metrics.py imports NO anthropic/boto3/supabase/network (purity guard, like rubric.py).
  </behavior>
  <action>
    Create `scripts/genui_eval/style_metrics.py` (pure, no Bedrock/network — same purity discipline as rubric.py)
    exporting: `wcag_contrast_ratio(fg, bg) -> float` (parse HSL channel triplets → relative luminance → WCAG
    ratio); `passes_aa(fg, bg, large=False) -> bool` (>=4.5 normal / >=3.0 large); `resolve_node_contrast_pairs(spec,
    pack_token_values) -> list[tuple[fg,bg]]` (resolve each themed node's text-vs-surface pair from the active
    pack); `distinctiveness_score(spec_a, spec_b) -> float` (pairwise divergence over emitted token aliases +
    node-type/structure multiset — D-16, formula is Claude's discretion, document it); and
    `retrieval_overlap_ratio(spec, retrieved_ids) -> float` + `assert_retrieval_influence(...)` (RAG-02 floor —
    threshold is Claude's discretion, document it). In `rubric.py`, extend `a11y(spec)` to ALSO fail when any
    resolved token contrast pair fails AA (D-09) — pass the active pack's token values in (add a parameter;
    default to the baseline pack so existing single-pack calls still work). DO NOT change WEIGHTS or the
    four-criterion aggregate (D-15 comparability — distinctiveness is reported separately, not weighted in).
    Named exports; frozen/immutable; type every signature; no eval.
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/test_genui_eval_style.py -x && uv run ruff check scripts/genui_eval/style_metrics.py scripts/genui_eval/rubric.py && uv run mypy scripts/genui_eval/style_metrics.py</automated>
  </verify>
  <acceptance_criteria>
    - behavior: wcag_contrast_ratio + passes_aa correct on known AA pass/fail pairs (tests green — D-09).
    - behavior: a11y(spec) fails on a contrast-failing themed node, passes when all pairs meet AA; WEIGHTS unchanged (grep WEIGHTS still 0.30/0.30/0.25/0.15 — D-15).
    - behavior: distinctiveness_score high for divergent specs, ~0 for identical; retrieval_overlap_ratio + assertion work (tests green — D-16/RAG-02).
    - source: `grep -cE 'anthropic|boto3|supabase' scripts/genui_eval/style_metrics.py` == 0 (purity guard).
    - test-command: `cd apps/email-listener && uv run pytest tests/test_genui_eval_style.py` exits 0; ruff + mypy clean.
  </acceptance_criteria>
  <done>Pure deterministic contrast (folded into a11y, D-09 hard), pairwise distinctiveness (D-16), and retrieval-overlap (RAG-02) metrics exist; WEIGHTS/aggregate untouched (baseline comparability preserved); tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Custom-not-generic/on-brand LLM judge sub-criterion</name>
  <read_first>
    - apps/email-listener/scripts/genui_eval/judge_adapter.py (JudgeAdapter pattern — single forced-tool-use, temp 0, escalation model, never-raises)
    - apps/email-listener/app/settings.py (genui_escalation_model_id — the judge model, D-17)
    - .planning/phases/17-tier-a-design-token-theme-layer-style-packs-assembly-rag/17-CONTEXT.md (D-17 custom-not-generic, same adapter discipline as the Phase-16 judge)
  </read_first>
  <files>apps/email-listener/scripts/genui_eval/judge_adapter.py, apps/email-listener/tests/test_genui_eval_style.py</files>
  <behavior>
    - Test (mocked Bedrock client): score_brand(intent, spec, style_pack_id) returns a JudgeResult-like score in [0,1] from a single forced-tool-use call at temperature=0 against the escalation model (D-17).
    - Test: on any error/timeout it returns score=None (never raises — mirrors the existing judge), so the aggregate/report degrade gracefully.
    - Test: the brand-judge system prompt contains only static trusted content (no user prose interpolated into system) — same security posture as the existing judge.
  </behavior>
  <action>
    Extend `judge_adapter.py` with a brand/custom-not-generic scorer: add a `score_brand(*, intent, spec,
    style_pack_id) -> JudgeResult` method (or a sibling adapter class reusing the same client/timeout) that makes
    a SINGLE forced-tool-use Bedrock call at temperature=0 against `genui_escalation_model_id`, with a static
    system prompt asking "does this read as a custom, on-brand UI (for the stated pack personality) rather than
    default/generic shadcn?" → a 0..1 score + brief rationale. Mirror the existing JudgeAdapter discipline
    exactly: forced tool_choice, asyncio.timeout, clamp to [0,1], never raise (return score=None on failure),
    static trusted system prompt only (no user prose in system). This is reported ALONGSIDE on-intent (D-17), not
    folded into the four-criterion aggregate. Named exports; type every signature; no eval.
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/test_genui_eval_style.py -k "brand or judge" -x && uv run ruff check scripts/genui_eval/judge_adapter.py</automated>
  </verify>
  <acceptance_criteria>
    - behavior: score_brand returns a clamped [0,1] score from a single temp-0 forced-tool-use call on the escalation model (mocked-client test green — D-17).
    - behavior: error/timeout → score=None, never raises (test green).
    - source: brand-judge system prompt is static (no f-string interpolation of intent/spec into the `system` argument — grep confirms user content only in the user turn).
    - test-command: `cd apps/email-listener && uv run pytest tests/test_genui_eval_style.py -k "brand or judge"` exits 0; ruff clean.
  </acceptance_criteria>
  <done>Custom-not-generic/on-brand judge added with the same discipline as the Phase-16 judge; reported alongside on-intent (additive); never raises; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: --style-pack/--all-packs runner + per-pack + distinctiveness reporting</name>
  <read_first>
    - apps/email-listener/scripts/genui_eval/run_eval.py (the runner, _eval_prompt, run(), CLI args to extend)
    - apps/email-listener/scripts/genui_eval/report.py (PromptReport/EvalReport — add style fields; keep prompt_reports a tuple, WR-05)
    - apps/email-listener/scripts/genui_eval/compare_reports.py (the lift/regression gate — the STYLE-04 pass-bar mechanism, D-18)
    - apps/email-listener/scripts/genui_eval/style_metrics.py (Task 1 — distinctiveness across per-pack runs)
    - .planning/phases/17-tier-a-design-token-theme-layer-style-packs-assembly-rag/17-CONTEXT.md (D-18 pass bar via compare diff, D-19 --all-packs reuses runner)
  </read_first>
  <files>apps/email-listener/scripts/genui_eval/run_eval.py, apps/email-listener/scripts/genui_eval/report.py, apps/email-listener/scripts/genui_eval/compare_reports.py, apps/email-listener/tests/test_genui_eval_style.py</files>
  <behavior>
    - Test: run() / _eval_prompt threads a style_pack_id into use_case.execute(...) and records it on the PromptReport (per-pack attribution).
    - Test: --all-packs runs the golden set (or a sampled subset) under each known pack and emits per-pack aggregates + a cross-pack distinctiveness aggregate (same intent across packs → distinctiveness_score) — reusing the Phase-16 runner/report modules, NOT a fork (D-19).
    - Test: the report adds additive style fields (a11y_contrast_passed, brand_score, distinctiveness, retrieval_overlap) WITHOUT changing the existing four mean_* fields or WEIGHTS (D-15 — baseline JSON stays diff-able by compare_reports.py).
    - Test: compare_reports.py still diffs the four core criteria unchanged AND additionally surfaces the new style signals + an a11y HARD-regression flag (a11y delta < 0 is a hard fail, D-18); the four-criterion regression gate (delta < -0.05) is unchanged.
    - Test (offline, no Bedrock): the per-pack/distinctiveness aggregation logic is unit-tested with synthetic per-pack PromptReports (no live run needed to prove the math).
  </behavior>
  <action>
    Add `--style-pack <id>` and `--all-packs` CLI args + plumb a `style_pack_id` through `run()` → `_eval_prompt`
    → `use_case.execute(..., style_pack_id=...)` (Plan 17-04). For `--all-packs`, loop the golden set (or a
    `--limit`-sampled subset) over each KNOWN_STYLE_PACK_ID, collect per-pack PromptReports, and compute a
    cross-pack distinctiveness aggregate via `distinctiveness_score` (pair the same prompt_id across packs).
    Extend `report.py` PromptReport/EvalReport with ADDITIVE style fields (a11y_contrast_passed, brand_score,
    distinctiveness, retrieval_overlap) — keep `prompt_reports` a tuple (WR-05) and DO NOT alter the existing
    four mean_* fields, WEIGHTS, or the JSON keys compare_reports reads (D-15 comparability is the hard
    constraint). Extend `compare_reports.py` to additionally report the style signals + an a11y HARD-regression
    flag (any negative a11y delta is a blocking fail per D-18), while leaving the existing four-criterion
    regression table + threshold untouched — this IS the STYLE-04 pass bar (no brittle absolute threshold). Keep
    the runner standalone (not imported by app code), no eval, concurrency cap preserved. Type every signature.
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/test_genui_eval_style.py -k "runner or all_packs or report or compare" -x && uv run ruff check scripts/genui_eval/run_eval.py scripts/genui_eval/report.py scripts/genui_eval/compare_reports.py && uv run python -c "import scripts.genui_eval.run_eval"</automated>
  </verify>
  <acceptance_criteria>
    - behavior: --all-packs aggregation + cross-pack distinctiveness unit-tested offline with synthetic per-pack reports (test green — D-19).
    - behavior: report adds additive style fields; the four mean_* fields + WEIGHTS + JSON keys compare reads are unchanged (D-15 — grep WEIGHTS unchanged; baseline JSON shape preserved).
    - behavior: compare_reports surfaces an a11y HARD-regression flag + the style signals; the four-criterion delta<-0.05 gate is unchanged (D-18 test green).
    - source: run_eval.py exposes `--style-pack` and `--all-packs` (grep both flags present).
    - test-command: `cd apps/email-listener && uv run pytest tests/test_genui_eval_style.py -k "runner or all_packs or report or compare"` exits 0; module imports clean; ruff clean.
  </acceptance_criteria>
  <done>Runner gains --style-pack/--all-packs (per-pack + distinctiveness aggregates, reusing Phase-16 modules); report + compare extended additively; baseline comparability + a11y HARD no-regression preserved (STYLE-04 pass bar wired); offline tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Connected-env live --all-packs eval + STYLE-04 gate</name>
  <action>
    Connected-environment verification checkpoint — no new code is written here. All eval math is
    offline-unit-tested; a human runs the live Bedrock --all-packs eval + compare_reports diff to produce the
    actual STYLE-04 measured before/after. Follow the how-to-verify steps and capture the comparison report.
  </action>
  <what-built>
    The full eval extension (contrast in a11y, distinctiveness, brand judge, --all-packs runner, per-pack +
    distinctiveness reporting, lift/regression gate). All math is offline-unit-tested. This checkpoint runs the
    LIVE eval against Bedrock to produce the actual STYLE-04 measured before/after — which requires a connected
    environment (Bedrock IAM) and a recorded baseline, so it cannot run offline.
  </what-built>
  <how-to-verify>
    1. Ensure the email-listener can reach Bedrock (IAM role / dev creds) and the local Supabase is up (SUPABASE_URL
       host note in STATE.md).
    2. If no Phase-16 baseline JSON exists in scripts/genui_eval/reports/ (currently only .gitkeep — the Phase-16
       baseline run was deferred), first record a BASELINE on the nauta-teal pack:
       `cd apps/email-listener && uv run python -m scripts.genui_eval.run_eval --style-pack nauta-teal --label baseline-17`
    3. Run the candidate across all packs:
       `uv run python -m scripts.genui_eval.run_eval --all-packs --label tier-a-17`
    4. Diff candidate vs baseline through the gate:
       `uv run python -m scripts.genui_eval.compare_reports reports/<baseline-17>.json reports/<tier-a-17 or per-pack>.json`
    5. Confirm the STYLE-04 pass bar (D-18): (a) NO regression on the four criteria — a11y is a HARD no-regression
       INCLUDING the new contrast check; (b) a measurable lift on composed-not-placeholder and/or on-intent; (c) a
       POSITIVE style-distinctiveness signal across packs; (d) the RAG-02 retrieval-overlap is above the documented
       floor (retrieval is not inert). Capture the comparison report into the phase directory as evidence.
  </how-to-verify>
  <resume-signal>Type "approved" with the measured deltas (or paste the compare_reports output) once the STYLE-04 bar is met, or describe the regression/shortfall to feed a gap-closure replan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated spec → eval rubric | Specs scored by the eval are model output; the rubric/style_metrics must parse them defensively (no eval), like the existing pure rubric. |
| eval judge → Bedrock | The brand judge sends intent+spec to Bedrock; static system prompt only, never interpolating user prose into the system role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-30 | Tampering | a11y contrast bypass | mitigate | Contrast is computed deterministically on RESOLVED token pairs from the pack values (not model claims); a contrast-failing themed node fails the a11y criterion (Task 1) — the D-09 hard no-regression gate cannot be talked around by the model. |
| T-17-31 | Information Disclosure | brand-judge prompt | mitigate | System prompt is static trusted content; intent+spec go only in the user turn (Task 2) — mirrors the existing judge; no secret/PII in the prompt. |
| T-17-32 | Tampering | baseline comparability | mitigate | WEIGHTS + the four mean_* fields + the JSON keys compare_reports reads are unchanged (Task 3); style signals are additive — a baseline diff cannot be silently skewed by the new metrics (D-15). |
| T-17-33 | Repudiation | RAG-02 inert retrieval | mitigate | retrieval_overlap_ratio + the assertion (Task 1) + the per-generation overlap log (Plan 17-04) give a regression-tracked, auditable proof that retrieval influenced the spec (D-14). |
</threat_model>

<verification>
- `cd apps/email-listener && uv run pytest tests/test_genui_eval_style.py` all green (offline-deterministic).
- `uv run ruff check` + `uv run mypy` clean on style_metrics.py + the extended modules.
- `grep -E 'WEIGHTS' scripts/genui_eval/rubric.py` still shows 0.30/0.30/0.25/0.15 (D-15 comparability).
- `grep -cE 'anthropic|boto3|supabase' scripts/genui_eval/style_metrics.py` == 0 (purity).
- `run_eval.py` exposes `--style-pack` + `--all-packs`.
- CONNECTED-ENV (human checkpoint): live --all-packs run + compare diff meets the STYLE-04 bar (no a11y regression incl. contrast, lift on composed/on-intent, positive distinctiveness, RAG-02 overlap above floor).
</verification>

<success_criteria>
- WCAG-AA contrast on resolved token pairs is part of a11y — token-driven styling that fails contrast is a regression (D-09 HARD).
- Style-distinctiveness measured deterministically + reported additively without breaking baseline comparability (D-15/D-16).
- Custom-not-generic/on-brand judge reported alongside on-intent (D-17).
- --all-packs runner emits per-pack + distinctiveness aggregates reusing Phase-16 modules (D-19).
- RAG-02 retrieval-influence asserted in eval; STYLE-04 pass bar gated via compare_reports lift/regression diff (D-18).
</success_criteria>

<output>
Create `.planning/phases/17-tier-a-design-token-theme-layer-style-packs-assembly-rag/17-05-SUMMARY.md` when done.
</output>
