---
phase: 18-tier-a-catalog-expansion
plan: 03
type: execute
wave: 3
depends_on: [18-01, 18-02]
files_modified:
  - packages/genui/src/__tests__/manifest.test.ts
  - packages/genui/src/studio/__tests__/catalog-example-render.test.tsx
  - packages/genui/artifacts/spec.schema.json
  - packages/genui/artifacts/genui-prompt.json
autonomous: false
requirements: [CTLG-07, CTLG-08]
user_setup: []
must_haves:
  truths:
    - "CI proves all 16 catalog entries render a real component (never a fallback) through the shared renderer"
    - "CI proves every catalog example passes the wire SpecNodeSchema (wire/render parity — no onClick-style drift)"
    - "The re-emitted Bedrock artifacts advertise the 6 new components and the drift gate stays green"
    - "The registry version SHA-256 changed from the pre-Phase-18 value, so the Phase-14 cache auto-invalidates"
  artifacts:
    - path: "packages/genui/src/__tests__/manifest.test.ts"
      provides: "count bumped to 16, a11y negative tests for new required props, and the wire/render parity describe block"
      contains: "SpecNodeSchema"
    - path: "packages/genui/src/studio/__tests__/catalog-example-render.test.tsx"
      provides: "entry-count assertion bumped to 16"
      contains: "toHaveLength(16)"
    - path: "packages/genui/artifacts/genui-prompt.json"
      provides: "re-emitted compact catalog advertising the 6 new components"
      contains: "avatar"
  key_links:
    - from: "manifest.test.ts parity block"
      to: "packages/genui/src/schema/spec-schema.ts SpecNodeSchema"
      via: "each catalog example is spread into { type, ...example } and safeParsed against the wire union"
      pattern: "SpecNodeSchema.safeParse"
    - from: "packages/genui/artifacts/*.json"
      to: "packages/genui/src/generation/__tests__/artifacts.test.ts drift gate"
      via: "committed artifacts must match freshly emitted output"
      pattern: "artifacts"
---

<objective>
Close the phase: extend the CI example gate to cover all 16 entries (count bumps + a11y negatives), add the wire/render parity test that spreads each catalog example into a wire node and safeParses it against `SpecNodeSchema` (the standing guard against the Phase-17 `onClick` drift class), re-emit the Bedrock artifacts and confirm the drift gate is green, assert the registry-version SHA-256 changed (proving Phase-14 cache auto-invalidation still holds), and record the deferred connected-env eval lift-vs-baseline checkpoint.

Purpose: This plan makes the whole component set CI-verified and machine-provable (CTLG-07), keeps the generator's advertised catalog current (CTLG-08 / D-06), and proves the cache-invalidation contract (CTLG-08 / D-05). It depends on every catalog entry existing (18-01 layout + 18-02 leaves), so it runs last. The live eval corpus run (D-10) is a deferred connected-env checkpoint — same posture as Phase 17 — and must NOT block phase completion.
Output: extended tests (green), re-emitted committed artifacts (drift gate green), a registry-version-changed assertion, and a documented deferred eval checkpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-tier-a-catalog-expansion/18-CONTEXT.md
@.planning/phases/18-tier-a-catalog-expansion/18-PATTERNS.md
@.planning/phases/18-tier-a-catalog-expansion/18-01-SUMMARY.md
@.planning/phases/18-tier-a-catalog-expansion/18-02-SUMMARY.md

<interfaces>
<!-- Test/artifact contracts the executor needs. -->

Current failing-after-add assertions (will break once 6 entries land — must be bumped to 16):
  packages/genui/src/__tests__/manifest.test.ts line ~136: `expect(REGISTERED_TYPES.length).toBe(10);`  → 16
  packages/genui/src/studio/__tests__/catalog-example-render.test.tsx line ~53: `expect(entries).toHaveLength(10);` → 16

CTLG-04 example loop (manifest.test.ts ~lines 30-43) already iterates COMPONENT_REGISTRY dynamically — new entries auto-covered.
a11y negative-test pattern (manifest.test.ts ~lines 51-58): `COMPONENT_REGISTRY.X?.propsSchema.safeParse({...omitting required...})` then `expect(result?.success).toBe(false)`.

Wire/render parity block (NEW — 18-PATTERNS Section 11.C):
  import { SpecNodeSchema } from "../schema/spec-schema";
  for each [type, entry] in COMPONENT_REGISTRY: SpecNodeSchema.safeParse({ type, ...entry.example }) must succeed.
  NOTE: input entry example uses `inputType` (not `type`) so the spread does not clobber the discriminant.

Artifact re-emit: `pnpm gen:artifacts` (== tsx scripts/emit-bedrock-artifacts.ts) regenerates
  packages/genui/artifacts/{spec.schema.json,genui-prompt.json}. Drift gate:
  packages/genui/src/generation/__tests__/artifacts.test.ts asserts committed == freshly emitted.

Registry version: packages/genui/src/registry/registry-version.ts REGISTRY_VERSION.version = computeRegistryHash(COMPONENT_REGISTRY) (SHA-256). Adding 6 entries changes the key set → new hash → Phase-14 cache invalidates.

Eval harness (deferred live run): apps/email-listener/scripts/genui_eval/ (run_eval/rubric/style_metrics/compare_reports).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend CI example gate to 16 entries + a11y negatives + wire/render parity test</name>
  <read_first>
    - packages/genui/src/__tests__/manifest.test.ts (the file being modified — the CTLG-04 loop ~lines 30-43, the a11y negative block ~lines 51-101, the REGISTERED_TYPES count ~line 136, the hash-sensitivity test ~lines 172-186)
    - packages/genui/src/studio/__tests__/catalog-example-render.test.tsx (the file being modified — the toHaveLength(10) assertion ~line 53)
    - packages/genui/src/schema/spec-schema.ts (SpecNodeSchema export — the wire union the parity test safeParses against)
    - .planning/phases/18-tier-a-catalog-expansion/18-PATTERNS.md Section 11 (A, B, C) and GOTCHAs 4, 6
  </read_first>
  <behavior>
    - manifest.test.ts REGISTERED_TYPES count assertion is 16 and passes.
    - catalog-example-render.test.tsx entry-count assertion is 16 and passes; every entry renders a real component (no FALLBACK_MARKER).
    - New a11y negative tests: omitting avatar.alt, input.label, nav.aria-label, tabs.aria-label each fails propsSchema.
    - New parity describe block: for every entry, `SpecNodeSchema.safeParse({ type, ...example })` succeeds; if any entry drifts (wire missing a field the render has, or vice versa) the test fails with the type name and the Zod error.
    - The existing hash-sensitivity test still passes.
  </behavior>
  <action>
    In `manifest.test.ts`: bump `REGISTERED_TYPES.length` to `16` (GOTCHA 4). Add a11y negative tests for the 4 new top-level required props (18-PATTERNS Section 11.A) — avatar (omit alt), input (omit label), nav (omit aria-label), tabs (omit aria-label), each asserting `success === false`. Add the wire/render parity `describe("Wire/render schema parity (Phase-18 D-05)", ...)` block (18-PATTERNS Section 11.C): import `SpecNodeSchema` from `../schema/spec-schema`, iterate `Object.entries(COMPONENT_REGISTRY)`, safeParse `{ type, ...entry.example }`, and throw an informative error including `result.error.format()` on failure.

    In `catalog-example-render.test.tsx`: bump `entries.toHaveLength(10)` to `16` (GOTCHA 4). The `it.each` render + no-FALLBACK_MARKER assertions auto-cover the new entries.
  </action>
  <verify>
    <automated>cd packages/genui && npx vitest run src/__tests__/manifest.test.ts src/studio/__tests__/catalog-example-render.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/manifest.test.ts src/studio/__tests__/catalog-example-render.test.tsx` exits 0.
    - `grep 'toBe(16)' packages/genui/src/__tests__/manifest.test.ts` returns 1 (REGISTERED_TYPES count).
    - `grep 'toHaveLength(16)' packages/genui/src/studio/__tests__/catalog-example-render.test.tsx` returns 1.
    - `grep -c 'SpecNodeSchema.safeParse' packages/genui/src/__tests__/manifest.test.ts` returns >=1 (parity block present).
    - a11y negative tests exist for avatar/input/nav/tabs (grep the 4 test titles returns each).
    - The parity block passes for all 16 entries (proven by the test-run exit 0) — no wire/render drift.
  </acceptance_criteria>
  <done>example gate covers 16 entries; a11y negatives added; wire/render parity test green for all 16 entries; both test files exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Re-emit Bedrock artifacts, confirm drift gate, and assert registry-version bump</name>
  <read_first>
    - packages/genui/scripts/emit-bedrock-artifacts.ts (the emit script invoked by `pnpm gen:artifacts` — no code change, run it)
    - packages/genui/src/generation/__tests__/artifacts.test.ts (the drift gate that must stay green)
    - packages/genui/src/registry/registry-version.ts (REGISTRY_VERSION.version = computeRegistryHash(COMPONENT_REGISTRY))
    - packages/genui/artifacts/spec.schema.json and packages/genui/artifacts/genui-prompt.json (the committed artifacts being re-emitted)
    - .planning/phases/18-tier-a-catalog-expansion/18-PATTERNS.md Sections 10, 12
  </read_first>
  <action>
    Run `pnpm gen:artifacts` in packages/genui (== `tsx scripts/emit-bedrock-artifacts.ts`) to regenerate `artifacts/spec.schema.json` and `artifacts/genui-prompt.json` from the now-16-entry catalog + extended wire schema. Commit the regenerated artifacts. Confirm the drift gate (`src/generation/__tests__/artifacts.test.ts`) is green against the fresh files. Add (or confirm) a test that captures the pre-Phase-18 registry-version SHA-256 as a known-changed value: assert `REGISTRY_VERSION.version` differs from the prior committed hash (proving the version bumped, which is what auto-invalidates the Phase-14 cache per D-05). If a fixed prior-hash literal is brittle, instead assert the version string is a 64-char hex AND that the 6 new type keys are present in `Object.keys(COMPONENT_REGISTRY)` (the hash is a pure function of the key set + schemas, so a changed key set guarantees a changed hash — the existing hash-sensitivity test already proves this mechanism). Confirm the re-emitted `genui-prompt.json` compact catalog now advertises avatar/input/nav/feed-item/tabs/section so the generator can compose them.
  </action>
  <verify>
    <automated>cd packages/genui && npx vitest run src/generation/__tests__/artifacts.test.ts src/registry</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/generation/__tests__/artifacts.test.ts` exits 0 (drift gate green — committed artifacts match freshly emitted).
    - `grep -l 'avatar' packages/genui/artifacts/genui-prompt.json` finds the string (new components advertised); likewise `input`, `nav`, `feed-item`, `tabs`, `section` each present in the compact catalog.
    - `grep -o '"avatar"\|"input"\|"nav"\|"feed-item"\|"tabs"\|"section"' packages/genui/artifacts/spec.schema.json | sort -u` lists all 6 new type literals in the emitted JSON Schema.
    - The registry-version test asserts the 6 new keys are in COMPONENT_REGISTRY (or the hash differs from the prior value) — test-run exit 0.
    - Full package suite is green: `npx vitest run` in packages/genui exits 0.
  </acceptance_criteria>
  <done>artifacts re-emitted + committed; drift gate green; new components advertised in both artifacts; registry-version bump proven; full genui suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The deterministic Phase-18 deliverables are complete and CI-proven: 6 new catalog entries (avatar, input, nav, feed-item, tabs, section) render real components; wire/render parity is enforced in CI; Bedrock artifacts advertise the new components with a green drift gate; the registry version bumped (cache auto-invalidates). This checkpoint records the DEFERRED connected-env eval lift-vs-baseline run (D-10 / success criterion 4).
  </what-built>
  <how-to-verify>
    The live eval run needs Bedrock creds + a seeded DB (same posture as the Phase-17 deferred `--all-packs` run) and is therefore a connected-env checkpoint, NOT a blocker for phase completion. When a connected environment is available, run the Phase-16/17 eval harness in `apps/email-listener/scripts/genui_eval/` on the profile/feed/nav corpus prompts and confirm:
    1. The new components (avatar/feed-item/nav/section) are COMPOSED in the generated specs (not degraded to generic cards).
    2. A measurable rubric lift over the recorded Phase-17 score (`compare_reports` vs the Phase-17 baseline).
    3. No a11y/contrast regression — the Phase-17 hard a11y gate still holds.
    Record the outcome as `DEF-18-03-01` in the phase SUMMARY. Until then, mark this checkpoint deferred (do NOT block the phase — the offline/deterministic checks in Tasks 1-2 are the phase gate).
  </how-to-verify>
  <resume-signal>Type "deferred" to complete the phase with the eval-lift run recorded as a connected-env follow-up (DEF-18-03-01), or "approved: &lt;summary&gt;" if a live run was performed and the lift + no-a11y-regression are confirmed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog/schema change → committed Bedrock artifacts | the grammar/prompt the generator is constrained by is regenerated from source |
| registry version → Phase-14 cache key | a stale artifact/version could serve poisoned cached specs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09 | Tampering | wire/render drift shipping to production (onClick class) | mitigate | The parity test (Task 1) safeParses every catalog example against the wire `SpecNodeSchema` in CI — any drift fails the build with the offending type name. This is the standing regression guard. |
| T-18-10 | Tampering | stale committed Bedrock artifacts (generator advertises a schema that no longer matches) | mitigate | `pnpm gen:artifacts` re-emit + the drift gate (`artifacts.test.ts`) asserts committed == freshly emitted; CI fails if the artifacts are stale relative to the 16-entry catalog. |
| T-18-11 | Tampering (cache poisoning) | a fallback/invalid spec cached under a bumped registry version | mitigate | Registry version SHA-256 is a pure function of the catalog key set + schemas; adding 6 entries changes it, so the Phase-14 exact cache lazily invalidates pre-bump entries. Task 2 proves the key set (and thus the hash) changed. The Phase-14 persist gate (never cache the fallback) remains in force upstream. |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | This phase installs NO new packages — all work uses existing @nauta/ui, zod, react, tsx already in packages/genui. No package-legitimacy gate required (RESEARCH package-audit N/A — zero installs). |
</threat_model>

<verification>
- `cd packages/genui && npx vitest run` exits 0 (full suite: example gate 16 entries, a11y negatives, wire/render parity, drift gate, registry hash).
- `packages/genui/artifacts/genui-prompt.json` and `spec.schema.json` advertise all 6 new components; drift gate green.
- Registry version changed (6 new keys present in COMPONENT_REGISTRY → Phase-14 cache auto-invalidates).
- Deferred eval lift-vs-baseline recorded as DEF-18-03-01 (connected-env checkpoint, non-blocking).
</verification>

<success_criteria>
- CI proves all 16 entries render a real component and every example passes the wire SpecNodeSchema (parity — no drift).
- a11y-required props enforced by negative tests for all new entries (CTLG-07).
- Bedrock artifacts re-emitted + committed, drift gate green, new components advertised to the generator (CTLG-08 / D-06).
- Registry version bumped; Phase-14 cache-invalidation-on-version-change holds (CTLG-08 / D-05).
- Live eval lift-vs-baseline is a documented deferred connected-env checkpoint (D-10) — does not block the phase.
</success_criteria>

<output>
Create `.planning/phases/18-tier-a-catalog-expansion/18-03-SUMMARY.md` when done
</output>
