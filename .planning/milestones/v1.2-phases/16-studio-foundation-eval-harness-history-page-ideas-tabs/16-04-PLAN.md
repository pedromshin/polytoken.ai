---
phase: 16-studio-foundation-eval-harness-history-page-ideas-tabs
plan: 04
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - packages/genui/src/studio/pick-page-idea.ts
  - packages/genui/src/studio/index.ts
  - packages/genui/src/__tests__/pick-page-idea.test.ts
  - apps/web/src/app/studio/_components/page-ideas-island.tsx
  - apps/web/src/app/studio/_components/studio-tabs.tsx
  - apps/web/src/app/studio/_components/generation-sandbox-island.tsx
autonomous: false
requirements: [STDO-07, IDEA-01]
user_setup: []

must_haves:
  truths:
    - "pick-page-idea.ts is a PURE, seedable sampler (injected RNG) weighting curveball 3x / Tier-B 2x / Tier-A 1x, composed multiplicatively + normalized — unit-tested for determinism (D-20)"
    - "the Page Ideas tab browses page-ideas.json (direct static import, no network) with filter controls by category/complexity/tier + a curveball-only toggle (D-20)"
    - "a Surprise me button random-samples one idea via the weighted sampler (D-20)"
    - "clicking any idea (card or Surprise me) fills the Sandbox intent and switches to the Sandbox tab, without auto-generating (D-21, D-06 manual-only preserved)"
    - "StudioTabs lifts a controlled active-tab value + a pendingIntent string; GenerationSandboxIsland accepts an optional initialIntent prop seeding its intent state (D-21)"
    - "Page Ideas surfaces only REAL corpus prompts (page-ideas.json from 16-01) — never AI-invented (IDEA-01)"
  artifacts:
    - path: "packages/genui/src/studio/pick-page-idea.ts"
      provides: "Pure seedable weighted sampler"
      contains: "pickPageIdea"
    - path: "apps/web/src/app/studio/_components/page-ideas-island.tsx"
      provides: "Page Ideas browse/filter grid + Surprise me, wired to onUseIdea"
      contains: "Surprise"
    - path: "apps/web/src/app/studio/_components/studio-tabs.tsx"
      provides: "Controlled Tabs + lifted pendingIntent + History/Page-Ideas triggers"
      contains: "pendingIntent"
  key_links:
    - from: "apps/web/src/app/studio/_components/page-ideas-island.tsx"
      to: "packages/genui/src/eval/page-ideas.json"
      via: "direct static import of PAGE_IDEAS (no network)"
      pattern: "page-ideas"
    - from: "apps/web/src/app/studio/_components/page-ideas-island.tsx"
      to: "apps/web/src/app/studio/_components/studio-tabs.tsx"
      via: "onUseIdea(prompt) sets pendingIntent + switches to sandbox tab"
      pattern: "onUseIdea"
    - from: "apps/web/src/app/studio/_components/studio-tabs.tsx"
      to: "apps/web/src/app/studio/_components/generation-sandbox-island.tsx"
      via: "pendingIntent passed as initialIntent prop"
      pattern: "initialIntent"
---

<objective>
Ship the Page-Ideas tab: a pure seedable weighted sampler (pick-page-idea.ts), a Page-Ideas island that
browses the REAL corpus (page-ideas.json, static import) with category/complexity/tier filters + a
curveball-only toggle + a Surprise-me button, and the D-21 lift in studio-tabs.tsx (controlled active-tab +
pendingIntent) so clicking an idea fills the Sandbox intent and switches tabs without auto-generating.
This plan OWNS studio-tabs.tsx: it adds BOTH the History and Page-Ideas TabsTriggers + the lift, and adds the
optional initialIntent prop to generation-sandbox-island.tsx. The History tab CONTENT lands in 16-05.

Purpose: Diversity is engineered ONLY by sampling the real corpus (never by an LLM inventing prompts) — the
user's hard constraint (IDEA-01). The lift is the minimal controlled-Tabs change; no global store (D-21).
Output: pure sampler + unit test, page-ideas-island.tsx, the studio-tabs lift, the sandbox initialIntent prop.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-studio-foundation-eval-harness-history-page-ideas-tabs/16-CONTEXT.md

<interfaces>
PageIdea entry (from 16-01, @nauta/genui/eval): id, prompt, category, complexity, tier "A" or "B", source, curveball.
PAGE_IDEAS: readonly PageIdea array (all 76) exported from @nauta/genui/eval (16-01 Task 1).

Existing studio-tabs.tsx (apps/web/src/app/studio/_components/studio-tabs.tsx):
  Currently UNCONTROLLED: Tabs defaultValue catalog; TabsList with Catalog + Sandbox triggers + a Showcase Link;
  TabsContent catalog -> CatalogBrowserIsland, TabsContent sandbox -> GenerationSandboxIsland. "use client".

Existing GenerationSandboxIsland (generation-sandbox-island.tsx):
  const [intent, setIntent] = useState string "" — local controlled textarea; D-06 manual Generate only
  (enabled:false + refetch). The textarea reads/writes `intent`.

@nauta/ui primitives: Tabs/TabsList/TabsTrigger/TabsContent (./tabs), Button (./button), Badge (./badge),
  Card/CardHeader/CardContent (./card). catalog-browser-island.tsx shows the static-import + filter-input precedent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pure seedable weighted sampler pick-page-idea.ts</name>
  <files>packages/genui/src/studio/pick-page-idea.ts, packages/genui/src/studio/index.ts, packages/genui/src/__tests__/pick-page-idea.test.ts</files>
  <behavior>
    - weightFor(idea): curveball multiplies 3x, Tier-B 2x, Tier-A 1x, composed multiplicatively (curveball Tier-B = 3*2 = 6; Tier-A non-curveball = 1).
    - pickPageIdea(ideas, rng): returns one idea sampled proportional to normalized weights; rng is an injected function returning a number in [0,1) so the pick is deterministic in tests.
    - A stub rng returning a fixed value picks a deterministic, reproducible idea (same input + same rng -> same output every run).
    - Empty input array: documented contract (throw) and tested.
    - PURE: no Math.random internally, no Date, no side effects.
  </behavior>
  <read_first>
    - packages/genui/src/studio/build-catalog-example-spec.ts (the pure-helper style in this package — named export, explicit types, no React)
    - packages/genui/src/studio/index.ts (the barrel to add the export to)
    - packages/genui/src/__tests__/manifest.test.ts (vitest style)
    - 16-CONTEXT.md D-20 (curveball 3x / Tier-B 2x / Tier-A 1x, multiplicative, normalized; pure + seedable)
  </read_first>
  <action>
    Create pick-page-idea.ts as a PURE framework-free helper. Module constants CURVEBALL_WEIGHT=3, TIER_B_WEIGHT=2,
    TIER_A_WEIGHT=1. Export weightFor(idea: PageIdea): number returning (curveball ? 3 : 1) times (tier B ? 2 : 1).
    Export pickPageIdea(ideas: readonly PageIdea array, rng: function returning number): PageIdea — build the cumulative
    normalized weight array, draw r = rng() in [0,1), return the first idea whose cumulative normalized weight exceeds r;
    throw a clear Error on an empty array. Import the PageIdea type from ../eval/page-ideas-schema (type-only). Add both
    exports to studio/index.ts. Write pick-page-idea.test.ts: weightFor returns 6 for a curveball Tier-B idea and 1 for a
    Tier-A non-curveball; pickPageIdea with a stub rng picks the expected idea deterministically across repeated calls; the
    empty-array throw is asserted. Named exports, explicit types, no Math.random in the module.
  </action>
  <verify>
    <automated>cd packages/genui; npx vitest run src/__tests__/pick-page-idea.test.ts; npx tsc --noEmit; grep -c Math.random src/studio/pick-page-idea.ts</automated>
  </verify>
  <acceptance_criteria>
    - pick-page-idea.test.ts passes; the determinism case asserts the same idea on repeated calls with the same stub rng.
    - weightFor encodes curveball 3x / Tier-B 2x / Tier-A 1x multiplicatively (grep the three weight constants).
    - grep finds zero Math.random in pick-page-idea.ts; genui typechecks.
    - pickPageIdea + weightFor exported from @nauta/genui/studio.
  </acceptance_criteria>
  <done>Pure, seedable, unit-tested weighted sampler exported from the studio barrel.</done>
</task>

<task type="auto">
  <name>Task 2: page-ideas-island.tsx — browse/filter grid + Surprise me</name>
  <files>apps/web/src/app/studio/_components/page-ideas-island.tsx</files>
  <read_first>
    - apps/web/src/app/studio/_components/catalog-browser-island.tsx (the EXACT precedent: "use client" island, direct static import of committed data, filter-input + card-grid + aria-live region, sub-components under 50 lines, named export, immutable filter)
    - packages/genui/src/eval/index.ts (PAGE_IDEAS static import surface from 16-01)
    - packages/genui/src/studio/pick-page-idea.ts (pickPageIdea from Task 1)
    - 16-CONTEXT.md D-20, D-21, IDEA-01
  </read_first>
  <action>
    Create page-ideas-island.tsx ("use client"). Direct-import PAGE_IDEAS from @nauta/genui/eval (static committed data,
    NO network — mirrors catalog-browser-island's direct import; IDEA-01: real corpus only). Accept a prop
    onUseIdea: function taking a prompt string. Render filter controls: a category select (distinct categories derived
    from PAGE_IDEAS), a complexity select (simple/medium/complex), a tier select (A/B), and a curveball-only toggle —
    all client-side filtering over the in-memory array (immutable filter, never mutate). A card grid (aria-live polite)
    of the filtered ideas: each card shows the prompt text + category/complexity/tier/curveball chips (Badge) and a
    "Use this idea" button calling onUseIdea(idea.prompt). A "Surprise me" button at the top calling
    pickPageIdea(filtered.length ? filtered : PAGE_IDEAS, () => Math.random()) then onUseIdea(result.prompt) — the island
    supplies Math.random at the call site so the sampler stays pure (D-20). Empty-filter state message. Reuse @nauta/ui
    primitives (Card, Badge, Button, the existing select/input pattern); reuse Phase-12 design tokens — NO new tokens.
    Named export PageIdeasIsland. No eval/Function/dangerouslySetInnerHTML.
  </action>
  <verify>
    <automated>cd apps/web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - page-ideas-island.tsx is a "use client" island importing PAGE_IDEAS directly (no tRPC/fetch — grep no useQuery/fetch).
    - It renders category/complexity/tier filters + a curveball-only toggle + a Surprise me button; each card has a Use-this-idea action calling onUseIdea.
    - No eval/Function/dangerouslySetInnerHTML (grep clean); apps/web typechecks.
  </acceptance_criteria>
  <done>The Page-Ideas browse/filter grid + Surprise me exists, wired to an onUseIdea callback, fed only by the real corpus.</done>
</task>

<task type="auto">
  <name>Task 3: studio-tabs lift (controlled Tabs + pendingIntent + History/Page-Ideas triggers) + sandbox initialIntent</name>
  <files>apps/web/src/app/studio/_components/studio-tabs.tsx, apps/web/src/app/studio/_components/generation-sandbox-island.tsx</files>
  <read_first>
    - apps/web/src/app/studio/_components/studio-tabs.tsx (the uncontrolled Tabs to convert to controlled + add 2 triggers)
    - apps/web/src/app/studio/_components/generation-sandbox-island.tsx (the intent useState to seed from a prop — D-06 manual-only Generate must stay)
    - apps/web/src/app/studio/_components/page-ideas-island.tsx (the PageIdeasIsland + its onUseIdea prop from Task 2)
    - 16-CONTEXT.md D-21, D-06
  </read_first>
  <action>
    Convert StudioTabs to a CONTROLLED Tabs: useState for activeTab (default "catalog") wired to Tabs value/onValueChange,
    and useState for pendingIntent (string | undefined). Add TWO new TabsTriggers — "History" (value "history") and
    "Page Ideas" (value "pageideas") — after Sandbox, matching the existing trigger styling. Add a TabsContent for
    "pageideas" rendering PageIdeasIsland with onUseIdea = (prompt) => { setPendingIntent(prompt); setActiveTab("sandbox"); }
    (D-21: fill intent + switch tab, no auto-generate). Add a TabsContent for "history" — render a placeholder element now
    (16-05 lands the HistoryIsland here; leave a clearly-marked slot so 16-05 only swaps the child). Pass pendingIntent to
    GenerationSandboxIsland as an initialIntent prop. In generation-sandbox-island.tsx: add an optional prop
    initialIntent?: string; seed the intent useState from it via useState(() => initialIntent ?? "") AND a useEffect that,
    when initialIntent changes to a new non-empty value, calls setIntent(initialIntent) WITHOUT firing the query (D-06: the
    user still clicks Generate — do NOT call refetch). Keep everything else (enabled:false + manual refetch) unchanged.
    Immutable state updates only; no new tokens; no eval.
  </action>
  <verify>
    <automated>cd apps/web; npx tsc --noEmit; npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - StudioTabs is controlled (Tabs value + onValueChange + activeTab useState) and carries a pendingIntent useState (grep pendingIntent).
    - History + Page Ideas TabsTriggers + their TabsContent exist; Page Ideas content is PageIdeasIsland with an onUseIdea that sets pendingIntent + switches to "sandbox".
    - GenerationSandboxIsland accepts initialIntent and seeds intent from it WITHOUT auto-generating (grep: no refetch in the initialIntent effect; the existing manual Generate handler is intact).
    - apps/web typechecks AND next build succeeds.
  </acceptance_criteria>
  <done>Studio tabs are controlled with a lifted pendingIntent; both new triggers exist; clicking a page idea fills the Sandbox + switches tabs without generating; History content slot is ready for 16-05.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Browser-verify the Page-Ideas tab (filters + Surprise-me + fill-and-switch)</name>
  <action>Human-only browser verification AFTER the autonomous tasks complete: confirm the Page-Ideas filters, weighted Surprise-me, and fill-and-switch (no auto-generate) behave correctly in the browser (see how-to-verify). No code change.</action>
  <what-built>
    The Page-Ideas tab (browse/filter grid over the real 76-prompt corpus + a weighted Surprise-me) and the
    studio-tabs lift that fills the Sandbox intent + switches to the Sandbox tab when an idea is chosen — without
    auto-generating. This has a visual + interactive surface, so it needs a browser eyeball.
  </what-built>
  <how-to-verify>
    1. cd apps/web && npm run dev, then open http://localhost:3000/studio
    2. Click the "Page Ideas" tab — confirm a grid of real prompts renders with category/complexity/tier/curveball chips
    3. Use the filters (category, complexity, tier) and the curveball-only toggle — confirm the grid narrows correctly
    4. Click "Surprise me" a few times — confirm it jumps you to the Sandbox tab with the textarea pre-filled by the picked prompt, and that curveballs/Tier-B come up more often than Tier-A over several clicks
    5. Click "Use this idea" on a specific card — confirm the Sandbox tab opens with THAT prompt in the textarea and that NO generation fires automatically (you must click Generate yourself — D-06)
    6. Confirm the History tab trigger is present (its content is a placeholder until 16-05) and that Catalog + Sandbox still work
  </how-to-verify>
  <resume-signal>Type "approved" once the Page-Ideas browse/filter + Surprise-me + fill-and-switch (no auto-generate) all work in the browser, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset -> UI | page-ideas.json is committed trusted content rendered as text in the browser |
| page idea -> Sandbox intent | A picked prompt fills the Sandbox textarea (then the existing generate path applies) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-T | Tampering | page-ideas content authenticity | mitigate | The tab imports the CI-gated page-ideas.json (16-01 asserts 76 real corpus entries with provenance); no AI-invented prompts can reach the UI (IDEA-01) |
| T-16-04-I | Information Disclosure | island data | accept | PAGE_IDEAS is public corpus text; the island makes no network call and reads no secrets (static import only) |
| T-16-04-E | Elevation of Privilege | renderer/eval surface | mitigate | The island only sets the Sandbox intent string; rendering still flows through the existing generate -> SpecRenderer safeParse path (no new render path); no eval/Function/dangerouslySetInnerHTML (grep gate) |
| T-16-04-SC | Tampering | npm installs | mitigate | No new npm packages (reuses @nauta/ui + @nauta/genui already in the workspace) — package-legitimacy gate N/A |
</threat_model>

<verification>
- genui: pick-page-idea.test.ts green; tsc clean; zero Math.random in the pure sampler.
- apps/web: tsc clean + next build green.
- grep: page-ideas-island makes no network call; no eval/Function/dangerouslySetInnerHTML; studio-tabs carries pendingIntent; sandbox initialIntent effect does not call refetch.
- Browser checkpoint: filters + Surprise-me + fill-and-switch (no auto-generate) verified.
</verification>

<success_criteria>
STDO-07 + IDEA-01: a Page-Ideas tab surfaces realistic curveball prompts seeded ONLY from the real corpus
(weighted Surprise-me toward curveballs/Tier-B via a pure seedable sampler), and clicking one sends it straight
into the generation Sandbox (fills intent + switches tab) without auto-generating — the D-06 manual contract preserved.
</success_criteria>

<output>
Create `.planning/phases/16-studio-foundation-eval-harness-history-page-ideas-tabs/16-04-SUMMARY.md` when done
</output>
