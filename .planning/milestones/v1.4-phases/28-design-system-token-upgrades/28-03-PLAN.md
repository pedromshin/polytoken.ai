---
phase: 28-design-system-token-upgrades
plan: 03
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - apps/web/src/app/studio/_components/history-island.tsx
  - apps/web/src/app/studio/_components/page-ideas-island.tsx
  - apps/web/src/app/chat/_components/conversation-rail.tsx
  - docs/design/product-register-and-bans.md
autonomous: true
requirements: [TOKEN-04, TOKEN-05]

must_haves:
  truths:
    - "Studio history rows and page-idea cards animate in with a capped-6 stagger (40ms step: 0,40,80,120,160,200ms then flat 200ms), motion-reduce cancels the entrance"
    - "the /chat conversation rail no longer uses backdrop-blur-md — it renders a solid token surface (default bg-background/95) and the row text stays legible over canvas content"
    - "the backdrop-blur-md debt is CLOSED in docs/design/product-register-and-bans.md item 3 (either removed-exception or promoted-allowlist wording, per which branch shipped)"
    - "the two-value radius allowlist (TOKEN-04) is recorded as forward guidance under bans-doc item 10"
  artifacts:
    - path: "apps/web/src/app/studio/_components/history-island.tsx"
      provides: "per-row stagger entrance"
      contains: "slide-in-from-bottom-1"
    - path: "apps/web/src/app/studio/_components/page-ideas-island.tsx"
      provides: "per-card stagger entrance via index prop"
      contains: "animationDelay"
    - path: "apps/web/src/app/chat/_components/conversation-rail.tsx"
      provides: "blur-debt resolution (solid token surface)"
      contains: "bg-background/95"
    - path: "docs/design/product-register-and-bans.md"
      provides: "item 3 blur-debt closure + item 10 radius allowlist note"
      contains: "Radius allowlist (added Phase 28)"
  key_links:
    - from: "apps/web/src/app/studio/_components/history-island.tsx"
      to: "tailwindcss-animate + inline animationDelay"
      via: "Math.min(index, 5) * 40 ms per <li>"
      pattern: "Math\\.min\\(index, 5\\) \\* 40"
    - from: "apps/web/src/app/chat/_components/conversation-rail.tsx"
      to: "docs/design/product-register-and-bans.md item 3"
      via: "blur-debt closure note"
      pattern: "backdrop-blur-md"
---

<objective>
Finish TOKEN-05 with the Studio list-item stagger, resolve the folded conversation-rail
backdrop-blur debt via the UI-SPEC's 3-branch chain, and close both doc obligations (blur-debt
closure + the TOKEN-04 radius-allowlist forward-guidance note).

Purpose: The stagger makes Studio history/page-ideas lists animate in beyond bare Radix transitions;
the blur-debt resolution removes the last standing glassmorphism exception; the docs updates keep the
bans doc honest.
Output: 2 Studio list files staggered, conversation-rail de-blurred, bans doc updated in two places.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-design-system-token-upgrades/28-UI-SPEC.md

Depends on 28-01 for ordering only: the bans-doc radius note (Task 3) describes `--radius-xl`/
`--radius-2xl`, which 28-01 declares — so those tokens exist before they are documented. The stagger
utilities (`animate-in`/`fade-in-0`/`slide-in-from-bottom-1`) already exist (tailwindcss-animate,
pre-phase). Zero new CSS/config this plan.

<interfaces>
Exact current call sites (verified):
- history-island.tsx ~line 304: `{rows.map((row) => (` … `<li key={row.id} role="listitem">` (the
  `<ul role="list">` inside HistoryMasterList).
- page-ideas-island.tsx: `function IdeaCard({ idea, onUseIdea }: {...})` (line 115) returning
  `<Card className="flex flex-col gap-0">` (line 127); mapped at line 353 as
  `{filtered.map((idea) => (` … `<IdeaCard key={idea.id} idea={idea} onUseIdea={onUseIdea} />`.
- conversation-rail.tsx line 111: `"h-full shrink-0 overflow-hidden border-r border-border/50
  bg-background/70 backdrop-blur-md"` (first entry of a `cn(...)`; the `"t-panel-reveal"` and width
  entries below it are UNCHANGED).
- docs/design/product-register-and-bans.md: item 3 "Known exception (documented debt, 2026-07-06)"
  paragraph (lines ~59-63); item 10 "Over-rounded corners" (lines ~83-85).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: history-island.tsx per-row stagger</name>
  <read_first>
    - apps/web/src/app/studio/_components/history-island.tsx (the `rows.map` block ~lines 302-317)
    - 28-UI-SPEC.md → "TOKEN-05 (b) … Consumer 1" (the exact `<li>` classes + delay formula)
  </read_first>
  <action>
    Change `rows.map((row) => …)` to `rows.map((row, index) => …)` and apply to the `<li>` wrapper (keep
    `key={row.id}` and `role="listitem"`): className
    `"animate-in fade-in-0 slide-in-from-bottom-1 duration-200 motion-reduce:animate-none"` plus
    `style={{ animationDelay: `${Math.min(index, 5) * 40}ms` }}`. The HistoryRow child is unchanged. The
    inline style is required because Tailwind's JIT cannot scan a dynamic `delay-[${n}ms]` class — this
    is the standard escape hatch. Delays: 0,40,80,120,160,200ms then flat 200ms (cap 6).
  </action>
  <verify>
    <automated>grep -F 'slide-in-from-bottom-1 duration-200 motion-reduce:animate-none' apps/web/src/app/studio/_components/history-island.tsx && grep -F 'Math.min(index, 5) * 40' apps/web/src/app/studio/_components/history-island.tsx</automated>
    <automated>npm --prefix apps/web run typecheck</automated>
  </verify>
  <done>History rows carry the stagger classes + per-index animationDelay, capped at 6; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: page-ideas-island.tsx per-card stagger via index prop</name>
  <read_first>
    - apps/web/src/app/studio/_components/page-ideas-island.tsx (IdeaCard signature line 115, its Card
      line 127, the `filtered.map` at line 353)
    - 28-UI-SPEC.md → "TOKEN-05 (b) … Consumer 2" (add `index` prop, apply to `<Card>`)
  </read_first>
  <action>
    Add a `readonly index: number` prop to IdeaCard's props type and destructure it. Change
    `filtered.map((idea) => …)` to `filtered.map((idea, index) => …)` and pass `index={index}` into
    `<IdeaCard …>` (keep `key={idea.id}`). On IdeaCard's `<Card>`, extend the className to
    `"flex flex-col gap-0 animate-in fade-in-0 slide-in-from-bottom-1 duration-200 motion-reduce:animate-none"`
    and add `style={{ animationDelay: `${Math.min(index, 5) * 40}ms` }}`. Same cap-6 formula as Task 1.
  </action>
  <verify>
    <automated>grep -F 'index: number' apps/web/src/app/studio/_components/page-ideas-island.tsx && grep -F 'slide-in-from-bottom-1 duration-200 motion-reduce:animate-none' apps/web/src/app/studio/_components/page-ideas-island.tsx && grep -F 'Math.min(index, 5) * 40' apps/web/src/app/studio/_components/page-ideas-island.tsx</automated>
    <automated>npm --prefix apps/web run typecheck</automated>
    <human-check>With the dev server up, open Studio history + page-ideas tabs and confirm items cascade in (≤6 staggered, rest flat), and reduced-motion shows them appear immediately with no cascade.</human-check>
  </verify>
  <done>IdeaCard takes an index prop, threaded from filtered.map; Card carries the stagger classes + animationDelay; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 3: conversation-rail blur-debt resolution + bans-doc closure + radius allowlist note</name>
  <read_first>
    - apps/web/src/app/chat/_components/conversation-rail.tsx (line 111, the `cn(...)` first entry)
    - docs/design/product-register-and-bans.md (item 3 exception paragraph ~lines 59-63; item 10 ~lines 83-85)
    - 28-UI-SPEC.md → "Conversation-Rail Backdrop-Blur Debt Resolution" (the 3-branch chain + both docs
      templates) and "TOKEN-04 … Docs update" (the exact radius-allowlist blockquote)
  </read_first>
  <action>
    Blur debt — apply the UI-SPEC's prescribed first attempt (default, autonomous): in the `cn(...)`
    first entry, drop `backdrop-blur-md` and raise opacity `/70`→`/95` → `"h-full shrink-0 overflow-hidden
    border-r border-border/50 bg-background/95"`. Keep the `"t-panel-reveal"` and width entries below it
    unchanged. Branch policy (from UI-SPEC): default = ship `bg-background/95`; if the human-check shows
    perceptible canvas bleed-through, fall back to fully opaque `bg-background` (Fallback 1); only if BOTH
    solid variants read wrong, restore `bg-background/70 backdrop-blur-md` (Fallback 2). In this
    autonomous run ship the default and record the outcome; a human-check flags if a fallback is needed.
    Docs — item 3: REPLACE the "Known exception (documented debt, 2026-07-06)" paragraph with the
    UI-SPEC's "blur removed" template (outcome 1/2: state the rail was de-blurred to a solid
    bg-background/95 surface, no glassmorphism exception remains) — OR, if Fallback 2 shipped, the
    "permanent allowlist entry" template instead. Use today's date.
    Docs — item 10: APPEND the UI-SPEC's exact radius-allowlist blockquote ("**Radius allowlist (added
    Phase 28):** interactive controls … use the base radius family … containers use rounded-xl/
    rounded-2xl …") as forward guidance, not retroactive enforcement.
  </action>
  <verify>
    <automated>grep -F 'bg-background/95' apps/web/src/app/chat/_components/conversation-rail.tsx && ! grep -F 'backdrop-blur-md' apps/web/src/app/chat/_components/conversation-rail.tsx</automated>
    <automated>grep -F 'Radius allowlist (added Phase 28)' docs/design/product-register-and-bans.md && ! grep -F 'documented debt, 2026-07-06' docs/design/product-register-and-bans.md</automated>
    <human-check>With the dev server up, populate the canvas with ≥3 overlapping panels behind the rail, open the rail, and confirm conversation-row text/hover states read cleanly with no canvas bleed-through. If bleed-through degrades legibility, apply Fallback 1 (bg-background) and re-check; document which branch shipped.</human-check>
  </verify>
  <done>conversation-rail uses a solid token surface (default bg-background/95, no backdrop-blur-md); bans-doc item 3 exception is closed (removed or promoted to allowlist per branch shipped); bans-doc item 10 carries the radius-allowlist note; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | `className`/`style` and prose-doc edits only. No user input, network, markup injection, or data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | Denial of Service (accessibility) | Studio list stagger | mitigate | Risk: staggered entrance without reduced-motion respect harms vestibular users. `motion-reduce:animate-none` is required in both consumers' class strings and grep-asserted (Tasks 1-2). |
| T-28-03-02 | Information Disclosure (legibility) | conversation-rail solid surface | mitigate | Risk: dropping blur could let canvas content bleed through and degrade row legibility. UI-SPEC's 3-branch fallback + a human-check over ≥3 overlapping panels gate the final surface; the debt is closed either by de-blur or documented promotion. |
| T-28-03-03 | Repudiation | bans-doc exception left open | mitigate | Risk: shipping the code change but leaving the "documented debt" note orphans the record. Grep asserts the old "documented debt, 2026-07-06" wording is GONE and the closure/allowlist note is present. |
| T-28-03-SC | Tampering (supply chain) | npm installs | accept | Zero new dependencies; only pre-vetted tailwindcss-animate consumption. No install task. |
</threat_model>

<verification>
- grep gates confirm stagger classes + cap-6 delay formula in both Studio files.
- grep gates confirm the rail is solid (bg-background/95, no backdrop-blur-md) and the bans doc closed both notes.
- `npm --prefix apps/web run typecheck` green.
- Visual (human-check): stagger cascades and reduces; rail stays legible over canvas.
</verification>

<success_criteria>
Maps to Phase 28 success criterion 4 (Studio history/page-ideas list items animate in) and closes the
folded conversation-rail blur debt + records the TOKEN-04 radius allowlist:
- Studio history + page-ideas lists stagger in via tailwindcss-animate, motion-reduce-safe.
- The last glassmorphism exception is resolved and the bans doc reflects it, plus the radius-allowlist note.
</success_criteria>

<output>
Create `.planning/phases/28-design-system-token-upgrades/28-03-SUMMARY.md` when done.
</output>
