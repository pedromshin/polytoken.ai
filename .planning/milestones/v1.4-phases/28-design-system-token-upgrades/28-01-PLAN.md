---
phase: 28-design-system-token-upgrades
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/globals.css
  - packages/tailwind-config/base.ts
  - packages/tailwind-config/web.ts
  - apps/web/src/app/__tests__/token-contrast.test.ts
autonomous: true
requirements: [TOKEN-01, TOKEN-02, TOKEN-03, TOKEN-04]

must_haves:
  truths:
    - "secondary, muted, accent render as tonally distinct neutrals (hue 164, saturation ≤12%) in BOTH light and dark mode — no longer one shared 0 0% 96.1% gray"
    - "chart-1..5 resolve to a teal-anchored categorical ramp and every sidebar-* var aliases an existing token (blue --sidebar-ring is gone, now var(--primary))"
    - "shadow-elevation-1/2/3 Tailwind utilities exist (base.ts) and resolve to teal-tinted stacked-hairline --elevation-* values (globals.css), max blur 8px"
    - "rounded-xl / rounded-2xl now resolve to --radius-xl / --radius-2xl (12px / 16px) via web.ts, token-driven"
    - "a committed vitest test parses globals.css and asserts WCAG-AA (≥4.5:1) for all 6 changed TOKEN-01 *-foreground pairs in both modes, and exits 0"
  artifacts:
    - path: "apps/web/src/app/globals.css"
      provides: "TOKEN-01/02 value changes + TOKEN-03 --elevation-* + TOKEN-04 --radius-* custom properties"
      contains: "164 6% 95.3%"
    - path: "packages/tailwind-config/base.ts"
      provides: "boxShadow elevation-1/2/3 theme.extend entries"
      contains: "elevation-1"
    - path: "packages/tailwind-config/web.ts"
      provides: "borderRadius xl/2xl theme.extend entries"
      contains: "--radius-xl"
    - path: "apps/web/src/app/__tests__/token-contrast.test.ts"
      provides: "committed WCAG-AA contrast regression gate over globals.css neutral pairs"
      contains: "4.5"
  key_links:
    - from: "packages/tailwind-config/base.ts"
      to: "apps/web/src/app/globals.css"
      via: "var(--elevation-N)"
      pattern: "var\\(--elevation-"
    - from: "packages/tailwind-config/web.ts"
      to: "apps/web/src/app/globals.css"
      via: "var(--radius-xl) / var(--radius-2xl)"
      pattern: "var\\(--radius-2?xl\\)"
    - from: "apps/web/src/app/__tests__/token-contrast.test.ts"
      to: "apps/web/src/app/globals.css"
      via: "node:fs read + HSL→luminance→ratio"
      pattern: "globals\\.css"
---

<objective>
Rebase the foundational token layer so every consuming surface benefits at once: differentiate the
three neutral fills (TOKEN-01), rebase chart + sidebar off teal (TOKEN-02), declare a teal-tinted
elevation scale + register its Tailwind utilities (TOKEN-03), and add the xl/2xl radius steps
(TOKEN-04) — then lock the neutral-pair contrast with a committed vitest regression gate.

Purpose: This is the ONE phase allowed to change token VALUES. All changes are pure `:root`/`.dark`
value edits and `theme.extend` config entries — no consumer file is touched here (consumers land in
28-02 / 28-03). Every existing `bg-muted`/`hover:bg-accent`/`shadow-elevation-*`/`rounded-xl` usage
inherits the new values automatically.
Output: Updated globals.css, base.ts, web.ts, and a new committed WCAG contrast test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-design-system-token-upgrades/28-UI-SPEC.md

The UI-SPEC is copy-paste-ready — every value below is quoted verbatim from it. Do NOT invent or
"round" a single number. The two config files the executor must know the current shape of:

<interfaces>
Current `apps/web/src/app/globals.css` — neutral/chart/sidebar vars live inside `:root {…}` (lines
~15-48) and `.dark {…}` (lines ~49-82). `--radius: 0.5rem` is the only radius var today; `--radius`
has NO `.dark` override. There is NO `--elevation-*` var anywhere yet.

Current `packages/tailwind-config/base.ts` — `theme.extend` holds `colors` + `borderColor` only. No
`boxShadow` key yet. (This file, per the UI-SPEC drift-check, is where boxShadow belongs — cross-preset primitive.)

Current `packages/tailwind-config/web.ts` — `theme.extend.borderRadius` = `{ lg, md, sm }` only, and
`tailwindcss-animate` is already registered in `plugins`. (This is where `xl`/`2xl` borderRadius
belongs, NOT base.ts — see the UI-SPEC's drift-check correction at its top.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TOKEN-01 + TOKEN-02 value changes in globals.css</name>
  <read_first>
    - apps/web/src/app/globals.css (`:root` lines ~15-48, `.dark` lines ~49-82)
    - 28-UI-SPEC.md → "TOKEN-01 … Final values" table + "TOKEN-02 … Final values" table + the
      sidebar alias CSS block. Copy every triplet EXACTLY.
  </read_first>
  <action>
    In `:root`, replace the 6 neutral values with the TOKEN-01 light column: `--muted: 164 6% 95.3%`,
    `--muted-foreground: 164 5% 41.5%`, `--secondary: 164 8% 93.5%`, `--secondary-foreground: 164 4% 11%`,
    `--accent: 164 10% 94.3%`, `--accent-foreground: 164 6% 12%`. Replace the 5 chart values with the
    TOKEN-02 light column: `--chart-1: 164 39% 22%`, `--chart-2: 168 45% 38%`, `--chart-3: 182 40% 46%`,
    `--chart-4: 152 32% 54%`, `--chart-5: 196 38% 30%`. Replace all 8 `--sidebar-*` lines with the alias
    form (var references, identical text in both blocks): `--sidebar-background: var(--background)`,
    `--sidebar-foreground: var(--foreground)`, `--sidebar-primary: var(--primary)`,
    `--sidebar-primary-foreground: var(--primary-foreground)`, `--sidebar-accent: var(--accent)`,
    `--sidebar-accent-foreground: var(--accent-foreground)`, `--sidebar-border: var(--border)`,
    `--sidebar-ring: var(--primary)`.
    In `.dark`, apply the TOKEN-01 dark column: `--muted: 164 6% 15%`, `--muted-foreground: 164 6% 65%`,
    `--secondary: 164 8% 17.7%`, `--secondary-foreground: 164 10% 97%`, `--accent: 164 12% 17.5%`,
    `--accent-foreground: 164 8% 97%`. Apply the TOKEN-02 dark chart column: `--chart-1: 164 45% 52%`,
    `--chart-2: 168 50% 66%`, `--chart-3: 182 42% 73%`, `--chart-4: 152 35% 80%`, `--chart-5: 196 45% 59%`.
    Apply the SAME 8 sidebar alias lines in `.dark` (the `var()` resolves per-mode automatically).
    Do NOT touch `--primary` (teal `164 39% 22%`), `--background`, `--foreground`, `--border`, `--ring`,
    `--destructive`, or `--radius` — those are unchanged. `--sidebar-ring` points at `var(--primary)`,
    NOT `var(--ring)` (kill the accidental blue).
    Regression guard (UI-SPEC "Executor note"): `--muted-foreground` light is `164 5% 41.5%` —
    intentionally darker than the old stock `45.1%` for contrast safety. Do not round it back up.
  </action>
  <verify>
    <automated>grep -F '164 6% 95.3%' apps/web/src/app/globals.css && grep -F '164 45% 52%' apps/web/src/app/globals.css && grep -F -- '--sidebar-ring: var(--primary)' apps/web/src/app/globals.css</automated>
    <human-check>With the dev server up, screenshot the main /chat + /studio surfaces (light + dark) and confirm the change reads as "the same app, slightly more defined," not a retheme (28-CONTEXT max-drift guard; all 6 neutrals stay ≤2.8 lightness points from baseline).</human-check>
  </verify>
  <done>All 6 TOKEN-01 neutral values (both modes), all 10 chart values (both modes), and all 16 sidebar alias lines (8 × 2 blocks) match the UI-SPEC verbatim; --primary and --radius untouched.</done>
</task>

<task type="auto">
  <name>Task 2: TOKEN-03 elevation vars + TOKEN-04 radius vars, and their Tailwind config entries</name>
  <read_first>
    - apps/web/src/app/globals.css (end of `:root` and `.dark` blocks)
    - packages/tailwind-config/base.ts (the `theme.extend` object)
    - packages/tailwind-config/web.ts (the `theme.extend.borderRadius` object)
    - 28-UI-SPEC.md → "TOKEN-03 — Elevation/Shadow Scale" (both CSS blocks + the base.ts boxShadow
      block) and "TOKEN-04 — Radius Steps" (the two `--radius-*` vars + the web.ts borderRadius block).
  </read_first>
  <action>
    In globals.css `:root` (after `--sidebar-ring`), add the three light `--elevation-1/2/3`
    declarations verbatim from the UI-SPEC (stacked hairline `0 0 0 1px hsl(var(--border) / 0.6)` +
    teal-tinted ambient `hsl(164 25% 15% / …)` layers; elevation-3's largest blur is exactly 8px — the
    hard ceiling from ban item 9, never exceed it). In globals.css `.dark` (after `--sidebar-ring`),
    add the three dark `--elevation-1/2/3` declarations verbatim (hairline `hsl(var(--border))` at full
    alpha + neutral-black `hsl(0 0% 0% / …)` ambient). Also in `:root` only, add
    `--radius-xl: calc(var(--radius) + 4px)` and `--radius-2xl: calc(var(--radius) + 8px)` (no `.dark`
    override — `--radius` has none).
    In base.ts, add a `boxShadow` key to the existing `theme.extend` (alongside colors/borderColor):
    `"elevation-1": "var(--elevation-1)"`, `"elevation-2": "var(--elevation-2)"`,
    `"elevation-3": "var(--elevation-3)"`. This generates the `shadow-elevation-*` utilities consumed by
    28-02.
    In web.ts, add to the existing `theme.extend.borderRadius` object (keep lg/md/sm): `xl: "var(--radius-xl)"`,
    `"2xl": "var(--radius-2xl)"`. `extend` semantics make these override Tailwind's stock xl/2xl, so
    `card.tsx`'s literal `rounded-xl` becomes token-driven at the SAME 12px (no card.tsx edit here).
  </action>
  <verify>
    <automated>grep -F -- '--elevation-3: 0 0 0 1px' apps/web/src/app/globals.css && grep -F 'calc(var(--radius) + 4px)' apps/web/src/app/globals.css && grep -F '"elevation-1": "var(--elevation-1)"' packages/tailwind-config/base.ts && grep -F 'var(--radius-2xl)' packages/tailwind-config/web.ts</automated>
    <automated>npm --prefix apps/web run typecheck</automated>
  </verify>
  <done>Both elevation blocks (light + dark) and both radius vars exist in globals.css; boxShadow elevation-1/2/3 in base.ts; borderRadius xl/2xl in web.ts; apps/web typecheck is green; no elevation layer blur exceeds 8px.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Committed WCAG-AA contrast regression test over the neutral pairs</name>
  <read_first>
    - apps/web/vitest.config.ts (env jsdom, include `src/**/*.test.{ts,tsx}`)
    - apps/web/src/app/globals.css (the values Task 1 just wrote)
    - 28-UI-SPEC.md → "Computed Contrast Evidence" (the 6-row ratio table + the formula
      `(L1+0.05)/(L2+0.05)`) — use it as a sanity anchor (light muted pair ≈4.70:1 is the tightest).
  </read_first>
  <behavior>
    - Reads apps/web/src/app/globals.css via node:fs at `new URL("../globals.css", import.meta.url)`.
    - Parses BOTH the `:root {…}` and `.dark {…}` blocks separately, extracting the CURRENT value of
      each of: --muted, --muted-foreground, --secondary, --secondary-foreground, --accent,
      --accent-foreground (regex on `--token: H S% L%`). It asserts against the file's actual values,
      NOT hardcoded expected numbers — so "rounding a value back" that drops contrast is caught.
    - Converts each `H S% L%` triplet → sRGB → WCAG relative luminance → contrast ratio
      `(Llighter+0.05)/(Ldarker+0.05)`.
    - Asserts each of the 3 pairs (muted/secondary/accent) ≥ 4.5:1 in `:root` AND in `.dark` — 6
      assertions total. Test FAILS if any pair regresses below 4.5:1.
  </behavior>
  <action>
    Create apps/web/src/app/__tests__/token-contrast.test.ts. Implement pure helpers (typed
    explicitly, immutable) `parseHslTriplet`, `hslToLinearRgb`, `relativeLuminance`, `contrastRatio`,
    and a `readTokenBlock(css, blockName)` that returns a `Record<string, string>` of the vars inside a
    named block. Drive the 6 assertions from a table of `[pair, mode]`. Chart tokens are graphical with
    no `*-foreground` counterpart — do NOT gate them (UI-SPEC: informational only). This is the phase's
    single committed token gate; the retired "token count == 55" idea is NOT reintroduced.
  </action>
  <verify>
    <automated>npm --prefix apps/web run test -- token-contrast</automated>
  </verify>
  <done>token-contrast.test.ts exists, runs under vitest, computes ratios from the live globals.css values, asserts all 6 neutral pairs ≥4.5:1 in both modes, and exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | No user input, network, or data flow in this plan — pure static CSS custom-property + Tailwind-config edits. Surface is design-token values only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | globals.css neutral `*-foreground` pairs | mitigate | The real hazard: a value edit silently drops a text pair below WCAG-AA. Committed token-contrast.test.ts (Task 3) parses the live file and fails <4.5:1 in either mode — a blocking, permanent regression gate. |
| T-28-02 | Denial of Service | base.ts / web.ts theme.extend | mitigate | A malformed `boxShadow`/`borderRadius` entry breaks Tailwind generation. `npm --prefix apps/web run typecheck` (Task 2) gates config validity before the consumers in 28-02 rely on the utilities. |
| T-28-03 | Information Disclosure | any | accept | No PII, no secrets, no data flow — CSS token values only. Nothing to disclose. |
| T-28-SC | Tampering (supply chain) | npm installs | accept | Zero new npm dependencies this phase (hard constraint). No install tasks; `tailwindcss-animate` was vetted at its original install. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `grep` gates confirm every exact HSL / shadow / radius string from the UI-SPEC is present.
- `npm --prefix apps/web run typecheck` green (config + consumers compile).
- `npm --prefix apps/web run test -- token-contrast` exits 0 (all 6 neutral pairs ≥4.5:1).
- Visual (human-check): main surfaces read "slightly more defined," not rethemed; max-drift guard holds.
</verification>

<success_criteria>
Maps to Phase 28 success criteria 1, 2, and the "scale exists" half of 3:
- secondary/muted/accent are tonally distinct neutrals in both modes, 60/30/10-compliant (WCAG-gated).
- chart series + sidebar use teal-derived / teal-aliased values (no stock demo colors, no blue ring).
- elevation-1/2/3 shadow scale + xl/2xl radius steps exist and are registered as Tailwind utilities.
</success_criteria>

<output>
Create `.planning/phases/28-design-system-token-upgrades/28-01-SUMMARY.md` when done.
</output>
