---
phase: 28-design-system-token-upgrades
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - packages/ui/src/card.tsx
  - apps/web/src/app/chat/_components/composer.tsx
  - apps/web/src/app/chat/_canvas/chat-node.tsx
  - apps/web/src/app/chat/_canvas/genui-panel-node.tsx
autonomous: true
requirements: [TOKEN-03, TOKEN-05]

must_haves:
  truths:
    - "card and composer visibly render the new elevation shadows (card = elevation-1, composer dock = elevation-2 top-shadow)"
    - "both canvas node shells lift on selection: resting = shadow-elevation-1, selected = shadow-elevation-2 (reinforcing the existing ring-2 ring-primary idiom, not replacing it)"
    - "the genui panel node mount animates in (fade-in-0 + zoom-in-95, 250ms) via tailwindcss-animate, and motion-reduce cancels the entrance"
    - "the entrance is applied ONLY to GenuiPanelNode's outer shell div — never inside GenuiPanelNodeBody / GenuiPartBoundary / InteractiveWidgetBoundary / SpecRenderer"
  artifacts:
    - path: "packages/ui/src/card.tsx"
      provides: "card elevation-1 consumption"
      contains: "shadow-elevation-1"
    - path: "apps/web/src/app/chat/_components/composer.tsx"
      provides: "composer dock elevation-2"
      contains: "shadow-elevation-2"
    - path: "apps/web/src/app/chat/_canvas/chat-node.tsx"
      provides: "resting/selected elevation swap"
      contains: "shadow-elevation-1"
    - path: "apps/web/src/app/chat/_canvas/genui-panel-node.tsx"
      provides: "resting/selected elevation + mount entrance animation"
      contains: "animate-in fade-in-0 zoom-in-95"
  key_links:
    - from: "packages/ui/src/card.tsx"
      to: "shadow-elevation-1 utility"
      via: "Tailwind class generated by 28-01 base.ts boxShadow"
      pattern: "shadow-elevation-1"
    - from: "apps/web/src/app/chat/_canvas/genui-panel-node.tsx"
      to: "tailwindcss-animate utilities"
      via: "animate-in / fade-in-0 / zoom-in-95 / motion-reduce:animate-none"
      pattern: "motion-reduce:animate-none"
---

<objective>
Apply the elevation scale (TOKEN-03) to its four named consumers, and give the genui panel node its
mount entrance animation (TOKEN-05 item a) — combined into ONE className on the panel's outer shell so
the file is edited once.

Purpose: Make the elevation scale from 28-01 visibly do work (card lift, composer dock, canvas node
selection lift) and give freshly-dropped canvas panels a subtle fade+zoom entrance.
Output: 4 consumer files updated (2 shadow-only swaps, 2 canvas node shells, one of which also gains
the entrance classes).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-design-system-token-upgrades/28-UI-SPEC.md
@.planning/phases/28-design-system-token-upgrades/28-01-SUMMARY.md

Depends on 28-01: the `shadow-elevation-1/2/3` utilities only exist after 28-01 registered them in
`base.ts` + `globals.css`. The `animate-in`/`fade-in-0`/`zoom-in-95` utilities already exist
(tailwindcss-animate, registered pre-phase in web.ts) — this plan adds zero new CSS/config.

<interfaces>
Exact current strings (verified) the executor swaps — preserve ALL other classes on each line:

- `card.tsx:12` → `"rounded-xl border bg-card text-card-foreground shadow"`
- `composer.tsx:76` → `"w-full shrink-0 border-t border-border/60 bg-background shadow-sm"`
- `chat-node.tsx:150` (note the FIX-04 `border-l-2 border-l-primary` differentiation classes — KEEP them):
  `flex h-full min-h-[320px] w-full min-w-[400px] flex-col overflow-hidden rounded-lg border border-border/60 border-l-2 border-l-primary bg-background shadow-sm transition-shadow duration-150${selected ? ` ${SELECTED_RING}` : ""}`
- `genui-panel-node.tsx:157`:
  `flex h-full min-h-[240px] w-full min-w-[320px] flex-col overflow-hidden rounded-lg border border-border/60 bg-background shadow-sm transition-shadow duration-150${selected ? ` ${SELECTED_RING}` : ""}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: card.tsx + composer.tsx elevation swaps</name>
  <read_first>
    - packages/ui/src/card.tsx (line 12, the base `cn(...)` string)
    - apps/web/src/app/chat/_components/composer.tsx (line 76, the dock wrapper div)
    - 28-UI-SPEC.md → "TOKEN-03 … Named consumers" table (rows 1 and 2)
  </read_first>
  <action>
    In card.tsx line 12, change the trailing `shadow` to `shadow-elevation-1` — result:
    `"rounded-xl border bg-card text-card-foreground shadow-elevation-1"`. This is card.tsx's ONLY edit
    this phase (its `rounded-xl` becomes token-driven purely via 28-01's web.ts config — no radius edit
    here; UI-SPEC "zero className change" note).
    In composer.tsx line 76, change `shadow-sm` to `shadow-elevation-2` — result:
    `"w-full shrink-0 border-t border-border/60 bg-background shadow-elevation-2"`. elevation-2's ambient
    layers read as a soft rise ABOVE the composer's `border-t` (dock effect) since nothing renders below
    it — no negative-offset token needed.
  </action>
  <verify>
    <automated>grep -F 'shadow-elevation-1' packages/ui/src/card.tsx && grep -F 'shadow-elevation-2' apps/web/src/app/chat/_components/composer.tsx && ! grep -F 'shadow-sm' apps/web/src/app/chat/_components/composer.tsx</automated>
  </verify>
  <done>card.tsx uses shadow-elevation-1; composer.tsx uses shadow-elevation-2 with no residual shadow-sm; all other classes on both lines preserved.</done>
</task>

<task type="auto">
  <name>Task 2: canvas node shells — resting/selected elevation + genui panel entrance</name>
  <read_first>
    - apps/web/src/app/chat/_canvas/chat-node.tsx (line 150, the ChatNode shell div; note the memo wrapper)
    - apps/web/src/app/chat/_canvas/genui-panel-node.tsx (line 157, the GenuiPanelNode shell div — the
      top-level returned element, NOT GenuiPanelNodeBody which is memo'd below it)
    - 28-UI-SPEC.md → "TOKEN-03 … Named consumers" rows 3-4 + "TOKEN-05 (a) Genui panel mount" (the
      full final className on its line 494)
  </read_first>
  <action>
    chat-node.tsx line 150: drop the inline `shadow-sm`, keep `transition-shadow duration-150`, and
    replace the selection ternary so resting adds `shadow-elevation-1` and selected adds
    `${SELECTED_RING} shadow-elevation-2`. PRESERVE the FIX-04 `border-l-2 border-l-primary` classes.
    Final: `flex h-full min-h-[320px] w-full min-w-[400px] flex-col overflow-hidden rounded-lg border
    border-border/60 border-l-2 border-l-primary bg-background transition-shadow duration-150 ${selected
    ? `${SELECTED_RING} shadow-elevation-2` : "shadow-elevation-1"}`.
    genui-panel-node.tsx line 157: same resting/selected elevation swap AND append the TOKEN-05 entrance
    classes to the static portion. Final (verbatim from UI-SPEC line 494): `flex h-full min-h-[240px]
    w-full min-w-[320px] flex-col overflow-hidden rounded-lg border border-border/60 bg-background
    transition-shadow duration-150 animate-in fade-in-0 zoom-in-95 duration-[250ms]
    motion-reduce:animate-none ${selected ? `${SELECTED_RING} shadow-elevation-2` : "shadow-elevation-1"}`.
    Apply the entrance to the OUTER shell div ONLY — never inside GenuiPanelNodeBody / GenuiPartBoundary
    / InteractiveWidgetBoundary / SpecRenderer (hard constraint). Do NOT add a GeneratingRing or `.t-*`
    class to either node — one animation per reveal (the a08cb6c "no stacking" precedent).
  </action>
  <verify>
    <automated>grep -F 'shadow-elevation-2' apps/web/src/app/chat/_canvas/chat-node.tsx && grep -F 'border-l-2 border-l-primary' apps/web/src/app/chat/_canvas/chat-node.tsx && grep -F 'animate-in fade-in-0 zoom-in-95 duration-[250ms] motion-reduce:animate-none' apps/web/src/app/chat/_canvas/genui-panel-node.tsx</automated>
    <automated>npm --prefix apps/web run typecheck</automated>
    <human-check>With the dev server up, drop a genui panel on the canvas and confirm it fades+zooms in once on mount (not on drag/stream/select), select a node and confirm the shadow lifts, and set OS reduced-motion to confirm the entrance is cancelled.</human-check>
  </verify>
  <done>chat-node resting=elevation-1/selected=elevation-2 with border-l-2 border-l-primary preserved; genui-panel node carries both the elevation swap and the exact entrance-class string on its outer shell; no `.t-*`/GeneratingRing added; apps/web typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | Pure `className` string swaps on already-rendered elements. No user input, network, markup, or data flow changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Denial of Service (accessibility) | genui-panel-node entrance animation | mitigate | Risk: an entrance that ignores `prefers-reduced-motion` harms vestibular users. `motion-reduce:animate-none` is required in the class string and grep-asserted (Task 2); Tailwind's variant fully cancels the keyframe. |
| T-28-02-02 | Tampering | genui-panel-node shell placement | mitigate | Risk: applying animation deeper than the outer shell would re-trigger mid-stream/mid-drag or touch the locked renderer. Constraint pins the class to the outer shell only; the memo'd Body keeps re-renders from remounting the DOM node, so the entrance never replays. |
| T-28-02-03 | Denial of Service | shadow-elevation-* utility resolution | mitigate | Depends on 28-01 having registered the utilities; `npm --prefix apps/web run typecheck` + the grep gates confirm the classes are present and the app compiles. |
| T-28-02-SC | Tampering (supply chain) | npm installs | accept | Zero new dependencies; only consumption of the pre-vetted tailwindcss-animate plugin. No install task. |
</threat_model>

<verification>
- grep gates confirm the elevation classes, preserved FIX-04 stripe, and the exact entrance-class string.
- `npm --prefix apps/web run typecheck` green.
- Visual (human-check): mount entrance fires once, selection lifts, reduced-motion cancels.
</verification>

<success_criteria>
Maps to Phase 28 success criteria 3 ("visibly applied") and 4 (genui panel mount animates in):
- card / composer / both canvas nodes visibly consume the elevation scale.
- genui panel node mount animates in via tailwindcss-animate, motion-reduce-safe, outer-shell only.
</success_criteria>

<output>
Create `.planning/phases/28-design-system-token-upgrades/28-02-SUMMARY.md` when done.
</output>
