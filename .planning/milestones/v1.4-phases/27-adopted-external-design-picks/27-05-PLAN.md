---
phase: 27-adopted-external-design-picks
plan: 05
type: execute
wave: 2
depends_on: [27-03]
files_modified:
  - apps/web/src/app/chat/_components/delete-conversation-dialog.tsx
  - apps/web/src/app/chat/_components/conversation-rail.tsx
  - apps/web/src/app/chat/_components/model-picker.tsx
  - packages/ui/src/command.tsx
autonomous: true
requirements: [ADOPT-05]
tags: [react, transitions, chat, command, fix-02-spillover]

must_haves:
  truths:
    - "The delete-conversation dialog's inner content mounts with .t-modal-reveal (new inner wrapper, not an AlertDialogContent className override)"
    - "The conversation rail's collapse uses .t-panel-reveal (replacing its ad hoc motion-safe:transition-[width] pair)"
    - "The model-picker dropdown's Command wrapper mounts with .t-dropdown-reveal (new inner wrapper inside PopoverContent)"
    - "No copy, border, or padding changes beyond the transition wrapper at any of the 3 consumers"
    - "packages/ui/src/command.tsx CommandGroup group-heading uses font-semibold + on-grid py (no font-medium / py-1.5) — FIX-02 spillover"
  artifacts:
    - path: "apps/web/src/app/chat/_components/delete-conversation-dialog.tsx"
      provides: "t-modal-reveal on the dialog content"
      contains: "t-modal-reveal"
    - path: "apps/web/src/app/chat/_components/conversation-rail.tsx"
      provides: "t-panel-reveal on the collapse outer div"
      contains: "t-panel-reveal"
    - path: "apps/web/src/app/chat/_components/model-picker.tsx"
      provides: "t-dropdown-reveal on the Command wrapper"
      contains: "t-dropdown-reveal"
    - path: "packages/ui/src/command.tsx"
      provides: "CommandGroup group-heading register fixed to font-semibold + on-grid py"
      contains: "font-semibold"
  key_links:
    - from: "the 3 chat consumers"
      to: ".t-modal-reveal / .t-panel-reveal / .t-dropdown-reveal (globals.css, Plan 03)"
      via: "className on a new inner wrapper (or the rail's own div)"
      pattern: "t-(modal|panel|dropdown)-reveal"
---

<objective>
Land the ADOPT-05 wiring: attach each of Plan 03's 3 transition utilities to its single designated
consumer — `.t-modal-reveal` on the delete-conversation dialog content, `.t-panel-reveal` on the
conversation-rail collapse, `.t-dropdown-reveal` on the model-picker dropdown — so the snippets are
observable, not dead CSS. Also fold in the Phase-26 FIX-02 spillover: fix
`packages/ui/src/command.tsx`'s `CommandGroup` group-heading (baked `font-medium` + `py-1.5`, live in
ModelPicker today) to `font-semibold` + an on-grid `py`.

Purpose: make the transition utilities visible at real UI moments and close the FIX-02 typography
escape that lives in `packages/ui` (outside Phase 26's app-scoped grep).
Output: 3 minimal consumer edits + 1 shared-component typography fix. Depends on Plan 03 (`.t-*` CSS).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-adopted-external-design-picks/27-UI-SPEC.md
@.planning/phases/27-adopted-external-design-picks/27-CONTEXT.md
@apps/web/src/app/chat/_components/delete-conversation-dialog.tsx
@apps/web/src/app/chat/_components/conversation-rail.tsx
@apps/web/src/app/chat/_components/model-picker.tsx
@packages/ui/src/command.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the 3 transition utilities to their designated consumers</name>
  <files>apps/web/src/app/chat/_components/delete-conversation-dialog.tsx, apps/web/src/app/chat/_components/conversation-rail.tsx, apps/web/src/app/chat/_components/model-picker.tsx</files>
  <read_first>
    - 27-UI-SPEC.md section "ADOPT-05" -> the "Wiring" table (exact consumer + exact wiring for each snippet) and the "Do not restyle beyond the transition itself" rule + the inner-wrapper rationale (tailwind-merge does not dedupe tailwindcss-animate custom names, so override on shared primitives risks two animation systems fighting)
    - apps/web/src/app/chat/_components/delete-conversation-dialog.tsx lines 40-59 (the AlertDialogHeader..AlertDialogFooter content to wrap in a NEW `<div className="t-modal-reveal">`)
    - apps/web/src/app/chat/_components/conversation-rail.tsx lines 109-115 (the outer collapse div carrying `motion-safe:transition-[width] motion-safe:duration-200 motion-safe:ease-in-out` to REPLACE with `t-panel-reveal`)
    - apps/web/src/app/chat/_components/model-picker.tsx lines 118-155 (the `<PopoverContent>` > `<Command>` to wrap in a NEW `<div className="t-dropdown-reveal">`)
  </read_first>
  <action>
    MODAL: in `delete-conversation-dialog.tsx`, wrap the existing `<AlertDialogHeader>…</AlertDialogFooter>`
    children in a NEW `<div className="t-modal-reveal">…</div>` INSIDE `<AlertDialogContent>` — do NOT
    add `t-modal-reveal` to `AlertDialogContent`'s own className (that shared primitive already carries
    tailwindcss-animate `data-[state=open]:animate-in` treatment; the inner wrapper avoids two animation
    systems colliding). PANEL: in `conversation-rail.tsx`, on the SAME outer collapse div (lines
    109-115), REPLACE the three `motion-safe:transition-[width] motion-safe:duration-200
    motion-safe:ease-in-out` classes with the single class `t-panel-reveal` (this is the app's own
    bespoke div — full control, replace in place; keep the `collapsed ? "w-0" : "w-[280px]"` and all
    other classes untouched). DROPDOWN: in `model-picker.tsx`, wrap the existing `<Command>` (inside
    `<PopoverContent>`) in a NEW `<div className="t-dropdown-reveal">…</div>` — do NOT override
    `PopoverContent`'s className (same inner-wrapper rationale). Change NOTHING else at any consumer —
    no copy, no border, no padding, no handler changes (per CONTEXT.md "do not restyle beyond the
    transition itself").
  </action>
  <acceptance_criteria>
    - `grep -q 't-modal-reveal' apps/web/src/app/chat/_components/delete-conversation-dialog.tsx`
    - `grep -q 't-panel-reveal' apps/web/src/app/chat/_components/conversation-rail.tsx` AND `! grep -q 'motion-safe:transition-\[width\]' apps/web/src/app/chat/_components/conversation-rail.tsx` (old ad hoc pair removed)
    - `grep -q 't-dropdown-reveal' apps/web/src/app/chat/_components/model-picker.tsx`
    - copy unchanged: `grep -q 'Delete this conversation?' apps/web/src/app/chat/_components/delete-conversation-dialog.tsx` AND `grep -q 'Keep conversation' apps/web/src/app/chat/_components/delete-conversation-dialog.tsx`
    - `! grep -rq 'font-medium' apps/web/src/app/chat` (no font-medium regression in chat)
    - `npm run typecheck --workspace=@nauta/web` exits 0
    - `npx vitest run --root apps/web` exits 0 (chat suites — no regression)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 't-modal-reveal' apps/web/src/app/chat/_components/delete-conversation-dialog.tsx && grep -q 't-panel-reveal' apps/web/src/app/chat/_components/conversation-rail.tsx && grep -q 't-dropdown-reveal' apps/web/src/app/chat/_components/model-picker.tsx && npm run typecheck --workspace=@nauta/web && npx vitest run --root apps/web</automated>
  </verify>
  <done>Each of the 3 transition utilities is attached to exactly its designated consumer via a new inner wrapper (or the rail's own div); no copy/border/padding changed; chat suite + typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix command.tsx CommandGroup group-heading typography (FIX-02 spillover)</name>
  <files>packages/ui/src/command.tsx</files>
  <read_first>
    - packages/ui/src/command.tsx lines 85-99 (the `CommandGroup` `[&_[cmdk-group-heading]]:py-1.5` + `[&_[cmdk-group-heading]]:font-medium` selector — the live-in-ModelPicker violation to fix)
    - 27-UI-SPEC.md section "Design System" scout finding (lines 61-66) + "Registry Safety" deferred note (562-564) — documents this exact spillover
    - Phase 27 planning brief extra_task_requirement (FIX-02 spillover: font-medium -> font-semibold, py-1.5 -> py-1 or py-2)
  </read_first>
  <action>
    In `packages/ui/src/command.tsx`, edit ONLY the `CommandGroup` className (line ~92): change
    `[&_[cmdk-group-heading]]:font-medium` to `[&_[cmdk-group-heading]]:font-semibold` (heading
    register, the app's 2-weight contract) and `[&_[cmdk-group-heading]]:py-1.5` to
    `[&_[cmdk-group-heading]]:py-1` (4px, on the 4-point grid; `py-2` = 8px is also acceptable — pick
    `py-1` to keep the compact section-label register). Change NOTHING else in this file — leave the
    separate `CommandDialog` `font-medium` (line ~32) alone: it belongs to the command-palette surface,
    is NOT rendered in /chat or /studio, and is outside this task's scope (note it in the SUMMARY as a
    still-open, out-of-scope occurrence). This is the FIX-02 escape the Phase-26 grep (scoped to
    app/chat + app/studio) missed because the violation lives in `packages/ui`.
  </action>
  <acceptance_criteria>
    - `grep -q '\[&_\[cmdk-group-heading\]\]:font-semibold' packages/ui/src/command.tsx` (heading now semibold)
    - `! grep -q '\[&_\[cmdk-group-heading\]\]:py-1.5' packages/ui/src/command.tsx` (off-grid py-1.5 removed)
    - `grep -qE '\[&_\[cmdk-group-heading\]\]:py-(1|2)\b' packages/ui/src/command.tsx` (on-grid py)
    - the CommandGroup block no longer contains `[&_[cmdk-group-heading]]:font-medium`
    - `npm run typecheck --workspace=@nauta/ui` exits 0 AND `npm run typecheck --workspace=@nauta/web` exits 0
    - `npx vitest run --root apps/web` exits 0 (ModelPicker path — chat vitest suites, no regression)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'cmdk-group-heading\]\]:font-semibold' packages/ui/src/command.tsx && ! grep -q 'cmdk-group-heading\]\]:py-1.5' packages/ui/src/command.tsx && npm run typecheck --workspace=@nauta/ui && npm run typecheck --workspace=@nauta/web && npx vitest run --root apps/web</automated>
  </verify>
  <done>CommandGroup group-heading is font-semibold + on-grid py (py-1); py-1.5/font-medium gone from that selector; ui + web typecheck and chat vitest green; CommandDialog occurrence noted as out-of-scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new inner wrapper → shared @nauta/ui primitive | transition class attaches to a NEW element at the call site, never overrides the shared primitive's own tailwindcss-animate className |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | Tampering | AlertDialogContent / PopoverContent (shared @nauta/ui, tailwindcss-animate) | mitigate | attach `.t-*` to a NEW inner wrapper, never to the shared primitive's className (tailwind-merge does not dedupe animate-* custom names — override would let two animation systems fight); acceptance asserts inner-wrapper wiring |
| T-27-05-02 | Tampering | copy/layout of the 3 consumers | mitigate | change only the transition wrapper — acceptance greps assert the delete-dialog copy strings are unchanged and no font-medium enters chat |
| T-27-05-SC | Tampering | source provenance | accept | no external fetch in this plan — the `.t-*` CSS was fetched + vetted in Plan 03; this plan only wires existing utilities and fixes an in-repo typography escape |
</threat_model>

<verification>
- `git diff --name-only` limited to the 4 declared files.
- Each `.t-*` utility appears in exactly its one designated consumer; the rail's old motion-safe pair is gone.
- command.tsx CommandGroup heading is font-semibold + on-grid py; ui + web typecheck green; chat vitest green.
</verification>

<success_criteria>
- ROADMAP Phase 27 success criterion 5 (3-4 retokenized transitions.dev snippets visibly used at their UI moments) — MET (CSS from Plan 03, wiring here).
- FIX-02 spillover closed in packages/ui/src/command.tsx (extra task requirement).
</success_criteria>

<output>
Create `.planning/phases/27-adopted-external-design-picks/27-05-SUMMARY.md` when done — noting the still-open, out-of-scope CommandDialog font-medium occurrence.
</output>
