---
phase: 27-adopted-external-design-picks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/file-tree.tsx
  - apps/web/src/components/file-tree.test.tsx
  - apps/web/src/app/studio/_components/code-sandbox-island.tsx
autonomous: true
requirements: [ADOPT-02]
tags: [react, radix-accordion, lucide-react, studio, file-tree]

must_haves:
  truths:
    - "The Code-Island tab renders a FileTree of the 4 existing PRESETS, each as a folder containing one island.js leaf"
    - "Selecting an island.js leaf calls the same handlePreset(presetId) the old <Select> wired — never auto-runs; 'Run preset' stays the separate manual trigger (D-06)"
    - "FileTree is built directly on raw @radix-ui/react-accordion (NOT @nauta/ui/accordion), carries zero font-medium, and adds zero npm dependencies"
  artifacts:
    - path: "apps/web/src/components/file-tree.tsx"
      provides: "Ported Magic UI FileTree — data-driven API on raw AccordionPrimitive + lucide icons"
      exports: ["FileTree", "FileTreeNode", "FileTreeProps"]
      min_lines: 70
      contains: "@radix-ui/react-accordion"
    - path: "apps/web/src/components/file-tree.test.tsx"
      provides: "Colocated vitest coverage (render, select-callback, selected-row treatment)"
    - path: "apps/web/src/app/studio/_components/code-sandbox-island.tsx"
      provides: "FileTree mounted as the preset browser (replaces the preset <Select>)"
      contains: "FileTree"
  key_links:
    - from: "apps/web/src/app/studio/_components/code-sandbox-island.tsx"
      to: "handlePreset"
      via: "FileTree onSelect callback"
      pattern: "onSelect=\\{"
    - from: "apps/web/src/components/file-tree.tsx"
      to: "@radix-ui/react-accordion"
      via: "AccordionPrimitive import (raw primitive, not the @nauta/ui wrapper)"
      pattern: "react-accordion"
---

<objective>
Land ADOPT-02: hand-port Magic UI's `file-tree` into `apps/web/src/components/file-tree.tsx` (a
trimmed, data-driven API on the RAW `@radix-ui/react-accordion` primitive + `lucide-react`, zero new
deps, attribution header) and MOUNT it (Plan A) in the Code-Island tab's `code-sandbox-island.tsx` as
the preset/fixture browser — replacing the "Or try a preset" `<Select>` with a FileTree that renders
the 4 existing PRESETS as folders, each holding one `island.js` leaf.

Purpose: the fixtures ARE a real file-shaped structure today; when multi-file islands arrive (v1.5+)
the primitive is already in place. FileTree ships MOUNTED and VISIBLE, never dead code.
Output: 2 new files + 1 mount edit. No token/CSS changes; no shared primitive touched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-adopted-external-design-picks/27-UI-SPEC.md
@.planning/phases/27-adopted-external-design-picks/27-CONTEXT.md
@apps/web/src/app/studio/_components/code-sandbox-island.tsx
@apps/web/src/app/chat/_components/conversation-row.tsx

<interfaces>
<!-- The trimmed FileTree contract to build (from 27-UI-SPEC.md § ADOPT-02). Executor implements exactly this surface — no arbitrary JSX-composition API. -->

FileTreeNode: { readonly id: string; readonly name: string; readonly type: "folder" | "file"; readonly children?: readonly FileTreeNode[] }
FileTreeProps: { readonly data: readonly FileTreeNode[]; readonly selectedId?: string; readonly onSelect?: (node: FileTreeNode) => void; readonly defaultExpandedIds?: readonly string[] }
export function FileTree(props: FileTreeProps): React.ReactElement

<!-- Selected-file-row treatment is a VERBATIM reuse of conversation-row.tsx's isActive recipe: `bg-primary/10 text-primary`. cn() comes from "@nauta/ui". -->
<!-- Colocated test convention: mirror apps/web/src/app/studio/_components/json-pane.test.tsx (createRoot + act + IS_REACT_ACT_ENVIRONMENT). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port Magic UI file-tree to a raw-Radix, data-driven FileTree (+ colocated test)</name>
  <files>apps/web/src/components/file-tree.tsx, apps/web/src/components/file-tree.test.tsx</files>
  <read_first>
    - 27-UI-SPEC.md § "ADOPT-02 — FileTree" (the full Visual contract class table, the trimmed API, the "build on AccordionPrimitive NOT @nauta/ui/accordion" constraint, indentation lookup, accessibility)
    - packages/ui/src/accordion.tsx lines 25-40 (the baked-in `font-medium` on AccordionTrigger — the exact violation FileTree must NOT inherit by using the raw primitive instead)
    - apps/web/src/app/chat/_components/conversation-row.tsx (the `bg-primary/10 text-primary` isActive recipe reused verbatim for the selected-file row)
    - apps/web/src/app/studio/_components/json-pane.test.tsx (colocated createRoot+act vitest convention)
  </read_first>
  <behavior>
    - Renders top-level folder nodes as AccordionPrimitive items; a folder's `children` render nested one indent step deeper (depth 0 `pl-2`, depth 1 `pl-6`, depth >=2 clamped to `pl-6`)
    - Folder rows are AccordionPrimitive.Trigger buttons (Radix supplies aria-expanded); chevron rotates via `data-[state=open]:rotate-90`; Folder icon swaps to FolderOpen on open
    - File rows are native <button>; clicking one calls `onSelect(node)` with the file node
    - The row whose id === `selectedId` gets `bg-primary/10 text-primary`; unselected rows get `hover:bg-muted`
    - Every row is `min-h-11`, `font-normal` (never font-medium), `text-sm`
  </behavior>
  <action>
    Fetch Magic UI's `file-tree` source at execution time via `gh`/`curl` from `magicuidesign/magicui`
    (MIT) — look under the registry/components path (e.g. `registry/magicui/file-tree.tsx`). Re-verify
    the MIT license and CONTENT-REVIEW the fetched source (confirm no `fetch`/`eval`/`process.env`/
    dynamic-import — a file-tree port should be pure presentational). Create
    `apps/web/src/components/file-tree.tsx` with a mandatory attribution header (Magic UI,
    `magicuidesign/magicui`, MIT, "Fetched: 2026-07-06"). Implement the trimmed data-driven API from
    the `<interfaces>` block — do NOT reproduce Magic UI's compound `Tree`/`Folder`/`File`/
    `CollapseButton` surface. Import `* as AccordionPrimitive from "@radix-ui/react-accordion"`
    directly (it resolves from the workspace-hoisted install; this is exactly how the repo already
    imports `lucide-react` directly). Do NOT import `Accordion`/`AccordionTrigger` from `@nauta/ui` —
    that wrapper bakes `font-medium`. Icons `Folder`/`FolderOpen`/`FileCode2`/`ChevronRight` from
    `lucide-react`, all `size-4 shrink-0 text-muted-foreground`. Apply the exact class strings from the
    UI-SPEC Visual contract table (row `flex min-h-11 items-center gap-2 rounded-md px-2 text-sm
    transition-colors`; children container `border-l border-border/40 ml-4`; selected file row
    `bg-primary/10 text-primary`). Use `cn` from `@nauta/ui`. Trim any Magic UI prop with no consumer
    here (Claude's Discretion). Write colocated `file-tree.test.tsx` mirroring json-pane.test.tsx's
    createRoot+act convention: cover (a) folders+files render from data, (b) clicking a file leaf fires
    `onSelect` with the node, (c) the `selectedId` row carries `text-primary`, (d) no rendered row
    className contains `font-medium`. FALLBACK: if `magicuidesign/magicui` is unreachable at fetch,
    SKIP this plan, record the deviation in the SUMMARY, and do NOT substitute a different library.
  </action>
  <acceptance_criteria>
    - `grep -q 'from "@radix-ui/react-accordion"' apps/web/src/components/file-tree.tsx` (built on raw primitive)
    - `! grep -q '@nauta/ui/accordion' apps/web/src/components/file-tree.tsx` (wrapper NOT used)
    - `! grep -q 'font-medium' apps/web/src/components/file-tree.tsx` (zero font-medium — 2-weight contract)
    - `grep -q 'bg-primary/10 text-primary' apps/web/src/components/file-tree.tsx` (selected-row recipe reused verbatim)
    - `head -20 apps/web/src/components/file-tree.tsx | grep -iqE "magic ?ui|magicuidesign|MIT"` (attribution header present)
    - `grep -qE 'export (function|interface|type) (FileTree|FileTreeNode|FileTreeProps)' apps/web/src/components/file-tree.tsx` (contract exported)
    - `npx vitest run --root apps/web file-tree` exits 0
    - `npm run typecheck --workspace=@nauta/web` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --root apps/web file-tree && npm run typecheck --workspace=@nauta/web</automated>
  </verify>
  <done>file-tree.tsx exports the trimmed API on raw @radix-ui/react-accordion with an attribution header, zero font-medium, and passing colocated tests; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Mount FileTree as the preset browser in code-sandbox-island.tsx (Plan A)</name>
  <files>apps/web/src/app/studio/_components/code-sandbox-island.tsx</files>
  <read_first>
    - apps/web/src/app/studio/_components/code-sandbox-island.tsx lines 62-67 (the PRESETS array — its ids/labels become FileTree folders) and lines 169-190 (the "Or try a preset" `<Select>` + "Run preset" Button being replaced/kept) and 129-139 (`handlePreset`/`handleRunPreset`)
    - 27-UI-SPEC.md § "ADOPT-02 — FileTree" → "Mount — Plan A" (the exact tree shape: 4 preset folders each with one island.js leaf; leaf select calls handlePreset; Run preset unchanged) and "Mount — Plan B" (the read-only fallback to use ONLY if Plan A regresses UX)
    - 27-CONTEXT.md "ADOPT-02 file-tree — consumer gap resolved" decision
  </read_first>
  <action>
    In `code-sandbox-island.tsx`, build a `readonly FileTreeNode[]` from the existing `PRESETS` array:
    one folder per preset (`id`/`name` = preset id/label, `type:"folder"`) each containing a single
    child file node (`id` = `${preset.id}/island.js`, `name:"island.js"`, `type:"file"`). REPLACE the
    "Or try a preset" `<Select>` block (the `<label>…<Select>…</Select></label>` around lines 170-185)
    with `<FileTree data={…} selectedId={…} onSelect={…} />`. Wire `onSelect` so selecting an
    `island.js` leaf resolves its parent preset id and calls the SAME `handlePreset(presetId)` the
    `<Select>` used (derive presetId from the leaf id prefix or the parent folder). Track the selected
    leaf id in local state for `selectedId` (initialise to the current `presetId`'s island.js). LEAVE
    the separate "Run preset" `<Button>` (lines 186-189) and `handleRunPreset` UNCHANGED — selecting a
    file never auto-runs (D-06 manual-trigger-only). Remove the now-unused `Select`/`SelectContent`/
    `SelectItem`/`SelectTrigger`/`SelectValue` imports (lines 23-29) if nothing else uses them; add the
    `FileTree` import from `~/components/file-tree`. Do NOT touch the LIVE "Generate from intent" block,
    the editable code Textarea, or the CodeIslandFrame render. If, at execution time, the leaf-picker
    replacement genuinely reads as worse UX than the current control, instead use Plan B from the
    UI-SPEC (mount FileTree read-only/additively showing the active island's island.js + sandbox.html,
    leaving the `<Select>`/Run flow intact) and note the choice in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -q '<FileTree' apps/web/src/app/studio/_components/code-sandbox-island.tsx` (mounted, visible — not dead code)
    - `grep -q 'handlePreset\|handleRunPreset' apps/web/src/app/studio/_components/code-sandbox-island.tsx` (Plan A: handlePreset still wired; Run preset trigger kept)
    - `! grep -rq 'font-medium' apps/web/src/app/studio` (Studio font-medium count still 0 — no regression)
    - `grep -c '"@radix-ui/react-accordion"\|"lucide-react"' apps/web/src/components/file-tree.tsx` >= 1 AND no new entry added to any package.json dependencies (zero new deps): `git diff --name-only | grep -q 'package.json' && exit 1 || true`
    - `npm run typecheck --workspace=@nauta/web` exits 0
    - `npx vitest run --root apps/web` exits 0 (full web suite — no studio regression)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '<FileTree' apps/web/src/app/studio/_components/code-sandbox-island.tsx && npm run typecheck --workspace=@nauta/web && npx vitest run --root apps/web</automated>
  </verify>
  <done>The Code-Island tab shows FileTree (4 preset folders → island.js leaves); leaf-select calls handlePreset; Run preset unchanged; Studio font-medium stays 0; no package.json dependency added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| magicuidesign/magicui → apps/web | executable TSX component source fetched and hand-ported into the app |
| FileTree props → render | typed `FileTreeNode[]` data crosses into presentational render (no user free-text executed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-SC | Tampering | Magic UI file-tree fetch (curl/gh) | mitigate | fetch from canonical `magicuidesign/magicui` only; re-verify MIT; content-review the fetched source for `fetch`/`eval`/`process.env`/dynamic-import (none expected in a presentational port); attribution header on file-tree.tsx; NO npm install (zero new deps → no install-time scripts); record vetting outcome ("fetched + reviewed — no flags — 2026-07-06") in SUMMARY |
| T-27-06 | Tampering | 2-weight typography contract | mitigate | build on raw `@radix-ui/react-accordion` so the wrapper's baked `font-medium` is never inherited; acceptance grep asserts zero `font-medium` in file-tree.tsx and Studio |
| T-27-07 | Information Disclosure | FileTree render | accept | renders from a typed data prop only; no `dangerouslySetInnerHTML`/`eval`; leaf names are static technical labels |
</threat_model>

<verification>
- FileTree file exists with MIT attribution, exports the trimmed contract, imports the raw accordion primitive, contains zero `font-medium`.
- code-sandbox-island.tsx renders `<FileTree>`; handlePreset still wired; Run preset trigger intact.
- `git diff --name-only` shows no `package.json` change (zero new deps); Studio font-medium count still 0.
</verification>

<success_criteria>
- ROADMAP Phase 27 success criterion 2 (code-island file browser renders a ported Magic UI file-tree from already-installed @radix-ui/react-accordion + lucide-react, zero new deps) — MET.
</success_criteria>

<output>
Create `.planning/phases/27-adopted-external-design-picks/27-02-SUMMARY.md` when done — including the Magic UI per-source vetting evidence line and whether Plan A or Plan B mount was used.
</output>
