---
phase: 27-adopted-external-design-picks
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/globals.css
autonomous: true
requirements: [ADOPT-03, ADOPT-05]
tags: [css, tailwind, globals, motion, generating-ring, transitions]

must_haves:
  truths:
    - "globals.css defines a .generating-ring @layer-utilities technique — a background-position sweep, teal-only via hsl(var(--primary)), animated ONLY under @media (prefers-reduced-motion: no-preference)"
    - "globals.css defines .t-modal-reveal, .t-panel-reveal, .t-dropdown-reveal, all disabled under @media (prefers-reduced-motion: reduce)"
    - "No token VALUE is added, changed, or removed — the --token declaration count stays exactly 55"
  artifacts:
    - path: "apps/web/src/app/globals.css"
      provides: "Additive @layer utilities: .generating-ring (ADOPT-03) + 3 transition recipes (ADOPT-05)"
      contains: "generating-ring-sweep"
  key_links:
    - from: "globals.css .generating-ring::before"
      to: "--primary token"
      via: "hsl(var(--primary)) sweep gradient (teal-only)"
      pattern: "hsl\\(var\\(--primary\\)"
    - from: "globals.css reduced-motion queries"
      to: ".generating-ring / .t-* animations"
      via: "prefers-reduced-motion gating"
      pattern: "prefers-reduced-motion"
---

<objective>
Land the additive CSS for ADOPT-03 and ADOPT-05 in the single file that owns it: append to
`apps/web/src/app/globals.css` (after Phase 26's `.scrollbar-token` block) the `.generating-ring`
utility (hand-ported CSS TECHNIQUE from Magic UI's shine-border + animated-shiny-text — a pure
`background-position` sweep, zero JS, teal-only) and the 3 transitions.dev recipes (`.t-modal-reveal`,
`.t-panel-reveal`, `.t-dropdown-reveal`), retokenized to the app's existing color custom properties.

Purpose: globals.css is a shared CSS sink for both picks; concentrating ALL additive CSS in this one
plan keeps `files_modified` disjoint from the component/wiring plans (04, 05) so ordering is free.
Output: additive `@layer utilities` + `@keyframes` only — no token VALUE touched (count stays 55).
This plan is a wave-1 dependency of Plans 04 (consumes `.generating-ring`) and 05 (consumes `.t-*`).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-adopted-external-design-picks/27-UI-SPEC.md
@.planning/phases/27-adopted-external-design-picks/27-CONTEXT.md
@apps/web/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the .generating-ring CSS technique (ADOPT-03)</name>
  <files>apps/web/src/app/globals.css</files>
  <read_first>
    - 27-UI-SPEC.md section "ADOPT-03 — GeneratingRing" -> the CSS technique block (literal `.generating-ring` + `.generating-ring::before`, the `generating-ring-sweep` keyframes, the `@media (prefers-reduced-motion: no-preference)` gating, ring geometry inset:-2px/padding:2px, mask-composite exclude, the attribution-comment text)
    - apps/web/src/app/globals.css lines 108-131 (Phase 26 `.scrollbar-token` `@layer utilities` block — append AFTER it, same additive pattern)
    - 27-CONTEXT.md "ADOPT-03 GeneratingRing" decision (teal-only via --primary, motion-safe/reduced-motion gating, additive @layer)
  </read_first>
  <action>
    Fetch Magic UI's `shine-border` + `animated-shiny-text` source at execution time via `gh`/`curl`
    from `magicuidesign/magicui` (MIT) to confirm the TECHNIQUE (a moving `background-position` linear
    gradient) — porting the technique, NOT the components' JS APIs. Re-verify MIT and content-review
    (CSS-only port — confirm no `<script>`/JS orchestration copied). Append to
    `apps/web/src/app/globals.css`, after the `.scrollbar-token` block, a NEW `@layer utilities` block
    with exactly the UI-SPEC `.generating-ring` technique: `.generating-ring` (position:relative;
    isolation:isolate); `.generating-ring::before` (inset:-2px; padding:2px; border-radius:inherit; a
    teal `linear-gradient` from `hsl(var(--primary))` / `hsl(var(--primary) / 0.85)` / transparent
    stops; background-size 250% 100%; the `-webkit-mask`/`mask-composite: exclude` ring mask;
    pointer-events:none); `@keyframes generating-ring-sweep` (0% to 200% background-position); and the
    `@media (prefers-reduced-motion: no-preference)` rule applying `generating-ring-sweep 2000ms linear
    infinite` to `.generating-ring::before`. Lead with the UI-SPEC attribution comment (Magic UI
    shine-border + animated-shiny-text, MIT, technique-not-API note, and the reduced-motion rationale).
    Use ONLY existing color tokens via `hsl(var(--x))`; add NO new `--token: value` and NO named/hex
    palette color (the `#000` mask literals are the standard mask technique and are allowed). FALLBACK:
    if `magicuidesign/magicui` is unreachable, SKIP this task, record the deviation in the SUMMARY, do
    NOT substitute a different source.
  </action>
  <acceptance_criteria>
    - `grep -q 'generating-ring-sweep' apps/web/src/app/globals.css` (technique + keyframes present)
    - `grep -q 'hsl(var(--primary))' apps/web/src/app/globals.css` (teal-only sweep)
    - `grep -q 'prefers-reduced-motion: no-preference' apps/web/src/app/globals.css` (ring motion gated)
    - `grep -iqE 'shine-border|animated-shiny' apps/web/src/app/globals.css` (attribution comment present)
    - token count unchanged == 55 (see verify)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'generating-ring-sweep' apps/web/src/app/globals.css && test $(grep -cE '^\s*--[a-zA-Z-]+:\s' apps/web/src/app/globals.css) -eq 55</automated>
  </verify>
  <done>globals.css carries the additive `.generating-ring` technique (teal-only, motion-safe-gated, attributed); token-value count still 55.</done>
</task>

<task type="auto">
  <name>Task 2: Append the 3 transitions.dev recipes (ADOPT-05)</name>
  <files>apps/web/src/app/globals.css</files>
  <read_first>
    - 27-UI-SPEC.md section "ADOPT-05 — transitions.dev snippets" -> the CSS block (literal `.t-modal-reveal`/`t-modal-in`, `.t-panel-reveal`, `.t-dropdown-reveal`/`t-dropdown-in`, the `@media (prefers-reduced-motion: reduce)` disable rule, the attribution comment, the literal 150/250/350/400ms + 0.96/0.98 numbers)
    - 27-UI-SPEC.md section "Spacing Scale" -> Exemptions "Motion values" (why durations/scales are literal source numbers, not token/grid violations)
    - apps/web/src/app/globals.css (append AFTER Task 1's `.generating-ring` block)
  </read_first>
  <action>
    Fetch the 3 recipes via `gh`/`curl` from `Jakubantalik/transitions.dev` (MIT) — `06-modal.md`,
    `07-panel-reveal.md`, `05-menu-dropdown.md`. Re-verify MIT and content-review (CSS-ONLY — do NOT
    copy the source's JS event-handler orchestration). Append to `apps/web/src/app/globals.css`, after
    Task 1's block, a NEW `@layer utilities` block with exactly the UI-SPEC recipes: `.t-modal-reveal`
    (+ `@keyframes t-modal-in`, 250ms, opacity + scale(0.96)->1 + box-shadow using
    `hsl(var(--foreground) / 0.12)`); `.t-panel-reveal` (width 400ms + opacity 350ms transition);
    `.t-dropdown-reveal` (+ `@keyframes t-dropdown-in`, 150ms, opacity + scale(0.98)->1); and a closing
    `@media (prefers-reduced-motion: reduce)` rule setting `animation:none` on `.t-modal-reveal`/
    `.t-dropdown-reveal` and `transition:none` on `.t-panel-reveal`. Lead with the UI-SPEC attribution
    comment (Jakubantalik/transitions.dev, MIT, retokenized-color-only note). Retokenize ONLY colors
    via `hsl(var(--x))`; duration/scale stay literal source numbers (no `--duration-*` token set exists
    to retokenize into). Add NO new `--token: value`. FALLBACK: if the source is unreachable, SKIP this
    task, record the deviation in the SUMMARY, do NOT substitute a different source.
  </action>
  <acceptance_criteria>
    - all three utilities present: `.t-modal-reveal`, `.t-panel-reveal`, `.t-dropdown-reveal` in globals.css
    - `grep -q 'prefers-reduced-motion: reduce' apps/web/src/app/globals.css` (transitions disabled under reduced motion)
    - `grep -q 'hsl(var(--foreground) / 0.12)' apps/web/src/app/globals.css` (modal shadow retokenized)
    - `grep -iq 'transitions.dev' apps/web/src/app/globals.css` (attribution comment present)
    - token count STILL == 55 (see verify)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 't-modal-reveal' apps/web/src/app/globals.css && grep -q 't-panel-reveal' apps/web/src/app/globals.css && grep -q 't-dropdown-reveal' apps/web/src/app/globals.css && grep -q 'prefers-reduced-motion: reduce' apps/web/src/app/globals.css && test $(grep -cE '^\s*--[a-zA-Z-]+:\s' apps/web/src/app/globals.css) -eq 55</automated>
  </verify>
  <done>globals.css carries all 3 retokenized transition utilities (attributed, reduced-motion-disabled); token-value count still 55.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| magicuidesign/magicui + transitions.dev → globals.css | external CSS techniques fetched and hand-copied into the app stylesheet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-SC | Tampering | Magic UI + transitions.dev fetch (curl/gh) | mitigate | fetch from canonical `magicuidesign/magicui` + `Jakubantalik/transitions.dev` only; re-verify MIT for each; content-review as CSS-ONLY (no `<script>`/JS orchestration copied); attribution comments on both blocks; NO npm install (zero new deps); record per-source vetting ("fetched + reviewed — no flags — 2026-07-06") in SUMMARY |
| T-27-04 | Tampering | token layer (Phase 28 owns tokens) | mitigate | additive `@layer utilities`/`@keyframes` only; NO new `--token: value`; acceptance asserts the --token count stays 55 |
| T-27-05 | Denial of Service (a11y) | reduced-motion users | mitigate | ring animates only under `prefers-reduced-motion: no-preference`; the 3 transitions are disabled under `prefers-reduced-motion: reduce` |
</threat_model>

<verification>
- `git diff --name-only` limited to `apps/web/src/app/globals.css`.
- `grep -cE '^\s*--[a-zA-Z-]+:\s' apps/web/src/app/globals.css` == 55 (token-value count unchanged — Phase 28 owns tokens).
- SUMMARY records the per-source vetting outcome for Magic UI and transitions.dev.
</verification>

<success_criteria>
- Provides the `.generating-ring` utility consumed by Plan 04 (ROADMAP criterion 3) and the 3 `.t-*` utilities consumed by Plan 05 (ROADMAP criterion 5).
- No token VALUE changed; all additions are `@layer`-scoped and reduced-motion-safe.
</success_criteria>

<output>
Create `.planning/phases/27-adopted-external-design-picks/27-03-SUMMARY.md` when done — including the per-source vetting evidence line for Magic UI and transitions.dev.
</output>
