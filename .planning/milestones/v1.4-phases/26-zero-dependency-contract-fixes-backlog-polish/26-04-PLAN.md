---
phase: 26-zero-dependency-contract-fixes-backlog-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/chat/_canvas/chat-node.tsx
  - apps/web/src/app/chat/_canvas/genui-panel-node.tsx
  - apps/web/src/app/chat/_canvas/canvas-layout.ts
autonomous: true
requirements: [FIX-04, POLISH-02]
must_haves:
  truths:
    - "ChatNode and GenuiPanelNode are visually distinguishable at a glance: ChatNode has a teal left-edge stripe + MessageSquare header icon; GenuiPanelNode has a lighter (bg-muted/40) header + PanelsTopLeft icon"
    - "The node accent uses primary only on ChatNode's left edge + icon; GenuiPanelNode stays neutral (no second hue)"
    - "New canvas panels no longer stack in a cramped vertical column by default"
    - "Node dimensions, drag-handle selector, and selection ring are unchanged; persistence/registry layers untouched"
  artifacts:
    - path: "apps/web/src/app/chat/_canvas/chat-node.tsx"
      provides: "ChatNode left-edge primary stripe + MessageSquare header icon"
      contains: "border-l-2 border-l-primary"
    - path: "apps/web/src/app/chat/_canvas/genui-panel-node.tsx"
      provides: "GenuiPanelNode neutral tonal shift (bg-muted/40) + PanelsTopLeft icon"
      contains: "PanelsTopLeft"
    - path: "apps/web/src/app/chat/_canvas/canvas-layout.ts"
      provides: "Auto-layout tuned so sibling panels don't cram vertically"
  key_links:
    - from: "apps/web/src/app/chat/_canvas/chat-node.tsx"
      to: "accent allowlist"
      via: "border-l-2 border-l-primary + MessageSquare text-primary"
      pattern: "border-l-primary"
    - from: "apps/web/src/app/chat/_canvas/canvas-layout.ts"
      to: "dagre layout"
      via: "nodesep/ranksep on 8-pt scale"
      pattern: "nodesep|ranksep"
---

<objective>
Differentiate the two byte-identical canvas node shells (FIX-04) — ChatNode gets the teal `primary`
left-edge stripe + a `MessageSquare` header icon (the one new accent-allowlist member this phase),
GenuiPanelNode gets a neutral tonal shift (`bg-muted/40` header) + a `PanelsTopLeft` icon — and tune
the dagre auto-layout (POLISH-02) so sibling panels stop cramming into one vertical column.

Purpose: Satisfies the node-differentiation half of ROADMAP criterion 1 and the canvas-layout half
of ROADMAP criterion 5.
Output: Visually distinct node headers (differentiation in header fill + icon + one accent stripe,
never in layout math) and a breathing-room auto-layout that keeps every spacing value on the 8-pt grid.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md

<interfaces>
<!-- Current byte-identical header/shell (chat-node.tsx & genui-panel-node.tsx) -->
- header row: "node-drag-handle flex h-9 shrink-0 cursor-grab items-center gap-2 border-b border-border/60 bg-muted/60 px-3 active:cursor-grabbing"
- outer shell: "...rounded-lg border border-border/60 bg-background shadow-sm..."
- selection ring "ring-2 ring-primary ring-offset-1", min-w/min-h, drag-handle selector: UNCHANGED
<!-- canvas-layout.ts current dagre config (drift-checked 2026-07-06) -->
- rankdir: "LR" (already horizontal), ranksep: 64, nodesep: 32
- offsetCascadePosition CASCADE_STEP_PX = 32 (fallback placement for live materialization ~lines 100-144)
- node dims: chat 400x320, genui-panel 320x240 (UNCHANGED)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Differentiate ChatNode vs GenuiPanelNode header chrome (FIX-04)</name>
  <files>apps/web/src/app/chat/_canvas/chat-node.tsx, apps/web/src/app/chat/_canvas/genui-panel-node.tsx</files>
  <read_first>
    - apps/web/src/app/chat/_canvas/chat-node.tsx (line ~112 header row, ~144 outer shell, title span)
    - apps/web/src/app/chat/_canvas/genui-panel-node.tsx (line ~81 header row, ~146 outer shell, "From turn {n}" caption)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-04" (literal shell/header targets) and § "Accessibility" (icons aria-hidden)
  </read_first>
  <action>
    ChatNode (chat-node.tsx): outer shell (~144) add `border-l-2 border-l-primary` alongside the existing
    `border border-border/60` so only the left edge carries the accent — full target:
    "flex h-full min-h-[320px] w-full min-w-[400px] flex-col overflow-hidden rounded-lg border border-border/60 border-l-2 border-l-primary bg-background shadow-sm transition-shadow duration-150".
    Header (~112) keeps `bg-muted/60` (no accent FILL); prepend a lucide `MessageSquare` icon
    `className="size-3 text-primary shrink-0"` `aria-hidden` before the title span.
    GenuiPanelNode (genui-panel-node.tsx): outer shell unchanged (no left-edge accent). Header (~81)
    change `bg-muted/60` -> `bg-muted/40` (lighter step, same single gray token); prepend a lucide
    `PanelsTopLeft` icon `className="size-3 text-muted-foreground shrink-0"` `aria-hidden` before the
    "From turn {n}" caption. Do NOT change min-w/min-h, the drag-handle selector, or the selection ring.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "border-l-2 border-l-primary" apps/web/src/app/chat/_canvas/chat-node.tsx` returns >= 1
    - `grep -c "MessageSquare" apps/web/src/app/chat/_canvas/chat-node.tsx` returns >= 1
    - `grep -c "bg-muted/40" apps/web/src/app/chat/_canvas/genui-panel-node.tsx` returns >= 1
    - `grep -c "PanelsTopLeft" apps/web/src/app/chat/_canvas/genui-panel-node.tsx` returns >= 1
    - `grep -c "border-l-primary" apps/web/src/app/chat/_canvas/genui-panel-node.tsx` returns 0
    - `grep -c "aria-hidden" apps/web/src/app/chat/_canvas/chat-node.tsx` returns >= 1
    - `grep -c "min-w-\[400px\]" apps/web/src/app/chat/_canvas/chat-node.tsx` returns >= 1
    - `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>ChatNode has the primary left stripe + MessageSquare icon; GenuiPanelNode has bg-muted/40 header + PanelsTopLeft icon; both icons aria-hidden; dimensions/ring untouched; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Tune dagre auto-layout so sibling panels don't cram (POLISH-02)</name>
  <files>apps/web/src/app/chat/_canvas/canvas-layout.ts</files>
  <read_first>
    - apps/web/src/app/chat/_canvas/canvas-layout.ts (lines ~57-61 dagre config, ~100-144 offsetCascadePosition)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "POLISH-02" (8-pt spacing contract; approach is Claude's discretion)
  </read_first>
  <action>
    The cramped column is same-rank stacking (every genui-panel connected directly to the chat node
    lands in ONE rank, stacked vertically separated only by `nodesep: 32`) and/or
    `offsetCascadePosition`'s `+32,+32` cascade for live materialization. Fix at the layout-utility level:
    increase `nodesep` to the next 8-pt step (`nodesep: 64`) for vertical breathing room, and/or introduce
    a same-rank column-wrap cap (start a new visual column after N sibling panels) using ONLY 8-pt gutters
    (32px or 64px). Keep `rankdir: "LR"`, keep node dimensions (chat 400x320, genui-panel 320x240), the
    drag-handle convention, and the selection ring unchanged. If `CASCADE_STEP_PX` is tuned, it must stay
    on the 8-pt scale (32 or 64). Do NOT rewrite the persistence or node-registry layers. Every numeric
    spacing value introduced MUST be a multiple of 8 — no arbitrary pixel value.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web && npm run test -w @nauta/web -- canvas-layout</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eoc "nodesep: (32|64)" apps/web/src/app/chat/_canvas/canvas-layout.ts` returns >= 1 (nodesep stays on the 8-pt scale)
    - `grep -Ec "rankdir: \"LR\"" apps/web/src/app/chat/_canvas/canvas-layout.ts` returns >= 1 (unchanged)
    - `grep -Ec "(400|320|240)" apps/web/src/app/chat/_canvas/canvas-layout.ts` returns >= 1 (node dims unchanged)
    - No arbitrary spacing literal: `grep -Eo "(nodesep|ranksep|CASCADE_STEP_PX|STEP_PX)[: =]+[0-9]+" apps/web/src/app/chat/_canvas/canvas-layout.ts` shows only multiples of 8
    - `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>Sibling panels get horizontal/8-pt breathing room instead of one cramped vertical column; rankdir/dims/ring unchanged; every spacing value is a multiple of 8; typecheck (and any canvas-layout test) passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | Node presentational classes + pure layout-math tuning; no input, data, or auth flow; node shells wrap but never reach inside GenuiPartBoundary/InteractiveWidgetBoundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06 | Tampering | canvas-layout dagre config | accept | Numeric spacing tuning only; persistence/registry layers untouched; node dimensions unchanged so saved layouts remain valid |
| T-26-07 | Tampering | Node header class edits | accept | Presentational classes + aria-hidden decorative icons; drag-handle behavior and selection ring untouched |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (zero new deps) |
</threat_model>

<verification>
- `npm run typecheck -w @nauta/web` exits 0
- `grep -c "border-l-2 border-l-primary" apps/web/src/app/chat/_canvas/chat-node.tsx` returns >= 1
- `grep -c "PanelsTopLeft" apps/web/src/app/chat/_canvas/genui-panel-node.tsx` returns >= 1
- (visual, at UI review) On a fresh canvas with several panels, panels spread with 8-pt breathing room instead of a cramped vertical column; ChatNode and GenuiPanelNode are distinguishable at a glance
</verification>

<success_criteria>
ChatNode (teal left stripe + MessageSquare icon) and GenuiPanelNode (bg-muted/40 header +
PanelsTopLeft icon) are distinguishable at a glance with no second hue; new canvas panels lay out
with 8-pt breathing room; node dimensions, drag-handle, selection ring, and persistence/registry
layers are unchanged; web typecheck passes.
</success_criteria>

<output>
Create `.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-04-SUMMARY.md` when done
</output>
