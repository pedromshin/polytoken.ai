---
phase: 26-zero-dependency-contract-fixes-backlog-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/empty-state.tsx
  - apps/web/src/app/chat/_components/chat-home-empty-state.tsx
  - apps/web/src/app/chat/_canvas/canvas-empty-state.tsx
  - apps/web/src/app/chat/_canvas/unknown-node-type-placeholder.tsx
autonomous: true
requirements: [FIX-11]
must_haves:
  truths:
    - "One shared EmptyState primitive drives all three empty-state call sites via variant props (layout/tone/size/action/caption)"
    - "Each call site's visible output is pixel-identical to its current rendering (de-dup refactor, not redesign — no copy changes)"
    - "The three variants are explicitly differentiated: ChatHome (centered/muted/spacious + action), Canvas (centered/muted/compact), UnknownNodeType (inline/destructive/compact + caption)"
    - "Every variant keeps its icon aria-hidden"
  artifacts:
    - path: "apps/web/src/components/empty-state.tsx"
      provides: "Shared EmptyState primitive with layout/tone/size/action/caption variants"
      exports: ["EmptyState", "EmptyStateProps"]
    - path: "apps/web/src/app/chat/_canvas/unknown-node-type-placeholder.tsx"
      provides: "inline/destructive variant with caption"
  key_links:
    - from: "apps/web/src/app/chat/_components/chat-home-empty-state.tsx"
      to: "apps/web/src/components/empty-state.tsx"
      via: "import { EmptyState }"
      pattern: "EmptyState"
    - from: "apps/web/src/app/chat/_canvas/canvas-empty-state.tsx"
      to: "apps/web/src/components/empty-state.tsx"
      via: "import { EmptyState }"
      pattern: "EmptyState"
---

<objective>
Collapse the three near-duplicate empty-state components into ONE shared `EmptyState` primitive whose
`layout`/`tone`/`size`/`action`/`caption` variant props make the differentiation explicit — each call
site rendering pixel-identically to today (a de-dup refactor, not a redesign).

Purpose: Satisfies the empty-state half of ROADMAP criterion 5 (the 3-4 empty states are differentiated
instead of repeating one identical icon+heading+paragraph recipe).
Output: `apps/web/src/components/empty-state.tsx` primitive + three call sites rewired to variant
configurations. This is the only plan touching these four files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md

<interfaces>
<!-- Current per-site renderings the variant props must reproduce pixel-for-pixel -->
- ChatHomeEmptyState: MessageSquarePlus size-10 text-muted-foreground/40, heading text-2xl font-semibold,
  body text-sm text-muted-foreground max-w-sm, centered full-pane, HAS a primary CTA button ("New chat")
- CanvasEmptyState: LayoutGrid size-8 text-muted-foreground, heading text-base font-semibold, same body,
  centered full-pane, NO button
- UnknownNodeTypePlaceholder: AlertTriangle size-4 text-destructive, inline icon+text row (NOT centered
  full-pane — fits inside a bounded node card), plus a caption line, NO button

<!-- Target EmptyStateProps (FIX-11) -->
- readonly icon: LucideIcon
- readonly heading: string
- readonly body: string
- readonly layout?: "centered" | "inline"   (default "centered")
- readonly tone?: "muted" | "destructive"    (default "muted")
- readonly size?: "compact" | "spacious"     (default "compact")
- readonly action?: { label: string; icon?: LucideIcon; onClick: () => void; disabled?: boolean }
- readonly caption?: string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the shared EmptyState primitive</name>
  <files>apps/web/src/components/empty-state.tsx</files>
  <read_first>
    - apps/web/src/app/chat/_components/chat-home-empty-state.tsx (spacious/centered/action reference JSX)
    - apps/web/src/app/chat/_canvas/canvas-empty-state.tsx (compact/centered reference JSX)
    - apps/web/src/app/chat/_canvas/unknown-node-type-placeholder.tsx (inline/destructive/caption reference JSX)
    - apps/web/src/components/app-sidebar.tsx (this directory's cross-cutting-component convention)
    - packages/ui/src/button.tsx (Button for the one action variant)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-11" (props + variant table) and § "Interaction Contracts" (inline variant must center within its own bounded box, not the viewport)
  </read_first>
  <behavior>
    - layout="centered" (default): full-pane vertical center; layout="inline": compact icon+text row that
      centers within its own bounded container ONLY (never against the viewport)
    - tone="muted" (default): icon uses muted tint; tone="destructive": icon uses text-destructive
    - size="spacious": icon size-10 + heading text-2xl; size="compact" (default): icon size-8 (centered) /
      size-4 (inline) + heading text-base
    - action present -> renders a @nauta/ui Button with optional leading icon, onClick, disabled
    - caption present -> renders a text-xs text-muted-foreground line below body
    - icon is always aria-hidden; heading remains the accessible content
  </behavior>
  <action>
    Create `EmptyState` (named exports `EmptyState` + `EmptyStateProps`; type the icon as lucide `LucideIcon`;
    props exactly as in the interfaces block above, all `readonly`). Body text is `text-sm text-muted-foreground`
    (add `max-w-sm` for centered layout). Icon always `aria-hidden`. Use only token classes (no hex, no
    font-medium — heading is `font-semibold`). This lives in `apps/web/src/components/` (cross-route, next to
    `app-sidebar.tsx`/`theme-provider.tsx`), NOT in `packages/ui`. Add colocated `empty-state.test.tsx`
    asserting: spacious renders size-10 + text-2xl; inline+destructive renders the destructive icon tint and
    does not apply full-pane centering; action renders a button that calls onClick; caption renders when set.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web && npm run test -w @nauta/web -- empty-state</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "export function EmptyState|export const EmptyState" apps/web/src/components/empty-state.tsx` returns >= 1
    - `grep -c "aria-hidden" apps/web/src/components/empty-state.tsx` returns >= 1
    - `grep -Ec "layout|tone|size|action|caption" apps/web/src/components/empty-state.tsx` returns >= 1
    - `grep -v '^\s*//' apps/web/src/components/empty-state.tsx | grep -c "font-medium"` returns 0
    - `npm run typecheck -w @nauta/web` exits 0
    - `npm run test -w @nauta/web -- empty-state` exits 0
  </acceptance_criteria>
  <done>EmptyState primitive + colocated test exist; supports all five variant dimensions; icon aria-hidden; typecheck and empty-state test pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the three call sites to EmptyState variants</name>
  <files>apps/web/src/app/chat/_components/chat-home-empty-state.tsx, apps/web/src/app/chat/_canvas/canvas-empty-state.tsx, apps/web/src/app/chat/_canvas/unknown-node-type-placeholder.tsx</files>
  <read_first>
    - apps/web/src/components/empty-state.tsx (the primitive from Task 1)
    - apps/web/src/app/chat/_components/chat-home-empty-state.tsx (current copy + CTA to preserve)
    - apps/web/src/app/chat/_canvas/canvas-empty-state.tsx (current copy to preserve)
    - apps/web/src/app/chat/_canvas/unknown-node-type-placeholder.tsx (current copy + caption to preserve)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-11" (call-site variant table) and § "Copywriting Contract" (unchanged per-site copy)
  </read_first>
  <action>
    Rewrite each component's body to render `<EmptyState ... />` (their exported component names/props may stay
    as thin wrappers so call sites remain stable). ChatHomeEmptyState: `icon=MessageSquarePlus` layout="centered"
    tone="muted" size="spacious" action={{ label: "New chat", icon: Plus, onClick: <existing handler> }};
    copy "Start a new conversation" / "Ask the agent anything — responses stream in and can include interactive
    widgets.". CanvasEmptyState: `icon=LayoutGrid` layout="centered" tone="muted" size="compact", no action;
    copy "No panels yet" / "Genui responses from this chat will appear here as panels. Switch to Chat view to
    start a conversation.". UnknownNodeTypePlaceholder: `icon=AlertTriangle` layout="inline" tone="destructive"
    size="compact", no action, caption "Type: {nodeType} · The canvas layout is unaffected — this panel is
    skipped safely."; heading "This panel type isn't supported in this version.". NO copy changes — reproduce
    each site's current rendering exactly.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "EmptyState" apps/web/src/app/chat/_components/chat-home-empty-state.tsx` returns >= 1
    - `grep -c "EmptyState" apps/web/src/app/chat/_canvas/canvas-empty-state.tsx` returns >= 1
    - `grep -c "EmptyState" apps/web/src/app/chat/_canvas/unknown-node-type-placeholder.tsx` returns >= 1
    - `grep -Ec "layout=\"inline\"|layout={\"inline\"}" apps/web/src/app/chat/_canvas/unknown-node-type-placeholder.tsx` returns >= 1
    - `grep -Ec "size=\"spacious\"" apps/web/src/app/chat/_components/chat-home-empty-state.tsx` returns >= 1
    - `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>All three call sites render via EmptyState with the correct variant configuration and unchanged copy; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | Presentational component de-dup; action.onClick wires existing handlers; no new input/data/auth flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-10 | Tampering | EmptyState action callback | accept | onClick receives the same handler the call site already used (e.g. "New chat"); no new privileged action; heading strings are static literals, not user input |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (zero new deps) |
</threat_model>

<verification>
- `npm run typecheck -w @nauta/web` exits 0
- `npm run test -w @nauta/web -- empty-state` exits 0
- `grep -rc "EmptyState" apps/web/src/app/chat/_components/chat-home-empty-state.tsx apps/web/src/app/chat/_canvas/canvas-empty-state.tsx apps/web/src/app/chat/_canvas/unknown-node-type-placeholder.tsx` each returns >= 1
- (visual, at UI review) the three empty states read as differentiated (spacious CTA vs compact vs inline destructive) yet each matches its prior rendering
</verification>

<success_criteria>
One `EmptyState` primitive drives all three call sites via explicit variant props; each site's visible
output is pixel-identical to today with unchanged copy; every variant keeps its icon aria-hidden;
typecheck and the empty-state test pass.
</success_criteria>

<output>
Create `.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-06-SUMMARY.md` when done
</output>
