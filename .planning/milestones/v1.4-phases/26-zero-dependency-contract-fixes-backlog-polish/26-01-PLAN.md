---
phase: 26-zero-dependency-contract-fixes-backlog-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/studio/_components/json-pane.tsx
  - apps/web/src/app/studio/_components/generation-sandbox-island.tsx
  - apps/web/src/app/studio/preview/page.tsx
  - apps/web/src/app/studio/_components/history-island.tsx
autonomous: true
requirements: [FIX-05, FIX-02, FIX-03]
must_haves:
  truths:
    - "The 3 studio JSON debug panes (generation-sandbox, history, preview) render from one shared JsonPane component"
    - "Each JSON pane has a working copy button that writes formatted JSON to the clipboard and shows a checkmark for 1500ms"
    - "history-island's list-row title and detail caption no longer use font-medium"
    - "history-island's FallbackNotice renders with the destructive token recipe, correct in dark mode with no dark: raw-palette override"
  artifacts:
    - path: "apps/web/src/app/studio/_components/json-pane.tsx"
      provides: "Shared JsonPane component (ScrollArea + ghost copy Button + label)"
      exports: ["JsonPane", "JsonPaneProps"]
    - path: "apps/web/src/app/studio/_components/generation-sandbox-island.tsx"
      provides: "generation-sandbox pane consumes JsonPane"
    - path: "apps/web/src/app/studio/preview/page.tsx"
      provides: "preview pane consumes JsonPane"
    - path: "apps/web/src/app/studio/_components/history-island.tsx"
      provides: "history pane consumes JsonPane + token cleanup"
  key_links:
    - from: "apps/web/src/app/studio/_components/generation-sandbox-island.tsx"
      to: "apps/web/src/app/studio/_components/json-pane.tsx"
      via: "import { JsonPane }"
      pattern: "JsonPane"
    - from: "apps/web/src/app/studio/preview/page.tsx"
      to: "apps/web/src/app/studio/_components/json-pane.tsx"
      via: "import from ../_components/json-pane"
      pattern: "json-pane"
---

<objective>
Replace the three duplicated raw-`JSON.stringify` debug panes on `/studio` (generation-sandbox,
history, preview) with one shared, token-compliant `JsonPane` component that adds the currently
missing copy affordance — and, while inside `history-island.tsx`, clear its three token drifts
(FIX-02 #9/#10 font-medium, FIX-03e FallbackNotice).

Purpose: Satisfies ROADMAP success criterion 3 (JSON panes share one component with a copy button)
and removes two of the eleven studio `font-medium` sites plus one hardcoded amber color system.
Output: New `JsonPane` component + three rewired call sites; `history-island.tsx` fully
token-compliant.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md

<interfaces>
<!-- Copy-button idiom to replicate exactly (from apps/web/src/app/chat/_components/turn-action-row.tsx) -->
- const COPIED_RESET_MS = 1500;
- import { Check, Copy } from "lucide-react";
- const [copied, setCopied] = useState(false);
- handler: void navigator.clipboard.writeText(text).then(() => { setCopied(true); setTimeout(() => setCopied(false), COPIED_RESET_MS); });
- copy trigger uses @nauta/ui Button ghost/icon variant with an aria-label

<!-- Existing pane markup being replaced (identical at all 3 sites) -->
- <ScrollArea className="flex-1"><pre className="p-4 font-mono text-xs text-foreground">{JSON.stringify(x, null, 2)}</pre></ScrollArea>
- wrapped by a labeled header bar: "border-b border-border/50 px-4 py-3", label text "Spec JSON"
- outer wrapper: "bg-muted" (unchanged)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared JsonPane component</name>
  <files>apps/web/src/app/studio/_components/json-pane.tsx</files>
  <read_first>
    - apps/web/src/app/studio/_components/generation-sandbox-island.tsx (lines ~404-408 — the current pane markup to preserve visually)
    - apps/web/src/app/chat/_components/turn-action-row.tsx (COPIED_RESET_MS + clipboard + Copy/Check swap idiom to replicate)
    - packages/ui/src/scroll-area.tsx (ScrollArea export signature)
    - packages/ui/src/button.tsx (Button variants — use ghost, icon size)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-05" (literal component contract) and § "Interaction Contracts" (JsonPane copy button)
  </read_first>
  <behavior>
    - Renders a header bar "border-b border-border/50 px-4 py-3" with the label text (default "Spec JSON")
      styled "text-xs font-semibold uppercase tracking-wide text-muted-foreground"
    - Renders body: <ScrollArea className="flex-1"><pre className="p-4 font-mono text-xs text-foreground">{formatted}</pre></ScrollArea>
      where formatted = JSON.stringify(value, null, 2) (single source of truth for 2-space indentation)
    - Copy button (ghost Button, icon-only, aria-label="Copy JSON") writes `formatted` to navigator.clipboard
      and swaps the Copy icon to Check for COPIED_RESET_MS = 1500ms
    - Accepts props: readonly value: unknown; readonly label?: string (default "Spec JSON")
  </behavior>
  <action>
    Create `JsonPane` exporting named `JsonPane` and `JsonPaneProps` (named exports only, per repo
    style; type `value` as `unknown`). Header row is the existing "border-b border-border/50 px-4 py-3"
    bar; add a right-aligned ghost `Button` (from `@nauta/ui`, `variant="ghost"`, icon size) whose
    child icon swaps `Copy` -> `Check` (lucide-react) on click, resetting after `COPIED_RESET_MS = 1500`.
    Body is `<ScrollArea className="flex-1"><pre className="p-4 font-mono text-xs text-foreground">`
    over `JSON.stringify(value, null, 2)`. Use only token classes (text-foreground, text-muted-foreground,
    border-border/50) — no hex, no font-medium. `aria-label="Copy JSON"` on the button (icon-only control).
    Do NOT create a packages/ui primitive — this is app-local per CONTEXT.md (debug/inspector affordance).
    Add a colocated `json-pane.test.tsx` covering: renders formatted JSON text; clicking copy calls
    navigator.clipboard.writeText with the 2-space-indented string and swaps to the Check icon.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/studio/_components/json-pane.tsx` exists and `grep -c "export function JsonPane\|export const JsonPane" apps/web/src/app/studio/_components/json-pane.tsx` returns >= 1
    - `grep -c "aria-label=\"Copy JSON\"" apps/web/src/app/studio/_components/json-pane.tsx` returns 1
    - `grep -c "JSON.stringify(value, null, 2)" apps/web/src/app/studio/_components/json-pane.tsx` returns >= 1
    - `grep -v '^\s*//' apps/web/src/app/studio/_components/json-pane.tsx | grep -c "font-medium"` returns 0
    - `npm run typecheck -w @nauta/web` exits 0
    - `npm run test -w @nauta/web -- json-pane` exits 0
  </acceptance_criteria>
  <done>JsonPane component + colocated test exist; typecheck and the json-pane test pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire JsonPane into generation-sandbox + preview panes</name>
  <files>apps/web/src/app/studio/_components/generation-sandbox-island.tsx, apps/web/src/app/studio/preview/page.tsx</files>
  <read_first>
    - apps/web/src/app/studio/_components/generation-sandbox-island.tsx (current pane block ~404-408)
    - apps/web/src/app/studio/preview/page.tsx (current pane block ~96-100)
    - apps/web/src/app/studio/_components/json-pane.tsx (the component created in Task 1)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-05"
  </read_first>
  <action>
    In `generation-sandbox-island.tsx`, replace the inline `<ScrollArea>...<pre>{JSON.stringify(...)}</pre>`
    block with `<JsonPane value={spec} />` (label defaults to "Spec JSON"), keeping the existing
    `bg-muted` outer wrapper and surrounding header/footer chrome intact. Import `JsonPane` from
    `./json-pane`. In `preview/page.tsx`, do the same, importing from `../_components/json-pane`
    (one route segment up from `preview/_components/`). Remove any now-unused `ScrollArea`/`pre`
    imports left dangling. Visual footprint stays identical; the only new affordance is the copy button.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "JsonPane" apps/web/src/app/studio/_components/generation-sandbox-island.tsx` returns >= 1
    - `grep -c "JsonPane" apps/web/src/app/studio/preview/page.tsx` returns >= 1
    - `grep -c "JSON.stringify" apps/web/src/app/studio/_components/generation-sandbox-island.tsx` returns 0
    - `grep -c "JSON.stringify" apps/web/src/app/studio/preview/page.tsx` returns 0
    - `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>Both call sites render via JsonPane; no residual JSON.stringify pane markup; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: history-island — JsonPane swap + token cleanup (FIX-02 #9/#10, FIX-03e)</name>
  <files>apps/web/src/app/studio/_components/history-island.tsx</files>
  <read_first>
    - apps/web/src/app/studio/_components/history-island.tsx (lines ~144, ~246, ~405, ~445-449)
    - apps/web/src/app/studio/_components/json-pane.tsx
    - apps/web/src/app/studio/_components/generation-state-chrome.tsx (the `fallback`-state destructive recipe to reuse verbatim)
    - apps/web/src/app/chat/_components/conversation-row.tsx (sibling row-title convention `text-sm` with no weight override)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-02" (rows #9, #10) and § "FIX-03" (e)
  </read_first>
  <action>
    Three edits in this one file:
    (1) FIX-05: replace the `<ScrollArea>...<pre>{JSON.stringify(...)}</pre>` block (~445-449) with
        `<JsonPane value={...} />`, importing from `./json-pane`; remove dangling ScrollArea/pre imports.
    (2) FIX-02 #9 (~144): the row-title `<p className="text-sm font-medium text-foreground leading-snug
        line-clamp-2 flex-1 min-w-0">` DROPS `font-medium` entirely (plain font-normal) to match
        conversation-row's sibling list-row-title convention. FIX-02 #10 (~405): the detail-header caption
        `<span className="text-xs font-medium text-muted-foreground truncate flex-1">` changes
        `font-medium` -> `font-normal` (caption role).
    (3) FIX-03e (~246): replace the `FallbackNotice` classes `border-amber-500/30 bg-amber-500/10
        text-amber-700 dark:text-amber-400` with the verbatim destructive recipe
        `role="alert" className="shrink-0 border-b border-destructive/30 bg-destructive/5 px-4 py-2 text-xs text-destructive"`.
        Copy text unchanged. No `dark:` raw-palette override remains.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "JsonPane" apps/web/src/app/studio/_components/history-island.tsx` returns >= 1
    - `grep -c "JSON.stringify" apps/web/src/app/studio/_components/history-island.tsx` returns 0
    - `grep -c "font-medium" apps/web/src/app/studio/_components/history-island.tsx` returns 0
    - `grep -c "amber" apps/web/src/app/studio/_components/history-island.tsx` returns 0
    - `grep -c "text-destructive" apps/web/src/app/studio/_components/history-island.tsx` returns >= 1
    - `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>history-island uses JsonPane, has zero font-medium, zero amber classes, and the FallbackNotice uses the destructive token recipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click -> clipboard | Copy button writes JSON to the browser clipboard (write-only, user-initiated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | JsonPane copy button (navigator.clipboard.writeText) | accept | Value copied is the same spec JSON already rendered on screen in a debug/inspector pane; write-only, no clipboard read; no secrets present in a genui spec |
| T-26-02 | Tampering | JSON.stringify of `unknown` value rendered in `<pre>` | accept | Display-only; React text-escapes `<pre>` content; no eval/dangerouslySetInnerHTML |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (zero new deps); nothing to vet |
</threat_model>

<verification>
- `npm run typecheck -w @nauta/web` exits 0
- `grep -rn "JSON.stringify" apps/web/src/app/studio/_components/generation-sandbox-island.tsx apps/web/src/app/studio/_components/history-island.tsx apps/web/src/app/studio/preview/page.tsx` returns 0
- `npm run test -w @nauta/web -- json-pane` exits 0
</verification>

<success_criteria>
All three studio JSON panes render via one `JsonPane` component with a working copy button;
`history-island.tsx` has zero `font-medium` and zero `amber` classes and uses the destructive token
recipe for its fallback notice; web typecheck passes.
</success_criteria>

<output>
Create `.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-01-SUMMARY.md` when done
</output>
