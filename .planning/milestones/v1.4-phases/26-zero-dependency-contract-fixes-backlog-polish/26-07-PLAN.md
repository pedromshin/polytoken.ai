---
phase: 26-zero-dependency-contract-fixes-backlog-polish
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/email-listener/tests/infrastructure/test_genui_generator_adapter.py
  - apps/email-listener/app/infrastructure/llm/genui_generator_adapter.py
  - packages/genui/artifacts/genui-prompt.json
autonomous: true
requirements: [POLISH-01]
must_haves:
  truths:
    - "The declarative generator's system prompt teaches declared-state display via dataRef-bound nodes (list/conditional), never {{mustache}} inside a text node's content"
    - "The system prompt clarifies setState absolute-vs-increment semantics"
    - "A regression test asserts the state-binding guidance is present in the built system prompt and goes RED before the prompt edit, GREEN after"
    - "Zero renderer/schema changes — spec-renderer.tsx and spec-schema.ts are untouched"
    - "The system prompt stays deterministic and pack-agnostic (existing byte-identical test still passes)"
  artifacts:
    - path: "apps/email-listener/app/infrastructure/llm/genui_generator_adapter.py"
      provides: "_SYSTEM_PROMPT_TEXT with dataRef/conditional binding + setState guidance"
      contains: "dataRef"
    - path: "apps/email-listener/tests/infrastructure/test_genui_generator_adapter.py"
      provides: "Regression test for state-binding prompt guidance"
  key_links:
    - from: "apps/email-listener/app/infrastructure/llm/genui_generator_adapter.py"
      to: "declared-state binding"
      via: "_SYSTEM_PROMPT_TEXT guidance on dataRef-bound list/conditional nodes"
      pattern: "dataRef"
---

<objective>
Teach the declarative genui generator (Python) to express declared-state display through
`dataRef`-bound nodes — `list` (iterate a state array) or `conditional` (branch on a state value with
plain `text` nodes per branch) — instead of emitting `{"type":"text","content":"{{count}}"}` that the
locked, unmodified `SpecRenderer` never interpolates. Also clarify `setState` absolute-vs-increment
semantics. This is a generator-PROMPT-only fix (POLISH-01 / 999.8 option (a)) with ZERO renderer or
schema changes, following the repo's RED->GREEN pytest convention.

Purpose: Satisfies the "counter bound to state produces a live-updating dataRef render instead of a
static {{count}} literal" half of ROADMAP criterion 5.
Output: An enriched `_SYSTEM_PROMPT_TEXT` (and optionally the `genui-prompt.json` catalog reference)
plus a regression test, all in the email-listener app.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md

<interfaces>
<!-- Binding facts (from spec schema + renderer; DO NOT modify either) -->
- text node schema: content: z.string() ONLY (no dataRef) — a {{mustache}} in content is never interpolated
- ONLY list (dataRef -> iterate an array) and conditional (dataRef -> branch on a value) resolve live
  state at render time (packages/genui/src/renderer/render-node.tsx resolveDataRef) — both already
  render through the unmodified, token-compliant SpecRenderer
- The bug (999.8): generator emits text content "{{count}}" (static), even though the paired
  button.onClick {type:"setState", key:"count"} DOES write the store

<!-- Where the prompt lives (apps/email-listener/app/infrastructure/llm/genui_generator_adapter.py) -->
- _SYSTEM_PROMPT_TEXT (constant, ~line 87) — the trusted static system prompt string
- _format_catalog_reference() (~line 117) injects genui-prompt.json's actionRules + allowedProcedures
- _build_system_blocks() (~line 153) = _SYSTEM_PROMPT_TEXT + catalog reference, as one cached block

<!-- Existing test constraints (tests/infrastructure/test_genui_generator_adapter.py) -->
- test_build_system_blocks_identical_regardless_of_pack asserts _build_system_blocks() is BYTE-IDENTICAL
  regardless of style_pack_id/retrieval — the new guidance must be static, not pack/retrieval-dependent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — regression test for declared-state binding guidance</name>
  <files>apps/email-listener/tests/infrastructure/test_genui_generator_adapter.py</files>
  <read_first>
    - apps/email-listener/tests/infrastructure/test_genui_generator_adapter.py (imports + test_build_system_blocks_identical_regardless_of_pack for style)
    - apps/email-listener/app/infrastructure/llm/genui_generator_adapter.py (_SYSTEM_PROMPT_TEXT, _build_system_blocks)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "POLISH-01"
  </read_first>
  <action>
    Add a test (e.g. `test_system_prompt_teaches_dataRef_state_binding`) importing `_build_system_blocks`
    (and/or `_SYSTEM_PROMPT_TEXT`) and asserting the built system text contains the declared-state binding
    guidance: it mentions `dataRef`, references `list`/`conditional` as the state-bound node types, forbids
    `{{`-mustache placeholders inside a `text` node's `content`, and clarifies `setState`
    absolute-vs-increment semantics. Assert against the concatenated block text (lowercased where helpful)
    so it is deterministic. This test MUST fail now (RED) — the current prompt has none of this guidance.
    Since a full "counter -> dataRef spec" behavioral assertion requires live Bedrock (out of offline CI
    scope), this prompt-content assertion is the deterministic proxy per the UI-SPEC; note that in the test
    docstring.
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/infrastructure/test_genui_generator_adapter.py -k dataRef_state_binding</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "dataRef|state.binding|state_binding" apps/email-listener/tests/infrastructure/test_genui_generator_adapter.py` returns >= 1
    - Running `cd apps/email-listener && uv run pytest tests/infrastructure/test_genui_generator_adapter.py -k dataRef_state_binding` FAILS (RED) before Task 2 (the new assertion is unsatisfied)
  </acceptance_criteria>
  <done>The regression test exists and is RED (fails) against the current, un-enriched prompt.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — enrich the generator prompt with dataRef binding + setState semantics</name>
  <files>apps/email-listener/app/infrastructure/llm/genui_generator_adapter.py, packages/genui/artifacts/genui-prompt.json</files>
  <read_first>
    - apps/email-listener/app/infrastructure/llm/genui_generator_adapter.py (_SYSTEM_PROMPT_TEXT ~87-114, _format_catalog_reference ~117, _build_system_blocks ~153)
    - packages/genui/artifacts/genui-prompt.json (actionRules + allowedProcedures — the catalog reference source)
    - apps/email-listener/tests/infrastructure/test_genui_generator_adapter.py (test_build_system_blocks_identical_regardless_of_pack — must still pass)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "POLISH-01"
  </read_first>
  <action>
    Extend `_SYSTEM_PROMPT_TEXT` with a static rules block teaching declared-state display: to reflect
    state (`setState` targets), bind through a `dataRef`-bound `list` (iterate a state array) or
    `conditional` (branch on a state value, each branch a plain `text` node with realistic static copy) —
    and NEVER put a `{{mustache}}` placeholder inside a `text` node's `content` (the renderer does not
    interpolate text content). Clarify `setState` semantics: whether a handler sets an absolute value vs.
    increments, and that the display node must be bound via `dataRef` to the same state key the `setState`
    writes. Keep it concise and DETERMINISTIC (no pack/retrieval-dependent content — the byte-identical
    test must still pass). Optionally mirror the setState/dataRef guidance into `genui-prompt.json`'s
    `actionRules` so `_format_catalog_reference()` surfaces it too (discretionary; keep it deterministic).
    ZERO changes to spec-renderer.tsx, render-node.tsx, or spec-schema.ts (999.8 option (b) is out of scope).
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/infrastructure/test_genui_generator_adapter.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dataRef" apps/email-listener/app/infrastructure/llm/genui_generator_adapter.py` returns >= 1
    - `grep -Ec "setState" apps/email-listener/app/infrastructure/llm/genui_generator_adapter.py` returns >= 1
    - `grep -Ec "conditional|list" apps/email-listener/app/infrastructure/llm/genui_generator_adapter.py` returns >= 1
    - `git diff --name-only | grep -Ec "spec-renderer|render-node|spec-schema"` returns 0 (renderer/schema untouched)
    - `cd apps/email-listener && uv run pytest tests/infrastructure/test_genui_generator_adapter.py -k dataRef_state_binding` PASSES (GREEN)
    - `cd apps/email-listener && uv run pytest tests/infrastructure/test_genui_generator_adapter.py` exits 0 (incl. the byte-identical pack-agnostic test)
  </acceptance_criteria>
  <done>The prompt teaches dataRef-bound state display + setState semantics; the RED test is now GREEN; the full generator-adapter test file passes; renderer/schema untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| system prompt -> LLM (Bedrock) | Trusted static instructions sent to the model; user document content never enters this prompt (quarantined into <DATA_SECTION> upstream) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-P1 | Tampering / Prompt Injection | _SYSTEM_PROMPT_TEXT enrichment | mitigate | Change is additive output-shape guidance only; the quarantine boundary (generator never receives raw prose, SAFE-02/D-09), jsonschema Draft7 validation, and MAX_SPEC_NODES/DEPTH bounds are unchanged — no new user-content path is introduced |
| T-26-P2 | Information Disclosure | Prompt is trusted static content | accept | No secrets or user PII embedded; content is the committed prompt artifact only |
| T-26-P3 | Tampering | Determinism/cache stability | mitigate | New guidance is static (not pack/retrieval-dependent); test_build_system_blocks_identical_regardless_of_pack must still pass, preserving cache_control prefix stability (D-21) |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (zero new deps) |
</threat_model>

<verification>
- `cd apps/email-listener && uv run pytest tests/infrastructure/test_genui_generator_adapter.py` exits 0
- `grep -c "dataRef" apps/email-listener/app/infrastructure/llm/genui_generator_adapter.py` returns >= 1
- `git diff --name-only | grep -Ec "spec-renderer|render-node|spec-schema"` returns 0
</verification>

<success_criteria>
The declarative generator's system prompt teaches `dataRef`-bound `list`/`conditional` state display
(never `{{mustache}}` text content) and `setState` absolute-vs-increment semantics; a regression test
goes RED->GREEN; the full generator-adapter test file passes including the byte-identical pack-agnostic
test; renderer and schema are untouched.
</success_criteria>

<output>
Create `.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-07-SUMMARY.md` when done
</output>
