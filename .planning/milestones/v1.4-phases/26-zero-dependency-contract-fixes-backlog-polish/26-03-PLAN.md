---
phase: 26-zero-dependency-contract-fixes-backlog-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/button.tsx
  - apps/web/src/app/chat/_components/cost-cap-blocked-card.tsx
  - apps/web/src/app/chat/_components/inline-error-card.tsx
  - apps/web/src/app/chat/_components/conversation-row.tsx
  - apps/web/src/app/chat/_components/turn-action-row.tsx
  - apps/web/src/app/chat/_components/message-turn.tsx
autonomous: true
requirements: [FIX-02, FIX-07, FIX-08]
must_haves:
  truths:
    - "The shared Button base class inherits font-normal — no Button label renders at font-medium"
    - "No rendered className in /chat uses font-medium (buttonVariants source + two chat drift sites fixed)"
    - "Conversation rows and their overflow-menu trigger have transition-colors and a real hover background (hover:bg-muted)"
    - "Turn-action copy/regenerate buttons ease their background+text on hover"
    - "Assistant messages carry a thin neutral left rail (border-l-2 border-l-border/60 pl-3) distinguishing role beyond alignment"
  artifacts:
    - path: "packages/ui/src/button.tsx"
      provides: "buttonVariants base with font-normal (font-medium removed at source)"
      contains: "font-normal"
    - path: "apps/web/src/app/chat/_components/message-turn.tsx"
      provides: "Assistant-branch left rail"
      contains: "border-l-2 border-l-border/60 pl-3"
  key_links:
    - from: "apps/web/src/app/chat/_components/conversation-row.tsx"
      to: "hover affordance"
      via: "transition-colors + hover:bg-muted"
      pattern: "transition-colors"
    - from: "apps/web/src/app/chat/_components/turn-action-row.tsx"
      to: "hover affordance"
      via: "hover:bg-muted hover:text-foreground"
      pattern: "hover:bg-muted"
---

<objective>
Fix the 2-weight typography contract at its source (`packages/ui/src/button.tsx` `buttonVariants`
base `font-medium` -> `font-normal`), sweep the two chat-side `font-medium` drift sites the UI-SPEC's
grep gate exposes, and add the eased hover affordances (FIX-07) and assistant-message left rail
(FIX-08) that give `/chat` role chrome beyond alignment.

Purpose: Restores the locked 2-weight contract app-wide at the design-system level (ROADMAP criterion
2) and satisfies the hover/role-chrome half of ROADMAP criterion 4.
Output: `Button` base emits `font-normal`; zero rendered `font-medium` in `/chat`; conversation rows,
turn-action buttons, and assistant messages carry consistent eased hover + role chrome.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md

<interfaces>
<!-- packages/ui/src/button.tsx:9 buttonVariants base (blast radius = every Button in the app) -->
- current base: "inline-flex ... rounded-md text-sm font-medium transition-colors ..."
- target base:  "inline-flex ... rounded-md text-sm font-normal transition-colors ..."
- Button base already carries transition-colors (only the missing bg hover needs adding at call sites)
<!-- Two chat-side font-medium drift sites NOT in the UI-SPEC's 11-item studio table but required
     by success criterion 2 ("no rendered font-medium in /chat or /studio"): both are
     `text-sm font-medium text-destructive` heading spans -> font-semibold (same register as
     generation-state-chrome.tsx:75, which the UI-SPEC maps to font-semibold). -->
- apps/web/src/app/chat/_components/cost-cap-blocked-card.tsx:20
- apps/web/src/app/chat/_components/inline-error-card.tsx:30
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove font-medium at the Button source + two chat drift sites (FIX-02)</name>
  <files>packages/ui/src/button.tsx, apps/web/src/app/chat/_components/cost-cap-blocked-card.tsx, apps/web/src/app/chat/_components/inline-error-card.tsx</files>
  <read_first>
    - packages/ui/src/button.tsx (line 9, buttonVariants base class)
    - apps/web/src/app/chat/_components/cost-cap-blocked-card.tsx (line ~20)
    - apps/web/src/app/chat/_components/inline-error-card.tsx (line ~30)
    - apps/web/src/app/studio/_components/generation-state-chrome.tsx (line ~75 — the font-semibold destructive-heading register these two match)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-02"
  </read_first>
  <action>
    In `packages/ui/src/button.tsx` line 9, change `font-medium` -> `font-normal` in the `buttonVariants`
    base string (explicit `font-normal`, not a bare omission — keeps the 2-weight contract legible at the
    declaration site). Blast radius is intentional (every Button label changes weight — Send / New chat /
    Retry / model-picker-trigger). Then in `cost-cap-blocked-card.tsx:20` and `inline-error-card.tsx:30`,
    change the `text-sm font-medium text-destructive` heading spans' `font-medium` -> `font-semibold`
    (destructive-heading emphasis register, matching generation-state-chrome). These two are drift the
    UI-SPEC's chat+studio grep gate requires clearing even though they are not in the 11-item studio table.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/ui && npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "font-medium" packages/ui/src/button.tsx` returns 0
    - `grep -c "font-normal" packages/ui/src/button.tsx` returns >= 1
    - `grep -c "font-medium" apps/web/src/app/chat/_components/cost-cap-blocked-card.tsx` returns 0
    - `grep -c "font-medium" apps/web/src/app/chat/_components/inline-error-card.tsx` returns 0
    - `npm run typecheck -w @nauta/ui` exits 0 and `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>Button base emits font-normal; both chat cards use font-semibold headings; both typechecks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Eased hover affordances on conversation-row + turn-action-row (FIX-07)</name>
  <files>apps/web/src/app/chat/_components/conversation-row.tsx, apps/web/src/app/chat/_components/turn-action-row.tsx</files>
  <read_first>
    - apps/web/src/app/chat/_components/conversation-row.tsx (lines ~64-69 row base, ~100-108 overflow trigger)
    - apps/web/src/app/chat/_components/turn-action-row.tsx (lines ~54-75 raw copy/regenerate buttons)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-07"
  </read_first>
  <action>
    conversation-row.tsx: (row base ~64-69) append `transition-colors` to the
    `"group flex items-center gap-1 rounded-md px-1 py-1"` base so the isActive `bg-primary/10` and
    `hover:bg-muted` swap eases instead of snapping. (overflow-menu trigger ~100-108) change
    `"size-11 shrink-0 text-muted-foreground hover:text-foreground"` to add `hover:bg-muted`
    -> `"size-11 shrink-0 text-muted-foreground hover:bg-muted hover:text-foreground"` (Button base
    already carries transition-colors). turn-action-row.tsx: (both raw `<button>` at ~54-75) change
    `"rounded p-1 hover:text-foreground"` -> `"rounded p-1 transition-colors hover:bg-muted hover:text-foreground"`.
    Regenerate keeps its existing `disabled:opacity-30`. Keep `p-1` (dense inline action row — the 44px
    touch-target rule does not apply here per the UI-SPEC).
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "transition-colors" apps/web/src/app/chat/_components/conversation-row.tsx` returns >= 1
    - `grep -c "hover:bg-muted" apps/web/src/app/chat/_components/conversation-row.tsx` returns >= 1
    - `grep -c "transition-colors" apps/web/src/app/chat/_components/turn-action-row.tsx` returns >= 1
    - `grep -c "hover:bg-muted hover:text-foreground" apps/web/src/app/chat/_components/turn-action-row.tsx` returns >= 1
    - `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>Conversation rows + overflow trigger + both turn-action buttons ease background and text on hover; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Assistant-message thin left rail (FIX-08)</name>
  <files>apps/web/src/app/chat/_components/message-turn.tsx</files>
  <read_first>
    - apps/web/src/app/chat/_components/message-turn.tsx (lines ~122-128 — the role-conditional wrapper; assistant branch currently "w-full max-w-full")
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-08"
  </read_first>
  <action>
    In the role-conditional wrapper, change the assistant branch from `"w-full max-w-full"` to
    `"w-full max-w-full border-l-2 border-l-border/60 pl-3"`. Use NEUTRAL `border-l-border/60`
    (NOT `border-primary` — a per-message marker is not on the accent allowlist). `pl-3` (12px) is on
    the 4px grid. Do not touch the user branch (its `bg-muted` bubble is unchanged); no bubble, no avatar.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "border-l-2 border-l-border/60 pl-3" apps/web/src/app/chat/_components/message-turn.tsx` returns >= 1
    - `grep -c "border-l-primary" apps/web/src/app/chat/_components/message-turn.tsx` returns 0
    - `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>Assistant branch carries the neutral left rail with pl-3; no primary accent leaked into messages; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | Design-system class change + presentational hover/border edits; no input, data, or auth flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Tampering | buttonVariants base class (app-wide blast radius) | accept | One Tailwind utility swap (font-medium->font-normal); no behavior/prop/API change; existing markdown-renderer.test.tsx already asserts no font-medium leaks |
| T-26-05 | Denial of Service (visual) | Button weight change across app | accept | Weight-only change; contrast ratios unaffected; visually verify Send/New chat/Retry/model-picker at UI review |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (zero new deps) |
</threat_model>

<verification>
- `grep -rn "font-medium" packages/ui/src/button.tsx apps/web/src/app/chat/_components/cost-cap-blocked-card.tsx apps/web/src/app/chat/_components/inline-error-card.tsx` returns 0
- `npm run typecheck -w @nauta/ui` exits 0
- `npm run typecheck -w @nauta/web` exits 0
- (visual, at UI review) Send / New chat / Retry / model-picker-trigger labels render at normal weight; emphasis lives only in font-semibold headings
</verification>

<success_criteria>
`Button` base emits `font-normal`; zero rendered `font-medium` remains in `/chat`; conversation rows,
their overflow trigger, and both turn-action buttons ease their background+text on hover; assistant
messages carry a neutral thin left rail; both typechecks pass.
</success_criteria>

<output>
Create `.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-03-SUMMARY.md` when done
</output>
