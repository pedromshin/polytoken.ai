---
phase: 47-brand-foundation-verification-tooling
plan: 03
type: execute
wave: 2
depends_on: [47-01, 47-02]
files_modified:
  - docs/design/brand-guide.md
  - .planning/PROJECT.md
autonomous: true
requirements: [BRND-03]
must_haves:
  truths:
    - "A developer can read docs/design/brand-guide.md to learn the warm polytoken voice register, do/don't, and mark usage"
    - "The brand guide references (never contradicts) product-register-and-bans.md for the bans"
    - "The guide records the USER-LOCKED naming verbatim, the explicitly ACCEPTED `polytoken` CLI-tool collision, AND an explicit 'NOT done — user-gated' list (domain purchase, trademark)"
    - "PROJECT.md Key Decisions records the USER-LOCKED brand decision + the accepted collision (user decision overriding the dossier recommendation)"
    - "No purged brand name appears anywhere in app copy or committed docs — the product is named only 'polytoken'"
  artifacts:
    - path: "docs/design/brand-guide.md"
      provides: "In-repo polytoken brand guide (voice, do/don't, mark usage, USER-LOCKED naming, accepted collision, NOT-done list)"
      min_lines: 40
      contains: "polytoken.ai"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions entry for the v1.8 USER-LOCKED brand decision"
      contains: "Phase 47"
  key_links:
    - from: "docs/design/brand-guide.md"
      to: "docs/design/product-register-and-bans.md"
      via: "reference for the bans (glassmorphism etc.)"
      pattern: "product-register-and-bans"
    - from: "docs/design/brand-guide.md"
      to: "apps/web/src/components/brand-mark.tsx"
      via: "mark-usage section references the real mark asset"
      pattern: "brand-mark"
---

<objective>
Record the brand decision so it is durable and discoverable: an in-repo brand guide (voice + do/don't + mark usage + the USER-LOCKED naming record + the accepted collision + an explicit "NOT done — user-gated" list) and a PROJECT.md Key Decisions entry.

Purpose: closes BRND-03. Turns the USER-LOCKED direction (D-47-01 2026-07-10 / D-47-02 / D-47-03) into repo-of-record artifacts every later phase and future reader relies on.
Output: `docs/design/brand-guide.md` (new) + a Key Decisions row in `.planning/PROJECT.md`.
Depends on 47-01 (so the mark-usage section references the real `brand-mark.tsx` / `icon.svg` assets) and 47-02 (so the repo-level brand-name guard runs after all wave-1 app copy is in place).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-brand-foundation-verification-tooling/47-CONTEXT.md
@.planning/research/v1.8-design/BRAND-IDENTITY-OPTIONS.md
@docs/design/product-register-and-bans.md
@.planning/phases/47-brand-foundation-verification-tooling/47-01-SUMMARY.md

<interfaces>
<!-- LOCKED sources for this doc — transcribe, do not re-decide. -->

USER-LOCK (D-47-01, 2026-07-10 — overrides all prior direction). Verbatim: "everything will be called polytoken
and domain polytoken.ai. everything else is purged." The product names itself **polytoken** everywhere; domain
of record is polytoken.ai (recorded only — not purchased). No alternate brand direction survives. There is NO
rename pending — the name is permanently polytoken. The warm/companion TONE from the dossier's Direction-B
ANALYSIS survives, but as the copy REGISTER only (no alternate naming).

D-47-03 brand guide contents: voice principles + do/don't table; mark usage (clear space, min sizes,
monochrome rules); the USER-LOCKED naming record (polytoken / polytoken.ai; verbatim quote; alternates purged);
the ACCEPTED `polytoken` CLI-tool collision (see below — recorded as an accepted risk, NOT a mitigation posture);
and an explicit "NOT done — user-gated" list (domain purchase, trademark search/filing). The guide SITS BESIDE
product-register-and-bans.md which stays authoritative for bans — the guide REFERENCES it, never contradicts
(esp. the glassmorphism ban item 3 — the guide must not license new blur).

Voice register (D-47-01, warm polytoken voice): warm, human, companion — "remember, recall, connect, your
workspace"; first-person framing; reference points Notion AI / Mem; NEVER infrastructure vocabulary. The product
NAME is "polytoken" (the register is a TONE, not a name — there is no alternate brand name).

Naming collision (ACCEPTED by the user 2026-07-10): an existing local-first AI coding-agent dev tool is also
named `polytoken` (docs.polytoken.dev, npm package `polytoken`, `polytoken.com` registered) — an exact-name
collision in an adjacent space. The user EXPLICITLY ACCEPTED this collision; record it as an accepted risk. Do
NOT frame product copy around avoiding it and do NOT present the voice register as a "mitigation" — it is not.

PROJECT.md Key Decisions FORMAT (existing table at ## Key Decisions): a 3-column markdown table
| Decision | Rationale | Outcome | — append one new row (do not restructure the table). Milestone-prefix the
Decision cell like the existing rows (e.g. "**v1.8 Phase 47 (BRND)**: ...").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the in-repo polytoken brand guide</name>
  <read_first>
    - .planning/phases/47-brand-foundation-verification-tooling/47-CONTEXT.md (D-47-01 USER-LOCK verbatim; D-47-03 required contents; D-47-02 mark)
    - .planning/research/v1.8-design/BRAND-IDENTITY-OPTIONS.md (historical — read ONLY for the warm/companion register analysis + the collision facts; the naming recommendation is SUPERSEDED by the USER-LOCK)
    - docs/design/product-register-and-bans.md (the authoritative bans this guide references, not restates)
    - apps/web/src/components/brand-mark.tsx (created by 47-01 — the real variants/geometry the mark-usage section cites)
    - apps/web/src/app/icon.svg (created by 47-01 — the favicon asset)
  </read_first>
  <files>docs/design/brand-guide.md</files>
  <action>
    Create `docs/design/brand-guide.md` with these sections (per D-47-03): (1) USER-LOCKED naming — quote the user
    decision verbatim ("everything will be called polytoken and domain polytoken.ai. everything else is purged.",
    2026-07-10), state the product is named **polytoken** everywhere, the domain of record is polytoken.ai
    (recorded, not purchased), and that all alternate brand directions are purged. (2) Voice principles — the warm
    polytoken voice register (warm/companion/first-person; Notion AI / Mem reference points; never infrastructure
    vocabulary) with a concrete do/don't table of at least 5 before→after copy pairs drawn from the real surfaces
    (login greeting, empty states, toasts). (3) Mark usage — reference the REAL assets
    `apps/web/src/components/brand-mark.tsx` (its `variant` glyph/lockup + `tone` brand/mono) and
    `apps/web/src/app/icon.svg`; state clear-space, minimum sizes, and when to use the monochrome variant.
    (4) Accepted collision — record that an existing `polytoken` CLI dev tool (docs.polytoken.dev, npm `polytoken`)
    is an exact-name collision that the USER EXPLICITLY ACCEPTED on 2026-07-10; frame it as an accepted risk, NOT a
    mitigation posture, and do NOT license copy that dances around it. (5) "NOT done — user-gated" list: domain
    purchase (polytoken.ai) and trademark search/filing are explicitly NOT done (no rename is listed — the name is
    locked). Add a one-line pointer that `docs/design/product-register-and-bans.md` remains authoritative for the
    absolute bans (the guide must NOT license new blur / contradict the glassmorphism ban). Do NOT write any
    alternate brand name anywhere in the guide. Keep it a working reference doc, not marketing prose.
  </action>
  <acceptance_criteria>
    - `docs/design/brand-guide.md` exists, is ≥40 lines, and contains "polytoken.ai". [automated: wc -l + grep]
    - It references `product-register-and-bans` and `brand-mark` (or `brand-mark.tsx`): `grep -n "product-register-and-bans\|brand-mark" docs/design/brand-guide.md` matches both. [automated]
    - It records the verbatim USER-LOCK quote: `grep -n "everything will be called polytoken" docs/design/brand-guide.md` matches. [automated]
    - It contains a "NOT done" / "user-gated" section naming domain purchase and trademark: `grep -niE "domain|trademark" docs/design/brand-guide.md` matches both concepts. [automated]
    - It contains a do/don't voice table (≥5 before→after pairs).
    - REPO-LEVEL BRAND GUARD — no user-facing copy or committed doc names the product anything but polytoken: `grep -rniE "cortex|nodal|lattice|constellation" apps/web/src docs` returns NO matches (historical `.planning/research/` files are out of scope and retained; the grep scope covers only app source + committed docs). [automated]
    - It does NOT license blur/glassmorphism: `grep -niE "glassmorphism|backdrop-blur|frosted" docs/design/brand-guide.md` — any match must be in a "banned/see bans" context, not a permission. [manual read]
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Record the USER-LOCKED brand decision in PROJECT.md</name>
  <read_first>
    - .planning/PROJECT.md (## Key Decisions table at ~L454 — the format to append to)
    - .planning/phases/47-brand-foundation-verification-tooling/47-CONTEXT.md (D-47-01 USER-LOCK / D-47-02 / D-47-03 — what to record)
    - .planning/research/v1.8-design/BRAND-IDENTITY-OPTIONS.md (the dossier recommendation the user OVERRODE — cite as the superseded prior direction)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    Append ONE row to the existing `## Key Decisions` markdown table (do not restructure it). Decision cell,
    milestone-prefixed like the existing rows: "**v1.8 Phase 47 (BRND)**: Product name USER-LOCKED to 'polytoken',
    domain polytoken.ai; warm/companion voice register (tone) + node/brain logo mark adopted". Rationale cell: the
    user decided this on 2026-07-10 ("everything will be called polytoken and domain polytoken.ai. everything else
    is purged"), OVERRIDING the dossier's Direction-B rename recommendation; the exact-name collision with the
    `polytoken` CLI dev tool (docs.polytoken.dev) is EXPLICITLY ACCEPTED (not mitigated), and domain purchase +
    trademark filing remain user-gated (not done). Outcome cell: point to `docs/design/brand-guide.md` as the
    repo-of-record. Keep it one row consistent with the table's voice; do NOT write any alternate brand name.
  </action>
  <acceptance_criteria>
    - `.planning/PROJECT.md` Key Decisions table has a new row containing "polytoken" and "Phase 47": `grep -n "Phase 47" .planning/PROJECT.md` matches within the Key Decisions table. [automated]
    - The row states the decision is USER-LOCKED (2026-07-10) overriding the dossier recommendation, notes the accepted collision, notes domain/trademark are user-gated (not done), and points to `brand-guide.md`.
    - The table structure is intact (still a 3-column | Decision | Rationale | Outcome | table) — no other rows altered.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → committed history | brand-guide.md + PROJECT.md are committed; must carry no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-06 | Information disclosure | docs/design/brand-guide.md, PROJECT.md | mitigate | Docs record brand voice/posture only — no credentials, tokens, or private domains-as-secrets; reviewer confirms no secret values before commit |
| T-47-07 | Repudiation | accepted-collision + user-gated record | mitigate | The guide explicitly records the collision as USER-ACCEPTED and what was NOT done (no purchase/filing) so a future reader cannot assume trademark/domain protection that does not exist |
</threat_model>

<verification>
- brand-guide.md exists (≥40 lines), references the bans doc + real mark asset, quotes the USER-LOCK verbatim, has a do/don't table + accepted-collision note + NOT-done list.
- Repo-level brand guard is green: no purged brand name in apps/web/src or committed docs.
- PROJECT.md Key Decisions has the new Phase 47 USER-LOCKED brand row pointing to the guide.
</verification>

<success_criteria>
BRND-03 satisfied: the USER-LOCKED brand decision (polytoken / polytoken.ai), the explicitly accepted `polytoken` CLI-tool collision, and the user-gated remainder are recorded in-repo (brand guide + PROJECT.md), and the repo-level guard confirms the product is named only polytoken across app copy + committed docs.
</success_criteria>

<output>
Create `.planning/phases/47-brand-foundation-verification-tooling/47-03-SUMMARY.md` when done.
</output>
