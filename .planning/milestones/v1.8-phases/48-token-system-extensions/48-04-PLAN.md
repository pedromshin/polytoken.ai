---
phase: 48-token-system-extensions
plan: 04
type: execute
wave: 3
depends_on: [48-02]
files_modified:
  - apps/web/src/app/knowledge/_components/graph-nodes.tsx
  - apps/web/src/app/knowledge/_components/filter-rail.tsx
  - apps/web/src/app/knowledge/_components/node-detail-pane.tsx
  - apps/web/src/app/knowledge/_components/tier-edge-style.ts
  - apps/web/src/app/knowledge/_components/tier-filter-control.tsx
  - apps/web/src/app/knowledge/_components/tier-edge-style.test.ts
autonomous: true
requirements: [TOKN-04, TOKN-05]
must_haves:
  truths:
    - "xyflow node chrome differentiates entity_instance/email_component/email via the closed graph palette (bg-graph-* / border-graph-* / text-graph-*), not violet/amber/slate palette classes"
    - "The node-type filter dots and node-type detail badges use the same graph palette (single source of truth, no drift)"
    - "The knowledge tier encoding (INFERRED/EXTRACTED edges + legend) consumes tier-ladder tokens instead of overloading color.muted-foreground"
    - "Zero raw hex and zero hardcoded Tailwind palette-color classes remain in the touched knowledge components"
  artifacts:
    - path: "apps/web/src/app/knowledge/_components/graph-nodes.tsx"
      provides: "graph-palette node chrome + tokenized glow"
      contains: "graph-"
    - path: "apps/web/src/app/knowledge/_components/tier-edge-style.ts"
      provides: "tier-token edge encoding"
      contains: "tier-"
  key_links:
    - from: "graph-nodes.tsx + filter-rail.tsx + node-detail-pane.tsx"
      to: "the color.graph.* palette (48-02)"
      via: "bg-graph-* / border-graph-* / text-graph-* classes"
      pattern: "graph-(entity|email)"
    - from: "tier-edge-style.ts + graph-legend.tsx (auto)"
      to: "the color.tier.* ladder (48-02)"
      via: "hsl(var(--tier-inferred)) etc."
      pattern: "tier-(inferred|extracted)"
---

<objective>
Consume the two novel token systems from 48-02 on the knowledge canvas (D-48-04, D-48-05): replace the hardcoded `violet-500`/`amber-500`/`slate` node differentiation in the xyflow node chrome (and its mirror surfaces — the filter-rail dots and node-detail-pane badges) with the closed `color.graph.*` palette, and migrate the tier encoding (INFERRED/EXTRACTED edges + legend + filter) off the overloaded `color.muted-foreground` onto the purpose-built `color.tier.*` ladder. Capture a before/after screenshot of a node/badge surface (D-48-08).

Purpose: These are the polytoken differentiators made visible. This closes the raw-color leak the dossier flagged (flow d) and makes the tier ladder read as an intentional system.

Output: Token-driven node chrome + tier encoding, zero raw hex / palette-color, plus a visual artifact.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-token-system-extensions/48-CONTEXT.md
@.planning/phases/48-token-system-extensions/48-02-SUMMARY.md
@.claude/skills/polytoken-design-system/SKILL.md

<interfaces>
<!-- Available from 48-02: bg-tier-inferred / text-tier-inferred-foreground / bg-tier-extracted (+fg),
     and bg-graph-entity / border-graph-entity / text-graph-entity (+ email-component, email variants),
     all with working /opacity modifiers (like bg-primary/10). -->

apps/web/src/app/knowledge/_components/graph-nodes.tsx
- Six node components sharing `BASE` + `nodeClasses(color, selected)`. Hardcoded per-type color strings to replace:
  entity_instance -> `bg-violet-500/10 border-violet-500/40` + icon `text-violet-500`
  email_component -> `bg-amber-500/10 border-amber-500/40` + icon `text-amber-500`
  email -> `bg-slate-100/60 border-slate-400/40 dark:bg-slate-800/40 dark:border-slate-600/40` + icon `text-slate-500`
  knowledge_node -> raw HSL glow `shadow-[0_0_8px_hsl(164_39%_22%/0.25)]` (a raw HSL literal — replace with a var ref).
  entity_type / knowledge_node already use `bg-primary/*`; entity_type_field uses `bg-muted/*` — leave those (semantic aliases, not the graph palette).

apps/web/src/app/knowledge/_components/filter-rail.tsx
- `dotClass` per node type: `bg-violet-500/80 border-violet-500/40`, `bg-slate-400/80 border-slate-400/40`, `bg-amber-500/80 border-amber-500/40` — must match graph-nodes exactly (same palette source).

apps/web/src/app/knowledge/_components/node-detail-pane.tsx
- Node-type badges: `bg-violet-500/10 text-violet-700 border-violet-500/30` (Instance), `bg-amber-500/10 text-amber-700 border-amber-500/30` (Component).

apps/web/src/app/knowledge/_components/tier-edge-style.ts
- `tierEdgeStyle(tier)` — INFERRED: `stroke: "hsl(var(--muted-foreground))"` + dash; AMBIGUOUS: `stroke: "hsl(var(--muted-foreground))"` + opacity; EXTRACTED/undefined: `{}` (default). This OVERLOADS muted — migrate the tier strokes onto the tier ladder. Single source of truth for graph-legend.tsx's swatches too (do NOT hand-duplicate).

apps/web/src/app/knowledge/_components/tier-edge-style.test.ts — asserts the exact stroke values; UPDATE its expectations to the tier tokens.
apps/web/src/app/knowledge/_components/tier-filter-control.tsx — segmented Confirmed/Inferred/Ambiguous filter; active state currently `border-primary bg-primary text-primary-foreground`.
apps/web/src/app/knowledge/_components/graph-legend.tsx — reads `tierEdgeStyle` (auto-follows; NOT in files_modified — no edit needed, it inherits the new strokes).
</interfaces>

<constraints>
- Keep graph-nodes.tsx, filter-rail.tsx, and node-detail-pane.tsx palette values IDENTICAL to each other (one palette, three surfaces) — that is the anti-drift point of the closed palette.
- Tier semantics (D-48-04): EXTRACTED = confirmed (may adopt `tier-extracted`); INFERRED/AMBIGUOUS quieter via `tier-inferred` (distinct from muted). Preserve the dashed/opacity line encoding (the ladder colors the stroke; dash/opacity stay).
- graph-legend.tsx inherits stroke changes via tierEdgeStyle — verify visually, do NOT hand-duplicate values there.
- Apply the hover/active recipe consistently (one-step-stronger active; the 48-05 doc records it).
- Do NOT touch the locked renderer, supabase/, or .env. npm workspaces.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Graph node-type palette on node chrome, filter dots, and detail badges</name>
  <read_first>
    apps/web/src/app/knowledge/_components/graph-nodes.tsx (full)
    apps/web/src/app/knowledge/_components/filter-rail.tsx (dotClass map)
    apps/web/src/app/knowledge/_components/node-detail-pane.tsx (Instance/Component badge classes)
  </read_first>
  <action>
    In `graph-nodes.tsx` replace the hardcoded node colors with the closed graph palette (D-48-05): entity_instance `bg-violet-500/10 border-violet-500/40`+`text-violet-500` -> `bg-graph-entity/10 border-graph-entity/40`+`text-graph-entity`; email_component amber -> `bg-graph-email-component/10 border-graph-email-component/40`+`text-graph-email-component`; email slate (light+dark) -> `bg-graph-email/10 border-graph-email/40`+`text-graph-email` (the token already carries per-mode values via globals.css :root/.dark, so drop the ad-hoc `dark:` slate overrides). Replace the raw HSL glow `shadow-[0_0_8px_hsl(164_39%_22%/0.25)]` with a var-based shadow `shadow-[0_0_8px_hsl(var(--primary)/0.25)]` (zero raw HSL literal). Leave entity_type/entity_type_field/knowledge_node on their existing primary/muted aliases.
    In `filter-rail.tsx` update `dotClass` to the SAME palette: violet -> `bg-graph-entity/80 border-graph-entity/40`, slate -> `bg-graph-email/80 border-graph-email/40`, amber -> `bg-graph-email-component/80 border-graph-email-component/40`.
    In `node-detail-pane.tsx` update the node-type badges: Instance `bg-violet-500/10 text-violet-700 border-violet-500/30` -> `bg-graph-entity/10 text-graph-entity border-graph-entity/30`; Component amber -> `bg-graph-email-component/10 text-graph-email-component border-graph-email-component/30`.
    Grep the three files for `violet-|amber-|slate-` and the `hsl(164` literal; confirm zero remain.
  </action>
  <verify>
    <automated>npm run typecheck -w @polytoken/web && grep -RIno "violet-\|amber-\|bg-slate-\|border-slate-\|text-slate-\|hsl(164" apps/web/src/app/knowledge/_components/graph-nodes.tsx apps/web/src/app/knowledge/_components/filter-rail.tsx apps/web/src/app/knowledge/_components/node-detail-pane.tsx; test $? -eq 1</automated>
  </verify>
  <done>Node chrome, filter dots, and detail badges all use the closed graph palette identically; the raw HSL glow is a var ref; zero palette-color/raw-HSL leaks remain; web typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Tier-ladder tokens on the tier edge encoding + filter, legend auto-follows</name>
  <read_first>
    apps/web/src/app/knowledge/_components/tier-edge-style.ts (full)
    apps/web/src/app/knowledge/_components/tier-edge-style.test.ts (full — expectation strings to update)
    apps/web/src/app/knowledge/_components/tier-filter-control.tsx (active/inactive classes)
    apps/web/src/app/knowledge/_components/graph-legend.tsx (confirm it reads tierEdgeStyle — no edit)
  </read_first>
  <action>
    In `tier-edge-style.ts` migrate the tier strokes off the overloaded muted-foreground onto the tier ladder (D-48-04): INFERRED stroke `hsl(var(--muted-foreground))` -> `hsl(var(--tier-inferred))` (keep `strokeDasharray: "5 3"`); AMBIGUOUS stroke -> `hsl(var(--tier-inferred))` (keep the opacity/labelStyle quieting). Leave EXTRACTED/undefined returning `{}` (React Flow default solid) — optionally set EXTRACTED to an explicit `hsl(var(--tier-extracted))` stroke if it reads better; if so, update graph-legend's default-swatch fallback expectation accordingly. Update `tier-edge-style.test.ts` expected stroke strings to the new tier-token values.
    In `tier-filter-control.tsx` keep the active "Confirmed" semantics coherent with the ladder — the active segment may adopt `bg-tier-extracted text-tier-extracted-foreground border-tier-extracted` (confirmed = extracted) instead of generic primary, so the filter visually ties to the tier system; inactive stays muted. Keep arrow-key/radiogroup behavior byte-identical.
    graph-legend.tsx needs NO edit — it derives swatch stroke from `tierEdgeStyle`; verify the legend now shows tier-token strokes.
  </action>
  <verify>
    <automated>npm run typecheck -w @polytoken/web && npm run test -w @polytoken/web -- src/app/knowledge/_components/tier-edge-style.test.ts</automated>
  </verify>
  <done>Tier edges + legend + filter consume the tier ladder (not muted); tier-edge-style test updated and green; dashed/opacity encoding preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Before/after screenshot of the knowledge node/tier surface</name>
  <read_first>
    apps/web/package.json (the `screenshot:review` script)
    .planning/phases/47-brand-foundation-verification-tooling/47-05-SUMMARY.md (harness usage + output)
  </read_first>
  <action>
    Run the Phase 47 screenshot harness (`npm run screenshot:review`) to capture the `/knowledge` canvas node + legend surface (graph palette + tier encoding) as the D-48-08 visual artifact, and commit it to the harness's committed output directory. Keep it cheap — reuse an existing harness target if `/knowledge` is already covered; add a target only if needed. If a running server / live graph data is unavailable in this concurrent-session window, capture what is cheap and record in the SUMMARY which surface was/was not captured (do NOT touch supabase/.env).
  </action>
  <verify>
    <human-check>The committed artifact shows entity/email-component/email nodes in the closed graph palette and the tier legend/edges in the tier ladder; the before/after difference from raw violet/amber/slate is visible.</human-check>
  </verify>
  <done>A before/after visual artifact of the knowledge node/tier surface is produced and committed (or the cheap subset captured with the gap recorded).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge tier value -> stroke style | `kne-*` edge tiers map to a fixed style table; structural FK edges never receive a tier (enforced by the caller) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-08 | Tampering | tier/graph classes in TSX | mitigate | Node/edge colors now reference tokens (`hsl(var(--graph-*))`, `hsl(var(--tier-*))`) — zero raw hex / raw HSL literal, verified by grep gate; keeps D-03/STYLE-03 intact. |
| T-48-09 | Spoofing | tier encoding meaning | accept | Tier tokens only recolor an existing, already-authorized edge-tier map; the tier value still comes from the vetted `kne-*` union seam, not user input. |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No package installs (className/style swaps + harness run). |
</threat_model>

<verification>
- `npm run typecheck -w @polytoken/web` clean; tier-edge-style test green.
- Zero `violet-`/`amber-`/`slate-` palette classes or `hsl(164` literals remain in the three graph files.
- Legend/edges render tier-ladder strokes; nodes render the closed graph palette; screenshot artifact committed (or cheap subset + gap recorded).
</verification>

<success_criteria>
The xyflow node chrome, filter dots, and detail badges differentiate node types via the closed graph palette; the tier encoding (edges + legend + filter) consumes the purpose-built tier ladder instead of overloading muted; zero raw color leaks; a visual artifact evidences it.
</success_criteria>

<output>
Create `.planning/phases/48-token-system-extensions/48-04-SUMMARY.md` when done.
</output>
