---
phase: 48-token-system-extensions
plan: 05
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - docs/design/hover-active-convention.md
  - docs/design/breakpoint-decision.md
  - docs/design/brand-guide.md
autonomous: true
requirements: [TOKN-06, TOKN-07]
must_haves:
  truths:
    - "A single documented hover/active-state derivation rule exists in docs/design/, with the recipe and worked examples applied to the chips/badges this phase touches"
    - "A breakpoint-awareness decision doc records the chosen shape (pack tokens stay breakpoint-static; md=768px is the canvas->feed switch line), the touch-target >=44px guard, and what Phase 50 may/may not add"
    - "The brand guide cites both design conventions"
  artifacts:
    - path: "docs/design/hover-active-convention.md"
      provides: "the one hover/active derivation rule + examples"
      contains: "hover"
    - path: "docs/design/breakpoint-decision.md"
      provides: "the TOKN-07 breakpoint decision + Phase 50 scope boundary"
      contains: "768"
    - path: "docs/design/brand-guide.md"
      provides: "citations to both convention docs"
      contains: "hover-active-convention"
  key_links:
    - from: "docs/design/breakpoint-decision.md"
      to: "the .touch-target utility + md convention (48-01)"
      via: "documented mechanism reference"
      pattern: "touch-target"
    - from: "docs/design/hover-active-convention.md"
      to: "the resting aliases (accent/success/tier)"
      via: "derivation recipe from resting token"
      pattern: "accent"
---

<objective>
Record the two design CONVENTIONS this phase must land (D-48-06, D-48-07): ONE hover/active-state derivation rule (so interactive-state styling stops being ad-hoc per component), and the breakpoint-awareness DECISION that gates Phase 50 (the dossier's "largest structural gap — its own design conversation"). Both are documentation deliverables in `docs/design/`, cited from the brand guide. The minimal breakpoint MECHANISM (the `.touch-target` guard + md convention) was already built in 48-01; this plan is the recorded decision that mechanism implements.

Purpose: The deliverable is the written RULE + DECISION (with worked examples), not a component sweep — Phase 49 applies the hover/active rule broadly and Phase 50 builds on the breakpoint decision.

Output: two design-convention docs + brand-guide citations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-token-system-extensions/48-CONTEXT.md
@.planning/phases/48-token-system-extensions/48-01-SUMMARY.md
@.planning/research/v1.8-design/DESIGN-PATTERN-DOSSIER.md
@docs/design/brand-guide.md
@docs/design/product-register-and-bans.md

<interfaces>
<!-- Mechanism + tokens the docs describe (already shipped in 48-01/48-02). -->
- 48-01 added: `rounded-pill`, `bg-success`/`text-success-foreground`, `font-code`, and a `.touch-target` utility (min 44px) in apps/web/src/app/globals.css, plus the documented md (768px) canvas->feed convention comment.
- Resting aliases the hover/active recipe derives from: `color.accent`/`accentForeground` (hover surface), `color.muted`/`mutedForeground`, `color.success`, `color.tier.*`.
- Chips/badges this phase touched (worked examples): the ProvenanceLink citation chip (`hover:bg-accent hover:text-accent-foreground`), the success confirm affordances (`bg-success hover:bg-success/90`), the knowledge tier edges/filter (tier ladder).
- docs/design/ already contains: brand-guide.md (Phase 47), product-register-and-bans.md, references/*.md.
</interfaces>

<constraints>
- Docs only — no code edits. Do NOT restate token VALUES (they live in packs.ts); describe the RULE/DECISION and reference alias names.
- The hover/active rule must be ONE rule (a fixed recipe), not a per-component table (D-48-06).
- The breakpoint doc REJECTS a per-breakpoint token dimension this milestone (D-48-07 / deferred list) — say so explicitly and scope what Phase 50 may add.
- No supabase/.env access.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the hover/active-state derivation rule (TOKN-06)</name>
  <read_first>
    docs/design/brand-guide.md (voice + structure to match)
    docs/design/product-register-and-bans.md (existing convention-doc style)
    .planning/research/v1.8-design/DESIGN-PATTERN-DOSSIER.md (flow c hover/active gap)
    apps/web/src/components/provenance-link.tsx (the chip hover example)
  </read_first>
  <action>
    Create `docs/design/hover-active-convention.md`. Record ONE derivation rule (D-48-06): interactive-state styling derives from the resting alias via a fixed recipe — hover = move to the accent surface pair (`hover:bg-accent hover:text-accent-foreground`) for neutral/ghost elements, or a one-step-stronger fill (`hover:bg-{alias}/90`) for filled semantic elements (primary/success/destructive); active/pressed = one step stronger again (e.g. `/80` or the accent-foreground pair); focus-visible keeps the existing `ring-ring ring-offset` pattern. State that this is token-driven where cheap and a documented utility where not. Include a short worked-examples section applying the rule to the exact chips/badges this phase touched: the ProvenanceLink citation chip, the success confirm affordance, and the knowledge tier filter/edges. Note Phase 49 applies the rule broadly; this doc is the source of truth.
  </action>
  <verify>
    <automated>test -f docs/design/hover-active-convention.md && grep -qi "hover" docs/design/hover-active-convention.md && grep -qi "active" docs/design/hover-active-convention.md && grep -qi "accent" docs/design/hover-active-convention.md && grep -qi "provenancelink\|citation chip" docs/design/hover-active-convention.md</automated>
  </verify>
  <done>The doc exists with a single derivation rule + worked examples referencing this phase's chips/badges.</done>
</task>

<task type="auto">
  <name>Task 2: Write the breakpoint-awareness decision (TOKN-07) + brand-guide citations</name>
  <read_first>
    .planning/research/v1.8-design/DESIGN-PATTERN-DOSSIER.md (flow e — mobile/breakpoint gap)
    .planning/phases/48-token-system-extensions/48-01-SUMMARY.md (the .touch-target utility + md convention as built)
    docs/design/brand-guide.md (where to add citations)
  </read_first>
  <action>
    Create `docs/design/breakpoint-decision.md` recording the D-48-07 decision. Chosen shape: pack tokens stay breakpoint-STATIC; breakpoint behavior lives in a small set of documented layout primitives / Tailwind conventions, NOT in the token dimension. Answer the three required questions: (1) which breakpoint switches canvas->feed — the Tailwind `md` breakpoint (768px) is the switch line; (2) how `spacing.density` interacts with small screens — density stays a single per-pack scalar (airiness), and a minimum touch-target guard (`.touch-target`, >=44px) protects interactive elements regardless of pack density, even under denser packs like linear-clean; (3) what Phase 50 may/may not add — MAY: layout primitives/Tailwind conventions that collapse canvas surfaces to an inline list/feed below md and a density mechanism ONLY if genuinely needed by the feed layout; MAY NOT: a per-breakpoint token dimension (explicitly rejected this milestone). Cite the market evidence briefly (ChatGPT removed Canvas 2026-05-28; Claude Artifacts render inline on mobile) as the rationale for inline-first-on-mobile. Reference the `.touch-target` utility and md convention shipped in 48-01 as the minimal working mechanism.
    Then append a short "Design conventions" pointer in `docs/design/brand-guide.md` citing both `hover-active-convention.md` and `breakpoint-decision.md`.
  </action>
  <verify>
    <automated>test -f docs/design/breakpoint-decision.md && grep -q "768" docs/design/breakpoint-decision.md && grep -qi "touch-target" docs/design/breakpoint-decision.md && grep -qi "canvas" docs/design/breakpoint-decision.md && grep -q "hover-active-convention" docs/design/brand-guide.md && grep -q "breakpoint-decision" docs/design/brand-guide.md</automated>
  </verify>
  <done>The breakpoint decision doc answers all three required questions, rejects a per-breakpoint token dimension, and references the touch-target/md mechanism; the brand guide cites both convention docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Documentation-only plan; no runtime trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-10 | Tampering | docs content | accept | Markdown documentation only; no code, no tokens, no dependencies introduced — no attack surface. |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No package installs (docs only). |
</threat_model>

<verification>
- Both convention docs exist under docs/design/ with the required content (grep-gated above).
- brand-guide.md cites both docs.
</verification>

<success_criteria>
A single hover/active derivation rule and a breakpoint-awareness decision (with the minimal touch-target/md mechanism referenced) are recorded in docs/design/ and cited from the brand guide — the conventions Phases 49 and 50 build on.
</success_criteria>

<output>
Create `.planning/phases/48-token-system-extensions/48-05-SUMMARY.md` when done.
</output>
