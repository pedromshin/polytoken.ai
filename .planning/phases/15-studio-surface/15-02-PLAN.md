---
phase: 15-studio-surface
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - apps/web/src/app/studio/_components/spec-renderer-island.tsx
  - apps/web/src/app/studio/preview/_components/spec-renderer-island.tsx
  - apps/web/src/app/studio/preview/page.tsx
  - apps/web/src/app/studio/_components/catalog-browser-island.tsx
  - apps/web/src/app/studio/_components/studio-tabs.tsx
  - apps/web/src/app/studio/page.tsx
  - apps/web/src/components/app-sidebar.tsx
autonomous: true
requirements: [STDO-01, STDO-02]
user_setup: []

must_haves:
  truths:
    - "A developer navigating to /studio sees a browseable catalog of all 10 whitelisted @nauta/ui components, each with type+description, a live rendered example, a prop schema table, and slot rules (STDO-01, success criterion 1, D-10/D-11/D-12)"
    - "Each catalog example renders through the SAME production SpecRenderer + COMPONENT_REGISTRY — no second/stub renderer exists in apps/web (STDO-02, D-07/D-15)"
    - "The catalog browser imports NAUTA_CATALOG directly into a client island — Zod propsSchema + React component refs never cross the server→client boundary (D-10)"
    - "/studio is a server-component shell mirroring /studio/preview: frosted SidebarInset, h-12 header, server-rendered REGISTRY_VERSION chip, Tabs[Catalog,Sandbox] + Showcase link (D-01/D-13/D-14)"
    - "The Studio sidebar nav points at /studio; /studio/preview stays active-under-/studio (D-14)"
  artifacts:
    - path: "apps/web/src/app/studio/page.tsx"
      provides: "/studio server-component landing shell with Tabs"
      min_lines: 30
    - path: "apps/web/src/app/studio/_components/catalog-browser-island.tsx"
      provides: "Catalog browser client island (direct NAUTA_CATALOG import, per-entry cards, live examples, prop tables)"
      contains: "NAUTA_CATALOG"
    - path: "apps/web/src/app/studio/_components/spec-renderer-island.tsx"
      provides: "Shared dynamic(ssr:false) SpecRenderer island (lifted from preview/_components/)"
      exports: ["SpecRendererIsland"]
    - path: "apps/web/src/app/studio/_components/studio-tabs.tsx"
      provides: "Client Tabs shell: Catalog | Sandbox | Showcase-link"
      contains: "TabsList"
  key_links:
    - from: "apps/web/src/app/studio/_components/catalog-browser-island.tsx"
      to: "@nauta/genui/catalog NAUTA_CATALOG + @nauta/genui/studio describePropsSchema"
      via: "direct client import + map over Object.values"
      pattern: "NAUTA_CATALOG|describePropsSchema"
    - from: "apps/web/src/app/studio/_components/catalog-browser-island.tsx"
      to: "SpecRendererIsland (shared, production SpecRenderer)"
      via: "wrappedExample SpecRoot per entry"
      pattern: "SpecRendererIsland"
    - from: "apps/web/src/app/studio/preview/_components/spec-renderer-island.tsx"
      to: "../../_components/spec-renderer-island"
      via: "re-export after lift"
      pattern: "export"
---

<objective>
Build the /studio landing shell, the catalog browser, and the shared renderer island — delivering
STDO-01 (browseable catalog) and STDO-02's catalog-side reuse (every example renders through the
production SpecRenderer, never a stub).

Three pieces:
1. Lift the existing `SpecRendererIsland` from `studio/preview/_components/` to a shared
   `studio/_components/` module so both /studio and /studio/preview import one island (D-07).
2. The `/studio` server-component page (mirrors `studio/preview/page.tsx`): frosted shell, h-12 header,
   server-rendered REGISTRY_VERSION chip, and the Tabs[Catalog | Sandbox | Showcase-link] client shell.
   The Sandbox tab gets a placeholder panel this plan; Plan 15-03 fills it.
3. The `CatalogBrowserIsland` — directly imports `NAUTA_CATALOG` (D-10, serialization boundary), renders
   per-entry cards with the four D-11 facets: type+description, a live example via the shared
   SpecRendererIsland, a prop table via `describePropsSchema` (from 15-01), and slot chips.
Plus the one-line sidebar repoint (D-14).

Purpose: STDO-01 success criterion 1 + STDO-02's "same renderer, never a stub" on the catalog side.
Output: /studio renders the Catalog tab fully; Sandbox tab is a placeholder pending 15-03.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-studio-surface/15-CONTEXT.md
@.planning/phases/15-studio-surface/15-UI-SPEC.md

@apps/web/src/app/studio/preview/page.tsx
@apps/web/src/app/studio/preview/_components/spec-renderer-island.tsx

<interfaces>
<!-- The exact genui surfaces this plan consumes. No exploration needed. -->

@nauta/genui/catalog:
```
NAUTA_CATALOG: ComponentRegistry            // Readonly<Record<string, AnyManifestEntry>>, 10 entries
// ManifestEntry fields the browser reads:
//   type: SpecNodeType; description: string; example: Record<string, unknown>;
//   propsSchema: ZodType; lockedProps?: ReadonlyArray<keyof TProps>;
//   slots?: ReadonlyArray<string>; acceptsChildren?: boolean; component: ComponentType
```

@nauta/genui/studio (shipped in 15-01):
```
describePropsSchema(schema, lockedProps?) -> ReadonlyArray<{ name; typeLabel; required; locked }>
```

@nauta/genui/schema:
```
type SpecRoot = { v: 1; root: {...}; data?: ... }   // wrap entry.example per UI-SPEC §14
```

@nauta/genui/registry (server-only — Node crypto, render in page.tsx server component):
```
REGISTRY_VERSION: { catalogId: string; version: string }   // chip: version.slice(0,8)
```

Shared island (after lift): SpecRendererIsland({ spec: SpecRoot; data?: Record<string,unknown> })
— dynamic(ssr:false) wrapper; SpecRenderer defaults registry = COMPONENT_REGISTRY (no registry prop).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift the shared SpecRendererIsland; repoint preview + sidebar</name>
  <files>apps/web/src/app/studio/_components/spec-renderer-island.tsx, apps/web/src/app/studio/preview/_components/spec-renderer-island.tsx, apps/web/src/app/studio/preview/page.tsx, apps/web/src/components/app-sidebar.tsx</files>
  <read_first>
    - apps/web/src/app/studio/preview/_components/spec-renderer-island.tsx (the exact island to move — keep its doc-comment + dynamic(ssr:false) + default-registry rationale verbatim)
    - apps/web/src/app/studio/preview/page.tsx (line 15 import of the island — repoint to the shared module)
    - apps/web/src/components/app-sidebar.tsx lines 37-51 (LIVE_NAV_ITEMS + isActiveRoute — change the Studio href only)
    - .planning/phases/15-studio-surface/15-CONTEXT.md D-07 (lift recommended so both routes share one island) + D-14 (sidebar repoint) + UI-SPEC §5 (SpecRendererIsland lifted to studio/_components/)
  </read_first>
  <action>
    Create `apps/web/src/app/studio/_components/spec-renderer-island.tsx` containing the EXACT content of the current `studio/preview/_components/spec-renderer-island.tsx` (the "use client" dynamic(ssr:false) SpecRenderer wrapper, default-registry-via-COMPONENT_REGISTRY rationale, SpecRendererIslandProps). Replace `studio/preview/_components/spec-renderer-island.tsx` with a one-line re-export: `export { SpecRendererIsland } from "../../_components/spec-renderer-island";` (plus its `SpecRendererIslandProps` type) so the preview page keeps working without changing its import, OR repoint preview/page.tsx's import directly to `../_components/spec-renderer-island` and delete the preview-local island — pick the cleaner import graph (D-07 grants this discretion); whichever is chosen, /studio/preview must still compile and render SHOWCASE_SPEC unchanged. In app-sidebar.tsx, change the Studio LIVE_NAV_ITEMS entry `href` from `/studio/preview` to `/studio` (single-line change; keep label "Studio" + FlaskConical icon). The existing isActiveRoute already lights the nav for both /studio and /studio/preview via the startsWith branch — do not change isActiveRoute.
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` clean in apps/web.
    - The shared island at `studio/_components/spec-renderer-island.tsx` is the single source; grep confirms exactly ONE `dynamic(` SpecRenderer wrapper definition under apps/web/src/app/studio (the preview path re-exports, does not redefine — no second renderer, D-15).
    - app-sidebar.tsx Studio href === "/studio"; grep confirms `/studio/preview` no longer appears as a nav href.
    - /studio/preview still imports a working SpecRendererIsland (re-export or repointed import resolves).
  </acceptance_criteria>
  <done>One shared SpecRendererIsland; preview repointed; sidebar Studio href = /studio; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Build the /studio server shell + Tabs (Catalog | Sandbox-placeholder | Showcase link)</name>
  <files>apps/web/src/app/studio/page.tsx, apps/web/src/app/studio/_components/studio-tabs.tsx</files>
  <read_first>
    - apps/web/src/app/studio/preview/page.tsx (the server shell to mirror: h-12 header, ml-auto chips, REGISTRY_VERSION server-side chip, layout classes)
    - apps/web/src/app/layout.tsx (the frosted shell + SidebarInset the page renders inside — reference only, do not edit)
    - .planning/phases/15-studio-surface/15-UI-SPEC.md §6 (page shell + header + Tab structure) + §10 (header/tab copy) + §11 (Tabs a11y: TabsContent aria-labels, Showcase link aria-label) + D-01/D-13
    - .planning/phases/15-studio-surface/15-CONTEXT.md D-01 (Tabs in one route; Showcase as link to /studio/preview)
  </read_first>
  <action>
    Create `apps/web/src/app/studio/page.tsx` as a server component (export const metadata; default function) mirroring studio/preview/page.tsx: a `main` with the h-12 header (`<h1 className="text-sm font-semibold text-foreground">Studio</h1>`) and an `ml-auto flex items-center gap-2` chip group containing a static `<Badge variant="secondary">v1</Badge>` and the server-rendered `<Badge variant="secondary" className="font-mono text-xs">Registry {REGISTRY_VERSION.version.slice(0,8)}</Badge>` (REGISTRY_VERSION imported from @nauta/genui/registry, consumed server-side per the T-12-15 Node-crypto constraint — keep it out of any "use client" module). Below the header, render `<StudioTabs />`. Create `apps/web/src/app/studio/_components/studio-tabs.tsx` ("use client") using @nauta/ui/tabs: TabsList (shrink-0, px-4, border-b) with TabsTrigger "Catalog" + TabsTrigger "Sandbox" + a `next/link` to `/studio/preview` styled as the "Showcase" affordance (`text-sm text-muted-foreground hover:text-foreground flex items-center gap-1`, with an `ExternalLink` size-3 aria-hidden icon, `aria-label="Open Component Showcase"`) — the Showcase is a link, NOT a TabsContent (D-01). TabsContent "catalog" (aria-label="Component catalog") renders `<CatalogBrowserIsland />`. TabsContent "sandbox" (aria-label="Generation sandbox") renders a placeholder `<div>` ("Sandbox — coming in 15-03") for now; Plan 15-03 replaces this. Use only existing tokens/typography per §2-4 (no new design system, D-13). Layout: Tabs is `flex-1 min-h-0 flex flex-col`; each TabsContent is `flex-1 overflow-y-auto`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` + `pnpm build` green; the build output lists a `/studio` route that compiles.
    - REGISTRY_VERSION is imported only in the server page.tsx (grep: no `@nauta/genui/registry` import inside any "use client" studio module — Node-crypto server-only, T-12-15).
    - studio-tabs.tsx renders TabsList with exactly two TabsTrigger (Catalog, Sandbox) + one next/link Showcase affordance; the Showcase is a Link to /studio/preview, not a TabsContent.
    - TabsContent wrappers carry the §11 aria-labels ("Component catalog" / "Generation sandbox").
  </acceptance_criteria>
  <done>/studio renders the frosted shell + header chips + Tabs; Catalog tab hosts the browser; Sandbox tab is a placeholder; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Build the CatalogBrowserIsland — direct NAUTA_CATALOG import, four facets per entry, live examples via the production renderer</name>
  <files>apps/web/src/app/studio/_components/catalog-browser-island.tsx</files>
  <read_first>
    - packages/genui/src/catalog/types.ts (ManifestEntry fields the cards read: type/description/example/propsSchema/lockedProps/slots/acceptsChildren)
    - packages/genui/src/catalog/index.ts (NAUTA_CATALOG export) + packages/genui/src/catalog/manifest.ts lines 1-90 (real entry shapes — example objects, lockedProps, slots, a11y-required props)
    - apps/web/src/app/studio/_components/spec-renderer-island.tsx (the shared island Task 1 created — the live example mounts through it)
    - packages/genui/src/studio/describe-props-schema.ts (the prop-table source from 15-01)
    - .planning/phases/15-studio-surface/15-UI-SPEC.md §6 Catalog Browser (card structure, prop table columns, slot chips, filter), §8 (catalog filter no-results state), §11 (live-example region role + prop table aria-label), §14 (wrappedExample SpecRoot shape)
    - .planning/phases/15-studio-surface/15-CONTEXT.md D-10 (direct client import, serialization boundary) + D-11 (four facets) + D-12 (card grid + optional filter)
  </read_first>
  <action>
    Create `apps/web/src/app/studio/_components/catalog-browser-island.tsx` ("use client"). Import `NAUTA_CATALOG` directly from @nauta/genui/catalog (D-10 — never as a server prop; Zod schemas + component refs are unserializable), `describePropsSchema` from @nauta/genui/studio, `SpecRendererIsland` from the shared sibling module, and the @nauta/ui Card/Input/Badge components. Hold a local `useState` filter string; an Input (`aria-label="Filter catalog components"`, `placeholder="Filter components…"`, `max-w-sm`) filters `Object.values(NAUTA_CATALOG)` by case-insensitive substring over `type`/`description`. Render the matched entries in a `grid grid-cols-1 gap-4 xl:grid-cols-2`; on zero matches render the §8 no-results line (`aria-live="polite"` on the grid container). For each entry render a Card with the four D-11 facets per §6: (1) CardHeader — `entry.type` in a mono code chip + `entry.description`; (2) live example region (`role="region" aria-label={\`Live example: ${entry.type}\`}`) rendering `<SpecRendererIsland spec={wrappedExample} />` where `wrappedExample` is built per UI-SPEC §14: `{ v: 1, root: { type: entry.type, props: entry.example, children: [] } }` (construct it inside this client module — never passed from a server component); (3) a prop table built from `describePropsSchema(entry.propsSchema, entry.lockedProps as ReadonlyArray<string> | undefined)` with columns Name/Type/Required/Locked per the §6 prop-table spec (mono Name+Type cells, "required"/"—", a `<Badge variant="outline">locked</Badge>` when locked); if `describePropsSchema` returns [] render "Props — (unavailable)" per §12; (4) slot chips — a `Badge variant="secondary" font-mono` per `entry.slots` name + a `children: yes/no` badge from `entry.acceptsChildren`, or "none" when both empty. Use only existing tokens/classes from §2-4 (D-13). No eval/Function/dangerouslySetInnerHTML (D-15). All spec rendering goes through the shared SpecRendererIsland — no bespoke render path.
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` + `pnpm build` green; /studio compiles with the catalog browser.
    - grep confirms `NAUTA_CATALOG` is imported directly in the "use client" catalog-browser-island.tsx (D-10) and that the browser maps `Object.values(NAUTA_CATALOG)` (lists ALL entries — drift-proof; the count equals the real catalog length, currently 10).
    - grep confirms every example renders via `SpecRendererIsland` — no second SpecRenderer / COMPONENT_REGISTRY definition anywhere in apps/web (STDO-02 reuse, D-15).
    - grep confirms zero `eval(` / `new Function` / `dangerouslySetInnerHTML` in the new studio files (D-15 gate).
    - The prop table reads from `describePropsSchema` (live Zod), not a hand-written per-entry doc (drift-proof, D-11).
  </acceptance_criteria>
  <done>The Catalog tab lists every NAUTA_CATALOG entry with type+description, a live production-renderer example, a live-Zod prop table, and slot chips; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| genui modules → web client island | Zod schemas + React component refs in NAUTA_CATALOG/COMPONENT_REGISTRY are imported directly client-side (cannot SSR-serialize) |
| catalog example spec → SpecRenderer | The `entry.example` is the CI-verified (CTLG-04) example wrapped in a SpecRoot, rendered by the production interpreter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Tampering | renderer reuse (STDO-02) | mitigate | The catalog examples mount the EXACT shared `SpecRendererIsland` (production SpecRenderer + default COMPONENT_REGISTRY). grep asserts no second renderer/registry defined in apps/web — only the one shared island. No stub path. |
| T-15-06 | Tampering | render sink | mitigate | No `eval`/`Function`/`dangerouslySetInnerHTML` in any new studio file (grep gate). The wrappedExample is plain data rendered via createElement inside SpecRenderer (GR-01/SPEC-02/D-15). |
| T-15-07 | Information disclosure | REGISTRY_VERSION (Node crypto) | mitigate | REGISTRY_VERSION is consumed only in the server page.tsx; grep asserts it is NOT imported in any "use client" studio module (T-12-15 — keeps Node crypto out of the browser bundle). |
| T-15-08 | DoS | catalog example specs | accept | Examples are the 10 CI-verified `entry.example` objects (CTLG-04) — bounded, trusted, non-user-supplied. Each is a single-node SpecRoot; no unbounded recursion. |
| T-15-SC | Tampering | npm installs | mitigate | NO new packages — all @nauta/ui + @nauta/genui + next/react are already present. No install task ⇒ no legitimacy gate needed. |
</threat_model>

<verification>
- `cd apps/web && pnpm typecheck && pnpm build` green; `/studio` route compiles.
- Catalog tab enumerates all NAUTA_CATALOG entries (count === real catalog length); each example renders through the shared production SpecRendererIsland.
- D-10 serialization boundary honored: NAUTA_CATALOG imported directly in a client island.
- D-15 grep gate: no eval/Function/dangerouslySetInnerHTML; no second renderer/registry in apps/web.
- D-14: Studio nav href === /studio; /studio/preview still renders SHOWCASE_SPEC.
</verification>

<success_criteria>
- STDO-01 (success criterion 1): /studio shows a browseable catalog of all whitelisted @nauta/ui components, each with prop schema, slot rules, and a rendered example.
- STDO-02 (catalog side): every rendered example flows through the same production SpecRenderer + COMPONENT_REGISTRY — not a stub (success criterion 2, catalog half).
- Design fidelity: /studio shell is indistinguishable from /studio/preview (D-13); no new tokens.
</success_criteria>

<output>
Create `.planning/phases/15-studio-surface/15-02-SUMMARY.md` when done.
</output>
