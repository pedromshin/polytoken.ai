---
phase: 55-platform-migration-tailwind-v4-react-19
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - apps/web/src/app/globals.css
  - apps/web/tailwind.config.ts
  - packages/tailwind-config/web.ts
  - packages/tailwind-config/base.ts
  - packages/ui/tailwind.config.ts
  - apps/web/src/app/chat/_canvas/chat-canvas.tsx
  - apps/web/src/app/chat/_canvas/panel-theme-scope.tsx
  - apps/web/src/app/chat/_canvas/knowledge-preview-mini-graph.tsx
  - apps/web/src/app/knowledge/_components/graph-legend.tsx
  - apps/web/src/app/knowledge/_components/graph-nodes.tsx
  - apps/web/src/app/knowledge/_components/tier-edge-style.ts
  - apps/web/src/app/knowledge/_components/tier-edge-style.test.ts
  - packages/ui/src/sidebar.tsx
  - packages/ui/src/spreadsheet-grid/theme.css
  - packages/ui/src/spreadsheet-grid/SpreadsheetGrid.tsx
  - packages/ui/src/spreadsheet-grid/conditional-formatting-dialog.tsx
  - packages/genui/src/theme/themed-wrapper.tsx
  - packages/genui/src/theme/tokens.ts
autonomous: true
requirements: [STCK-01]
must_haves:
  truths:
    - "globals.css :root/.dark tokens are full oklch(...) color functions (not bare HSL triplets), mapped into Tailwind via @theme inline"
    - "globals.css's OWN internal token consumers (React Flow Controls fill, .scrollbar-token, .generating-ring gradient, modal/dropdown box-shadow keyframes) drop the hsl() wrapper — zero hsl(var(--x)) remains anywhere in globals.css"
    - "packages/ui and packages/genui classes survive the production purge because explicit @source directives register their source trees"
    - "Every direct hsl(var(--x)) call site consuming a globals.css token drops the hsl() wrapper to var(--x)/color-mix(); no hsl(oklch()) is ever produced"
    - "The genui runtime re-theme surface (packs.ts) stays HSL; ThemedRoot wraps its color vars in hsl() so overrides remain valid color functions"
    - "The token-render guard and full E2E suite stay green — no silent transparent/unstyled regression on any canvas/graph/sidebar/spreadsheet surface or the React Flow Controls icon"
  artifacts:
    - path: "apps/web/src/app/globals.css"
      provides: "oklch tokens + @theme inline mappings + @source directives + native @theme animation/radius/breakpoint port + converted internal consumers"
      contains: "oklch("
    - path: "apps/web/src/app/globals.css"
      provides: "monorepo source registration"
      contains: "@source"
  key_links:
    - from: "apps/web/src/app/globals.css @theme inline"
      to: ":root/.dark oklch vars"
      via: "--color-* : var(--*)"
      pattern: "--color-[a-z].*var\\(--"
    - from: "packages/genui/src/theme/themed-wrapper.tsx"
      to: "packs.ts HSL color vars"
      via: "hsl() wrapping of color-group vars only"
      pattern: "hsl\\("
---

<objective>
Stage 2 — the crux and highest-risk stage (55-RESEARCH.md Summary + Pitfalls 1/2/4). Port globals.css from bare-HSL-triplet CSS vars to full `oklch(...)` color functions in the shadcn v4 shape (`@theme inline` + `:root`/`.dark`), port the small JS theme surface natively to `@theme`/`@keyframes`, register the two sibling packages with explicit `@source`, remove the `@config` bridge, and — atomically in the same stage — fix EVERY direct `hsl(var(--x))` call site (including globals.css's OWN internal consumers) so it never wraps an oklch value.

Purpose: This is the single biggest mechanical change surface in the phase, and its failures are INVISIBLE to `npm run build` (RESEARCH stresses build success ≠ migration success here): a missing `@source` silently purges vendored styling in production, and a leftover `hsl(var(--oklch-value))` is invalid CSS the browser drops silently. The oklch port and the call-site fixes MUST land together — separating them would leave surfaces rendering transparent between waves and fail the guard. This plan has an ELEVATED blast radius (17+ files across three packages plus globals.css's own internal rules) — run the gates between Task 1 and Task 2 (do not batch to the end).

Output: globals.css in the canonical shadcn v4 oklch shape with `@source` + native `@theme` animations/radius/breakpoints AND zero internal `hsl(var(--x))`; all ~16 external call-site files converted; the genui re-theme surface adapted; the obsolete JS configs neutralized. Verified by executable, non-visual gates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

Gate commands (scope every command with `-w`; root `npm run test`/`typecheck` run the Python service):
- Production build (real purge path): `npm run web:build`
- Vitest: `npm run test -w @polytoken/web` (NOTE: token-contrast + token-registration WILL fail after this stage — expected; rewritten in 55-03)
- Typecheck: `npm run typecheck -w @polytoken/web`, `npm run typecheck -w @polytoken/ui`
- E2E (includes the token-render guard from 55-01): `npm run test:e2e -w @polytoken/web`
- Screenshot capture (artifact only, not a gate): `npm run screenshot:review -w @polytoken/web`

ELEVATED BLAST RADIUS: run `npm run web:build` + `npm run test:e2e -w @polytoken/web` (the token-render guard) AFTER Task 1 and again AFTER Task 2, so a regression bisects to the CSS port vs the call-site fixes rather than surfacing only at the end.
</execution_context>

<context>
@.planning/phases/55-platform-migration-tailwind-v4-react-19/55-RESEARCH.md
@.planning/phases/55-platform-migration-tailwind-v4-react-19/55-01-SUMMARY.md

<interfaces>
Token surface (from apps/web/src/app/globals.css, read in full before editing):
- :root + .dark each declare bare-HSL-triplet vars for: background, foreground, card(+fg), popover(+fg), primary(+fg), secondary(+fg), muted(+fg), accent(+fg), destructive(+fg), success(+fg), border, input, ring, chart-1..5, tier-extracted(+fg), tier-inferred(+fg), graph-entity/graph-email-component/graph-email (+fg each), sidebar-* (8, some `var(--x)` aliases), plus non-color: radius, radius-xl, radius-2xl, radius-pill, font-code, elevation-1..3 (shadow strings that EMBED `hsl(var(--border) / ...)` and literal `hsl(164 25% 15% / ...)`).
- brand primary today: `164 39% 22%` (light + dark).
- globals.css ALSO consumes its own tokens via `hsl(var(--x))` OUTSIDE the :root/.dark blocks (in @layer components/@layer utilities) — these are NOT token declarations, they are call sites and MUST be converted (they become invalid `hsl(oklch())` and vanish silently otherwise):
  - `.react-flow__controls-button svg { fill: hsl(var(--foreground)); }` — Controls icons on /knowledge + /chat (guarded by 55-01's token-render spec).
  - `.scrollbar-token` — `scrollbar-color: hsl(var(--border)) transparent;` and `background-color: hsl(var(--border));`.
  - `.generating-ring::before` gradient stops — `hsl(var(--primary) / 0.85)`, `hsl(var(--primary))`, `hsl(var(--primary) / 0.85)` (consumed by generating-ring.tsx: composer, message-turn, genui-panel-node, model-picker, delete-conversation-dialog, /studio islands).
  - `.t-modal-reveal` / `.t-dropdown-reveal` box-shadow keyframes — `hsl(var(--foreground) / 0)` and `hsl(var(--foreground) / 0.12)`.

JS theme to port to native @theme (from packages/tailwind-config/base.ts + web.ts + apps/web/tailwind.config.ts):
- colors: every family above defined as `hsl(var(--x))` (base.ts) — these become `@theme inline { --color-x: var(--x); }`.
- borderRadius: lg/md/sm/xl/2xl/pill (var(--radius*) based).
- container: center, padding 2rem, screens 2xl=1400px.
- keyframes + animation: accordion-down/up, marquee, marquee-vertical, shimmer-slide, spin-around, shine, blink-cursor (9 total) + the `plugins: [animate]` tailwindcss-animate utilities.
- boxShadow: elevation-1..3 = var(--elevation-*).
- fontFamily: sans, mono, code (code = var(--font-code) + fallbacks).

The 16 EXTERNAL hsl(var(--x)) call-site files (verified via `grep -rln "hsl(var(--" apps/web/src packages/ui/src packages/genui/src` excluding globals.css):
apps/web: chat-canvas.tsx, panel-theme-scope.tsx (comment only), knowledge-preview-mini-graph.tsx, graph-legend.tsx, graph-nodes.tsx, tier-edge-style.ts, tier-edge-style.test.ts, dev/design/page.tsx + design-data.json (untracked scratch — handled in 55-06, DO NOT touch here).
packages/ui: sidebar.tsx, spreadsheet-grid/theme.css, spreadsheet-grid/SpreadsheetGrid.tsx, spreadsheet-grid/conditional-formatting-dialog.tsx.
packages/genui: themed-wrapper.tsx (STAYS HSL — special handling below), tokens.ts (comments only).

genui re-theme contract (packages/genui/src/theme/tokens.ts + themed-wrapper.tsx):
- ThemedRoot injects `pack.resolvedVars` (keyed by CSS var name, no `--`) as inline `--<name>: <value>`.
- packs.ts values are bare HSL triplets and STAY HSL (Pitfall 1 — do not convert; converting drags in the Python mirror + Bedrock prompt for zero requirement credit).
- `TOKEN_ALIAS_TO_CSS_VAR` maps each DTCG alias → CSS var name; the COLOR vars are exactly those whose alias starts with `color.` (radius/spacing/shadow/typography vars are non-color).
</interfaces>

RESEARCH exact-shape references to follow:
- @theme inline + full-color-function shape: 55-RESEARCH.md "Pattern 1" (before/after example).
- @source relative path: 55-RESEARCH.md Pitfall 4 + Assumption A2 — path is relative to globals.css's own directory (apps/web/src/app). To reach repo root from there is FOUR `../` (app→src→web→apps→root), so the directives are `@source "../../../../packages/ui/src";` and `@source "../../../../packages/genui/src";`. The example in Pattern 1 shows three `../` and is flagged as unverified — DO NOT trust the count; verify by the compiled-CSS assertion below.
- @theme animation port: 55-RESEARCH.md "Code Examples > @theme breakpoint/animation port".
- oklch conversion strategy: Open Question 2 recommendation (RESOLVED — adopted here) — PRECOMPUTE oklch literals once (no runtime/test dependency, no culori); verify each against WCAG-AA before committing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port globals.css to oklch + @theme inline + @source + native @theme; convert internal consumers; remove the @config bridge</name>
  <read_first>
    - apps/web/src/app/globals.css (entire file — INCLUDING the @layer components/@layer utilities blocks with the 4 internal call-site clusters)
    - packages/tailwind-config/base.ts + packages/tailwind-config/web.ts + apps/web/tailwind.config.ts + packages/ui/tailwind.config.ts
    - apps/web/src/app/__tests__/token-contrast.test.ts (reuse its exact WCAG contrastRatio/relativeLuminance math to verify converted values)
    - 55-RESEARCH.md Pattern 1, Pattern 2, Pitfall 4, Anti-Patterns, Code Examples, Open Question 2
  </read_first>
  <action>
Convert globals.css to the canonical shadcn v4 shape. Do NOT change any DESIGN intent — every oklch value must be the faithful conversion of its current HSL triplet (same perceived color), verified below.

1. Precompute oklch equivalents: write a throwaway Node script (scratch dir, not committed) that reads each `:root`/`.dark` bare-HSL triplet and produces the equivalent `oklch(L C H)` literal. For every neutral pair currently gated (muted/secondary/accent + foregrounds) AND every `*`/`*-foreground` pair, verify with the EXACT WCAG math copied from token-contrast.test.ts (relativeLuminance/contrastRatio) that the converted oklch still clears the same ratio (≥4.5:1 for the gated neutral pairs). If any conversion drops a gated pair below its current ratio, the conversion is wrong — fix it, do not lower the bar. Record the before/after ratios in the SUMMARY.
2. Rewrite `:root` and `.dark`: each COLOR var becomes a full `oklch(...)` function (e.g. `--primary: oklch(37.6% 0.061 165.6);`). Keep the `--sidebar-*` `var(--x)` aliases as aliases (they point at other now-oklch vars — still valid). Keep non-color vars (radius, radius-xl/2xl/pill, font-code) unchanged. For the `--elevation-*` shadow strings: they embed `hsl(var(--border) / ...)` and literal `hsl(164 25% 15% / ...)` — convert the embedded var-consumer color so it does NOT become `hsl(oklch())` (use `color-mix(in srgb, var(--border) <pct>, transparent)` for the alpha-modified border, and either the oklch equivalent or a valid literal `oklch(... / a)` for the literal shadow tint). Preserve the D-48/D-* comment provenance lines.
3. Convert globals.css's OWN internal call sites (outside :root/.dark — the 4 clusters listed in <interfaces>) in this SAME pass, using the same pattern as Task 2's external fixes: `hsl(var(--x))` → bare `var(--x)`; `hsl(var(--x) / <a>)` → `color-mix(in srgb, var(--x) <a*100>%, transparent)`. Specifically: `.react-flow__controls-button svg` fill → `var(--foreground)`; `.scrollbar-token` scrollbar-color/background-color → `var(--border)`; `.generating-ring::before` gradient stops → `var(--primary)` and `color-mix(...)` for the 0.85 stops; the `.t-modal-reveal`/`.t-dropdown-reveal` box-shadow keyframes → `color-mix(...)`/`transparent` for the `/ 0` and `/ 0.12` alphas. After this step, NO `hsl(var(--` may remain anywhere in globals.css.
4. Add, immediately after `@import "tailwindcss";`, the two `@source` directives for the sibling packages (see Assumption A2 — expect `../../../../packages/ui/src` and `../../../../packages/genui/src`; VERIFY via the compiled-CSS assertion — adjust the `../` count if it fails).
5. Add an `@theme inline { ... }` block mapping every existing token family to `--color-<name>: var(--<name>);` — one line per family member currently in base.ts (background, foreground, card+fg, popover+fg, primary+fg, secondary+fg, muted+fg, accent+fg, destructive+fg, success+fg, border, input, ring, chart-1..5, tier-inferred/-extracted(+fg), graph-entity/email-component/email(+fg), and the sidebar family). `inline` is required so `.dark` overrides cascade at runtime. Nothing is auto-inferred — every alias needs a mapping line.
6. Add a native `@theme` (non-inline) block + top-level `@keyframes` porting web.ts's config: `--radius-*` are already CSS vars (map borderRadius lg/md/sm/xl/2xl/pill via `--radius-*` theme vars), container 2xl breakpoint (`--breakpoint-2xl: 1400px` or the v4 container equivalent), the 9 keyframes + their `--animate-*` pairs, the elevation boxShadow (`--shadow-elevation-1..3: var(--elevation-*)`), and fontFamily sans/mono/code (`--font-sans`, `--font-mono`, `--font-code`). Replace the `tailwindcss-animate` plugin's needed utilities (accordion + Radix `animate-in`/`animate-out`/`fade`/`zoom`/`slide` used by dialogs/dropdowns/tooltips) — if the animate plugin does not load under v4, port the specific keyframes those components use into `@theme`/`@keyframes` here so no Radix animation regresses.
7. Remove the `@config "../../tailwind.config.ts";` bridge line. Neutralize the now-obsolete JS theme: gut `apps/web/tailwind.config.ts`, `packages/tailwind-config/{base,web}.ts`, and `packages/ui/tailwind.config.ts` down to the minimum still needed for editor IntelliSense (or delete if unused by tooling) — the CSS `@theme` is now the single source of truth. Confirm nothing imports these configs at build time anymore (the postcss plugin no longer reads them; `components.json` `tailwind.config` is handled in 55-06).

Run the gates now (build + token-render guard) before starting Task 2 — this isolates a CSS-port regression from a call-site regression.
  </action>
  <acceptance_criteria>
    - `grep -c "oklch(" apps/web/src/app/globals.css` returns ≥ the number of color tokens (both modes) — bare `H S% L%` triplets no longer present for color tokens (`grep -nE "^\\s*--(primary|background|accent|border|graph-entity):\\s*[0-9]" apps/web/src/app/globals.css` returns 0).
    - HARD ASSERTION (checker blocker fix): `grep -c "hsl(var(--" apps/web/src/app/globals.css` returns EXACTLY 0 — every internal token consumer (React Flow Controls fill, .scrollbar-token, .generating-ring gradient, modal/dropdown box-shadow keyframes) AND the elevation var-consumers have dropped the hsl() wrapper. A single remaining match is a silent-regression bug and fails this task.
    - `grep -c "@source" apps/web/src/app/globals.css` returns 2; `grep -c "@theme inline" apps/web/src/app/globals.css` returns ≥1; `grep -c "@config" apps/web/src/app/globals.css` returns 0.
    - `npm run web:build` exits 0.
    - @source PROOF (Pitfall 4 — the faked-proof check): pick a Tailwind utility class that appears ONLY in packages/ui/src (grep to find one, e.g. a distinctive class in sidebar.tsx not used anywhere in apps/web/src), then confirm that class is present in the built CSS under `apps/web/.next` (e.g. `grep -rl "<class>" apps/web/.next/static/css/ 2>/dev/null`). If absent, the `@source` path is wrong (fix the `../` count) — a green build does NOT prove this.
    - WCAG PROOF: the throwaway verification confirms every gated neutral pair's oklch ratio ≥ its prior HSL ratio ≥ 4.5:1; before/after numbers recorded in the SUMMARY.
    - REGRESSION GATE (run after Task 1, before Task 2): `npm run test:e2e -w @polytoken/web` exits 0 — the token-render guard (incl. the `.react-flow__controls-button svg` fill assertion) stays green, proving the internal-consumer conversions in step 3 didn't break the Controls icon or canvas surfaces.
    - `npm run typecheck -w @polytoken/web` + `-w @polytoken/ui` exit 0.
  </acceptance_criteria>
  <done>globals.css is the canonical shadcn v4 oklch shape with @source + native @theme AND zero internal hsl(var(--x)); the JS config is no longer the source of truth; @source is proven by a packages/ui-only class surviving the production purge; contrast parity proven numerically; the guard is green after the CSS port.</done>
</task>

<task type="auto">
  <name>Task 2: Drop the hsl() wrapper at every EXTERNAL call site + adapt the genui re-theme surface</name>
  <read_first>
    - Each of the 14 in-scope EXTERNAL call-site files listed in <interfaces> (chat-canvas.tsx, panel-theme-scope.tsx, knowledge-preview-mini-graph.tsx, graph-legend.tsx, graph-nodes.tsx, tier-edge-style.ts, tier-edge-style.test.ts, sidebar.tsx, spreadsheet-grid/theme.css, spreadsheet-grid/SpreadsheetGrid.tsx, spreadsheet-grid/conditional-formatting-dialog.tsx)
    - packages/genui/src/theme/themed-wrapper.tsx + packages/genui/src/theme/tokens.ts
    - apps/web/src/app/globals.css (already converted in Task 1 — do not re-touch; referenced only to confirm the var contract)
    - 55-RESEARCH.md Pattern 1 "Critical corollary", Pitfall 1 (packs.ts stays HSL; ThemedRoot must wrap color vars), Pitfall 2 (grep completion check)
  </read_first>
  <action>
Every EXTERNAL call site that reads a globals.css token via `hsl(var(--x))` must drop the wrapper to `var(--x)` — because those vars now hold full `oklch(...)` functions and `hsl(oklch(...))` is invalid CSS the browser drops silently (Pitfall 2 / Anti-Patterns). (globals.css's own internal consumers were already converted in Task 1.)

1. In the 11 in-scope apps/web + packages/ui files, replace every `hsl(var(--x))` → `var(--x)`. For opacity-modified token colors (`hsl(var(--x) / <a>)`), replace with `color-mix(in srgb, var(--x) <a*100>%, transparent)` — the same pattern Task 1 used for globals.css internals (v4 targets modern browsers per RESEARCH, so color-mix is available). Do the same inside spreadsheet-grid/theme.css and the ag-grid theme objects.
2. panel-theme-scope.tsx and tokens.ts contain ONLY comment/docstring references to `hsl(var(--...))` — update the wording so the docs describe the new `var(--x)` (oklch) contract; no runtime code changes there.
3. Adapt `packages/genui/src/theme/themed-wrapper.tsx` (Pitfall 1 corollary): packs.ts stays bare-HSL, so when ThemedRoot builds `cssVarStyle`, it must wrap ONLY color vars in `hsl(...)` so an override like `--primary: 164 39% 22%` becomes `--primary: hsl(164 39% 22%)` (a valid color function that components now read via bare `var(--primary)`); non-color vars (radius/spacing/shadow/typography) stay raw. Derive the color-var set from `TOKEN_ALIAS_TO_CSS_VAR` in tokens.ts — the CSS var names whose alias starts with `color.`. Import/compute that set (immutable, explicitly typed) and branch on membership when assigning `cssVarStyle[`--${varName}`]`. Do NOT change any packs.ts value (that would scope-creep into the Python mirror + Bedrock prompt).
4. tier-edge-style.test.ts asserts on the produced style strings — update its expected values to the new `var(--x)`/`color-mix` form so it stays green.
  </action>
  <acceptance_criteria>
    - Grep completion check (Pitfall 2, now stronger): `grep -rn "hsl(var(--" apps/web/src packages/ui/src packages/genui/src` returns ZERO matches everywhere EXCEPT the untracked dev/design scratch (page.tsx/design-data.json, handled in 55-06) — specifically zero in globals.css (from Task 1), zero in the 11 in-scope component/test files, and zero in themed-wrapper.tsx/tokens.ts/panel-theme-scope.tsx after the comment rewording.
    - `packages/genui/src/theme/packs.ts` is unchanged: `git diff --stat packages/genui/src/theme/packs.ts` shows no changes.
    - `themed-wrapper.tsx` wraps color vars in `hsl(...)` conditionally: `grep -nE "hsl\\(|color\\." packages/genui/src/theme/themed-wrapper.tsx` shows the color-group branch.
    - `npm run typecheck -w @polytoken/web` + `-w @polytoken/ui` exit 0.
    - REGRESSION GATE (faked-proof): `npm run test:e2e -w @polytoken/web` exits 0 — the token-render.spec.ts guard from 55-01 stays green, proving canvas background/minimap, sidebar ring, graph nodes/edges, spreadsheet surfaces, AND the React Flow Controls icon still resolve to valid non-transparent colors (this is what catches a missed hsl(oklch()) that the build cannot).
    - `npm run web:build` exits 0.
    - EXPECTED (not a failure): `npm run test -w @polytoken/web` now fails ONLY on token-contrast.test.ts (oklch unparseable by its HSL regex) and token-registration.test.ts (resolveConfig gone) — both are rewritten in 55-03. Confirm no OTHER vitest file regressed (e.g. palette-ban.test.ts still passes).
  </acceptance_criteria>
  <done>Zero `hsl(var(--x))` remains anywhere in the source tree (outside the dev/design scratch handled in 55-06); the genui re-theme override path is valid; the executable guard proves no surface silently lost its color.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build-time CSS → browser paint | Invalid `hsl(oklch())` or purged `@source` classes silently degrade rendered output (integrity of the shipped UI) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02a | Information disclosure / integrity (silent visual corruption) | oklch port + hsl(var()) call sites, INCL. globals.css's own internal consumers | mitigate | token-render.spec.ts computed-style guard (incl. React Flow Controls fill) rejects transparent/empty colors; hard `grep -c "hsl(var(--" globals.css == 0` assertion after Task 1; post-port whole-tree grep completion check; contrast parity verified numerically — none satisfiable by a merely-successful build. Elevated blast radius → gates run between Task 1 and Task 2. |
| T-55-02b | Integrity (production-only style purge) | @source monorepo registration | mitigate | Compiled-CSS assertion that a packages/ui-only class survives the real production purge (`npm run web:build` + grep `.next` CSS) — dev mode can mask this. |
| T-55-02c | Scope creep across a cross-language contract | packs.ts / Python mirror / Bedrock prompt | accept (by exclusion) | packs.ts values explicitly NOT converted (Pitfall 1); `git diff --stat` proves packs.ts unchanged; only themed-wrapper.tsx adapts. No new install surface in this plan → no T-55-SC row. |
</threat_model>

<verification>
Stage 2 complete when all executable, non-visual gates pass:
- `npm run web:build` → 0; packages/ui-only class present in `.next` CSS (@source proof).
- `grep -c "hsl(var(--" apps/web/src/app/globals.css` → 0 (internal consumers converted).
- Whole-tree grep completion: zero `hsl(var(--` in globals.css + the 11 in-scope files + genui themed-wrapper/tokens (only the untracked dev/design scratch may remain, handled in 55-06); packs.ts unchanged.
- `npm run typecheck -w @polytoken/web` + `-w @polytoken/ui` → 0.
- `npm run test:e2e -w @polytoken/web` → 0 (token-render guard green, incl. Controls fill — the real regression gate; run after Task 1 and after Task 2).
- WCAG parity recorded numerically.
- `npm run test -w @polytoken/web` fails ONLY on the two named token gates (handed to 55-03); nothing else regressed.
- `npm run screenshot:review -w @polytoken/web` runs and writes a capture dir (artifact for later human review at the Phase-58 gate; not a pass/fail gate here).
</verification>

<success_criteria>
- globals.css tokens are oklch, in the shadcn v4 `@theme inline` + `:root`/`.dark` shape, with `@source` for both sibling packages and a native `@theme` port of the animation/radius/breakpoint/font config.
- No `hsl(oklch())` anywhere — including globals.css's own internal consumers; every globals.css-token call site uses bare `var(--x)`/`color-mix`.
- The genui runtime re-theme surface stays HSL and remains valid via ThemedRoot's color-var wrapping.
- The executable token-render guard + full E2E suite prove zero silent rendering regression; @source + WCAG parity proven numerically.
</success_criteria>

<output>
Create `.planning/phases/55-platform-migration-tailwind-v4-react-19/55-02-SUMMARY.md`. Record: the verified `@source` relative-path `../` count, the packages/ui-only class used for the compiled-CSS proof, before/after WCAG ratios per gated pair, the animate-plugin resolution (ported vs. plugin-loaded), confirmation `grep hsl(var(-- globals.css == 0`, and explicit confirmation packs.ts was NOT touched.
</output>
