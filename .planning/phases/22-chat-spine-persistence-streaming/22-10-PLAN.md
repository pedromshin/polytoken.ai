---
phase: 22-chat-spine-persistence-streaming
plan: 10
type: execute
wave: 7
depends_on: [22-02, 22-05, 22-07]
files_modified:
  - packages/api-client/src/router/chat/models.ts
  - packages/api-client/src/router/chat/cost.ts
  - packages/api-client/src/router/chat/conversations.ts
  - packages/api-client/src/router/chat/index.ts
  - packages/api-client/src/router/chat/__tests__/cost.test.ts
  - apps/web/src/app/chat/_components/model-picker.tsx
  - apps/web/src/app/chat/_components/model-picker-entry.tsx
  - apps/web/src/app/chat/_components/cost-meter.tsx
  - apps/web/src/app/chat/_components/cost-breakdown-popover.tsx
  - apps/web/src/app/chat/page.tsx
autonomous: true
requirements: [STREAM-01, STREAM-03]
must_haves:
  truths:
    - "User can pick a per-conversation model from a curated list grouped by provider, and the choice persists (D-04, D-10)"
    - "Each picker entry honestly shows capability flags, a cost estimate (or Local · Free), and a 'best for' caption (D-05, D-06)"
    - "The remembered last-used model carries a Recommended marker and defaults new conversations (D-10)"
    - "A subtle session cost meter shows the running total; a breakdown popover shows per-turn model/tokens/cost (D-23)"
  artifacts:
    - path: "apps/web/src/app/chat/_components/model-picker.tsx"
      provides: "cmdk model picker grouped by provider (D-04..D-06)"
      contains: "Command"
    - path: "packages/api-client/src/router/chat/models.ts"
      provides: "tRPC proxy to GET /v1/chat/models"
      contains: "v1/chat/models"
    - path: "apps/web/src/app/chat/_components/cost-meter.tsx"
      provides: "Session cost meter + breakdown trigger (D-23)"
      contains: "Session"
  key_links:
    - from: "models.ts"
      to: "FastAPI /v1/chat/models"
      via: "getListenerConfig server-side proxy"
      pattern: "getListenerConfig"
    - from: "model-picker.tsx"
      to: "chat.setModel"
      via: "selection persists conversation.model_id (D-10)"
      pattern: "setModel"
    - from: "cost.ts"
      to: "chat_cost_ledger"
      via: "Drizzle session sum + per-turn breakdown"
      pattern: "ChatCostLedger|chat_cost_ledger"
---

<objective>
Deliver the multi-provider model picker (D-04..D-06, D-10) and the cost visibility surface (D-23,
STREAM-03): a tRPC proxy to the curated registry endpoint, a cmdk picker grouped by provider that
shows honest capability flags + cost estimates + "best for" captions + a Recommended/last-used marker,
persistence of the per-conversation model choice, and a subtle session cost meter with a per-turn
breakdown popover reading the cost ledger.

Purpose: makes model choice effortless (the user's explicit ask) and makes spend visible without ever
being modal or blocking — enforcement stays server-side (22-04).

Output: chat.models + chat.cost tRPC + chat.setModel, ModelPicker/Entry, CostMeter/BreakdownPopover,
toolbar wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md
@.planning/phases/22-chat-spine-persistence-streaming/22-CONTEXT.md

<interfaces>
<!-- From 22-02/05/07. -->
Registry endpoint (22-02): GET /v1/chat/models (X-API-Key) → { models: [{ id, display_name, transport,
execution_locus, price_in_per_mtok, price_out_per_mtok, capabilities: { tools, genui, streaming,
context_tokens }, best_for }], registry_version }.
Proxy pattern — packages/api-client/src/router/genui/generate.ts + _listener-config.ts: getListenerConfig()
server-side, fetch FastAPI with X-API-Key, return typed output; never NEXT_PUBLIC_.
chat_cost_ledger (22-01): conversation_id, run_id, model_id, execution_locus, input_tokens,
output_tokens, cost_usd, created_at — Drizzle read for the meter.
conversations.ts (22-05): has createConversation (default = last-used, D-10); add setModel.
UI-SPEC picker contract: cmdk Command grouped Bedrock / OpenRouter / Browser; each row always shows
name + capability row ("Tools ✓ · GenUI ✓ · 128K ctx" — never omit a lacked flag) + cost line
("~$X.XX / 1M tok") or browser Badge ("Local · Free"); "Best for: …" caption; last-used gets a
"Recommended" outline in primary (the one picker element allowed the accent). Cost meter = subtle
`text-xs text-muted-foreground` "Session: $0.12" → click opens a popover breakdown per turn.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: chat.models proxy + chat.setModel + ModelPicker (capabilities, cost, best-for, recommended)</name>
  <read_first>
    - packages/api-client/src/router/genui/generate.ts (server-side proxy + getListenerConfig + envelope extraction pattern)
    - packages/api-client/src/router/chat/conversations.ts (add setModel mutation here — from 22-05)
    - apps/web/src/app/chat/page.tsx (toolbar location in the conversation view — from 22-08)
    - .planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md (Model picker interaction + copywriting contract)
  </read_first>
  <action>
    Create chat/models.ts: a tRPC query proxying GET /v1/chat/models via getListenerConfig (X-API-Key
    server-side), returning the typed registry + registry_version. Add setModel(input: { conversationId,
    modelId }) to conversations.ts updating chat_conversations.model_id (D-10 — selection persists;
    combined with createConversation's last-used default this remembers the last model). Register
    models in chat/index.ts. Build ModelPicker (toolbar trigger showing the current model's short name;
    opens a cmdk Command grouped by transport Bedrock/OpenRouter/Browser) and ModelPickerEntry (always-
    visible: name, honest capability row from the flags — never omit a lacked flag; cost line
    "~$X.XX / 1M tok" computed from price_in/out or the "Local · Free" Badge for browser; "Best for: …"
    caption; a primary Recommended outline on the last-used/default entry). Selecting a server model
    calls chat.setModel + closes; the browser entry's selection/loading is handled in 22-11 (leave a
    typed hook/prop seam, do not stub the capability display).
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && cd ../../packages/api-client && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - models.ts uses getListenerConfig + fetches /v1/chat/models; `grep -c "NEXT_PUBLIC" packages/api-client/src/router/chat/models.ts` returns 0
    - conversations.ts contains a setModel mutation updating chat_conversations.model_id
    - model-picker-entry.tsx renders every capability flag (present AND absent) and a cost line or "Local · Free" Badge; the last-used entry gets the primary Recommended outline
    - `grep -c "Command" apps/web/src/app/chat/_components/model-picker.tsx` ≥ 1 (cmdk grouping)
    - both `pnpm tsc --noEmit` runs exit 0
  </acceptance_criteria>
  <done>The picker lists the curated registry grouped by provider with honest capabilities + cost + best-for + a Recommended marker, and selection persists the conversation model; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: chat.cost tRPC (ledger read) + CostMeter + CostBreakdownPopover + toolbar wiring</name>
  <read_first>
    - packages/api-client/src/router/entities/gallery.ts (Drizzle aggregate/select + importer scoping pattern)
    - packages/db/src/schema/chat-cost-ledger.ts (columns to sum/list — from 22-01)
    - apps/web/src/app/chat/page.tsx (toolbar slot next to the model picker — from Task 1 / 22-08)
    - .planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md (session meter subtle text + popover breakdown per turn: model, tokens in/out, cost; never modal/blocking)
  </read_first>
  <action>
    Create chat/cost.ts: sessionCost(input: { conversationId }) → Drizzle sum of chat_cost_ledger.cost_usd
    for the conversation + a per-turn breakdown list (run_id, model_id, input_tokens, output_tokens,
    cost_usd), importer-scoped, bounded. Register in chat/index.ts. Build CostMeter (subtle
    `text-xs text-muted-foreground` "Session: $0.12", clickable) and CostBreakdownPopover (per-turn rows:
    model, tokens in/out, cost). Wire ModelPicker + CostMeter into the conversation-view toolbar in
    page.tsx (top bar: picker left, meter right, per UI-SPEC layout). The meter is display-only — it
    never blocks or gates a turn (enforcement is server-side, 22-04). Invalidate sessionCost after each
    completed turn so the meter updates live.
  </action>
  <verify>
    <automated>cd packages/api-client && pnpm vitest run src/router/chat/__tests__/cost.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - cost.ts sessionCost sums chat_cost_ledger.cost_usd for a conversation and returns a per-turn breakdown list (test asserts the sum + shape with a fake db)
    - CostMeter renders `text-xs text-muted-foreground` and opens the breakdown popover on click (not modal)
    - The meter has no code path that disables/blocks the composer or send (display-only)
    - page.tsx toolbar renders both ModelPicker and CostMeter
    - `pnpm vitest run src/router/chat/__tests__/cost.test.ts` passes; `apps/web` + `packages/api-client` tsc clean
  </acceptance_criteria>
  <done>The session cost meter shows the running total with a per-turn breakdown from the ledger, subtle and non-blocking, wired into the toolbar; tests + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → chat.models proxy → FastAPI | registry fetched via the server-side X-API-Key proxy (never client key) |
| Browser → chat.cost (Drizzle) | ledger reads are importer-scoped, parameterized |
| Cost display vs enforcement | the meter is display-only; caps are enforced server-side (22-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-37 | Information disclosure | API key / other importers' ledger data | mitigate | models proxy keeps the key server-side (no NEXT_PUBLIC_); cost queries importer-scoped + parameterized Drizzle |
| T-22-38 | Tampering | client "raising" caps via the meter | mitigate | meter is read-only display; enforcement lives only in the server breaker (22-04); no cap mutation exposed |
| T-22-39 | Spoofing | picker showing dishonest capabilities | mitigate | picker renders the server registry verbatim (D-05 honesty); genui offered only where the registry flags it (22-07) |
</threat_model>

<verification>
- `pnpm vitest run src/router/chat/__tests__/cost.test.ts` passes; `apps/web` + `packages/api-client` tsc clean; `pnpm next build` green
- Manual: pick a model (persists across reload); capabilities/cost/best-for render honestly; meter updates after a turn
</verification>

<success_criteria>
- D-04..D-06 curated picker with honest capabilities + cost + guidance; D-10 selection persists
- D-23 session meter + per-turn breakdown, display-only; STREAM-03 spend visible (enforcement server-side)
</success_criteria>

<output>
Create `.planning/phases/22-chat-spine-persistence-streaming/22-10-SUMMARY.md` when done.
</output>
