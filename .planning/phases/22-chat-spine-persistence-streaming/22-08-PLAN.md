---
phase: 22-chat-spine-persistence-streaming
plan: 08
type: execute
wave: 5
depends_on: [22-03, 22-05, 22-07]
files_modified:
  - apps/web/src/app/api/chat/stream/route.ts
  - apps/web/src/app/api/chat/regenerate/route.ts
  - apps/web/src/app/chat/_hooks/use-chat-stream.ts
  - apps/web/src/app/chat/_components/message-list.tsx
  - apps/web/src/app/chat/_components/message-turn.tsx
  - apps/web/src/app/chat/_components/composer.tsx
  - apps/web/src/app/chat/_components/jump-to-bottom-button.tsx
  - apps/web/src/app/chat/_components/__tests__/use-chat-stream.test.ts
  - apps/web/src/app/chat/page.tsx
autonomous: true
requirements: [CHAT-01, CHAT-03, CHAT-06, CHAT-07, STREAM-01]
must_haves:
  truths:
    - "User can send a message and watch the agent response stream in live, then it persists across reload"
    - "The composer supports multi-line input, send-on-enter (Shift+Enter = newline), is disabled while streaming, and optimistically renders the user's message"
    - "Assistant text renders as sanitized markdown; the list auto-scrolls with a jump-to-bottom affordance"
    - "A visible generating indicator shows while streaming; Stop replaces Send in the same slot and ends the stream keeping the partial"
    - "The SSE stream is proxied through a Next route handler that injects X-API-Key server-side (never exposed to the browser)"
  artifacts:
    - path: "apps/web/src/app/api/chat/stream/route.ts"
      provides: "Next route-handler SSE proxy injecting X-API-Key (D-24)"
      contains: "EMAIL_LISTENER_API_KEY"
    - path: "apps/web/src/app/chat/_hooks/use-chat-stream.ts"
      provides: "SSE-consuming hook + streaming state machine"
      contains: "ReadableStream"
    - path: "apps/web/src/app/chat/_components/composer.tsx"
      provides: "Composer (multi-line, enter-to-send, disabled-while-streaming, stop morph)"
      contains: "Shift"
  key_links:
    - from: "use-chat-stream.ts"
      to: "/api/chat/stream"
      via: "fetch streaming POST"
      pattern: "/api/chat/stream"
    - from: "route.ts"
      to: "FastAPI /v1/chat/stream"
      via: "server-side proxy with getListenerConfig()"
      pattern: "v1/chat/stream"
    - from: "message-turn.tsx"
      to: "MarkdownRenderer"
      via: "text-part rendering"
      pattern: "MarkdownRenderer"
---

<objective>
Deliver the streamed chat core (CHAT-01/06/07, STREAM-01, CHAT-03 stop): a Next.js route-handler that
proxies the FastAPI SSE stream while injecting `X-API-Key` server-side (D-24 — streaming bypasses
tRPC), a `useChatStream` hook that consumes the SSE and drives the `idle→streaming→terminal` state
machine, and the message list + composer that make it feel like a real chat product — optimistic user
message, live-streaming assistant markdown, auto-scroll + jump-to-bottom, generating indicator, and a
Stop button that morphs from Send in the same slot.

Purpose: the first end-to-end streamed conversation. Rich mechanics (regenerate siblings, error
recovery, progressive genui) layer on in 22-09; the model picker + cost meter in 22-10.

Output: the SSE proxy (+ regenerate proxy), the stream hook, MessageList/MessageTurn, Composer,
JumpToBottom, and the /chat conversation view wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md
@.planning/phases/22-chat-spine-persistence-streaming/22-CONTEXT.md

<interfaces>
<!-- From 22-03/05/07 + existing patterns. -->
Key posture — packages/api-client/src/router/_listener-config.ts: `getListenerConfig()` reads
EMAIL_LISTENER_URL + EMAIL_LISTENER_API_KEY server-side at call time (never NEXT_PUBLIC_). The Next
route handler mirrors this to inject X-API-Key when proxying the SSE.
FastAPI SSE (22-07): POST /v1/chat/stream { conversation_id, user_text, model_id } → text/event-stream
of `data: {json ChatRunEvent}` frames; POST /v1/chat/regenerate { conversation_id,
assistant_message_id }.
Run event types (22-06): started | text_delta_checkpoint | tool_call | tool_result | usage | completed
| stopped | failed | cost_capped | interrupted.
MarkdownRenderer (22-03): <MarkdownRenderer content={text} /> for assistant text parts.
History (22-05): api.chat.getHistory returns messages with parts (jsonb), role, status, siblingGroupId,
version, isActive.
UI-SPEC interaction contract: composer disabled while the active turn is streaming; Enter sends,
Shift+Enter newline; textarea min-h 44px auto-grows to max-h-52; Stop replaces Send (no layout shift),
neutral variant; generating indicator = Loader2 animate-spin + "Generating…" (reuse
generation-state-chrome idiom) + a blinking caret at the text tail (motion-safe). Message column
max-w-3xl mx-auto; user message = bubble (bg-muted), assistant = plain on background. Jump-to-bottom =
floating icon button aria-label "Jump to latest message".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Next route-handler SSE proxy (+ regenerate) + useChatStream hook</name>
  <read_first>
    - packages/api-client/src/router/_listener-config.ts (server-side key read pattern to replicate in the route handler)
    - apps/web/src/app/api/trpc/[trpc]/route.ts (Next route-handler conventions in this repo)
    - .planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md (streaming state machine: idle→streaming→terminal; disabled-while-streaming)
    - .planning/phases/22-chat-spine-persistence-streaming/22-CONTEXT.md (D-24 proxy injects X-API-Key server-side)
  </read_first>
  <behavior>
    - the route handler reads EMAIL_LISTENER_URL + EMAIL_LISTENER_API_KEY at request time and POSTs to FastAPI /v1/chat/stream with X-API-Key, then pipes the SSE body straight to the browser (text/event-stream)
    - the key never reaches the client bundle (no NEXT_PUBLIC_); a missing key returns 500 without leaking detail
    - useChatStream reads the ReadableStream, parses SSE `data:` frames into ChatRunEvent objects, appends text deltas into a growing assistant parts array, and transitions state idle→streaming→(completed|stopped|failed|cost_capped)
    - calling stop() aborts the fetch (AbortController) → the stream ends → state goes to stopped without throwing
  </behavior>
  <action>
    Create apps/web/src/app/api/chat/stream/route.ts and .../regenerate/route.ts: POST handlers that
    read the listener URL+key server-side (mirror getListenerConfig), forward the JSON body to the
    corresponding FastAPI endpoint with X-API-Key, and return a streaming Response piping the upstream
    body with `Content-Type: text/event-stream`, `Cache-Control: no-cache`. Create useChatStream: a
    hook exposing { state, parts, send(userText), regenerate(assistantMessageId), stop() } that fetches
    the proxy with an AbortController, reads response.body.getReader(), decodes + splits SSE frames,
    JSON-parses each event, and updates parts + state per the run-event taxonomy. Keep the parser pure
    and unit-testable (export a parseSseChunk helper).
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/app/chat/_hooks/__tests__/use-chat-stream.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NEXT_PUBLIC" apps/web/src/app/api/chat/stream/route.ts` returns 0; `grep -c "EMAIL_LISTENER_API_KEY" ...` ≥ 1
    - route.ts returns a streaming Response with Content-Type text/event-stream
    - Test feeds a sequence of SSE frames (started, several text_delta_checkpoint, completed) to the parser and asserts the accumulated assistant text + terminal state = 'completed'; a 'stopped' terminal → state 'stopped'
    - `pnpm vitest run` for the hook test passes; `cd apps/web && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The SSE proxy injects the key server-side and pipes the stream; useChatStream consumes it into parts + a state machine with a working stop(); tests + tsc pass.</done>
</task>

<task type="auto">
  <name>Task 2: MessageList + MessageTurn (markdown text parts) + auto-scroll + JumpToBottom + generating indicator</name>
  <read_first>
    - apps/web/src/app/chat/_components/markdown-renderer.tsx (assistant text-part rendering — from 22-03)
    - apps/web/src/app/studio/_components/ (generation-state-chrome Loader2+text idiom referenced by the UI-SPEC)
    - .planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md (Layout max-w-3xl, user bubble vs assistant plain, generating indicator, jump-to-bottom, interleaved parts spacing)
  </read_first>
  <action>
    Create MessageList (scroll-area, max-w-3xl mx-auto, renders turns from getHistory + the live
    streaming parts) and MessageTurn (renders a turn's interleaved parts in emission order: text parts
    via MarkdownRenderer, genui parts as a bordered Card placeholder for now — the real GenuiPartBoundary
    arrives in 22-09; user turn = bg-muted bubble, assistant = plain on background). Add auto-scroll:
    stick to bottom while streaming unless the user has scrolled up, in which case show
    JumpToBottomButton (floating, aria-label "Jump to latest message"). Add the generating indicator
    (Loader2 animate-spin + "Generating…" above the composer while state==='streaming') and a
    motion-safe blinking caret at the tail of the growing assistant text. Token classes only; xl gap
    between turns per the spacing scale.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && pnpm next build 2>&1 | grep -iE "compiled|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - message-turn.tsx renders text parts through MarkdownRenderer and iterates a parts array (interleaved order preserved)
    - MessageList shows a JumpToBottomButton only when scrolled away from the bottom (state/ref-driven, not always-visible)
    - The generating indicator renders while streaming and disappears on any terminal state
    - `pnpm tsc --noEmit` exits 0; `pnpm next build` completes without errors
  </acceptance_criteria>
  <done>The message list renders interleaved turns with markdown text, auto-scrolls with jump-to-bottom, and shows a generating indicator while streaming; build + tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Composer (multi-line, enter-to-send, disabled-while-streaming, optimistic) + Stop morph + page wiring</name>
  <read_first>
    - apps/web/src/app/chat/_hooks/use-chat-stream.ts (send/stop/state — from Task 1)
    - apps/web/src/app/chat/page.tsx (conversation view to wire the list + composer into — from 22-05)
    - .planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md (Composer spec, Send/Stop same slot, 44px hit target, focus management, keyboard)
  </read_first>
  <action>
    Create Composer: a @nauta/ui textarea (min-h 44px auto-grow to max-h-52), Enter submits /
    Shift+Enter inserts a newline, disabled while state==='streaming' (CHAT-06). On submit, optimistically
    append the user's message to the rendered list immediately, clear the input, and call
    useChatStream.send(); keep focus in the textarea after submit. The send button (primary, aria-label
    "Send message", 44×44 hit area) morphs into a Stop button (neutral secondary, Square glyph,
    aria-label "Stop generating") in the SAME slot while streaming, calling stop() (CHAT-03). Wire the
    MessageList + Composer + generating indicator into page.tsx's conversation view (replacing the 22-05
    placeholder), selecting the conversation's model_id for send. On stream completion, invalidate
    chat.getHistory so the persisted turn replaces the transient streamed parts on the next read/reload.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - composer.tsx: Enter (no Shift) triggers submit; Shift+Enter does not; the textarea is `disabled` while streaming
    - The user's message renders optimistically before the assistant stream begins (optimistic append in the submit handler)
    - The Send↔Stop morph occupies one slot (single button element, icon/variant swap by state) — no separate layout branch that shifts position
    - page.tsx conversation view renders MessageList + Composer and passes the conversation model_id to send()
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The composer streams a real turn end-to-end with optimistic render, enter-to-send, disabled-while-streaming, and a Send↔Stop morph; the conversation view is wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next route handler → FastAPI | the browser must never see EMAIL_LISTENER_API_KEY; the handler injects it server-side (D-24) |
| SSE frames → client state | streamed event JSON is untrusted input parsed in the browser |
| Assistant markdown → DOM | untrusted model text (handled by the 22-03 sanitized renderer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-29 | Information disclosure | API key leaking to the client | mitigate | key read only in the server route handler (mirrors getListenerConfig); never NEXT_PUBLIC_; grep gate = 0 |
| T-22-30 | Tampering | malformed SSE frame corrupting client state | mitigate | each frame JSON-parsed defensively; unknown event types ignored; parser unit-tested |
| T-22-31 | Tampering (XSS) | assistant markdown | mitigate | rendered via the sanitized MarkdownRenderer (22-03); no dangerouslySetInnerHTML |
| T-22-32 | Denial of service | runaway client stream | mitigate | AbortController stop() + server-side cost cap + disconnect handling (22-07) |
</threat_model>

<verification>
- `pnpm vitest run src/app/chat/_hooks` passes; `apps/web` tsc + `pnpm next build` green
- Manual: send → live stream → persists across reload; Enter sends, Shift+Enter newlines; Stop keeps the partial
- `grep -rc "NEXT_PUBLIC" apps/web/src/app/api/chat/` returns 0
</verification>

<success_criteria>
- CHAT-01 send+stream+persist, CHAT-06 composer, CHAT-07 markdown+scroll, STREAM-01 indicator, CHAT-03 stop — all live end-to-end
- D-24 server-side key injection preserved; streaming bypasses tRPC
</success_criteria>

<output>
Create `.planning/phases/22-chat-spine-persistence-streaming/22-08-SUMMARY.md` when done.
</output>
