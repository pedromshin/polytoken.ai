---
phase: 22-chat-spine-persistence-streaming
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/app/chat/_components/markdown-renderer.tsx
  - apps/web/src/app/chat/_components/__tests__/markdown-renderer.test.tsx
autonomous: false
user_setup: []
requirements: [CHAT-07]
must_haves:
  truths:
    - "Assistant markdown (headings, lists, tables, links, inline + fenced code) renders as React"
    - "Fenced code blocks are syntax-highlighted"
    - "No raw HTML from model output is ever injected as live DOM (no dangerouslySetInnerHTML on untrusted content)"
    - "Rendered markdown headings map into the app's existing 2-weight type system (no third weight)"
  artifacts:
    - path: "apps/web/src/app/chat/_components/markdown-renderer.tsx"
      provides: "Sanitized MarkdownRenderer component (CHAT-07, D-28)"
      contains: "react-markdown"
  key_links:
    - from: "markdown-renderer.tsx"
      to: "react-markdown + remark-gfm + rehype-highlight + rehype-sanitize"
      via: "plugin pipeline"
      pattern: "remarkPlugins|rehypePlugins"
---

<objective>
Add the markdown + code-block renderer (CHAT-07, D-28) — a new dependency the repo does not yet have.
Pick concretely: `react-markdown` + `remark-gfm` (tables/strikethrough/task-lists) + `rehype-highlight`
(fenced-code highlighting) + `rehype-sanitize` (harden against raw-HTML injection). Build a
`MarkdownRenderer` that renders assistant prose safely, mapping heading levels into the app's existing
2-weight (400/600) type system per the UI Design Contract.

Purpose: assistant turns must read like a real chat product. This component is a reusable primitive
consumed by the message list (22-08) and is built independently up front so it does not gate the
streaming work.

Output: vetted dependency install (behind a package-legitimacy checkpoint), the MarkdownRenderer
component, and unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md
@.planning/phases/22-chat-spine-persistence-streaming/22-CONTEXT.md

<interfaces>
<!-- Typography contract the renderer must honor (from 22-UI-SPEC.md). -->
- Body = text-sm (14px) weight 400 line-height 1.5; Heading = text-base (16px) font-semibold (600)
- Markdown `##`/`###` render as text-base font-semibold (Heading role) — NEVER a third weight
- Token-bound Tailwind classes only (text-foreground, bg-muted, text-muted-foreground); no hardcoded hex
- Code renders inside token-styled surfaces (bg-muted rounded); inline code text-xs/sm mono
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    No RESEARCH.md Package Legitimacy Audit table exists for this phase, so per the package-legitimacy
    fallback policy these four npm packages are treated as [ASSUMED] and must be human-verified before
    install: react-markdown, remark-gfm, rehype-highlight, rehype-sanitize.
  </what-built>
  <how-to-verify>
    1. Open npmjs.com for each package: react-markdown, remark-gfm, rehype-highlight, rehype-sanitize.
    2. Confirm each is the genuine unified/remark-ecosystem package (author: unified collective /
       rehypejs / remarkjs), has substantial weekly downloads (millions / hundreds-of-thousands),
       a recent publish, and a public GitHub repo — not a typosquat.
    3. Confirm licenses are permissive (MIT) and bundle sizes are acceptable for a client component.
    4. Approve to proceed with the install, or name a safer alternative.
  </how-to-verify>
  <resume-signal>Type "approved" to install the four packages, or specify replacements.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Install deps + build the sanitized MarkdownRenderer</name>
  <read_first>
    - apps/web/package.json (dependency block + existing React 19 / Next versions to stay compatible)
    - apps/web/src/components/app-sidebar.tsx (token-class conventions: text-muted-foreground, bg-muted, size-4, no hardcoded colors)
    - .planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md (Typography + Color sections: 2-weight mapping, token-only classes)
  </read_first>
  <behavior>
    - Given markdown "# H1\n\n**bold** and `code`" the renderer outputs an h-element with class containing font-semibold and a <code> element (no raw HTML string injected)
    - Given a fenced ```ts block, the output contains a <pre><code> with a language/hljs class (highlighted)
    - Given a GFM table, a <table> renders (remark-gfm active)
    - Given raw-HTML input like `<img src=x onerror=alert(1)>`, no <img> with an onerror handler reaches the output (rehype-sanitize strips it; no dangerouslySetInnerHTML path)
    - h1..h3 all render at the Heading weight (font-semibold) — no font-medium or bespoke weight introduced
  </behavior>
  <action>
    After checkpoint approval, add react-markdown, remark-gfm, rehype-highlight, rehype-sanitize to
    apps/web (pnpm add in the web workspace). Create MarkdownRenderer({ content }: { content: string }):
    a client component wrapping ReactMarkdown with remarkPlugins=[remarkGfm],
    rehypePlugins=[rehypeSanitize, rehypeHighlight]. Provide a `components` map that binds h1/h2/h3 →
    text-base font-semibold, p → text-sm leading-relaxed, code (inline) → token-styled mono, pre →
    bg-muted rounded overflow-x-auto, a → text-primary underline. Import a highlight.js stylesheet
    theme (token-neutral). Do NOT enable rehype-raw and never use dangerouslySetInnerHTML — model
    markdown is untrusted (FOUND-6 / project guardrails). Keep the file focused (<200 lines).
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/app/chat/_components/__tests__/markdown-renderer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dangerouslySetInnerHTML" apps/web/src/app/chat/_components/markdown-renderer.tsx` returns 0
    - `grep -c "rehypeSanitize" apps/web/src/app/chat/_components/markdown-renderer.tsx` ≥ 1 and rehype-raw is NOT imported
    - Tests assert: heading elements carry font-semibold; a fenced code block yields a highlighted <pre><code>; a GFM table renders; an injected onerror attribute is stripped
    - `pnpm vitest run` for the test file passes; `cd apps/web && pnpm tsc --noEmit` exits 0
    - react-markdown, remark-gfm, rehype-highlight, rehype-sanitize appear in apps/web/package.json dependencies
  </acceptance_criteria>
  <done>MarkdownRenderer renders sanitized, highlighted markdown in the 2-weight system; deps vetted + installed; tests + tsc pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model-generated markdown → DOM | Assistant output is untrusted; markdown may embed raw HTML / scripts (FOUND-6) |
| Package supply chain | Four new npm packages enter the web bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | Tampering (XSS) | raw HTML/script in assistant markdown | mitigate | react-markdown default (no raw HTML) + rehype-sanitize; rehype-raw NOT used; zero dangerouslySetInnerHTML |
| T-22-11 | Elevation of privilege | code-block content executed | mitigate | fenced code is rendered as inert highlighted text only — never evaluated |
| T-22-SC | Tampering | supply-chain: 4 ASSUMED npm packages | mitigate | blocking-human legitimacy checkpoint (npmjs verification) before install; not auto-approvable |
</threat_model>

<verification>
- Package-legitimacy checkpoint completed (human-approved) before install
- `pnpm vitest run` for markdown-renderer passes; no dangerouslySetInnerHTML; sanitize active
- `cd apps/web && pnpm tsc --noEmit` clean
</verification>

<success_criteria>
- CHAT-07 markdown + code-block rendering delivered as a sanitized, reusable component
- Renders in the app's existing 2-weight type system; token classes only
</success_criteria>

<output>
Create `.planning/phases/22-chat-spine-persistence-streaming/22-03-SUMMARY.md` when done.
</output>
