---
phase: 22-chat-spine-persistence-streaming
plan: 11
type: execute
wave: 8
depends_on: [22-01, 22-08, 22-10]
files_modified:
  - apps/web/package.json
  - apps/web/src/app/chat/_hooks/use-webllm-engine.ts
  - apps/web/src/app/chat/_components/webllm-loading.tsx
  - apps/web/src/app/chat/_components/model-picker-entry.tsx
  - apps/web/src/app/chat/page.tsx
  - packages/api-client/src/router/chat/browser-turn.ts
  - packages/api-client/src/router/chat/index.ts
  - packages/api-client/src/router/chat/__tests__/browser-turn.test.ts
autonomous: false
user_setup: []
requirements: [STREAM-01, STREAM-03]
must_haves:
  truths:
    - "A curated in-browser model appears as a real picker entry and, when selected, loads and runs locally via WebGPU (D-08)"
    - "The browser model streams a text-only chat response locally (no genui tool)"
    - "A browser turn persists its user + assistant messages and run events in the same shape as server turns, with a $0-cost usage event that still records token counts (D-08, D-22, D-09 metering)"
    - "On a browser without WebGPU the entry is disabled with an explanatory caption (D-08)"
    - "The architecture stays open for richer browser execution later (locus is a registry field, not a hardcoded branch)"
  artifacts:
    - path: "apps/web/src/app/chat/_hooks/use-webllm-engine.ts"
      provides: "WebLLM engine init + WebGPU detection + local streaming"
      contains: "navigator.gpu"
    - path: "packages/api-client/src/router/chat/browser-turn.ts"
      provides: "recordBrowserTurn — persist a locally-generated turn in the canonical shape"
      contains: "recordBrowserTurn"
    - path: "apps/web/src/app/chat/_components/webllm-loading.tsx"
      provides: "Progress loading UX (download → WebGPU → ready)"
      contains: "Progress"
  key_links:
    - from: "use-webllm-engine.ts"
      to: "@mlc-ai/web-llm"
      via: "CreateMLCEngine + local streaming"
      pattern: "web-llm|MLCEngine"
    - from: "browser-turn.ts"
      to: "chat_messages / chat_runs / chat_run_events / chat_cost_ledger"
      via: "Drizzle insert of the canonical turn shape ($0 usage)"
      pattern: "executionLocus|browser"
    - from: "page.tsx"
      to: "conversation model execution_locus"
      via: "send routes browser-locus models to the WebLLM engine, server-locus to SSE"
      pattern: "execution_locus|locus"
---

<objective>
Deliver the minimal, honest in-browser model prototype (D-08, D-09 locus seam): one curated small
model (e.g. Qwen3-4B / Gemma-3-4B class via `@mlc-ai/web-llm` + WebGPU) as a real picker entry that
loads locally with an honest download/loading UX, streams a text-only chat response in the browser,
and persists the turn (messages + run events + a $0 usage event carrying real token counts) in the
SAME canonical shape as server turns. WebGPU-unsupported browsers show a disabled entry.

Purpose: prove the browser execution locus for the sovereign/distributed-inference north star while
keeping it minimal (no genui from browser models). The locus stays a registry-driven property so the
send path branches on data, not a hardcoded special case — richer browser execution can extend it.

Output: vetted @mlc-ai/web-llm install (behind a legitimacy checkpoint), the WebLLM engine hook +
loading UX, the browser-locus send branch, and the recordBrowserTurn persistence mutation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md
@.planning/phases/22-chat-spine-persistence-streaming/22-CONTEXT.md

<interfaces>
<!-- From 22-01/08/10. -->
Registry entry (22-02): a browser entry with transport='browser', execution_locus='browser',
price 0, capabilities.genui=false, capabilities.streaming=true.
ModelPicker (22-10): entries render capabilities + "Local · Free"; the browser entry's
selection/loading seam is filled here.
useChatStream (22-08): server-model send path; browser send must be a sibling path with the same
parts/state-machine outputs so MessageList renders identically.
Canonical turn shape (22-01/06): chat_messages(parts jsonb, role, turn_index, run_id, sibling_group_id,
version, is_active, status), chat_runs(agent_id, model_id, status), chat_run_events(run_id, seq, type,
data), chat_cost_ledger(execution_locus, input_tokens, output_tokens, cost_usd).
Drizzle mutation pattern — packages/api-client/src/router/entities/mutations.ts (parameterized inserts,
Zod-at-boundary, importer scoping).
UI-SPEC WebLLM copy: Progress label states "Downloading model… (~2.5GB, first run only)" → "Loading
into WebGPU…" → ready ("Ready" tag / green dot); WebGPU-unsupported → disabled row + "Your browser
doesn't support WebGPU — choose another model."
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    No RESEARCH.md Package Legitimacy Audit table exists for this phase, so `@mlc-ai/web-llm` is
    treated as [ASSUMED] under the package-legitimacy fallback and must be human-verified before install.
  </what-built>
  <how-to-verify>
    1. Open npmjs.com/package/@mlc-ai/web-llm and github.com/mlc-ai/web-llm.
    2. Confirm it is the genuine MLC-AI project (Apache-2.0, active maintenance, substantial downloads,
       the WebLLM/WebGPU in-browser inference library) — not a typosquat.
    3. Confirm the bundle/runtime approach (WASM + WebGPU, model weights fetched from a CDN at runtime)
       is acceptable and note the model-download size for the loading copy.
    4. Approve to install, or name an alternative in-browser inference package.
  </how-to-verify>
  <resume-signal>Type "approved" to install @mlc-ai/web-llm, or specify a replacement.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install web-llm + engine hook (WebGPU detect + loading UX) + picker entry activation</name>
  <read_first>
    - apps/web/package.json (dependency block + React 19 compatibility)
    - apps/web/src/app/chat/_components/model-picker-entry.tsx (the browser-entry seam left by 22-10)
    - .planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md (WebLLM loading Progress states + unsupported copy)
  </read_first>
  <action>
    After checkpoint approval, add @mlc-ai/web-llm to apps/web. Create useWebllmEngine: detects WebGPU
    (navigator.gpu), lazily creates the MLC engine for the curated browser model id with an
    initProgressCallback driving load phases (download → WebGPU load → ready), and exposes
    { supported, status, progress, ensureLoaded(), generateStream(messages) }. Create WebLLMLoading (a
    @nauta/ui Progress with the UI-SPEC label states). Activate the browser entry in ModelPickerEntry:
    when WebGPU is unsupported render the row disabled with the "doesn't support WebGPU" caption;
    otherwise selecting it triggers ensureLoaded() inline (Progress in the row) before it becomes the
    active model. Keep engine + weights lazily loaded (never on first paint).
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && pnpm next build 2>&1 | grep -iE "compiled|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `@mlc-ai/web-llm` appears in apps/web/package.json dependencies
    - use-webllm-engine.ts checks `navigator.gpu` for support and lazily creates the engine (not at module load)
    - WebLLMLoading renders the ordered Progress label states; the unsupported path renders a disabled entry + the WebGPU caption
    - `pnpm tsc --noEmit` exits 0 and `pnpm next build` completes without errors
  </acceptance_criteria>
  <done>The browser model is a real, WebGPU-gated picker entry that loads locally with an honest progress UX; deps vetted + installed; build + tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Browser-locus send branch (local streaming) + recordBrowserTurn persistence</name>
  <read_first>
    - apps/web/src/app/chat/page.tsx (the send handler that currently routes to useChatStream — from 22-08/22-10)
    - apps/web/src/app/chat/_hooks/use-webllm-engine.ts (generateStream — Task 1)
    - packages/api-client/src/router/entities/mutations.ts (Drizzle insert + Zod-at-boundary pattern)
    - packages/db/src/schema/chat-messages.ts + chat-runs.ts + chat-run-events.ts + chat-cost-ledger.ts (columns — 22-01)
  </read_first>
  <action>
    Branch the conversation-view send handler on the selected model's execution_locus: server-locus →
    the existing useChatStream.send (SSE); browser-locus → useWebllmEngine.generateStream, feeding the
    same growing-parts + state-machine outputs so MessageList renders identically (text-only; NO genui
    tool for browser models, D-08). On completion, call a new tRPC chat.recordBrowserTurn mutation:
    Zod-validate + persist, in one logical write, the user message + assistant message (parts, status
    completed) + a chat_runs row (agent_id 'chat-agent-v1', model_id, status completed) + started/
    completed chat_run_events + a chat_cost_ledger row with execution_locus='browser', cost_usd=0, and
    the real input/output token counts the engine reports (D-22 — $0 but metered; D-09 locus). Register
    in chat/index.ts. This is the "same API / same shape" persistence D-08 requires. Do NOT introduce a
    hardcoded browser special-case in the renderer — the branch is on the registry locus value only.
  </action>
  <verify>
    <automated>cd packages/api-client && pnpm vitest run src/router/chat/__tests__/browser-turn.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - recordBrowserTurn Zod-validates its input (conversationId uuid, bounded parts) and inserts a chat_cost_ledger row with executionLocus='browser' and costUsd 0 but non-null token counts
    - A test with a fake db asserts recordBrowserTurn writes a message + run + run_events + a $0 ledger row in the canonical shape
    - page.tsx send branches on the model's execution_locus (server → SSE, browser → engine) — a single data-driven branch, not per-model hardcoding
    - browser turns render through the SAME MessageList/MessageTurn as server turns (no separate browser renderer)
    - `pnpm vitest run src/router/chat/__tests__/browser-turn.test.ts` passes; `apps/web` + `packages/api-client` tsc clean
  </acceptance_criteria>
  <done>A browser-locus model streams locally and its turn persists in the canonical message/run/event/ledger shape with a $0 metered usage event; the send path branches purely on registry locus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-browser model output → persistence | browser-generated content is untrusted; it must cross the same Zod/schema gate as server turns before storage (FOUND-6) |
| Client → chat.recordBrowserTurn | a client could POST arbitrary turn data; it is validated + importer-scoped |
| Package supply chain | @mlc-ai/web-llm + runtime-fetched model weights |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-40 | Tampering | forged/oversized browser-turn payload | mitigate | recordBrowserTurn Zod-validates shape + bounds; parameterized Drizzle inserts; importer-scoped |
| T-22-41 | Tampering (XSS) | untrusted browser-model text | mitigate | rendered via the sanitized MarkdownRenderer (22-03); browser models get no genui tool (D-08) |
| T-22-42 | Repudiation | browser usage untracked | mitigate | $0 ledger row still records real token counts + execution_locus='browser' (D-22/D-09) |
| T-22-SC | Tampering | supply-chain: @mlc-ai/web-llm + CDN weights | mitigate | blocking-human legitimacy checkpoint before install; lazy runtime load; not auto-approvable |
| T-22-43 | Elevation of privilege | browser model reaching server tools/data | mitigate | browser path never offers emit_ui_spec or any tool (text-only, D-08); persistence is a fixed-shape write only |
</threat_model>

<verification>
- Package-legitimacy checkpoint completed (human-approved) before install
- `pnpm vitest run src/router/chat/__tests__/browser-turn.test.ts` passes; `apps/web` + `packages/api-client` tsc + `pnpm next build` green
- Manual (WebGPU browser): select the local model → loads → streams a reply → persists across reload; non-WebGPU browser shows the disabled entry
</verification>

<success_criteria>
- D-08 minimal honest in-browser prototype: real entry, local WebGPU streaming, text-only, no genui
- Turn persisted in the canonical shape via the same schema gate; $0 metered usage (D-22/D-09); locus is a data-driven seam
</success_criteria>

<output>
Create `.planning/phases/22-chat-spine-persistence-streaming/22-11-SUMMARY.md` when done.
</output>
