---
phase: 22-chat-spine-persistence-streaming
plan: 09
type: execute
wave: 6
depends_on: [22-07, 22-08]
files_modified:
  - apps/web/src/app/chat/_components/turn-action-row.tsx
  - apps/web/src/app/chat/_components/sibling-nav.tsx
  - apps/web/src/app/chat/_components/inline-error-card.tsx
  - apps/web/src/app/chat/_components/cost-cap-blocked-card.tsx
  - apps/web/src/app/chat/_components/turn-status-badge.tsx
  - apps/web/src/app/chat/_components/genui-part-boundary.tsx
  - apps/web/src/app/chat/_components/__tests__/genui-part-boundary.test.tsx
  - apps/web/src/app/chat/_components/message-turn.tsx
autonomous: true
requirements: [CHAT-04, CHAT-05, STREAM-02]
must_haves:
  truths:
    - "User can regenerate the last response without retyping; sibling versions navigate with a ‹ N/M › counter (D-16)"
    - "A failed turn shows an inline, retryable error card and the user's in-flight composer draft is never lost (CHAT-05, D-19)"
    - "A pre-turn cost-cap block shows a distinct no-retry card; stopped and cost-capped partials carry a neutral marker badge (D-15/D-21)"
    - "Declarative genui specs render progressively during generation — valid subtrees render live, pending children show skeletons (STREAM-02, D-17)"
    - "A genui spec that fails final validation falls back to the existing SAFE_FALLBACK_SPEC path; no untrusted spec is executed (FOUND-6)"
  artifacts:
    - path: "apps/web/src/app/chat/_components/genui-part-boundary.tsx"
      provides: "Partial-JSON-parse + skeleton wrapper around the unmodified SpecRenderer (D-17)"
      contains: "safeParse"
    - path: "apps/web/src/app/chat/_components/sibling-nav.tsx"
      provides: "‹ N/M › sibling navigation (D-16)"
      contains: "siblings"
    - path: "apps/web/src/app/chat/_components/inline-error-card.tsx"
      provides: "Inline retryable error (CHAT-05)"
      contains: "Retry"
  key_links:
    - from: "genui-part-boundary.tsx"
      to: "@nauta/genui SpecRootSchema.safeParse + SpecRenderer + SAFE_FALLBACK_SPEC"
      via: "web-boundary schema gate + unmodified renderer"
      pattern: "SpecRootSchema|SpecRenderer|SAFE_FALLBACK_SPEC"
    - from: "turn-action-row.tsx"
      to: "useChatStream.regenerate"
      via: "regenerate button → sibling run"
      pattern: "regenerate"
    - from: "inline-error-card.tsx"
      to: "composer draft"
      via: "retry never clears draft"
      pattern: "Retry"
---

<objective>
Deliver the rich chat mechanics on top of the streamed core: regenerate-as-versioned-siblings with a
`‹ N/M ›` navigator (CHAT-04, D-16), inline retryable error recovery that never loses the composer
draft (CHAT-05, D-19), the distinct pre-turn cost-cap-blocked card + stopped/cost-capped marker badges
(D-15/D-21), and progressive partial-tree genui rendering — render-what's-valid + skeletons for pending
children (STREAM-02, D-17), wrapping the UNMODIFIED SpecRenderer and re-validating at the web boundary
(FOUND-6).

Purpose: the ChatGPT-grade table stakes plus the progressive genui payoff that closes GEN-04.

Output: TurnActionRow, SiblingNav, InlineErrorCard, CostCapBlockedCard, TurnStatusBadge,
GenuiPartBoundary, and the MessageTurn wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md
@.planning/phases/22-chat-spine-persistence-streaming/22-CONTEXT.md

<interfaces>
<!-- From 22-07/08 + existing genui package. -->
Renderer (UNMODIFIED) — packages/genui/src/renderer/spec-renderer.tsx expects a PRE-VALIDATED spec.
Web-boundary gate — @nauta/genui/schema exports SpecRootSchema (safeParse) + SAFE_FALLBACK_SPEC (the
existing genui.generate.ts pattern: safeParse fail → SAFE_FALLBACK_SPEC).
Skeleton — @nauta/ui/skeleton. Card — bordered `border border-border rounded-lg p-4 my-2` per UI-SPEC.
useChatStream (22-08): { state, parts, send, regenerate, stop }; regenerate(assistantMessageId).
History (22-05): messages carry siblingGroupId, version, isActive → group assistant siblings for the counter.
Run-event terminal states (22-06): completed | stopped | failed | cost_capped | interrupted.
UI-SPEC contracts: regenerate produces a new sibling (never replaces); SiblingNav ‹ N/M › shown only
when M>1; failed turn → InlineErrorCard (role="alert", border-destructive/30 bg-destructive/5,
AlertTriangle, Retry) in place of the turn content; cost-cap block → CostCapBlockedCard (same family,
NO retry); stopped/cost-capped → neutral Badge variant="secondary" "Stopped by user" / "Cost-capped ·
partial response" (mutually exclusive, one slot); progressive genui → render valid subtrees + 3 stacked
Skeleton bars (w-full, w-5/6, w-2/3) for pending children; final invalid → SAFE_FALLBACK_SPEC.
Draft/turn state are fully decoupled — a failure elsewhere never touches the composer draft.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TurnActionRow + SiblingNav (regenerate → versioned siblings)</name>
  <read_first>
    - apps/web/src/app/chat/_components/message-turn.tsx (where the action row mounts — from 22-08)
    - apps/web/src/app/chat/_hooks/use-chat-stream.ts (regenerate() → /api/chat/regenerate — from 22-08)
    - .planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md (TurnActionRow always-visible, SiblingNav ‹ N/M › shown only when M>1, active is the only indicator)
  </read_first>
  <action>
    Create TurnActionRow (always-visible subtle icon row under an assistant turn: copy + regenerate;
    text-muted-foreground, not hover-only per accessibility) and SiblingNav (‹ prev  N/M  next ›,
    rendered only when a turn's sibling group has M>1). Regenerate calls useChatStream.regenerate(
    assistantMessageId) → the FastAPI regenerate SSE (22-07) → a new active sibling; after completion
    invalidate getHistory so the sibling group reflects M+1. SiblingNav navigation swaps only the
    rendered sibling's content locally (does not re-fetch or affect other turns); the active sibling is
    the server's source of truth for forward context (D-16 — UI shows the counter, no extra chrome).
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - sibling-nav.tsx renders the ‹ N/M › counter only when siblings.length > 1 (guarded)
    - turn-action-row.tsx renders regenerate as an always-visible button (not gated behind a hover-only class)
    - Regenerate invokes useChatStream.regenerate and invalidates chat.getHistory on completion
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Regenerate creates a navigable sibling version with a ‹ N/M › counter; the action row is always-visible; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Inline error recovery + cost-cap-blocked card + status marker badges</name>
  <read_first>
    - apps/web/src/app/studio/_components/ (generation-state-chrome fallback banner: role="alert", border-destructive/30 bg-destructive/5, AlertTriangle — reuse verbatim)
    - apps/web/src/app/chat/_components/composer.tsx (draft state — must stay untouched on turn failure, from 22-08)
    - .planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md (error/cap copy, marker badges, draft-decoupling rule)
  </read_first>
  <action>
    Create InlineErrorCard (role="alert", destructive-tinted banner reused verbatim from
    generation-state-chrome, copy "Something went wrong generating this response." + a Retry button that
    re-runs the turn) rendered in place of a failed assistant turn's content — WITHOUT touching the
    composer draft (draft and turn state are decoupled; assert this). Create CostCapBlockedCard (same
    visual family, copy "This turn would exceed today's cost limit." + the admin/config line, NO Retry)
    rendered when a pre-turn cost block occurs. Create TurnStatusBadge rendering a single neutral
    Badge variant="secondary": "Stopped by user" for status='stopped'/'interrupted', "Cost-capped ·
    partial response" for status='cost_capped' (mutually exclusive, one slot). Wire these into
    MessageTurn by terminal status.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - inline-error-card.tsx uses role="alert" + a Retry action; cost-cap-blocked-card.tsx has NO retry action
    - The retry handler does not clear/modify the composer draft (draft state is not referenced in the error/retry path)
    - turn-status-badge.tsx maps stopped/interrupted → "Stopped by user" and cost_capped → "Cost-capped · partial response", never both at once
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Failed turns show an inline retryable error (draft preserved), cost blocks show a no-retry card, and stopped/cost-capped partials carry the correct neutral marker; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GenuiPartBoundary — progressive partial-tree rendering + final-invalid fallback</name>
  <read_first>
    - packages/genui/src/renderer/spec-renderer.tsx (the UNMODIFIED renderer it wraps; expects a pre-validated spec)
    - packages/genui/src/schema/safe-fallback-spec.ts + spec-schema.ts (SAFE_FALLBACK_SPEC + SpecRootSchema.safeParse — the web-boundary gate)
    - apps/web/src/app/chat/_components/message-turn.tsx (where genui parts mount, interleaved with text — from 22-08)
    - .planning/phases/22-chat-spine-persistence-streaming/22-UI-SPEC.md (D-17 render valid subtrees + 3-bar skeletons; final invalid → SAFE_FALLBACK_SPEC; genui part in a bordered Card)
  </read_first>
  <behavior>
    - given a complete valid spec, GenuiPartBoundary renders it via the unmodified SpecRenderer inside a bordered Card
    - given a partial (still-streaming) JSON buffer, it renders the subtrees that already validate and shows 3 stacked Skeleton bars (w-full, w-5/6, w-2/3) for not-yet-arrived children — the tree grows as more arrives
    - given a completed-but-invalid spec, it renders SAFE_FALLBACK_SPEC via the existing path (no new fallback UI)
    - no eval / new Function anywhere on this render path (declarative only)
    - text and genui parts render in emission order (D-18) — genui in a Card, text as plain flowing markdown
  </behavior>
  <action>
    Create GenuiPartBoundary({ specJson, isStreaming }): progressively parse the accumulating tool-call
    JSON (a lenient partial parse — e.g. attempt JSON.parse on the buffer, and on failure render the
    last-valid partial + skeletons for pending children); run SpecRootSchema.safeParse on the current
    parseable subtree and render valid nodes via the UNMODIFIED SpecRenderer inside a bordered Card,
    with the 3-bar Skeleton placeholder for pending children. When isStreaming ends, run a final
    safeParse — success renders the full spec, failure renders SAFE_FALLBACK_SPEC (existing path,
    unchanged). Update MessageTurn to route genui parts through GenuiPartBoundary interleaved with text
    parts. Never introduce eval/Function on this path.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/app/chat/_components/__tests__/genui-part-boundary.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test: a complete valid spec renders the SpecRenderer output (a known node) inside a Card
    - Test: a partial JSON buffer renders at least one valid subtree AND at least one Skeleton placeholder
    - Test: a completed invalid spec renders the SAFE_FALLBACK_SPEC content (fallback path hit)
    - `grep -rEc "eval\(|new Function" apps/web/src/app/chat/_components/genui-part-boundary.tsx` returns 0 (no-eval gate on the renderer path)
    - `pnpm vitest run` for the boundary test passes; `cd apps/web && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GenuiPartBoundary renders valid subtrees live with skeletons for pending children, falls back safely on final-invalid, and executes no code — the SpecRenderer stays unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Streamed genui spec → renderer | model-authored spec JSON is untrusted (FOUND-6); it must cross the schema gate before rendering |
| Retry/regenerate actions → server | user-initiated re-runs must not double-fire or corrupt turn state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-33 | Tampering (malicious spec) | genui partial/final spec | mitigate | SpecRootSchema.safeParse at the web boundary; invalid → SAFE_FALLBACK_SPEC; renderer stays declarative (FOUND-6) |
| T-22-34 | Elevation of privilege | code execution via spec | mitigate | no eval / new Function on the render path (grep gate = 0); unmodified declarative SpecRenderer only |
| T-22-35 | Denial of service | pathological partial JSON parse loop | mitigate | partial parse is a bounded best-effort attempt per delta; no unbounded retry; skeleton fallback |
| T-22-36 | Availability | a failure elsewhere wiping the user's draft | mitigate | draft and turn state fully decoupled — the retry/error path never references the composer draft (CHAT-05/D-19) |
</threat_model>

<verification>
- `pnpm vitest run src/app/chat/_components/__tests__/genui-part-boundary.test.tsx` passes
- `apps/web` tsc + `pnpm next build` green; no-eval grep gate on the renderer path returns 0
- Manual: regenerate → ‹ 1/2 › navigates; a forced failure shows Retry with the draft intact; a genui spec renders progressively
</verification>

<success_criteria>
- CHAT-04 regenerate siblings, CHAT-05 inline retryable error (draft preserved), STREAM-02 progressive genui — all live
- D-15/17/18/19/21 honored; unmodified SpecRenderer; no-eval renderer path
</success_criteria>

<output>
Create `.planning/phases/22-chat-spine-persistence-streaming/22-09-SUMMARY.md` when done.
</output>
