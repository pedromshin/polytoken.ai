---
phase: 49-live-loop-gate-deploy-oauth-real-email
plan: 04
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - packages/db/scripts/verify-0031-live.ts
  - packages/db/scripts/verify-0032-live.ts
  - packages/db/scripts/verify-0033-live.ts
  - packages/db/scripts/verify-0034-live.ts
  - packages/db/scripts/verify-0035-live.ts
  - .planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/migration-verification.md
autonomous: true
requirements: [LIVE-02]
must_haves:
  truths:
    - "Migrations 0026–0035 are applied to staging FIRST, live-verified, THEN to production, live-verified (migrations-first discipline)"
    - "Live verification is a read-only SQL check of the actual expected objects (threads, forwarding_addresses, user_id columns/NOT NULL, RLS policies, extracted_only view), not a trust of the migrate tool's exit code"
    - "The Supabase API roles can actually read the migrated tables on each host (has_table_privilege) — the fresh-DB grant gotcha did not silently 403 the app"
    - "ECS + Vercel are observably green on the deployed codebase (ALB /health 200 + Vercel prod URL 200)"
  artifacts:
    - path: "packages/db/scripts/verify-0035-live.ts"
      provides: "Read-only assertion that threads + forwarding_addresses tables and emails.thread_id exist"
      contains: "forwarding_addresses"
    - path: ".planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/migration-verification.md"
      provides: "Captured staging+prod verifier output + health-check evidence"
  key_links:
    - from: "packages/db/scripts/verify-003N-live.ts"
      to: "hosted Postgres (POSTGRES_URL_NON_POOLING)"
      via: "direct pg query, mirrors verify-0030-live.ts idiom"
      pattern: "POSTGRES_URL_NON_POOLING"
    - from: "deploy verification"
      to: "ALB /health"
      via: "curl the alb_dns_name /health → 200"
      pattern: "/health"
---

<objective>
Apply migrations 0026–0035 to staging AND production with migrations-first discipline (staging →
live-verify → prod → live-verify) and confirm ECS + Vercel are green on the deployed codebase.
Verification is read-only SQL against each hosted DB for the ACTUAL objects the v1.5–v1.7 migrations
introduced — never the migrate tool's exit code alone — plus a role-access check that catches the
fresh-DB grant gotcha, plus health-endpoint proof of the running services. Deploys are
pre-authorized this phase (STANDING RULE); this is autonomous machine work, no user gate.

Purpose: LIVE-02 — "Migrations 0026–0035 applied to staging AND production and live-verified; ECS +
Vercel deploys green on the renamed codebase."
Output: verify-0031..0035-live.ts + a captured staging/prod verification artifact.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-live-loop-gate-deploy-oauth-real-email/49-CONTEXT.md

<interfaces>
<!-- Exact tooling + object anchors. -->

Migration runners (packages/db, read POSTGRES_URL_NON_POOLING from the per-env dotenv file):
  npm run db:migrate:staging   → migrate:staging = with-env:staging (../../.env.staging) tsx src/migrate.ts
  npm run db:migrate:prod      → migrate:prod    = with-env:prod    (../../.env.production) tsx src/migrate.ts
  migrate.ts applies ALL pending drizzle migrations (0000–0035); staging/prod are behind, so it fills the gap.
  Pooler host is aws-1 (NOT aws-0); non-pooling port 5432 for migrations.

Existing live verifiers to MIRROR (same pg.Client + POSTGRES_URL_NON_POOLING + exit 0/1 idiom):
  packages/db/scripts/verify-0026-live.ts … verify-0030-live.ts   (run these too)
  Run one: cd packages/db && npm run with-env:staging -- tsx scripts/verify-0026-live.ts
           (and with-env:prod for production)

Objects the NEW verifiers must assert (from the migration SQL):
  0031 add_user_id_columns   → user_id column on chat_conversations, chat_cost_ledger, importers (+ FKs, indexes)
  0032 backfill_user_id      → no NULL user_id remaining on those three tables (backfill completed)
  0033 user_id_not_null      → user_id is NOT NULL on chat_conversations, chat_cost_ledger, importers
  0034 rls_user_scoping      → owner RLS policies present (pg_policies) on the user-scoped tables
  0035 threads_forwarding    → tables threads + forwarding_addresses exist; emails.thread_id column exists;
                                unique indexes idx_forwarding_addresses_token_unique / _user_id_unique exist

Deploy verification (no gh needed):
  ALB health: curl http://nauta-services-email-listener-2115368239.us-east-1.elb.amazonaws.com/health → 200 JSON {"status":"alive"}
  Vercel prod: curl the production web URL → 200 (derive URL from `vercel ls`/dashboard; if not derivable
    autonomously, record the URL-confirmation as a checklist item for plan 49-06).

Grant remediation (if a hosted host shows 42501 / has_table_privilege false after migrate) — SAME idempotent
  GRANT + NOTIFY pgrst as docs/RUN-LOCAL.md, applied via the hosted DB connection. This is within the
  pre-authorized migration scope (a migration that lands unreadable tables is not "done").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write live verifiers for 0031–0035 (mirror the 0030 idiom)</name>
  <read_first>
    - packages/db/scripts/verify-0030-live.ts (the exact pg.Client + assert + exit-code pattern to copy)
    - packages/db/migrations/0031_add_user_id_columns.sql, 0033_user_id_not_null.sql, 0034_rls_user_scoping.sql, 0035_threads_forwarding.sql (the ground truth objects to assert)
    - packages/db/migrations/0032_backfill_user_id.sql (what "backfill complete" means → assert no NULL user_id)
    - packages/db/src/client.ts (the env import verifiers use)
  </read_first>
  <action>
    Create packages/db/scripts/verify-0031-live.ts through verify-0035-live.ts, each following the
    verify-0030-live.ts structure exactly (pg.Client on env.POSTGRES_URL_NON_POOLING; print each
    assertion; process.exit(1) on any failure, exit(0) on all-pass). Assert the concrete objects listed
    in <interfaces> per migration: 0031 → the three user_id columns exist (information_schema.columns);
    0032 → SELECT count(*) WHERE user_id IS NULL = 0 on chat_conversations, chat_cost_ledger, importers;
    0033 → is_nullable = 'NO' for user_id on those three tables; 0034 → pg_policies rows exist for the
    owner policies on the user-scoped tables; 0035 → to_regclass for public.threads and
    public.forwarding_addresses is non-null, emails.thread_id column exists, and the two unique indexes
    exist (pg_indexes). Type everything explicitly. These scripts are host-agnostic — they run against
    whichever DB the with-env dotenv file points at (staging or prod).
  </action>
  <verify>
    <automated>bash -lc "for n in 0031 0032 0033 0034 0035; do test -f packages/db/scripts/verify-$n-live.ts || { echo MISSING $n; exit 1; }; done; grep -q 'forwarding_addresses' packages/db/scripts/verify-0035-live.ts && grep -q 'is_nullable' packages/db/scripts/verify-0033-live.ts && npm run -s typecheck -w @polytoken/db"</automated>
  </verify>
  <done>Five new verifier scripts exist, typecheck under packages/db, and assert the concrete objects for 0031–0035 using the established pg-query idiom.</done>
  <acceptance_criteria>
    - `npm run typecheck -w @polytoken/db` passes with the five new files present.
    - verify-0032-live.ts asserts zero NULL user_id (grep `IS NULL` + count assertion).
    - verify-0035-live.ts asserts both threads and forwarding_addresses via to_regclass and emails.thread_id.
    - Each script exits nonzero on a failed assertion (grep `process.exit(1)`).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply migrations staging → verify → prod → verify (migrations-first)</name>
  <read_first>
    - packages/db/src/migrate.ts (extensions setup, BACKFILL_USER_ID GUC, single-connection behavior)
    - .env.staging and .env.production presence (confirm POSTGRES_URL_NON_POOLING is set — do NOT print values)
    - .planning/phases/49-live-loop-gate-deploy-oauth-real-email/49-CONTEXT.md (Deploy & Migration Execution decisions: staging first, live-verify = SQL + health, fix-forward)
  </read_first>
  <action>
    Execute the migrations-first sequence. STAGING FIRST: assert .env.staging has a non-empty
    POSTGRES_URL_NON_POOLING (fail-closed with a clear message if absent — per the deploy playbook the
    hosted DB credentials are available on this machine); run `npm run db:migrate:staging`; then run ALL
    ten verifiers (verify-0026 … verify-0035-live.ts) via `npm run with-env:staging -- tsx
    scripts/verify-00NN-live.ts` from packages/db and require every one to exit 0; also run a
    role-access check (has_table_privilege('service_role', ...) on a migrated table such as
    forwarding_addresses) and, if it is false / any 42501 appears, apply the idempotent GRANT + NOTIFY
    pgrst remediation against staging and re-check. ONLY when staging is fully green, repeat the entire
    block against PRODUCTION with the :prod / with-env:prod variants. Capture every verifier's PASS/FAIL
    line plus the health checks into
    .planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/migration-verification.md.
    Do NOT print connection strings or secrets into the artifact.
  </action>
  <verify>
    <automated>bash -lc "test -f .planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/migration-verification.md && grep -qi 'staging' .planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/migration-verification.md && grep -qi 'production' .planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/migration-verification.md && grep -Eqi 'VERIFICATION PASSED|PASS' .planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/migration-verification.md && echo OK"</automated>
  </verify>
  <done>0026–0035 are applied and all ten live verifiers pass on staging THEN production; role-access is confirmed (or remediated); the artifact records both hosts' results with no secret leakage.</done>
  <acceptance_criteria>
    - The artifact shows staging verified BEFORE production (order visible in the log).
    - All ten verifiers report PASS on both hosts (or a fix-forward is documented and re-verified green).
    - A has_table_privilege('service_role', ...) check is recorded true for both hosts.
    - `grep -Ei "postgresql://|:[0-9]{4,5}/postgres|GOCSPX-" .../migration-verification.md` returns nothing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Confirm ECS + Vercel deploys are green (health-observable)</name>
  <read_first>
    - .github/workflows/deploy-email-listener.yml (trigger = push to main touching apps/email-listener/**; own pytest gate; ALB smoke test)
    - infrastructure/aws/terraform.tfvars (alb_dns_name for the ECS health URL)
    - infrastructure/aws/iam.tf (OIDC trust binds deploys to var.github_repository — DO NOT rename the GitHub repo here; that break is handled in plan 49-06)
  </read_first>
  <action>
    Confirm the deployed services are green WITHOUT depending on `gh` (its auth is invalid this run).
    Push main via git if there is pending apps/email-listener/** work to deploy (deploys are
    pre-authorized this phase); the ECS workflow runs its own pytest gate — if it is red, diagnose and
    fix forward within the phase (the deploy gate is first-class, not rolled back). Verify ECS
    observably: curl http://nauta-services-email-listener-2115368239.us-east-1.elb.amazonaws.com/health
    and require HTTP 200 with {"status":"alive"}. Verify Vercel: curl the production web URL and require
    200. Record both results in the migration-verification artifact. IMPORTANT: do NOT rename the GitHub
    repo in this plan — the OIDC deploy trust (iam.tf:128, `repo:${var.github_repository}:*`) would
    break and its terraform-apply fix is re-parked; the GitHub rename is deferred to the user checkpoint
    (49-06). If the ECS deploy cannot be triggered/watched autonomously (gh unauthed and no
    apps/email-listener change to push) OR the Vercel prod URL is not derivable autonomously, record the
    exact "push/trigger + confirm deploy SHA + confirm URL 200" steps as a hand-off item for plan 49-06
    rather than faking a pass.
  </action>
  <verify>
    <automated>bash -lc "grep -Eqi '/health|alb|vercel|deploy' .planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/migration-verification.md && echo OK"</automated>
  </verify>
  <done>ECS /health returns 200 and the Vercel prod URL returns 200 (both recorded), OR the un-automatable trigger/URL-confirm steps are cleanly handed off to the 49-06 checklist. The GitHub repo was NOT renamed here.</done>
  <acceptance_criteria>
    - The artifact records the ALB /health result (200 alive) or an explicit checkpoint hand-off with the exact steps.
    - The artifact records the Vercel prod result (200) or an explicit checkpoint hand-off.
    - No `gh` command is relied upon for a pass; no GitHub repo rename occurred in this plan.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine → hosted Supabase (staging/prod) | migration writes + role grants against live DBs |
| public internet → ALB /health | health probe of the deployed ECS task |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04-01 | Tampering | applying prod migrations before staging is verified | mitigate | strict staging → verify → prod → verify ordering; prod block gated on staging all-green |
| T-49-04-02 | Denial of Service | fresh-DB grant gotcha silently 403s the app post-migrate | mitigate | has_table_privilege role check + idempotent GRANT/NOTIFY remediation on each host |
| T-49-04-03 | Information Disclosure | connection strings/secrets leaking into the artifact | mitigate | acceptance grep forbids postgresql:// and key literals in the artifact |
| T-49-04-04 | Elevation of Privilege | renaming GitHub repo breaks OIDC deploy trust (re-parked apply) | mitigate | GitHub rename explicitly NOT done here; deferred to 49-06 with the OIDC hazard surfaced |
| T-49-04-SC | Tampering | npm/pip/cargo installs | mitigate | no new packages — drizzle/tsx/pg already present |
</threat_model>

<verification>
- Five verifiers created and typechecked; all ten verifiers pass on staging then prod.
- Role-access confirmed on both hosts; artifact free of secrets.
- ECS + Vercel green recorded, or cleanly handed off; no GitHub rename performed.
</verification>

<success_criteria>
LIVE-02 met: 0026–0035 live on staging and production, live-verified by read-only SQL and role-access
checks, with ECS + Vercel observably green (or the residual confirmation cleanly handed to the user
checkpoint). No re-parked AWS/OIDC surface was touched.
</success_criteria>

<output>
Create `.planning/phases/49-live-loop-gate-deploy-oauth-real-email/49-04-SUMMARY.md` when done,
recording per-host verifier results and the deploy-green evidence (or hand-off).
</output>
