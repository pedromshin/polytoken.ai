---
phase: 49-live-loop-gate-deploy-oauth-real-email
plan: 05
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - .planning/phases/49-live-loop-gate-deploy-oauth-real-email/EXTERNAL-IDENTITY-DECISIONS.md
  - .planning/milestones/v1.7-phases/43-auth-google-oauth-sessions-supabase-auth/JWT-SIGNING-KEY-AUDIT.md
  - .planning/STATE.md
autonomous: true
requirements: [LIVE-07]
must_haves:
  truths:
    - "Every EXTERNAL-RENAME-RUNBOOK.md leftover is DECIDED and recorded — GitHub (execute, but OIDC-gated → user checkpoint), Vercel (execute now), AWS/Terraform (re-parked explicitly with rationale), domain (user-only, out of scope)"
    - "The AWS/Terraform re-park decision is recorded in STATE.md, not silently parked"
    - "The local Supabase project-id nauta→polytoken decision (actualized in plan 49-01) is recorded in STATE.md"
    - "The GitHub-repo-rename → OIDC-deploy-trust coupling is documented so the user does not blindly rename and break deploys"
    - "The JWT signing-key audit (both hosted = ES256) is folded into the tracked planning tree, not left as an untracked file"
  artifacts:
    - path: ".planning/phases/49-live-loop-gate-deploy-oauth-real-email/EXTERNAL-IDENTITY-DECISIONS.md"
      provides: "Per-item disposition table for all LIVE-07 external-identity leftovers + the OIDC coupling analysis"
      contains: "OIDC"
    - path: ".planning/STATE.md"
      provides: "Recorded LIVE-07 decisions (AWS re-park + local project-id)"
      contains: "LIVE-07"
  key_links:
    - from: "EXTERNAL-IDENTITY-DECISIONS.md"
      to: "infrastructure/aws/iam.tf OIDC trust"
      via: "documents that GitHub rename breaks repo:${var.github_repository}:* until a re-parked terraform apply"
      pattern: "github_repository|OIDC"
    - from: ".planning/STATE.md"
      to: "AWS/Terraform re-park + local project-id decisions"
      via: "recorded decision entries"
      pattern: "re-park|project.?id"
---

<objective>
Decide-not-park every external-identity leftover (LIVE-07) and record the decisions durably. GitHub
repo rename and Vercel project rename were decided EXECUTE; AWS/Terraform renames RE-PARK; the local
Supabase project-id was RENAME-now (actualized in plan 49-01). This plan: (1) writes the per-item
disposition dossier including the newly-discovered GitHub-rename → OIDC-deploy-trust coupling,
(2) attempts the Vercel project rename autonomously (falling back to a documented checklist step if
the CLI is unauthed), (3) records the AWS re-park and local project-id decisions in STATE.md, and
(4) folds the existing JWT-signing-key audit into the tracked tree.

Purpose: the STANDING RULE is that these are decided, never deferrable-by-default. The GitHub rename
itself is HELD for the user checkpoint (49-06) because doing it autonomously would break the OIDC
deploy trust whose fix (terraform apply) is re-parked — a landmine under the deploy gate.
Output: EXTERNAL-IDENTITY-DECISIONS.md, STATE.md decision records, tracked JWT audit.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-live-loop-gate-deploy-oauth-real-email/49-CONTEXT.md
@.planning/milestones/v1.7-phases/42-atomic-rename-nauta-polytoken/EXTERNAL-RENAME-RUNBOOK.md
@.planning/milestones/v1.7-phases/43-auth-google-oauth-sessions-supabase-auth/JWT-SIGNING-KEY-AUDIT.md

<interfaces>
<!-- The concrete coupling this dossier must capture. -->

OIDC coupling (discovered during planning — the reason GitHub rename is user-gated):
  infrastructure/aws/iam.tf:110-131  github_assume trust condition:
    token.actions.githubusercontent.com:sub  StringLike  "repo:${var.github_repository}:*"
  infrastructure/aws/terraform.tfvars: github_repository = "pedromshin/nauta.services.email-listener"
  → After a GitHub repo rename, new CI runs present sub=repo:pedromshin/<new-name>:... which no longer
    matches the trust condition → the OIDC AssumeRoleWithWebIdentity fails → ECS deploys break.
    Fixing it requires updating var.github_repository + `terraform apply` on the IAM role — and that
    apply is RE-PARKED (AWS/Terraform section). So GitHub rename ≠ a safe blind autonomous action.

CLI reality this run:
  gh auth = INVALID (keyring token bad) → no autonomous GitHub rename, no `gh workflow run`.
  vercel CLI present at %APPDATA%/npm/vercel → attempt `vercel project rename`; if unauthed, document.

Decisions to record (from 49-CONTEXT.md External-Identity Decisions):
  GitHub rename  = EXECUTE, but OIDC-gated → surfaced as a user checkpoint step in 49-06.
  Vercel rename  = EXECUTE now (no OIDC coupling; Vercel git link is by repo id, survives GitHub rename).
  AWS/Terraform  = RE-PARK explicitly (Hazards A/B/C: two unsynced sources of truth, ECR force_delete=false
                   destroy risk, local-only tfstate). Record in STATE.md.
  Local Supabase project-id nauta→polytoken = RENAMED (actualized in plan 49-01; config.toml project_id="polytoken").
  Domain/DNS = user-only, out of scope (REQUIREMENTS.md Out of Scope).

JWT audit: JWT-SIGNING-KEY-AUDIT.md (currently UNTRACKED) records staging+prod = ES256 (asymmetric),
  local = HS256. Fold into the tracked tree; the user will re-confirm while in the Supabase Dashboard (49-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write EXTERNAL-IDENTITY-DECISIONS.md + attempt Vercel rename</name>
  <read_first>
    - .planning/milestones/v1.7-phases/42-atomic-rename-nauta-polytoken/EXTERNAL-RENAME-RUNBOOK.md (§1 GitHub, §2 AWS hazards, §3 Vercel, §4 domain)
    - infrastructure/aws/iam.tf (the github_assume trust block — the OIDC coupling to document)
    - infrastructure/aws/terraform.tfvars (github_repository current value)
    - .planning/phases/49-live-loop-gate-deploy-oauth-real-email/49-CONTEXT.md (External-Identity Decisions)
  </read_first>
  <action>
    Create .planning/phases/49-live-loop-gate-deploy-oauth-real-email/EXTERNAL-IDENTITY-DECISIONS.md
    with a per-item disposition table covering all five leftovers (GitHub, Vercel, AWS/Terraform, local
    Supabase project-id, domain/DNS), each with: decision, rationale, executed-by (autonomous vs user
    checkpoint), and status. Include a dedicated "GitHub rename ↔ OIDC deploy trust" subsection spelling
    out the coupling from <interfaces>: renaming the repo changes the OIDC `sub` claim so
    `repo:${var.github_repository}:*` no longer matches, breaking ECS deploys until var.github_repository
    is updated and `terraform apply` is run — and that apply sits in the re-parked AWS section. State the
    two safe options for the user (either rename the repo AND run the companion IAM `terraform apply`
    together, or accept that CI deploys pause until they do) and mark GitHub rename as a 49-06 checkpoint
    step, NOT an autonomous action. Then ATTEMPT the Vercel project rename: run `vercel project rename`
    (new name matching the polytoken identity); if the CLI is unauthenticated or fails, do NOT block —
    record the exact dashboard steps (Vercel → Project Settings → General → Project Name) as a checklist
    item to fold into 49-06, and note that the git integration (repo-id-based) survives the rename.
    Record the actual outcome (renamed vs deferred) in the dossier.
  </action>
  <verify>
    <automated>bash -lc "test -f .planning/phases/49-live-loop-gate-deploy-oauth-real-email/EXTERNAL-IDENTITY-DECISIONS.md && for s in GitHub Vercel 'AWS' OIDC 'project_id' domain; do grep -qi \"$s\" .planning/phases/49-live-loop-gate-deploy-oauth-real-email/EXTERNAL-IDENTITY-DECISIONS.md || { echo MISSING $s; exit 1; }; done; echo OK"</automated>
  </verify>
  <done>The dossier records a decision + rationale + executor for all five leftovers, documents the GitHub/OIDC coupling and the two safe user options, and records the Vercel rename outcome (done or deferred to 49-06).</done>
  <acceptance_criteria>
    - The dossier's GitHub row explicitly says the rename is a 49-06 checkpoint step and explains the OIDC break.
    - The dossier's AWS row says RE-PARK with the three hazards named.
    - The Vercel row records either "renamed via CLI" or "deferred to 49-06 dashboard step" — not an unstated assumption.
    - `grep -qi "repo:" ...DECISIONS.md` shows the OIDC sub-claim is quoted.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Record LIVE-07 decisions in STATE.md + fold in the JWT audit</name>
  <read_first>
    - .planning/STATE.md (first ~40 lines — the current-position / decisions area; edit directly, the SDK STATE handlers fail on this free-form file)
    - .planning/phases/49-live-loop-gate-deploy-oauth-real-email/49-01-SUMMARY.md (local project-id actualization outcome to cite)
    - .planning/milestones/v1.7-phases/43-auth-google-oauth-sessions-supabase-auth/JWT-SIGNING-KEY-AUDIT.md (content to confirm + fold in)
  </read_first>
  <action>
    Add a concise "Phase 49 — LIVE-07 external-identity decisions" section to .planning/STATE.md (edit
    the file directly — do not use the gsd-sdk STATE handlers, which fail on this project's free-form
    STATE.md) recording: (a) AWS/Terraform resource renames RE-PARKED explicitly with the Hazard A/B/C
    rationale and a pointer to EXTERNAL-IDENTITY-DECISIONS.md; (b) the local Supabase project-id
    nauta→polytoken RENAME actualized in plan 49-01 (config.toml project_id="polytoken", fresh
    containers accepted, migrations re-run); (c) GitHub rename decided EXECUTE but held for the 49-06
    checkpoint due to the OIDC coupling; (d) Vercel rename outcome from Task 1. Keep it factual and
    ASCII. Then fold the JWT-SIGNING-KEY-AUDIT.md into the tracked tree: confirm its content still reads
    staging+prod = ES256 / local = HS256 (the value the user will re-confirm in the Supabase Dashboard
    during 49-06), add a one-line "verified 2026-07-10 (Phase 49); user to re-confirm in Dashboard"
    note, and ensure it is git-added (it is currently untracked) so it stops being an orphan file. Do
    not alter its recorded findings unless the content is self-evidently stale.
  </action>
  <verify>
    <automated>bash -lc "grep -qi 'LIVE-07' .planning/STATE.md && grep -Eqi 're-park|reparked|re-parked' .planning/STATE.md && grep -Eqi 'project.?id' .planning/STATE.md && grep -qi 'ES256' .planning/milestones/v1.7-phases/43-auth-google-oauth-sessions-supabase-auth/JWT-SIGNING-KEY-AUDIT.md && echo OK"</automated>
  </verify>
  <done>STATE.md records the AWS re-park, local project-id rename, GitHub-held, and Vercel outcome; the JWT audit is confirmed, annotated, and git-tracked.</done>
  <acceptance_criteria>
    - `grep -qi "LIVE-07" .planning/STATE.md` succeeds and the section names the AWS re-park + local project-id.
    - STATE.md notes GitHub rename is held for the 49-06 checkpoint (not silently executed).
    - `git status --short .planning/milestones/v1.7-phases/43-auth-google-oauth-sessions-supabase-auth/JWT-SIGNING-KEY-AUDIT.md` shows it staged/tracked (no longer `??`).
    - STATE.md additions are ASCII only.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Vercel account | project rename via CLI/dashboard |
| planning docs → live deploy trust | documenting (not executing) the OIDC-coupled GitHub rename |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05-01 | Denial of Service | blind GitHub rename breaks OIDC → deploys fail | mitigate | rename held for user checkpoint with the coupling + safe options documented; not executed autonomously |
| T-49-05-02 | Repudiation | a leftover silently parked instead of decided | mitigate | every item gets an explicit recorded disposition in STATE.md + the dossier |
| T-49-05-03 | Information Disclosure | JWT-mode audit contains no secrets, only key-type facts | accept | audit records ES256/HS256 mode only — no key material |
| T-49-05-SC | Tampering | npm/pip/cargo installs | mitigate | no new packages (docs + optional vercel CLI only) |
</threat_model>

<verification>
- EXTERNAL-IDENTITY-DECISIONS.md covers all five leftovers with dispositions and the OIDC analysis.
- STATE.md records the AWS re-park + local project-id + GitHub-held + Vercel outcome (ASCII).
- JWT audit is confirmed and now git-tracked.
</verification>

<success_criteria>
LIVE-07's "decided, not parked" bar is met for the autonomous items; the one item that cannot be
safely automated (GitHub rename, due to the OIDC coupling) is fully analyzed and handed to the user
checkpoint rather than parked or blindly executed.
</success_criteria>

<output>
Create `.planning/phases/49-live-loop-gate-deploy-oauth-real-email/49-05-SUMMARY.md` when done,
listing each leftover's final disposition and the Vercel rename outcome.
</output>
