---
phase: 49-live-loop-gate-deploy-oauth-real-email
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/RUN-LOCAL.md
  - scripts/preflight-local.ps1
autonomous: true
requirements: [LIVE-01, LIVE-07]
must_haves:
  truths:
    - "A single documented procedure brings the whole local stack up green from cold, with no manual zombie hunting"
    - "The local stack runs under Supabase project_id=polytoken (not nauta) with a fresh DB and all migrations 0000–0035 applied"
    - "After a fresh DB rebuild, app tables are readable/writable by the Supabase roles (anon/authenticated/service_role) — no 42501 permission errors"
    - "The Phase-44 backfill (0032) succeeds because exactly one auth user is seeded before migrations run"
  artifacts:
    - path: "docs/RUN-LOCAL.md"
      provides: "Canonical cold-start procedure: env-file split, start order, zombie preflight, fresh-DB seed+grant gotchas"
      min_lines: 90
      contains: "preflight-local.ps1"
    - path: "scripts/preflight-local.ps1"
      provides: "Scripted preflight: kill stale procs, verify ports, sb start, seed single auth user, migrate, grant, NOTIFY pgrst"
      min_lines: 60
  key_links:
    - from: "docs/RUN-LOCAL.md"
      to: "scripts/preflight-local.ps1"
      via: "doc instructs running the preflight script first"
      pattern: "preflight-local\\.ps1"
    - from: "scripts/preflight-local.ps1"
      to: "GoTrue admin API"
      via: "seed single auth user before migrations"
      pattern: "admin/users"
    - from: "scripts/preflight-local.ps1"
      to: "Supabase roles grant"
      via: "idempotent GRANT + NOTIFY pgrst after migrate"
      pattern: "NOTIFY pgrst"
---

<objective>
Make the local stack reproducibly green from cold and finish actualizing the local Supabase
project-id rename (nauta → polytoken, LIVE-07). Produce the canonical start procedure
(`docs/RUN-LOCAL.md`) and a scripted preflight (`scripts/preflight-local.ps1`) that encodes every
local-run gotcha this project has already paid for: the env-file split, the zombie-process kill,
running servers WITHOUT `--reload`, the fresh-DB single-user seed the Phase-44 backfill requires,
and the post-migration Supabase-role GRANT + `NOTIFY pgrst`.

Purpose: LIVE-01 requires "a documented, reproducible start procedure — no zombie-process
ambiguity, verified against the DB not the terminal." This plan builds that procedure and the
preflight tooling; plan 49-03 consumes it to run the DB-verified end-to-end green path.
Output: docs/RUN-LOCAL.md, scripts/preflight-local.ps1.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/49-live-loop-gate-deploy-oauth-real-email/49-CONTEXT.md

# Source-of-truth local-run knowledge (READ — these encode the gotchas this plan must script):
# - Memory: chat-stack-local-run (env split, start order, project-id rename, fresh-DB seed/grant)
# - Memory: local-dev-logs-buffering (zombie processes; run WITHOUT --reload; verify via DB)

<interfaces>
<!-- Concrete anchors the executor uses; no exploration needed. -->

Root package.json scripts (already exist):
  sb:start = "supabase start"   sb:stop = "supabase stop"   sb:status = "supabase status"
  db:migrate = "npm run migrate:local -w @polytoken/db"   (tsx packages/db/src/migrate.ts)

supabase/config.toml (already renamed):
  project_id = "polytoken"      # local Docker containers/volumes are named supabase_*_polytoken
  [auth.external.google] uses env(SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID / _SECRET)

Env-file split (non-obvious — MUST be documented):
  - apps/email-listener/.env         → FastAPI listener (SUPABASE_URL=http://127.0.0.1:54321, Bedrock/AWS creds)
  - repo-root .env.local             → web app (dotenv -e ../../.env.local -- next dev) AND packages/db (db:migrate)

Start order (verified working):
  1. Docker Desktop up → npm run sb:start   (DB 54322, API 54321)
  2. Listener: cd apps/email-listener && uv run uvicorn app.main:app --host 127.0.0.1 --port 8000  (NO --reload)
  3. Web: cd apps/web && npm run dev  (port 3000)

Fresh-DB order-of-operations gotchas (from memory chat-stack-local-run):
  - 0032 backfill REFUSES to run unless auth.users has exactly ONE row → seed the user via GoTrue
    admin BEFORE `db:migrate`: POST http://127.0.0.1:54321/auth/v1/admin/users with the service_role
    key, body {"email":"pedromaschio.shin@gmail.com","email_confirm":true}.
  - After Drizzle migrations on a fresh DB, PostgREST + listener get 42501 "permission denied for
    table X" because Drizzle-owned tables lack grants → apply idempotent GRANTs to anon,
    authenticated, service_role (USAGE on schema + ALL on tables/sequences/routines + ALTER DEFAULT
    PRIVILEGES), then NOTIFY pgrst 'reload schema'. Pipe SQL via `docker exec -i` (plain docker exec
    drops stdin). pgvector/pg_trgm "no privileges granted" WARNINGs are harmless.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/RUN-LOCAL.md — the canonical cold-start procedure</name>
  <read_first>
    - docs/ (confirm no pre-existing RUN-LOCAL.md; if one exists, refresh it in place instead of duplicating)
    - README.md (Quickstart / Local development / Deployments sections — RUN-LOCAL.md links back to these, does not contradict them)
    - .planning/phases/49-live-loop-gate-deploy-oauth-real-email/49-CONTEXT.md (Local Stack Green Procedure decisions)
    - supabase/config.toml (confirm project_id = "polytoken")
    - apps/email-listener/.env.example and .env.example (the two env files the doc must distinguish)
  </read_first>
  <action>
    Create docs/RUN-LOCAL.md as the single source of truth for running the full local stack
    (Supabase + FastAPI listener + Next web). Structure it with these mandatory sections and
    concrete values:
    (1) Prerequisites — Docker Desktop running, uv, node 20+, npm workspaces (NOT pnpm — this repo
        overrides the global pnpm rule).
    (2) The env-file split, called out explicitly as the #1 footgun: apps/email-listener/.env feeds
        the listener; repo-root .env.local feeds BOTH the web app (via dotenv -e ../../.env.local)
        and packages/db (db:migrate). State that config.toml's env() Google refs resolve from the
        PROCESS env, not .env.local, so the shell must have SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID
        and _SECRET loaded before sb:start for local Google sign-in to work.
    (3) One-command cold start — run scripts/preflight-local.ps1 (Task 2), which brings up Supabase
        under project_id=polytoken, seeds the single auth user, migrates, grants, and reloads
        PostgREST. Then start the listener and web per the start order in <interfaces>.
    (4) Zombie-process rule — servers run WITHOUT --reload (a --reload child worker detaches stdout on
        Windows and becomes a silent zombie answering :8000); when a terminal is silent, TRUST THE DB
        not the terminal. Cite the exact kill command the preflight uses.
    (5) Project-id rename note (LIVE-07) — local stack now runs as polytoken; the old nauta stack, if
        still up, is stopped with `npx supabase stop --project-id nauta`; old nauta Docker volumes hold
        stale data that was NOT migrated (disposable per decision — local data is disposable).
    (6) Fresh-DB recovery — the exact seed-before-migrate ordering and the grant/NOTIFY remediation,
        pointing at the preflight script as the automated path.
    (7) Verification — how to confirm green via the DB (query row counts / has_table_privilege) rather
        than terminal output, and a pointer forward to the plan 49-03 e2e green-path spec.
    Do NOT paste any real secret value into the doc — reference sources only.
  </action>
  <verify>
    <automated>bash -lc "for s in 'env-file split' 'preflight-local.ps1' '--reload' 'NOTIFY pgrst' 'project_id' 'polytoken'; do grep -qi \"$s\" docs/RUN-LOCAL.md || { echo \"MISSING: $s\"; exit 1; }; done; echo OK"</automated>
  </verify>
  <done>docs/RUN-LOCAL.md exists, covers all seven sections, references the preflight script, states the env-file split and the WITHOUT-`--reload` rule, and contains no literal secret values.</done>
  <acceptance_criteria>
    - `grep -qi "env-file split" docs/RUN-LOCAL.md` succeeds and the surrounding text names both apps/email-listener/.env and root .env.local.
    - The doc names project_id=polytoken and the `npx supabase stop --project-id nauta` old-stack shutdown.
    - The doc states servers run WITHOUT --reload and that verification is DB-based, not terminal-based.
    - `grep -Ei "GOCSPX-[A-Za-z0-9]" docs/RUN-LOCAL.md` returns nothing (no leaked OAuth secret).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write scripts/preflight-local.ps1 — scripted zombie kill + fresh-DB green</name>
  <read_first>
    - packages/db/src/migrate.ts (the migrate entrypoint; note it runs `npm run migrate:local -w @polytoken/db` via root db:migrate and reads POSTGRES_URL_NON_POOLING from root .env.local)
    - supabase/config.toml (project_id, api port 54321, db port 54322)
    - package.json (sb:start / sb:status / db:migrate script names — call these, do not re-implement)
    - apps/email-listener/.env.example (listener env keys)
  </read_first>
  <action>
    Create scripts/preflight-local.ps1, a PowerShell script (Windows is the primary shell) that runs
    the local stack to a known-green DB state, idempotently, in this exact order:
    (1) Kill stale processes: `Get-Process python,uvicorn,node -ErrorAction SilentlyContinue |
        Stop-Process -Force` and confirm port 8000 and 3000 are free
        (`Get-NetTCPConnection -LocalPort 8000 -State Listen`) — warn, do not hard-fail, if none found.
    (2) Ensure Supabase is up under project_id=polytoken: run `npm run sb:start`; if a stale nauta
        stack is detected, stop it with `npx supabase stop --project-id nauta` first. Capture
        `npm run sb:status` output to read the service_role key and API URL (http://127.0.0.1:54321).
    (3) Seed EXACTLY ONE auth user BEFORE migrating (the 0032 backfill precondition): POST to
        http://127.0.0.1:54321/auth/v1/admin/users with header apikey/Authorization = the service_role
        key, JSON body {"email":"pedromaschio.shin@gmail.com","email_confirm":true}. Make it idempotent
        — treat "user already registered" as success. Do NOT create a second user.
    (4) Run migrations: `npm run db:migrate` (applies 0000–0035 via drizzle from root .env.local's
        POSTGRES_URL_NON_POOLING pointing at 127.0.0.1:54322).
    (5) Apply the idempotent Supabase-role grants by piping SQL into the DB container via
        `docker exec -i` (NOT plain docker exec): GRANT USAGE ON SCHEMA public and GRANT ALL ON ALL
        TABLES/SEQUENCES/ROUTINES IN SCHEMA public TO anon, authenticated, service_role; plus
        ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ...; then `NOTIFY pgrst, 'reload schema';`.
    (6) DB-based green assertion: query `has_table_privilege('service_role','public.chat_conversations','SELECT')`
        and confirm the expected table count — print a clear PASS/FAIL and exit nonzero on FAIL.
    Never echo the service_role key or any secret to stdout. Keep all values ASCII (a non-ASCII secret
    once caused a prod outage — the same discipline applies to anything this script writes).
  </action>
  <verify>
    <automated>bash -lc "for s in 'Stop-Process' 'sb:start' 'admin/users' 'db:migrate' 'docker exec -i' 'NOTIFY pgrst' 'has_table_privilege'; do grep -qi \"$s\" scripts/preflight-local.ps1 || { echo \"MISSING: $s\"; exit 1; }; done; echo OK"</automated>
  </verify>
  <done>scripts/preflight-local.ps1 exists and encodes, in order: zombie kill → sb:start (polytoken) → single-user seed → db:migrate → grant → NOTIFY pgrst → DB-based green assertion, with no secret echoed.</done>
  <acceptance_criteria>
    - The script seeds the auth user BEFORE calling db:migrate (grep line order: `admin/users` appears before `db:migrate`).
    - Grant SQL is piped via `docker exec -i` (grep matches `docker exec -i`), never plain `docker exec` for stdin.
    - Final step asserts `has_table_privilege('service_role',...)` and exits nonzero on failure.
    - `grep -Ei "GOCSPX-|service_role.*=.*ey" scripts/preflight-local.ps1` returns nothing (no hardcoded secret).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell env → local Supabase (GoTrue admin) | service_role key handled in preflight to seed the single user |
| Drizzle-owned tables → Supabase API roles | grants decide what anon/authenticated/service_role can read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01-01 | Information Disclosure | service_role key in preflight-local.ps1 | mitigate | key is read from `sb:status` at runtime, never hardcoded, never echoed to stdout; doc/script grep-gated to contain no `GOCSPX-`/`ey...` literals |
| T-49-01-02 | Elevation of Privilege | over-broad GRANT to anon on fresh DB | accept | grants mirror Supabase's own default role model on a LOCAL, disposable DB; RLS policies from 0034/0035 still gate row access; local-only, no hosted exposure |
| T-49-01-03 | Tampering | non-ASCII value written into an env/script | mitigate | script keeps all emitted values ASCII (prior non-ASCII-secret prod outage discipline) |
| T-49-01-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NO new packages (uses existing scripts); no legitimacy gate needed |
</threat_model>

<verification>
- `docs/RUN-LOCAL.md` and `scripts/preflight-local.ps1` both exist and pass their grep gates above.
- Neither file contains a literal secret (`grep -REi "GOCSPX-" docs/RUN-LOCAL.md scripts/preflight-local.ps1` is empty).
- The preflight ordering is correct: user-seed precedes migrate; grant+NOTIFY follow migrate.
</verification>

<success_criteria>
LIVE-01's "documented, reproducible start procedure — no zombie-process ambiguity" exists as
docs/RUN-LOCAL.md backed by scripts/preflight-local.ps1, and the local Supabase project-id rename
(LIVE-07) is captured in the procedure. Plan 49-03 can now bring the stack up and run the
DB-verified green path.
</success_criteria>

<output>
Create `.planning/phases/49-live-loop-gate-deploy-oauth-real-email/49-01-SUMMARY.md` when done,
noting the local project-id actualization outcome so plan 49-05 can record the decision in STATE.md.
</output>
