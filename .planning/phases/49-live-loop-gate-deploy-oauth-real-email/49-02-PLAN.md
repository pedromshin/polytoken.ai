---
phase: 49-live-loop-gate-deploy-oauth-real-email
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - infrastructure/aws/ses.tf
  - .planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/forwarding-catchall-tfplan.txt
autonomous: true
requirements: [LIVE-04]
must_haves:
  truths:
    - "A domain-level catch-all SES receipt rule exists in ses.tf, positioned AFTER the three exact-match rules so it never shadows agent-local/agent-staging/agent-prod"
    - "A read-only `terraform plan` proves exactly ONE new resource (aws_ses_receipt_rule.forwarding_catchall) is created, with zero unexpected diffs on the three existing rules"
    - "The saved plan output is ready for the user to review before they run `terraform apply` in the checkpoint (plan 49-06)"
  artifacts:
    - path: "infrastructure/aws/ses.tf"
      provides: "forwarding_catchall receipt rule (recipients = bare domain, routes to prod SNS/prefix)"
      contains: "forwarding_catchall"
    - path: ".planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/forwarding-catchall-tfplan.txt"
      provides: "Saved read-only terraform plan output for user review"
  key_links:
    - from: "infrastructure/aws/ses.tf"
      to: "aws_ses_receipt_rule.prod"
      via: "after = aws_ses_receipt_rule.prod.name (catch-all evaluated last)"
      pattern: "after\\s*=\\s*aws_ses_receipt_rule\\.prod\\.name"
    - from: "aws_ses_receipt_rule.forwarding_catchall"
      to: "aws_sns_topic.ses_inbound[\"prod\"]"
      via: "s3_action routes to prod pipeline (forwarding user lives in prod DB)"
      pattern: "ses_inbound\\[\"prod\"\\]"
---

<objective>
Write the domain-level SES catch-all receipt rule that routes any `u-{token}@magnitudetech.com.br`
forwarding address into the prod ingestion pipeline, and produce the read-only `terraform plan`
proof the user will review before running `apply` in the checkpoint. This is the autonomous half of
LIVE-04 — the terraform CHANGE and its read-only proof. The `terraform apply` itself is user-gated
(checkpoint plan 49-06), exactly as FORWARDING-RUNBOOK.md and EXTERNAL-RENAME-RUNBOOK.md prescribe.

Purpose: without the catch-all, SES only matches the three exact recipients; no `u-{token}@` address
routes anywhere, so the Gmail forwarding handshake can never complete.
Output: forwarding_catchall rule in ses.tf + saved plan artifact.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/49-live-loop-gate-deploy-oauth-real-email/49-CONTEXT.md
@.planning/milestones/v1.7-phases/45-email-threads-forwarding-seam/FORWARDING-RUNBOOK.md

<interfaces>
<!-- Exact existing ses.tf anchors the new rule must chain onto. -->

infrastructure/aws/ses.tf (existing, DO NOT reorder the three rules):
  aws_ses_receipt_rule_set.main        rule_set_name = "${var.project}-inbound"
  aws_ses_receipt_rule.local   "agent-local"    position 1
  aws_ses_receipt_rule.staging "agent-staging"  after = local.name
  aws_ses_receipt_rule.prod    "agent-prod"     after = staging.name   (recipients ["agent@magnitudetech.com.br"])
  aws_s3_bucket.ses_inbound            bucket = "${var.project}-ses-inbound-emails"
  aws_sns_topic.ses_inbound["prod"]    for_each over prod/staging/local
  aws_s3_bucket_policy.ses_inbound     (depends_on target)

Runbook draft (FORWARDING-RUNBOOK.md §1) — the shape to implement:
  resource "aws_ses_receipt_rule" "forwarding_catchall" {
    name = "forwarding-catchall"; rule_set_name = ...main.rule_set_name
    recipients = ["magnitudetech.com.br"]   # bare domain = catch-all
    enabled = true; scan_enabled = false; after = aws_ses_receipt_rule.prod.name
    s3_action { bucket_name = ...ses_inbound.bucket; object_key_prefix = "inbound/prod/";
                topic_arn = ...ses_inbound["prod"].arn; position = 1 }
    depends_on = [aws_s3_bucket_policy.ses_inbound]
  }

npm script: infra:tf = "terraform -chdir=infrastructure/aws"  → plan via `npm run infra:tf -- plan`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the forwarding_catchall receipt rule to ses.tf</name>
  <read_first>
    - infrastructure/aws/ses.tf (the full file — see the three existing rules and the SNS/S3 anchors)
    - .planning/milestones/v1.7-phases/45-email-threads-forwarding-seam/FORWARDING-RUNBOOK.md (§1 draft HCL + routing rationale)
  </read_first>
  <action>
    Append an `aws_ses_receipt_rule` resource named `forwarding_catchall` to infrastructure/aws/ses.tf,
    AFTER the existing `aws_ses_receipt_rule.prod` block. Set name = "forwarding-catchall";
    rule_set_name = aws_ses_receipt_rule_set.main.rule_set_name; recipients = ["magnitudetech.com.br"]
    (bare domain = catch-all — every `u-{token}@magnitudetech.com.br` matches); enabled = true;
    scan_enabled = false; after = aws_ses_receipt_rule.prod.name (so it is evaluated LAST and never
    shadows the three exact-match rules). Give it a single s3_action with bucket_name =
    aws_s3_bucket.ses_inbound.bucket, object_key_prefix = "inbound/prod/", topic_arn =
    aws_sns_topic.ses_inbound["prod"].arn, position = 1. Add depends_on = [aws_s3_bucket_policy.ses_inbound].
    Route it at the PROD pipeline per the CONTEXT decision (the forwarding user's account lives in the
    prod database). Add a comment above the block explaining the position/`after` chaining hazard: SES
    evaluates rules in order and stops at first match, so a bare-domain rule placed AFTER the exact
    matches cannot shadow them. Do NOT modify the three existing rules, their `after` chain, or their
    positions.
  </action>
  <verify>
    <automated>bash -lc "grep -q 'forwarding_catchall' infrastructure/aws/ses.tf && grep -q 'recipients *= *\\[\"magnitudetech.com.br\"\\]' infrastructure/aws/ses.tf && grep -q 'after *= *aws_ses_receipt_rule.prod.name' infrastructure/aws/ses.tf && grep -q 'inbound/prod/' infrastructure/aws/ses.tf && echo OK"</automated>
  </verify>
  <done>ses.tf contains the forwarding_catchall rule with recipients=["magnitudetech.com.br"], after=aws_ses_receipt_rule.prod.name, routing to the prod SNS topic and inbound/prod/ prefix; the three existing rules are byte-for-byte unchanged.</done>
  <acceptance_criteria>
    - `terraform -chdir=infrastructure/aws validate` (or `fmt -check`) parses ses.tf with no syntax error.
    - `git diff infrastructure/aws/ses.tf` shows additions ONLY (no lines removed from the three existing rules).
    - The new rule's s3_action topic_arn references `aws_sns_topic.ses_inbound["prod"].arn`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run read-only terraform plan and save the proof artifact</name>
  <read_first>
    - infrastructure/aws/main.tf (provider/version pin; confirms plan uses local tfstate — Hazard C)
    - infrastructure/aws/terraform.tfvars (var.project, alb_dns_name — confirm this is the authoritative-state machine)
    - .planning/milestones/v1.7-phases/42-atomic-rename-nauta-polytoken/EXTERNAL-RENAME-RUNBOOK.md (§2.4 Hazard C — local-only tfstate; run plan on the machine holding authoritative state)
  </read_first>
  <action>
    Run the read-only proof: `npm run infra:tf -- plan` and capture the full stdout+stderr to
    .planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/forwarding-catchall-tfplan.txt
    (create the artifacts/ directory if absent). This is READ-ONLY — never run `apply`. Then inspect
    the saved output and confirm, in the SUMMARY: (a) the plan reports "1 to add, 0 to change, 0 to
    destroy" (or explicitly shows exactly one resource `aws_ses_receipt_rule.forwarding_catchall` to
    be created), and (b) NONE of the three existing rules (agent-local/agent-staging/agent-prod) show
    a change or replacement — SES can silently reorder rules if the `after` chain is perturbed, so any
    diff on them is a stop-and-investigate. If `terraform plan` cannot authenticate to AWS or the local
    tfstate is stale/missing (Hazard C), do NOT force it: save the rendered HCL plus a clear note that
    the plan-proof must be run on the authoritative-state machine, and flag it for the checkpoint
    (49-06) so the user runs plan+apply together. This is a valid degraded outcome, not a failure to
    hide.
  </action>
  <verify>
    <automated>bash -lc "test -f .planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/forwarding-catchall-tfplan.txt && grep -Eiq 'forwarding_catchall|must be run on the authoritative-state machine' .planning/phases/49-live-loop-gate-deploy-oauth-real-email/artifacts/forwarding-catchall-tfplan.txt && echo OK"</automated>
  </verify>
  <done>The plan artifact exists and either shows exactly one new forwarding_catchall resource with no diffs on the three existing rules, OR records the degraded "run on authoritative-state machine" note for the checkpoint. No `terraform apply` was run.</done>
  <acceptance_criteria>
    - The artifact file exists at the stated path.
    - If the plan ran: it shows `forwarding_catchall` being created and the SUMMARY asserts 0 changes/0 destroys on the existing three rules.
    - The SUMMARY explicitly states apply was NOT run and is deferred to the user checkpoint (49-06).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → SES receipt (domain catch-all) | any sender to any `u-{token}@magnitudetech.com.br` now reaches the ingest pipeline |
| operator machine → live AWS (terraform) | plan is read-only; apply is user-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02-01 | Spoofing/Elevation | domain-level catch-all opens a broad ingest path | mitigate | ingestion still scopes mail per-token → user via the existing ForwardingAddressResolver (Plan 45-05); unknown/unrouted tokens do not cross into another user's data; `scan_enabled=false` and importer scoping are unchanged (not weakened) |
| T-49-02-02 | Tampering | perturbing the `after` chain reorders SES rules | mitigate | new rule chains `after = prod.name` only; plan-proof asserts zero diffs on the three existing rules before any apply |
| T-49-02-03 | Repudiation/Tampering | applying against stale local-only tfstate | mitigate | plan run on the authoritative-state machine (Hazard C); apply deferred to user with saved plan output to review |
| T-49-02-SC | Tampering | npm/pip/cargo installs | mitigate | no new packages installed (terraform + existing npm scripts only) |
</threat_model>

<verification>
- ses.tf parses and adds only the forwarding_catchall rule (git diff is additive).
- The saved plan artifact exists and either proves the one-resource-add or records the degraded checkpoint hand-off.
- No `terraform apply` was executed by this plan.
</verification>

<success_criteria>
The SES catch-all change is written and read-only-proven, ready for the user to review-and-apply in
the morning checkpoint. LIVE-04's terraform half is complete; the live round-trip verification is
gated behind the user's `apply` in plan 49-06.
</success_criteria>

<output>
Create `.planning/phases/49-live-loop-gate-deploy-oauth-real-email/49-02-SUMMARY.md` when done,
stating whether the plan-proof ran clean or was deferred to the checkpoint machine.
</output>
