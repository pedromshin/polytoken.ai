---
phase: 26-zero-dependency-contract-fixes-backlog-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/globals.css
  - apps/web/src/app/chat/_canvas/chat-canvas.tsx
  - apps/web/src/app/chat/_components/markdown-renderer.tsx
  - apps/web/src/app/chat/_components/composer.tsx
autonomous: true
requirements: [FIX-01, FIX-09, FIX-10]
must_haves:
  truths:
    - "React Flow's Controls/MiniMap/Background/Attribution render with app token vars in both light and dark mode instead of stock light-gray boxes"
    - "The composer reads as a dock: a full-width band with border-t and a subtle shadow-sm above the message list"
    - "Every native-scroll spot on /chat matches the ScrollArea thumb aesthetic (one scrollbar aesthetic per page)"
    - "No token VALUE in globals.css :root/.dark is added, changed, or removed — only new @layer selectors/utilities consuming existing tokens"
  artifacts:
    - path: "apps/web/src/app/globals.css"
      provides: "@layer components react-flow chrome + @layer utilities .scrollbar-token"
      contains: ".react-flow__controls"
    - path: "apps/web/src/app/chat/_canvas/chat-canvas.tsx"
      provides: "Background/MiniMap token SVG-fill props"
    - path: "apps/web/src/app/chat/_components/composer.tsx"
      provides: "Dock outer band + scrollbar-token textarea"
      contains: "border-t border-border/60"
  key_links:
    - from: "apps/web/src/app/chat/_canvas/chat-canvas.tsx"
      to: "apps/web/src/app/globals.css .react-flow__* rules"
      via: "React Flow class names + inline color props"
      pattern: "MiniMap|Background"
    - from: "apps/web/src/app/chat/_components/composer.tsx"
      to: "globals.css .scrollbar-token"
      via: "className scrollbar-token on textarea"
      pattern: "scrollbar-token"
---

<objective>
Style React Flow's stock chrome with the app's existing token vars (FIX-01), make the composer read
as a full-width "dock" (FIX-09), and unify scrollbar treatment so every native-scroll spot on `/chat`
matches the Radix `ScrollArea` thumb aesthetic (FIX-10) — all from existing tokens and Tailwind's
stock utility scale, with NO change to any token value in `globals.css`.

Purpose: Satisfies the React-Flow-chrome half of ROADMAP criterion 1 and the composer-dock + scrollbar
half of criterion 4.
Output: A token-correct canvas chrome (light + dark), a docked composer, and one scrollbar aesthetic
per page. This is the only plan that edits `globals.css`, `chat-canvas.tsx`, `markdown-renderer.tsx`,
and `composer.tsx`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md

<interfaces>
<!-- Tailwind v3.4.4 (NOT v4) — CSS additions must use v3 @layer/@apply, never v4 CSS-first syntax -->
<!-- globals.css FIX-01: new @layer components block; FIX-10: new @layer utilities block.
     Neither touches the existing :root / .dark custom-property declarations (no token VALUE change). -->
<!-- ScrollArea thumb reference (packages/ui/src/scroll-area.tsx): 10px (w-2.5) thumb, bg-border,
     rounded-full, transition-colors. .scrollbar-token replicates this for native elements. -->
<!-- chat-canvas.tsx ~642-644 mounts <Background>, <Controls showZoom showFitView showInteractive>,
     and (conditionally) <MiniMap>. Controls props unchanged — only its CSS changes. -->
<!-- markdown-renderer.tsx: Pre (~82-94) "my-2 overflow-x-auto rounded-lg bg-muted p-3 text-xs";
     Table wrapper (~120-131) "my-2 overflow-x-auto". Import ScrollArea + ScrollBar from @nauta/ui. -->
<!-- composer.tsx:76 single centered row; :85 Textarea "max-h-52 min-h-[44px] resize-none overflow-y-auto" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add React Flow chrome + scrollbar-token CSS to globals.css (FIX-01 + FIX-10 CSS)</name>
  <files>apps/web/src/app/globals.css</files>
  <read_first>
    - apps/web/src/app/globals.css (confirm existing @layer usage + the :root/.dark token declarations that must NOT change)
    - packages/ui/src/scroll-area.tsx (the thumb aesthetic to replicate: w-2.5/10px, bg-border, rounded-full)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-01" (literal @layer components block) and § "FIX-10" Target (1) (literal .scrollbar-token utility)
  </read_first>
  <action>
    Add a NEW `@layer components` block styling the React Flow classes from existing tokens (v3 @apply):
    `.react-flow__controls` -> "rounded-md border border-border bg-card shadow-sm";
    `.react-flow__controls-button` -> "border-b border-border/60 bg-transparent text-foreground last:border-b-0";
    `.react-flow__controls-button:hover` -> "bg-muted"; `.react-flow__controls-button svg` -> `fill: hsl(var(--foreground));`
    `.react-flow__minimap` -> "overflow-hidden rounded-md border border-border bg-card";
    `.react-flow__attribution` -> "bg-background/70 text-[10px] text-muted-foreground".
    Add a NEW `@layer utilities` block with `.scrollbar-token`: `scrollbar-width: thin;`
    `scrollbar-color: hsl(var(--border)) transparent;` plus `::-webkit-scrollbar { width: 10px; height: 10px; }`,
    `::-webkit-scrollbar-track { background: transparent; }`,
    `::-webkit-scrollbar-thumb { background-color: hsl(var(--border)); border-radius: 9999px; background-clip: content-box; border: 1px solid transparent; }`.
    DO NOT add, change, or remove any custom property in `:root`/`.dark` (Phase 28 owns token values).
  </action>
  <verify>
    <automated>npm run build -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ".react-flow__controls" apps/web/src/app/globals.css` returns >= 1
    - `grep -c ".react-flow__minimap" apps/web/src/app/globals.css` returns >= 1
    - `grep -c ".scrollbar-token" apps/web/src/app/globals.css` returns >= 1
    - `grep -Ec "hsl\(var\(--(foreground|border|background)\)" apps/web/src/app/globals.css` returns >= 1
    - No new token variable: `grep -Ec "^\s*--[a-z-]+:" apps/web/src/app/globals.css` is unchanged from HEAD (compare `git show HEAD:apps/web/src/app/globals.css | grep -Ec "^\s*--[a-z-]+:"`)
    - `npm run build -w @nauta/web` exits 0 (Tailwind compiles the new @layer blocks)
  </acceptance_criteria>
  <done>globals.css has the react-flow @layer components block + .scrollbar-token @layer utility, all consuming existing tokens; no :root/.dark token value changed; the web build compiles.</done>
</task>

<task type="auto">
  <name>Task 2: chat-canvas Background/MiniMap props + markdown ScrollArea wrappers (FIX-01 + FIX-10)</name>
  <files>apps/web/src/app/chat/_canvas/chat-canvas.tsx, apps/web/src/app/chat/_components/markdown-renderer.tsx</files>
  <read_first>
    - apps/web/src/app/chat/_canvas/chat-canvas.tsx (lines ~642-644 — Background/Controls/MiniMap mount)
    - apps/web/src/app/chat/_components/markdown-renderer.tsx (lines ~82-94 Pre, ~120-131 Table wrapper)
    - packages/ui/src/scroll-area.tsx (ScrollArea + ScrollBar exports, orientation prop)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-01" (Background/MiniMap props) and § "FIX-10" Target (2)/(3) + Fallback
  </read_first>
  <action>
    chat-canvas.tsx: set token SVG-fill props inline — `<Background gap={16} size={1} color="hsl(var(--border))" />`;
    and on the conditional `<MiniMap>` add `maskColor="hsl(var(--background) / 0.6)"`
    `nodeColor="hsl(var(--muted-foreground) / 0.35)"` `nodeStrokeColor="hsl(var(--border))"` (keep `pannable zoomable`).
    Leave `<Controls showZoom showFitView showInteractive />` props untouched (its look comes from the CSS in Task 1).
    markdown-renderer.tsx: wrap the fenced-code `Pre` and the `Table` in a Radix `ScrollArea`
    (`orientation="horizontal"` ScrollBar) — Pre: `<ScrollArea className="my-2 rounded-lg bg-muted"><pre className="p-3 text-xs" {...props}>{children}</pre><ScrollBar orientation="horizontal" /></ScrollArea>`;
    Table: `<ScrollArea className="my-2"><table className="w-full border-collapse text-sm" {...props}>{children}</table><ScrollBar orientation="horizontal" /></ScrollArea>`.
    FALLBACK (verify at implementation): if ScrollArea clips wide-table row height or misbehaves in the
    variable-height markdown flow, instead apply the `.scrollbar-token` utility to these two `overflow-x-auto`
    wrappers (CONTEXT.md permits this). Either way exactly ONE scrollbar aesthetic exists per page.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hsl(var(--border))" apps/web/src/app/chat/_canvas/chat-canvas.tsx` returns >= 1
    - `grep -Ec "maskColor|nodeColor|nodeStrokeColor" apps/web/src/app/chat/_canvas/chat-canvas.tsx` returns >= 1
    - `grep -Ec "ScrollArea|scrollbar-token" apps/web/src/app/chat/_components/markdown-renderer.tsx` returns >= 1
    - `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>Background/MiniMap use token colors; markdown code + table wrappers use the ScrollArea aesthetic (or scrollbar-token fallback); typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Composer dock band + scrollbar-token textarea (FIX-09 + FIX-10)</name>
  <files>apps/web/src/app/chat/_components/composer.tsx</files>
  <read_first>
    - apps/web/src/app/chat/_components/composer.tsx (line ~76 centered row, ~85 Textarea)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-09" (literal dock wrapper) and § "FIX-10" Target (1) (textarea)
  </read_first>
  <action>
    FIX-09: wrap the existing centered content row in a NEW full-width outer band so the border/shadow
    span the composer's full width — outer div `className="w-full shrink-0 border-t border-border/60 bg-background shadow-sm"`,
    inner div keeps `className="mx-auto flex w-full max-w-3xl items-end gap-2 px-4 py-4"` with the existing
    Textarea + Button unchanged. `shadow-sm` is the same stock utility ChatNode already uses (no new shadow value).
    FIX-10: append `scrollbar-token` to the Textarea class -> `"max-h-52 min-h-[44px] resize-none overflow-y-auto scrollbar-token"`.
    No change to focus order or Textarea/Button behavior.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "w-full shrink-0 border-t border-border/60 bg-background shadow-sm" apps/web/src/app/chat/_components/composer.tsx` returns >= 1
    - `grep -c "scrollbar-token" apps/web/src/app/chat/_components/composer.tsx` returns >= 1
    - `grep -c "max-w-3xl" apps/web/src/app/chat/_components/composer.tsx` returns >= 1 (centered inner row preserved)
    - `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>Composer renders as a full-width dock band (border-t + shadow-sm) with the centered content row intact; the textarea uses scrollbar-token; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | CSS/class + presentational SVG-fill props; no input, data, or auth flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-08 | Tampering | globals.css @layer additions | accept | New selectors/utilities only; existing :root/.dark token values untouched (grep-gated); Tailwind v3 @layer/@apply syntax verified by the web build |
| T-26-09 | Information Disclosure | React Flow attribution restyle | accept | Only color/background of the library's own attribution changes; its accessible labels/markup are preserved |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (zero new deps) |
</threat_model>

<verification>
- `npm run build -w @nauta/web` exits 0
- `git show HEAD:apps/web/src/app/globals.css | grep -Ec "^\s*--[a-z-]+:"` equals `grep -Ec "^\s*--[a-z-]+:" apps/web/src/app/globals.css` (no token value added/removed)
- `grep -c ".react-flow__controls" apps/web/src/app/globals.css` returns >= 1
- (visual, at UI review) canvas Controls/MiniMap/Background render token-correct in both light and dark; composer reads as a dock; one scrollbar aesthetic per page
</verification>

<success_criteria>
React Flow chrome renders from app tokens in light + dark mode; the composer reads as a full-width
dock; every native-scroll spot on `/chat` matches the ScrollArea thumb aesthetic; no token value in
`globals.css` is added, changed, or removed; the web build compiles and typecheck passes.
</success_criteria>

<output>
Create `.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-05-SUMMARY.md` when done
</output>
