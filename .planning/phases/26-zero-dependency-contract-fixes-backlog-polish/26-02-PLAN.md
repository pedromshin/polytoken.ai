---
phase: 26-zero-dependency-contract-fixes-backlog-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/studio/_components/code-island-frame.tsx
  - apps/web/src/app/studio/_components/catalog-browser-island.tsx
  - apps/web/src/app/studio/_components/generation-state-chrome.tsx
  - apps/web/src/app/studio/_components/code-sandbox-island.tsx
  - apps/web/src/app/studio/_components/page-ideas-island.tsx
autonomous: true
requirements: [FIX-02, FIX-03, FIX-06]
must_haves:
  truths:
    - "code-island-frame's PHASE_TONE map and ViolationList tones render from semantic tokens (destructive/primary/muted), correct in dark mode with no dark: raw-palette override"
    - "the code-island iframe wrapper uses bg-background instead of bg-white"
    - "the catalog prop table has a muted header band and zebra rows matching its surrounding card chrome"
    - "the curveball badge and code-sandbox error text use token treatments, not amber/red palette classes"
    - "these five studio files contain zero font-medium"
  artifacts:
    - path: "apps/web/src/app/studio/_components/code-island-frame.tsx"
      provides: "PHASE_TONE + ViolationList + iframe wrapper on tokens; PHASE_LABEL font-semibold"
    - path: "apps/web/src/app/studio/_components/catalog-browser-island.tsx"
      provides: "Zebra prop table with muted header band; th font-semibold"
    - path: "apps/web/src/app/studio/_components/page-ideas-island.tsx"
      provides: "curveball Badge variant=outline; Page Ideas heading font-semibold"
  key_links:
    - from: "apps/web/src/app/studio/_components/code-island-frame.tsx"
      to: "semantic tokens"
      via: "destructive/primary/muted recipe classes"
      pattern: "border-(destructive|primary)/30"
    - from: "apps/web/src/app/studio/_components/catalog-browser-island.tsx"
      to: "card chrome"
      via: "odd:bg-muted zebra + bg-muted header band"
      pattern: "odd:bg-muted"
---

<objective>
Replace the hardcoded amber/red/white color systems and the remaining `font-medium` sites across
five `/studio` files with semantic tokens, and style the catalog prop table (zebra rows + muted
header band) to match its surrounding `Card`. Every change reuses recipes already established in
this codebase (`generation-state-chrome`'s cache-hit/in-progress/fallback treatments,
`page-ideas-island`'s own `Badge variant="outline"`).

Purpose: Satisfies ROADMAP success criterion 3 (studio hardcoded color systems -> tokens; catalog
prop table zebra + muted header) and clears 8 of the 11 studio `font-medium` sites.
Output: Five studio files rendering only from `destructive`/`primary`/`muted`/`border` tokens with
`font-normal`/`font-semibold` only.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md

<interfaces>
<!-- Semantic-token recipes already established in generation-state-chrome.tsx (reuse verbatim) -->
- success / "good outcome":  border-primary/30 bg-primary/10 text-primary   (its cache_hit state)
- in-progress / neutral:     border-border bg-muted/40 text-foreground       (its in_progress state)
- failure / destructive:     border-destructive/30 bg-destructive/10 text-destructive
- Badge base already declares font-semibold (packages/ui/src/badge.tsx) — deleting a local
  font-medium override is sufficient; variant="outline" is the neutral chip already used in page-ideas
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: code-island-frame — PHASE_TONE + ViolationList + iframe wrapper + PHASE_LABEL weight</name>
  <files>apps/web/src/app/studio/_components/code-island-frame.tsx</files>
  <read_first>
    - apps/web/src/app/studio/_components/code-island-frame.tsx (lines ~62-69 PHASE_TONE map, ~160 iframe wrapper, ~200 PHASE_LABEL span, ~216-219 ViolationList tone recipes)
    - apps/web/src/app/studio/_components/generation-state-chrome.tsx (the cache_hit / in_progress recipes being reused)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-03" (a),(b),(c) and § "FIX-02" (#8)
  </read_first>
  <action>
    (a) PHASE_TONE map (~62-69): collapse the 6 raw-palette entries to 3 token buckets — In progress
        (`running`,`healing`) -> "border-border bg-muted/40 text-foreground"; Success (`rendered`,`healed`)
        -> "border-primary/30 bg-primary/10 text-primary"; Failure (`rejected`,`fallback`) ->
        "border-destructive/30 bg-destructive/10 text-destructive".
    (b) ViolationList `tone` prop (~216-219): rename accepted values to the token vocabulary and collapse
        to 2 recipes — blocking allowlist violations -> destructive
        ("border-destructive/30 bg-destructive/10 text-destructive"); informational/accessibility findings
        -> neutral muted ("border-border bg-muted text-foreground"). No amber token is invented.
    (c) iframe wrapper (~160): `bg-white` -> `bg-background`.
    (#8) PHASE_LABEL span (~200): `font-medium` -> `font-semibold`.
    Remove every `amber-*`/`red-*`/`emerald-*`/`bg-white` and any `dark:` raw-palette override from this file.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "amber-|red-[0-9]|emerald-|bg-white" apps/web/src/app/studio/_components/code-island-frame.tsx` returns 0
    - `grep -c "font-medium" apps/web/src/app/studio/_components/code-island-frame.tsx` returns 0
    - `grep -Ec "dark:(bg|text|border)-(amber|red|emerald)" apps/web/src/app/studio/_components/code-island-frame.tsx` returns 0
    - `grep -Ec "border-(primary|destructive)/30|bg-muted/40" apps/web/src/app/studio/_components/code-island-frame.tsx` returns >= 1
    - `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>code-island-frame renders PHASE_TONE, ViolationList, iframe wrapper, and PHASE_LABEL from tokens only; no amber/red/white/dark-palette classes remain; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: catalog-browser prop table — zebra + muted header (FIX-06) + th font-semibold (FIX-02 #1-4)</name>
  <files>apps/web/src/app/studio/_components/catalog-browser-island.tsx</files>
  <read_first>
    - apps/web/src/app/studio/_components/catalog-browser-island.tsx (lines ~109-143 — the bare table)
    - apps/web/src/app/chat/_components/markdown-renderer.tsx (TableHeadCell font-semibold convention referenced by FIX-06)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-06" (literal thead/tbody target) and § "FIX-02" (#1-4)
  </read_first>
  <action>
    Style the prop table in place (plain markup, no table library). thead row:
    `<tr className="border-b border-border/50 bg-muted/40 text-muted-foreground">` with each `<th>`
    `className="px-2 py-2 text-left font-semibold">` (this is the FIX-02 #1-4 `font-medium`->`font-semibold`
    change folded into the header restyle). tbody rows:
    `<tr className="border-b border-border/30 odd:bg-muted/20 last:border-0">` with each `<td>`
    `className="px-2 py-1">` (first cell keeps `font-mono`). Zebra `odd:bg-muted/20` stays subtly under
    the header `bg-muted/40` band. No hex, no font-medium.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "font-medium" apps/web/src/app/studio/_components/catalog-browser-island.tsx` returns 0
    - `grep -c "odd:bg-muted" apps/web/src/app/studio/_components/catalog-browser-island.tsx` returns >= 1
    - `grep -c "bg-muted/40" apps/web/src/app/studio/_components/catalog-browser-island.tsx` returns >= 1
    - `grep -c "font-semibold" apps/web/src/app/studio/_components/catalog-browser-island.tsx` returns >= 4
    - `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>Prop table has a muted header band + zebra rows; all four th use font-semibold; zero font-medium; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: generation-state-chrome + code-sandbox + page-ideas — remaining token/weight fixes</name>
  <files>apps/web/src/app/studio/_components/generation-state-chrome.tsx, apps/web/src/app/studio/_components/code-sandbox-island.tsx, apps/web/src/app/studio/_components/page-ideas-island.tsx</files>
  <read_first>
    - apps/web/src/app/studio/_components/generation-state-chrome.tsx (lines ~75, ~108)
    - apps/web/src/app/studio/_components/code-sandbox-island.tsx (lines ~152, ~165)
    - apps/web/src/app/studio/_components/page-ideas-island.tsx (lines ~106 curveball badge, ~318 heading; note IdeaChips one line above uses Badge variant="outline")
    - packages/ui/src/badge.tsx (base already declares font-semibold)
    - .planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-UI-SPEC.md § "FIX-02" (#5,#6,#7,#11), § "FIX-03" (d),(f)
  </read_first>
  <action>
    generation-state-chrome.tsx: (#5, ~75) fallback-banner heading `font-medium` -> `font-semibold`;
      (#6, ~108) DELETE `font-medium` from the `<Badge className="border border-primary/30 bg-primary/10
      text-xs font-medium text-primary">` (Badge's own base font-semibold takes over).
    code-sandbox-island.tsx: (#7, ~152) "Generate from intent" span `font-medium` -> `font-semibold`;
      (FIX-03f, ~165) error text `text-red-600` -> `text-destructive`.
    page-ideas-island.tsx: (#11, ~318) "Page Ideas" span `font-medium` -> `font-semibold`;
      (FIX-03d, ~106) replace the curveball badge's hand-rolled `text-xs bg-amber-500/15 text-amber-700
      border border-amber-400/40 hover:bg-amber-500/20` with `<Badge variant="outline" className="text-xs">curveball</Badge>`
      (same component + variant already used one line above). No new hue.
  </action>
  <verify>
    <automated>npm run typecheck -w @nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "font-medium" apps/web/src/app/studio/_components/generation-state-chrome.tsx` returns 0
    - `grep -c "font-medium" apps/web/src/app/studio/_components/code-sandbox-island.tsx` returns 0
    - `grep -c "font-medium" apps/web/src/app/studio/_components/page-ideas-island.tsx` returns 0
    - `grep -Ec "text-red-|amber-" apps/web/src/app/studio/_components/code-sandbox-island.tsx apps/web/src/app/studio/_components/page-ideas-island.tsx` returns 0
    - `grep -c "text-destructive" apps/web/src/app/studio/_components/code-sandbox-island.tsx` returns >= 1
    - `npm run typecheck -w @nauta/web` exits 0
  </acceptance_criteria>
  <done>All three files have zero font-medium and zero amber/red palette classes; curveball uses Badge variant=outline; error text uses text-destructive; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | Presentational class/token swaps only; no input handling, no data flow, no new surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Tampering | Studio class/token edits | accept | Pure Tailwind class substitution; no logic, input, or trust boundary changes; iframe sandbox attributes on code-island-frame are untouched |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (zero new deps) |
</threat_model>

<verification>
- `grep -rn "font-medium" apps/web/src/app/studio/_components/code-island-frame.tsx apps/web/src/app/studio/_components/catalog-browser-island.tsx apps/web/src/app/studio/_components/generation-state-chrome.tsx apps/web/src/app/studio/_components/code-sandbox-island.tsx apps/web/src/app/studio/_components/page-ideas-island.tsx` returns 0
- `grep -rEn "amber-|red-[0-9]|emerald-|bg-white" apps/web/src/app/studio/_components/code-island-frame.tsx apps/web/src/app/studio/_components/code-sandbox-island.tsx apps/web/src/app/studio/_components/page-ideas-island.tsx` returns 0
- `npm run typecheck -w @nauta/web` exits 0
</verification>

<success_criteria>
Five studio files render only from semantic tokens (`destructive`/`primary`/`muted`/`border`/
`background`) with `font-normal`/`font-semibold` only; the catalog prop table has a muted header band
and zebra rows; dark mode needs zero `dark:` raw-palette overrides; web typecheck passes.
</success_criteria>

<output>
Create `.planning/phases/26-zero-dependency-contract-fixes-backlog-polish/26-02-SUMMARY.md` when done
</output>
