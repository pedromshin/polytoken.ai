---
phase: 44-tenancy-user-id-scoping-enforced-isolation
plan: 04
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - packages/db/migrations/0034_rls_user_scoping.sql
  - packages/db/migrations/meta/_journal.json
autonomous: true
requirements: [TENA-04]

must_haves:
  truths:
    - "RLS policies keyed on auth.uid() are active on the user-owned tables (importers anchor + its hard-FK descendants + chat_conversations + chat_cost_ledger)"
    - "The authenticated role can only see rows it owns; the anon role stays fully denied"
    - "System-default rows (importer_id IS NULL, e.g. seeded entity_types) remain readable to authenticated users"
    - "Applying the RLS migration locally causes ZERO app regression (Drizzle superuser + FastAPI service_role both bypass RLS by design)"
  artifacts:
    - path: "packages/db/migrations/0034_rls_user_scoping.sql"
      provides: "auth.uid()-based ownership policies replacing the authenticated deny-all on user-owned tables"
      contains: "auth.uid()"
  key_links:
    - from: "packages/db/migrations/0034_rls_user_scoping.sql"
      to: "importers.user_id"
      via: "USING (user_id = auth.uid())"
      pattern: "auth.uid\\(\\)"
---

<objective>
Add the defense-in-depth layer: RLS policies keyed on `auth.uid()` on the user-owned tables, so a future non-superuser / PostgREST-direct path cannot read across tenants even though the current app paths (Drizzle superuser, FastAPI service_role) bypass RLS entirely. Depends on Plan 01's decision record (the TENA-04 ordering gate) and its user_id columns.

Purpose: RLS is the second wall, not the primary — the primary is the app boundary (Plans 02/03/05/06/07). This plan makes the two-wall model real.
Output: migration 0034 replacing the authenticated deny-all with ownership policies, applied and proven non-regressing locally.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-CONTEXT.md
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-01-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase — no exploration needed. -->

packages/db/migrations/0001_rls_deny_all.sql — the existing baseline: every Phase-4 table has ENABLE ROW LEVEL SECURITY + two RESTRICTIVE deny-all policies (anon and authenticated, USING(false) WITH CHECK(false)). A RESTRICTIVE authenticated deny-all blocks ALL rows regardless of any permissive policy — so to grant auth.uid() access you must DROP the authenticated RESTRICTIVE deny-all on the user-owned tables and replace it with a PERMISSIVE ownership policy. Keep the anon deny-all.

packages/db/migrations/0020_knowledge_node_edges_rls.sql — precedent for later RLS migrations on knowledge tables (read it for the established policy-naming style).

User-owned table set (from Plan 01 + the importers subtree):
- Direct user_id: importers, chat_conversations, chat_cost_ledger → USING (user_id = auth.uid()).
- Hard-FK importer descendants (importer_id NOT NULL): emails, email_attachments, email_components, extraction_records, entity_instances, sender_profiles → USING (importer_id IN (SELECT id FROM importers WHERE user_id = auth.uid())).
- Nullable importer_id (system defaults + overrides): entity_types, entity_type_fields → USING (importer_id IS NULL OR importer_id IN (SELECT id FROM importers WHERE user_id = auth.uid())) so seeded system defaults stay readable.
- knowledge_nodes / knowledge_node_edges: scope by their importer_id the same way (confirm the column exists in the schema before writing the policy).

Both app paths BYPASS RLS: Drizzle connects as Postgres superuser (client.ts:28-36) and FastAPI uses SUPABASE_SECRET_KEY / service_role (infrastructure/supabase/client.py). Therefore applying RLS must NOT regress any app test. Migration script: `npm run migration:generate:custom` then `npm run migrate:local` from packages/db. Next migration number is 0034.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the auth.uid() RLS ownership policies migration</name>
  <files>packages/db/migrations/0034_rls_user_scoping.sql, packages/db/migrations/meta/_journal.json</files>
  <read_first>
    - packages/db/migrations/0001_rls_deny_all.sql (the deny-all policies to supersede on user-owned tables; keep anon deny)
    - packages/db/migrations/0020_knowledge_node_edges_rls.sql (RLS policy-naming precedent)
    - packages/db/src/schema/index.ts (the authoritative table list to cover)
    - packages/db/src/schema/entity-types.ts (nullable importer_id — system defaults must stay readable)
    - packages/db/src/schema/knowledge-nodes.ts + knowledge-node-edges.ts (confirm importer_id columns for their policies)
  </read_first>
  <action>
    Create a custom migration 0034_rls_user_scoping.sql. For each user-owned table: DROP the RESTRICTIVE authenticated deny-all policy from 0001 (`DROP POLICY IF EXISTS "deny_all_<table>_authenticated" ON "<table>";`), keep the anon deny-all, and CREATE a PERMISSIVE policy FOR ALL TO authenticated with the USING/WITH CHECK expression from the interfaces table set:
    - Direct-user_id tables (importers, chat_conversations, chat_cost_ledger): `USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid())`.
    - Hard-FK descendants: `USING (importer_id IN (SELECT id FROM importers WHERE user_id = auth.uid()))` (and matching WITH CHECK).
    - Nullable importer_id tables (entity_types, entity_type_fields): add `importer_id IS NULL OR` to the USING clause so seeded system defaults remain readable; WITH CHECK should NOT allow writing importer_id IS NULL rows from an authenticated session (system defaults are seeded by migration only).
    - knowledge_nodes / knowledge_node_edges: importer_id-scoped, same pattern.
    Use `--> statement-breakpoint` between statements (match the 0001 file's convention). Register the migration tag in meta/_journal.json. Add a top-of-file comment stating explicitly that Drizzle (superuser) and FastAPI (service_role) BYPASS these policies — they defend PostgREST/future non-superuser paths only (echoing the PROJECT.md decision from Plan 01).
  </action>
  <verify>
    <automated>grep -c "auth.uid()" packages/db/migrations/0034_rls_user_scoping.sql</automated>
    <automated>grep -n "importers\|chat_conversations\|chat_cost_ledger\|emails\|entity_types" packages/db/migrations/0034_rls_user_scoping.sql</automated>
  </verify>
  <acceptance_criteria>
    - 0034_rls_user_scoping.sql references auth.uid() for every user-owned table and drops the prior authenticated deny-all on those tables.
    - entity_types/entity_type_fields policies include `importer_id IS NULL OR` in the USING clause.
    - The anon deny-all policies are NOT dropped (grep the file — no `deny_all_*_anon` DROP).
    - The migration tag is present in meta/_journal.json.
    - A top-of-file comment records the Drizzle-superuser / FastAPI-service_role bypass.
  </acceptance_criteria>
  <done>The RLS ownership policy migration is authored covering every user-owned table, preserving system-default readability and anon denial.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply RLS locally + prove zero app regression</name>
  <files>packages/db/migrations/0034_rls_user_scoping.sql</files>
  <read_first>
    - packages/db/src/migrate.ts (the runner)
    - packages/db/migrations/0034_rls_user_scoping.sql (the migration authored in Task 1)
  </read_first>
  <action>
    Apply the migration locally: `npm run migrate:local` from packages/db ([BLOCKING] — RLS DDL only lands in the live DB via apply). Then prove non-regression: because both app paths bypass RLS, existing suites must stay green. Run the packages/api-client vitest suite and the apps/email-listener pytest suite (targeted to the tenancy-relevant tests is sufficient for this plan's proof; the full cross-cutting proof is Plan 08). Query the local catalog to confirm the policies exist with the auth.uid() predicate.
  </action>
  <verify>
    <automated>cd packages/db && npm run migrate:local 2>&1 | grep -E "Migrations completed"</automated>
    <automated>cd packages/db && node -e "const p=require('postgres');const s=p(process.env.POSTGRES_URL_NON_POOLING,{prepare:false});s\`SELECT tablename, count(*) c FROM pg_policies WHERE schemaname='public' AND qual ILIKE '%auth.uid()%' GROUP BY tablename ORDER BY tablename\`.then(r=>{console.log(r);process.exit(r.length>=8?0:1)})"</automated>
    <automated>cd apps/email-listener && uv run pytest tests/presentation/api/v1 --no-cov 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - migrate:local completes without error.
    - pg_policies reports auth.uid()-qualified policies on at least 8 user-owned tables (query returns exit 0).
    - The email-listener api/v1 pytest tests remain green after RLS is applied (no regression, confirming service_role bypass).
    - The SUMMARY records the pg_policies table list observed.
  </acceptance_criteria>
  <done>RLS ownership policies are live on the local DB and no app path regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgREST / future non-superuser role → Postgres | RLS is the wall for any path that does NOT connect as superuser/service_role |
| authenticated JWT → row visibility | auth.uid() must map a session to exactly its owned rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04-01 | Information Disclosure | authenticated deny-all vs ownership | mitigate | Replace RESTRICTIVE authenticated deny-all with PERMISSIVE `user_id/importer_id = auth.uid()` ownership policies; anon stays denied |
| T-44-04-02 | Elevation of Privilege | system-default rows | mitigate | entity_types/entity_type_fields WITH CHECK forbids authenticated inserts of importer_id-NULL rows; USING allows read-only visibility |
| T-44-04-03 | Tampering | app-path regression | accept | Drizzle superuser + FastAPI service_role bypass RLS by design (documented); non-regression proven by re-running suites post-apply |
| T-44-04-SC | Tampering | no package installs | accept | No new packages added |
</threat_model>

<verification>
- `npm run migrate:local` applies 0034 cleanly.
- pg_policies shows auth.uid() policies on the user-owned tables; anon deny-all intact.
- api-client vitest + email-listener api/v1 pytest remain green post-apply.
</verification>

<success_criteria>
- RLS policies active on user-owned tables as defense-in-depth, with the enforcement decision already recorded in PROJECT.md by Plan 01 (ROADMAP SC4, TENA-04).
</success_criteria>

<output>
Create `.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-04-SUMMARY.md` when done.
</output>
