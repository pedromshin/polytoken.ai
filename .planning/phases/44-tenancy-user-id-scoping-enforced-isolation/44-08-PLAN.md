---
phase: 44-tenancy-user-id-scoping-enforced-isolation
plan: 08
type: execute
wave: 4
depends_on: ["44-03", "44-04", "44-05", "44-06", "44-07"]
files_modified:
  - packages/api-client/src/router/__tests__/cross-tenant-adversarial.test.ts
  - apps/email-listener/tests/adversarial/test_cross_tenant.py
  - apps/web/src/app/api/attachments/__tests__/cross-tenant.test.ts
  - .planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-SWEEP-INVENTORY.md
autonomous: true
requirements: [TENA-03]

must_haves:
  truths:
    - "A second user cannot READ the first user's data via ANY tRPC procedure, web route, or FastAPI endpoint"
    - "A second user cannot WRITE/mutate the first user's data via ANY procedure, route, or endpoint"
    - "The attachments download route and the knowledge-promote proxy are explicitly covered by the adversarial suite"
    - "A committed sweep inventory enumerates every route/procedure with its scoping status, and every user-owned one is marked enforced + locked by a test"
  artifacts:
    - path: "packages/api-client/src/router/__tests__/cross-tenant-adversarial.test.ts"
      provides: "the tRPC/web acceptance-gate suite (second user, reads + writes, every router)"
      min_lines: 80
    - path: "apps/email-listener/tests/adversarial/test_cross_tenant.py"
      provides: "the FastAPI acceptance-gate suite (second user, emails + promote)"
      min_lines: 40
    - path: ".planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-SWEEP-INVENTORY.md"
      provides: "enumerated route/procedure inventory with per-item scoping status + locking test"
      min_lines: 40
  key_links:
    - from: "packages/api-client/src/router/__tests__/cross-tenant-adversarial.test.ts"
      to: "every router"
      via: "second-user caller attempts"
      pattern: "UNAUTHORIZED|NOT_FOUND|FORBIDDEN"
---

<objective>
Ship the phase ACCEPTANCE GATE: an adversarial cross-tenant test suite that creates a SECOND user and attempts cross-tenant reads AND writes through EVERY route/procedure — explicitly including the attachments download route and the knowledge-promote proxy — and proves each is denied. vitest covers the web/tRPC surfaces; pytest covers the FastAPI surface. Plus the committed sweep inventory enumerating every route/procedure with its scoping status and the test that locks it.

Purpose: This is the gate the whole phase is measured by (ROADMAP SC2). It also proves the sweep (SC3) is complete, not spot-checked.
Output: two adversarial suites + the sweep inventory doc, all green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-CONTEXT.md
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-02-SUMMARY.md
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-03-SUMMARY.md
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-05-SUMMARY.md
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-06-SUMMARY.md
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-07-SUMMARY.md

<interfaces>
<!-- The surfaces the gate must cover. Extracted — no exploration. -->

tRPC routers (packages/api-client/src/root.ts): emails, entityTypes, entities, knowledge, genui, chat. protectedProcedure rejects null ctx.user with TRPCError UNAUTHORIZED; ownership violations map to TRPCError NOT_FOUND (per Plans 05/06/07). Adversarial-test pattern precedent: the 43-03 identity-injection test built a throwaway router driven via createCallerFactory with a fabricated ctx.user — reuse that shape, but here drive the REAL appRouter with two distinct ctx.user ids over a seeded fake/local db.

Fixture for two users: seed user A (owns importer A + its emails/components/entities/knowledge/conversations) and user B (owns importer B + its rows). Then, acting AS user B (ctx.user = { id: B }), attempt to read/write A's ids across every router; assert each throws UNAUTHORIZED (sessionless) or NOT_FOUND/FORBIDDEN (owned-by-A). Use the same fake-db stub style as the per-router scoping tests from Plans 05-07, OR run against the local Supabase DB with two seeded importers/users — executor's choice; the local-DB variant gives the strongest proof and mirrors "live-verified locally".

FastAPI (apps/email-listener): emails endpoints (list/detail/download/reprocess) require X-User-Id (Plan 03); promote requires user ownership (Plan 03). Use the FastAPI TestClient + dishka override idiom (see apps/email-listener/app/presentation/api/v1/__tests__/test_chat_widget.py). Seed importer A owned by user A; drive requests with X-User-Id=B and assert 401 (missing header) / 404 / 403 as appropriate. Test cmd: `cd apps/email-listener && uv run pytest tests/adversarial/test_cross_tenant.py --no-cov`.

attachments route (apps/web): mirror Plan 07's route.test.ts but as a dedicated cross-tenant case (user B cannot obtain a signed URL for user A's attachment -> 404).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tRPC + web cross-tenant adversarial suite (reads AND writes, every router)</name>
  <files>packages/api-client/src/router/__tests__/cross-tenant-adversarial.test.ts, apps/web/src/app/api/attachments/__tests__/cross-tenant.test.ts</files>
  <read_first>
    - packages/api-client/src/root.ts (the full router list to iterate)
    - packages/api-client/src/trpc.test.ts (43-03 identity-injection / createCallerFactory pattern)
    - packages/api-client/src/router/emails/__tests__/emails-user-scoping.test.ts (per-router fake-db seeding idiom from Plan 05)
    - packages/api-client/src/router/chat/chat-user-scoping.test.ts (chat seeding idiom from Plan 07)
    - apps/web/src/app/api/attachments/__tests__/route.test.ts (attachments route test harness from Plan 07)
  </read_first>
  <action>
    Author cross-tenant-adversarial.test.ts: seed two users (A, B) and their owned rows in the shared fake/local db. Acting as user B, iterate the routers and attempt, for each user-owned procedure: (a) a READ of an A-owned id -> expect NOT_FOUND; (b) a WRITE/mutation targeting an A-owned id -> expect NOT_FOUND/FORBIDDEN; (c) a sessionless call (ctx.user=null) -> expect UNAUTHORIZED. Cover at least one representative read and one representative write per router: emails, entities, entityTypes, knowledge, chat; and genui.historyList/historyById (B must not see A's history). Assert POSITIVE control too: user B CAN access B's own rows (so the suite proves scoping, not blanket denial). Author attachments/__tests__/cross-tenant.test.ts: user B requesting A's attachment id -> 404; user A -> 200. Keep the genui generation cache OUT of the deny set (it is intentionally cross-tenant — assert it is auth-gated only, not ownership-denied).
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run src/router/__tests__/cross-tenant-adversarial.test.ts</automated>
    <automated>cd apps/web && npx vitest run src/app/api/attachments/__tests__/cross-tenant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The suite exercises every router in root.ts (emails, entities, entityTypes, knowledge, genui, chat) with at least one cross-tenant read AND one cross-tenant write attempt each, all denied.
    - Sessionless calls assert UNAUTHORIZED; owned-by-A calls assert NOT_FOUND/FORBIDDEN.
    - A positive-control case proves user B CAN reach B's own data.
    - The attachments cross-tenant case returns 404 for the non-owner and 200 for the owner.
    - Both suites pass.
  </acceptance_criteria>
  <done>The tRPC + web acceptance-gate suite is green: no second-user read/write reaches the first user's data.</done>
</task>

<task type="auto">
  <name>Task 2: FastAPI cross-tenant adversarial suite (emails + promote)</name>
  <files>apps/email-listener/tests/adversarial/test_cross_tenant.py</files>
  <read_first>
    - apps/email-listener/app/presentation/api/v1/__tests__/test_chat_widget.py (TestClient + dishka override idiom)
    - apps/email-listener/tests/presentation/api/v1/test_emails_user_scoping.py (Plan 03 email scoping tests to build on)
    - apps/email-listener/tests/application/test_promote_edge_user_scoping.py (Plan 03 promote scoping tests to build on)
    - apps/email-listener/app/presentation/middleware/user_context.py (require_user_id)
  </read_first>
  <action>
    Author tests/adversarial/test_cross_tenant.py: seed importer A owned by user A (emails/attachments/edge under A). Drive the FastAPI TestClient as user B (X-User-Id=B): GET /v1/emails (must not list A's rows), GET /v1/emails/{A_email} -> 404, GET /v1/emails/{A_email}/attachments/{A_att} -> 404, POST /v1/emails/{A_email}/reprocess -> 404, POST /v1/knowledge/edges/{A_edge}/promote with A's importer_id in the body -> rejected. Also assert a request with NO X-User-Id -> 401 on each user-scoped endpoint. Positive control: user A reaches A's own data (200). Use --no-cov for the targeted run.
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/adversarial/test_cross_tenant.py --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - Every user-scoped FastAPI endpoint (emails list/detail/download/reprocess + promote) denies user B access to user A's data and 401s a missing X-User-Id.
    - A positive control confirms user A reaches A's own data.
    - The suite passes under `uv run pytest tests/adversarial/test_cross_tenant.py --no-cov`.
  </acceptance_criteria>
  <done>The FastAPI acceptance-gate suite is green: no second-user request reaches the first user's data.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep inventory doc + full-suite confirmation</name>
  <files>.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-SWEEP-INVENTORY.md</files>
  <read_first>
    - packages/api-client/src/root.ts (router list)
    - .planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-05-SUMMARY.md
    - .planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-06-SUMMARY.md
    - .planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-07-SUMMARY.md
    - .planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-03-SUMMARY.md
  </read_first>
  <action>
    Author 44-SWEEP-INVENTORY.md: a table enumerating EVERY route/procedure across the tRPC routers, the FastAPI user-scoped endpoints, and the apps/web routes, with columns: surface | scoping mechanism (userOwnedImporterIds / assert*Ownership / direct user_id / require_user_id / deliberately-unscoped) | requires-auth (Y/N) | locking test file. Mark the genui generation cache rows as "deliberately unscoped (SC5)". Every user-owned row must show an enforcement mechanism AND a locking test; if any row cannot be marked enforced, flag it explicitly as an open gap (do not silently omit). Then run the full tenancy test set (both adversarial suites + the per-router suites from Plans 05/06/07 + the FastAPI scoping tests from Plan 03) and record the pass counts in the doc.
  </action>
  <verify>
    <automated>grep -c "|" .planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-SWEEP-INVENTORY.md</automated>
    <automated>cd packages/api-client && npx vitest run src/router 2>&1 | tail -5</automated>
    <automated>cd apps/email-listener && uv run pytest tests/adversarial tests/presentation/api/v1 tests/application --no-cov 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 44-SWEEP-INVENTORY.md enumerates every tRPC procedure + FastAPI user-scoped endpoint + apps/web route with a scoping mechanism, auth flag, and locking test per row.
    - genui generation cache rows are marked deliberately unscoped (SC5); no user-owned surface is left without an enforcement mechanism (or any exception is flagged as an open gap).
    - The packages/api-client router suite and the email-listener adversarial/scoping suites report zero failures.
  </acceptance_criteria>
  <done>Every route/procedure is inventoried with its scoping status + locking test, and the full tenancy suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| second authenticated user -> first user's data | the entire adversarial premise: a valid session for user B must never reach user A's rows via any surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-08-01 | Information Disclosure | every read surface | mitigate | Adversarial suite asserts NOT_FOUND for every cross-tenant read across all routers + FastAPI + attachments |
| T-44-08-02 | Tampering | every write surface | mitigate | Adversarial suite asserts NOT_FOUND/FORBIDDEN for every cross-tenant write/mutation |
| T-44-08-03 | Repudiation | coverage gaps | mitigate | Sweep inventory enumerates every surface + its locking test; unmarked surfaces are flagged, not omitted |
| T-44-08-04 | Information Disclosure | genui cache false-positive | accept | Cache rows asserted auth-gated but intentionally cross-tenant (SC5) — excluded from the deny set by design |
| T-44-08-SC | Tampering | no package installs | accept | No new packages added |
</threat_model>

<verification>
- cross-tenant-adversarial.test.ts (tRPC) + attachments cross-tenant.test.ts (web) green.
- tests/adversarial/test_cross_tenant.py (FastAPI) green.
- 44-SWEEP-INVENTORY.md covers every surface with a locking test; full router + FastAPI tenancy suites report zero failures.
</verification>

<success_criteria>
- Adversarial cross-tenant suite passes as the acceptance gate — a second user cannot read/write the first user's data via ANY route/procedure, including attachments download and the knowledge-promote proxy (ROADMAP SC2).
- The route/procedure sweep is enumerated with per-item scoping status + regression tests (ROADMAP SC3).
</success_criteria>

<output>
Create `.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-08-SUMMARY.md` when done.
</output>
