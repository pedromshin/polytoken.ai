---
phase: 44-tenancy-user-id-scoping-enforced-isolation
plan: 06
type: execute
wave: 3
depends_on: ["44-02"]
files_modified:
  - packages/api-client/src/router/entities/gallery.ts
  - packages/api-client/src/router/entities/detail.ts
  - packages/api-client/src/router/entities/mutations.ts
  - packages/api-client/src/router/entity-types.ts
  - packages/api-client/src/router/entity-types-write.ts
  - packages/api-client/src/router/knowledge/graph.ts
  - packages/api-client/src/router/knowledge/list.ts
  - packages/api-client/src/router/knowledge/detail.ts
  - packages/api-client/src/router/knowledge/expand.ts
  - packages/api-client/src/router/__tests__/entity-types-write.test.ts
  - packages/api-client/src/router/__tests__/component-relationship-mutations.test.ts
  - packages/api-client/src/router/entities/entities-user-scoping.test.ts
  - packages/api-client/src/router/knowledge/knowledge-user-scoping.test.ts
autonomous: true
requirements: [TENA-03]

must_haves:
  truths:
    - "Every entities, entity-types, and knowledge procedure is protectedProcedure"
    - "List/feed reads scope to the caller's owned importers (system-default entity_types with importer_id NULL stay visible)"
    - "Id-addressed entity reads/mutations reject rows whose importer is not owned by ctx.user"
    - "knowledge.expandNode validates the seed node is owned before expanding, and continues to scope neighbours to the seed's importer"
  artifacts:
    - path: "packages/api-client/src/router/knowledge/graph.ts"
      provides: "owned-importer scoped knowledge graph read"
      contains: "protectedProcedure"
    - path: "packages/api-client/src/router/entities/entities-user-scoping.test.ts"
      provides: "cross-tenant regression for the entities router"
      min_lines: 30
  key_links:
    - from: "packages/api-client/src/router/entities/gallery.ts"
      to: "userOwnedImporterIds"
      via: "@polytoken/db/ownership"
      pattern: "userOwnedImporterIds"
---

<objective>
Sweep the entities, entity-types, and knowledge tRPC routers onto `protectedProcedure` + the central ownership helper. All three are importer-anchored (entity_instances / entity_types / knowledge_nodes carry importer_id), so scoping is uniform: reads filter to owned importers (preserving NULL-importer system defaults for entity_types), and id-addressed operations assert the row's importer is owned.

Purpose: closes the entity + knowledge-graph surface of TENA-03.
Output: guarded entities/entity-types/knowledge routers + per-router cross-tenant regressions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-CONTEXT.md
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-02-SUMMARY.md

<interfaces>
<!-- Sweep recipe + procedures. Extracted — no exploration. -->

protectedProcedure + ctx {headers,db,user} from ../../trpc (or ../trpc for entity-types.ts at router root).
Ownership helper (@polytoken/db/ownership): userOwnedImporterIds(db,userId); assertImporterOwnership(db,importerId,userId). For an entity/knowledge row addressed by id: load the row's importer_id, then assertImporterOwnership(ctx.db, importerId, ctx.user.id); map OwnershipError → TRPCError NOT_FOUND.

Procedures:
- entities/gallery.ts — list (importerId filter at line 133). Scope to owned importers.
- entities/detail.ts — byId (loads entity, importerId at line 163/451). Assert the entity's importer is owned.
- entities/mutations.ts — confirmMerge, rejectMerge, unmerge (keyed on entity/candidate ids). Assert ownership of every referenced entity's importer.
- entity-types.ts — list (system defaults, importer_id may be NULL). Make protected; include NULL-importer defaults OR-ed with owned-importer overrides.
- entity-types-write.ts — create, update, createField, updateField, deleteField, reorderFields. These write entity_types/entity_type_fields; a NON-NULL importer_id write must target an owned importer (assertImporterOwnership); reject writes that would create/modify another user's override. System-default (NULL importer) writes are NOT allowed from a user session — reject or require an owned importer.
- knowledge/list.ts — list (importerId filter, line 24). Scope to owned importers.
- knowledge/graph.ts — graph (importerId filter, D-12, applied across entity_types/instances/candidate-links). Replace client-importerId trust with owned-importer derivation; keep the isNull(importerId) OR-branch for system-default entity_types (line 232-256) so the seeded taxonomy stays visible.
- knowledge/detail.ts — byId. Assert the node's importer is owned.
- knowledge/expand.ts — expandNode (already derives importerId from the SEED node, line 209). ADD: assert the seed node's importer is owned by ctx.user before expanding (today any caller can expand any node id).

Pre-existing tests entity-types-write.test.ts and component-relationship-mutations.test.ts construct callers with user:null → update to a valid user + seeded ownership.
Test cmd: `cd packages/api-client && npx vitest run <file>`; typecheck `npx tsc --noEmit`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard the entities router (gallery, detail, mutations)</name>
  <files>packages/api-client/src/router/entities/gallery.ts, packages/api-client/src/router/entities/detail.ts, packages/api-client/src/router/entities/mutations.ts, packages/api-client/src/router/entities/entities-user-scoping.test.ts, packages/api-client/src/router/__tests__/component-relationship-mutations.test.ts</files>
  <read_first>
    - packages/api-client/src/router/entities/gallery.ts (importerId filter line 133)
    - packages/api-client/src/router/entities/detail.ts (entity importerId reads)
    - packages/api-client/src/router/entities/mutations.ts (confirmMerge/rejectMerge/unmerge id inputs)
    - packages/api-client/src/router/__tests__/component-relationship-mutations.test.ts (user:null to update)
    - packages/api-client/src/trpc.ts (protectedProcedure)
  </read_first>
  <action>
    Swap the three entities files to protectedProcedure. gallery.list: scope via userOwnedImporterIds + inArray(EntityInstances.importerId, owned); validate any provided importerId against owned. detail.byId: after loading the entity, assertImporterOwnership on its importerId. mutations (confirmMerge/rejectMerge/unmerge): assert ownership of every referenced entity's importer before the write (a merge must not join an entity the caller does not own). Map OwnershipError → TRPCError NOT_FOUND. Author entities-user-scoping.test.ts (list isolation + byId + merge cross-tenant rejection); update component-relationship-mutations.test.ts callers.
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run src/router/entities/entities-user-scoping.test.ts src/router/__tests__/component-relationship-mutations.test.ts</automated>
    <automated>grep -c "publicProcedure" packages/api-client/src/router/entities/gallery.ts packages/api-client/src/router/entities/detail.ts packages/api-client/src/router/entities/mutations.ts</automated>
  </verify>
  <acceptance_criteria>
    - Zero publicProcedure remain in the three entities files.
    - gallery.list returns only owned-importer entities; detail/merge reject non-owned targets (tests).
    - entities-user-scoping.test.ts + updated component-relationship-mutations.test.ts pass.
  </acceptance_criteria>
  <done>The entities router is protected and user-scoped with regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Guard the entity-types router (read + writes)</name>
  <files>packages/api-client/src/router/entity-types.ts, packages/api-client/src/router/entity-types-write.ts, packages/api-client/src/router/__tests__/entity-types-write.test.ts</files>
  <read_first>
    - packages/api-client/src/router/entity-types.ts (list — system defaults, importer_id may be NULL)
    - packages/api-client/src/router/entity-types-write.ts (create/update/createField/updateField/deleteField/reorderFields)
    - packages/db/src/schema/entity-types.ts (nullable importer_id semantics: NULL=system default, uuid=override)
    - packages/api-client/src/router/__tests__/entity-types-write.test.ts (user:null to update)
  </read_first>
  <action>
    Swap entity-types.ts and entity-types-write.ts to protectedProcedure. list: return system-default types (importer_id IS NULL) OR-ed with the caller's owned-importer overrides — never another user's overrides. Writes: any write targeting a NON-NULL importer_id must assertImporterOwnership on that importer; reject attempts to create/modify/delete a system-default (NULL importer) type or field from a user session (system defaults are migration-seeded only). For field operations addressed by entityTypeFieldId/entityTypeId, load the owning type's importer_id and assert ownership before mutating. Map OwnershipError → TRPCError NOT_FOUND/FORBIDDEN. Update entity-types-write.test.ts callers with a valid user + seeded ownership and add a cross-tenant write-rejection case.
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run src/router/__tests__/entity-types-write.test.ts</automated>
    <automated>grep -c "publicProcedure" packages/api-client/src/router/entity-types.ts packages/api-client/src/router/entity-types-write.ts</automated>
  </verify>
  <acceptance_criteria>
    - Zero publicProcedure remain in entity-types.ts / entity-types-write.ts.
    - list surfaces system defaults + own overrides only (no other user's overrides).
    - A write against a non-owned importer is rejected; a NULL-importer system-default write from a user session is rejected (tests).
    - entity-types-write.test.ts passes.
  </acceptance_criteria>
  <done>The entity-types router is protected; writes are ownership-validated and system defaults are read-only for user sessions.</done>
</task>

<task type="auto">
  <name>Task 3: Guard the knowledge router (list, graph, detail, expandNode)</name>
  <files>packages/api-client/src/router/knowledge/list.ts, packages/api-client/src/router/knowledge/graph.ts, packages/api-client/src/router/knowledge/detail.ts, packages/api-client/src/router/knowledge/expand.ts, packages/api-client/src/router/knowledge/knowledge-user-scoping.test.ts</files>
  <read_first>
    - packages/api-client/src/router/knowledge/list.ts (importerId filter line 24)
    - packages/api-client/src/router/knowledge/graph.ts (importerId + isNull system-default OR-branch lines 232-256)
    - packages/api-client/src/router/knowledge/detail.ts (node byId)
    - packages/api-client/src/router/knowledge/expand.ts (seed-node importerId derivation line 209)
  </read_first>
  <action>
    Swap all four knowledge files to protectedProcedure. list: scope to userOwnedImporterIds. graph: replace client-importerId trust with owned-importer derivation while KEEPING the isNull(EntityTypes.importerId) OR-branch (system-default taxonomy stays visible); every sub-query must be bounded to owned importers OR NULL-defaults. detail.byId: assert the node's importer is owned. expandNode: after loading the seed node, assertImporterOwnership on the seed's importerId before expansion (close the "expand any node id" gap); keep the existing seed-derived neighbour scoping. Map OwnershipError → TRPCError NOT_FOUND. Author knowledge-user-scoping.test.ts (list isolation; detail/expand cross-tenant rejection; system-default taxonomy still visible).
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run src/router/knowledge/knowledge-user-scoping.test.ts</automated>
    <automated>grep -c "publicProcedure" packages/api-client/src/router/knowledge/list.ts packages/api-client/src/router/knowledge/graph.ts packages/api-client/src/router/knowledge/detail.ts packages/api-client/src/router/knowledge/expand.ts</automated>
    <automated>cd packages/api-client && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Zero publicProcedure remain across the four knowledge files.
    - knowledge.list/graph return only owned-importer rows plus NULL-importer system defaults; expandNode rejects a non-owned seed (tests).
    - knowledge-user-scoping.test.ts passes; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The knowledge router is protected and user-scoped, system defaults preserved, expand gated on seed ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tRPC client → resolver | importerId / entity id / node id inputs are untrusted; scope derives from ctx.user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06-01 | Information Disclosure | list/gallery/graph importerId | mitigate | Scope to userOwnedImporterIds (+ NULL system defaults where applicable) |
| T-44-06-02 | Elevation of Privilege | entity/type/node byId + writes | mitigate | assertImporterOwnership on the row's importer before read/write |
| T-44-06-03 | Elevation of Privilege | knowledge.expandNode | mitigate | Assert seed-node ownership before expansion |
| T-44-06-04 | Tampering | entity-types system defaults | mitigate | Reject NULL-importer writes from user sessions (seed-only) |
| T-44-06-SC | Tampering | no package installs | accept | No new npm packages |
</threat_model>

<verification>
- entities/entity-types/knowledge user-scoping tests green; updated pre-existing tests green.
- `grep -c publicProcedure` across all swept files returns 0.
- `npx tsc --noEmit` in packages/api-client clean.
</verification>

<success_criteria>
- No entities/entity-types/knowledge procedure accepts a client-supplied importer id for scoping; cross-tenant reads AND writes rejected (ROADMAP SC2/SC3).
</success_criteria>

<output>
Create `.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-06-SUMMARY.md` when done.
</output>
