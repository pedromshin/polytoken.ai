---
phase: 44-tenancy-user-id-scoping-enforced-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/importers.ts
  - packages/db/src/schema/chat-conversations.ts
  - packages/db/src/schema/chat-cost-ledger.ts
  - packages/db/src/schema/genui-generation-events.ts
  - packages/db/src/schema/ui-spec-templates.ts
  - packages/db/src/schema/_auth.ts
  - packages/db/src/migrate.ts
  - packages/db/migrations/0031_add_user_id_columns.sql
  - packages/db/migrations/0032_backfill_user_id.sql
  - packages/db/migrations/0033_user_id_not_null.sql
  - packages/db/migrations/meta/_journal.json
  - .planning/PROJECT.md
autonomous: true
requirements: [TENA-01, TENA-02, TENA-04]

must_haves:
  truths:
    - "importers, chat_conversations, chat_cost_ledger each carry a user_id column referencing auth.users(id)"
    - "Every existing row in those three tables has a non-null user_id after migration (backfilled to the first real user)"
    - "The expand→backfill→contract migration sequence runs cleanly against the local Supabase DB"
    - "genui exact-match cache tables (genui_generation_events, ui_spec_templates) are documented as deliberately unscoped in both schema comments and PROJECT.md"
    - "PROJECT.md Key Decisions records app-boundary-primary / RLS-defense-in-depth enforcement architecture, citing the Drizzle superuser-connection precedent"
  artifacts:
    - path: "packages/db/src/schema/importers.ts"
      provides: "user_id column on the tenant anchor table"
      contains: "user_id"
    - path: "packages/db/migrations/0032_backfill_user_id.sql"
      provides: "fail-loud backfill to the single auth.users row with BACKFILL_USER_ID override"
      contains: "RAISE EXCEPTION"
    - path: ".planning/PROJECT.md"
      provides: "recorded enforcement-architecture decision (TENA-04 ordering gate)"
      contains: "app-boundary"
  key_links:
    - from: "packages/db/src/schema/importers.ts"
      to: "auth.users"
      via: "user_id foreign key"
      pattern: "user_id"
    - from: "packages/db/migrations/0032_backfill_user_id.sql"
      to: "importers.user_id"
      via: "UPDATE ... SET user_id"
      pattern: "UPDATE .*importers"
---

<objective>
Anchor tenancy in the schema: add a `user_id` column referencing `auth.users(id)` to the tenant anchor (`importers`) and directly to the two chat tables that are not importer-descendants (`chat_conversations`, `chat_cost_ledger`), then migrate all existing data to the first real user via an expand→backfill→contract sequence, live-verified against the local Supabase DB. Also record the enforcement-architecture decision in PROJECT.md (the TENA-04 ordering gate that must land before any RLS policy work) and document the genui exact-match cache tables as deliberately unscoped.

Purpose: This is the structural foundation every downstream enforcement plan depends on — the ownership helper, the tRPC/FastAPI sweep, and the RLS policies all key off these `user_id` columns.
Output: 3 migrations (0031 expand, 0032 backfill, 0033 contract), updated Drizzle schema files, an updated migrate.ts that injects `BACKFILL_USER_ID`, and PROJECT.md Key Decisions + genui-unscoped documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-CONTEXT.md

<interfaces>
<!-- Existing tenant-anchoring shape the executor must respect. Extracted from the codebase — no exploration needed. -->

packages/db/src/client.ts:28-36 — Drizzle connects via POSTGRES_URL_NON_POOLING as the Postgres SUPERUSER (session-mode) because the transaction-mode pooler broke auth.uid(). Consequence: RLS is bypassed for every app query through Drizzle. This is the precedent the PROJECT.md decision must cite.

packages/db/src/schema/importers.ts — Importers table: id uuid PK defaultRandom, slug text unique, name text, config jsonb, createdAt. This is the tenant anchor; its descendant tables (emails, email_attachments, email_components, extraction_records, entity_instances, sender_profiles, entity_types/entity_type_fields, knowledge_nodes) carry a hard `importer_id uuid NOT NULL REFERENCES importers(id) ON DELETE CASCADE` and are scoped via ONE join — they get NO user_id column.

packages/db/src/schema/chat-conversations.ts — chat_conversations.importerId is a plain uuid with NO FK; gets a DIRECT user_id (not importer-anchored).
packages/db/src/schema/chat-cost-ledger.ts — chat_cost_ledger.importerId is a plain uuid NOT NULL with NO FK; gets a DIRECT user_id.
packages/db/src/schema/genui-generation-events.ts + ui-spec-templates.ts — the exact-match cache tables; DELIBERATELY stay unscoped (cross-tenant cache hits are the point). Only schema-comment documentation changes here.

Migration scripts (run from packages/db):
- `npm run migration:generate` — drizzle-kit generate (auto-numbers, updates meta snapshot). Use for column ADD and SET NOT NULL (drift-safe).
- `npm run migration:generate:custom` — empty custom migration for hand-written data SQL. Use for the backfill.
- `npm run migrate:local` — tsx src/migrate.ts against local Supabase via ../../.env.local.
Existing migrations end at 0030; new ones are 0031/0032/0033. Journal is packages/db/migrations/meta/_journal.json.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record enforcement-architecture decision + document genui cache tables as deliberately unscoped</name>
  <files>.planning/PROJECT.md, packages/db/src/schema/genui-generation-events.ts, packages/db/src/schema/ui-spec-templates.ts</files>
  <read_first>
    - .planning/PROJECT.md (find the "Key Decisions" section and its existing entry format)
    - .planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-CONTEXT.md (the decision text to record, lines 25-34)
    - packages/db/src/client.ts (the superuser-connection precedent to cite, lines 28-36)
    - packages/db/src/schema/genui-generation-events.ts (current module docstring/comment style)
    - packages/db/src/schema/ui-spec-templates.ts (current module docstring/comment style)
  </read_first>
  <action>
    Add a new dated entry to PROJECT.md "Key Decisions" recording the tenancy enforcement architecture: app-boundary enforcement is PRIMARY (session-derived user_id, never client-supplied), RLS policies are DEFENSE-IN-DEPTH only. State the load-bearing rationale explicitly: Drizzle connects as the Postgres superuser via POSTGRES_URL_NON_POOLING (packages/db/src/client.ts:28-36) because the transaction-mode pooler strips superuser privileges and breaks auth.uid(), so RLS is bypassed for every app query — meaning the app boundary is the real wall and RLS defends only PostgREST/future non-superuser paths. This entry satisfies the TENA-04 ordering requirement that the decision be recorded BEFORE any RLS policy work (Plan 04) begins. In the same PROJECT.md entry (or an adjacent bullet), document that the genui exact-match cache tables (genui_generation_events, ui_spec_templates) are DELIBERATELY left unscoped because cross-tenant cache hits are the intended behavior (Success Criterion 5).
    In genui-generation-events.ts and ui-spec-templates.ts, extend the top-of-file module comment with a line stating the table is intentionally NOT tenant-scoped for user_id purposes (exact-match cache reuse is shared cross-tenant by design, per D-05/Phase-44), so a future reader does not "fix" it as a gap.
    Do NOT modify any column definitions in this task — comments and PROJECT.md prose only.
  </action>
  <verify>
    <automated>grep -n "app-boundary" .planning/PROJECT.md && grep -n "unscoped\|deliberately" packages/db/src/schema/genui-generation-events.ts packages/db/src/schema/ui-spec-templates.ts</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md Key Decisions contains a new entry naming both "app-boundary" (primary) and "RLS" (defense-in-depth) and cites packages/db/src/client.ts superuser connection.
    - PROJECT.md states genui_generation_events and ui_spec_templates are deliberately unscoped.
    - Both genui cache schema files contain a comment marking them intentionally not user-scoped.
    - No column definition lines changed in either schema file (git diff shows only comment additions).
  </acceptance_criteria>
  <done>The enforcement decision is recorded (unblocking Plan 04) and the genui unscoped exclusion is documented in schema comments + PROJECT.md.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Add nullable user_id columns + FK to auth.users, generate & apply the EXPAND migration locally</name>
  <files>packages/db/src/schema/_auth.ts, packages/db/src/schema/importers.ts, packages/db/src/schema/chat-conversations.ts, packages/db/src/schema/chat-cost-ledger.ts, packages/db/migrations/0031_add_user_id_columns.sql, packages/db/migrations/meta/_journal.json</files>
  <read_first>
    - packages/db/src/schema/importers.ts (add the column here)
    - packages/db/src/schema/chat-conversations.ts (add the column here)
    - packages/db/src/schema/chat-cost-ledger.ts (add the column here)
    - packages/db/src/schema/index.ts (export order — a new _auth.ts module must be exported before importers.ts)
    - packages/db/migrations/0001_rls_deny_all.sql (how existing migrations reference Supabase roles; note NO existing migration references auth.users yet)
    - packages/db/src/migrate.ts (the runner that applies migrations locally)
  </read_first>
  <action>
    Create packages/db/src/schema/_auth.ts declaring the Supabase `auth` schema and a minimal reference to `auth.users(id)` so Drizzle can model a cross-schema foreign key. Use `pgSchema("auth")` and define a table `users` with at least `id: uuid("id").primaryKey()` (do NOT redeclare the whole Supabase auth.users table — only what a FK reference needs). Export it from packages/db/src/schema/index.ts BEFORE the importers export so dependency order holds.
    Add `userId: uuid("user_id").references(() => AuthUsers.id, { onDelete: "cascade" })` as a NULLABLE column (no .notNull() yet — this is the expand step) to: importers.ts, chat-conversations.ts, chat-cost-ledger.ts. For chat_conversations and chat_cost_ledger, place user_id next to the existing importerId column and add an index on user_id (mirror the existing importerId index naming: idx_chat_conversations_user_id, idx_chat_cost_ledger_user_id). For importers, add an index idx_importers_user_id.
    Generate the expand migration with `npm run migration:generate` from packages/db; drizzle-kit will emit ADD COLUMN + FK + indexes and update the meta snapshot (drift-safe). Rename the generated file to 0031_add_user_id_columns.sql if drizzle names it differently, keeping the _journal.json tag in sync.
    Apply locally: `npm run migrate:local` from packages/db. This is a [BLOCKING] step — the migration MUST run against the local DB before verification; type/build checks pass without it (types come from schema files) which would be a false positive.
  </action>
  <verify>
    <automated>cd packages/db && npm run migrate:local 2>&1 | grep -E "Migrations completed|user_id"</automated>
    <automated>cd packages/db && node -e "const p=require('postgres');const s=p(process.env.POSTGRES_URL_NON_POOLING,{prepare:false});s\`SELECT table_name,column_name,is_nullable FROM information_schema.columns WHERE column_name='user_id' AND table_name IN ('importers','chat_conversations','chat_cost_ledger') ORDER BY table_name\`.then(r=>{console.log(r);process.exit(r.length===3?0:1)})"</automated>
  </verify>
  <acceptance_criteria>
    - packages/db/src/schema/_auth.ts exports an AuthUsers pgSchema("auth") table with id uuid PK.
    - importers.ts, chat-conversations.ts, chat-cost-ledger.ts each declare `user_id` referencing auth.users(id), nullable (no .notNull()).
    - 0031_add_user_id_columns.sql exists and its tag is registered in meta/_journal.json.
    - After migrate:local, information_schema reports user_id present and is_nullable='YES' on all three tables (the query above returns exactly 3 rows, exit 0).
  </acceptance_criteria>
  <done>All three tables have a nullable user_id column + FK to auth.users applied to the local DB; the meta snapshot is updated (no drift).</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Backfill to the first real user (fail-loud + override) + CONTRACT to NOT NULL, applied & verified locally</name>
  <files>packages/db/migrations/0032_backfill_user_id.sql, packages/db/migrations/0033_user_id_not_null.sql, packages/db/migrations/meta/_journal.json, packages/db/src/migrate.ts, packages/db/src/schema/importers.ts, packages/db/src/schema/chat-conversations.ts, packages/db/src/schema/chat-cost-ledger.ts</files>
  <read_first>
    - packages/db/src/migrate.ts (extend it to inject the BACKFILL_USER_ID override as a session GUC)
    - packages/db/migrations/0005_seed_default_importer.sql (idempotent-SQL style precedent)
    - packages/db/src/schema/importers.ts (flip user_id to .notNull() for the contract migration)
  </read_first>
  <action>
    BACKFILL (0032, custom migration via `npm run migration:generate:custom`): hand-write a `DO $$ ... $$` block that resolves the target user id as: `coalesce(nullif(current_setting('app.backfill_user_id', true), ''), (SELECT id::text FROM auth.users ORDER BY created_at ASC LIMIT 1))`. Guard: if no override is set AND `(SELECT count(*) FROM auth.users) <> 1`, `RAISE EXCEPTION 'Backfill requires exactly one auth.users row (found %). Set BACKFILL_USER_ID to override.', (SELECT count(*) FROM auth.users);`. Then `UPDATE importers SET user_id = target WHERE user_id IS NULL;` and the same for chat_conversations and chat_cost_ledger. After the updates, assert completeness: if any of the three tables still has a `user_id IS NULL` row, `RAISE EXCEPTION`. This makes the backfill idempotent and fail-loud.
    Wire the override: in migrate.ts, before calling `migrate(...)`, if `process.env.BACKFILL_USER_ID` is set, run `await pool.query("SET app.backfill_user_id = $1", [process.env.BACKFILL_USER_ID])` on a dedicated connection used for the migration run (set it at the database/session level so the DO block's current_setting can read it). Read the value via the existing env import pattern; do not hardcode.
    Pre-apply check inside this task: query `SELECT count(*) FROM auth.users` against the local DB. If exactly 1 → proceed with plain migrate:local. If 0 → the local session has no signed-in user; seed a single deterministic local-dev auth.users row (fixed uuid, minimal required columns) and document it in the SUMMARY as local-dev-only, then proceed. If >1 → set BACKFILL_USER_ID to the earliest-created row and document the choice.
    CONTRACT (0033, via `npm run migration:generate`): flip `userId` to `.notNull()` in all three schema files, generate the ALTER ... SET NOT NULL migration (drizzle updates the snapshot), then apply. This is the contract step — it must run AFTER the backfill leaves zero nulls.
    Apply both locally with `npm run migrate:local` ([BLOCKING]).
  </action>
  <verify>
    <automated>cd packages/db && npm run migrate:local 2>&1 | grep -E "Migrations completed"</automated>
    <automated>cd packages/db && node -e "const p=require('postgres');const s=p(process.env.POSTGRES_URL_NON_POOLING,{prepare:false});s\`SELECT (SELECT count(*) FROM importers WHERE user_id IS NULL) a,(SELECT count(*) FROM chat_conversations WHERE user_id IS NULL) b,(SELECT count(*) FROM chat_cost_ledger WHERE user_id IS NULL) c\`.then(r=>{console.log(r);const{a,b,c}=r[0];process.exit(a==0&&b==0&&c==0?0:1)})"</automated>
    <automated>cd packages/db && node -e "const p=require('postgres');const s=p(process.env.POSTGRES_URL_NON_POOLING,{prepare:false});s\`SELECT is_nullable FROM information_schema.columns WHERE table_name='importers' AND column_name='user_id'\`.then(r=>{process.exit(r[0].is_nullable==='NO'?0:1)})"</automated>
  </verify>
  <acceptance_criteria>
    - 0032_backfill_user_id.sql contains a RAISE EXCEPTION guard keyed on `count(*) <> 1` of auth.users AND reads `current_setting('app.backfill_user_id', true)`.
    - migrate.ts issues `SET app.backfill_user_id` when process.env.BACKFILL_USER_ID is present.
    - 0033_user_id_not_null.sql sets user_id NOT NULL on all three tables; both migrations are registered in meta/_journal.json.
    - After migrate:local: zero user_id IS NULL rows across importers/chat_conversations/chat_cost_ledger (query returns exit 0) and importers.user_id is_nullable='NO' (exit 0).
    - The SUMMARY records the observed auth.users count and which backfill path (single/seed/override) was taken.
  </acceptance_criteria>
  <done>Every existing row is backfilled to a real user id, columns are NOT NULL, and the sequence is proven live against the local DB (Success Criterion 1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → local DB | migrate:local applies DDL/DML as superuser; a bad backfill could mis-assign ownership of all data |
| auth.users → app tables | user_id FK is the anchor for every downstream ownership check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01-01 | Tampering | 0032 backfill | mitigate | Fail-loud RAISE EXCEPTION when auth.users count ≠ 1 (no silent mis-assignment); BACKFILL_USER_ID override is explicit and logged in SUMMARY |
| T-44-01-02 | Information Disclosure | user_id FK correctness | mitigate | FK REFERENCES auth.users(id); post-backfill assertion that zero user_id IS NULL rows remain before contract |
| T-44-01-03 | Repudiation | enforcement decision | mitigate | Decision recorded in PROJECT.md with the superuser-connection rationale (TENA-04 ordering gate) before any RLS work |
| T-44-01-SC | Tampering | no package installs | accept | This plan adds no npm/pip/cargo packages — no supply-chain surface |
</threat_model>

<verification>
- `npm run migrate:local` from packages/db completes without error and reports the table count.
- All three tables report a NOT NULL user_id with zero null rows.
- `npm run typecheck` in packages/db passes (schema TS compiles with the new columns + _auth.ts).
- PROJECT.md and the two genui schema files carry the documented-unscoped exclusion.
</verification>

<success_criteria>
- user_id anchored on importers + direct on chat_conversations/chat_cost_ledger, migrated + backfilled, live-verified locally (ROADMAP SC1).
- Enforcement-architecture decision recorded in PROJECT.md Key Decisions before RLS work (ROADMAP SC4 ordering half, TENA-04).
- genui exact-match cache tables documented as deliberately unscoped (ROADMAP SC5).
</success_criteria>

<output>
Create `.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-01-SUMMARY.md` when done.
</output>
