---
phase: 44-tenancy-user-id-scoping-enforced-isolation
plan: 07
type: execute
wave: 3
depends_on: ["44-02"]
files_modified:
  - packages/api-client/src/router/chat/conversations.ts
  - packages/api-client/src/router/chat/history.ts
  - packages/api-client/src/router/chat/cost.ts
  - packages/api-client/src/router/chat/browser-turn.ts
  - packages/api-client/src/router/chat/canvas.ts
  - packages/api-client/src/router/chat/widget-interactions.ts
  - packages/api-client/src/router/genui/generate.ts
  - packages/api-client/src/router/genui/code-island.ts
  - packages/api-client/src/router/genui/history.ts
  - packages/api-client/src/router/chat/__tests__/conversations.test.ts
  - packages/api-client/src/router/chat/chat-user-scoping.test.ts
  - packages/api-client/src/router/genui/__tests__/history.test.ts
  - apps/web/src/app/api/attachments/[id]/route.ts
  - apps/web/src/app/api/attachments/__tests__/route.test.ts
autonomous: true
requirements: [TENA-03]

must_haves:
  truths:
    - "Every chat and genui procedure is protectedProcedure — sessionless calls are UNAUTHORIZED"
    - "chat conversations create/list/rename/delete/setModel and all conversation-scoped reads are keyed on chat_conversations.user_id = ctx.user.id"
    - "genui.historyList is user-scoped (never returns all importers' rows — closes backlog 999.1); genui generation cache stays deliberately cross-tenant"
    - "The attachments download route resolves getUser() and denies a signed URL for an attachment the caller does not own (importer→user)"
  artifacts:
    - path: "packages/api-client/src/router/chat/conversations.ts"
      provides: "user_id-scoped conversation CRUD"
      contains: "user"
    - path: "apps/web/src/app/api/attachments/[id]/route.ts"
      provides: "ownership-gated attachment signed-URL route"
      contains: "getUser"
  key_links:
    - from: "apps/web/src/app/api/attachments/[id]/route.ts"
      to: "@polytoken/db/ownership"
      via: "assertImporterOwnership on the attachment's importer"
      pattern: "assertImporterOwnership|getUser"
    - from: "packages/api-client/src/router/chat/history.ts"
      to: "assertConversationOwnership"
      via: "@polytoken/db/ownership"
      pattern: "assertConversationOwnership"
---

<objective>
Sweep the chat and genui tRPC routers onto `protectedProcedure` + ownership, and close the last currently-unscoped web route (the attachments download proxy). Chat scopes on the DIRECT `chat_conversations.user_id`; genui requires auth (generation cache stays deliberately cross-tenant) while `genui.historyList` becomes user-scoped (absorbing backlog 999.1). The attachments route — which today has ZERO tenant scoping — gains a session + ownership gate before minting a signed URL.

Purpose: closes the chat, genui-history, and attachments surfaces of TENA-03, including the attachments-download acceptance-gate target.
Output: guarded chat/genui routers + ownership-gated attachments route + regressions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-CONTEXT.md
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-02-SUMMARY.md

<interfaces>
<!-- Sweep recipe + the attachments-route contract. Extracted — no exploration. -->

protectedProcedure + ctx {headers,db,user} from ../../trpc.
Ownership helper (@polytoken/db/ownership): assertConversationOwnership(db, conversationId, userId) (chat_conversations.user_id); userOwnedImporterIds(db,userId); assertImporterOwnership(db,importerId,userId). Map OwnershipError -> TRPCError NOT_FOUND.

CHAT recipe (direct user_id; Plan 01 added chat_conversations.user_id + chat_cost_ledger.user_id):
- conversations.ts createConversation: set `user_id: ctx.user.id` on insert (keep importerId as-is). listConversations: filter `eq(ChatConversations.user_id, ctx.user.id)` (replace the importerId-only scoping). renameConversation/deleteConversation/setModel: assertConversationOwnership(ctx.db, id, ctx.user.id) before the write.
- history.ts getHistory (conversationId): assertConversationOwnership before reading messages.
- cost.ts: scope ledger reads by ctx.user.id (chat_cost_ledger.user_id) or by conversation ownership — pick the column that matches the query.
- browser-turn.ts, canvas.ts, widget-interactions.ts: assertConversationOwnership on the conversationId before read/write.

GENUI recipe (cache deliberately unscoped, per CONTEXT + Plan 01 docs):
- generate.ts (line 137 forwards importer_id) + code-island.ts (line 83): make protectedProcedure (require auth). Do NOT add ownership scoping to the generation cache — cross-tenant exact-match cache reuse is intended. Auth-gate only.
- history.ts historyList (line 163 sets importer_id param): make protectedProcedure and ALWAYS forward an owned-importer scope derived from ctx.user (userOwnedImporterIds); NEVER call FastAPI with importer_id omitted (that returns all importers' rows — the 999.1 gap). historyById: after fetching, verify the returned event's importer_id is owned by ctx.user (deny rows whose importer is not owned; a NULL-importer event is not a browsable history row for a user). Update genui/__tests__/history.test.ts (Test 4 forwards importerId) to the new user-scoped contract.

ATTACHMENTS route (apps/web/src/app/api/attachments/[id]/route.ts — TODAY: uuid check -> db.select(storageKey).from(EmailAttachments).where(eq(id)) -> signed URL, NO tenant scoping):
- Resolve the acting user server-side via `~/lib/supabase/server` createClient().auth.getUser() (mirror the 43-04 promote route pattern); 401 on null user.
- Select the attachment's importerId (email_attachments carries importer_id directly) alongside storageKey; then assertImporterOwnership(db, importerId, user.id) (import db from @polytoken/db/client, helper from @polytoken/db/ownership). On OwnershipError -> 404 (fail-closed, no existence oracle). Only then mint the signed URL.
- Preserve the existing missing-secret guard and error handling.

Pre-existing chat/genui tests that pass user:null must be updated to a valid user + seeded ownership.
Test cmds: `cd packages/api-client && npx vitest run <file>`; for the web route test use the apps/web configured runner (`cd apps/web && npx vitest run <file>`); `npx tsc --noEmit`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope the chat router on chat_conversations.user_id</name>
  <files>packages/api-client/src/router/chat/conversations.ts, packages/api-client/src/router/chat/history.ts, packages/api-client/src/router/chat/cost.ts, packages/api-client/src/router/chat/browser-turn.ts, packages/api-client/src/router/chat/canvas.ts, packages/api-client/src/router/chat/widget-interactions.ts, packages/api-client/src/router/chat/chat-user-scoping.test.ts, packages/api-client/src/router/chat/__tests__/conversations.test.ts</files>
  <read_first>
    - packages/api-client/src/router/chat/conversations.ts (create/list/rename/delete/setModel)
    - packages/api-client/src/router/chat/history.ts (getHistory by conversationId)
    - packages/api-client/src/router/chat/cost.ts (which user/conversation column it aggregates on)
    - packages/api-client/src/router/chat/browser-turn.ts + canvas.ts + widget-interactions.ts (conversationId inputs)
    - packages/db/src/schema/chat-conversations.ts (user_id column from Plan 01)
    - packages/api-client/src/router/chat/__tests__/conversations.test.ts (pre-existing tests to update)
  </read_first>
  <action>
    Apply the CHAT recipe. createConversation writes user_id = ctx.user.id. listConversations filters eq(user_id, ctx.user.id). renameConversation/deleteConversation/setModel + getHistory + browser-turn + canvas + widget-interactions all call assertConversationOwnership(ctx.db, the conversationId, ctx.user.id) before their read/write (OwnershipError to TRPCError NOT_FOUND). cost.ts scopes ledger reads by ctx.user.id. Swap every chat procedure to protectedProcedure. Author chat-user-scoping.test.ts: sessionless to UNAUTHORIZED; user A's listConversations excludes B's conversations; A cannot rename/delete/getHistory on B's conversation. Update the pre-existing conversations.test.ts callers to pass a valid ctx.user + seeded ownership.
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run src/router/chat/chat-user-scoping.test.ts src/router/chat/__tests__/conversations.test.ts</automated>
    <automated>grep -rc "publicProcedure" packages/api-client/src/router/chat/conversations.ts packages/api-client/src/router/chat/history.ts packages/api-client/src/router/chat/cost.ts packages/api-client/src/router/chat/browser-turn.ts packages/api-client/src/router/chat/canvas.ts packages/api-client/src/router/chat/widget-interactions.ts</automated>
  </verify>
  <acceptance_criteria>
    - Zero publicProcedure remain across the six chat files (grep sums to 0).
    - createConversation persists user_id = ctx.user.id; listConversations returns only the caller's conversations (two-user test).
    - rename/delete/setModel/getHistory/browser-turn/canvas/widget-interactions reject a non-owned conversationId (tests).
    - chat-user-scoping.test.ts + updated conversations.test.ts pass.
  </acceptance_criteria>
  <done>The chat router is protected and scoped on chat_conversations.user_id with regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Auth-gate genui + user-scope genui history (closes 999.1)</name>
  <files>packages/api-client/src/router/genui/generate.ts, packages/api-client/src/router/genui/code-island.ts, packages/api-client/src/router/genui/history.ts, packages/api-client/src/router/genui/__tests__/history.test.ts</files>
  <read_first>
    - packages/api-client/src/router/genui/generate.ts (importer_id forward line 137)
    - packages/api-client/src/router/genui/code-island.ts (importer_id forward line 83)
    - packages/api-client/src/router/genui/history.ts (historyList importer_id param line 163; historyById)
    - packages/api-client/src/router/genui/__tests__/history.test.ts (Test 4 forwards importerId — update to user-scoped contract)
  </read_first>
  <action>
    Swap all four genui procedures to protectedProcedure. generate + codeIslandGenerate: auth-gate only — do NOT scope the generation cache (cross-tenant exact-match reuse is intended per CONTEXT/Plan 01 docs). historyList: derive owned importer scope from ctx.user via userOwnedImporterIds and ALWAYS forward an owned importer_id to FastAPI — never call with importer_id omitted (the 999.1 all-importers gap); if the user owns multiple importers, forward the owned set (or make one call per owned importer and merge). historyById: after fetching the event, verify its importer_id is owned by ctx.user (assertImporterOwnership or membership in the owned set) and return NOT_FOUND otherwise; a NULL-importer event is not user-browsable. Update history.test.ts Test 4 to assert the forwarded importer_id is the caller's owned id (not an arbitrary client-passed one) and add a not-owned-history-rejected case.
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run src/router/genui/__tests__/history.test.ts</automated>
    <automated>grep -c "publicProcedure" packages/api-client/src/router/genui/generate.ts packages/api-client/src/router/genui/code-island.ts packages/api-client/src/router/genui/history.ts</automated>
  </verify>
  <acceptance_criteria>
    - Zero publicProcedure remain across the three genui files.
    - historyList never forwards importer_id=omitted; it forwards the caller's owned importer scope (test asserts the forwarded param).
    - historyById returns NOT_FOUND for a non-owned event (test).
    - generate/codeIslandGenerate require auth but apply NO cache scoping (cache stays cross-tenant).
    - history.test.ts passes.
  </acceptance_criteria>
  <done>genui requires auth, its history is user-scoped (999.1 closed), and its generation cache remains deliberately cross-tenant.</done>
</task>

<task type="auto">
  <name>Task 3: Ownership-gate the attachments download route</name>
  <files>apps/web/src/app/api/attachments/[id]/route.ts, apps/web/src/app/api/attachments/__tests__/route.test.ts</files>
  <read_first>
    - apps/web/src/app/api/attachments/[id]/route.ts (current no-scoping implementation)
    - apps/web/src/app/api/knowledge/edges/[edgeId]/promote/route.ts (43-04 getUser() + 401 pattern to mirror)
    - packages/db/src/schema/attachments.ts (email_attachments carries importer_id + email_id)
    - packages/db/src/ownership.ts (assertImporterOwnership signature)
  </read_first>
  <action>
    Rework the GET handler: keep the uuid validation and missing-secret guard. Add server-verified identity via `~/lib/supabase/server` createClient().auth.getUser(); return 401 when user is null (mirror the promote route). Widen the DB select to fetch importerId alongside storageKey from EmailAttachments by id. Call assertImporterOwnership(db, importerId, user.id) from @polytoken/db/ownership; on OwnershipError return 404 (fail-closed — do not distinguish "not found" from "not owned"). Only mint the Supabase signed URL after ownership passes. Author route.test.ts: 401 without a session; 404 for an attachment whose importer belongs to another user; 200 + { url } for the owner. Mock supabase getUser + the db select + storage createSignedUrl (mirror existing apps/web route test patterns).
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/api/attachments/__tests__/route.test.ts</automated>
    <automated>grep -n "getUser\|assertImporterOwnership" apps/web/src/app/api/attachments/[id]/route.ts</automated>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep -v "src/app/dev/design" | grep -c "error TS" || true</automated>
  </verify>
  <acceptance_criteria>
    - The route calls auth.getUser() and returns 401 on null user.
    - The route calls assertImporterOwnership on the attachment's importer and returns 404 on OwnershipError before any signed URL is generated.
    - route.test.ts covers 401 / 404-cross-tenant / 200-owner and passes.
    - No new tsc errors outside the known src/app/dev/design baseline.
  </acceptance_criteria>
  <done>The attachments download route is session-gated and ownership-checked — the acceptance-gate target is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tRPC client -> chat/genui resolver | conversationId / importerId inputs are untrusted; scope derives from ctx.user |
| browser -> attachments route | the attachment id path param is untrusted; a signed URL must never be minted for a non-owned attachment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-07-01 | Elevation of Privilege | chat conversation ops | mitigate | assertConversationOwnership on every id-addressed chat read/write; list filters by user_id |
| T-44-07-02 | Information Disclosure | genui.historyList | mitigate | Always forward owned importer scope; never omit importer_id (999.1) |
| T-44-07-03 | Information Disclosure | attachments download | mitigate | getUser() 401 + assertImporterOwnership 404 before signed URL (IDOR closed) |
| T-44-07-04 | Spoofing | sessionless chat/genui/attachment | mitigate | protectedProcedure / getUser() null-guard |
| T-44-07-05 | Information Disclosure | genui generation cache | accept | Cross-tenant exact-match cache reuse is intended and documented (Plan 01, SC5) — auth-gated but not ownership-scoped |
| T-44-07-SC | Tampering | no package installs | accept | No new npm packages |
</threat_model>

<verification>
- chat-user-scoping.test.ts, conversations.test.ts, genui history.test.ts, attachments route.test.ts all green.
- `grep -c publicProcedure` across chat + genui swept files returns 0.
- `npx tsc --noEmit` clean in packages/api-client and apps/web (outside the dev/design baseline).
</verification>

<success_criteria>
- No chat/genui procedure and no web route accepts a client-supplied importer/user id for scoping; the attachments download route and genui history are user-scoped (ROADMAP SC2/SC3, backlog 999.1 absorbed).
</success_criteria>

<output>
Create `.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-07-SUMMARY.md` when done.
</output>
