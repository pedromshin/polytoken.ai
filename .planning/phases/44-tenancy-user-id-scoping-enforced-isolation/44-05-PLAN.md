---
phase: 44-tenancy-user-id-scoping-enforced-isolation
plan: 05
type: execute
wave: 3
depends_on: ["44-02"]
files_modified:
  - packages/api-client/src/router/emails/index.ts
  - packages/api-client/src/router/emails/detail.ts
  - packages/api-client/src/router/emails/entity-summary.ts
  - packages/api-client/src/router/emails/mutations.ts
  - packages/api-client/src/router/emails/__tests__/emails-user-scoping.test.ts
  - packages/api-client/src/router/__tests__/mutations.test.ts
autonomous: true
requirements: [TENA-03]

must_haves:
  truths:
    - "Every emails-router procedure is protectedProcedure — a sessionless call is UNAUTHORIZED"
    - "emails.list returns only emails under importers owned by ctx.user (client importerId is validated against ownership, never trusted for scoping)"
    - "emails.byId / emails.detail / emails.entitySummary reject an email owned by another user"
    - "Every component mutation asserts the target component/email belongs to ctx.user before writing"
  artifacts:
    - path: "packages/api-client/src/router/emails/mutations.ts"
      provides: "ownership-guarded component mutations"
      contains: "assertComponentOwnership"
    - path: "packages/api-client/src/router/emails/__tests__/emails-user-scoping.test.ts"
      provides: "per-procedure cross-tenant regression tests for the emails router"
      min_lines: 40
  key_links:
    - from: "packages/api-client/src/router/emails/index.ts"
      to: "userOwnedImporterIds"
      via: "@polytoken/db/ownership"
      pattern: "userOwnedImporterIds|assertEmailOwnership"
---

<objective>
Sweep the emails tRPC router (the largest cluster: reads + ~18 component mutations) onto `protectedProcedure` and the central ownership helper. Reads scope to the caller's owned importers; every id-addressed read and mutation asserts ownership through `@polytoken/db/ownership` (no ad-hoc SQL). Client-supplied importerId is accepted but validated against ownership — never trusted for scoping.

Purpose: emails + email_components are the highest-volume user-owned surface; this is the bulk of the sweep (TENA-03).
Output: guarded emails router + per-procedure regression tests + updated pre-existing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-CONTEXT.md
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-02-SUMMARY.md

<interfaces>
<!-- The uniform sweep recipe + the exact procedures to transform. Extracted — no exploration. -->

trpc.ts (Phase 43): `protectedProcedure = t.procedure.use(...)` throws TRPCError UNAUTHORIZED when ctx.user is null, else narrows ctx.user to non-null. ctx = { headers, db, user }. Import protectedProcedure from "../../trpc".

Ownership helper (Plan 02, @polytoken/db/ownership): userOwnedImporterIds(db, userId) → string[]; assertEmailOwnership(db, emailId, userId) throws OwnershipError; assertComponentOwnership(db, componentId, userId) throws OwnershipError. Map OwnershipError → TRPCError({ code: "NOT_FOUND" }) (fail-closed, no existence oracle) at each call site or via a tiny local wrapper.

SWEEP RECIPE (apply uniformly):
1. Swap `publicProcedure` → `protectedProcedure` (import + usage).
2. Reads with an importerId filter (emails.list): resolve `const owned = await userOwnedImporterIds(ctx.db, ctx.user.id)`; filter with `inArray(Emails.importerId, owned)`; if input.importerId is provided, only honor it when `owned.includes(input.importerId)` (else NOT_FOUND / empty). Never return rows outside `owned`.
3. Reads/mutations addressed by id: call the matching assert (assertEmailOwnership for email ids, assertComponentOwnership for component ids) at the TOP, before any read/write; catch OwnershipError → throw TRPCError NOT_FOUND.

Emails router procedures to transform:
- emails/index.ts — list (importerId filter, line 31), byId (email id, line 80).
- emails/detail.ts — detail (email id, line 41). Remove the stale "D-18: no importerId filter — tenancy not enforced" comment.
- emails/entity-summary.ts — entitySummary (line 163; read the file to see whether it keys on emailId or importerId and scope accordingly).
- emails/mutations.ts — accept, reject, redraw, split, merge, nest, createRegion, classifyDocument, autofillComponent, confirmComponent, reprocessEmail, setRole, setEntityType, setFieldRelationship, autofillFields, denyField, confirmField (each keyed on a componentId or emailId → assertComponentOwnership/assertEmailOwnership).

Pre-existing tests that construct callers with `user: null` (added as a 43-03 deviation fix) will now fail against protectedProcedure — update them to pass a valid user + seed ownership in the fake db.
Test cmd: `cd packages/api-client && npx vitest run <file>` ; typecheck: `npx tsc --noEmit`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard the emails reads (list, byId, detail, entitySummary)</name>
  <files>packages/api-client/src/router/emails/index.ts, packages/api-client/src/router/emails/detail.ts, packages/api-client/src/router/emails/entity-summary.ts, packages/api-client/src/router/emails/__tests__/emails-user-scoping.test.ts</files>
  <read_first>
    - packages/api-client/src/router/emails/index.ts (list + byId, the importerId filter at line 61)
    - packages/api-client/src/router/emails/detail.ts (the D-18 no-scoping comment to replace)
    - packages/api-client/src/router/emails/entity-summary.ts (determine emailId vs importerId keying)
    - packages/api-client/src/trpc.ts (protectedProcedure + ctx shape)
    - packages/api-client/src/router/entities/gallery.ts (existing owned-scoping read idiom to mirror)
  </read_first>
  <action>
    Apply the sweep recipe to emails.list (scope via userOwnedImporterIds + inArray; validate any provided importerId against owned), emails.byId and emails.detail (assertEmailOwnership on input.id → TRPCError NOT_FOUND on OwnershipError), and emails.entitySummary (scope by its key — assertEmailOwnership if emailId-keyed, owned-importer scope if importerId-keyed). Replace the stale D-18 comment in detail.ts with the new user-ownership contract. Author emails-user-scoping.test.ts covering: sessionless call → UNAUTHORIZED; user A cannot read user B's email via byId/detail; list returns only A's rows even when B's importerId is passed.
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run src/router/emails/__tests__/emails-user-scoping.test.ts</automated>
    <automated>grep -c "protectedProcedure" packages/api-client/src/router/emails/index.ts packages/api-client/src/router/emails/detail.ts packages/api-client/src/router/emails/entity-summary.ts</automated>
  </verify>
  <acceptance_criteria>
    - No `publicProcedure` remains in emails/index.ts, detail.ts, entity-summary.ts (grep shows 0).
    - emails.list never returns rows outside userOwnedImporterIds(ctx.db, ctx.user.id) — proven by a two-user isolation test.
    - byId/detail/entitySummary throw NOT_FOUND for a non-owned target — proven by test.
    - emails-user-scoping.test.ts passes.
  </acceptance_criteria>
  <done>All emails read procedures require auth and are user-scoped through the central helper, with passing regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Guard the emails component mutations (mutations.ts, ~18 procedures)</name>
  <files>packages/api-client/src/router/emails/mutations.ts, packages/api-client/src/router/__tests__/mutations.test.ts, packages/api-client/src/router/emails/__tests__/emails-user-scoping.test.ts</files>
  <read_first>
    - packages/api-client/src/router/emails/mutations.ts (all ~18 mutations; identify each one's id key — componentId vs emailId)
    - packages/api-client/src/router/__tests__/mutations.test.ts (pre-existing tests using user:null to update)
    - packages/api-client/src/router/emails/__tests__/emails-user-scoping.test.ts (extend from Task 1)
  </read_first>
  <action>
    Swap every mutation in mutations.ts to protectedProcedure and add the matching ownership assert at the top of each resolver: assertComponentOwnership for componentId-keyed mutations (accept, reject, redraw, setRole, setEntityType, setFieldRelationship, autofillComponent, confirmComponent, autofillFields, denyField, confirmField, and the split/merge/nest/createRegion operations per their id inputs), assertEmailOwnership for emailId-keyed ones (reprocessEmail, classifyDocument — confirm each key by reading the input schema). For multi-id operations (merge, nest) assert ownership of EVERY referenced id so a mutation cannot splice a component the caller does not own into one they do. Map OwnershipError → TRPCError NOT_FOUND. Update the pre-existing mutations.test.ts callers to pass a valid ctx.user and seed ownership. Extend emails-user-scoping.test.ts with a write-side matrix: user A cannot mutate user B's component/email (each representative mutation rejected).
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run src/router/emails/__tests__/emails-user-scoping.test.ts src/router/__tests__/mutations.test.ts</automated>
    <automated>grep -c "publicProcedure" packages/api-client/src/router/emails/mutations.ts</automated>
    <automated>cd packages/api-client && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "publicProcedure" packages/api-client/src/router/emails/mutations.ts` returns 0.
    - Every mutation calls assertComponentOwnership or assertEmailOwnership (or asserts every referenced id for multi-id ops) before its write.
    - A cross-tenant write attempt is rejected for at least one representative componentId-keyed and one emailId-keyed mutation (tests).
    - mutations.test.ts (updated) and emails-user-scoping.test.ts pass; `npx tsc --noEmit` clean (no new errors outside the known dev/design baseline).
  </acceptance_criteria>
  <done>Every emails component mutation is ownership-guarded; pre-existing tests updated; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tRPC client → resolver | input importer/email/component ids are untrusted; scope derives from server-verified ctx.user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05-01 | Information Disclosure | emails.list importerId | mitigate | Scope to userOwnedImporterIds; provided importerId validated against owned set |
| T-44-05-02 | Elevation of Privilege | byId/detail/entitySummary | mitigate | assertEmailOwnership → NOT_FOUND fail-closed |
| T-44-05-03 | Tampering | component mutations | mitigate | assertComponentOwnership/assertEmailOwnership before every write; multi-id ops assert all ids |
| T-44-05-04 | Spoofing | sessionless call | mitigate | protectedProcedure → UNAUTHORIZED |
| T-44-05-SC | Tampering | no package installs | accept | No new npm packages |
</threat_model>

<verification>
- emails-user-scoping.test.ts + mutations.test.ts green.
- `grep -c publicProcedure` across emails router files returns 0.
- `npx tsc --noEmit` in packages/api-client clean.
</verification>

<success_criteria>
- No emails route/procedure accepts a client-supplied importer id for scoping; cross-tenant reads AND writes rejected (ROADMAP SC2/SC3, emails surface).
</success_criteria>

<output>
Create `.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-05-SUMMARY.md` when done.
</output>
