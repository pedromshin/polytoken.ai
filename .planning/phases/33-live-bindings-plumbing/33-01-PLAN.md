---
phase: 33-live-bindings-plumbing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/chat/_canvas/use-data-bindings.ts
  - apps/web/src/app/chat/_canvas/__tests__/use-data-bindings.test.tsx
autonomous: true
requirements: [BIND-01, BIND-02]

must_haves:
  truths:
    - "A record of spec.bindings resolves to live query data via a compile-time switch over exactly the 5 allowlisted procedures, never dynamic string dispatch"
    - "The 3 by-id procedures (entities.byId, emails.detail, knowledge.byId) source their id ONLY from panelData (render context) and skip the query entirely when that id is absent — never from binding.params"
    - "entities.list and knowledge.graph accept model-authored non-ID params from binding.params, safeParse-revalidated, with knowledge.graph's importerId always sourced from panelData/DEFAULT_IMPORTER_ID, never from binding.params"
    - "Each procedure's query uses its own staleTime tier (10s knowledge.*, 30s entities.*, 60s emails.detail) — no bespoke polling/setInterval anywhere"
    - "A spec that is still streaming with no parsed bindings yet, or whose bindings fail to parse/validate, degrades to an empty result — never throws"
  artifacts:
    - path: "apps/web/src/app/chat/_canvas/use-data-bindings.ts"
      provides: "useDataBindings hook + STALE_TIME_MS lookup + bindings extraction/parse logic"
      exports: ["useDataBindings", "STALE_TIME_MS"]
    - path: "apps/web/src/app/chat/_canvas/__tests__/use-data-bindings.test.tsx"
      provides: "coverage of the 5-procedure switch, params-from-context convention, staleTime tiers, and degrade-to-empty paths"
  key_links:
    - from: "apps/web/src/app/chat/_canvas/use-data-bindings.ts"
      to: "apps/web/src/app/chat/_components/genui-part-boundary.tsx"
      via: "import { attemptRepairJson } from \"../_components/genui-part-boundary\""
      pattern: "attemptRepairJson"
    - from: "apps/web/src/app/chat/_canvas/use-data-bindings.ts"
      to: "packages/genui/src/schema (DataBindingSchema, AllowedProcedure)"
      via: "import from \"@nauta/genui/schema\""
      pattern: "DataBindingSchema|AllowedProcedure"
    - from: "apps/web/src/app/chat/_canvas/use-data-bindings.ts"
      to: "~/trpc/react (api.useQueries)"
      via: "api.useQueries((t) => [...])"
      pattern: "api\\.useQueries"
---

<objective>
Build the standalone `use-data-bindings.ts` hook: the new seam that resolves a genui spec's
`bindings` record into live tRPC query data via a compile-time switch over the 5 already-allowlisted
procedures, with render-context-only params for the 3 by-id procedures and per-procedure staleTime
tiers. This plan produces the hook and its tests in isolation — it is not yet wired into
`genui-panel-node.tsx` (that happens in 33-02, which depends on this plan's exports).

Purpose: BIND-01 (live data via compile-time allowlist switch, never model-authored IDs) and the
staleTime half of BIND-02 (per-procedure freshness tiers, no bespoke polling).
Output: `use-data-bindings.ts` (new, exported `useDataBindings` + `STALE_TIME_MS`) and its test file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-live-bindings-plumbing/33-CONTEXT.md
@.planning/research/v1.6-chat-knowledge/SYNTHESIS.md
</context>

<interfaces>
<!-- Extracted from the codebase — executor should use these directly, no exploration needed. -->

From `packages/genui/src/generation/allowed-procedures.ts`:
```
export const ALLOWED_PROCEDURES = ["emails.list","emails.byId","emails.detail","entities.list","entities.byId","entityTypes.list","knowledge.graph","knowledge.list","knowledge.byId"] as const;
export type AllowedProcedure = (typeof ALLOWED_PROCEDURES)[number];
```
Only 5 of these 9 are wired this phase: `entities.byId`, `entities.list`, `emails.detail`,
`knowledge.byId`, `knowledge.graph`. Do NOT add cases for the other 4 — the switch's `default` arm
must be `never`-typed so a 6th wired case is a compile error until deliberately added.

From `packages/genui/src/schema/data-binding-schema.ts`:
```
export const DataBindingSchema = z.object({
  procedure: AllowedProcedureSchema,
  params: z.record(z.string(), z.union([z.string(), z.number(), z.boolean()])).optional(),
}).strict().refine(noUuidValues, {...});
export type DataBinding = z.infer<typeof DataBindingSchema>;
```
Both re-exported from `@nauta/genui/schema`. `params` already rejects UUID-shaped string values at
parse time (GR-15) — this is defense-in-depth, not the primary control (the primary control is: by-id
procedures never read `binding.params` for their id at all).

From `packages/genui/src/schema/spec-schema.ts:583`:
```
bindings: z.record(z.string(), DataBindingSchema).optional(),
```
This is the `SpecRoot.bindings` field — a named record, e.g. `{ "myEntity": { procedure: "entities.byId", params: {} } }`.

From `apps/web/src/app/chat/_components/genui-part-boundary.tsx:139`:
```
export function attemptRepairJson(raw: string): string | null
```
Best-effort completion of a truncated JSON prefix (adds missing closing brackets/braces). Import it;
never duplicate or modify `genui-part-boundary.tsx` itself (locked file, SC2).

From `packages/api-client/src/router/entities/gallery.ts:32` (`entities.list` input):
```
export const listInputSchema = z.object({
  importerId: z.string().uuid().optional(),
  entityTypeId: z.string().uuid().optional(),
  status: z.enum(["confirmed","all","candidate","has-pending-duplicates"]).default("confirmed"),
  search: z.string().max(200).optional(),
  sort: z.enum(["last_seen","name","occurrences"]).default("last_seen"),
  limit: z.number().int().min(1).max(100).default(25),
  offset: z.number().int().min(0).default(0),
});
```

From `packages/api-client/src/router/knowledge/graph.ts:64` (`knowledge.graph` input):
```
export const graphInputSchema = z.object({
  importerId: z.string().uuid().optional(),
  includeInstances: z.boolean().default(false),
  includeEmails: z.boolean().default(false),
  nodeTypes: z.array(z.enum(NODE_TYPES)).optional(),
});
```
`importerId` is UUID-shaped, so `DataBindingSchema`'s `.refine()` already rejects it if model-authored
— it MUST come from `panelData.importerId ?? DEFAULT_IMPORTER_ID` (mirror the local const at
`apps/web/src/app/knowledge/_components/knowledge-graph.tsx:130`:
`"00000000-0000-0000-0000-000000000001"` — duplicate it locally in `use-data-bindings.ts`, same
precedent as that file's own comment "mirrors DEFAULT_IMPORTER_ID").

From `apps/web/src/trpc/react.tsx`:
```
export const api = createTRPCReact<AppRouter>();
```
`api.useQueries` is the trpc-react-query v11 proxy (`@trpc/react-query@11.8.0`) wrapping TanStack's
`useQueries` — confirmed present at `node_modules/@trpc/react-query/dist/shared-*.mjs` (`useQueries$1`
+ `createUseQueries`). Signature: `api.useQueries((t) => Query[])` where each `Query` comes from
`t.<router>.<procedure>.queryOptions(input, { enabled, staleTime })`. THIS is the mechanism to use —
a single hook call whose internal query count can vary per render without violating rules-of-hooks
(unlike calling `useQuery` per-binding-entry in a loop, which WOULD violate them since the entry count
changes as a spec streams in). Returns an array of `UseQueryResult`-shaped objects in the same order
as the callback's returned array.

From `apps/web/src/trpc/query-client.ts:12`: app-wide default `staleTime: 30 * 1000` — the baseline
the per-procedure STALE_TIME_MS table deviates from (do not touch this file).

Existing mock convention for `~/trpc/react` in tests
(`apps/web/src/app/chat/_canvas/__tests__/use-canvas-persistence-edges-stable.test.tsx:26-38`):
```
vi.mock("~/trpc/react", () => ({ api: { <router>: { <procedure>: { useQuery: () => ({...}) } } } }));
```
For this hook's tests, mock `api.useQueries` directly as a plain function.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for use-data-bindings</name>
  <files>apps/web/src/app/chat/_canvas/__tests__/use-data-bindings.test.tsx</files>
  <read_first>
    apps/web/src/app/chat/_canvas/__tests__/use-canvas-persistence-edges-stable.test.tsx
    apps/web/src/app/chat/_canvas/__tests__/panel-data-flow.test.tsx
    packages/genui/src/schema/data-binding-schema.ts
    packages/genui/src/schema/spec-schema.ts
    .planning/phases/33-live-bindings-plumbing/33-CONTEXT.md
  </read_first>
  <behavior>
    - Test: a spec whose parsed `bindings` contains a `knowledge.byId` entry AND
      `panelData.selectedNodeId` set resolves that binding via `api.useQueries` with an `enabled:
      true` query for `knowledge.byId` with `{id: selectedNodeId}` and `staleTime: 10_000`.
    - Test: the same `knowledge.byId` binding with `panelData.selectedNodeId` absent/undefined never
      fires a query (the query passed to `useQueries` has `enabled: false`, or the binding is
      excluded from the queries array entirely — either is acceptable, assert whichever the
      implementation does) and the returned data for that binding key is `undefined`.
    - Test: same params-from-context skip behavior for `entities.byId`
      (`panelData.selectedEntityId`) and `emails.detail` (`panelData.selectedEmailId`).
    - Test: an `entities.list` binding with model-authored `binding.params = {search: "acme",
      limit: 10}` passes those params through to the resolved query input (safeParse-revalidated
      against a narrow schema, or passed as-is — assert the query fires with `search`/`limit`
      present).
    - Test: a `knowledge.graph` binding's resolved query input NEVER takes `importerId` from
      `binding.params` (assert the DEFAULT_IMPORTER_ID or `panelData.importerId` value is used
      regardless of any `importerId`-shaped value present on `binding.params`, since
      `DataBindingSchema` itself already blocks UUID literals — this test proves the render-context
      sourcing, not just schema rejection).
    - Test: staleTime tiers — assert `STALE_TIME_MS["knowledge.byId"] === 10_000`,
      `STALE_TIME_MS["knowledge.graph"] === 10_000`, `STALE_TIME_MS["entities.byId"] === 30_000`,
      `STALE_TIME_MS["entities.list"] === 30_000`, `STALE_TIME_MS["emails.detail"] === 60_000`.
    - Test: `isStreaming: true` with a `specJson` whose `bindings` field hasn't appeared yet in the
      truncated buffer → hook returns `{}` (no queries fired, `api.useQueries` called with an empty
      array or not called at all).
    - Test: malformed `specJson` (invalid JSON, `attemptRepairJson` also returns null) → hook returns
      `{}`, never throws.
    - Test: `bindings` present but fails `z.record(z.string(), DataBindingSchema).optional().safeParse`
      (e.g. an unknown procedure string) → hook returns `{}` for that binding, never throws.
    - Test: merged result — with two bindings (`"a": knowledge.byId`, `"b": entities.list`) both
      resolving successfully, the hook's returned `Record<string, unknown>` has keys `"a"` and `"b"`
      mapped to their respective query `data` (or `undefined` while loading — assert the `isLoading`
      case surfaces as `undefined`, per CONTEXT's "loading value inside the merged data" decision).
  </behavior>
  <action>
    Create the test file mocking `~/trpc/react`'s `api.useQueries` as a `vi.fn()` that returns an
    array of stub `UseQueryResult`-shaped objects (`{data, isLoading, isError}`) matching whatever the
    callback constructed, and assert on the callback's constructed queries (capture the callback
    argument passed to `api.useQueries` and invoke it against a lightweight fake `t` proxy object,
    OR — simpler — mock `api.useQueries` to just record its callback's return value directly by
    calling it with a fake proxy whose `<router>.<procedure>.queryOptions(input, opts)` returns
    `{ queryKey: [...], ...opts, __input: input }`, then assert on the array contents). Use React
    Testing Library's `renderHook` (already a project devDependency per Vitest+RTL conventions used by
    `panel-data-flow.test.tsx`) or a minimal manual test harness matching this repo's existing
    `use-canvas-persistence-edges-stable.test.tsx` pattern (manual `createRoot`/`act`). Run the suite
    and confirm every new test FAILS (module `use-data-bindings.ts` does not exist yet) before Task 2.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_canvas/__tests__/use-data-bindings.test.tsx</automated>
  </verify>
  <done>Test file exists, imports a not-yet-created `use-data-bindings.ts`, and the run fails with a module-not-found or all-assertions-red result (RED confirmed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement use-data-bindings.ts</name>
  <files>apps/web/src/app/chat/_canvas/use-data-bindings.ts</files>
  <read_first>
    apps/web/src/app/chat/_canvas/__tests__/use-data-bindings.test.tsx
    apps/web/src/app/chat/_components/genui-part-boundary.tsx
    packages/genui/src/schema/data-binding-schema.ts
    packages/genui/src/generation/allowed-procedures.ts
    apps/web/src/app/knowledge/_components/knowledge-graph.tsx
  </read_first>
  <action>
    Create `apps/web/src/app/chat/_canvas/use-data-bindings.ts` (named exports only, explicit types on
    every export, `.strict()`-style narrow validation per CLAUDE.md). Structure:
    1. `extractBindings(specJson: string): Record<string, DataBinding>` — pure helper: `JSON.parse`
       in try/catch; on failure, retry with `attemptRepairJson(specJson)` imported from
       `../_components/genui-part-boundary` (import only, never modify that file); pull the
       top-level `bindings` key from the parsed object (if present) and run
       `z.record(z.string(), DataBindingSchema).optional().safeParse(candidate)`; any failure at any
       step returns `{}`.
    2. `const STALE_TIME_MS: Record<AllowedProcedure, number>` — but since only 5 of the 9
       `AllowedProcedure` values are wired, type it as
       `Partial<Record<AllowedProcedure, number>>` or a narrower
       `Record<WiredProcedure, number>` where `type WiredProcedure = "entities.byId" |
       "entities.list" | "emails.detail" | "knowledge.byId" | "knowledge.graph"` (prefer the
       narrower type — it is what the exhaustive switch's `never` check needs). Values: 10_000 for
       both `knowledge.*`, 30_000 for both `entities.*`, 60_000 for `emails.detail`.
    3. `const DEFAULT_IMPORTER_ID = "00000000-0000-0000-0000-000000000001"` (local duplicate, mirrors
       `knowledge-graph.tsx:130`'s own precedent comment).
    4. `export interface UseDataBindingsArgs { readonly specJson: string; readonly isStreaming:
       boolean; readonly panelData: Record<string, unknown>; }`
    5. `export function useDataBindings(args: UseDataBindingsArgs): Record<string, unknown>` — the
       hook. Body: call `extractBindings(args.specJson)` (plain function call, not itself a hook — safe
       to call conditionally/every render). Build the queries array via
       `api.useQueries((t) => Object.entries(bindings).map(([key, binding]) => { switch
       (binding.procedure) { case "entities.byId": ...; case "entities.list": ...; case
       "emails.detail": ...; case "knowledge.byId": ...; case "knowledge.graph": ...; default: {
       const _exhaustive: never = binding.procedure; return null; } } }).filter(isNotNull))` — each
       case constructs `t.<router>.<procedure>.queryOptions(input, { enabled, staleTime:
       STALE_TIME_MS[procedure] })`. Only wire the 5 named cases; the `default` arm's `never`
       assignment is the compile-time guard against silently handling a 6th allowlisted procedure
       later. Since 4 of the 9 `AllowedProcedure` values (`emails.list`, `emails.byId`,
       `entityTypes.list`, `knowledge.list`) are NOT in the switch, TypeScript's exhaustiveness check
       on `binding.procedure: AllowedProcedure` will not be literally exhaustive over all 9 — narrow
       the switch's discriminant type first (e.g. `if` guard mapping only the 5 wired strings before
       the switch, or a `switch` with an explicit `default` that handles "not one of the 5 wired" by
       returning `null` — do NOT force a `never` cast that fights the wider `AllowedProcedure` type;
       use a runtime `default: return null;` combined with a comment citing this as the deliberate,
       reviewed boundary, consistent with "ALLOWED_PROCEDURES is NOT expanded").
       Per-procedure param sourcing (CONTEXT's locked convention table):
       - `entities.byId`: `id = args.panelData.selectedEntityId as string | undefined`; if undefined,
         `enabled: false`, still include the query entry with a placeholder input (or omit the entry
         — either is fine, tests assert on effective behavior not exact array shape) so the returned
         record has a defined key mapped to `undefined`.
       - `emails.detail`: `id = args.panelData.selectedEmailId as string | undefined`, same pattern.
       - `knowledge.byId`: `id = args.panelData.selectedNodeId as string | undefined`, same pattern.
       - `entities.list`: input = `binding.params` passed through (optionally re-validated with a
         narrow `.pick`/partial safeParse against `listInputSchema`-shaped fields per Claude's
         Discretion — prefer safeParse per D-15 posture) — `importerId` sourced from
         `args.panelData.importerId ?? DEFAULT_IMPORTER_ID`, NEVER from `binding.params` even if
         present there (strip it before merging params).
       - `knowledge.graph`: same pass-through for `includeInstances`/`includeEmails`/`nodeTypes`,
         `importerId` ALWAYS `args.panelData.importerId ?? DEFAULT_IMPORTER_ID`.
    6. Reduce the `useQueries` result array back into `Record<string, unknown>` keyed by the original
       binding names (zip the `Object.keys(bindings)` order with the results array — build both in
       the SAME `Object.entries` pass so ordering is guaranteed stable), mapping each entry to
       `result.data` (which is naturally `undefined` while loading or erroring — satisfies CONTEXT's
       "loading value inside the merged data" decision with zero extra chrome).
    7. Early-return `{}` before calling `api.useQueries` when `Object.keys(bindings).length === 0`
       (covers both the streaming-not-parsed-yet and the parse-failure cases uniformly) — BUT
       `api.useQueries` must still be called unconditionally (even with an empty array) to satisfy
       rules-of-hooks if any other hook is added later; for now returning early before any hook call
       is safe since `useDataBindings` calls exactly one hook (`api.useQueries`) total, so an early
       return before that single call cannot cause an order mismatch across renders in isolation —
       however to be defensive AND future-proof, always call `api.useQueries(() => queries)` (which
       may be `[]`) rather than skipping the call, and only shortcut the RETURN VALUE construction.
    Run tests until GREEN (Task 1's suite).
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_canvas/__tests__/use-data-bindings.test.tsx</automated>
  </verify>
  <done>All tests from Task 1 pass; `npx tsc --noEmit -p apps/web` (or the workspace's typecheck script) is clean for the new file; no `console.log`; named exports only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| LLM-generated spec.bindings → resolved query params | `binding.params` is model-authored/untrusted; must never source an ID that scopes data access |
| Streaming/truncated specJson → JSON.parse | Partial JSON text mid-stream is attacker-adjacent input shape (not attacker-controlled content, but parser-hostile) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-33-01 | Tampering | `use-data-bindings.ts` by-id param sourcing | mitigate | The 3 by-id procedures NEVER read `binding.params` for their id — id is always `panelData.selected*Id`, structurally impossible for model output to select an arbitrary id (BIND-01's core control) |
| T-33-02 | Information Disclosure | `entities.list`/`knowledge.graph` param pass-through | mitigate | `importerId` is always sourced from `panelData.importerId ?? DEFAULT_IMPORTER_ID`, stripped from any `binding.params` value before use, even though `DataBindingSchema`'s UUID-refine already blocks it at parse time (defense-in-depth, two independent layers) |
| T-33-03 | Tampering | Truncated/malformed `specJson` during streaming | mitigate | `extractBindings` try/catch + `attemptRepairJson` fallback + `safeParse`; any failure degrades to `{}`, never throws, never partially-trusts a malformed record |
| T-33-04 | Denial of Service | Unbounded binding count in a single spec | accept | Bounded in practice by the generator's own token/spec-size budget (existing upstream control, out of scope to re-litigate this phase); no new cap introduced here |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (uses existing `@trpc/react-query`, `zod`) |
</threat_model>

<verification>
- `cd apps/web && npx vitest run src/app/chat/_canvas/__tests__/use-data-bindings.test.tsx` — all green.
- `npx tsc --noEmit -p apps/web` (or `npm run typecheck -w apps/web` if defined) clean.
- `grep -n "setInterval\|setTimeout.*poll" apps/web/src/app/chat/_canvas/use-data-bindings.ts` returns nothing (no bespoke polling).
</verification>

<success_criteria>
`use-data-bindings.ts` exports a working `useDataBindings` hook that: resolves exactly the 5 wired
procedures via a compile-time switch, sources by-id params only from render context, applies
per-procedure staleTime tiers, and degrades to `{}` on any parse/streaming edge case — all proven by
a green test suite, with zero edits to any of the 3 locked renderer files (none of which this plan's
`files_modified` list touches).
</success_criteria>

<output>
Create `.planning/phases/33-live-bindings-plumbing/33-01-SUMMARY.md` when done
</output>
