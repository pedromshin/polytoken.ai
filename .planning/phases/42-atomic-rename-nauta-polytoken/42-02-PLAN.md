---
phase: 42-atomic-rename-nauta-polytoken
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/42-atomic-rename-nauta-polytoken/EXTERNAL-RENAME-RUNBOOK.md
autonomous: true
requirements: [RENM-02]

must_haves:
  truths:
    - "An external-rename runbook doc exists covering GitHub repo rename, AWS/Terraform, Vercel project, and domain purchase/DNS"
    - "The runbook documents the ECR force_delete=false destroy/recreate hazard, the local-only tfstate hazard, and the two-source-of-truth (Terraform var.project vs GitHub Actions env) reconciliation — all verbatim"
    - "The runbook lists terraform plan (read-only) as the proof step and explicitly forbids terraform apply within this phase"
    - "Live AWS/Terraform/Vercel resource-name strings in the repo are unchanged by this plan (documentation only)"
  artifacts:
    - path: ".planning/phases/42-atomic-rename-nauta-polytoken/EXTERNAL-RENAME-RUNBOOK.md"
      provides: "user runbook for external renames (RENM-02) — documented, NOT executed"
      min_lines: 60
      contains: "force_delete"
  key_links:
    - from: "EXTERNAL-RENAME-RUNBOOK.md"
      to: "infrastructure/aws/variables.tf + .github/workflows/deploy-email-listener.yml"
      via: "documents the exact live resource names that must be reconciled in one PR"
      pattern: "nauta-services"
---

<objective>
Produce the external-rename runbook: a user-executed (NOT auto-executed) document covering the GitHub repo rename, AWS/Terraform resource renames (with the ECR `force_delete`/local-tfstate/two-source-of-truth hazards spelled out verbatim), the Vercel project rename, and the domain purchase/DNS — with `terraform plan` (read-only) named as the human-reviewed proof step and `terraform apply` explicitly out of scope for this phase.

Purpose: Satisfies RENM-02. External renames are outward-facing and need the user (domain purchase, DNS, Terraform apply against live infra) — a naive Terraform rename would fail on ECR's immutable name (`force_delete=false`) or risk destroy, and the tfstate is local-only. This plan documents the safe procedure; it changes ZERO live resource-name strings.
Output: `.planning/phases/42-atomic-rename-nauta-polytoken/EXTERNAL-RENAME-RUNBOOK.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-atomic-rename-nauta-polytoken/42-CONTEXT.md
@.planning/phases/42-atomic-rename-nauta-polytoken/42-RESEARCH.md

This plan is DOCUMENTATION ONLY. It touches no code, no `.tf` file, no workflow YAML, no live resource-name
string. All source material is already in 42-RESEARCH.md § "RUNBOOK-only — live external resources" (the exact
file:line table of every live resource name) and § "Common Pitfalls" #1/#2. This plan runs in Wave 1 parallel
with Plan 42-01 (no file overlap — it creates one new doc under `.planning/`).

<operator_hard_constraints>
- `terraform plan` is the read-only proof step; NEVER document `terraform apply` as part of THIS phase (it is the user's later, deliberate action, gated on their own review).
- Do not edit any live resource-name string in the repo (`infrastructure/**`, `.github/workflows/*.yml`, `README.md` deploy-target table cells, `SES_S3_BUCKET`). This plan only DESCRIBES the renames the user will perform later.
- Stage only the explicit new doc path when committing — never `git add -A`/`git add .`.
</operator_hard_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the external-rename runbook</name>
  <files>.planning/phases/42-atomic-rename-nauta-polytoken/EXTERNAL-RENAME-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/42-atomic-rename-nauta-polytoken/42-RESEARCH.md (§ RUNBOOK-only live-resource table with exact file:line for every AWS/Terraform/Vercel name; § Common Pitfalls #1 two-source-of-truth + #2 lockfile; the § "Runbook must document, verbatim" paragraph)
    - infrastructure/aws/variables.tf (the `var.project` default `nauta-services` — read to cite the exact value/line)
    - .github/workflows/deploy-email-listener.yml (the `ECR_REPOSITORY`/`ECS_CLUSTER`/`ECS_SERVICE` env block — the second, unsynced source of truth)
  </read_first>
  <action>
    Create `.planning/phases/42-atomic-rename-nauta-polytoken/EXTERNAL-RENAME-RUNBOOK.md` — a user-facing, step-by-step runbook for the four external renames this phase deliberately does NOT execute. Structure it as ordered sections, each with concrete steps and the exact live resource names from 42-RESEARCH.md:

    1. GitHub repo rename — steps to rename the repository and update the local remote (`git remote set-url`), noting GitHub auto-redirects old URLs.
    2. AWS / Terraform resources — the load-bearing section. List every live name and where it lives: Terraform `var.project` default `"nauta-services"` (`infrastructure/aws/variables.tf:16`, flows into `locals.service_name`, ECR repo, ECS cluster/service, CloudWatch log group); `tg_prefix` local `"nauta-el"` (`locals.tf:4`, ALB target-group prefix, 32-char cap); the GitHub Actions env blocks `ECR_REPOSITORY`/`ECS_CLUSTER`/`ECS_SERVICE = nauta-services-email-listener` in BOTH `deploy-email-listener.yml:13-15` and `deploy-email-listener-staging.yml:13-15`; the commented S3 backend `nauta-services-terraform-state` (`main.tf:13`); the local `terraform.tfstate` (gitignored, holds live ALB DNS/ECR URL/ECS names/IAM ARN/S3 bucket/3 SNS topic ARNs); the `README.md:56-57` deploy-target table cells; and `SES_S3_BUCKET = "nauta-services-ses-inbound-emails"` (`settings.py:97`). Document, VERBATIM, the three hazards: (a) TWO UNSYNCED SOURCES OF TRUTH — Terraform `var.project` and the workflow YAML env vars are independent strings; change both in the SAME PR or a workflow will deploy to a resource Terraform just renamed away from; (b) ECR `force_delete=false` — a Terraform rename of immutable-name resources is destroy+recreate; `apply` fails loudly on a non-empty ECR repo (safer than silent success); do NOT casually flip `force_delete=true`, and do NOT attempt `terraform state mv` without confirming the AWS provider's actual rename-in-place support; (c) LOCAL-ONLY tfstate — confirm which machine/runner holds current state before any rename-triggered `apply`; never hand-edit `terraform.tfstate`. State that `SES_S3_BUCKET` / GitHub Actions env vars / `terraform.tfstate` all name the SAME live resources from different angles — renaming one without the others breaks deploy or ingestion. Name `terraform plan` (read-only) as the mandatory human-reviewed proof step BEFORE any apply, and state `terraform apply` is out of scope for phase 42.
    3. Vercel project — rename via dashboard/CLI; note `.vercel/project.json` (`"projectName":"nauta-web"`) is a gitignored local link artifact regenerated by `vercel link`, not a committed surface.
    4. Domain purchase / DNS — purchase the polytoken domain, configure DNS, and update Vercel + SES/MX records; flag that this is a user-only step (billing + registrar access).

    Commit as `docs(42): add external-rename runbook (GitHub/AWS/Vercel/domain)`, staging only the new doc path.
  </action>
  <verify>
    <automated>test -f .planning/phases/42-atomic-rename-nauta-polytoken/EXTERNAL-RENAME-RUNBOOK.md && grep -q 'force_delete' .planning/phases/42-atomic-rename-nauta-polytoken/EXTERNAL-RENAME-RUNBOOK.md && grep -qi 'terraform plan' .planning/phases/42-atomic-rename-nauta-polytoken/EXTERNAL-RENAME-RUNBOOK.md && echo RUNBOOK_OK</automated>
  </verify>
  <done>The runbook exists covering all four external renames with the ECR `force_delete`/tfstate/two-source-of-truth hazards documented verbatim, `terraform plan` named as the read-only proof step, and `terraform apply` marked out of scope; no live resource-name string in the repo was changed.</done>
  <acceptance_criteria>
    - `.planning/phases/42-atomic-rename-nauta-polytoken/EXTERNAL-RENAME-RUNBOOK.md` exists and has four ordered sections: GitHub repo, AWS/Terraform, Vercel, domain/DNS.
    - It contains the literal string `force_delete` and describes the destroy+recreate hazard for immutable-name ECR resources.
    - It contains `terraform plan` as the proof step AND explicitly states `terraform apply` is out of scope for phase 42.
    - It names both sources of truth to reconcile in one PR: Terraform `var.project` (`variables.tf`) and the GitHub Actions env vars (`deploy-email-listener.yml` / `-staging.yml`).
    - It documents the local-only `terraform.tfstate` hazard and warns never to hand-edit it.
    - `git status --porcelain infrastructure/ .github/workflows/ README.md` shows NO modifications from this plan (documentation only — zero live-resource strings edited).
    - Commit message is `docs(42): add external-rename runbook (GitHub/AWS/Vercel/domain)`, staged by explicit path only.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook → future user action against live infra | The doc's correctness determines whether the user later destroys/recreates live resources safely |
| runbook content → repo | Risk that authoring accidentally edits a live resource-name string instead of only describing it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06 | Denial of Service (availability) | user executing the external AWS rename later | mitigate | Runbook documents (verbatim) the ECR `force_delete=false` destroy/recreate hazard, the local-only tfstate hazard, the two-source-of-truth reconciliation, and `terraform plan`-not-`apply` — so the user does not destroy live ECR/ECS/ALB/S3/SNS resources |
| T-42-07 | Information Disclosure | runbook content | accept | The runbook lists live resource NAMES (already present in the repo/tfstate) and registrar/dashboard steps, but NO credential values or secrets — low-value, already-known-to-the-owner identifiers |
| T-42-08 | Tampering | live resource-name strings in `infrastructure/**` / workflows / README | mitigate | This plan is documentation-only; acceptance requires `git status` to show zero modifications under `infrastructure/`, `.github/workflows/`, `README.md` — it DESCRIBES renames, never performs them |
| T-42-SC | Tampering | npm/pip/cargo installs | N/A (accept) | Zero package installs this plan (pure documentation); supply-chain gate not applicable |
</threat_model>

<verification>
- The runbook covers all four external renames (GitHub, AWS/Terraform, Vercel, domain) with the exact live resource names from 42-RESEARCH.md.
- The three hazards (two-source-of-truth, ECR `force_delete`, local-only tfstate) are documented verbatim.
- `terraform plan` is the named read-only proof step; `terraform apply` is out of scope for phase 42.
- Zero live resource-name strings changed in the repo (git status clean for `infrastructure/`, workflows, README).
</verification>

<success_criteria>
- RENM-02 satisfied: external renames delivered as a documented user runbook, NOT executed; live AWS resource name strings untouched.
- SC3 (roadmap): the external-rename runbook exists (GitHub repo, AWS/Terraform incl. ECR `force_delete`/tfstate warnings, Vercel, domain), with `terraform plan` named as the proof step (executed as proof in Plan 42-01 Task 3).
</success_criteria>

<output>
Create `.planning/phases/42-atomic-rename-nauta-polytoken/42-02-SUMMARY.md` when done. Record: the runbook's section list, confirmation that all three hazards are documented, and confirmation that zero live resource-name strings were edited (git status clean for infra/workflows/README).
</output>
