---
phase: 42-atomic-rename-nauta-polytoken
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vercel.json
  - packages/api-client/package.json
  - packages/db/package.json
  - packages/genui/package.json
  - packages/tailwind-config/package.json
  - packages/ui/package.json
  - apps/web/package.json
  - apps/web/src/**
  - apps/web/tailwind.config.ts
  - packages/ui/tailwind.config.ts
  - packages/*/src/**
  - packages/genui/src/theme/packs.ts
  - packages/genui/src/theme/__tests__/packs.test.ts
  - apps/email-listener/pyproject.toml
  - apps/email-listener/app/settings.py
  - apps/email-listener/app/infrastructure/llm/genui_style_packs.py
  - apps/email-listener/app/infrastructure/llm/exemplars/__init__.py
  - apps/email-listener/tests/application/test_cache_key.py
  - supabase/config.toml
  - .env.example
  - README.md
  - apps/web/README.md
  - apps/email-listener/README.md
  - .claude/skills/polytoken-design-system/**
  - .planning/phases/42-atomic-rename-nauta-polytoken/rename-nauta-to-polytoken.mjs
autonomous: true
requirements: [RENM-01]

must_haves:
  truths:
    - "Zero @nauta/ references remain in code/config (excluding .planning/ historical docs + the documented dirty-file gaps apps/web/src/app/dev/design/ and graphify-out/)"
    - "All 6 workspace packages are named @polytoken/* and node_modules/@polytoken/* has 6 symlinks; node_modules/@nauta no longer exists"
    - "npm run typecheck passes for @polytoken/{web,api-client,db,genui,ui}; npm run test passes for @polytoken/{web,api-client,genui}; npm run check (Python) passes"
    - "User-visible chrome reads Polytoken (page <title> metadata + app-sidebar brand text)"
    - "The skill directory is .claude/skills/polytoken-design-system with zero @nauta/ refs; SKILL.md's pre-existing uncommitted edits + the untracked build-design-data.mjs are preserved"
    - "nauta_id/nautaId/nauta_sync occurrence counts are identical before and after (KEEP surface untouched)"
    - "terraform plan shows no diff (live AWS resource names untouched by the rename pass)"
  artifacts:
    - path: "package.json"
      provides: "root name + 11 @polytoken/* -w selectors"
      contains: "@polytoken/"
    - path: "apps/web/package.json"
      provides: "@polytoken/web package name"
      contains: "@polytoken/web"
    - path: "vercel.json"
      provides: "build command referencing @polytoken/web"
      contains: "@polytoken/web"
    - path: ".claude/skills/polytoken-design-system/SKILL.md"
      provides: "renamed design-system skill teaching @polytoken/ui conventions"
      contains: "polytoken"
  key_links:
    - from: "package.json (-w selectors + vercel.json buildCommand)"
      to: "packages/*/package.json + apps/web/package.json name fields"
      via: "npm workspace scope resolution"
      pattern: "@polytoken/(web|db|api-client|genui|ui|tailwind-config)"
---

<objective>
Execute the internal nauta → polytoken rename as ONE atomic pass with no hybrid states: every `@nauta/*` package scope, workspace `-w` selector, TS/TSX import, vercel.json build command, tailwind-config import, UI chrome string, doc mention, Python identifier, and the `nauta-teal` style-pack id becomes polytoken; the `nauta-design-system` skill directory is renamed as its own isolated commit; the workspace is regenerated; and the full verification matrix goes green — while the KEEP surfaces (`entity_instances.nauta_id`/`nauta_sync`, all live AWS/Terraform resource names, and the pre-existing dirty working-tree files) are provably untouched.

Purpose: Satisfies RENM-01. Every subsequent v1.7 phase (43 Auth, 44 Tenancy, 45 Threads, 46 Hygiene) authors new files under the final name — the milestone sequenced this rename FIRST per VISION's "rename once" guardrail. A hybrid state (package names changed but `-w` selectors not, or vice versa) breaks the workspace, so this work cannot be split across the workspace-breaking boundary.
Output: A renamed, workspace-regenerated, fully-green codebase where zero `@nauta/` references remain (minus documented deliberate gaps), plus a committed one-time rename script for auditability.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-atomic-rename-nauta-polytoken/42-CONTEXT.md
@.planning/phases/42-atomic-rename-nauta-polytoken/42-RESEARCH.md

<autonomous_decisions>
This plan was authored in autonomous mode. Two open questions from 42-RESEARCH.md were decided:

DECISION 1 — `nauta-teal` style-pack identifier: RENAME to `polytoken-teal` (no alias). Rationale: it is a
brand identifier (unlike `nauta_id`, which is a live Postgres column + stored data value from an unrelated
upstream system). The phase goal "polytoken everywhere internally — no hybrid states" and VISION's "rename
once" guardrail both forbid lingering brand-named identifiers. Its only coupling is genui exact-match cache
keys, which are deliberately unscoped/regenerable (TENA-01) — stale entries simply regenerate. An alias
keeping "nauta-teal" resolvable would itself be a hybrid state, so no alias is added. All 21 files
(incl. hardcoded test-assertion strings) are updated in the same pass.

DECISION 2 — `.claude/skills/nauta-design-system/` directory: RENAME to `polytoken-design-system/` as its OWN
isolated, reviewable commit within this phase (Task 2), per 42-RESEARCH.md Open Question 2 recommendation.
</autonomous_decisions>

<operator_hard_constraints>
- NEVER `git add -A` or `git add .` — stage explicit paths only. NEVER push, force, or `git checkout`/`git restore`/`git clean` on any dirty file.
- Pre-existing dirty working-tree files MUST NOT be reverted, clobbered, or swept into the rename. Hard-exclude: `infrastructure/aws/ecs.tf`, `.planning/HANDOFF.json`, `.planning/v1.6-MILESTONE-AUDIT.md` (a pre-existing deletion), `apps/web/src/app/dev/design/`, `graphify-out/`, `links.md`, `COWORK-BRIEFING.md`, `0 - nauta_design_case.pdf`, `Nauta - Guia de Arquitetura (PT-BR).pdf`.
- EXCEPTION — the skill dir's own dirty files (`.claude/skills/nauta-design-system/SKILL.md` modified, `scripts/build-design-data.mjs` untracked) ARE renamed by Task 2 (content substitution + directory move preserve the pre-existing edits — they are NOT discarded).
- This repo uses npm workspaces, NOT pnpm. Workspace regeneration is `rm -rf node_modules **/node_modules package-lock.json apps/web/.next && npm install`.
- The UI safety gate was skipped for this phase (mechanical rename, no UI design scope) — Task 1 changes UI chrome strings without a visual-design checkpoint.
- `terraform plan` is the read-only proof step (Task 3). NEVER `terraform apply` in this phase.
</operator_hard_constraints>

<keep_surfaces_deny_list>
The rename script MUST NOT match/touch any of the following (string-level match on `@nauta/` scope + the enumerated `Nauta`/`nauta-teal`/config-label sites ONLY — never a case-insensitive blanket `nauta` replace):

- `nauta_id`, `nautaId`, `nauta_sync` — anywhere, any case. Specifically NEVER edit `packages/db/migrations/*.sql` or the `nautaId`/`source` field definitions in `packages/db/src/schema/entity-instances.ts` (live Postgres column + partial unique index + stored data value from a legacy external system).
- `infrastructure/**` (all `*.tf`, incl. `terraform.tfstate`) and `.github/workflows/*.yml` — RUNBOOK-only live AWS resource names (Plan 42-02).
- `README.md` deploy-target table cells (`nauta-services-email-listener` / `-staging`, ~lines 56-57) — prose renames only, table cells are live resource names.
- `SES_S3_BUCKET: str = "nauta-services-ses-inbound-emails"` in `apps/email-listener/app/settings.py` — live S3 bucket name (KEEP the value; only `APP_NAME` in that file renames).
- `.planning/` historical docs (except this phase's own new deliverables).
- The dirty-file hard-exclusion list above. NOTE: `apps/web/src/app/dev/design/` DOES contain `@nauta/*` refs — it is a deliberate, documented gap against SC1 (excluded because it is pre-existing untracked scratch content the operator asked not to touch).
- gitignored/generated: `.env.local`/`.env.production`/`.env.staging`/`.env.vercel.*`, `.vercel/`, `packages/api-client/dist/`, `node_modules/`, `apps/web/.next/`.
- `COMMANDS.MD` local absolute paths.
- `.claude/skills/nauta-design-system/` — excluded from the Task 1 bulk script; handled entirely by Task 2.
</keep_surfaces_deny_list>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + run the bulk rename script (all MUST-rename surfaces except the skill dir)</name>
  <files>.planning/phases/42-atomic-rename-nauta-polytoken/rename-nauta-to-polytoken.mjs, package.json, vercel.json, packages/*/package.json, apps/web/package.json, apps/web/src/**, packages/*/src/**, apps/web/tailwind.config.ts, packages/ui/tailwind.config.ts, packages/genui/src/theme/packs.ts, packages/genui/src/theme/__tests__/packs.test.ts, apps/email-listener/pyproject.toml, apps/email-listener/app/settings.py, apps/email-listener/app/infrastructure/llm/genui_style_packs.py, apps/email-listener/app/infrastructure/llm/exemplars/__init__.py, apps/email-listener/tests/application/test_cache_key.py, supabase/config.toml, .env.example, README.md, apps/web/README.md, apps/email-listener/README.md</files>
  <read_first>
    - .planning/phases/42-atomic-rename-nauta-polytoken/42-RESEARCH.md (Rename Surface Inventory: MUST-rename / KEEP / RUNBOOK tables; the exact file:line sites)
    - .planning/phases/42-atomic-rename-nauta-polytoken/42-CONTEXT.md (locked decisions + casing convention)
    - package.json (root — the 11 `-w @nauta/*` selectors + `"name": "nauta-services"`)
    - apps/web/package.json (a workspace name + cross-package `@nauta/*` deps)
    - vercel.json (the single buildCommand occurrence)
    - apps/web/src/app/layout.tsx (a UI chrome `<title>` site)
    - packages/db/src/schema/entity-instances.ts (the KEEP `nautaId` surface — read so you SEE what must NOT change)
    - packages/genui/src/theme/packs.ts (the `nauta-teal` STYLE_PACKS/DEFAULT_PACK_ID surface)
  </read_first>
  <action>
    Create a single reviewable, zero-dependency Node ESM script at `.planning/phases/42-atomic-rename-nauta-polytoken/rename-nauta-to-polytoken.mjs` (placed under `.planning/` deliberately so its literal `@nauta/` search strings do not trip the Task 3 grep gate). Author it with named-export helper functions and a pure `(content: string) => string` substitution function per rule (per CLAUDE.md style), plus a top-of-file data block holding the explicit allow-list globs and the deny-list from `<keep_surfaces_deny_list>`. Enforce the deny-list by construction — never a case-insensitive blanket `nauta` replace.

    Substitution rules to apply (target identifiers, lowercase scope / product-case chrome per the two-tier casing convention):
    - `@nauta/` → `@polytoken/` — covers all TS/TSX import specifiers (195 files), all 6 workspace `package.json` `"name"` fields, every cross-package `dependencies`/`devDependencies` `@nauta/*` entry, the 11 root `package.json` `-w @nauta/*` selectors, `vercel.json` buildCommand, both `tailwind.config.ts` imports, and `@nauta/*` mentions in `apps/web/README.md`.
    - Root `package.json` `"name": "nauta-services"` → `"polytoken-services"`.
    - UI chrome (product-case) `Nauta` → `Polytoken` ONLY in the 9 enumerated `apps/web/src/**` sites: `app/layout.tsx`, `app/emails/[id]/page.tsx`, `app/entities/page.tsx`, `app/entities/[id]/page.tsx`, `app/knowledge/page.tsx`, `app/studio/page.tsx` (title AND description), `app/studio/preview/page.tsx`, `components/app-sidebar.tsx`.
    - `apps/email-listener/app/settings.py` `APP_NAME` value `"Nauta Email Listener"` → `"Polytoken Email Listener"` (do NOT touch `SES_S3_BUCKET` in the same file).
    - Prose `Nauta`/`nauta` → `Polytoken`/`polytoken` in `README.md` (lines ~1,3 only), `apps/web/README.md`, `apps/email-listener/README.md` — NEVER the `README.md` deploy-target table cells.
    - `apps/email-listener/pyproject.toml` description "Nauta email listener" → "Polytoken email listener".
    - `supabase/config.toml` `project_id = "nauta"` → `"polytoken"`.
    - `.env.example` comment labels `nauta-staging`/`nauta-prod` → `polytoken-staging`/`polytoken-prod` (comment labels only; the real project refs stay).
    - Exemplar copy in `apps/email-listener/app/infrastructure/llm/exemplars/__init__.py` "Nauta" → "Polytoken".
    - DECISION 1: `nauta-teal` → `polytoken-teal` across all 21 files, incl. `packages/genui/src/theme/packs.ts` (`STYLE_PACKS["nauta-teal"]`, `DEFAULT_PACK_ID`), `apps/email-listener/app/infrastructure/llm/genui_style_packs.py`, and the hardcoded test-assertion strings in `packages/genui/src/theme/__tests__/packs.test.ts` and `apps/email-listener/tests/application/test_cache_key.py`.

    EXCLUDE the skill dir `.claude/skills/nauta-design-system/` (Task 2 owns it). Run the script. Then stage explicit changed paths (never `git add -A`) and commit as `refactor(42): rename @nauta/* → @polytoken/* across code, config, docs, UI chrome`. Include the script file itself in the commit for auditability. Workspace regeneration + full verification is Task 3 — do not run `npm install` here.
  </action>
  <verify>
    <automated>test -z "$(grep -rn '@nauta/' --include='*.ts' --include='*.tsx' --include='*.json' --include='*.mjs' --include='*.py' apps/web/src packages/*/src packages/*/package.json apps/web/package.json package.json vercel.json 2>/dev/null | grep -v -E 'node_modules|/dist/|/dev/design/')" && echo NO_NAUTA_SCOPE_LEFT</automated>
  </verify>
  <done>The bulk substitution is applied and committed; a repo-wide grep for `@nauta/` returns zero outside the documented exclusions; all 9 UI chrome strings read Polytoken; `nauta-teal` is `polytoken-teal` everywhere including test assertions; the KEEP surfaces are untouched.</done>
  <acceptance_criteria>
    - `.planning/phases/42-atomic-rename-nauta-polytoken/rename-nauta-to-polytoken.mjs` exists and is committed.
    - `grep -c '"@nauta/' packages/*/package.json apps/web/package.json` returns 0 matches; each of the 6 files' `"name"` field now reads `@polytoken/{api-client,db,genui,tailwind-config,ui,web}` respectively.
    - `package.json` contains zero `@nauta/` and its 11 workspace scripts reference `@polytoken/{db,web,api-client}`; its `"name"` is `polytoken-services`.
    - `vercel.json` buildCommand reads `npm run build -w @polytoken/web`.
    - `grep -rn 'Nauta' apps/web/src` returns 0 matches (all 9 chrome sites now say Polytoken).
    - `grep -rn 'nauta-teal' packages apps` returns 0 matches; `grep -rn 'polytoken-teal' packages/genui/src/theme/packs.ts` returns ≥1.
    - KEEP proof: the combined count from `grep -rc 'nauta_id\|nautaId\|nauta_sync' packages/db apps/email-listener` is identical to its pre-run value (record both), and `git diff --name-only` for this commit lists NO file under `packages/db/migrations/`, `infrastructure/`, or `.github/workflows/`.
    - `git diff --name-only` for this commit does NOT include any dirty-file hard-exclusion path (ecs.tf, HANDOFF.json, dev/design/, graphify-out/, links.md, COWORK-BRIEFING.md, the two PDFs).
    - Commit message is `refactor(42): rename @nauta/* → @polytoken/* across code, config, docs, UI chrome`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rename the nauta-design-system skill directory as its own isolated commit</name>
  <files>.claude/skills/nauta-design-system/** (renamed to .claude/skills/polytoken-design-system/**): SKILL.md, references/component-catalog.md, scripts/build-catalog.mjs, scripts/build-design-data.mjs</files>
  <read_first>
    - .planning/phases/42-atomic-rename-nauta-polytoken/42-RESEARCH.md (Special-Handling Surface section — the dirty/untracked state + the git-safety guidance)
    - .claude/skills/nauta-design-system/SKILL.md (current content incl. its frontmatter `name:` field and the pre-existing 7-line uncommitted addition — read so you preserve it)
    - .claude/skills/nauta-design-system/scripts/build-catalog.mjs (import-path template strings referencing @nauta/*)
  </read_first>
  <action>
    Rename the skill directory `.claude/skills/nauta-design-system/` → `.claude/skills/polytoken-design-system/` and substitute every `@nauta/` → `@polytoken/` and `nauta-design-system` → `polytoken-design-system` inside its files (SKILL.md frontmatter `name:` field + prose, `references/component-catalog.md`, `scripts/build-catalog.mjs`, and the untracked `scripts/build-design-data.mjs`). This directory carries pre-existing uncommitted state — `SKILL.md` is modified and `scripts/build-design-data.mjs` is untracked — which MUST be preserved, not discarded.

    Safe mechanism: move the whole directory at the filesystem level (which carries tracked, modified, and untracked files intact to the new path), then apply the string substitutions to the moved files, then stage the rename EXPLICITLY by path — stage the new tracked file paths and the old path's deletions with `git add .claude/skills/polytoken-design-system` and `git add .claude/skills/nauta-design-system` (both are explicit paths — this is NOT `git add -A`/`git add .`). Leave the previously-untracked `build-design-data.mjs` untracked at its new location (do not `git add` it — preserve the operator's untracked intent). NEVER use `git checkout`/`git restore`/`git clean` on this path. Commit the tracked portion as `refactor(42): rename nauta-design-system skill → polytoken-design-system`. Keep this diff isolated so a reviewer sees "pre-existing SKILL.md edit + rename, combined" clearly.
  </action>
  <verify>
    <automated>test -d .claude/skills/polytoken-design-system && test ! -d .claude/skills/nauta-design-system && test -z "$(grep -rn '@nauta/' .claude/skills/polytoken-design-system 2>/dev/null)" && echo SKILL_RENAMED_CLEAN</automated>
  </verify>
  <done>The skill directory is `polytoken-design-system`, the old path no longer exists, zero `@nauta/` refs remain inside it, and both the pre-existing SKILL.md modification and the untracked build-design-data.mjs are preserved (present at the new path, contents intact).</done>
  <acceptance_criteria>
    - `.claude/skills/polytoken-design-system/` exists; `.claude/skills/nauta-design-system/` does not.
    - `grep -rn '@nauta/\|nauta-design-system' .claude/skills/polytoken-design-system` returns 0 matches.
    - `.claude/skills/polytoken-design-system/SKILL.md` frontmatter reads `name: polytoken-design-system` and still contains the pre-existing 7-line uncommitted addition (verify the added lines survive).
    - `.claude/skills/polytoken-design-system/scripts/build-design-data.mjs` exists with its content intact and shows as untracked in `git status` (not staged, not discarded).
    - The commit for this task is isolated (`git show --stat` lists only `.claude/skills/**` paths) and messaged `refactor(42): rename nauta-design-system skill → polytoken-design-system`.
    - No `git add -A`/`git add .`/`git checkout`/`git restore`/`git clean` was used.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Regenerate the workspace + run the full verification matrix + prove infra untouched</name>
  <files>package.json, package-lock.json (node_modules/@polytoken/* symlinks regenerated)</files>
  <read_first>
    - .planning/phases/42-atomic-rename-nauta-polytoken/42-RESEARCH.md (Architecture Patterns step 3-4: workspace regeneration + verification command matrix; Common Pitfalls #2 lockfile hazard)
    - package.json (the root `check`/`typecheck`/`test`/`infra:tf` scripts + the now-renamed `-w @polytoken/*` selectors)
  </read_first>
  <action>
    Regenerate the workspace and prove the atomic rename is green and scope-clean. Steps:
    1. Delete stale symlinks + lockfile + Next build cache: `rm -rf node_modules **/node_modules package-lock.json apps/web/.next`, then `npm install` (regenerates `node_modules/@polytoken/*`). A green local `npm install` alone is NOT proof — also run `npm ci` to prove the regenerated lockfile is CI-consistent.
    2. Run the per-workspace TypeScript gate: `npm run typecheck -w @polytoken/web`, `-w @polytoken/api-client`, `-w @polytoken/db`, `-w @polytoken/genui`, `-w @polytoken/ui`.
    3. Run the web/TS test gate: `npm run test -w @polytoken/web`, `-w @polytoken/api-client`, `-w @polytoken/genui`.
    4. Run the Python gate: `npm run check` (ruff check + ruff format --check + mypy + lint-imports + pytest for apps/email-listener).
    5. Run the rename-completeness acceptance grep (must return zero, excluding the documented gaps): `grep -rn "@nauta/" --include="*.ts" --include="*.tsx" --include="*.json" --include="*.mjs" --include="*.py" . | grep -v -E "node_modules|\.next|/dist/|/\.planning/|apps/web/src/app/dev/design/|graphify-out/"`.
    6. Run the infra proof step (read-only, RENM-02 acceptance): `npm run infra:tf -- plan`. Expect zero diff — any `# forces replacement` or resource-name diff means a `.tf` file was wrongly touched (hard stop). NEVER `terraform apply`.
    Stage explicit paths (`package.json`, `package-lock.json`) and commit the regenerated lockfile as `refactor(42): regenerate workspace lockfile for @polytoken/* scope`. If any typecheck/test failure surfaces a straggler `@nauta/` reference in an allow-list file, fix it in place, re-run the failing gate, and amend/extend the commit — never mark green with a red gate.
  </action>
  <verify>
    <automated>ls node_modules/@polytoken >/dev/null 2>&1 && test ! -d node_modules/@nauta && test -z "$(grep -rn '@nauta/' --include='*.ts' --include='*.tsx' --include='*.json' --include='*.mjs' --include='*.py' . | grep -v -E 'node_modules|\.next|/dist/|/\.planning/|apps/web/src/app/dev/design/|graphify-out/')" && echo WORKSPACE_CLEAN</automated>
  </verify>
  <done>`node_modules/@polytoken/*` has the 6 entries and `node_modules/@nauta` is gone; `npm ci`, all 5 typechecks, all 3 TS test suites, and `npm run check` (Python) pass; the acceptance grep returns zero; `terraform plan` shows no diff.</done>
  <acceptance_criteria>
    - `ls node_modules/@polytoken` lists exactly 6 entries (api-client, db, genui, tailwind-config, ui, web); `node_modules/@nauta` does not exist.
    - `npm ci` exits 0 (proves the regenerated `package-lock.json` is CI-consistent, not silently rewritten).
    - `npm run typecheck -w @polytoken/{web,api-client,db,genui,ui}` each exit 0.
    - `npm run test -w @polytoken/{web,api-client,genui}` each exit 0.
    - `npm run check` exits 0 (Python lint+format+typecheck+architecture+pytest).
    - The rename-completeness grep in step 5 returns zero lines.
    - `npm run infra:tf -- plan` output contains no `forces replacement` and no resource-name diff; `git status` shows no modified files under `infrastructure/`.
    - `package-lock.json` is committed with message `refactor(42): regenerate workspace lockfile for @polytoken/* scope`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rename script → repo source tree | The script's only input is this repo's own (fully trusted) source; risk is scope leakage, not untrusted input |
| this phase → live AWS/Terraform infra | Code-rename commits could accidentally include `.tf`/workflow files that name live resources |
| rename pass → pre-existing dirty working tree | A broad rename/stage could clobber or discard uncommitted operator work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | `entity_instances.nauta_id`/`nautaId`/`nauta_sync` (live Postgres column + stored data) | mitigate | Deny-list excludes `packages/db/migrations/*.sql` + the `nautaId`/`source` field defs; script matches only `@nauta/` scope + enumerated `Nauta`/`nauta-teal`/config sites, never case-insensitive `nauta`; Task 1 acceptance re-counts `nauta_id`/`nautaId`/`nauta_sync` before/after and requires identical counts |
| T-42-02 | Denial of Service (availability) | Terraform / live AWS resource names (ECR/ECS/ALB/CloudWatch/S3/SNS) | mitigate | Allow-list excludes all `infrastructure/**` + `.github/workflows/*.yml`; Task 3 runs `terraform plan` (read-only) requiring zero diff as proof; `terraform apply` is forbidden this phase (RENM-02 defers external execution to Plan 42-02's runbook) |
| T-42-03 | Information Disclosure / Tampering | `.env.*` secret files | mitigate | Allow-list touches ONLY `.env.example` comment labels (`nauta-staging`/`nauta-prod`); `.env.local`/`.production`/`.staging`/`.vercel.*` are gitignored + excluded; no secret VALUES are matched (only human-readable comment labels), `SES_S3_BUCKET` value is explicitly KEEP |
| T-42-04 | Tampering (data loss) | pre-existing dirty working-tree files | mitigate | Hard-exclusion list (ecs.tf, HANDOFF.json, v1.6-MILESTONE-AUDIT.md deletion, dev/design/, graphify-out/, links.md, COWORK-BRIEFING.md, 2 PDFs) enforced by the script glob; skill-dir dirty files preserved via filesystem move + explicit-path staging (Task 2); NO `git checkout`/`git restore`/`git clean` on any dirty path |
| T-42-05 | Tampering | git staging / history | mitigate | Explicit-path staging only — never `git add -A`/`git add .`; never push/force/amend published history; each task is its own scoped conventional commit |
| T-42-SC | Tampering | npm/pip/cargo installs | N/A (accept) | This phase installs ZERO new packages (42-RESEARCH.md Package Legitimacy Audit: not applicable); the only `npm install` is a symlink/lockfile regeneration of already-present workspace packages — no supply-chain gate needed |
</threat_model>

<verification>
- Zero `@nauta/` references remain repo-wide except the documented deliberate gaps (`.planning/` historical docs, `apps/web/src/app/dev/design/`, `graphify-out/`) — proven by the Task 3 acceptance grep.
- Workspace symlinks regenerated: `node_modules/@polytoken/*` present (6), `node_modules/@nauta` absent; `npm ci` green.
- All 5 TS typechecks + 3 TS test suites + Python `npm run check` green.
- KEEP surfaces provably untouched: `nauta_id`/`nautaId`/`nauta_sync` counts unchanged; no `.tf`/workflow/migration file in any commit; `terraform plan` zero diff.
- Skill directory renamed with pre-existing dirty edits preserved.
</verification>

<success_criteria>
- RENM-01 satisfied: internal rename is atomic and complete, typecheck + web tests + Python tests green, workspace symlinks regenerated, no hybrid states.
- SC1 (roadmap): zero `@nauta/` in code/config (documented dev/design gap noted); user-visible chrome says Polytoken.
- SC2 (roadmap): workspace regenerated; typecheck + web tests + Python tests green post-rename.
- SC3 partial: `terraform plan` proves live AWS resource names untouched (the runbook itself is Plan 42-02).
</success_criteria>

<output>
Create `.planning/phases/42-atomic-rename-nauta-polytoken/42-01-SUMMARY.md` when done. Record: the pre/post `nauta_id` KEEP-surface counts, the final acceptance-grep result, the `terraform plan` diff result, and confirmation that the dirty-file exclusions + skill-dir dirty edits were preserved.
</output>
