---
phase: 48-token-system-extensions
plan: 02
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - packages/genui/src/theme/tokens.ts
  - packages/genui/src/theme/packs.ts
  - packages/genui/src/theme/__tests__/packs.test.ts
  - apps/web/src/app/globals.css
  - packages/tailwind-config/base.ts
autonomous: true
requirements: [TOKN-04, TOKN-05]
must_haves:
  truths:
    - "Purpose-built tier-ladder aliases color.tier.inferred/extracted (+Foreground pairs) exist in TOKEN_ALIASES and all 6 packs, distinct from accent/muted"
    - "A closed graph node-type palette (color.graph.* for the categories the canvas differentiates by non-token color today) exists in all 6 packs — zero raw hex"
    - "WCAG-AA contrast gate covers the tier + graph foreground/background pairs in all 6 packs"
    - "App-layer utilities bg-tier-inferred/extracted (+foreground) and bg-graph-* exist and are buildable, resolving opacity modifiers like bg-primary/10"
  artifacts:
    - path: "packages/genui/src/theme/tokens.ts"
      provides: "tier + graph aliases in TOKEN_ALIASES + TOKEN_ALIAS_TO_CSS_VAR"
      contains: "color.tier.inferred"
    - path: "packages/genui/src/theme/packs.ts"
      provides: "tier + graph values per pack + resolveVars extension + WCAG comments"
      contains: "color.graph"
    - path: "packages/tailwind-config/base.ts"
      provides: "tier + graph tailwind color groups"
      contains: "tier"
  key_links:
    - from: "packages/genui/src/theme/tokens.ts (tier + graph aliases)"
      to: "packages/genui/src/theme/__tests__/packs.test.ts contrast + registration loops"
      via: "appended to the named pair array from 48-01 Task 3"
      pattern: "color\\.(tier|graph)"
    - from: "apps/web/src/app/globals.css :root (tier + graph vars)"
      to: "packages/tailwind-config/base.ts colors.tier / colors.graph"
      via: "hsl(var(--tier-inferred)) / hsl(var(--graph-entity))"
      pattern: "var\\(--(tier|graph)-"
---

<objective>
Land the two NOVEL, purpose-built token systems the dossier confirmed have no competitor precedent to borrow (D-48-04, D-48-05): a knowledge tier-ladder token pair (INFERRED vs EXTRACTED) that NEVER overloads `color.accent`/`color.muted`, and a small CLOSED graph node-type palette so the xyflow canvas differentiates node categories without hardcoded hex/palette colors. Both are additive across all 6 packs, AA-verified, and extend the same regression gates 48-01 created.

Purpose: These are polytoken differentiators — design them to read as intentional systems, not bolted-on colors. They unblock the knowledge-canvas consumer plan (48-04).

Output: tier + graph aliases wired through both token layers, WCAG-AA-gated, with the "closed palette" rule documented (adding a node type later requires a new alias).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/48-token-system-extensions/48-CONTEXT.md
@.planning/phases/48-token-system-extensions/48-01-SUMMARY.md

<interfaces>
<!-- 48-01 already added 4 utility aliases and the contrast + registration test scaffolding.
     This plan appends tier + graph aliases to the SAME structures. -->

packages/genui/src/theme/tokens.ts — TOKEN_ALIASES (now 24) + TOKEN_ALIAS_TO_CSS_VAR (`satisfies Record<TokenAlias,string>`).
packages/genui/src/theme/packs.ts — makeTokens() completeness gate; resolveVars() hand-maintained (add a line per new alias); 6 pack maps.
packages/genui/src/theme/__tests__/packs.test.ts — 48-01 added: (1) a contrast helper + a named pair array looped for AA; (2) a token-family registration loop over TOKEN_ALIASES. APPEND the tier + graph fg/bg pairs to the named pair array; the registration loop auto-covers the new aliases.
packages/tailwind-config/base.ts — colors map; add `tier` and `graph` groups in the same `hsl(var(--x))` idiom as `primary` so `/10`,`/40` opacity modifiers work.
apps/web/src/app/globals.css — :root/.dark; add tier + graph vars.

Current hardcoded node-type differentiation this palette will replace (consumed in 48-04, defined here):
- entity_instance -> `violet-500` family
- email_component -> `amber-500` family
- email -> `slate` family
(entity_type / knowledge_node already use `primary`, entity_type_field uses `muted` — those stay on their semantic aliases; the graph palette covers only the categories that today reach for a NON-token color.)
</interfaces>

<constraints>
- Zero raw hex in packs.ts (HSL channel-triplets only). The graph palette must express violet/amber/slate-equivalents as HSL triplets, per pack, fitting each register.
- Tier semantics (D-48-04): EXTRACTED = confirmed/trustworthy (stronger, closer to the primary/success family); INFERRED = provisional (quieter, clearly distinct from disabled/muted). Never reuse accent or muted values verbatim.
- Closed palette (D-48-05): document inline in tokens.ts that adding a graph node type later REQUIRES a new alias (prevents palette drift).
- Concurrent-session + npm-workspaces constraints identical to 48-01. DB-free; theme vitest safe.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tier-ladder tokens (INFERRED / EXTRACTED) across all 6 packs</name>
  <read_first>
    packages/genui/src/theme/tokens.ts (TOKEN_ALIASES + TOKEN_ALIAS_TO_CSS_VAR)
    packages/genui/src/theme/packs.ts (resolveVars + 6 pack maps + existing WCAG comments)
    packages/genui/src/theme/__tests__/packs.test.ts (the named pair array + contrast loop from 48-01)
    apps/web/src/app/globals.css (:root/.dark)
    packages/tailwind-config/base.ts (colors idiom)
  </read_first>
  <behavior>
    - `TOKEN_ALIASES` gains `color.tier.inferred`, `color.tier.inferredForeground`, `color.tier.extracted`, `color.tier.extractedForeground`.
    - Each pack's tier fg/bg pairs compute >= 4.5:1 (asserted via the appended pair-array entries).
    - EXTRACTED reads stronger/confirmed; INFERRED reads quieter/provisional and is visibly distinct from that pack's `color.muted`/`color.mutedForeground`.
    - resolveVars emits `tier-inferred`, `tier-inferred-foreground`, `tier-extracted`, `tier-extracted-foreground` for every pack.
  </behavior>
  <action>
    Add the 4 tier aliases to `TOKEN_ALIASES` (grouped after the color block) and to `TOKEN_ALIAS_TO_CSS_VAR` (`color.tier.inferred`->`tier-inferred`, etc.). In each of the 6 pack maps add tier values: EXTRACTED in a confirmed register (lean toward the pack's primary/success hue at readable strength), INFERRED in a quieter provisional register (lower chroma/lightness contrast than EXTRACTED but NOT equal to that pack's muted). Author an inline `// Verified WCAG-AA:` comment per pack for each tier fg/bg pair. Extend `resolveVars` with the 4 lines.
    In `globals.css` add `--tier-inferred`, `--tier-inferred-foreground`, `--tier-extracted`, `--tier-extracted-foreground` to `:root` (polytoken-teal pack values) and `.dark`. In `base.ts` add a `tier` colors group: `tier: { inferred: "hsl(var(--tier-inferred))", "inferred-foreground": "hsl(var(--tier-inferred-foreground))", extracted: "hsl(var(--tier-extracted))", "extracted-foreground": "hsl(var(--tier-extracted-foreground))" }`.
    In `packs.test.ts` append the 4-pack... the 6-pack tier fg/bg pairs to the named contrast pair array so the existing AA loop covers them.
  </action>
  <verify>
    <automated>npm run typecheck -w @polytoken/genui && npm run test -w @polytoken/genui -- src/theme/__tests__/packs.test.ts</automated>
  </verify>
  <done>4 tier aliases defined in all 6 packs + both token layers; AA gate green for tier pairs; tier colors registered in tailwind; resolveVars complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Closed graph node-type palette across all 6 packs</name>
  <read_first>
    packages/genui/src/theme/tokens.ts (TOKEN_ALIASES + TOKEN_ALIAS_TO_CSS_VAR after Task 1)
    packages/genui/src/theme/packs.ts (resolveVars + 6 pack maps)
    apps/web/src/app/knowledge/_components/graph-nodes.tsx (the violet/amber/slate hardcodes this palette replaces — read to size the closed set)
    packages/genui/src/theme/__tests__/packs.test.ts (contrast pair array)
    packages/tailwind-config/base.ts (colors idiom)
  </read_first>
  <behavior>
    - `TOKEN_ALIASES` gains a closed graph palette: `color.graph.entity` (+Foreground), `color.graph.emailComponent` (+Foreground), `color.graph.email` (+Foreground) — the three categories graph-nodes.tsx differentiates with non-token color today.
    - Each pack expresses violet/amber/slate-equivalent hues as HSL triplets fitting its register; fg pairs compute >= 4.5:1.
    - tokens.ts carries a comment stating the palette is CLOSED — a new node type requires a new alias (D-48-05 anti-drift rule).
    - resolveVars emits the 6 graph CSS vars per pack.
  </behavior>
  <action>
    Add the 6 graph aliases to `TOKEN_ALIASES` (a `// graph node-type palette (closed — D-48-05)` group) and to `TOKEN_ALIAS_TO_CSS_VAR` (`color.graph.entity`->`graph-entity`, `color.graph.entityForeground`->`graph-entity-foreground`, `color.graph.emailComponent`->`graph-email-component`, `...Foreground`->`graph-email-component-foreground`, `color.graph.email`->`graph-email`, `...Foreground`->`graph-email-foreground`). Add a comment above the group documenting the closed-palette rule.
    In each of the 6 pack maps add the 6 graph values: entity in a violet-ish register, emailComponent in an amber/gold register, email in a neutral slate register — each retuned to the pack's own palette so it looks intentional (e.g. brutalist uses stark high-saturation versions, warm-editorial warms them). Author inline AA comments for the fg pairs. Extend `resolveVars` with the 6 lines.
    In `globals.css` add the 6 graph vars to `:root` (polytoken-teal values — the app default the knowledge canvas reads) and `.dark`. In `base.ts` add a `graph` colors group mirroring the `primary` idiom (`hsl(var(--graph-entity))` etc.) so `bg-graph-entity/10 border-graph-entity/40 text-graph-entity` resolve exactly like the existing `bg-primary/10`.
    Append the 6-pack graph fg/bg pairs to the named contrast pair array in `packs.test.ts`.
  </action>
  <verify>
    <automated>npm run typecheck -w @polytoken/genui && npm run typecheck -w @polytoken/web && npm run test -w @polytoken/genui -- src/theme/__tests__/packs.test.ts src/theme/__tests__/token-allowlist.test.ts</automated>
  </verify>
  <done>Closed graph palette in all 6 packs + both token layers; AA + registration + allowlist gates green; closed-palette rule documented; opacity modifiers resolve like bg-primary/10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM spec output -> token allowlist | New tier/graph aliases join the closed z.enum |
| Token value strings -> CSS | tier/graph values become `hsl(var(--x))` custom properties |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Tampering | tier + graph color values | mitigate | HSL-triplet + no-raw-hex assertions in packs.test.ts iterate `COLOR_ALIASES`, auto-covering the new `color.tier.*`/`color.graph.*` aliases. |
| T-48-05 | Tampering | closed graph palette drift | mitigate | Palette is a closed alias set gated by TokenAliasSchema z.enum; documented rule that a new node type requires a new alias, enforced by the allowlist tests. |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (additive data + config + tests). |
</threat_model>

<verification>
- `npm run typecheck -w @polytoken/genui` and `-w @polytoken/web` clean.
- `npm run test -w @polytoken/genui` green; contrast loop now covers success + tier + graph pairs across 6 packs; registration loop covers all aliases.
- tier + graph aliases present in TOKEN_ALIASES; `bg-tier-*`, `bg-graph-*` buildable.
</verification>

<success_criteria>
Tier-ladder tokens (never overloading accent/muted) and a closed graph node-type palette exist in all 6 packs and both token layers, AA-verified, with the closed-palette anti-drift rule documented — ready for the knowledge-canvas consumer plan.
</success_criteria>

<output>
Create `.planning/phases/48-token-system-extensions/48-02-SUMMARY.md` when done.
</output>
