---
phase: 48-token-system-extensions
plan: 03
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - apps/web/src/components/provenance-link.tsx
  - apps/web/src/app/chat/_components/markdown-renderer.tsx
  - apps/web/src/app/studio/_components/json-pane.tsx
  - apps/web/src/app/emails/[id]/_components/layers-tree-row.tsx
  - apps/web/src/app/emails/[id]/_components/extraction-summary-panel.tsx
  - apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx
autonomous: true
requirements: [TOKN-01, TOKN-02, TOKN-03]
must_haves:
  truths:
    - "The shared ProvenanceLink citation chip renders a true pill (rounded-pill) instead of rounded-md"
    - "Inline code + code blocks in chat markdown and the studio JSON pane render on font-code"
    - "Semantically-correct confirmed-good visuals use bg-success/text-success-foreground; zero hardcoded green/emerald classes remain in the touched files"
    - "Stop/cancel/deny controls are NOT relabelled success (they keep muted/destructive semantics)"
  artifacts:
    - path: "apps/web/src/components/provenance-link.tsx"
      provides: "pill-radius citation chip"
      contains: "rounded-pill"
    - path: "apps/web/src/app/chat/_components/markdown-renderer.tsx"
      provides: "code typography on font-code"
      contains: "font-code"
    - path: "apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx"
      provides: "success-token confirm affordance"
      contains: "success"
  key_links:
    - from: "provenance-link.tsx CHIP_CLASS_NAME"
      to: "the rounded-pill utility (48-01)"
      via: "className token"
      pattern: "rounded-pill"
    - from: "success visuals (layers-tree-row / extraction-summary-panel / confirm-deny-controls)"
      to: "the bg-success utility (48-01)"
      via: "className token replacing bg-green/bg-emerald"
      pattern: "bg-success"
---

<objective>
Consume the three utility tokens from 48-01 at their designated call sites (D-48-01/02/03): the ONE shared `<ProvenanceLink>` citation chip becomes a true pill; chat-markdown code + the studio JSON pane move onto `typography.code.family`; and the existing confirmed-good visuals migrate off hardcoded green/emerald onto `color.success` — WITHOUT relabelling any stop/cancel/deny control (the dossier rule: success is for confirmed-good outcomes only). Capture a before/after screenshot artifact as the visual evidence (D-48-08).

Purpose: Tokens are only real once consumed. This proves pill/success/code render correctly and removes raw-color success leaks.

Output: Pill chips, code-family code rendering, token-driven success visuals, and a committed screenshot artifact.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-token-system-extensions/48-CONTEXT.md
@.planning/phases/48-token-system-extensions/48-01-SUMMARY.md
@.claude/skills/polytoken-design-system/SKILL.md

<interfaces>
<!-- New utilities available from 48-01: rounded-pill, bg-success / text-success-foreground, font-code, .touch-target. -->

apps/web/src/components/provenance-link.tsx
- `CHIP_CLASS_NAME` — the single chip class string; currently `... rounded-md border border-transparent bg-muted px-2 py-1 ... hover:bg-accent hover:text-accent-foreground ...`. Change `rounded-md` -> `rounded-pill`. This chip is the ONE shared provenance primitive (citation chips + Phase-41 knowledge-preview footer) — editing it here converts all consumers at once.

apps/web/src/app/chat/_components/markdown-renderer.tsx
- `Code` component: inline branch uses `... font-mono text-xs`. `Pre` component: `<pre className="p-3 text-xs">` (inherits font). Migrate inline code + `<pre>` onto `font-code`.

apps/web/src/app/studio/_components/json-pane.tsx — studio JSON/code display (uses a mono class). Migrate to `font-code`.

Confirmed-good visuals with hardcoded green/emerald (replace with success tokens; leave the deny/destructive side untouched):
- apps/web/src/app/emails/[id]/_components/layers-tree-row.tsx — `bg-green-50` (confirmed row tint), `bg-green-500 hover:bg-green-600 text-white` (confirm dot).
- apps/web/src/app/emails/[id]/_components/extraction-summary-panel.tsx — `confirmed: "bg-emerald-500"`, `text-emerald-700 dark:text-emerald-400`.
- apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx — confirm button `bg-green-500 hover:bg-green-600 text-white`; the DENY button (destructive/red) stays as-is.
</interfaces>

<constraints>
- Do NOT edit the locked renderer files. Do NOT touch supabase/ or .env.
- Studio tabs (studio-tabs.tsx) are underline-style (`rounded-none`, `border-b-2`) — NOT pills. Do NOT force them into pills. If no true "tab pill" or "follow-up chip" component exists, convert the citation chip (+ any genuine `rounded-full` pill chip) only and RECORD the exception in the SUMMARY (D-48-01 discretion clause).
- hover/active: apply the derivation recipe consistently — for success affordances use a one-step-stronger hover (e.g. `hover:bg-success/90`) rather than a second hardcoded green; this is the recipe the 48-05 convention doc records.
- npm workspaces. Verify: `npm run typecheck -w @polytoken/web` + `npm run test -w @polytoken/web`.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pill radius on the shared citation chip + code typography</name>
  <read_first>
    apps/web/src/components/provenance-link.tsx (full)
    apps/web/src/components/provenance-link.test.tsx (confirm no snapshot pins rounded-md)
    apps/web/src/app/chat/_components/markdown-renderer.tsx (Code + Pre)
    apps/web/src/app/studio/_components/json-pane.tsx (mono code display)
  </read_first>
  <action>
    In `provenance-link.tsx` change `rounded-md` -> `rounded-pill` in `CHIP_CLASS_NAME` (per D-48-01 — surfaced 3x in the dossier; the shared primitive converts all citation/preview chips at once). Grep the chat surface for any other genuine pill-shaped chip (a `rounded-full` chip carrying label text, e.g. a follow-up chip); convert those to `rounded-pill` too. If none exist, note it.
    In `markdown-renderer.tsx` replace the inline `Code` branch's `font-mono` with `font-code`, and add `font-code` to the `<pre>` in `Pre` so fenced blocks render on the code family (D-48-03 consumer: chat markdown code). In `json-pane.tsx` swap its mono class to `font-code`.
    Do NOT change studio tabs (underline-style, not pills).
  </action>
  <verify>
    <automated>npm run typecheck -w @polytoken/web && npm run test -w @polytoken/web -- src/components/provenance-link.test.tsx</automated>
  </verify>
  <done>Citation chip renders rounded-pill; inline code + code blocks + studio JSON pane render on font-code; provenance-link tests green; studio-tab exception (if any) noted.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate confirmed-good visuals onto color.success (no control relabelling)</name>
  <read_first>
    apps/web/src/app/emails/[id]/_components/layers-tree-row.tsx (the bg-green-50 / bg-green-500 confirm affordances)
    apps/web/src/app/emails/[id]/_components/extraction-summary-panel.tsx (bg-emerald-500 / text-emerald-*)
    apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx (confirm vs deny buttons)
  </read_first>
  <action>
    Replace hardcoded green/emerald ONLY where the visual denotes a confirmed-good outcome (D-48-02):
    - layers-tree-row.tsx: `bg-green-50` -> `bg-success/10`; the confirm dot `bg-green-500 hover:bg-green-600 text-white` -> `bg-success hover:bg-success/90 text-success-foreground`.
    - extraction-summary-panel.tsx: the `confirmed` swatch `bg-emerald-500` -> `bg-success`; the confirmed label `text-emerald-700 dark:text-emerald-400` -> `text-success` (or `text-success-foreground` where it sits on a success fill).
    - confirm-deny-controls.tsx: the CONFIRM button `bg-green-500 hover:bg-green-600 text-white` -> `bg-success hover:bg-success/90 text-success-foreground`. Leave the DENY (destructive) button untouched — stop/deny is never success.
    After edits, grep the three files for `green-|emerald-|lime-` and confirm zero remain.
  </action>
  <verify>
    <automated>npm run typecheck -w @polytoken/web && grep -RIno "bg-green-\|emerald-\|bg-lime-" apps/web/src/app/emails/[id]/_components/layers-tree-row.tsx apps/web/src/app/emails/[id]/_components/extraction-summary-panel.tsx apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx; test $? -eq 1</automated>
  </verify>
  <done>Confirmed-good visuals use success tokens; deny/destructive untouched; zero hardcoded green/emerald/lime classes remain in the three files; web typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Before/after screenshot artifact for a chip + success surface</name>
  <read_first>
    apps/web/package.json (the `screenshot:review` script + playwright.screenshot.config.ts)
    .planning/phases/47-brand-foundation-verification-tooling/47-05-SUMMARY.md (harness usage + output location)
  </read_first>
  <action>
    Run the Phase 47 screenshot harness (`npm run screenshot:review` in apps/web) to capture the surfaces this plan changed — the citation-chip surface (chat tool-round row) and a confirmed-good email surface — producing the reviewable artifact the harness emits. Commit the artifact into the harness's committed output directory (per 47-05). This is the D-48-08 visual evidence; keep it cheap (reuse existing harness targets, add a target only if the chip/success surface is not already covered). If the harness requires a running dev server or live data unavailable in this concurrent-session window, capture what is cheap and record in the SUMMARY exactly which surface was/was not captured (do NOT touch supabase/.env to force it).
  </action>
  <verify>
    <human-check>The committed screenshot artifact shows the pill-shaped citation chip and the success-token confirmed visual; before/after difference is visible.</human-check>
  </verify>
  <done>A before/after visual artifact for the chip + success surface is produced and committed (or the cheap subset captured with the gap recorded).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model-authored citation route -> chip href | ProvenanceLink computes href from a fixed switch, never a caller string (pre-existing T-39-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-06 | Tampering | ProvenanceLink chip | accept | Only the border-radius class changes; the existing `hrefFor` fixed-switch route derivation is untouched — no new trust surface. |
| T-48-07 | Spoofing | success vs destructive semantics | mitigate | Success tokens applied ONLY to confirmed-good outcomes; deny/stop controls keep destructive/muted — prevents a deny action reading as a safe success. |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No package installs (className swaps + harness run only). |
</threat_model>

<verification>
- `npm run typecheck -w @polytoken/web` clean; provenance-link tests green.
- Zero `green-`/`emerald-`/`lime-` classes remain in the three success files.
- Screenshot artifact committed (or cheap subset + gap recorded).
</verification>

<success_criteria>
The shared citation chip is a true pill; chat/studio code renders on font-code; confirmed-good visuals are token-driven with deny/stop semantics preserved; a before/after artifact evidences the change.
</success_criteria>

<output>
Create `.planning/phases/48-token-system-extensions/48-03-SUMMARY.md` when done.
</output>
