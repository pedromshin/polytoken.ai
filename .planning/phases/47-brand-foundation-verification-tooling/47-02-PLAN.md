---
phase: 47-brand-foundation-verification-tooling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/entities/page.tsx
  - apps/web/src/app/entities/[id]/page.tsx
  - apps/web/src/app/knowledge/page.tsx
  - apps/web/src/app/studio/page.tsx
  - apps/web/src/app/studio/preview/page.tsx
  - apps/web/src/app/settings/forwarding/page.tsx
  - apps/web/src/app/emails/[id]/page.tsx
  - apps/web/src/app/chat/_components/chat-home-empty-state.tsx
  - apps/web/src/app/chat/_canvas/canvas-empty-state.tsx
  - apps/web/src/app/_components/inbox-three-pane.tsx
  - apps/web/src/app/emails/[id]/_components/email-detail.tsx
  - apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx
  - apps/web/src/app/emails/[id]/_components/use-autofill-fields.ts
  - apps/web/src/app/emails/[id]/_components/pdf-preview-pane.tsx
autonomous: true
requirements: [BRND-01]
must_haves:
  truths:
    - "Every page browser-tab title reads in the warm polytoken voice register (product-named 'Polytoken') — no bare systems labels"
    - "The chat, canvas, and inbox empty states use warm companion framing, not systems vocabulary"
    - "Success/info toasts read warm; error toasts stay clear and actionable (semantics preserved)"
    - "No purged brand name appears in the swept copy — the product is named only 'polytoken' (USER-LOCKED 2026-07-10)"
  artifacts:
    - path: "apps/web/src/app/layout.tsx"
      provides: "Root metadata title in the warm polytoken voice register"
    - path: "apps/web/src/app/chat/_components/chat-home-empty-state.tsx"
      provides: "polytoken-register chat empty state"
    - path: "apps/web/src/app/_components/inbox-three-pane.tsx"
      provides: "polytoken-register inbox empty state"
  key_links:
    - from: "apps/web/src/app/*/page.tsx"
      to: "browser tab title"
      via: "Metadata.title export"
      pattern: "title:"
---

<objective>
Sweep the warm polytoken voice register (D-47-01) across the non-chrome user-facing copy the app has today: every page `<title>`, the empty states, and the toasts. String-level only (D-47-06) — no layout or component restructuring (that is Phase 49).

Purpose: closes the remainder of BRND-01 (login + sidebar chrome are covered by Plan 47-01). This makes the whole product speak warm/companion first-person instead of systems vocabulary.
Output: register-shifted titles, empty states, and toast copy across ~15 files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-brand-foundation-verification-tooling/47-CONTEXT.md
@.planning/research/v1.8-design/BRAND-IDENTITY-OPTIONS.md
@docs/design/product-register-and-bans.md

<interfaces>
<!-- Voice source (LOCKED, D-47-01): warm, human, companion — "your workspace", first-person. Reference points
     Notion AI / Mem. NEVER infrastructure vocabulary. The tone survives from the Direction-B ANALYSIS; the
     product NAME is "polytoken" everywhere (USER-LOCKED 2026-07-10 — no alternate brand name). -->

Copy scope (D-47-06 — string-level ONLY, no layout/component change): page <title>s, empty states, and
existing toast messages. login card + sidebar chrome are OUT of this plan (owned by Plan 47-01) — in
particular DO NOT touch apps/web/src/app/login/page.tsx (its title lives with Plan 01).

Current page titles (grep of Metadata.title):
- apps/web/src/app/layout.tsx: "Polytoken — Emails" (root default)
- apps/web/src/app/entities/page.tsx: "Entities — Polytoken"
- apps/web/src/app/entities/[id]/page.tsx: "Entity — Polytoken"
- apps/web/src/app/knowledge/page.tsx: "Knowledge — Polytoken"
- apps/web/src/app/studio/page.tsx: "Studio — Polytoken"
- apps/web/src/app/studio/preview/page.tsx: "Studio — Polytoken"
- apps/web/src/app/settings/forwarding/page.tsx: "Forwarding address — Polytoken"
- apps/web/src/app/emails/[id]/page.tsx: "Loading… — Polytoken" (returned from generateMetadata)

Current empty states (call sites of the shared EmptyState primitive + inline strings):
- chat-home-empty-state.tsx: heading "Start a new conversation" / body "Ask the agent anything — responses stream in and can include interactive widgets."
- canvas-empty-state.tsx: heading "No panels yet" / body "Genui responses from this chat will appear here as panels. Switch to Chat view to start a conversation."
- inbox-three-pane.tsx (~L340): inline strings "No emails with extracted entities yet." and "No emails yet."

Current toasts (sonner `toast.*` call sites):
- email-detail.tsx: toast.success("Email sent for reprocessing"), toast.error("Could not reprocess email. Try again."), toast.error("Could not add field region. Try again."), a toast.warning(...)
- confirm-deny-controls.tsx: toast.info("Field value cleared.", { Undo, 3000ms }) — copy governed by an existing Copywriting Contract; keep the Undo affordance + 3000ms duration intact
- use-autofill-fields.ts: toast.error("AI autofill is unavailable — model access is pending.", ...) + a 6000ms degrade toast
- pdf-preview-pane.tsx: toast.warning("Rectangle too small. Draw a larger area.")

Copy bans that constrain this sweep (docs/design/product-register-and-bans.md): #13 no meta-criticism
(state the claim plainly), and the existing honesty convention for generation-state copy (never over-promise).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register-shift the page titles into the warm polytoken voice</name>
  <read_first>
    - apps/web/src/app/layout.tsx (root Metadata.title — the file being modified)
    - apps/web/src/app/entities/page.tsx, apps/web/src/app/entities/[id]/page.tsx, apps/web/src/app/knowledge/page.tsx, apps/web/src/app/studio/page.tsx, apps/web/src/app/studio/preview/page.tsx, apps/web/src/app/settings/forwarding/page.tsx, apps/web/src/app/emails/[id]/page.tsx (each carries a Metadata.title to shift)
    - .planning/phases/47-brand-foundation-verification-tooling/47-CONTEXT.md (D-47-06 lists page titles in scope; D-47-01 register + USER-LOCK)
  </read_first>
  <files>apps/web/src/app/layout.tsx, apps/web/src/app/entities/page.tsx, apps/web/src/app/entities/[id]/page.tsx, apps/web/src/app/knowledge/page.tsx, apps/web/src/app/studio/page.tsx, apps/web/src/app/studio/preview/page.tsx, apps/web/src/app/settings/forwarding/page.tsx, apps/web/src/app/emails/[id]/page.tsx</files>
  <action>
    Rewrite each `Metadata.title` string into the warm polytoken voice register while keeping the product name
    "Polytoken" (D-47-01 USER-LOCK: the name is polytoken everywhere) and a consistent `{Surface} — Polytoken`
    shape. Warm the surface label with first-person framing where it is systems-y: e.g. root "Polytoken — Emails" →
    "Your inbox — Polytoken"; "Knowledge — Polytoken" → "Your knowledge — Polytoken"; "Entities — Polytoken" →
    "Your entities — Polytoken"; "Forwarding address — Polytoken" is already plain-clear (keep or lightly warm).
    Leave the dynamic `generateMetadata` mechanics in `emails/[id]/page.tsx` intact — only the returned title string
    changes ("Loading… — Polytoken" may stay). Do NOT touch login/page.tsx (Plan 01 owns it). Change strings only —
    no imports, no logic. Finalize wording within the warm polytoken voice register; avoid infrastructure vocabulary
    and never introduce an alternate brand name.
  </action>
  <acceptance_criteria>
    - Root title in `layout.tsx` no longer reads the literal "Polytoken — Emails" (register-shifted).
    - Every listed page still exports a `title:` and each contains "Polytoken" (product name retained): `grep -rn "title:" apps/web/src/app/{layout.tsx,entities,knowledge,studio,settings,emails}` shows Polytoken in each. [automated]
    - `apps/web/src/app/login/page.tsx` is unchanged by this plan (owned by 47-01): `git diff --name-only` does NOT list it under this plan's commit.
    - `npm run typecheck -w @polytoken/web` passes. [automated]
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register-shift the empty states into warm companion framing</name>
  <read_first>
    - apps/web/src/app/chat/_components/chat-home-empty-state.tsx (file being modified)
    - apps/web/src/app/chat/_canvas/canvas-empty-state.tsx (file being modified)
    - apps/web/src/app/_components/inbox-three-pane.tsx (file being modified — inline empty strings ~L340)
    - apps/web/src/components/empty-state.tsx (the shared primitive these call — understand which props are copy)
    - .planning/phases/47-brand-foundation-verification-tooling/47-CONTEXT.md (D-47-06 example: "empty inbox → companion framing")
  </read_first>
  <files>apps/web/src/app/chat/_components/chat-home-empty-state.tsx, apps/web/src/app/chat/_canvas/canvas-empty-state.tsx, apps/web/src/app/_components/inbox-three-pane.tsx</files>
  <action>
    Rewrite the `heading`/`body` copy at each empty-state call site into warm companion framing (D-47-01/D-47-06),
    keeping the `action`/`icon`/variant props and all layout untouched. Chat-home: shift "Start a new conversation"
    to a warmer companion invite (e.g. "Ask me anything") while keeping the "New chat" CTA action prop verbatim.
    Canvas: shift "No panels yet" to companion framing that still explains panels appear here. Inbox
    (`inbox-three-pane.tsx`): shift the two inline strings "No emails yet." / "No emails with extracted entities
    yet." to companion framing (e.g. "Your inbox is clear — forwarded mail will land here." / "Nothing extracted
    yet — entities will show up as mail arrives."), preserving the existing conditional that picks between them.
    String-level only; do not change the EmptyState primitive or the conditional logic. Never introduce an
    alternate brand name — the product is named only polytoken.
  </action>
  <acceptance_criteria>
    - `chat-home-empty-state.tsx` no longer contains the literal "Start a new conversation"; the "New chat" action label is still present. [automated grep]
    - `canvas-empty-state.tsx` no longer contains the literal "No panels yet".
    - `inbox-three-pane.tsx` no longer contains the literal "No emails yet." (both inline empty strings register-shifted); the branch condition is unchanged.
    - No purged brand name in the swept copy: `grep -niE "cortex|nodal|lattice|constellation" apps/web/src/app/chat/_components/chat-home-empty-state.tsx apps/web/src/app/chat/_canvas/canvas-empty-state.tsx apps/web/src/app/_components/inbox-three-pane.tsx` returns NO matches. [automated]
    - `npm run typecheck -w @polytoken/web` passes and `npm run test -w @polytoken/web` stays green. [automated]
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Register-shift the toasts into the warm polytoken voice (preserve semantics)</name>
  <read_first>
    - apps/web/src/app/emails/[id]/_components/email-detail.tsx (toast.success/error/warning call sites)
    - apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx (toast.info + Undo affordance; governed by an existing Copywriting Contract)
    - apps/web/src/app/emails/[id]/_components/use-autofill-fields.ts (toast.error degrade copy + 6000ms duration)
    - apps/web/src/app/emails/[id]/_components/pdf-preview-pane.tsx (toast.warning)
    - docs/design/product-register-and-bans.md (ban #13 no meta-criticism; honesty convention for state copy)
  </read_first>
  <files>apps/web/src/app/emails/[id]/_components/email-detail.tsx, apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx, apps/web/src/app/emails/[id]/_components/use-autofill-fields.ts, apps/web/src/app/emails/[id]/_components/pdf-preview-pane.tsx</files>
  <action>
    Warm the toast COPY only, holding every semantic and API detail fixed. Success/info toasts may take a warmer
    tone (e.g. "Email sent for reprocessing" → "On it — reprocessing this email"). Error/warning toasts MUST stay
    clear and actionable — keep the failure state honest (project rule: friendly but truthful; never over-promise;
    ban #13 no meta-criticism). Preserve ALL non-copy args exactly: the Undo action + 3000ms duration in
    `confirm-deny-controls.tsx`, the 6000ms degrade duration + description in `use-autofill-fields.ts`, and the
    `toast.success/error/warning/info` VARIANT used at each site (do not downgrade an error to info). Do not change
    any surrounding logic, mutation callbacks, or imports — string arguments only.
  </action>
  <acceptance_criteria>
    - Each touched file still calls the SAME toast variant it did before (no error→info downgrades): `grep -nE "toast\.(success|error|warning|info)" apps/web/src/app/emails/[id]/_components/{email-detail.tsx,confirm-deny-controls.tsx,use-autofill-fields.ts,pdf-preview-pane.tsx}` shows the variants preserved. [automated]
    - `confirm-deny-controls.tsx` still passes the Undo action and 3000ms duration; `use-autofill-fields.ts` still passes 6000ms. [automated grep for "3000"/"Undo" and "6000"]
    - No meta-criticism / self-undercutting phrasing added (ban #13).
    - `npm run typecheck -w @polytoken/web` passes and `npm run test -w @polytoken/web` stays green. [automated]
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static copy → rendered DOM | title/empty-state/toast strings render as React/Next text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04 | Tampering (XSS) | toast/empty-state/title strings | accept | All new copy is static string literals with no interpolation of untrusted data; React/Next auto-escape text nodes — no injection surface introduced |
| T-47-05 | Repudiation | error toasts semantic downgrade | mitigate | Acceptance gate asserts toast variants are preserved, so a failure state cannot be silently softened into an info toast |
</threat_model>

<verification>
- Every page still has a `title:` containing "Polytoken"; root/knowledge/entities titles register-shifted.
- Empty-state headings register-shifted; action props + conditionals intact.
- Swept copy names the product only "polytoken" (no purged alternate brand name present).
- Toast variants + non-copy args (Undo, durations) preserved.
- `npm run typecheck -w @polytoken/web` clean; `npm run test -w @polytoken/web` green.
</verification>

<success_criteria>
BRND-01 remainder satisfied: titles, empty states, and toasts across the app speak the warm polytoken voice register (product named only polytoken), string-level only, with all behavior/semantics preserved.
</success_criteria>

<output>
Create `.planning/phases/47-brand-foundation-verification-tooling/47-02-SUMMARY.md` when done.
</output>
