---
phase: 47-brand-foundation-verification-tooling
plan: 03
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - docs/design/brand-guide.md
  - .planning/PROJECT.md
autonomous: true
requirements: [BRND-03]
must_haves:
  truths:
    - "A developer can read docs/design/brand-guide.md to learn the Cortex voice, do/don't, and mark usage"
    - "The brand guide references (never contradicts) product-register-and-bans.md for the bans"
    - "The guide records the naming-collision mitigation posture AND an explicit 'NOT done — user-gated' list"
    - "PROJECT.md Key Decisions records the brand decision + naming-collision posture"
  artifacts:
    - path: "docs/design/brand-guide.md"
      provides: "In-repo Cortex brand guide (voice, do/don't, mark usage, collision posture, NOT-done list)"
      min_lines: 40
      contains: "Cortex"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions entry for the v1.8 brand decision"
      contains: "Cortex"
  key_links:
    - from: "docs/design/brand-guide.md"
      to: "docs/design/product-register-and-bans.md"
      via: "reference for the bans (glassmorphism etc.)"
      pattern: "product-register-and-bans"
    - from: "docs/design/brand-guide.md"
      to: "apps/web/src/components/brand-mark.tsx"
      via: "mark-usage section references the real mark asset"
      pattern: "brand-mark"
---

<objective>
Record the brand decision so it is durable and discoverable: an in-repo brand guide (voice + do/don't + mark usage + collision-mitigation posture + an explicit "NOT done — user-gated" list) and a PROJECT.md Key Decisions entry.

Purpose: closes BRND-03. Turns the LOCKED Cortex direction (D-47-01/02/03) and the naming-collision research into repo-of-record artifacts every later phase and future reader relies on.
Output: `docs/design/brand-guide.md` (new) + a Key Decisions row in `.planning/PROJECT.md`.
Depends on 47-01 so the mark-usage section references the real `brand-mark.tsx` / `icon.svg` assets and their actual variants.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-brand-foundation-verification-tooling/47-CONTEXT.md
@.planning/research/v1.8-design/BRAND-IDENTITY-OPTIONS.md
@docs/design/product-register-and-bans.md
@.planning/phases/47-brand-foundation-verification-tooling/47-01-SUMMARY.md

<interfaces>
<!-- LOCKED sources for this doc — transcribe, do not re-decide. -->

D-47-03 brand guide contents: voice principles + do/don't table; mark usage (clear space, min sizes,
monochrome rules); the collision-mitigation posture (SEO differentiation vs the `polytoken` CLI dev-tool at
docs.polytoken.dev; personal-productivity search intent is the practical mitigation); and an explicit
"NOT done — user-gated" list (domain purchase, trademark search/filing, product rename decision).
The guide SITS BESIDE product-register-and-bans.md which stays authoritative for bans — the guide REFERENCES
it, never contradicts (esp. the glassmorphism ban item 3 — the guide must not license new blur).

D-47-01 voice (Cortex): warm, human, companion — "remember, recall, connect, your workspace"; first-person
framing; reference points Notion AI / Mem; NEVER infrastructure vocabulary. Product NAME in chrome stays
"Polytoken" (Cortex is a REGISTER, not a rename — the rename is user-gated).

Naming collision (BRAND-IDENTITY-OPTIONS.md "Naming collision" section): an existing product "Polytoken" —
a local-first AI coding-agent daemon at docs.polytoken.dev, npm package `polytoken`, `polytoken.com` registered.
Exact-name collision in an adjacent space. Cortex's personal-productivity register competes for different
search terms than that dev-tool collision — that register choice IS the mitigation available without registering
or buying anything now.

PROJECT.md Key Decisions FORMAT (existing table at ## Key Decisions): a 3-column markdown table
| Decision | Rationale | Outcome | — append one new row (do not restructure the table). Milestone-prefix the
Decision cell like the existing rows (e.g. "**v1.8 Phase 47 (BRND)**: ...").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the in-repo Cortex brand guide</name>
  <read_first>
    - .planning/phases/47-brand-foundation-verification-tooling/47-CONTEXT.md (D-47-03 required contents; D-47-01/02 voice + mark)
    - .planning/research/v1.8-design/BRAND-IDENTITY-OPTIONS.md (Direction B Cortex + Naming-collision section + Recommendation)
    - docs/design/product-register-and-bans.md (the authoritative bans this guide references, not restates)
    - apps/web/src/components/brand-mark.tsx (created by 47-01 — the real variants/geometry the mark-usage section cites)
    - apps/web/src/app/icon.svg (created by 47-01 — the favicon asset)
  </read_first>
  <files>docs/design/brand-guide.md</files>
  <action>
    Create `docs/design/brand-guide.md` with these sections (per D-47-03): (1) Voice principles — the Cortex
    register (warm/companion/first-person; Notion AI / Mem reference points; never infrastructure vocabulary) with
    a concrete do/don't table of at least 5 before→after copy pairs drawn from the real surfaces (login greeting,
    empty states, toasts). (2) Mark usage — reference the REAL assets `apps/web/src/components/brand-mark.tsx`
    (its `variant` glyph/lockup + `tone` brand/mono) and `apps/web/src/app/icon.svg`; state clear-space, minimum
    sizes, and when to use the monochrome variant. (3) Collision-mitigation posture — the docs.polytoken.dev CLI
    dev-tool collision and why Cortex's personal-productivity register is the practical SEO differentiation. (4) An
    explicit "NOT done — user-gated" list: no domain purchase, no trademark search/filing, no product rename to
    Cortex. Add a one-line pointer that `docs/design/product-register-and-bans.md` remains authoritative for the
    absolute bans (the guide must NOT license new blur / contradict the glassmorphism ban). Keep it a working
    reference doc, not marketing prose.
  </action>
  <acceptance_criteria>
    - `docs/design/brand-guide.md` exists, contains "Cortex", and is ≥40 lines. [automated: wc -l]
    - It references `product-register-and-bans` and `brand-mark` (or `brand-mark.tsx`): `grep -n "product-register-and-bans\|brand-mark" docs/design/brand-guide.md` matches both. [automated]
    - It contains a "NOT done" / "user-gated" section naming domain purchase, trademark, and rename: `grep -niE "domain|trademark|rename" docs/design/brand-guide.md` matches all three concepts. [automated]
    - It contains a do/don't voice table (≥5 before→after pairs).
    - It does NOT license blur/glassmorphism: `grep -niE "glassmorphism|backdrop-blur|frosted" docs/design/brand-guide.md` — any match must be in a "banned/see bans" context, not a permission. [manual read]
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Record the brand decision in PROJECT.md</name>
  <read_first>
    - .planning/PROJECT.md (## Key Decisions table at ~L454 — the format to append to)
    - .planning/phases/47-brand-foundation-verification-tooling/47-CONTEXT.md (D-47-01/02/03 — what to record)
    - .planning/research/v1.8-design/BRAND-IDENTITY-OPTIONS.md (Recommendation — the rationale to cite)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    Append ONE row to the existing `## Key Decisions` markdown table (do not restructure it). Decision cell,
    milestone-prefixed like the existing rows: "**v1.8 Phase 47 (BRND)**: Cortex adopted as brand REGISTER
    (voice + mark), product name stays 'Polytoken' (rename user-gated)". Rationale cell: VISION's north star is
    personal/companion (not infra/dev-tool), and the personal-productivity register is the practical mitigation for
    the confirmed docs.polytoken.dev name collision — with domain purchase / trademark filing / rename explicitly
    NOT done (user-gated). Outcome cell: point to `docs/design/brand-guide.md` as the repo-of-record. Keep it one
    row consistent with the table's voice.
  </action>
  <acceptance_criteria>
    - `.planning/PROJECT.md` Key Decisions table has a new row containing "Cortex" and "Phase 47": `grep -n "Phase 47" .planning/PROJECT.md` matches within the Key Decisions table. [automated]
    - The row notes the rename/domain/trademark are user-gated (not done) and points to `brand-guide.md`.
    - The table structure is intact (still a 3-column | Decision | Rationale | Outcome | table) — no other rows altered.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → committed history | brand-guide.md + PROJECT.md are committed; must carry no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-06 | Information disclosure | docs/design/brand-guide.md, PROJECT.md | mitigate | Docs record brand voice/posture only — no credentials, tokens, or private domains-as-secrets; reviewer confirms no secret values before commit |
| T-47-07 | Repudiation | naming-collision posture | mitigate | The guide explicitly records what was NOT done (no purchase/filing/rename) so a future reader cannot assume trademark/domain protection that does not exist |
</threat_model>

<verification>
- brand-guide.md exists (≥40 lines), references the bans doc + real mark asset, has a do/don't table + NOT-done list.
- PROJECT.md Key Decisions has the new Phase 47 brand row pointing to the guide.
</verification>

<success_criteria>
BRND-03 satisfied: the brand decision + naming-collision mitigation posture are recorded in-repo (brand guide + PROJECT.md), explicitly noting what was NOT done.
</success_criteria>

<output>
Create `.planning/phases/47-brand-foundation-verification-tooling/47-03-SUMMARY.md` when done.
</output>
