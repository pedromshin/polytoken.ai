---
phase: 16-studio-foundation-eval-harness-history-page-ideas-tabs
plan: 05
type: execute
wave: 3
depends_on: ["16-03", "16-04"]
files_modified:
  - apps/web/src/app/studio/_components/history-island.tsx
  - apps/web/src/app/studio/_components/studio-tabs.tsx
autonomous: false
requirements: [STDO-05, STDO-06]
user_setup: []

must_haves:
  truths:
    - "the History tab shows a newest-first master list of past generations (intent_text truncated, relative created_at, registry_version short badge, use_count, validation_status badge) with a pager (D-18, D-14 pagination)"
    - "clicking a row fires genui.historyById and re-renders the stored spec via the SHARED SpecRendererIsland in the SAME 55/45 ResizablePanelGroup render/JSON split lifted from the Sandbox (D-18, STDO-06)"
    - "the detail view is READ-ONLY: no Generate button, no editing; actions passed as the empty/no-op registry (D-18)"
    - "exactly ONE SpecRenderer island in apps/web is reused — NO second renderer is introduced (STDO-02 continued; grep gate)"
    - "the master list has empty + loading + error states (D-18)"
  artifacts:
    - path: "apps/web/src/app/studio/_components/history-island.tsx"
      provides: "History master list + detail (shared renderer, 55/45 split, read-only)"
      contains: "historyList"
    - path: "apps/web/src/app/studio/_components/studio-tabs.tsx"
      provides: "History TabsContent slot now renders HistoryIsland"
      contains: "HistoryIsland"
  key_links:
    - from: "apps/web/src/app/studio/_components/history-island.tsx"
      to: "genui.historyList / genui.historyById"
      via: "api.genui.historyList.useQuery + api.genui.historyById.useQuery"
      pattern: "historyById"
    - from: "apps/web/src/app/studio/_components/history-island.tsx"
      to: "apps/web/src/app/studio/_components/spec-renderer-island.tsx"
      via: "SHARED SpecRendererIsland for the detail re-render"
      pattern: "SpecRendererIsland"
---

<objective>
Land the History tab UI into the slot 16-04 created: a history-island.tsx with a newest-first paginated
master list (genui.historyList) and a detail view (genui.historyById) that re-renders the stored spec via the
SHARED SpecRendererIsland in the SAME 55/45 render/JSON split used by the Sandbox — read-only (no Generate, no
edit, empty/no-op action registry). Swap the History TabsContent placeholder in studio-tabs.tsx for HistoryIsland.

Purpose: STDO-05/06 — observe past generations from the persisted ui_spec_templates data and inspect any one
through the production renderer (STDO-02 reuse contract continued; never a second renderer).
Output: history-island.tsx (master + detail) + the studio-tabs.tsx slot swap.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-studio-foundation-eval-harness-history-page-ideas-tabs/16-CONTEXT.md

<interfaces>
tRPC procedures (from 16-03, packages/api-client/src/router/genui/history.ts):
  api.genui.historyList.useQuery({ limit?, offset?, importerId? }) -> array of
    { id, intentText, createdAt, registryVersion, useCount, validationStatus }   (no spec)
  api.genui.historyById.useQuery({ id }) -> { ...row, spec: SpecRoot } | null    (spec safeParsed, SAFE_FALLBACK on bad spec)

Shared renderer + split (reuse verbatim — STDO-02):
  SpecRendererIsland (apps/web/src/app/studio/_components/spec-renderer-island.tsx) — props: spec: SpecRoot, actions?: ActionRegistry.
  The 55/45 ResizablePanelGroup render/JSON split pattern is in generation-sandbox-island.tsx (ResizablePanelGroup
    direction horizontal; left ResizablePanel defaultSize 55 -> SpecRendererIsland; ResizableHandle; right ResizablePanel
    defaultSize 45 -> a Spec JSON ScrollArea/pre). Copy this layout shape for the detail; do NOT fork a new renderer.

Empty/no-op actions: SpecRendererIsland's `actions` is optional; OMIT it for read-only inspection (defaults to the empty
  context, all action ids resolve to noop) — no buildActionRegistry needed in the detail (D-18 read-only).

studio-tabs.tsx (from 16-04): the History TabsContent (value "history") currently holds a placeholder; HistoryIsland
  replaces that child. StudioTabs is already controlled (16-04). tRPC client: import { api } from "~/trpc/react".

@nauta/ui primitives: Badge (./badge), Button (./button) for the pager, Card (./card), ScrollArea (./scroll-area),
  ResizablePanelGroup/ResizablePanel/ResizableHandle (./resizable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: history-island.tsx — master list + read-only detail (shared renderer, 55/45)</name>
  <files>apps/web/src/app/studio/_components/history-island.tsx</files>
  <read_first>
    - apps/web/src/app/studio/_components/generation-sandbox-island.tsx (the 55/45 ResizablePanelGroup render/JSON split to copy + the SpecRendererIsland usage; the loading/empty/error chrome patterns)
    - apps/web/src/app/studio/_components/spec-renderer-island.tsx (the SHARED renderer — props spec + optional actions; OMIT actions for read-only)
    - apps/web/src/app/studio/_components/catalog-browser-island.tsx ("use client" island + sub-component-under-50-lines + named-export style)
    - packages/api-client/src/router/genui/history.ts (the historyList/historyById output shapes from 16-03)
    - 16-CONTEXT.md D-14, D-18, STDO-02/05/06
  </read_first>
  <action>
    Create history-island.tsx ("use client", named export HistoryIsland). State: selectedId (string | undefined) and
    offset (number, default 0) for the pager. Master list: api.genui.historyList.useQuery({ limit: 20, offset }) — render
    a newest-first list where each row shows intentText (truncated with title/aria for full text), a relative createdAt
    (a small relative-time helper or Intl), a registryVersion short badge (slice 0,8), useCount, and a validationStatus
    Badge. Provide loading (skeleton/spinner), empty ("No generations yet."), and error (role=alert) states (D-18).
    Provide a pager — "Load more" (offset += 20, append) OR prev/next page buttons (Claude's discretion per D-18); guard
    the next control when fewer than `limit` rows came back. Clicking a row sets selectedId. Detail: when selectedId is
    set, api.genui.historyById.useQuery({ id: selectedId }, { enabled: selectedId !== undefined }) — render the result in
    the SAME 55/45 ResizablePanelGroup as the Sandbox: left ResizablePanel defaultSize 55 -> SpecRendererIsland spec=
    detail.spec (NO actions prop — read-only, D-18); ResizableHandle; right ResizablePanel defaultSize 45 -> a Spec JSON
    ScrollArea/pre of detail.spec. Handle the null detail (e.g. deleted/unparseable -> SAFE_FALLBACK from 16-03) with a
    small notice. NO Generate button, NO edit controls (D-18 read-only). Reuse the SHARED SpecRendererIsland ONLY — do not
    create a second renderer (STDO-02). Reuse Phase-12 tokens; no new tokens. No eval/Function/dangerouslySetInnerHTML.
    Immutable state updates; sub-components under 50 lines; named exports.
  </action>
  <verify>
    <automated>cd apps/web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - HistoryIsland uses api.genui.historyList + api.genui.historyById (grep both); historyById is enabled only when a row is selected.
    - The detail reuses SpecRendererIsland (import from ./spec-renderer-island) inside a 55/45 ResizablePanelGroup; the JSON panel mirrors the Sandbox (grep ResizablePanelGroup + SpecRendererIsland).
    - No actions/buildActionRegistry in the detail (read-only, D-18) and no Generate/edit controls (grep: no genui.generate, no Generate button in history-island).
    - Master list has loading + empty + error states; apps/web typechecks.
  </acceptance_criteria>
  <done>History master list + read-only detail (shared renderer, 55/45 split) exists with all states; no second renderer.</done>
</task>

<task type="auto">
  <name>Task 2: wire HistoryIsland into the studio-tabs History slot</name>
  <files>apps/web/src/app/studio/_components/studio-tabs.tsx</files>
  <read_first>
    - apps/web/src/app/studio/_components/studio-tabs.tsx (the History TabsContent placeholder slot from 16-04 — controlled Tabs; replace only the History child)
    - apps/web/src/app/studio/_components/history-island.tsx (HistoryIsland from Task 1)
    - 16-CONTEXT.md D-18
  </read_first>
  <action>
    Import HistoryIsland and replace the History TabsContent (value "history") placeholder child with HistoryIsland.
    Change NOTHING else in studio-tabs.tsx — the controlled Tabs, pendingIntent lift, Catalog/Sandbox/Page-Ideas content,
    and the initialIntent wiring are all owned by 16-04 and stay intact (this is a minimal one-child swap to avoid
    re-touching the lift). Keep the TabsContent flex/scroll classes consistent with the other tabs.
  </action>
  <verify>
    <automated>cd apps/web; npx tsc --noEmit; npx next build 2>&1 | tail -20; grep -rc "dynamic(" src/app/studio/_components/spec-renderer-island.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The History TabsContent renders HistoryIsland (grep HistoryIsland in studio-tabs.tsx).
    - apps/web typechecks AND next build succeeds.
    - STDO-02 holds: exactly ONE dynamic(ssr:false) SpecRenderer wrapper still lives in spec-renderer-island.tsx; History reuses it (no new dynamic SpecRenderer import elsewhere — grep dynamic across studio/_components shows the single wrapper).
  </acceptance_criteria>
  <done>The History tab renders the live HistoryIsland; build green; the single shared renderer is reused.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Browser-verify the History tab (list + read-only shared-renderer detail)</name>
  <action>Human-only browser verification AFTER the autonomous tasks complete: confirm the History list, pager, and read-only shared-renderer 55/45 detail (plus safe-fallback degrade) work against a live backend (see how-to-verify). No code change.</action>
  <what-built>
    The History tab: a newest-first paginated list of past generations from ui_spec_templates, and a read-only
    detail that re-renders the stored spec through the SHARED production SpecRenderer in the 55/45 render/JSON split.
    Live data needs a running FastAPI backend (the list/detail endpoints from 16-03), so this needs a browser eyeball.
  </what-built>
  <how-to-verify>
    1. Ensure the email-listener FastAPI service is running with EMAIL_LISTENER_URL + EMAIL_LISTENER_API_KEY set for apps/web (the same config the Sandbox uses), and that ui_spec_templates has at least one row (generate something in the Sandbox first if empty)
    2. cd apps/web && npm run dev, open http://localhost:3000/studio, click the "History" tab
    3. Confirm the master list shows past generations newest-first with intent text, a relative timestamp, a registry-version short badge, use_count, and a validation_status badge
    4. Confirm the pager works (Load more / prev-next) and that empty/loading states look right (empty if no rows yet)
    5. Click a row — confirm the detail re-renders the stored spec on the left and its spec JSON on the right in the 55/45 split, identical in look to the Sandbox output, and that there is NO Generate button and NO edit control (read-only)
    6. Confirm a row whose stored spec no longer parses degrades to the safe fallback rather than crashing the detail
  </how-to-verify>
  <resume-signal>Type "approved" once the History list + read-only shared-renderer detail work against a live backend, or describe issues (or "defer" if no backend/data is available now).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tRPC -> browser | History rows + stored specs cross into the client; specs are re-rendered |
| stored spec -> renderer | A historical spec may no longer parse under the current schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-T | Tampering | re-rendering a stored spec | mitigate | The detail renders only via the SHARED SpecRendererIsland; genui.historyById already safeParses spec_json at the web boundary (16-03 / D-17) and degrades to SAFE_FALLBACK_SPEC — no spec-validation bypass, no second renderer |
| T-16-05-I | Information Disclosure | history list contents | mitigate | List shows intent_text + metadata only (no secrets); the importer scope is applied at the endpoint (16-03); EMAIL_LISTENER_API_KEY stays server-side (tRPC proxy) |
| T-16-05-E | Elevation of Privilege | detail action surface | mitigate | The detail passes NO action registry (read-only, D-18); no Generate/mutate path is exposed from History |
| T-16-05-SC | Tampering | npm installs | mitigate | No new npm packages (reuses @nauta/ui + the existing shared island + tRPC client) — package-legitimacy gate N/A |
</threat_model>

<verification>
- apps/web: tsc clean + next build green.
- grep: History uses api.genui.historyList + historyById; reuses SpecRendererIsland; passes no actions in the detail; no Generate/edit; exactly one dynamic SpecRenderer wrapper (STDO-02).
- Browser checkpoint: newest-first list + pager + read-only 55/45 detail through the shared renderer + safe-fallback degrade verified against a live backend.
</verification>

<success_criteria>
STDO-05 (History lists previous generations from the persisted data) and STDO-06 (an individual generation detail
re-renders the stored spec via the shared production SpecRenderer beside its spec JSON, 55/45 split, no second renderer)
are delivered as a read-only tab, continuing the STDO-02 reuse contract.
</success_criteria>

<output>
Create `.planning/phases/16-studio-foundation-eval-harness-history-page-ideas-tabs/16-05-SUMMARY.md` when done
</output>
