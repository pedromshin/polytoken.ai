---
phase: 27-adopted-external-design-picks
plan: 04
type: execute
wave: 2
depends_on: [27-03]
files_modified:
  - apps/web/src/components/generating-ring.tsx
  - apps/web/src/components/generating-ring.test.tsx
  - apps/web/src/app/studio/_components/generation-sandbox-island.tsx
  - apps/web/src/app/chat/_components/message-turn.tsx
autonomous: true
requirements: [ADOPT-03]
tags: [react, generating-ring, studio, chat, motion]

must_haves:
  truths:
    - "GeneratingRing wraps its children in a div and applies the .generating-ring class ONLY when active === true"
    - "Studio Generation Sandbox rings the #sandbox-output-region while chromeProps.isPending; the honest 'Generating…' label is unchanged"
    - "Chat rings ONLY the streaming GenuiPartBoundary sites (genui_spec_streaming + interactive_widget_streaming); the finalized genui_spec case is not ringed"
    - "GenuiPartBoundary and InteractiveWidgetBoundary are NOT edited — the ring wraps from message-turn (the caller)"
  artifacts:
    - path: "apps/web/src/components/generating-ring.tsx"
      provides: "GeneratingRing wrapper primitive consuming the .generating-ring utility"
      exports: ["GeneratingRing", "GeneratingRingProps"]
    - path: "apps/web/src/components/generating-ring.test.tsx"
      provides: "Colocated vitest: active toggles the class, decorative-only, children render"
    - path: "apps/web/src/app/studio/_components/generation-sandbox-island.tsx"
      provides: "Studio ring mount around #sandbox-output-region"
      contains: "GeneratingRing"
    - path: "apps/web/src/app/chat/_components/message-turn.tsx"
      provides: "Chat ring mount around the two streaming GenuiPartBoundary call sites"
      contains: "GeneratingRing"
  key_links:
    - from: "apps/web/src/components/generating-ring.tsx"
      to: ".generating-ring (globals.css, Plan 03)"
      via: "cn(active && 'generating-ring', className)"
      pattern: "generating-ring"
    - from: "generation-sandbox-island.tsx"
      to: "chromeProps.isPending"
      via: "GeneratingRing active prop"
      pattern: "active=\\{"
---

<objective>
Land the ADOPT-03 component + mounts: create `apps/web/src/components/generating-ring.tsx` (a trivial
teal ring wrapper that applies the `.generating-ring` utility from Plan 03 only while `active`), with a
colocated test, then mount it at two consumer sites — (a) Studio Generation Sandbox around
`#sandbox-output-region` driven by `chromeProps.isPending`, and (b) Chat `message-turn.tsx` around ONLY
the two streaming `GenuiPartBoundary` call sites.

Purpose: visibly mark "generating" state in Chat and the Studio sandbox tab, teal-only and
motion-safe, WITHOUT touching the locked `GenuiPartBoundary`/`InteractiveWidgetBoundary` chrome.
Output: 2 new files + 2 mount edits. Depends on Plan 03 (the `.generating-ring` CSS must exist).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-adopted-external-design-picks/27-UI-SPEC.md
@.planning/phases/27-adopted-external-design-picks/27-CONTEXT.md
@apps/web/src/app/studio/_components/generation-sandbox-island.tsx
@apps/web/src/app/chat/_components/message-turn.tsx

<interfaces>
<!-- GeneratingRing contract to build (from 27-UI-SPEC.md § ADOPT-03). cn from "@nauta/ui". -->
GeneratingRingProps: { readonly active: boolean; readonly className?: string; readonly children: React.ReactNode }
export function GeneratingRing({ active, className, children }: GeneratingRingProps): React.ReactElement
<!-- Body: <div className={cn(active && "generating-ring", className)}>{children}</div>
     border-radius: inherit means the consumer sets its own rounding on the SAME element (pass className="rounded-lg"). -->
<!-- Colocated test convention: mirror apps/web/src/app/studio/_components/json-pane.test.tsx (createRoot + act). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the GeneratingRing wrapper primitive (+ colocated test)</name>
  <files>apps/web/src/components/generating-ring.tsx, apps/web/src/components/generating-ring.test.tsx</files>
  <read_first>
    - 27-UI-SPEC.md section "ADOPT-03 — GeneratingRing" -> the Component block (GeneratingRingProps, the `cn(active && "generating-ring", className)` body) and the Accessibility note (decorative-only, pointer-events:none on the pseudo-element, never the sole in-progress signal)
    - apps/web/src/app/globals.css tail (the `.generating-ring` utility this component consumes — created by Plan 03; `border-radius: inherit` means the consumer must set its own rounding on the ringed element)
    - apps/web/src/app/studio/_components/json-pane.test.tsx (colocated createRoot+act vitest convention)
    - packages/ui/src/command.tsx line 8 (the `import { cn } from "@nauta/ui"` pattern)
  </read_first>
  <behavior>
    - active === true -> the wrapper div's className contains `generating-ring`
    - active === false -> the wrapper div's className does NOT contain `generating-ring`
    - the `className` prop is always merged (e.g. `rounded-lg` survives regardless of active)
    - children always render inside the wrapper
    - no ARIA role / no click handler is added (decorative-only)
  </behavior>
  <action>
    Create `apps/web/src/components/generating-ring.tsx` (`"use client"` not required — it is a pure
    presentational wrapper, but add it if the file needs it for consistency). Attribution header
    required (Magic UI shine-border + animated-shiny-text technique, MIT, "Fetched: 2026-07-06" — same
    provenance as the Plan 03 CSS). Implement exactly the `<interfaces>` contract: export
    `GeneratingRingProps` and `GeneratingRing`, rendering `<div className={cn(active &&
    "generating-ring", className)}>{children}</div>` with `cn` imported from `@nauta/ui`. Add no ARIA
    role and no handlers (decorative-only). Write colocated `generating-ring.test.tsx` mirroring
    json-pane.test.tsx's createRoot+act convention covering the 5 behaviors above (assert the wrapper
    className toggles `generating-ring` on `active`, that `className` merges, and that children render).
  </action>
  <acceptance_criteria>
    - `grep -q "generating-ring" apps/web/src/components/generating-ring.tsx` and `grep -q 'from "@nauta/ui"' apps/web/src/components/generating-ring.tsx`
    - `grep -qE 'export (function|interface) (GeneratingRing|GeneratingRingProps)' apps/web/src/components/generating-ring.tsx`
    - `head -12 apps/web/src/components/generating-ring.tsx | grep -iqE "shine-border|animated-shiny|magic ?ui"` (attribution header)
    - `npx vitest run --root apps/web generating-ring` exits 0
    - `npm run typecheck --workspace=@nauta/web` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --root apps/web generating-ring && npm run typecheck --workspace=@nauta/web</automated>
  </verify>
  <done>generating-ring.tsx exports the wrapper (attributed, decorative-only) and toggles `.generating-ring` on `active`; colocated tests pass; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Mount GeneratingRing in Studio (sandbox) and Chat (streaming parts)</name>
  <files>apps/web/src/app/studio/_components/generation-sandbox-island.tsx, apps/web/src/app/chat/_components/message-turn.tsx</files>
  <read_first>
    - apps/web/src/app/studio/_components/generation-sandbox-island.tsx lines 264-269 (`chromeProps.isPending` derivation) and lines 351-405 (the `#sandbox-output-region` div to wrap; `GenerationStateChrome` and its "Generating…" label stay unchanged)
    - apps/web/src/app/chat/_components/message-turn.tsx lines 139-200 (the part branches — wrap ONLY `genui_spec_streaming` (149-157) and `interactive_widget_streaming` (188-200); do NOT wrap the finalized `genui_spec` at 139-147 or the `interactive_widget` at 160-176)
    - 27-UI-SPEC.md section "ADOPT-03 — GeneratingRing" -> "Mount (a) — Studio", "Mount (b) — Chat", and "Mount (c) — explicitly NOT mounted" (history tab excluded — reasoned scope exclusion, not a gap)
    - 27-CONTEXT.md "ADOPT-03 GeneratingRing" (D-02: keep the honest "Generating…" label; chat wraps from the PARENT — GenuiPartBoundary is LOCKED)
  </read_first>
  <action>
    STUDIO: in `generation-sandbox-island.tsx`, import `GeneratingRing` from `~/components/generating-ring`
    and wrap the `#sandbox-output-region` div (the `showChrome && (<div id="sandbox-output-region" …>`)
    with `<GeneratingRing active={chromeProps.isPending} className="rounded-lg">…</GeneratingRing>`.
    Leave `GenerationStateChrome` and the honest "Generating…" label untouched (D-02) — the ring is an
    additional visual signal around the pane, not a replacement. CHAT: in `message-turn.tsx`, import
    `GeneratingRing` from `~/components/generating-ring` and wrap ONLY the two streaming
    `GenuiPartBoundary` call sites — the `part.type === "genui_spec_streaming"` return and the
    `part.type === "interactive_widget_streaming"` return — each with `<GeneratingRing active
    className="rounded-lg">…</GeneratingRing>`. Do NOT wrap the finalized `genui_spec` branch
    (isStreaming=false) or the interactive_widget branch. Do NOT edit `genui-part-boundary.tsx` or
    `interactive-widget-boundary.tsx` — the ring wraps from message-turn (the caller) ONLY. Keep the
    existing `key={index}` on each wrapped element (move it to the outer GeneratingRing or keep the
    boundary keyed inside — ensure React key stability). Do NOT mount on the Studio history tab
    (reasoned exclusion per the UI-SPEC).
  </action>
  <acceptance_criteria>
    - `grep -q 'GeneratingRing' apps/web/src/app/studio/_components/generation-sandbox-island.tsx` AND `grep -q 'active={chromeProps.isPending}' apps/web/src/app/studio/_components/generation-sandbox-island.tsx`
    - `grep -q 'GeneratingRing' apps/web/src/app/chat/_components/message-turn.tsx`
    - `git diff --name-only | grep -qE 'genui-part-boundary|interactive-widget-boundary' && exit 1 || true` (locked boundary files NOT edited)
    - `grep -q 'spec-renderer' <(git diff apps/web packages/genui) ; test $? -ne 0` conceptually — spec-renderer.tsx untouched (it is not in files_modified)
    - `! grep -rq 'font-medium' apps/web/src/app/chat apps/web/src/app/studio` (no font-medium regression)
    - `npm run typecheck --workspace=@nauta/web` exits 0
    - `npx vitest run --root apps/web` exits 0 (full web suite — chat + studio, no regression)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'GeneratingRing' apps/web/src/app/studio/_components/generation-sandbox-island.tsx && grep -q 'GeneratingRing' apps/web/src/app/chat/_components/message-turn.tsx && npm run typecheck --workspace=@nauta/web && npx vitest run --root apps/web</automated>
  </verify>
  <done>Studio sandbox rings #sandbox-output-region on isPending (label intact); Chat rings only the 2 streaming part sites; GenuiPartBoundary/InteractiveWidgetBoundary/spec-renderer untouched; full web suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message-turn (caller) → GenuiPartBoundary (locked) | the ring must wrap FROM the caller, never reach inside the locked boundary components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-01 | Tampering | GenuiPartBoundary / InteractiveWidgetBoundary / spec-renderer (LOCKED) | mitigate | wrap the ring from message-turn only; acceptance asserts those files are NOT in this plan's diff (non-interference, Phase 24/v1.4 constraint) |
| T-27-04-02 | Denial of Service (a11y) | reduced-motion users | mitigate | ring is decorative-only (pointer-events:none, no ARIA); motion gated in Plan 03's CSS; both mounts retain an independent accessible signal (aria-live "Generating…" / streaming skeleton) |
| T-27-04-SC | Tampering | source provenance | accept | no NEW external fetch in this plan — the shine-border technique was fetched + vetted in Plan 03; generating-ring.tsx carries the same attribution header for traceability |
</threat_model>

<verification>
- `git diff --name-only` limited to the 4 declared files — genui-part-boundary.tsx, interactive-widget-boundary.tsx, spec-renderer.tsx NOT present.
- Studio ring driven by `chromeProps.isPending`; Chat ring only on the 2 streaming branches; history tab not ringed.
- Full web vitest suite + web typecheck green; chat/studio `font-medium` count still 0.
</verification>

<success_criteria>
- ROADMAP Phase 27 success criterion 3 (teal-only, motion-safe GeneratingRing visibly marks "generating" state on genui cards in Chat and on the sandbox tab in Studio) — MET (CSS from Plan 03, component + mounts here).
</success_criteria>

<output>
Create `.planning/phases/27-adopted-external-design-picks/27-04-SUMMARY.md` when done.
</output>
