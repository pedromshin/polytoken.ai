---
phase: 25-anticipatory-prompting-spike
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - apps/email-listener/app/application/use_cases/__tests__/test_anticipatory_spike_harness.py
  - .planning/phases/25-anticipatory-prompting-spike/25-SPIKE-FINDINGS.md
autonomous: true
requirements: [ANTIC-01, ANTIC-02]
tags: [anticipatory-prompting, spike, findings, go-no-go]

must_haves:
  truths:
    - "The full pipeline (triggers → appropriateness eval → frequency cap → explicit-accept mapping → lifecycle) runs deterministically over all three fixtures and produces a reproducible outcome matrix (D-02)."
    - "The matrix demonstrates every gate outcome: shown, suppressed_by_eval, suppressed_by_cap, and flag-OFF-produces-nothing."
    - "25-SPIKE-FINDINGS.md states one explicit go/no-go verdict (ship | don't-ship | ship-with-conditions) grounded in the fixture evidence (D-03)."
    - "The findings enumerate the named seams a shippable feature would require (live-Bedrock eval, run-event/table persistence, live observation adapter, live web wiring, dismissal-cooldown durability)."
  artifacts:
    - path: "apps/email-listener/app/application/use_cases/__tests__/test_anticipatory_spike_harness.py"
      provides: "The deterministic end-to-end harness producing the go/no-go evidence matrix"
      contains: "def test_"
    - path: ".planning/phases/25-anticipatory-prompting-spike/25-SPIKE-FINDINGS.md"
      provides: "The phase deliverable — explicit go/no-go recommendation + evidence + seams (D-03)"
      contains: "## Verdict"
  key_links:
    - from: "apps/email-listener/app/application/use_cases/__tests__/test_anticipatory_spike_harness.py"
      to: "apps/email-listener/app/application/use_cases/evaluate_anticipatory_candidates.py"
      via: "the harness drives the real EvaluateAnticipatoryCandidates over the 3 fixtures with the stub judge + in-memory cap store"
      pattern: "EvaluateAnticipatoryCandidates"
    - from: ".planning/phases/25-anticipatory-prompting-spike/25-SPIKE-FINDINGS.md"
      to: "the harness outcome matrix"
      via: "the verdict cites the specific per-fixture outcome counts the harness asserts"
      pattern: "matrix"
---

<objective>
Produce the SPIKE's actual deliverable (D-03): a `25-SPIKE-FINDINGS.md` with an explicit go/no-go
recommendation, grounded in what the flag-gated prototype revealed when driven end-to-end against the
three scripted fixtures. First build a deterministic harness that runs the FULL pipeline
(triggers → eval → cap → explicit-accept mapping → lifecycle) across a matrix of appropriateness
scores and cap states and captures the per-fixture outcomes; then write the findings that cite that
evidence.

Purpose: The phase's exit criterion is a documented decision, not shipped code (D-03). The pipeline
built in Plans 25-01/25-02 is the EVIDENCE; this plan turns that evidence into a defensible verdict
and a named list of seams a real feature would need.

Output: The end-to-end spike harness test + the `25-SPIKE-FINDINGS.md` go/no-go document.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-anticipatory-prompting-spike/25-CONTEXT.md
@.planning/phases/25-anticipatory-prompting-spike/25-01-SUMMARY.md
@.planning/phases/25-anticipatory-prompting-spike/25-02-SUMMARY.md

<interfaces>
From Plan 25-01 / 25-02 (what the harness composes):
```python
# app/domain/anticipatory/fixtures.py: idle_after_genui_snapshot(), completed_artifact_snapshot(), ambiguous_intent_snapshot()
# app/domain/anticipatory/stubs.py: StubAppropriatenessJudge(score=...)
# app/infrastructure/anticipatory/in_memory_cap_store.py: InMemoryAnticipatoryCapStore() (+ seed(...))
# app/application/use_cases/evaluate_anticipatory_candidates.py:
#   EvaluateAnticipatoryCandidates(judge=..., cap_store=...).evaluate(snapshot, enabled=..., ...)
#   -> AnticipatoryPipelineResult(shown, events)   # events: ordered AnticipatoryLifecycleEvent
#   to_proposal_card_declaration(candidate), record_candidate_outcome(candidate, outcome, ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end spike harness — the deterministic go/no-go evidence matrix</name>
  <files>apps/email-listener/app/application/use_cases/__tests__/test_anticipatory_spike_harness.py</files>
  <read_first>
    - apps/email-listener/app/application/use_cases/evaluate_anticipatory_candidates.py (the use case + result shape + to_proposal_card_declaration + record_candidate_outcome)
    - apps/email-listener/app/domain/anticipatory/fixtures.py + stubs.py + app/infrastructure/anticipatory/in_memory_cap_store.py (the composable pieces)
    - apps/email-listener/app/application/use_cases/__tests__/test_evaluate_anticipatory_candidates.py (25-02 patterns to reuse — do not duplicate its unit assertions; this is the integrated matrix)
  </read_first>
  <action>
    Build a single integrated harness test module that runs the REAL EvaluateAnticipatoryCandidates
    over each of the three fixtures across a small scenario matrix and captures the outcomes — this is
    the raw material for the findings' false-positive-rate argument (D-13/D-03). For each fixture ×
    each scenario record the terminal lifecycle outcome:
      - Scenario A (appropriate + cap room): StubAppropriatenessJudge(score=0.9), empty cap → expect `shown`.
      - Scenario B (appropriate + capped): score=0.9, cap seeded at the per-window limit → expect `suppressed_by_cap`.
      - Scenario C (inappropriate + cap room): score=0.3, empty cap → expect `suppressed_by_eval`.
      - Scenario D (flag OFF): enabled=False → expect zero candidates / zero events for every fixture.
    Assert every expected outcome (deterministic — no live Bedrock). Also assert, for a `shown`
    candidate, that `to_proposal_card_declaration` yields a valid Phase-24 proposal-card declaration and
    that `record_candidate_outcome(candidate, "dismissed")` registers the trigger cooldown (explicit-
    accept + dismissal, D-11). Build the outcome matrix as an in-test data structure (fixture × scenario
    → outcome) and expose it via a `build_spike_outcome_matrix()` helper + a `pytest` `-s` print (or a
    structured log) so Task 2 can transcribe the exact numbers into the findings. Keep it domain-pure
    at the import level (application + domain + the two spike adapters only; no live infra/DB).
  </action>
  <acceptance_criteria>
    - `cd apps/email-listener && uv run pytest app/application/use_cases/__tests__/test_anticipatory_spike_harness.py -x --no-cov -s` passes and prints/logs the fixture×scenario outcome matrix.
    - The matrix covers all three fixtures (idle_after_genui, completed_artifact, ambiguous_intent) × scenarios A–D, and every asserted outcome matches the D-08/D-10/D-07/D-12 expectations above.
    - The `shown` path is asserted to produce a Phase-24-compatible proposal-card declaration (D-11) and the dismissal path registers a cooldown.
    - The test module imports only `app.application.*`, `app.domain.*`, and the two spike adapters — no live Supabase/Bedrock (`uv run lint-imports` clean; no network).
  </acceptance_criteria>
  <verify>
    <automated>cd apps/email-listener && uv run pytest app/application/use_cases/__tests__/test_anticipatory_spike_harness.py -x --no-cov -s</automated>
  </verify>
  <done>A deterministic end-to-end harness produces a reproducible fixture×scenario outcome matrix exercising every gate outcome — the evidence the go/no-go rests on.</done>
</task>

<task type="auto">
  <name>Task 2: Write 25-SPIKE-FINDINGS.md — explicit go/no-go + evidence + seams (D-03)</name>
  <files>.planning/phases/25-anticipatory-prompting-spike/25-SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/25-anticipatory-prompting-spike/25-CONTEXT.md (D-01..D-14 — the decisions the findings must reconcile; the Deferred Ideas the findings frame as future work)
    - .planning/phases/25-anticipatory-prompting-spike/25-01-SUMMARY.md + 25-02-SUMMARY.md (what was actually built + any deviations/chosen tuning numbers)
    - apps/email-listener/app/application/use_cases/__tests__/test_anticipatory_spike_harness.py (the Task-1 outcome matrix to transcribe)
    - .planning/research/v1.3/V1.3-RESEARCH-SYNTHESIS.md §R3 (the greenfield framing + "false-positive prompting is the main risk" the verdict must address)
  </read_first>
  <action>
    Write `25-SPIKE-FINDINGS.md` (no fenced code implementations — prose + tables). Required sections:
      - `## Verdict` — ONE explicit recommendation token: `ship` | `don't-ship` | `ship-with-conditions`.
        Follow it with a 2–4 sentence justification grounded in the harness evidence. (The evidence
        supports `ship-with-conditions` — the mechanism is sound and false-positive-averse, but live
        measurement + persistence + web wiring are unproven; the author should state the verdict the
        evidence actually supports, and if `ship-with-conditions`, list the conditions.)
      - `## What was built` — the real, dark, flag-gated pipeline: triggers (D-04), typed candidate
        (D-05), appropriateness eval (D-07/D-09), independent frequency cap (D-10/D-14 no-table
        decision), explicit-accept reuse of the Phase-24 proposal card (D-11), lifecycle log (D-13),
        global off switch (D-12). Name the files.
      - `## Evidence (fixture matrix)` — a dated table transcribing the Task-1 harness outcomes
        (fixture × scenario → outcome), plus the false-positive posture (both gates suppress by
        default; judge fails toward suppress; flag OFF ⇒ zero), the cost posture (Haiku, max_tokens
        256, cap-first short-circuit skips the paid call, D-09), and the intrusiveness posture
        (nothing fires without an explicit click, dismissal cooldown).
      - `## Seams a shippable feature needs` — a NUMBERED list of the concrete, named deferrals: (1)
        live-Bedrock appropriateness scoring + a real false-positive-rate study on live traffic
        (deferred per EVAL-LIFT/ISO-RUN convention); (2) durable cap + lifecycle persistence — the
        run-event projection (or dedicated table) that makes D-10 "survive reload" real; (3) the live
        observation adapter assembling AnticipatoryStateSnapshot from real Phase-22 run events +
        Phase-23 canvas store + Phase-24 widget-interaction state (the spike used fixtures); (4) live
        web wiring of a shown candidate into the existing Phase-24 proposal-card emit path; (5)
        dismissal-cooldown durability across reload. Map each to the CONTEXT Deferred Ideas.
      - `## Risks & open questions` — false-positive risk on live traffic, threshold/cap tuning
        (document the chosen 0.75 / 1-per-10min / 3-per-day values + rationale), judge cost at scale.
    Acceptance is structural + substantive: exactly one verdict token, a dated evidence table citing
    the harness, and a numbered seams list.
  </action>
  <acceptance_criteria>
    - The file exists with a `## Verdict` section whose first content line is exactly one of `ship`, `don't-ship`, or `ship-with-conditions` (grep: `grep -Eiq '^(ship|don.t-ship|ship-with-conditions)' 25-SPIKE-FINDINGS.md` after the Verdict header).
    - Contains a `## Evidence (fixture matrix)` section with a markdown table naming all three fixtures and referencing the harness outcomes.
    - Contains a `## Seams a shippable feature needs` section with a numbered list of ≥4 named seams, each traceable to a CONTEXT Deferred Idea or a D-XX decision.
    - The chosen tuning values (appropriateness threshold, cap windows) appear with a documented rationale (Claude's-discretion notes in D-04/D-10 require this).
    - No fenced code-block implementations in the doc (prose + tables only).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/25-anticipatory-prompting-spike/25-SPIKE-FINDINGS.md && grep -qi "## Verdict" .planning/phases/25-anticipatory-prompting-spike/25-SPIKE-FINDINGS.md && grep -qi "Seams" .planning/phases/25-anticipatory-prompting-spike/25-SPIKE-FINDINGS.md</automated>
  </verify>
  <done>25-SPIKE-FINDINGS.md exists with one explicit go/no-go verdict grounded in the fixture matrix and a numbered list of the seams a real feature would require — the phase's D-03 exit criterion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (documentation only) | This plan runs the already-gated pipeline over trusted fixtures and writes a findings doc — no new runtime trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08 | Information Disclosure | 25-SPIKE-FINDINGS.md | mitigate | The findings cite fixture-derived, synthetic outcomes only — no real user chat content or PII (the harness uses scripted fixtures, not live conversations). |
| T-25-SC | Tampering | dependency surface | mitigate | No package installs — the harness composes existing spike modules; the findings are prose. |
</threat_model>

<verification>
- `cd apps/email-listener && uv run pytest app/application/use_cases/__tests__/test_anticipatory_spike_harness.py -x --no-cov -s` — the matrix is reproducible and green.
- `25-SPIKE-FINDINGS.md` contains exactly one verdict token, a dated evidence table, and a numbered seams list.
- `packages/genui/src/renderer/spec-renderer.tsx` untouched; no web files modified (HARD CONSTRAINT).
</verification>

<success_criteria>
The SPIKE concludes (D-03): a deterministic end-to-end run over all three fixtures proves the gate
chain behaves as designed, and `25-SPIKE-FINDINGS.md` delivers an explicit go/no-go recommendation
grounded in that evidence plus the named seams a shippable feature would require. This is the phase's
exit criterion — a documented decision, not a shipped guarantee.
</success_criteria>

<output>
Create `.planning/phases/25-anticipatory-prompting-spike/25-03-SUMMARY.md` when done.
</output>
