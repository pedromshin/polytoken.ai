---
phase: 54-email-cluster-workflow-e3
plan: 07
type: execute
wave: 4
depends_on: [54-01, 54-02, 54-03, 54-04, 54-05, 54-06]
autonomous: true
requirements: [CLUS-07]
files_modified:
  - .planning/phases/49-live-loop-gate-deploy-oauth-real-email/MORNING-CHECKLIST.md

must_haves:
  truths:
    - "MORNING-CHECKLIST.md has a §H section that is the full CLUS-07 live-acceptance runsheet on the user's real inbox"
    - "§H lists its prerequisites: §A-§G done, migration 0036 applied local→staging→prod, and WEB_SEARCH_TOOL_ENABLED true only after the adversarial suite is green"
    - "§H walks the ONE scenario end to end: real thread → attach chat → web research with thread in context → capture sources → promote → new chat sees cluster context"
    - "§H is a runsheet ONLY — the scenario is user-executed live in the morning and is never faked/marked-passed tonight"
  artifacts:
    - path: .planning/phases/49-live-loop-gate-deploy-oauth-real-email/MORNING-CHECKLIST.md
      provides: "§H CLUS-07 live-acceptance runsheet"
      contains: "## H."
  key_links:
    - from: .planning/phases/49-live-loop-gate-deploy-oauth-real-email/MORNING-CHECKLIST.md
      to: "migration 0036 + WEB_SEARCH_TOOL_ENABLED prerequisites"
      via: "§H prerequisite list"
      pattern: "0036|WEB_SEARCH_TOOL_ENABLED"
---

<objective>
Author the §H live-acceptance runsheet for CLUS-07 — the milestone's acceptance
bar — appended to the existing consolidated MORNING-CHECKLIST.md. This plan
produces ONLY the runsheet entry; the scenario itself is user-executed live in the
morning and is never faked or marked passed tonight (Docker/WSL down; no OAuth;
no applied 0036; no real inbox this session).

Purpose: CLUS-07 is a live acceptance gate, not a code-built item — the depth-first
mandate says one scenario fully working live beats breadth. §H makes that scenario
executable in the morning with every prerequisite spelled out.
Output: the §H section in MORNING-CHECKLIST.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-email-cluster-workflow-e3/54-CONTEXT.md
@.planning/phases/49-live-loop-gate-deploy-oauth-real-email/MORNING-CHECKLIST.md
@.planning/phases/54-email-cluster-workflow-e3/54-01-SUMMARY.md
@.planning/phases/54-email-cluster-workflow-e3/54-02-SUMMARY.md
@.planning/phases/54-email-cluster-workflow-e3/54-03-SUMMARY.md
@.planning/phases/54-email-cluster-workflow-e3/54-04-SUMMARY.md
@.planning/phases/54-email-cluster-workflow-e3/54-05-SUMMARY.md
@.planning/phases/54-email-cluster-workflow-e3/54-06-SUMMARY.md

<interfaces>
MORNING-CHECKLIST.md structure: `## A.` … `## G.` sections; §G ("Docker/WSL recovery + queued live-stack verification") is the last section (line ~419), and §G already collects "queued to §G" items from Phases 52-54. §H is the next section header: `## H. Phase-54 Email-Cluster Workflow — live acceptance scenario (CLUS-07)`.

CLUS-07 scenario text (ROADMAP SC7 + 54-CONTEXT.md): real thread → attach chat → web research WITH the thread in context → sources captured → promotion confirmed → a follow-up chat sees the cluster context.

Prerequisites §H depends on (spell them out, cross-reference existing sections):
- §A OAuth done (real sign-in), §B forwarding (real inbound email → real threads), §G Docker/WSL recovery (local stack up), Bedrock reachable.
- Migration 0036 applied local→staging→prod via the Management-API path already proven (the §G / migration-chain step) BEFORE the scenario. Confirm the chat_conversations.thread_id column exists in each environment.
- WEB_SEARCH_TOOL_ENABLED is true ONLY after 54-02's adversarial suite is green (record which state shipped in 54-02-SUMMARY.md). If it shipped False, §H's first step is to confirm the suite is green then enable it.

The verification each SUMMARY deferred to §H (per each plan's <verification> block): live browser render of the thread card / picker / header indicator, a live web_search round, a live capture→confirm→promote round-trip, and a follow-up chat seeing cluster context.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the §H CLUS-07 live-acceptance runsheet</name>
  <read_first>
    - .planning/phases/49-live-loop-gate-deploy-oauth-real-email/MORNING-CHECKLIST.md (full — match the §A-§G heading style, numbered sub-steps, and the "What Claude verifies on your resume signal" idiom used in §A.7/§B.9)
    - each 54-0N-SUMMARY.md (to pull the ACTUAL shipped state: was WEB_SEARCH_TOOL_ENABLED flipped true? did 0036 apply anywhere? which literals/flags shipped?)
    - 54-CONTEXT.md CLUS-07 section (the scenario + "NOT executable tonight; never faked")
  </read_first>
  <action>
    Append a new `## H. Phase-54 Email-Cluster Workflow — live acceptance scenario (CLUS-07)` section to MORNING-CHECKLIST.md, matching the file's existing heading/sub-step style. Include:
    (1) A **Prerequisites** subsection: §A OAuth, §B forwarding (real thread in the inbox), §G stack up + Bedrock; migration 0036 applied local→staging→prod (verify the chat_conversations.thread_id column per env); WEB_SEARCH_TOOL_ENABLED true only after 54-02's adversarial suite is green (state the shipped value from 54-02-SUMMARY and the enable step if it shipped False).
    (2) A numbered **Scenario** walkthrough (the ONE scenario): pick a real thread → open /chat, Add-thread → card shows real subject/participants/summary → Attach chat → header shows the linked-thread indicator → ask a question that triggers a web_search round with the thread in context → agent proposes source captures → confirm via the confirm-action widget → promote a captured source (INFERRED→EXTRACTED) → open a NEW chat attached to the same thread and confirm it sees the cluster context (indicator counts + the agent referencing sibling context).
    (3) An **Acceptance** subsection: the explicit pass bar (each step observable in the real desktop browser), and a note that this is the milestone's acceptance gate.
    (4) A cross-reference note that §H supersedes the individual "queued to §H" verification deferrals in each 54-0N-SUMMARY. Do NOT mark any step passed — this is a runsheet for the user to execute live.
  </action>
  <verify>
    <automated>grep -c "## H\." .planning/phases/49-live-loop-gate-deploy-oauth-real-email/MORNING-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## H\." MORNING-CHECKLIST.md` and the section title references CLUS-07.
    - `grep -Eq "0036|thread_id" MORNING-CHECKLIST.md` within §H (migration prerequisite present).
    - `grep -q "WEB_SEARCH_TOOL_ENABLED" MORNING-CHECKLIST.md` (exposure prerequisite present).
    - §H contains all six scenario legs (thread card → attach → web_search-with-context → capture → promote → follow-up cluster context).
    - No step is marked passed/checked — it is an unexecuted runsheet.
  </acceptance_criteria>
  <done>MORNING-CHECKLIST.md has a complete, prerequisite-gated §H CLUS-07 runsheet; nothing is faked as passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runsheet → live execution | the runsheet must not assert unexecuted steps as passed (integrity of the acceptance gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-07-01 | Repudiation | falsely-claimed acceptance | mitigate | §H is a runsheet only; no step marked passed; scenario is user-executed live (never faked) |
| T-54-07-02 | Tampering | live run before 0036 applied | mitigate | prerequisites gate the scenario on 0036 applied per-env + WEB_SEARCH_TOOL_ENABLED behind the passing adversarial suite |
| T-54-07-SC | Tampering | npm/pip installs | accept | docs-only plan; no packages |
</threat_model>

<verification>
- `grep -n "## H\." .planning/phases/49-live-loop-gate-deploy-oauth-real-email/MORNING-CHECKLIST.md` returns the new section.
- The scenario is NOT executed this run — it is the morning live acceptance gate (CLUS-07 stays Pending until the user runs §H).
</verification>

<success_criteria>
CLUS-07 has a complete, prerequisite-gated §H runsheet in MORNING-CHECKLIST.md; the milestone's live acceptance bar is fully specified for morning execution and never faked tonight.
</success_criteria>

<output>
Create `.planning/phases/54-email-cluster-workflow-e3/54-07-SUMMARY.md` when done.
</output>
