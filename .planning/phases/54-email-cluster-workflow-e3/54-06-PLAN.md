---
phase: 54-email-cluster-workflow-e3
plan: 06
type: execute
wave: 3
depends_on: [54-01]
autonomous: true
requirements: [CLUS-02, CLUS-06, CLUS-03]
files_modified:
  - packages/api-client/src/router/chat/cluster-summary.ts
  - packages/api-client/src/router/chat/index.ts
  - packages/api-client/src/router/chat/__tests__/cluster-summary.test.ts
  - apps/web/src/app/chat/_components/thread-cluster-indicator.tsx
  - apps/web/src/app/chat/page.tsx
  - apps/web/src/app/chat/_components/tool-round-activity-row.tsx
  - apps/web/src/app/chat/_components/tool-invocation-result-row.tsx
  - apps/web/src/app/chat/_components/__tests__/thread-cluster-indicator.test.tsx
  - apps/web/src/app/chat/_components/__tests__/web-search-tool-copy.test.tsx

must_haves:
  truths:
    - "A thread-linked conversation shows a ThreadClusterIndicator in the chat header with a 'Linked thread' section and a 'Cluster context' section"
    - "A conversation that is NOT thread-linked shows zero new header chrome (additive-only)"
    - "The cluster-context copy reflects real counts of sibling chats and captured sources for the thread"
    - "web_search tool rounds render 'Searching the web…' / 'Searched the web' / 'Couldn't search the web.' via the existing tool-round chrome, with no new component"
    - "The cluster summary invalidates on chat-terminal following the 51-06 pattern"
  artifacts:
    - path: apps/web/src/app/chat/_components/thread-cluster-indicator.tsx
      provides: "ThreadClusterIndicator (CLUS-02 linked-thread + CLUS-06 cluster-context)"
      min_lines: 40
    - path: packages/api-client/src/router/chat/cluster-summary.ts
      provides: "chat.clusterSummary count query"
      exports: ["chatClusterSummaryProcedures"]
  key_links:
    - from: apps/web/src/app/chat/_components/thread-cluster-indicator.tsx
      to: api.chat.clusterSummary
      via: "useQuery for sibling/source counts"
      pattern: "clusterSummary"
    - from: apps/web/src/app/chat/page.tsx
      to: ThreadClusterIndicator
      via: "rendered in the h-11 top bar when threadId is set"
      pattern: "ThreadClusterIndicator"
    - from: apps/web/src/app/chat/_components/tool-invocation-result-row.tsx
      to: "web_search copy map"
      via: "COPY_BY_TOOL_NAME entry"
      pattern: "web_search"
---

<objective>
Surface the cluster in the chat header and label web_search rounds. Add the
`ThreadClusterIndicator` (one component, two popover sections: "Linked thread" +
"Cluster context") that renders only for thread-linked conversations, backed by a
`chat.clusterSummary` count query; and add the two web_search copy-map entries to
the existing tool-round chrome (zero new components).

Purpose: CLUS-02 (chat header shows the linked thread) + CLUS-06 (cluster-context
indicator, metadata-first) + CLUS-03's UI surface (web_search row copy). Binding
contract: 54-UI-SPEC.md Components 3 & 4.
Output: clusterSummary tRPC query, ThreadClusterIndicator, web_search copy-map
entries, cache invalidation per 51-06.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/54-email-cluster-workflow-e3/54-CONTEXT.md
@.planning/phases/54-email-cluster-workflow-e3/54-UI-SPEC.md
@.claude/skills/polytoken-design-system/SKILL.md
@.planning/phases/54-email-cluster-workflow-e3/54-01-SUMMARY.md

<interfaces>
tRPC (new — packages/api-client/src/router/chat/cluster-summary.ts):
- chat.clusterSummary: protectedProcedure, input { conversationId: uuid }; resolves the conversation's thread_id (reuse 54-01's getConversationThreadId logic / _column-detect), then counts:
    siblingChatCount = other chat_conversations with the same thread_id (excluding this one), scoped to ctx.user.id;
    capturedSourceCount = knowledge_nodes with source="web_search_capture" AND scope_ref_type="web_source" attached to this thread's cluster, scoped to the caller's importer(s);
    (panelCount optional/metadata-first).
  Returns { hasThread: boolean, siblingChatCount, capturedSourceCount }. Feature-detect the thread_id column (0036 may be unapplied) → { hasThread:false, 0, 0 }.
  SHARED LITERAL CONTRACT with 54-03: source="web_search_capture", scope_ref_type="web_source" — keep identical.
- Register `...chatClusterSummaryProcedures` in chat/index.ts (barrel spread — 54-01 already added chatThreadLinkProcedures there; add alongside).

ThreadClusterIndicator (UI-SPEC Component 3 — binding):
- New file apps/web/src/app/chat/_components/thread-cluster-indicator.tsx.
- Renders ONLY when the conversation's threadId is set (read via api.chat.getConversationThreadId from 54-01, or a threadId prop threaded from page.tsx). When null → renders nothing (additive-only).
- Trigger: CostMeter's compact-text-button recipe; Mail icon text-graph-email; label = subject truncated (max-w-[72px] sm:max-w-[140px], CSS-only responsive, NO useIsMobileViewport read).
- Popover: section 1 "Linked thread" (subject text-sm, "Open thread →" hrefFor("email", latestMessageId)); section 2 "Cluster context" with clusterContextCopy:
    has context → "This chat can see context from {n} other chat(s) and {m} captured source(s) on this thread."
    none → "No other chats or sources on this thread yet."
- Placement in page.tsx ConversationView top bar (the h-11 row at ~line 83): right-hand ambient group ordered SaveStatusIndicator | ThreadClusterIndicator | CostMeter. subject/latestMessageId come from api.emails.threadCard (54-01) keyed by threadId.
- Cache invalidation: follow the 51-06 invalidateOnChatTerminal pattern (find it: grep invalidateOnChatTerminal) — invalidate clusterSummary on chat terminal so a just-captured source updates the count.

web_search copy (UI-SPEC Component 4 — zero new components):
- tool-round-activity-row.tsx LABEL_BY_TOOL_NAME: add web_search: "Searching the web…".
- tool-invocation-result-row.tsx COPY_BY_TOOL_NAME: add web_search: { baseLabel: "Searched the web", errorLabel: "Couldn't search the web." }.
- NO citation chips for raw web results (Judgment Call #7) — do not touch parseCitations/ProvenanceLink/ProvenanceKind.

Gotchas: `import * as React from "react"` when a suite first mounts a file; extend ~/trpc/react mocks (add clusterSummary/getConversationThreadId/threadCard) in sibling tests; page.tsx top bar already renders CostMeter/ModelPicker/SaveStatusIndicator (read the region ~lines 83-114).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chat.clusterSummary count query</name>
  <read_first>
    - packages/api-client/src/router/chat/thread-link.ts (54-01 — getConversationThreadId + _column-detect feature-detection to reuse)
    - packages/api-client/src/router/chat/conversations.ts (ownership assertion + ctx.user.id scoping idiom)
    - packages/api-client/src/router/chat/index.ts (barrel spread — chatThreadLinkProcedures already registered by 54-01)
    - packages/db/src/schema/knowledge-nodes.ts (source/scopeRefType columns for the captured-source count)
  </read_first>
  <behavior>
    - Test: clusterSummary for a thread-linked conversation returns hasThread:true with siblingChatCount = number of OTHER conversations sharing the thread_id (own user only) and capturedSourceCount = count of web_search_capture / web_source nodes for the cluster.
    - Test: an unlinked conversation returns { hasThread:false, siblingChatCount:0, capturedSourceCount:0 }.
    - Test: a non-owned conversationId is NOT_FOUND (ownership fail-closed).
    - Test (feature-detect): when the thread_id column is absent, returns { hasThread:false, 0, 0 } (no throw).
    - Test: the captured-source count uses the EXACT literals source="web_search_capture", scope_ref_type="web_source".
  </behavior>
  <action>
    Create `packages/api-client/src/router/chat/cluster-summary.ts` exporting `chatClusterSummaryProcedures.clusterSummary` (protectedProcedure, input `{ conversationId: uuid }`). Assert conversation ownership; resolve thread_id (reuse 54-01's helper / _column-detect); count sibling conversations (own user, same thread_id, id != this) and captured sources (knowledge_nodes source="web_search_capture" scope_ref_type="web_source" for the caller's importer(s)). Return `{ hasThread, siblingChatCount, capturedSourceCount }`; degrade to hasThread:false when the column is absent. Register `...chatClusterSummaryProcedures` in chat/index.ts. Parameterized Drizzle only.
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run src/router/chat/__tests__/cluster-summary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All Task-1 behaviors pass with a mocked ctx.db.
    - `grep -q "web_search_capture" packages/api-client/src/router/chat/cluster-summary.ts` and `grep -q "web_source"` (shared literals with 54-03).
    - `grep -q "chatClusterSummaryProcedures" packages/api-client/src/router/chat/index.ts`.
    - Typecheck clean (`cd packages/api-client && npx tsc --noEmit`).
  </acceptance_criteria>
  <done>chat.clusterSummary returns real, ownership-scoped sibling/source counts and degrades cleanly on unapplied 0036.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThreadClusterIndicator + page.tsx top-bar mount + invalidation</name>
  <read_first>
    - apps/web/src/app/chat/_components/cost-meter.tsx (compact-text-button trigger recipe + Popover composition to mirror)
    - apps/web/src/app/chat/page.tsx lines 54-114 (ConversationView top bar: SaveStatusIndicator/ModelPicker/CostMeter placement; the h-11 row)
    - apps/web/src/app/chat/_components/provenance-link.tsx (hrefFor("email", id))
    - the 51-06 invalidateOnChatTerminal implementation (grep it) — the cache-invalidation hook to reuse
    - 54-UI-SPEC.md Component 3 + Copywriting Contract (exact copy)
  </read_first>
  <behavior>
    - Test: with threadId set, ThreadClusterIndicator renders the trigger (aria-label "Linked thread: {subject}") and the popover has "Linked thread" + "Cluster context" sections.
    - Test: with threadId null, the component renders nothing.
    - Test: clusterContextCopy has-context variant renders "This chat can see context from {n} other chat(s) and {m} captured source(s) on this thread." with real counts; the none variant renders "No other chats or sources on this thread yet."
    - Test: the "Open thread →" link points at hrefFor("email", latestMessageId).
    - Test (page mount): page.tsx renders ThreadClusterIndicator in the ambient group between SaveStatusIndicator and CostMeter, only when the conversation is thread-linked.
  </behavior>
  <action>
    Create `apps/web/src/app/chat/_components/thread-cluster-indicator.tsx` per Component 3 VERBATIM (trigger recipe from CostMeter, Mail `text-graph-email`, `max-w-[72px] sm:max-w-[140px]` CSS-only truncation, two popover sections, both clusterContextCopy variants). It reads threadId (via getConversationThreadId or a prop), fetches subject/latestMessageId via `api.emails.threadCard`, and counts via `api.chat.clusterSummary`. Mount it in page.tsx's ConversationView top bar right-hand ambient group (SaveStatusIndicator | ThreadClusterIndicator | CostMeter), rendering nothing when unlinked. Wire clusterSummary invalidation into the existing 51-06 invalidateOnChatTerminal path so a just-captured source refreshes the count. `import * as React from "react"`. Token classes only; exact copy strings.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_components/__tests__/thread-cluster-indicator.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All Task-2 behaviors pass (~/trpc/react mocks extended with clusterSummary/threadCard/getConversationThreadId).
    - `grep -q "ThreadClusterIndicator" apps/web/src/app/chat/page.tsx`.
    - `grep -q "clusterSummary" thread-cluster-indicator.tsx` and `grep -q "text-graph-email"`.
    - No `useIsMobileViewport` import in thread-cluster-indicator.tsx (CSS-only responsive per UI-SPEC).
    - palette-ban/token gates 10/10; `npm run typecheck -w @polytoken/web` clean.
  </acceptance_criteria>
  <done>The linked-thread + cluster-context indicator renders for thread-linked chats only, with real counts and correct copy, and refreshes on chat terminal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: web_search tool-round copy-map entries</name>
  <read_first>
    - apps/web/src/app/chat/_components/tool-round-activity-row.tsx (LABEL_BY_TOOL_NAME map + the search_knowledge entry precedent)
    - apps/web/src/app/chat/_components/tool-invocation-result-row.tsx (COPY_BY_TOOL_NAME map + the no-citation render path for search_knowledge)
    - 54-UI-SPEC.md Component 4 + Copywriting Contract
  </read_first>
  <behavior>
    - Test: tool-round-activity-row renders "Searching the web…" for a web_search round (in-progress).
    - Test: tool-invocation-result-row renders "Searched the web" for a successful web_search result and "Couldn't search the web." for an error result.
    - Test: a web_search result renders WITHOUT citation chips (Judgment Call #7 — the no-citation path, like search_knowledge today).
  </behavior>
  <action>
    Add `web_search: "Searching the web…"` to LABEL_BY_TOOL_NAME (tool-round-activity-row.tsx) and `web_search: { baseLabel: "Searched the web", errorLabel: "Couldn't search the web." }` to COPY_BY_TOOL_NAME (tool-invocation-result-row.tsx). No new component, no citation-chip work, no ProvenanceKind change. Exact copy strings from the Copywriting Contract.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_components/__tests__/web-search-tool-copy.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All Task-3 behaviors pass.
    - `grep -q "Searching the web" tool-round-activity-row.tsx` and `grep -q "Searched the web" tool-invocation-result-row.tsx`.
    - No new members added to ProvenanceKind (grep confirms unchanged).
    - Full web suite green (no regression); typecheck clean.
  </acceptance_criteria>
  <done>web_search rounds render the exact in-progress/success/error labels through the existing chrome with zero new components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → clusterSummary | conversationId untrusted; ownership + user scoping required |
| counts → display | counts must reflect only the caller's own cluster |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-06-01 | Information disclosure | clusterSummary counts | mitigate | ownership assertion + ctx.user.id / importer scoping; a foreign conversationId is NOT_FOUND |
| T-54-06-02 | Availability | unapplied 0036 | mitigate | feature-detect thread_id column → { hasThread:false, 0, 0 }, never a 500 |
| T-54-06-03 | Tampering (stale UI) | cluster count after capture | mitigate | invalidate on chat terminal (51-06 pattern) so counts stay truthful |
| T-54-06-04 | Spoofing (citation) | raw web results | accept | no citation chips for raw web results — nothing is presented as citable until confirmed capture (Judgment Call #7) |
| T-54-06-SC | Tampering | shadcn/registry install | accept | no new vendored component — Popover/Button/Badge already in @polytoken/ui |
</threat_model>

<verification>
- `cd packages/api-client && npx vitest run src/router/chat/__tests__/cluster-summary.test.ts` green.
- `cd apps/web && npx vitest run src/app/chat/_components/__tests__/thread-cluster-indicator.test.tsx src/app/chat/_components/__tests__/web-search-tool-copy.test.tsx` green.
- Full web suite reconfirmed green; `npm run typecheck -w @polytoken/web` clean; palette-ban/token gates 10/10.
- Live header render + real web_search round DEFERRED to MORNING-CHECKLIST §H.
</verification>

<success_criteria>
CLUS-02/CLUS-06 UI: thread-linked chats show a linked-thread + cluster-context indicator with real counts, unlinked chats unchanged. CLUS-03 UI: web_search rounds render correct labels through existing chrome.
</success_criteria>

<output>
Create `.planning/phases/54-email-cluster-workflow-e3/54-06-SUMMARY.md` when done.
</output>
