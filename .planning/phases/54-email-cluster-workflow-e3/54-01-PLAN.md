---
phase: 54-email-cluster-workflow-e3
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [CLUS-02, CLUS-01]
files_modified:
  - packages/db/migrations/0036_chat_conversation_thread_id.sql
  - packages/db/migrations/meta/_journal.json
  - packages/db/migrations/meta/0036_snapshot.json
  - packages/db/src/schema/chat-conversations.ts
  - packages/api-client/src/router/chat/conversations.ts
  - packages/api-client/src/router/chat/thread-link.ts
  - packages/api-client/src/router/chat/index.ts
  - packages/api-client/src/router/emails/thread-card.ts
  - packages/api-client/src/router/emails/index.ts
  - packages/api-client/src/router/_column-detect.ts
  - packages/api-client/src/router/chat/__tests__/thread-link.test.ts
  - packages/api-client/src/router/emails/__tests__/thread-card.test.ts

must_haves:
  truths:
    - "Migration 0036 exists and adds chat_conversations.thread_id (nullable uuid, FK threads(id) ON DELETE SET NULL) + index, but is applied to no environment"
    - "A user can attach a chat to a thread: chat.attachConversationToThread creates a conversation with thread_id set, scoped to the caller"
    - "A conversation read surfaces its threadId so the header can show the linked thread"
    - "emails.threadCard returns one thread's real subject, participant names, latest snippet and latest member email id, ownership-scoped"
    - "If the thread_id column is absent (0036 unapplied), thread-linkage procedures degrade cleanly instead of crashing"
  artifacts:
    - path: packages/db/migrations/0036_chat_conversation_thread_id.sql
      provides: "additive thread_id column + FK + index"
      contains: "ADD COLUMN"
    - path: packages/db/src/schema/chat-conversations.ts
      provides: "threadId drizzle column"
      contains: "threadId"
    - path: packages/api-client/src/router/chat/thread-link.ts
      provides: "attachConversationToThread mutation + threadId read"
      exports: ["chatThreadLinkProcedures"]
    - path: packages/api-client/src/router/emails/thread-card.ts
      provides: "emails.threadCard single-thread projection + deriveThreadCard pure helper"
      exports: ["emailThreadCardProcedures", "deriveThreadCard"]
  key_links:
    - from: packages/api-client/src/router/chat/thread-link.ts
      to: "@polytoken/db/ownership assertConversationOwnership"
      via: "ownership assertion before write"
      pattern: "assertConversationOwnership"
    - from: packages/api-client/src/router/emails/thread-card.ts
      to: "userOwnedImporterIds"
      via: "importer scope"
      pattern: "userOwnedImporterIds"
---

<objective>
Lay the durable thread↔conversation linkage seam and the single-thread read the
canvas node consumes. Author migration 0036 (thread_id on chat_conversations),
mirror it in the Drizzle schema, expose an ownership-scoped "attach chat to
thread" mutation + a threadId read, and add `emails.threadCard` (one thread's
subject/participants/summary/latest-message for the `EmailThreadNode` fetch).

Purpose: CLUS-02 needs a server-side linkage that survives canvas changes and is
readable at turn time; CLUS-01's node needs a real thread projection. Everything
here is additive and applied to NO environment tonight (Docker/WSL down) — the
morning §H flow applies 0036 local→staging→prod before the live scenario.

Output: migration 0036 (authored, unapplied), Drizzle schema column, tRPC
`chat.attachConversationToThread` + threadId read + `emails.threadCard`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-email-cluster-workflow-e3/54-CONTEXT.md

<interfaces>
Existing migration/journal convention (mirror EXACTLY — hand-authored SQL + journal entry + snapshot):
- packages/db/migrations/0035_threads_forwarding.sql is the latest applied migration; meta/_journal.json lists entries; last idx is 34 (0035). 0036 is idx 35.
- MEMORY: "drizzle journal gotcha" — the journal + snapshot must be updated by hand consistently or migrate breaks.

chat_conversations (packages/db/src/schema/chat-conversations.ts) TODAY:
  id uuid PK, importerId uuid (nullable, no FK), userId uuid NOT NULL FK auth.users ON DELETE cascade,
  title text NOT NULL, modelId text NOT NULL, createdAt, updatedAt.

threads (packages/db/src/schema/threads.ts):
  id uuid PK, importerId uuid NOT NULL FK importers ON DELETE cascade, subject text (nullable), timestamps.

emails.thread_id precedent (packages/db/src/schema/emails.ts):
  threadId: uuid("thread_id").references(() => Threads.id, { onDelete: "set null" })  — MIRROR THIS.

Existing tRPC patterns to reuse verbatim:
- protectedProcedure, ctx.db, ctx.user.id (packages/api-client/src/router/chat/conversations.ts)
- assertOwnedOrNotFound(() => assertConversationOwnership(ctx.db, id, ctx.user.id))  (fail-closed NOT_FOUND)
- userOwnedImporterIds(ctx.db, ctx.user.id) + resolveListScope(owned, importerId)  (emails/list-threads.ts)
- THREAD_SNIPPET_CHARS = 240, MEMBER_EMAIL_ID_CAP, MAX_SCAN_ROWS (emails/list-threads.ts) — reuse the same DoS bounds.
- Barrel registration: chat/index.ts spreads `...chatXProcedures`; emails/index.ts spreads `...emailXProcedures`.

createConversation (conversations.ts) already inserts { userId, modelId, importerId }. Attach adds threadId.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 0036 + Drizzle thread_id column (unapplied)</name>
  <read_first>
    - packages/db/migrations/0035_threads_forwarding.sql (SQL style: ALTER TABLE ... ADD COLUMN ... REFERENCES ... ON DELETE SET NULL; CREATE INDEX)
    - packages/db/migrations/meta/_journal.json (append the 0036 entry — idx 35, tag "0036_chat_conversation_thread_id"; keep the exact JSON shape)
    - packages/db/src/schema/emails.ts lines 37-42 (the emails.thread_id ON DELETE SET NULL precedent to mirror)
    - packages/db/src/schema/chat-conversations.ts (the table to extend)
    - packages/db/src/schema/threads.ts (FK target)
  </read_first>
  <action>
    Author `packages/db/migrations/0036_chat_conversation_thread_id.sql`: `ALTER TABLE "chat_conversations" ADD COLUMN "thread_id" uuid REFERENCES "threads"("id") ON DELETE SET NULL;` plus `CREATE INDEX "idx_chat_conversations_thread_id" ON "chat_conversations" ("thread_id");`. Additive-only, nullable, ON DELETE SET NULL (per D-CONTEXT migration discipline + security_enforcement: additive-only, ON DELETE SET NULL). Add the matching Drizzle column to chat-conversations.ts: `threadId: uuid("thread_id").references(() => Threads.id, { onDelete: "set null" })` importing `Threads` from "./threads", plus the index `chatConversationsThreadIdIdx: index("idx_chat_conversations_thread_id").on(t.threadId)`. Update meta/_journal.json (append idx-35 entry) and generate meta/0036_snapshot.json consistent with 0035's snapshot shape (hand-mirror if drizzle-kit cannot run offline). DO NOT apply the migration anywhere — Docker/WSL down; the morning §H flow applies it. Regenerate the drizzle snapshot only if `drizzle-kit` runs offline; otherwise hand-write it consistently and note that in the task summary.
  </action>
  <verify>
    <automated>cd packages/db && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "thread_id" packages/db/migrations/0036_chat_conversation_thread_id.sql` ≥ 1 and the file contains both `ADD COLUMN` and `ON DELETE SET NULL` and `CREATE INDEX`.
    - `grep -q "threadId" packages/db/src/schema/chat-conversations.ts` and `grep -q "onDelete: \"set null\"" packages/db/src/schema/chat-conversations.ts`.
    - meta/_journal.json parses as JSON and its last entry tag is `0036_chat_conversation_thread_id`.
    - No environment applied: no code calls `migrate`/`db push` in this task.
  </acceptance_criteria>
  <done>0036 SQL + journal + snapshot + Drizzle column exist, typecheck clean, applied to no environment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat.attachConversationToThread + threadId read (feature-detected)</name>
  <read_first>
    - packages/api-client/src/router/chat/conversations.ts (createConversation insert shape; assertConversationOwnership + assertOwnedOrNotFound usage; protectedProcedure)
    - packages/api-client/src/router/chat/index.ts (barrel spread to register the new procedures)
    - packages/db/src/schema/chat-conversations.ts (threadId column from Task 1)
  </read_first>
  <behavior>
    - Test: attachConversationToThread with a threadId + owned conversationId sets thread_id and returns { attached: true }.
    - Test: a non-owned conversationId throws NOT_FOUND (assertConversationOwnership fail-closed) — thread_id never written.
    - Test: createConversation may accept an optional threadId and persists it when the column exists.
    - Test: getConversationThreadId returns { threadId } for an owned conversation and null when unset.
    - Test (graceful degradation): when the underlying query raises a Postgres UndefinedColumn error (code 42703 — 0036 unapplied), the procedures catch it via `columnExists`/error-guard and return a clean unavailable result ({ attached: false, reason: "linkage_unavailable" } / { threadId: null }) instead of throwing.
  </behavior>
  <action>
    Create `packages/api-client/src/router/_column-detect.ts` exporting a small `tableColumnExists(db, table, column)` helper querying `information_schema.columns` (parameterized, cached per-process) — the single feature-detection point both this plan and later plans reuse so an unapplied 0036 never 500s. Create `packages/api-client/src/router/chat/thread-link.ts` exporting `chatThreadLinkProcedures`:
    - `attachConversationToThread`: protectedProcedure, input `{ conversationId: uuid, threadId: uuid }`; assert ownership via `assertOwnedOrNotFound(() => assertConversationOwnership(ctx.db, conversationId, ctx.user.id))`; if `tableColumnExists` is false, return `{ attached: false, reason: "linkage_unavailable" }`; else `UPDATE chat_conversations SET thread_id = :threadId, updated_at = now() WHERE id = :conversationId` and return `{ attached: true }`.
    - `getConversationThreadId`: protectedProcedure, input `{ conversationId: uuid }`; ownership-assert then return `{ threadId: string | null }`, `null` when column absent.
    Extend createConversation (conversations.ts) with an optional `threadId` in its input schema, persisted only when present AND the column exists. Register `...chatThreadLinkProcedures` in chat/index.ts. All queries parameterized Drizzle builders (no string interpolation); threadId validated as `z.string().uuid()` at the boundary.
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run src/router/chat/__tests__/thread-link.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - thread-link.test.ts: all five behaviors above pass with a mocked `ctx.db`.
    - `grep -q "assertConversationOwnership" packages/api-client/src/router/chat/thread-link.ts`.
    - `grep -q "chatThreadLinkProcedures" packages/api-client/src/router/chat/index.ts`.
    - Typecheck: `cd packages/api-client && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Attach mutation + threadId read exist, ownership-scoped, degrade cleanly on unapplied 0036, all tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: emails.threadCard single-thread projection</name>
  <read_first>
    - packages/api-client/src/router/emails/list-threads.ts (groupEmailsIntoThreads idiom, THREAD_SNIPPET_CHARS, MEMBER_EMAIL_ID_CAP, MAX_SCAN_ROWS, ThreadEmailRow shape, userOwnedImporterIds + resolveListScope)
    - packages/api-client/src/router/emails/index.ts (barrel spread)
    - packages/db/src/schema/emails.ts (senderName, senderAddress, toAddresses, ccAddresses, subject, bodyText, receivedAt columns for participant/summary derivation)
  </read_first>
  <behavior>
    - Test (pure deriveThreadCard): given member email rows, returns subject (latest member's subject), participantsSummary (up to 3 deduped sender display names, then "+{n} more"), latestSnippet (server-truncated), latestMessageId (the newest member's id), messageCount.
    - Test: sender display name falls back to sender address when senderName is null; dedupe collapses repeats.
    - Test: threadCard returns null when the threadId is not owned by the caller (no cross-tenant leak).
    - Test: an empty/unknown thread yields null (not a throw).
  </behavior>
  <action>
    Create `packages/api-client/src/router/emails/thread-card.ts` exporting a pure `deriveThreadCard(rows)` helper and `emailThreadCardProcedures.threadCard` (protectedProcedure, input `{ threadId: z.string().uuid() }`). Query: `userOwnedImporterIds` → scope; select member email rows `WHERE thread_id = :threadId AND importer_id = ANY(owned)` (never trust threadId alone for scope), ordered `received_at desc`, capped at MAX_SCAN_ROWS, projecting id/subject/senderName/senderAddress/receivedAt and a `left(body_text, THREAD_SNIPPET_CHARS)` snippet. Feed rows to `deriveThreadCard`; return `{ threadId, subject, participantsSummary, latestSnippet, latestMessageId, messageCount } | null`. Participant dedupe/"+N more" mirrors resolveFooterCopy/ProvenanceLink idiom (UI-SPEC Component 1). Register `...emailThreadCardProcedures` in emails/index.ts.
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run src/router/emails/__tests__/thread-card.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - thread-card.test.ts: all four behaviors pass with a mocked ctx.db (and pure-helper cases run DB-free).
    - `grep -q "userOwnedImporterIds" packages/api-client/src/router/emails/thread-card.ts` (ownership scope present).
    - `grep -q "emailThreadCardProcedures" packages/api-client/src/router/emails/index.ts`.
    - Typecheck clean.
  </acceptance_criteria>
  <done>emails.threadCard returns a real, ownership-scoped single-thread projection; pure helper unit-tested; registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tRPC | conversationId/threadId are untrusted input crossing into DB queries |
| conversation ownership | a caller must not attach/read a conversation they don't own |
| thread ownership | a caller must not read a thread outside their owned importers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01-01 | Spoofing/Elevation | attachConversationToThread | mitigate | assertConversationOwnership before any write (fail-closed NOT_FOUND); threadId validated as uuid |
| T-54-01-02 | Information disclosure | emails.threadCard | mitigate | scope query to userOwnedImporterIds; a foreign threadId yields null, never another tenant's rows |
| T-54-01-03 | Tampering | migration 0036 | mitigate | additive-only, nullable, ON DELETE SET NULL; applied to no env tonight |
| T-54-01-04 | Denial of service | threadCard row scan | mitigate | MAX_SCAN_ROWS cap + MEMBER_EMAIL_ID_CAP reused from list-threads |
| T-54-01-05 | Availability | unapplied 0036 (missing column) | mitigate | tableColumnExists feature-detection → clean unavailable result, never a 500 |
| T-54-01-SC | Tampering | npm/pip installs | accept | no new packages added this plan (drizzle/zod/trpc already present) |
</threat_model>

<verification>
- `cd packages/api-client && npx vitest run src/router/chat/__tests__/thread-link.test.ts src/router/emails/__tests__/thread-card.test.ts` green.
- `cd packages/api-client && npx tsc --noEmit` and `cd packages/db && npx tsc --noEmit` clean.
- Migration 0036 authored, journal/snapshot consistent, applied to NO environment (live apply is morning §H).
</verification>

<success_criteria>
CLUS-02 linkage seam and CLUS-01's thread projection exist and are ownership-scoped; 0036 is authored but unapplied; every thread-linkage read/write degrades cleanly when the column is absent.
</success_criteria>

<output>
Create `.planning/phases/54-email-cluster-workflow-e3/54-01-SUMMARY.md` when done.
</output>
