---
phase: 30-suggest-only-promotion-gate
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - packages/db/migrations/0027_edge_promotion_provenance.sql
  - packages/db/migrations/meta/_journal.json
  - packages/db/scripts/verify-0027-live.ts
  - packages/db/src/schema/knowledge-node-edges.ts
  - apps/email-listener/app/domain/ports/knowledge_graph_repository.py
  - apps/email-listener/app/infrastructure/supabase/knowledge_graph_repository.py
  - apps/email-listener/tests/test_knowledge_graph_repository.py
  - apps/email-listener/app/application/use_cases/promote_edge.py
  - apps/email-listener/tests/test_promote_edge.py
  - apps/email-listener/app/presentation/api/v1/knowledge_edges.py
  - apps/email-listener/app/main.py
  - apps/email-listener/app/container.py
  - apps/email-listener/tests/test_promote_edge_endpoint.py
autonomous: true
requirements: [TIER-03]

must_haves:
  truths:
    - "A human reviewer has an authenticated promote action that flips one INFERRED/AMBIGUOUS edge to EXTRACTED"
    - "Promoting an already-EXTRACTED or inactive edge is a fail-closed 4xx rejection made BEFORE any state flip"
    - "Promotion records promotion provenance {promoted_at, from_tier, mechanism} on a new nullable column, distinct from the synthesis provenance"
    - "Nothing auto-promotes — the tier only flips to EXTRACTED through this explicit endpoint"
  artifacts:
    - path: "packages/db/migrations/0027_edge_promotion_provenance.sql"
      provides: "nullable promotion jsonb column on knowledge_node_edges"
      contains: "promotion"
    - path: "apps/email-listener/app/application/use_cases/promote_edge.py"
      provides: "PromoteEdgeUseCase with fail-closed guard"
      contains: "PromoteEdgeUseCase"
    - path: "apps/email-listener/app/presentation/api/v1/knowledge_edges.py"
      provides: "authenticated POST promote endpoint"
      contains: "require_api_key"
  key_links:
    - from: "knowledge_edges.py endpoint"
      to: "PromoteEdgeUseCase.execute"
      via: "FromDishka use case, typed rejection → 4xx"
      pattern: "PromoteEdgeUseCase"
    - from: "PromoteEdgeUseCase"
      to: "knowledge_graph_repository.promote_edge"
      via: "guard-then-flip, writes promotion jsonb"
      pattern: "promote_edge"
---

<objective>
Ship the human promotion mechanic (TIER-03): migration 0027 adds a nullable `promotion jsonb` column,
a fail-closed use case flips exactly one ACTIVE INFERRED/AMBIGUOUS edge to EXTRACTED while recording
promotion provenance distinct from the synthesis provenance, and an authenticated FastAPI endpoint
exposes it. This is the WRITE that raises trust — nothing else may promote (ROADMAP SC3 + SC4).

Purpose: complete the suggest-only gate — a human, and only a human, turns a suggestion into trusted
truth, with an auditable promotion record.
Output: migration + live-verify script, promote_edge repo method + use case, POST endpoint + DI +
router wiring, and offline tests covering the fail-closed ordering and tenant isolation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-suggest-only-promotion-gate/30-CONTEXT.md
@.planning/phases/29-tier-ladder-edge-materialization/29-02-SUMMARY.md
@packages/db/migrations/0026_knowledge_trust_tier.sql
@packages/db/scripts/verify-0026-live.ts

<interfaces>
KnowledgeGraphRepository port (apps/email-listener/app/domain/ports/knowledge_graph_repository.py):
  async methods upsert_node / find_active_node / insert_edge / deactivate_edges_for_node /
  find_active_edges_for_node; add promote_edge here. Adapter uses supabase.Client table().update()/
  select() chains; strip_nul wraps written payloads; never .delete().

Endpoint conventions (apps/email-listener/app/presentation/api/v1/components.py):
  router = APIRouter(prefix=..., dependencies=[Depends(require_api_key)]) — X-API-Key fail-closed.
  @inject handlers, FromDishka[UseCase], ValueError → HTTPException(404/4xx), ApiResponse.ok(view).
  Phase-24 ordering discipline: load/ownership → guard checks → state flip → persist; rejections raised
  BEFORE any write.

DI (apps/email-listener/app/container.py:337-364 _provide_confirm_region_use_case): factory pattern —
  instantiate SupabaseKnowledgeGraphRepository(client=client) directly (concrete infra, not a port),
  build the use case, register via provider.provide(factory, provides=UseCase). Routers registered in
  app/main.py include_router(...) block (lines 62-73).

Schema source: packages/db/src/schema/knowledge-node-edges.ts (add the promotion jsonb column mirroring
the existing provenance jsonb column so Drizzle types match the live DB).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Migration 0027 — promotion jsonb column + live verify</name>
  <files>packages/db/migrations/0027_edge_promotion_provenance.sql, packages/db/migrations/meta/_journal.json, packages/db/scripts/verify-0027-live.ts, packages/db/src/schema/knowledge-node-edges.ts</files>
  <read_first>
    - packages/db/migrations/0026_knowledge_trust_tier.sql (idempotent hand-written style, --> statement-breakpoint)
    - packages/db/scripts/verify-0026-live.ts (pg direct-query live verification pattern, exit codes)
    - packages/db/migrations/meta/_journal.json (last entry idx=26, tag 0026_knowledge_trust_tier)
    - packages/db/src/schema/knowledge-node-edges.ts (existing provenance jsonb column definition to mirror)
  </read_first>
  <action>
    Write packages/db/migrations/0027_edge_promotion_provenance.sql as a hand-written idempotent migration
    (copy 0026's style): `ALTER TABLE "knowledge_node_edges" ADD COLUMN IF NOT EXISTS "promotion" jsonb;`
    — nullable, distinct from the existing `provenance` column (synthesis provenance). Add a header
    comment explaining the column holds human-promotion provenance {promoted_at, from_tier, mechanism}
    and that re-applying is a no-op. Append the journal entry to meta/_journal.json (idx 27, version "7",
    tag "0027_edge_promotion_provenance", next `when` timestamp, breakpoints true). Add the matching
    `promotion` jsonb column to the Drizzle schema in knowledge-node-edges.ts so types track the DB.
    Apply to LOCAL Postgres (postgresql://postgres:postgres@localhost:54322/postgres) and write
    packages/db/scripts/verify-0027-live.ts (mirror verify-0026-live.ts): direct pg query asserting
    knowledge_node_edges.promotion exists with udt=jsonb, nullable=YES; exit 0 on pass, 1 on fail.
    Do NOT DELETE or alter the existing provenance column.
  </action>
  <verify>
    <automated>cd packages/db && npm run with-env -- tsx scripts/verify-0027-live.ts</automated>
  </verify>
  <acceptance_criteria>
    - 0027_edge_promotion_provenance.sql adds `promotion jsonb` with IF NOT EXISTS; re-running is a no-op.
    - meta/_journal.json contains an idx=27 entry tagged 0027_edge_promotion_provenance.
    - verify-0027-live.ts prints "VERIFICATION PASSED" and exits 0 against local Postgres, asserting
      column udt=jsonb, nullable=YES.
    - knowledge-node-edges.ts declares the promotion jsonb column; `npx tsc --noEmit` clean in packages/db.
  </acceptance_criteria>
  <done>The promotion column exists live and is verified; the synthesis provenance column is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: promote_edge repo method + PromoteEdgeUseCase (fail-closed guard)</name>
  <files>apps/email-listener/app/domain/ports/knowledge_graph_repository.py, apps/email-listener/app/infrastructure/supabase/knowledge_graph_repository.py, apps/email-listener/tests/test_knowledge_graph_repository.py, apps/email-listener/app/application/use_cases/promote_edge.py, apps/email-listener/tests/test_promote_edge.py</files>
  <read_first>
    - apps/email-listener/app/infrastructure/supabase/knowledge_graph_repository.py (table().update()/select() call shapes, strip_nul, never .delete())
    - apps/email-listener/app/domain/ports/knowledge_graph_repository.py (Protocol style)
    - apps/email-listener/app/application/use_cases/synthesize_knowledge.py (best-effort domain-only import discipline)
    - .planning/phases/30-suggest-only-promotion-gate/30-CONTEXT.md (promotion action + guard semantics)
  </read_first>
  <behavior>
    - PromoteEdgeUseCase.execute(*, edge_id, importer_id) loads the edge, then rejects (raises a typed
      domain exception) BEFORE any write when: edge not found; edge.is_active is False; edge.tier is not
      in {INFERRED, AMBIGUOUS} (already EXTRACTED → reject); edge's source node importer_id != importer_id
      (tenant mismatch → reject). Only a load→check→flip ordering; no write on any rejection path.
    - On success it flips tier to EXTRACTED and writes promotion={promoted_at (UTC ISO8601), from_tier
      (the prior tier), mechanism:'human_promote'} into the new promotion column, leaving the original
      provenance column unchanged.
    - Repo promote_edge(*, edge_id, promotion) performs the update in one call filtered by edge id AND
      is_active=true AND tier in (INFERRED, AMBIGUOUS) (defense-in-depth CAS — a concurrent promote/dismiss
      cannot double-apply); returns whether a row was updated. Never .delete().
    - Tests: promote INFERRED→EXTRACTED writes promotion + leaves provenance; reject already-EXTRACTED;
      reject inactive; reject cross-importer; assert NO update call is issued on every rejection path.
  </behavior>
  <action>
    RED first: write test_promote_edge.py (AsyncMock repo port) for the five scenarios above plus a
    call-ordering assertion (load precedes any write; zero write on rejection), and a repo call-shape test
    in test_knowledge_graph_repository.py asserting promote_edge's update filter (id + is_active=true +
    tier in suggestion tiers) and promotion payload via strip_nul. Confirm RED. GREEN: add
    `promote_edge` + a read (e.g. `find_edge_by_id`) to the KnowledgeGraphRepository port + adapter, and
    implement PromoteEdgeUseCase (app.domain imports only) with a typed EdgeNotPromotable /
    EdgeNotFound exception module. Guard checks raise BEFORE calling promote_edge (mirrors Phase-24
    SubmitWidgetInteraction fail-closed CAS posture). Reject → flip → persist ordering enforced.
  </action>
  <verify>
    <automated>cd apps/email-listener && python -m pytest tests/test_promote_edge.py tests/test_knowledge_graph_repository.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - RED commit captured (tests fail before implementation).
    - PromoteEdgeUseCase rejects not-found / inactive / already-EXTRACTED / cross-importer BEFORE any
      write; a test asserts the repo update method is NOT called on each rejection path.
    - On success promotion={promoted_at, from_tier, mechanism} is written and the original provenance
      column is not modified; a test asserts both.
    - promote_edge update is filtered by is_active=true AND tier in (INFERRED, AMBIGUOUS); no .delete().
    - mypy + ruff + lint-imports clean (PromoteEdgeUseCase imports only app.domain.*).
  </acceptance_criteria>
  <done>A guarded, audit-recording promotion use case exists; only active suggestion-tier edges of the caller's tenant can flip to EXTRACTED.</done>
</task>

<task type="auto">
  <name>Task 3: Authenticated promote endpoint + DI + router registration</name>
  <files>apps/email-listener/app/presentation/api/v1/knowledge_edges.py, apps/email-listener/app/main.py, apps/email-listener/app/container.py, apps/email-listener/tests/test_promote_edge_endpoint.py</files>
  <read_first>
    - apps/email-listener/app/presentation/api/v1/components.py (lines 61-67 router+require_api_key, 221-259 @inject/FromDishka/ValueError→HTTPException/ApiResponse.ok)
    - apps/email-listener/app/container.py (lines 311-364 _provide_promote_entity_use_case + _provide_confirm_region_use_case factory pattern; line 823 provider.provide registration)
    - apps/email-listener/app/main.py (lines 15-25 router imports, 62-73 include_router block)
    - apps/email-listener/tests/test_confirm_region.py (TestClient + dishka HTTP-seam test helper idiom)
  </read_first>
  <action>
    Create app/presentation/api/v1/knowledge_edges.py: router = APIRouter(prefix="/v1/knowledge/edges",
    tags=["knowledge"], dependencies=[Depends(require_api_key)]) — X-API-Key fail-closed on the whole
    router. Add `POST /{edge_id}/promote` (@inject, edge_id: UUID path param, FromDishka[PromoteEdgeUseCase],
    importer_id supplied via the request body Pydantic model validated at the boundary — never trusted as
    an auth claim/header, mirroring D-12). Map EdgeNotFound → 404 and EdgeNotPromotable → 409 (or 422)
    with a generic detail; log full context server-side. Return ApiResponse.ok with a small view
    {edge_id, tier:'EXTRACTED'}. Add `_provide_promote_edge_use_case(client) -> PromoteEdgeUseCase`
    factory to container.py (instantiate SupabaseKnowledgeGraphRepository directly, mirror
    _provide_confirm_region_use_case) and register it with provider.provide(..., provides=PromoteEdgeUseCase).
    Import and include the router in main.py include_router block. Do NOT add promote/dismiss UI — that is
    Phase 32.
  </action>
  <verify>
    <automated>cd apps/email-listener && python -m pytest tests/test_promote_edge_endpoint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - POST /v1/knowledge/edges/{edge_id}/promote exists behind Depends(require_api_key); a test asserts a
      request without a valid X-API-Key is rejected (401/403) before the use case runs.
    - Container builds without dishka GraphMissingFactoryError; the router is included in main.py.
    - TestClient+dishka tests: valid promote of a seeded INFERRED edge returns 200 with tier 'EXTRACTED';
      promoting an already-EXTRACTED/inactive edge returns the mapped 4xx (EdgeNotPromotable); promoting an
      edge whose source node belongs to another importer returns a 4xx (tenant isolation) with no write.
    - ruff + lint-imports clean on all touched files; no regressions in the existing suite (excluding the
      logged test_genui_retrieval_provider.py flake).
  </acceptance_criteria>
  <done>An authenticated, tenant-scoped promote endpoint is reachable end-to-end and enforces the fail-closed guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /v1/knowledge/edges/{id}/promote | untrusted WRITE that RAISES trust (suggestion → EXTRACTED) |
| use case → knowledge_node_edges update | tier flip must be guarded and tenant-scoped |
| migration → live Postgres | schema change on a table with an existing provenance column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Spoofing | promote endpoint auth | mitigate | router-level Depends(require_api_key); X-API-Key fail-closed; test asserts unauthenticated request rejected before use case |
| T-30-05 | Elevation of privilege | PromoteEdgeUseCase guard | mitigate | only ACTIVE INFERRED/AMBIGUOUS promotable; already-EXTRACTED/inactive → 4xx rejected BEFORE state flip (Phase-24 posture); nothing auto-promotes — tier flips ONLY through this endpoint |
| T-30-06 | Tampering | double-submit / concurrent promote | mitigate | repo promote_edge update filtered by is_active=true AND tier in (INFERRED,AMBIGUOUS) — CAS prevents double-apply; rejections raised before any write |
| T-30-07 | Information disclosure | cross-importer promote | mitigate | importer derived/validated at boundary (never an auth claim, D-12); guard rejects when edge's source node importer_id != caller importer_id; no write on mismatch |
| T-30-08 | Repudiation | promotion audit | mitigate | promotion={promoted_at, from_tier, mechanism} recorded on the new column, distinct from synthesis provenance; superseded rows never deleted |
| T-30-SC | Tampering | migration 0027 / npm-pip installs | mitigate | idempotent IF NOT EXISTS migration, live-verified; no new runtime packages added (verified) |
</threat_model>

<verification>
- `tsx scripts/verify-0027-live.ts` prints VERIFICATION PASSED against local Postgres.
- `python -m pytest tests/test_promote_edge.py tests/test_promote_edge_endpoint.py tests/test_knowledge_graph_repository.py` all pass.
- Container builds (create_container() succeeds); ruff + mypy + lint-imports clean on touched Python files.
- `npx tsc --noEmit` clean in packages/db.
</verification>

<success_criteria>
- A human promote action flips one INFERRED/AMBIGUOUS edge to EXTRACTED (SC3).
- Promotion records {promoted_at, from_tier, mechanism} on the promotion column, distinct from synthesis provenance (SC4).
- Every rejection (not-found/inactive/already-EXTRACTED/cross-tenant) is fail-closed before any write; nothing auto-promotes.
</success_criteria>

<output>
Create `.planning/phases/30-suggest-only-promotion-gate/30-02-SUMMARY.md` when done.
</output>
