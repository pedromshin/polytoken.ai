---
phase: 46-kickoff-hygiene-v1-8-brand-design-dossier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-EVIDENCE.md
  - apps/email-listener/scripts/genui_eval/reports/
autonomous: true
requirements: [HYGN-01]

must_haves:
  truths:
    - "The genui eval harness was executed against the v1.2 corpus via live Bedrock, and its raw report numbers are recorded verbatim — OR the exact blocker (env/creds/service) is recorded as the evidence"
    - "The code-island isolation proof outcome is recorded per engine — the Playwright run is recorded as blocked-by-concurrency-lock (no root package.json mutation permitted), and the deterministic vitest AST-allowlist proof is run and recorded as the locally-feasible substitute"
    - "Each DEF item (DEF-17-05-01, DEF-18-03-01, DEF-19-01, DEF-20-01) has an explicit pass / fail / blocked disposition with the command that produced it"
    - "No AWS credentials, secrets, or env-var values appear in the evidence document"
  artifacts:
    - path: ".planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-EVIDENCE.md"
      provides: "Honest connected-env verification evidence: exact commands, raw outputs, per-DEF pass/fail/blocked"
      min_lines: 60
      contains: "DEF-20-01"
  key_links:
    - from: "46-EVIDENCE.md"
      to: "apps/email-listener/scripts/genui_eval/run_eval.py"
      via: "recorded command invocation + report path"
      pattern: "scripts.genui_eval.run_eval"
    - from: "46-EVIDENCE.md"
      to: "apps/web/e2e/code-island-isolation.spec.ts"
      via: "recorded blocker + substitute vitest proof"
      pattern: "validate-island-code"
---

<objective>
Produce honest, connected-env verification evidence for the locally-feasible subset of backlog 999.3, so the substrate is validated before v1.8 re-skins it (HYGN-01).

Two verification strands, both recorded in a single phase artifact `46-EVIDENCE.md`:
1. Run the Phase-16 genui eval harness against the v1.2 corpus via live Bedrock (existing AWS IAM transport — there is NO `ANTHROPIC_API_KEY`; do not look for or add one), recording raw numbers.
2. Establish the code-island isolation disposition: the Playwright browser spec cannot be run without installing `@playwright/test` (which mutates root `package.json`/`package-lock.json` — forbidden while Phase 43 owns that surface in a parallel track), so record that as an honest blocker and run the already-green deterministic host-side AST-allowlist vitest proof as the locally-feasible substitute.

Purpose: Blocked ≠ passed. This plan records what actually ran with exact commands and raw outputs, and records blockers as evidence rather than faking results.
Output: `.planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-EVIDENCE.md` plus any eval report files the harness writes under `apps/email-listener/scripts/genui_eval/reports/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-CONTEXT.md

# Eval-harness runbook (source of the exact commands + report contract):
@.planning/milestones/v1.2-phases/16-studio-foundation-eval-harness-history-page-ideas-tabs/16-05-SUMMARY.md

# The isolation spec + its cross-browser config (what "both engines" means):
@apps/web/e2e/code-island-isolation.spec.ts
@apps/web/playwright.config.ts
</context>

<constraints>
- Concurrency (Phase 43 parallel track): do NOT install any npm dependency, do NOT touch root `package.json` / `package-lock.json`, do NOT run `npm i`/`npm install`. Never `git add -A` — stage explicit paths only.
- LLM transport is AWS Bedrock via IAM role. There is NO `ANTHROPIC_API_KEY`. Do not add or search for one.
- Local-dev gotcha: zombie `uvicorn`/`python` processes can answer silently and mask failures. If any strand needs a running service, first kill stale `uvicorn`/`python` processes, then verify state via the DB/report file, NOT via terminal streaming output.
- Redact secrets: never paste AWS creds, tokens, or env-var values into `46-EVIDENCE.md`. Record model IDs, registry version, and numeric aggregates only.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Run the genui eval harness vs baseline on the v1.2 corpus (live Bedrock) and record raw evidence</name>
  <read_first>
    - .planning/milestones/v1.2-phases/16-studio-foundation-eval-harness-history-page-ideas-tabs/16-05-SUMMARY.md (exact runner commands, flags, and the report-contents contract)
    - apps/email-listener/scripts/genui_eval/run_eval.py (the runner: CLI flags, what "baseline" label writes, whether a service/DB is required)
    - apps/email-listener/scripts/genui_eval/compare_reports.py (the vs-baseline comparison entrypoint: it has an argparse main() taking two report paths)
    - apps/email-listener/scripts/genui_eval/report.py (report schema: registry_version, judge model id, weights/thresholds, per-prompt rows, aggregates)
  </read_first>
  <action>
    From `apps/email-listener`, run the eval harness with the live Bedrock judge exactly as documented in 16-05-SUMMARY.md: `uv run python -m scripts.genui_eval.run_eval --label baseline`. If a full live run is rate-limited or slow, ALSO capture a bounded run (`--limit 5`) and record which one produced the recorded numbers. The harness writes `<date>-baseline.json` + `<date>-baseline.md` under `apps/email-listener/scripts/genui_eval/reports/`. If two labelled reports are available (e.g. a deterministic `--no-judge` rubric pass and the live-judge pass), run `uv run python -m scripts.genui_eval.compare_reports <baseline.json> <candidate.json>` to produce the vs-baseline delta table.

    In `46-EVIDENCE.md`, create a section `## HYGN-01 — Eval harness (999.3)` recording, verbatim: the exact command(s) run, the resolved report file path(s), the `registry_version`, the judge model id, the weights/thresholds, and the run aggregates (and the compare delta table if produced). Assign explicit pass / fail / blocked dispositions to DEF-17-05-01, DEF-18-03-01, and DEF-19-01 based on the recorded numbers.

    If the run is blocked (Bedrock IAM not reachable from this environment, missing corpus data, service dependency, or any error), record the blocker AS the evidence: the exact command, the exact error text (secrets redacted), and mark the affected DEF items `blocked` (never `pass`). Do not fabricate numbers. Before deciding "blocked", kill any stale `uvicorn`/`python` processes and retry once.
  </action>
  <verify>
    <automated>test -f .planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-EVIDENCE.md && grep -q "DEF-17-05-01" .planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-EVIDENCE.md && grep -Eq "registry_version|blocked" .planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `46-EVIDENCE.md` contains a `## HYGN-01 — Eval harness (999.3)` section with the literal command string `scripts.genui_eval.run_eval` and the resolved report file path(s), OR an explicit blocker record with the exact error text (secrets redacted).
    - DEF-17-05-01, DEF-18-03-01, DEF-19-01 each carry an explicit `pass` / `fail` / `blocked` disposition line tied to the command that produced it.
    - If a run succeeded: the recorded numbers include `registry_version`, the judge model id, and at least the run aggregates copied from the generated report (not paraphrased).
    - No AWS credential, secret, or env-var value string appears anywhere in `46-EVIDENCE.md` (grep for `AWS_`, `SECRET`, `KEY=` returns nothing).
  </acceptance_criteria>
  <done>The eval-harness evidence section exists with either verbatim raw numbers + per-DEF pass/fail or an honest blocker record; no secrets leaked.</done>
</task>

<task type="auto">
  <name>Task 2: Record the code-island isolation disposition (Playwright blocked-by-lock + deterministic vitest substitute)</name>
  <read_first>
    - apps/web/e2e/code-island-isolation.spec.ts (what the spec asserts; its header notes the AST-allowlist vitest suite is the primary deterministic proof and this spec is the runtime backstop)
    - apps/web/playwright.config.ts (the two configured engines: chromium + firefox — this is what "both engines" means)
    - packages/genui/src/sandbox/validate-island-code.test.ts (the deterministic host-side AST-allowlist proof — 24 cases — runnable under vitest with no new dependency)
  </read_first>
  <action>
    First confirm the Playwright toolchain is genuinely unavailable without a dependency install: check for `@playwright/test` under `node_modules/@playwright` and `apps/web/node_modules/@playwright`. It is absent and is NOT listed in `apps/web/package.json`. Installing it (`npm i -D @playwright/test`) would mutate root `package.json`/`package-lock.json`, which is forbidden while Phase 43 owns that surface. Therefore DO NOT install it. In `46-EVIDENCE.md` add a section `## HYGN-01 — Code-island isolation (999.3, DEF-20-01)` recording: the check command and its output (absent), the exact reason the Playwright run is blocked (concurrency lock on root `package.json`/`package-lock.json` during the Phase-43 parallel track), and the two engines that the spec would have exercised (chromium, firefox). Mark DEF-20-01 `blocked (browser toolchain uninstallable under the Phase-46 concurrency constraint)`.

    Then run the locally-feasible deterministic substitute: from `packages/genui`, run the AST-allowlist vitest suite (`npx vitest run src/sandbox/validate-island-code.test.ts`). Record the exact command and the pass/fail count verbatim. Note in the evidence that this host-side allowlist is the primary deterministic isolation proof (per the spec header) and the Playwright spec is only the runtime backstop, so the isolation guarantee is substantively evidenced even with the runtime spec blocked.
  </action>
  <verify>
    <automated>grep -q "DEF-20-01" .planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-EVIDENCE.md && grep -q "validate-island-code" .planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-EVIDENCE.md && grep -Eiq "chromium|firefox" .planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `46-EVIDENCE.md` has a `## HYGN-01 — Code-island isolation (999.3, DEF-20-01)` section naming BOTH engines (chromium, firefox) and stating the Playwright run is `blocked` with the concurrency-lock reason (root `package.json`/`package-lock.json` must not be mutated).
    - The section records the deterministic substitute command `npx vitest run src/sandbox/validate-island-code.test.ts` and its verbatim pass/fail count.
    - DEF-20-01 carries a `blocked` (not `pass`) disposition; no npm install command was run and root `package.json`/`package-lock.json` are unchanged (`git status --porcelain package.json package-lock.json` is empty).
  </acceptance_criteria>
  <done>DEF-20-01 disposition recorded honestly as blocked-by-lock, with the deterministic vitest AST-allowlist result captured as the locally-feasible substitute; zero dependency installs performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval harness → AWS Bedrock (IAM) | Live model calls; AWS credentials resolve from the IAM role/env — untrusted output returns as JSON specs + judge scores |
| evidence author → 46-EVIDENCE.md | Command outputs (which may embed environment strings) get copied into a committed doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01-01 | Information Disclosure | 46-EVIDENCE.md | mitigate | Redact all secrets: record only model IDs, registry_version, and numeric aggregates; acceptance criterion greps for `AWS_`/`SECRET`/`KEY=` and requires zero matches |
| T-46-01-02 | Tampering | eval report files | accept | Reports are generated artifacts under a gitkeep'd dir; no external input path; recorded verbatim, not hand-edited |
| T-46-01-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing; Playwright install is explicitly forbidden and recorded as a blocker instead — no supply-chain surface introduced |
</threat_model>

<verification>
- `46-EVIDENCE.md` exists with both HYGN-01 sections (eval harness + code-island isolation).
- All four DEF items (DEF-17-05-01, DEF-18-03-01, DEF-19-01, DEF-20-01) have explicit pass/fail/blocked dispositions.
- `git status --porcelain package.json package-lock.json` is empty (no dependency mutation).
- No secret strings in the evidence doc.
</verification>

<success_criteria>
The locally-feasible 999.3 verifications are executed (or honestly blocked) with recorded evidence: exact commands, raw outputs/numbers, and per-DEF dispositions, with zero fabricated results and zero forbidden dependency installs.
</success_criteria>

<output>
Create `.planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-01-SUMMARY.md` when done.
</output>
