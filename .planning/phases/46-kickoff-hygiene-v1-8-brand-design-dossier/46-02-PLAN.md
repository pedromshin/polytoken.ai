---
phase: 46-kickoff-hygiene-v1-8-brand-design-dossier
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/email-listener/tests/test_genui_retrieval_provider.py
  - packages/genui/src/catalog/manifest.ts
  - packages/genui/src/__tests__/render-node.test.tsx
  - .planning/todos/pending/2026-07-08-genui-retrieval-provider-py313-asyncio.md
  - .planning/todos/done/2026-07-08-genui-retrieval-provider-py313-asyncio.md
autonomous: true
requirements: [HYGN-02]

must_haves:
  truths:
    - "The 10 formerly-failing tests in test_genui_retrieval_provider.py pass under Python 3.13 using asyncio.run() idioms (no asyncio.get_event_loop().run_until_complete() calls remain)"
    - "The production LexicalRetrievalProvider does not use the deprecated get_event_loop() call (verified, remains unchanged)"
    - "A grid can express an asymmetric main+sidebar layout: cols=12 with an 8-span child and a 4-span child renders a 12-track grid (the Phase-17 child-count clamp no longer collapses it)"
    - "Existing grid specs with no colSpan child still render equal columns with the Phase-17 clamp intact (backward-compatible)"
    - "The genui generator guidance (grid manifest description) documents colSpan for asymmetric layouts and no longer states 'there is NO column spanning'"
    - "The pending asyncio todo is moved to .planning/todos/done/ with a resolution note appended"
  artifacts:
    - path: "apps/email-listener/tests/test_genui_retrieval_provider.py"
      provides: "Python-3.13-correct async tests using asyncio.run()"
      contains: "asyncio.run"
    - path: "packages/genui/src/catalog/manifest.ts"
      provides: "colSpan-aware grid clamp + corrected generator guidance"
      contains: "colSpan"
    - path: "packages/genui/src/__tests__/render-node.test.tsx"
      provides: "Tests: asymmetric colSpan grid + preserved no-colSpan clamp"
  key_links:
    - from: "packages/genui/src/renderer/render-node.tsx"
      to: "packages/genui/src/catalog/manifest.ts GridComponent"
      via: "wrapper div style.gridColumn read by the colSpan-aware clamp"
      pattern: "gridColumn"
---

<objective>
Land the two backlog-999.2 debt folds (HYGN-02), each additive and each verified by targeted tests.

1. pytest event-loop cleanup: migrate the 10 tests in `test_genui_retrieval_provider.py` off the Python-3.13-removed `asyncio.get_event_loop().run_until_complete()` semantics to `asyncio.run()`, verify the production provider does not share the deprecated call, and resolve the pending todo per the repo's `done/` convention.

2. Grid `colSpan` — finish the half that remains. The colSpan MECHANISM already shipped in Phase 18 (schema `colSpan` field on every node + interpreter wrapper applying `style.gridColumn: span N`, commit 2f95147). The remaining gap is real and two-part: (a) the Phase-17 layout clamp `Math.min(requestedCols, childCount)` collapses a `cols:12` grid with 2 spanning children down to 2 tracks, making main+sidebar (e.g. 8/4) layouts impossible; and (b) the grid manifest `description` actively tells the model "there is NO column spanning," so the model never emits colSpan. Make the clamp colSpan-aware and correct the guidance — both additive, existing specs unchanged.

Purpose: Clean the Python 3.13 test debt and unlock asymmetric genui grid layouts the substrate already half-supports.
Output: Green targeted test runs on both the Python and TS sides; corrected generator guidance; resolved todo.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-CONTEXT.md

# Existing done-todo — the exact resolution convention to mirror (dir is done/, NOT resolved/):
@.planning/todos/done/2026-07-06-salvage-truncated-tool-calls.md

<interfaces>
<!-- The colSpan mechanism is ALREADY in place. Executor must extend, not re-invent. -->

packages/genui/src/schema/spec-schema.ts — every node schema already carries:
  colSpan: z.number().int().min(1).max(12).optional()   (additive; leave as-is)

packages/genui/src/renderer/render-node.tsx — renderPositionalChildren already wraps any
child that declares a numeric colSpan in a div with style { gridColumn: `span N` } (N clamped
1..12 via Math.max(1, Math.min(12, Math.floor(rawColSpan)))). It strips colSpan before
propsSchema.safeParse. This wrapper is the ONLY place spans are emitted — leave it as-is.

packages/genui/src/catalog/manifest.ts — GridComponent currently computes:
  childCount = React.Children.count(children)
  requestedCols = Number.isFinite(cols) ? Math.floor(cols) : 2
  effectiveCols = Math.max(1, Math.min(requestedCols, childCount || 1))
  style: { gridTemplateColumns: `repeat(${effectiveCols}, minmax(0, 1fr))` }
The GridComponent receives children as ALREADY-RENDERED React nodes; span-declaring children
are the wrapper divs above, each carrying props.style.gridColumn === "span N".
</interfaces>
</context>

<constraints>
- Concurrency (Phase 43 parallel track): NO new npm/pip dependencies, do NOT touch root `package.json`/`package-lock.json`, do NOT modify `.claude/skills/nauta-design-system/SKILL.md`. Stage explicit paths only (never `git add -A`).
- colSpan work MUST be additive: any spec valid before this plan stays valid and renders identically when it declares no colSpan on any grid child (Phase-17 clamp behavior preserved, commit 75ca1b4).
- Run ONLY the targeted test files named below — do NOT run the full suite (npm workspaces; Python via uv in apps/email-listener).
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the 10 async tests to asyncio.run() and resolve the pending todo</name>
  <read_first>
    - apps/email-listener/tests/test_genui_retrieval_provider.py (the 10 call sites use `asyncio.get_event_loop().run_until_complete(provider.retrieve(...))` — lines ~194, 203, 213, 223, 245-249, 260, 271, 285, 295, 307)
    - apps/email-listener/app/infrastructure/llm/genui_retrieval_provider.py (production provider — confirm it contains no get_event_loop()/run_until_complete() call; grep already shows none, re-verify)
    - .planning/todos/pending/2026-07-08-genui-retrieval-provider-py313-asyncio.md (the todo to resolve; note resolves_phase: 46)
    - .planning/todos/done/2026-07-06-salvage-truncated-tool-calls.md (the resolution convention: frontmatter + Problem/Solution; file lives under done/)
  </read_first>
  <behavior>
    - All 10 async retrieval tests execute the coroutine and assert the same results as before (RetrievalResult shape, descending-score ordering, top_k cap, determinism, empty/garbage-intent non-crash, style_pack_id acceptance, protocol conformance).
    - No `asyncio.get_event_loop()` or `.run_until_complete(` token remains in the test file.
    - The suite passes under Python 3.13 with no DeprecationWarning/RuntimeError about the current event loop.
  </behavior>
  <action>
    Replace every `asyncio.get_event_loop().run_until_complete(provider.retrieve(...))` call in `test_genui_retrieval_provider.py` with `asyncio.run(provider.retrieve(...))`. Each of these tests currently wraps a single awaitable, so `asyncio.run()` is the direct, correct replacement (it creates and tears down a fresh loop per call — the Python-3.13-supported idiom). Keep the existing per-test assertions byte-for-byte otherwise; do not restructure into `@pytest.mark.asyncio` fixtures unless a call awaits more than one coroutine (none here do — prefer the minimal `asyncio.run()` swap). Leave the `import asyncio` line in place.

    Confirm the production provider `apps/email-listener/app/infrastructure/llm/genui_retrieval_provider.py` does NOT itself call `get_event_loop()`/`run_until_complete()` (it does not — record this in the SUMMARY); make no change to it.

    Run ONLY this file: from `apps/email-listener`, `uv run pytest tests/test_genui_retrieval_provider.py -v --no-cov`. All previously-failing tests must pass.

    Resolve the todo per the repo convention: move `.planning/todos/pending/2026-07-08-genui-retrieval-provider-py313-asyncio.md` to `.planning/todos/done/2026-07-08-genui-retrieval-provider-py313-asyncio.md` (the dir is `done/`, confirmed by the existing done todo — NOT `resolved/` as the phase brief loosely worded it). Append a short `## Resolution` section noting the `asyncio.run()` migration, that the production provider was clean, and the passing test command.
  </action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/test_genui_retrieval_provider.py -q --no-cov 2>&1 | tail -5; grep -c "run_until_complete\|get_event_loop" tests/test_genui_retrieval_provider.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "get_event_loop\|run_until_complete" apps/email-listener/tests/test_genui_retrieval_provider.py` returns `0`.
    - `grep -c "asyncio.run(" apps/email-listener/tests/test_genui_retrieval_provider.py` returns >= 10.
    - `uv run pytest tests/test_genui_retrieval_provider.py -q --no-cov` reports all tests passed (0 failed).
    - `apps/email-listener/app/infrastructure/llm/genui_retrieval_provider.py` is unchanged (`git status --porcelain` shows no modification to it).
    - The todo file exists at `.planning/todos/done/2026-07-08-genui-retrieval-provider-py313-asyncio.md` (and no longer under `pending/`) with an appended `## Resolution` section.
  </acceptance_criteria>
  <done>All 10 async tests pass under Python 3.13 via asyncio.run(); production provider confirmed clean and untouched; todo moved to done/ with a resolution note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the grid clamp colSpan-aware and correct the generator guidance</name>
  <read_first>
    - packages/genui/src/catalog/manifest.ts (GridComponent at ~line 438-460 with the effectiveCols clamp; the `grid` manifest entry description + example at ~line 932-950)
    - packages/genui/src/renderer/render-node.tsx (renderPositionalChildren ~line 187-209 — the wrapper divs carry style.gridColumn === "span N"; this is what GridComponent must detect)
    - packages/genui/src/__tests__/render-node.test.tsx (existing grid tests: "clamps cols to child count" ~line 908, "keeps requested cols when children meet or exceed them" ~line 924, and Block 9 colSpan wrapper tests ~line 1094 — add new cases here, keep existing ones green)
    - packages/genui/src/schema/spec-schema.ts (confirm colSpan is already on every node — no schema edit needed)
  </read_first>
  <behavior>
    - Grid cols=12 with children [ text colSpan=8, text colSpan=4 ] renders `gridTemplateColumns: repeat(12, minmax(0, 1fr))` (NOT repeat(2, ...)) and the rendered HTML contains both `span 8` and `span 4` → asymmetric main+sidebar layout works.
    - Grid cols=12 with 2 plain children (no colSpan on either) still renders `repeat(2, ...)` — the Phase-17 child-count clamp is preserved when no child declares a span (backward compatible).
    - Grid cols=3 with 3 plain children still renders `repeat(3, ...)` (existing "keeps requested cols" test stays green).
    - Grid cols=12 with 1 plain child still collapses to `repeat(1, ...)` (existing clamp test stays green).
    - colSpan is never emitted as a DOM attribute (still consumed as the wrapper style only).
  </behavior>
  <action>
    In `GridComponent` (manifest.ts), make the clamp colSpan-aware. Detect whether any rendered child declares an explicit span by walking `React.Children.toArray(children)` and testing each element's `props?.style?.gridColumn` for a string beginning with `span ` (these are the wrapper divs emitted by renderPositionalChildren). Introduce a boolean `hasExplicitSpan`. Compute `effectiveCols` as: when `hasExplicitSpan` is true, respect the model's requested columns — `Math.max(1, Math.min(12, requestedCols))` (do NOT clamp down to childCount, so an 8/4 split gets its 12 tracks); when `hasExplicitSpan` is false, keep the existing Phase-17 gallery clamp — `Math.max(1, Math.min(requestedCols, childCount || 1))`. Keep `requestedCols = Number.isFinite(cols) ? Math.floor(cols) : 2` and the `repeat(${effectiveCols}, minmax(0, 1fr))` template unchanged otherwise. Type the child-walk defensively (children are `React.ReactNode`; guard with `React.isValidElement`), returning new values only — no mutation.

    In the `grid` manifest entry `description`, replace the now-false guidance. Remove the phrase "there is NO column spanning" and the "Do NOT use grid ... to hold a single wide region" clause. New description must state: grid renders equal-width columns by default (set `cols` to items-per-row, 2-4 typical for galleries; `cols` above the child count collapses to fewer wider columns when no child spans); AND that a grid CHILD may set `colSpan` (integer 1-12) to span multiple columns, enabling asymmetric layouts such as a main+sidebar split (e.g. `cols: 12` with an 8-span main child and a 4-span sidebar child). Keep the aria-label landmark note. Do not change the grid `propsSchema` (colSpan is a child-level spec field consumed by the interpreter, not a grid prop) or the `example` object shape.

    Add tests in `render-node.test.tsx` (Block 9 area) covering the five behaviors above: the asymmetric 8/4 case asserting `repeat(12,` present and `repeat(2,` absent plus `span 8` and `span 4` present; the no-colSpan cols=12 + 2-children case asserting `repeat(2,` (clamp preserved). Run ONLY this file.
  </action>
  <verify>
    <automated>cd packages/genui && npx vitest run src/__tests__/render-node.test.tsx 2>&1 | tail -8; grep -c "there is NO column spanning" src/catalog/manifest.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/render-node.test.tsx` passes with 0 failures, including the new asymmetric-layout and preserved-clamp cases.
    - A test asserts a `cols:12` grid with children `colSpan:8` + `colSpan:4` produces `repeat(12,` in the HTML and does NOT produce `repeat(2,`, and contains both `span 8` and `span 4`.
    - A test asserts a `cols:12` grid with two plain (no-colSpan) children still produces `repeat(2,` (clamp preserved).
    - `grep -c "there is NO column spanning" packages/genui/src/catalog/manifest.ts` returns `0`.
    - The grid `description` in manifest.ts contains the substring `colSpan` and mentions an asymmetric/main+sidebar layout.
    - The grid `propsSchema` and `example` object in the manifest entry are unchanged (diff touches only the `description` and `GridComponent` body).
  </acceptance_criteria>
  <done>Grid produces true asymmetric layouts when children declare colSpan while preserving the Phase-17 clamp for plain galleries; generator guidance corrected; targeted vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-generated spec → renderer | Untrusted `colSpan` integers flow from a model-authored spec into a CSS `grid-column` style string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-01 | Tampering | colSpan → style.gridColumn | mitigate | colSpan stays a bounded integer: schema `.int().min(1).max(12)` + interpreter `Math.max(1, Math.min(12, Math.floor(rawColSpan)))`; the new GridComponent clamp only reads `props.style.gridColumn` (never eval), so no string injection into layout |
| T-46-02-02 | Denial of Service | grid track count | accept | `effectiveCols` is bounded to [1,12] in both branches; MAX_SPEC_NODES/DEPTH refinements still cap tree size — no unbounded expansion |
| T-46-02-SC | Tampering | npm/pip installs | mitigate | Zero new dependencies; only test-file + existing-module edits; nothing installed |
</threat_model>

<verification>
- Python: `uv run pytest tests/test_genui_retrieval_provider.py -q --no-cov` → all pass; zero `get_event_loop`/`run_until_complete` tokens remain.
- TS: `npx vitest run src/__tests__/render-node.test.tsx` → all pass, including new asymmetric + preserved-clamp cases.
- `git status --porcelain package.json package-lock.json` empty; production provider unchanged; SKILL.md untouched.
- Todo moved to `.planning/todos/done/` with a resolution note.
</verification>

<success_criteria>
Both 999.2 folds land with targeted tests: Python-3.13 event-loop debt cleared, grid colSpan produces real asymmetric layouts without breaking existing equal-column specs, generator guidance corrected, and the todo resolved per convention.
</success_criteria>

<output>
Create `.planning/phases/46-kickoff-hygiene-v1-8-brand-design-dossier/46-02-SUMMARY.md` when done.
</output>
