{
  "name": "@polytoken/worker",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "Co-located graphile-worker runtime (Track 3a) — drains durable ingest/research jobs and re-enters the Python listener over localhost.",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js",
    "install-schema": "node dist/install-schema.js"
  },
  "dependencies": {
    "graphile-worker": "^0.17.0",
    "js-yaml": "^4.3.1",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/pg": "^8",
    "typescript": "^5.8.0",
    "vitest": "^2.1.9"
  }
}
