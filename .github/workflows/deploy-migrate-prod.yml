name: Migrate prod DB

# Applies pending Drizzle migrations to the PRODUCTION Supabase database.
# Manual-dispatch only, guarded by a typed confirmation, so it never fires by
# accident. The DB connection happens on the GitHub runner (which can reach
# Supabase over the public internet) using a repo secret — the credential lives
# in GitHub, never in a local checkout.
#
# ORDER: run this BEFORE merging to `main` (the app/listener deploys expect the
# new tables). Run it on the feature branch, confirm success, then merge.
#
# ROLLBACK: migrations are additive + forward-only. If a run goes wrong, restore
# the Supabase backup / PITR point you took immediately before dispatching.
#
# Required secrets — scoped to the `production` GitHub Environment (DEPLOY.md
# §5/§6: prod DB URLs live under `production`, not at repo level). The job
# declares `environment: production` below so these resolve; without it they
# come through EMPTY and migrate.ts throws "POSTGRES_URL_NON_POOLING is not
# defined" (the mode this workflow's first run failed in, 2026-07-23):
#   PROD_POSTGRES_URL_NON_POOLING  — the session/direct (port 5432) connection string
#   PROD_POSTGRES_URL              — same DB (pooled is fine; only NON_POOLING is used)
#   PROD_SUPABASE_URL              — https://<project-ref>.supabase.co

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type MIGRATE-PROD to proceed'
        required: true
        default: ''
      # Optional at-dispatch overrides for the prod connection. When the
      # `production` Environment secrets are not (yet) populated, the values can
      # be supplied here instead; when left blank they fall back to the secrets.
      # These are consumed only by this manual workflow and never persisted.
      db_non_pooling:
        description: 'override: session (5432) connection string'
        required: false
        default: ''
      db_pooling:
        description: 'override: pooled (6543) connection string'
        required: false
        default: ''
      supabase_url:
        description: 'override: https://<ref>.supabase.co'
        required: false
        default: ''

permissions:
  contents: read

jobs:
  migrate:
    runs-on: ubuntu-latest
    # Bind to the `production` Environment so the PROD_* secrets scoped there are
    # readable (and any required-reviewer gate on it applies). Without this the
    # secrets resolve empty and the run fails before touching the DB.
    environment: production
    steps:
      - name: Guard — require typed confirmation
        if: ${{ github.event.inputs.confirm != 'MIGRATE-PROD' }}
        run: |
          echo "::error::Refusing to run: 'confirm' must be exactly MIGRATE-PROD (got '${{ github.event.inputs.confirm }}')."
          exit 1

      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install workspace deps
        run: npm ci

      - name: Apply migrations to prod
        working-directory: packages/db
        env:
          # createEnv (client.ts) reads runtimeEnv: process.env, so setting the
          # vars here is sufficient — no .env.production file required in CI.
          # Dispatch overrides take precedence when supplied; otherwise the
          # production-scoped secrets are used (normal path).
          POSTGRES_URL_NON_POOLING: ${{ github.event.inputs.db_non_pooling || secrets.PROD_POSTGRES_URL_NON_POOLING }}
          POSTGRES_URL: ${{ github.event.inputs.db_pooling || secrets.PROD_POSTGRES_URL }}
          SUPABASE_URL: ${{ github.event.inputs.supabase_url || secrets.PROD_SUPABASE_URL }}
        run: npx tsx src/migrate.ts
